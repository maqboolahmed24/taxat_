# Patch Resolution Index

This index records the concrete defects resolved by the attached patch set. Each item states the
root cause, the likely systemic impact, and the patched location.

1. **Duplicate business partitions allowed** — root cause: `RunManifest.business_partitions` lacked canonical-set enforcement; impact: duplicate partition keys could perturb deterministic manifest hashing and replay. Patched in `schemas/run_manifest.schema.json`.
2. **Duplicate income-source partitions allowed** — root cause: `income_source_partitions` admitted repeated scope identifiers; impact: duplicate lineage and unstable downstream partition joins. Patched in `schemas/run_manifest.schema.json`.
3. **Duplicate non-deterministic module allowlist entries** — root cause: allowlist set semantics were not encoded; impact: hash drift and misleading audit output. Patched in `schemas/run_manifest.schema.json`.
4. **Duplicate gate decision records allowed at manifest level** — root cause: `gating_decisions` lacked `uniqueItems`; impact: repeated gate entries could inflate review counts and distort replay. Patched in `schemas/run_manifest.schema.json`.
5. **Duplicate config test-suite refs allowed in embedded config freeze entries** — root cause: config references were modeled as arrays without uniqueness; impact: repeated validation evidence and unstable freeze hashes. Patched in `schemas/run_manifest.schema.json` and `schemas/config_freeze.schema.json`.
6. **Duplicate environment allowlist entries allowed in config freezes** — root cause: environment lists were not canonicalized; impact: misleading compatibility posture. Patched in `schemas/run_manifest.schema.json` and `schemas/config_freeze.schema.json`.
7. **Duplicate config-freeze entries allowed** — root cause: embedded `entries[]` did not encode set semantics; impact: duplicated frozen configs could create ambiguous effective policy. Patched in `schemas/run_manifest.schema.json` and `schemas/config_freeze.schema.json`.
8. **Root manifests could serialize non-null inheritance mode** — root cause: `continuationSet` required a string for child lineage but never forced `null` for roots; impact: root manifests could masquerade as inherited branches. Patched in `schemas/run_manifest.schema.json`.
9. **Collection boundaries allowed duplicate source-boundary objects** — root cause: `source_boundaries[]` lacked uniqueness; impact: duplicate source windows could overstate coverage. Patched in `schemas/collection_boundary.schema.json`.
10. **Collection boundary request audit refs could repeat** — root cause: audit arrays lacked canonical-set constraints; impact: duplicate evidence trails and bloated audit surfaces. Patched in `schemas/collection_boundary.schema.json`.
11. **Collection boundary page-level audit refs could repeat** — root cause: no uniqueness on page audit refs; impact: duplicate request pages could be misread as separate fetches. Patched in `schemas/collection_boundary.schema.json`.
12. **Source plans allowed duplicate planned-source objects** — root cause: `planned_sources[]` lacked uniqueness; impact: duplicated collection work and unstable plan hashes. Patched in `schemas/source_plan.schema.json`.
13. **Planned-source partition scopes could repeat** — root cause: per-source partition scope was modeled as a plain list; impact: duplicate reads against the same slice. Patched in `schemas/source_plan.schema.json`.
14. **Required schema refs could repeat in source plans** — root cause: repeated schema requirements were not prevented; impact: noisy validation and misleading dependency inventories. Patched in `schemas/source_plan.schema.json`.
15. **Required source-class refs could repeat in source plans** — root cause: missing uniqueness; impact: duplicate coupling metadata. Patched in `schemas/source_plan.schema.json`.
16. **Normalization transformation version sets could repeat entries** — root cause: transformation versions were not treated as a set; impact: unstable normalization hashes and false version cardinality. Patched in `schemas/normalization_context.schema.json`.
17. **Schema bundles allowed duplicate bundle entries** — root cause: `schema_bundle.entries[]` lacked `uniqueItems`; impact: ambiguous bundle resolution and contract hashing drift. Patched in `schemas/schema_bundle.schema.json`.
18. **Allowed upgrade kinds could repeat in schema bundle entries** — root cause: upgrade policy arrays were not canonicalized; impact: misleading compatibility declarations. Patched in `schemas/schema_bundle.schema.json`.
19. **Allowed upgrade kinds could repeat in artifact contracts** — root cause: same issue at artifact-contract level; impact: inconsistent reader/writer compatibility projection. Patched in `schemas/schema_bundle.schema.json`.
20. **Snapshots required a timestamp that could still be null** — root cause: `created_at` was both required and nullable; impact: broken ordering and incomplete audit lineage. Patched in `schemas/snapshot.schema.json`.
21. **Green trust band did not force READY trust level** — root cause: trust schema only constrained `ALLOWED`, not `GREEN` itself; impact: contradictory green-but-blocked payloads could pass validation. Patched in `schemas/trust_summary.schema.json`.
22. **Amber trust band was under-constrained** — root cause: no direct mapping from `AMBER` to `REVIEW_REQUIRED`/`READY_REVIEW`; impact: amber payloads could serialize as ready-to-submit. Patched in `schemas/trust_summary.schema.json`.
23. **Red and insufficient-data trust bands were under-constrained** — root cause: no direct enforcement from those bands to blocked posture; impact: blocked states could validate as actionable. Patched in `schemas/trust_summary.schema.json`.
24. **READY_REVIEW filing posture admitted inconsistent trust combinations** — root cause: filing readiness lacked reverse constraints; impact: impossible state combinations during replay. Patched in `schemas/trust_summary.schema.json`.
25. **Decision bundle exposed an orphan dependency state** — root cause: `waiting_on = ASYNC_WORKER` existed without any outcome-class mapping or checkpoint contract; impact: persisted bundles could carry unreachable states. Patched in `schemas/decision_bundle.schema.json`.
26. **Decision bundle explanations were unbounded** — root cause: `plain_reason` had no maximum length; impact: low-noise replay surfaces could overflow budget and break queue summaries. Patched in `schemas/decision_bundle.schema.json`.
27. **Decision bundles allowed unbounded visible action lists** — root cause: `next_action_codes` had no size ceiling; impact: replay payloads could violate action-strip cognitive budgets. Patched in `schemas/decision_bundle.schema.json`.
28. **Decision bundles allowed unbounded blocked-action inventories** — root cause: `blocked_action_codes` had no size ceiling; impact: bloated terminal payloads and low-signal UX. Patched in `schemas/decision_bundle.schema.json`.
29. **Analysis-mode decision bundles could still serialize authority-owned review classes** — root cause: analysis mode only forbade `waiting_on = AUTHORITY`; impact: counterfactual runs could masquerade as live authority workflows. Patched in `schemas/decision_bundle.schema.json`.
30. **Review/pending decision classes did not require workflow refs** — root cause: workflow-bearing outcomes had no minimum linkage; impact: operator queues could receive opaque review states with no work item. Patched in `schemas/decision_bundle.schema.json`.
31. **Attention policy mirrored actionability incompletely** — root cause: nested `attentionPolicy` lacked the same `ACTION_AVAILABLE`/`NO_SAFE_ACTION` cross-field invariants as the outer envelope; impact: reconnect surfaces could disagree with top-level shell actionability. Patched in `schemas/experience_delta.schema.json`.
32. **Low-noise reconnect frames did not require focus anchors** — root cause: `experienceFrame` omitted `focus_anchor_ref` from required fields; impact: resumed drawers could lose object focus and restore incorrectly. Patched in `schemas/experience_delta.schema.json`.
33. **Low-noise reconnect frames did not require shell stability tokens** — root cause: `shell_stability_token` remained optional inside stored frames; impact: reconnect deduplication and stale-frame suppression weakened. Patched in `schemas/experience_delta.schema.json`.
34. **Experience-frame surface keys could disagree with inner surface codes** — root cause: generic `additionalProperties` accepted any `surfaceUpdate` under any valid key; impact: `CONTEXT_BAR` keys could carry `ACTION_STRIP` payloads and corrupt restoration logic. Patched in `schemas/experience_delta.schema.json`.
35. **Top-level low-noise deltas did not require focus anchors or shell stability tokens** — root cause: low-noise-specific root requirements omitted both continuity fields; impact: catch-up streams could not reliably preserve investigation context. Patched in `schemas/experience_delta.schema.json`.
36. **Drift lifecycle omitted the `EXPLANATION_ONLY` state used by materiality semantics** — root cause: the state machine and materiality taxonomy drifted apart; impact: workflow/UIs could not faithfully represent explanation-only drift. Patched in `state_machines.md` and `amendment_and_drift_semantics.md`.
37. **Drift lifecycle semantics conflated materiality with workflow escalation** — root cause: `REVIEW_REQUIRED` appeared alongside materiality labels without an explicit projection rule; impact: teams could treat workflow escalation as a sixth materiality class. Patched in `state_machines.md` and `amendment_and_drift_semantics.md`.
38. **Parity gate prose was precedence-ambiguous** — root cause: mixed `and`/`or` bullets lacked parenthesized grouping; impact: independent implementations could disagree on blocking vs. review posture. Patched in `exact_gate_logic_and_decision_tables.md`.
39. **Trust gate prose was precedence-ambiguous** — root cause: override and red-band clauses were not grouped explicitly; impact: blockers could be misclassified as review or notice states. Patched in `exact_gate_logic_and_decision_tables.md`.
40. **Amendment gate over-escalated PASS_WITH_NOTICE into manual review** — root cause: `validation_outcome in {MANUAL_REVIEW, PASS_WITH_NOTICE}` collapsed two distinct severities; impact: amendable cases could be unnecessarily stalled. Patched in `exact_gate_logic_and_decision_tables.md`.
41. **Snapshot summary in the data model was stale** — root cause: `data_model.md` still described a legacy `facts_ref` shape; impact: readers could implement against the wrong snapshot contract. Patched in `data_model.md`.
42. **Modernization review note was stale** — root cause: it still claimed `config_inheritance_mode` was absent from the schema and referenced an older allocator name mismatch as unresolved; impact: future reviewers could chase already-fixed issues instead of live gaps. Patched in `modernization_review.md`.
