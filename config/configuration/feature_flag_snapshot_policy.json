{
  "policyVersion": "FEATURE_FLAG_SNAPSHOT_POLICY_V1",
  "snapshotIdPrefix": "feature-flag-snapshot",
  "hashProfile": "FEATURE_FLAG_SNAPSHOT_V1",
  "noGovernedFlagSurfaceState": "NO_GOVERNED_FLAG_SURFACE",
  "requiredEvaluationContextKeys": [
    "tenant_id",
    "client_id_or_null",
    "environment_ref",
    "requested_scope",
    "executable_scope",
    "principal_context_ref_or_null",
    "access_binding_hash_or_null",
    "route_identity_ref_or_null"
  ],
  "reasonCodes": [
    "TARGETING_MATCH",
    "STATIC_DEFAULT",
    "NO_GOVERNED_FLAG_SURFACE"
  ],
  "notes": [
    "Resolved flag truth must stay provider-neutral and deterministic so replay and recovery never depend on live SDK state.",
    "When no governed feature-flag surface applies, the absence is explicit and hashes to null rather than being inferred from missing fields.",
    "Raw vendor payloads and credentials remain outside persisted snapshot truth."
  ]
}
