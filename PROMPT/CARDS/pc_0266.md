---
id: pc_0266
slug: phase_05_parallel_wave_03_track_frontend_portal_014_Playwright_or_other_appropriate_tooling_implement_portal_stale_view_rejection_refresh_and_graceful_rebase_experience
mode: parallel
phase: "05"
wave: "03"
track: "frontend_portal"
task_number: "014"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_portal_014
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0266

## Task Summary

014 Playwright or other appropriate tooling implement portal stale view rejection refresh and graceful rebase experience.

## Metadata

- ID: `pc_0266`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend portal`
- Task Number: `014`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_portal_014_Playwright_or_other_appropriate_tooling_implement_portal_stale_view_rejection_refresh_and_graceful_rebase_experience`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0265](./pc_0265.md) `phase_05_parallel_wave_03_track_frontend_portal_013_Playwright_or_other_appropriate_tooling_implement_portal_accessibility_keyboard_only_and_screen_reader_navigation`
- Next: [pc_0267](./pc_0267.md) `phase_05_parallel_wave_03_track_frontend_collaboration_001_Playwright_or_other_appropriate_tooling_implement_work_inbox_layout_filters_sorting_and_focus_stability`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_portal_014_Playwright_or_other_appropriate_tooling_implement_portal_stale_view_rejection_refresh_and_graceful_rebase_experience`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0265` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement portal stale-view rejection, refresh, reconnect, and graceful rebase experience across Home, Documents, Upload, Approvals, Onboarding, Help, and contextual request detail.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
Portal stale and degraded states must remain inline in the same mounted shell. The UI must distinguish freshening, reconnecting, catching up, stale review required, degraded read-only, receipt pending, and recovery required using workspace truth, durable receipts, and typed problem envelopes. Unsafe mutations are demoted until review/rebase is complete; drafts, upload selections, acknowledgement progress, and focus anchors survive only as lawful carry-forward context.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md` freshness/recovery, shell continuity, route_context, reliability summary, draft resume, stale/degraded rules, and Playwright validation minimum.
- `northbound_api_and_session_contract.md` problem response, command receipt, concurrency/stale-view rules, stream/reconnect rules, and session rules.
- `stream_resume_and_catch_up_ordering_contract.md` required rules and failure modes.
- `upload_session_request_binding_and_rebase_contract.md` and `upload_session_recovery_harness_contract.md` for upload-specific stale/rebase recovery.
- `schemas/client_portal_workspace.schema.json`, `problem_envelope.schema.json`, `api_command_receipt.schema.json`, `stream_recovery_contract.schema.json`, `upload_request_binding_contract.schema.json`.

Deliverables to create or patch:
1. `apps/client-portal-web/src/components/recovery/PortalInlineRecoveryNotice.tsx`
2. `apps/client-portal-web/src/components/recovery/PortalRebaseReviewCard.tsx`
3. `apps/client-portal-web/src/components/recovery/PortalReceiptPendingNotice.tsx`
4. `packages/frontend-shell-core/src/portal/portal_recovery_presenter.ts`
5. `packages/frontend-shell-core/src/portal/portal_stream_rebase_runtime.ts`
6. `packages/frontend-shell-core/src/portal/portal_problem_mapper.ts`
7. `tests/unit/frontend-shell-core/portal_recovery_presenter.spec.ts`
8. `tests/playwright/frontend/client_portal_stale_rebase_recovery.spec.ts`
9. `docs/frontend/client_portal_stale_view_rebase_experience.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Recovery UI is an inline freshness card, not a full-page error. It sits below the dominant status/task region and uses plain language: `We need to refresh this before you continue`, `We saved your upload choice, please confirm it against the latest request`, or `Your signature was sent; waiting for confirmation`.
- Use one promoted recovery region only. Show a mini before/after rebase comparison when it helps, but avoid dense diffs. Button labels are safe actions such as `Review latest`, `Refresh this page`, `Continue from saved upload`, or `Get help`.

Execution requirements:
- map workspace `freshness_state`, `settlement_state`, `recovery_posture`, `workspace_posture`, `reliability_summary`, command receipts, and problem envelopes to one coherent inline recovery surface
- pause unsafe mutation-capable CTAs during stale/degraded/rebase states and expose only safe review/refresh/reconnect/help actions
- preserve route, object, focus anchor, draft/upload/approval/onboarding carry-forward context where lawful; mark stale carry-forward read-only until confirmed
- enforce stream catch-up-before-live, duplicate idempotence, and exact route recovery binding
- handle `VIEW_STALE`, `REBASE_REQUIRED`, `ACCESS_REBIND_REQUIRED`, receipt pending/failure, and offline/reconnect states distinctly

Functional, architectural, and edge-case gaps to close:
- close the gap where stale-view errors appear as generic toast or full reload
- close the gap where stale drafts/uploads/sign-off acknowledgements remain mutably live
- close the gap where reconnect jumps to Home or loses contextual request focus
- close the gap where receipt-pending state is confused with completion

Mandatory edge cases:
- upload request rebases during transfer or before final attach
- approval pack rebases after acknowledgement but before signature
- onboarding requirements change while draft exists
- help request opened during degraded read-only state
- access/masking narrows during reconnect
- stream gap requires rebase and route object still exists vs disappears

Playwright, accessibility, and validation requirements:
- unit-test recovery mapping, unsafe-action demotion, carry-forward policy, problem mapping, and stream recovery transitions
- Playwright-test stale upload, stale approval sign-off, onboarding reconfirmation, contextual request reconnect, receipt pending/failure, access rebind, offline/reconnect catch-up, focus restoration, keyboard flow, and reduced motion
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- portal stale/reconnect/rebase states are inline, same-shell, and safe
- unsafe mutations fail closed while lawful carry-forward context is preserved
- Playwright proves recovery across portal flows and stream/problem scenarios
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
