---
id: pc_0106
slug: phase_03_parallel_wave_01_track_backend_manifest_010_implement_release_candidate_identity_contract_and_release_candidate_hashing
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "010"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0106

## Task Summary

implement release candidate identity contract and release candidate hashing.

## Metadata

- ID: `pc_0106`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `010`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_010_implement_release_candidate_identity_contract_and_release_candidate_hashing`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0105](./pc_0105.md) `phase_03_parallel_wave_01_track_backend_manifest_009_implement_build_preseal_gate_evaluation_and_seal_manifest_services`
- Next: [pc_0107](./pc_0107.md) `phase_03_parallel_wave_01_track_backend_manifest_011_implement_replay_attestation_and_deterministic_outcome_hash_persistence`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0102_to_0109.md](../shared_operating_contract_0102_to_0109.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_010_implement_release_candidate_identity_contract_and_release_candidate_hashing`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0105` if they exist, especially `pc_0017`, `pc_0027`, `pc_0028`, `pc_0030`, `pc_0031`, `pc_0059`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0080`, `pc_0084`, `pc_0100`, and `pc_0105`.

Mission:
Implement the release-candidate identity contract and its canonical hashing path so promotion, verification, replay comparability, and environment-specific evidence can anchor to one deterministic candidate tuple.
This task is not a release dashboard.
It is the machine-readable identity and hash substrate that later admissibility, verification, and deployment evidence layers will consume.

Source algorithm grounding you must honor:
- `release_candidate_identity_and_promotion_evidence_contract.md`, especially release-candidate tuple, promotion evidence, and environment-bound admissibility semantics.
- `verification_and_release_gates.md`, especially the relationship between candidate identity, verification artifacts, and admissibility.
- `manifest_and_config_freeze_contract.md`, especially config bundle and schema bundle identity interactions and canonical hash requirements.
- `data_model.md`, especially `ReleaseCandidateIdentityContract`, `SchemaBundleCompatibilityGateContract`, `VerificationSuiteResult`, `ReleaseVerificationManifest`, and `DeploymentRelease`.
- `deployment_and_resilience_contract.md`, especially environment / artifact promotion boundaries.
- Prior deterministic hashing utilities and canonical primitive semantics from `pc_0064` and `pc_0093`.
- Existing schemas including `release_candidate_identity_contract.schema.json`, `schema_bundle_compatibility_gate_contract.schema.json`, `gate_admissibility_record.schema.json`, and sample release-candidate JSON.

Deliverables to create:
1. `packages/backend-manifest/src/models/release_candidate_identity_contract.ts`.
2. `packages/backend-manifest/src/hash/release_candidate_identity_hash.ts`.
3. `packages/backend-manifest/src/services/build_release_candidate_identity_contract.ts`.
4. `packages/backend-manifest/src/services/normalize_candidate_identity_arrays.ts`.
5. `packages/backend-manifest/src/services/validate_candidate_identity_contract.ts`.
6. `packages/backend-manifest/src/services/release_candidate_hash_profile.ts`.
7. `packages/backend-manifest/src/fixtures/release_candidate_identity_vectors.ts`.
8. `docs/manifest/release_candidate_identity_hashing.md`.
9. `tests/unit/backend_manifest/release_candidate_identity_hash.spec.ts`.
10. `tests/integration/backend_manifest/release_candidate_identity_contract_roundtrip.spec.ts`.

Required design decisions you must make explicit:
- Define the canonical serialization and hashing strategy for `ReleaseCandidateIdentityContract`.
  Reuse prior canonical hash substrate rather than introducing a one-off hasher.
- Define exact ordering, uniqueness, and null-handling rules for `enabled_provider_profile_refs[]`, migration plan refs, supported client window refs, and any optional identity fields.
- Define how candidate identity contracts are nested or referenced by later verification and deployment artifacts without duplicating mutable truth.
- Define how schema bundle, config bundle, build artifact, environment, migration plan, and provider profile state together determine identity.
- Define how future schema versions or hash-profile changes will be versioned without silently changing existing candidate hashes.

Execution steps:
1. Implement typed contract wrappers for `ReleaseCandidateIdentityContract` and any directly supporting normalized field packets.
2. Implement `normalize_candidate_identity_arrays.ts` so ordered sets are canonical, duplicate-safe, and stable across repeated builds.
3. Implement `build_release_candidate_identity_contract.ts` so all required identity inputs are assembled from authoritative prior artifacts with source lineage retained.
4. Implement `release_candidate_identity_hash.ts` and `release_candidate_hash_profile.ts` using the existing canonical hash substrate.
5. Implement `validate_candidate_identity_contract.ts` so malformed or internally inconsistent candidate contracts fail closed.
6. Add fixtures for same-logical candidate / reordered arrays, changed config bundle, changed schema bundle, changed provider profile set, changed migration plan, and changed supported client window.
7. Add tests proving hash stability and mutation sensitivity.

Gap handling you must perform:
- Close the gap where release-candidate identity exists only as schema truth and prose but not as executable code.
- Close the gap where provider profile order or optional-null handling could create unstable hashes.
- Close the gap where later release evidence might duplicate or reinterpret candidate identity fields differently.
- Close the gap where release hashing could accidentally include persistence-only ids or environment-local write timestamps.
- Close the gap where future hash-profile evolution might silently change old identities.

Mandatory edge cases:
- logically identical provider profile sets in different order hash identically.
- duplicate provider profile refs are either canonicalized or rejected according to the contract; do not allow silent unstable duplication.
- null versus present `migration_plan_ref_or_null` changes the candidate identity hash when semantically different.
- null versus present `supported_client_window_ref_or_null` changes identity when semantically different.
- changed build artifact digest, schema bundle hash, config bundle hash, environment ref, or provider profile set must all change candidate identity hash.
- nested contract hash and top-level `candidate_identity_hash` must match or validation fails closed.
- serialization must remain stable even when optional arrays are empty or optional refs are absent.

Backend, performance, and security requirements:
- Candidate identity generation must be deterministic, pure, and side-effect free.
- Never mix mutable runtime environment secrets or transient timestamps into the candidate identity hash.
- Keep normalization reusable by later verification and promotion tasks.
- Ensure validation errors are log-safe and reveal structure problems without leaking sensitive configuration payloads.
- Keep the implementation provider-neutral and independent from deployment platform details.

Validation rules:
- Unit-test ordering stability, null handling, duplicate handling, and hash sensitivity across all material fields.
- Integration-test round-trip storage / reload of the contract and hash verification.
- Validate the contract against the imported schema and any sample vectors.
- Prove that persistence-only ids and write-time noise do not influence the candidate hash.
- Prove that any semantically material identity change alters the hash.

Definition of done:
- The repo has one canonical `ReleaseCandidateIdentityContract` builder and hasher.
- Later verification, admissibility, and deployment cards can rely on a stable candidate identity instead of recomputing environment/build tuples ad hoc.
- Release-candidate identity is now durable, deterministic, and explicitly validated.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
