---
id: pc_0188
slug: phase_04_parallel_wave_02_track_backend_portal_010_implement_artifact_affordance_contract_for_preview_export_print_and_browser_handoff
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "010"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0188

## Task Summary

implement artifact affordance contract for preview export print and browser handoff.

## Metadata

- ID: `pc_0188`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `010`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_010_implement_artifact_affordance_contract_for_preview_export_print_and_browser_handoff`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0187](./pc_0187.md) `phase_04_parallel_wave_02_track_backend_portal_009_implement_portal_language_contract_projection_and_banned_internal_vocabulary_filters`
- Next: [pc_0189](./pc_0189.md) `phase_04_parallel_wave_02_track_backend_governance_001_implement_tenant_governance_snapshot_projector_and_dominant_attention_summary`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0182_to_0189.md](../shared_operating_contract_0182_to_0189.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_010_implement_artifact_affordance_contract_for_preview_export_print_and_browser_handoff`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0187` if they exist, especially `pc_0179`, `pc_0180`, `pc_0183`, and `pc_0187`.

Mission:
Implement the shared artifact-affordance and externalization alignment layer that keeps same-shell preview, default download, default print, and external browser handoff all bound to the visible current-versus-history posture.
This task must stop current artifacts, historical artifacts, rejected uploads, superseded packs, receipts, and external handoff targets from drifting apart.
The read side must always know which artifact is visibly primary, which history is available, which preview mode is allowed, which download or print target is lawful right now, and when browser handoff is allowed versus blocked.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially `Artifact, print, and browser-handoff rules`, the document and approval route rules, and the Playwright validation minimum for current-vs-history targets, printable/downloadable declaration and receipt posture, and browser handoff return continuity.
- `frontend_shell_and_interaction_law.md`, especially section 7 `Artifact preview, export, print, and browser handoff`.
- `data_model.md`, especially the portal document-request and approval-pack sections plus `externalization_governance_contract`.
- `glossary.md`, especially the FE-57 `Externalization Governance Contract` and FE-69 `Artifact Affordance Contract` definitions.
- `schemas/artifact_selection_contract.schema.json`, `schemas/artifact_affordance_contract.schema.json`, `schemas/externalization_governance_contract.schema.json`, and any affected portal workspace/request/approval schemas.
- `scripts/validate_contracts.py`, especially the governed `artifact_affordance` validations and the route-specific target-alignment checks.
- `PATCH_RESOLUTION_INDEX.md`, especially the portal current-vs-history, export target, receipt separation, and browser-handoff hardening entries.

Deliverables to create:
1. `packages/backend-portal/src/services/derive_artifact_affordance_contract.ts`
2. `packages/backend-portal/src/services/derive_portal_externalization_governance_contract.ts`
3. `packages/backend-portal/src/services/validate_artifact_target_alignment.ts`
4. `packages/backend-portal/src/services/derive_artifact_focus_route_state.ts`
5. `packages/backend-portal/src/tests/artifact_affordance_contract_and_handoff.spec.ts`
6. `packages/backend-portal/src/tests/portal_artifact_selection_alignment.spec.ts`
7. `docs/portal/artifact_affordance_preview_export_print_and_browser_handoff.md`
8. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/artifact-affordance-lab.tsx`

You MUST patch existing portal document-request and approval projections so they consume these shared services rather than selecting preview/download/print targets from list order or view-state heuristics.

Required design decisions you must make explicitly:
- define the exact mapping from current-versus-history posture to `header_posture`, `primary_subject_role`, and `history_affordance_state`
- define where standalone artifact semantics end and workspace-embedded affordance semantics begin, especially for approval packs where standalone contracts differ from route-level declaration/receipt targets
- define when same-shell preview is allowed, when download-only posture is required, and when external browser handoff is lawful
- define how `externalization_governance_contract.delivery_binding_hash` and related binding fields stay locked to the same mounted slice the user is viewing
- define how `artifact_focus_bucket_or_null` and `artifact_focus_subject_ref_or_null` survive refresh, reconnect, responsive collapse, and browser handoff return

Execution requirements:
- derive schema-valid `ArtifactAffordanceContract` and `ExternalizationGovernanceContract` values wherever portal documents or approvals expose preview/export/print/handoff posture
- keep same-shell preview first whenever policy and format support it
- ensure the visible primary subject, header posture, and default preview/download/print targets always agree
- keep historical, superseded, rejected, expired, quarantined, or awaiting-replacement artifacts explicitly secondary
- keep declaration text, signed receipts, request uploads, and historical rows semantically distinct; they must never collapse into one generic artifact slot
- ensure identity- or authority-owned external checkpoints can hand off to the browser only with explicit explanation and same-route return continuity
- return from external handoff must not imply completion until the governed portal read model confirms settlement
- if you add the lab, make it a quiet two-layer explainer: one primary artifact sheet plus one secondary history strip, with a compact target-alignment matrix; no file-gallery dashboard

Gap handling you must close while implementing:
- close any gap where preview, download, print, and handoff targets can drift apart from the visible header posture
- close any gap where stale or historical rows can become the implicit default current artifact after refresh, reconnect, or narrow-screen restack
- close any gap where browser handoff or signed URLs can outlive or outscope the slice the user actually reviewed
- close any gap where current-vs-history meaning is encoded only in UI order instead of machine-readable contracts
- close any gap where route return / focus context is lost after external handoff

Mandatory edge cases:
- `history_affordance_state = NONE` must force `preview_open_policy = CURRENT_SUMMARY_FIRST_ONLY`
- any non-`NONE` history affordance must force `preview_open_policy = CURRENT_SUMMARY_FIRST_THEN_HISTORY_ON_DEMAND`
- `primary_subject_role = NO_CURRENT_ARTIFACT` must clear `visible_primary_subject_ref_or_null`
- historical or superseded artifacts must never become the default preview/download/print target for a current client task
- declaration and receipt targets must remain distinct in approval flows
- for portal document uploads, printable targets remain null unless an explicit printable artifact exists; do not invent print posture for binary uploads
- browser handoff must preserve route identity and focused artifact context on return
- externalization must remain blocked or approval-gated when the governing contract says so; never resolve a detached direct URL around the contract

Playwright and contract-test requirements:
- add direct tests for affordance target derivation, externalization alignment, header-posture mapping, and browser-handoff blocking or allowance rules
- if relevant northbound routes already exist, add Playwright APIRequestContext tests proving current-vs-history targets and externalization contracts are stable across route reads and stale-view recovery
- if you add the lab, add Playwright browser tests for same-shell preview, explicit history disclosure, default-target stability, and external handoff return continuity using semantic locators only
- capture traces or serialized affordance payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- max width `1240px`, background `#F6F4EE`, surface `#FFFFFF`, muted history rail `#F3F4F6`
- one primary artifact header with posture chip, one current target card, one secondary history rail or disclosure drawer
- action strip order should be `Preview`, `Download`, `Print`, `Open externally` only when lawful and only when those actions actually map to distinct targets
- the header must state `Current`, `Current with history`, `Historical`, `Superseded`, `Rejected`, `Expired`, `Quarantined`, or `Awaiting replacement` explicitly
- if a diagram helps, use one compact alignment matrix showing visible subject vs preview/download/print targets and one browser-handoff sequence strip
- animations remain subtle and reduced-motion-safe

Backend, performance, and security requirements:
- keep affordance and externalization truth server-side
- bind preview/export/print/handoff to the exact reviewed slice, current access binding, masking posture, limitation posture, and stale-guard basis
- do not let CDN URLs, browser cache, or stale local state widen or refresh artifact meaning outside the contract
- keep target derivation reusable by portal documents, approvals, and later collaboration/native surfaces without route-local forks

Validation rules:
- validate every emitted artifact-selection, artifact-affordance, and externalization contract against schema
- add regression coverage for history-secondary posture, declaration-vs-receipt separation, stale artifact demotion, and browser handoff return stability
- keep standalone artifact semantics and embedded route semantics explicit where the schema set requires them

Definition of done:
- the repo has one authoritative artifact-affordance and externalization alignment layer for portal surfaces
- current-vs-history meaning, preview/download/print targets, and browser handoff return posture are now machine-readable backend truth
- portal artifacts can no longer drift from current-safe primary posture into accidental historical defaults
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
