---
id: pc_0214
slug: phase_04_parallel_wave_02_track_backend_retention_security_observability_006_implement_audit_event_trace_span_metric_event_and_log_record_models
mode: parallel
phase: "04"
wave: "02"
track: "backend_retention_security_observability"
task_number: "006"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_retention_security_observability_006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0214

## Task Summary

006 implement audit event trace span metric event and log record models.

## Metadata

- ID: `pc_0214`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend retention security observability`
- Task Number: `006`
- Source Slug: `phase_04_parallel_wave_02_track_backend_retention_security_observability_006_implement_audit_event_trace_span_metric_event_and_log_record_models`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0213](./pc_0213.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_005_implement_authority_ingress_correlation_proof_and_reconciliation_control_contract_binding`
- Next: [pc_0215](./pc_0215.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_007_implement_observability_query_contracts_for_run_timelines_batch_timelines_and_privacy_ledgers`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0214_to_0221.md](../shared_operating_contract_0214_to_0221.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_retention_security_observability_006_implement_audit_event_trace_span_metric_event_and_log_record_models`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0213` if they exist, especially `pc_0134` through `pc_0141`, `pc_0178`, `pc_0194`, `pc_0206`, `pc_0207`, `pc_0212`, and `pc_0213`.

Mission:
Implement the canonical backend models, builders, and persistence helpers for `AuditEvent`, `TraceSpan`, `MetricEvent`, and `LogRecord`, together with one shared `TelemetryResource` / correlation-context layer.
This task is not allowed to create a second telemetry dialect, a log-only pseudo-audit stream, or renderer-owned correlation logic.
All four signal families must serialize from one authoritative contract layer and remain replay-safe, privacy-safe, and query-safe.

Source algorithm grounding you must honor:
- `observability_and_audit_contract.md`, end to end, especially signal families, mandatory correlation keys, audit-event contract, deterministic ordering rule, required event families, and the query-slice section.
- `retention_error_and_observability_contract.md`, especially the cross-domain linkage and minimization rules for retention/privacy-significant signals.
- `data_model.md`, especially `TelemetryResource`, `TraceSpan`, `MetricEvent`, `LogRecord`, and `AuditEvent`.
- `modules.md`, especially `EMIT_AUDIT_EVENT(...)` and any existing run-stage helpers that need to emit correlated telemetry.
- `audit_and_provenance.md` for append-only evidence posture.
- `nightly_autopilot_contract.md` where nightly batch and digest correlation keys are mandated.
- `northbound_api_and_session_contract.md` if you patch or expose any signal-inspection route.
- `schemas/audit_event.schema.json`, `schemas/trace_span.schema.json`, `schemas/metric_event.schema.json`, `schemas/log_record.schema.json`, plus any related sample payloads.
- `scripts/validate_contracts.py`, especially `validate_observability_correlation_context`, `validate_audit_event`, `validate_trace_span`, `validate_metric_event`, `validate_log_record`, and the drift checks named `audit_event_identity_and_correlation_mirror`, `audit_event_post_expiry_requires_reason_and_lineage_minimum`, `audit_event_nightly_selection_disposition_alignment`, `audit_event_remediation_requires_task_correlation`, `audit_event_compensation_requires_compensation_correlation`, `trace_span_root_context_and_chronology`, `trace_span_manifest_lineage_trace_ref_required`, `trace_span_manifest_start_claim_posture`, `trace_span_config_resolution_reuse_collision`, `metric_event_nightly_selection_correlation`, and `log_record_context_mirror_and_replay_posture`.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially any vectors around branch-decision drift, start-claim posture, replay drift, nightly correlation, payload expiry, and secret-bearing logging.

Deliverables to create:
1. `packages/backend-observability/src/models/observability_correlation_context.ts`
2. `packages/backend-observability/src/models/telemetry_resource.ts`
3. `packages/backend-observability/src/models/audit_event.ts`
4. `packages/backend-observability/src/models/trace_span.ts`
5. `packages/backend-observability/src/models/metric_event.ts`
6. `packages/backend-observability/src/models/log_record.ts`
7. `packages/backend-observability/src/services/build_observability_correlation_context.ts`
8. `packages/backend-observability/src/services/emit_audit_event.ts`
9. `packages/backend-observability/src/services/record_trace_span.ts`
10. `packages/backend-observability/src/services/record_metric_event.ts`
11. `packages/backend-observability/src/services/record_log_record.ts`
12. `packages/backend-observability/src/tests/observability_correlation_context.spec.ts`
13. `packages/backend-observability/src/tests/audit_event_model.spec.ts`
14. `packages/backend-observability/src/tests/trace_metric_log_models.spec.ts`
15. `docs/observability/audit_trace_metric_log_models_and_correlation.md`
16. Optional but recommended: `apps/admin-console-web/src/routes/debug/observability/signal-correlation-lab.tsx`

You MUST patch any existing code path that still:
- builds audit payloads ad hoc from route/controller code,
- emits logs without canonical correlation context,
- uses log messages as compliance proof,
- or serializes top-level identity fields that drift from nested `correlation_context{...}`.

Required design decisions you must make explicitly:
- define one authoritative `ObservabilityCorrelationContext` builder and mirror policy for top-level fields versus nested correlation fields
- define the exact allowed `span_code`, `metric_family`, `log_family`, `access_tier`, `sampling_class`, and `retention_class` usage boundaries your implementation will enforce at runtime
- define the post-expiry `retained_context{ payload_availability_state, audit_sufficiency_state, payload_expiry_at_or_null, lineage_refs[], limitation_reason_codes[] }` handling policy for audit events
- define how nightly keys (`nightly_batch_run_ref`, `nightly_window_key`, `selection_disposition`) and replay keys (`replay_class`, expected/actual basis hashes) are preserved without polluting unrelated signals
- define how `TelemetryResource` binds service/environment/build/deployment identity so logs, metrics, and spans share one resource tuple
- define a strict log-field minimization policy so `message_template` plus `structured_fields{...}` remain secret-safe and privacy-safe

Execution requirements:
- implement one canonical correlation-context builder used by audit, traces, metrics, and logs
- preserve as many mandatory correlation keys as are applicable, including manifest lineage, nightly lineage, replay lineage, request identity, access binding, policy snapshot, authority refs, workflow refs, and error/remediation refs
- keep `AuditEvent` append-only and hash/integrity oriented; do not collapse it into `LogRecord`
- require every `AuditEvent` to carry at least one of `actor_ref` or `service_ref`
- enforce canonical audit ordering through `audit_stream_ref + stream_sequence`; never let raw `event_time` become total-order truth
- ensure `correlation_context.tenant_id`, `client_id`, and `manifest_id` mirror top-level identity when repeated on `AuditEvent`
- ensure run-root nightly spans carry both `nightly_batch_run_ref` and `nightly_window_key`
- ensure replay run-root spans carry `replay_class`
- ensure config-resolution and manifest-start-claim spans preserve the exact lineage/branch packets the validator expects
- ensure `MetricEvent` supports nightly selection and digest-related families without inventing a second bucket taxonomy
- ensure `LogRecord` is structured, correlation-safe, and never a raw dump of tokens, secrets, or sensitive fields that policy would otherwise mask

Gap handling you must close while implementing:
- close any gap where audit, trace, metric, and log emitters each build their own partial correlation maps
- close any gap where audit payload expiry causes post-expiry reconstruction to lose typed reason codes, lineage refs, or sufficiency posture
- close any gap where logs or traces are treated as proof of legal action instead of supporting signals
- close any gap where nightly or replay keys can drift across signals because they are built from route-local context instead of canonical frozen artifacts
- close any gap where remediation or compensation audit families fail to retain the exact task or compensation correlation the validator requires
- close any gap where structured logging can still leak raw request/response payloads, authority secrets, or personal data that the retention/privacy model would not allow

Mandatory edge cases:
- `retained_context.payload_availability_state = FULL` must keep `audit_sufficiency_state = SUFFICIENT`, null expiry, and no limitation codes
- `HASH_ONLY`, `TOMBSTONED`, and `ERASED` audit postures must keep `audit_sufficiency_state = LIMITED`, non-null expiry, non-empty `reason_codes[]`, non-empty `object_refs[]`, non-empty lineage refs, and non-empty limitation reason codes
- `OutOfBandStateObserved` must preserve submission-lineage correlation
- `CONFIG_RESOLVE_OR_INHERITANCE_DECISION` spans must preserve branch-decision correlation and must not masquerade as bundle-return reuse
- `MANIFEST_START_CLAIM` spans must preserve start-claim correlation and must not emit for bundle-return paths that never claimed execution
- nightly metric events must mirror nightly selection/batch keys when the event family is nightly-specific
- log records for replay or recovery must preserve replay-safe / recovery-safe lineage keys rather than degrade into generic service logs

Playwright and contract-test requirements:
- add model and builder tests that prove top-level fields mirror nested correlation context correctly
- add contract tests for audit post-expiry posture, nightly/replay span requirements, and remediation/compensation linkage preservation
- if you expose or patch any read/debug route, add Playwright `APIRequestContext` coverage for serialized audit, trace, metric, and log payloads and for typed validation failures
- if you add the optional lab, add Playwright browser tests that verify semantic-locator navigation, focus order, reduced-motion-safe expansion, signal-family tabs, and correlation-key sidecar rendering using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- use a three-band layout: signal-family ribbon, ordered signal ledger, narrow correlation-key sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; info accent `#1D4ED8`; continuity accent `#0F766E`; caution `#B7791F`; danger `#C2410C`
- show audit ordering and payload-availability posture before raw payload or metric detail
- use ladders, tapes, and field ledgers rather than dashboard cards or charts
- animate only opacity / translate / height; respect reduced motion

Backend, performance, and security requirements:
- keep audit persistence append-only and transactionally separate from best-effort telemetry batching
- make telemetry builders idempotent where retries are possible and prohibit duplicated audit records under safe retry
- avoid high-cardinality free-text dimensions in metrics; keep dimensions typed and bounded
- keep message templates structured and parameterized rather than concatenated secret-bearing strings
- never use logs or traces to backfill missing audit truth
- ensure canonical serialization before hashing or integrity checks

Validation rules:
- validate every `AuditEvent`, `TraceSpan`, `MetricEvent`, and `LogRecord` payload against schema before persistence or publication
- update fixtures/builders so all relevant validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one authoritative backend model/builder layer for audit, trace, metric, and log artifacts
- correlation, nightly lineage, replay lineage, and post-expiry audit-sufficiency posture are durable and test-covered
- optional labs, if added, are read-only reflections of persisted truth rather than alternate implementations
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
