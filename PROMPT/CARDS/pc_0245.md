---
id: pc_0245
slug: phase_05_parallel_wave_03_track_frontend_low_noise_005_Playwright_or_other_appropriate_tooling_implement_detail_drawer_state_support_surface_and_non_modal_rules
mode: parallel
phase: "05"
wave: "03"
track: "frontend_low_noise"
task_number: "005"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_low_noise_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0245

## Task Summary

005 Playwright or other appropriate tooling implement detail drawer state support surface and non modal rules.

## Metadata

- ID: `pc_0245`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend low noise`
- Task Number: `005`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_low_noise_005_Playwright_or_other_appropriate_tooling_implement_detail_drawer_state_support_surface_and_non_modal_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0244](./pc_0244.md) `phase_05_parallel_wave_03_track_frontend_low_noise_004_Playwright_or_other_appropriate_tooling_implement_action_strip_state_rendering_and_mutation_legality_controls`
- Next: [pc_0246](./pc_0246.md) `phase_05_parallel_wave_03_track_frontend_low_noise_006_Playwright_or_other_appropriate_tooling_implement_dominant_question_dominant_action_and_support_region_promotion_behaviors`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_low_noise_005_Playwright_or_other_appropriate_tooling_implement_detail_drawer_state_support_surface_and_non_modal_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0244` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement `DetailDrawerState` support surface, entry modules, and non-modal support-region rules for the low-noise calm shell.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The detail drawer is the only default promoted support region in `CALM_SHELL`. It may expose Evidence Prism, Packet Forge, Authority Handshake Tunnel, Drift Ripple Field, Audit Echo Panel, and Twin Lens, but every module must start with a plain-language interpretation summary and only one module can be expanded by default unless explicit compare/audit mode is entered.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md detail drawer, low-noise detail-drawer binding, edge-case handling, algorithm translation requirements`.
- `frontend_shell_and_interaction_law.md support-region promotion, topology, artifact/accessibility rules`.
- `UIUX_DESIGN_SKILL.md signature observatory modules and low-noise binding`.
- `schemas/detail_drawer_state.schema.json, low_noise_budget_audit.schema.json, semantic_accessibility_contract.schema.json`.

Deliverables to create or patch:
1. `packages/shared-ui/src/low-noise/DetailDrawer.tsx`
2. `packages/shared-ui/src/low-noise/DetailEntryPointList.tsx`
3. `packages/shared-ui/src/low-noise/modules/EvidencePrismModule.tsx`
4. `packages/shared-ui/src/low-noise/modules/PacketForgeModule.tsx`
5. `packages/shared-ui/src/low-noise/modules/AuthorityHandshakeModule.tsx`
6. `packages/shared-ui/src/low-noise/modules/DriftRippleModule.tsx`
7. `packages/shared-ui/src/low-noise/modules/AuditEchoModule.tsx`
8. `packages/shared-ui/src/low-noise/modules/TwinLensModule.tsx`
9. `packages/frontend-shell-core/src/low-noise/detail_drawer_presenter.ts`
10. `tests/playwright/frontend/low_noise_detail_drawer_support_surface.spec.ts`
11. `docs/frontend/detail_drawer_support_surface_and_non_modal_rules.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Drawer width 360–400px on desktop; inline/bottom sheet under compact layouts. Entry points are quiet stacked cards with module name, one-line purpose, posture chip, and count/availability if lawful. Module visuals are bespoke but restrained: Evidence Prism = concentric evidence layers; Packet Forge = staged seal row; Authority Tunnel = preflight lock sequence; Drift Ripple = baseline/ripple field; Audit Echo = hash-linked tape; Twin Lens = paired timelines. Each visual must be secondary to a plain-language summary.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- render entry points from `entry_points`, expanded module code, expanded content state, focus anchor, fallback reason, compare/audit flags, and full-text ref
- enforce maximum five default entry points and one expanded module by default
- make ordinary drawer non-modal; focus trap only for explicit high-risk checkpoint modes introduced by a later contract
- show distinct empty states: collapsed, populated, not requested, not yet materialized, limited, not applicable
- preserve focus and module state across refresh/reconnect/viewport redock when the same object remains mounted

Functional, architectural, and edge-case gaps to close:
- close the gap where investigation modules become first-view peers
- close the gap where drawer state is local and lost on reconnect/restack
- close the gap where graph-heavy modules render before interpretation summary
- close the gap where empty/limited drawer states are indistinguishable

Mandatory edge cases:
- invalid active module must fallback deterministically to suggested module, first valid entry, or collapsed root with reason
- compare and audit modes must be explicit and mutually controlled
- limited/retention-erased support must show lawful tombstone/limitation copy, not absence
- closing drawer returns focus to invoking control or lawful ancestor

Playwright, accessibility, and validation requirements:
- Playwright-test entry-point count, single expanded module, each module summary-first rendering, empty/limited states, focus return, compact redock, and reduced motion
- assert no default drawer state creates a second promoted support region
- unit-test fallback order and module label mapping `FOCUS_LENS -> Audit Echo Panel`
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- detail drawer is the only default promoted support region
- modules are summary-first, accessible, and budget-bound
- fallback, focus, responsive, compare/audit, and limited states are tested
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
