---
id: pc_0215
slug: phase_04_parallel_wave_02_track_backend_retention_security_observability_007_implement_observability_query_contracts_for_run_timelines_batch_timelines_and_privacy_ledgers
mode: parallel
phase: "04"
wave: "02"
track: "backend_retention_security_observability"
task_number: "007"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_retention_security_observability_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0215

## Task Summary

007 implement observability query contracts for run timelines batch timelines and privacy ledgers.

## Metadata

- ID: `pc_0215`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend retention security observability`
- Task Number: `007`
- Source Slug: `phase_04_parallel_wave_02_track_backend_retention_security_observability_007_implement_observability_query_contracts_for_run_timelines_batch_timelines_and_privacy_ledgers`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0214](./pc_0214.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_006_implement_audit_event_trace_span_metric_event_and_log_record_models`
- Next: [pc_0216](./pc_0216.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_008_implement_error_record_retention_error_and_observability_contract_bindings`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0214_to_0221.md](../shared_operating_contract_0214_to_0221.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_retention_security_observability_007_implement_observability_query_contracts_for_run_timelines_batch_timelines_and_privacy_ledgers`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0214` if they exist, especially `pc_0158` through `pc_0165`, `pc_0166` through `pc_0173`, `pc_0194`, `pc_0206`, `pc_0207`, `pc_0213`, and `pc_0214`.

Mission:
Implement the canonical backend query layer for deterministic observability slices: audit trails, run timelines, nightly batch timelines, filing-evidence ledgers, and privacy-action ledgers.
These queries must materialize as `AuditInvestigationFrame` slices or an equivalent backend-owned query envelope with the same frozen semantics.
This task is not allowed to let browser code infer ordering, neighborhood, export posture, or log-vs-audit proof status.

Source algorithm grounding you must honor:
- `observability_and_audit_contract.md`, especially the query-contract section for `get_audit_trail(...)`, `get_run_timeline(...)`, `get_nightly_batch_timeline(...)`, `get_filing_evidence_ledger(...)`, and `get_privacy_action_ledger(...)`.
- `data_model.md`, especially `AuditInvestigationFrame`.
- `modules.md`, especially `QUERY_OBSERVABILITY_SLICE(...)`.
- `northbound_api_and_session_contract.md`, especially governance audit-investigation read surfaces and route-stable filter / cursor requirements.
- `frontend_shell_and_interaction_law.md`, `low_noise_experience_contract.md`, and `semantic_selector_and_accessibility_contract.md` if you patch route-visible diagnostics.
- `schemas/audit_investigation_frame.schema.json`, `schemas/audit_event.schema.json`, `schemas/trace_span.schema.json`, and `schemas/log_record.schema.json`.
- `scripts/validate_contracts.py`, especially `validate_audit_investigation_frame`, `audit_investigation_frame_query_contract_alignment`, `audit_investigation_frame_query_drift`, `audit_investigation_frame_workspace_selection_and_export_alignment`, and `audit_investigation_frame_interaction_layer_compaction_alignment`.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially any vectors around clock skew, merged stream ordering, active-slice export, or privacy-action audit continuity.

Deliverables to create:
1. `packages/backend-observability/src/projectors/build_audit_investigation_frame.ts`
2. `packages/backend-observability/src/queries/get_audit_trail.ts`
3. `packages/backend-observability/src/queries/get_run_timeline.ts`
4. `packages/backend-observability/src/queries/get_nightly_batch_timeline.ts`
5. `packages/backend-observability/src/queries/get_filing_evidence_ledger.ts`
6. `packages/backend-observability/src/queries/get_privacy_action_ledger.ts`
7. `packages/backend-observability/src/tests/audit_investigation_frame.spec.ts`
8. `packages/backend-observability/src/tests/observability_query_contracts.spec.ts`
9. `docs/observability/run_batch_privacy_query_contracts.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/observability/audit-investigation-lab.tsx`

You MUST patch any existing observability, audit, or governance route that still:
- merges audit records solely by raw timestamps,
- infers export eligibility outside the persisted active slice,
- or treats supporting logs as proof of legal action.

Required design decisions you must make explicitly:
- define the closed `query_contract_code` vocabulary you will support now (`AUDIT_TRAIL`, `RUN_TIMELINE`, `NIGHTLY_BATCH_TIMELINE`, `FILING_EVIDENCE_LEDGER`, `PRIVACY_ACTION_LEDGER`)
- define the exact `query_anchor_ref` grammar for each query mode
- define the allowed `ordering_basis` values and where each one is mandatory
- define how `supporting_trace_span_refs[]` and `supporting_log_record_refs[]` are included without weakening audit-proof boundaries
- define route-stable `active_filters`, cursor rules, selected-event rules, selected-object rules, and export-binding rules so reload/rebase cannot remount a different slice silently
- define how object-neighborhood reconstruction and diff-inspector posture are computed from persisted refs rather than UI-local joins

Execution requirements:
- materialize every supported query as a deterministic backend-owned envelope
- keep `AUDIT_TRAIL` ordering canonical with `AUDIT_STREAM_SEQUENCE`
- keep merged timelines and ledger-style slices on `RECORDED_AT_THEN_STREAM_SEQUENCE`, preserving canonical audit order inside merged positions
- make `query_contract_code`, `query_anchor_ref`, and `ordering_basis` explicit on every emitted slice
- preserve `ordered_event_refs[]`, `supporting_trace_span_refs[]`, `supporting_log_record_refs[]`, `correlation_keys[]`, and `integrity_chain_posture`
- preserve route-stable `focus_anchor_ref`, `active_filters`, and one selected event / selected object where the frame schema expects them
- keep `export_posture` bound to the active filtered slice; no detached export panel is allowed
- ensure privacy ledgers retain masked-preview / approval-gated export posture and never expose logs as the proof of record

Gap handling you must close while implementing:
- close any gap where merged run or batch timelines are ordered by raw `event_time` instead of a persisted ordering basis
- close any gap where privacy ledgers or filing ledgers reconstruct action history from logs rather than audit events plus supporting refs
- close any gap where export eligibility is computed from whatever happens to be visible in the UI instead of the persisted filtered slice
- close any gap where neighborhood pivots, diff posture, or selection context are lost on reconnect or pagination
- close any gap where browser or native clients must infer whether logs are explanatory or evidentiary

Mandatory edge cases:
- cross-stream clock skew must not perturb canonical ordering
- a slice may include supporting logs and supporting traces, but the audit order remains the proof spine
- `supporting_log_record_refs[]` may be empty for a fully sufficient audit slice and MUST NOT become required proof
- a privacy-action ledger must preserve masking / export / hold / erasure actions with explicit ordering and active-slice export posture
- a nightly batch timeline must include selection, shard, per-client outcome, digest-publication, and successor-recovery linkage where those refs exist
- pagination and cursoring must not drop `query_contract_code`, `query_anchor_ref`, or `ordering_basis`

Playwright and contract-test requirements:
- add tests that prove every query mode emits the correct `query_contract_code`, `query_anchor_ref`, and `ordering_basis`
- add tests that prove audit trails and merged timelines stay deterministic under clock skew and duplicate timestamps
- if you expose or patch northbound routes, add Playwright `APIRequestContext` coverage for every query mode, cursor continuation, export posture, and masked-preview posture
- if you add the optional lab, add Playwright browser tests for semantic-locator navigation, filter chips, selected-event persistence, diff-inspector behavior, reduced-motion expansion, and export-panel binding using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- use a three-band layout: query-mode ribbon, ordered investigation tape, narrow export/neighborhood sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; info accent `#1D4ED8`; continuity accent `#0F766E`; caution `#B7791F`; danger `#C2410C`
- prefer tapes, ladders, and neighborhood rails over dashboard cards or charts
- keep one promoted support surface only; never split diff, export, and neighborhood into competing primaries
- animate only opacity / translate / height; respect reduced motion

Backend, performance, and security requirements:
- query slices must be reproducible from persisted refs, not ad hoc joins over unordered tables
- export posture must be evaluated server-side and bound to the exact slice that the user is inspecting
- supporting log refs must remain secondary and may be more restricted than audit refs
- preserve immutable object refs and stable cursors so browser and native clients can recover the same slice after reconnect
- ensure privacy-ledger queries do not widen visibility or unmask content simply because a staff route exists

Validation rules:
- validate every emitted `AuditInvestigationFrame` against schema before returning or persisting it
- update fixtures/builders so all relevant validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one canonical backend query layer for audit trails, run timelines, batch timelines, filing ledgers, and privacy ledgers
- ordering, selection, export posture, and supporting-signal boundaries are durable and test-covered
- optional labs, if added, are read-only reflections of persisted truth rather than alternate implementations
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
