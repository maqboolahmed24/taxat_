---
id: pc_0114
slug: phase_03_parallel_wave_01_track_backend_collection_006_implement_extract_candidate_facts_pipeline_with_schema_validated_outputs
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "006"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0114

## Task Summary

implement extract candidate facts pipeline with schema validated outputs.

## Metadata

- ID: `pc_0114`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `006`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_006_implement_extract_candidate_facts_pipeline_with_schema_validated_outputs`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0113](./pc_0113.md) `phase_03_parallel_wave_01_track_backend_collection_005_implement_freeze_normalization_context_declared_exclusions_and_missing_source_tracking`
- Next: [pc_0115](./pc_0115.md) `phase_03_parallel_wave_01_track_backend_collection_007_implement_conflict_detection_and_conflict_set_persistence`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0110_to_0117.md](../shared_operating_contract_0110_to_0117.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_006_implement_extract_candidate_facts_pipeline_with_schema_validated_outputs`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0113` if they exist, especially `pc_0010`, `pc_0011`, `pc_0017`, `pc_0028`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0074`, `pc_0077`, `pc_0080`, `pc_0109`, `pc_0112`, and `pc_0113`.

Mission:
Implement the candidate-fact extraction pipeline that converts retained source/evidence artifacts into schema-valid `CandidateFact` outputs with exact lineage, exact single-partition binding, deterministic identity, explicit execution-mode posture, and promotion-readiness inputs.
This task must not shortcut support or partition rules.
A candidate fact may be weak or contested, but it may never become support-free, scope-widened, or visibility-limited.

Source algorithm grounding you must honor:
- `modules.md`, especially `EXTRACT_CANDIDATE_FACTS(...)`.
- `core_engine.md`, especially candidate extraction, lineage-hash requirements, exact partition rule, and unmasked-authoritative visibility rule.
- `canonical_source_and_evidence_taxonomy.md`, especially fact families, source strength tiers, and adjustment-fact separation.
- `canonical_fact_promotion_and_partition_isolation_contract.md`, especially the requirements on candidate facts before promotion.
- `data_model.md`, especially `CandidateFact` and `CandidateFactSet`.
- Existing schemas including `candidate_fact.schema.json`, `candidate_fact_set.schema.json`, and any sample candidate artifacts.

Deliverables to create:
1. `packages/backend-collection/src/models/candidate_fact.ts`.
2. `packages/backend-collection/src/repositories/candidate_fact_repository.ts`.
3. `packages/backend-collection/src/services/extract_candidate_facts.ts`.
4. `packages/backend-collection/src/services/candidate_identity_hash.ts`.
5. `packages/backend-collection/src/services/source_record_lineage_hash.ts`.
6. `packages/backend-collection/src/services/evidence_lineage_hash.ts`.
7. `packages/backend-collection/src/services/fact_family_classifier.ts`.
8. `packages/backend-collection/src/services/build_adjustment_binding.ts`.
9. `packages/backend-collection/src/services/validate_candidate_partition_scope.ts`.
10. `packages/backend-collection/src/types/candidate_fact_draft.ts`.
11. `docs/collection/candidate_fact_extraction_pipeline.md`.
12. `tests/unit/backend_collection/candidate_fact_extraction.spec.ts`.
13. `tests/integration/backend_collection/candidate_fact_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the canonical preimage for `candidate_identity_hash` and `dedupe_key`.
- Define how fact-family classification works from source/evidence kinds and frozen normalization rules.
- Define how extraction behaves in `COMPLIANCE` versus `ANALYSIS` execution mode, including `analysis_only`, `counterfactual_basis`, and non-compliance config refs.
- Define how `promotion_readiness` is populated even though full conflict detection lands in the next roadmap task.
  You must close that sequencing gap without weakening the candidate schema.
- Define how adjustment facts bind `applicable_reporting_scopes[]`, `quarterly_basis_profile`, `time_window_basis`, and `partition_application`.

Execution steps:
1. Implement schema-backed `CandidateFact` model and repository with indexes on `manifest_id`, partition, fact family, and identity hash.
2. Implement `extract_candidate_facts.ts` so it consumes `SourceRecord` and `EvidenceItem` artifacts plus the frozen normalization context and produces deterministic candidate drafts.
3. Implement identity and lineage hash helpers so every candidate preserves exact source/evidence lineage.
4. Implement `fact_family_classifier.ts` and `build_adjustment_binding.ts` so adjustment facts remain distinct from record-layer facts and preserve exact partition application.
5. Implement `validate_candidate_partition_scope.ts` so extraction fails closed if the normalized result widens beyond one exact partition.
6. Resolve the conflict-readiness sequencing gap by designing an explicit seam for a bootstrap frontier or injected conflict context that still yields schema-valid candidate outputs without inventing fake final conflict posture.
7. Add tests for structured external facts, documentary extraction, adjustment facts, analysis-mode facts, duplicate logical facts, and partition-widening failures.

Gap handling you must perform:
- Close the gap where the candidate schema requires `promotion_readiness.conflict_set_ref` and related frontier fields before the conflict-detection task officially lands.
- Close the gap where fact-family classification, value-payload shaping, and adjustment binding are named semantically but not operationally specified.
- Close the gap where extraction could otherwise widen partition scope or lose exact source/evidence lineage hash fidelity.
- Close the gap where analysis-mode candidates could silently leak into compliance posture.

Mandatory edge cases:
- every candidate fact must preserve at least one `source_record_ref` and at least one `supporting_evidence_ref`.
- `partition_scope_refs[]` must contain exactly one item and equal the scalar `partition_scope`.
- `visibility_basis` must remain `UNMASKED_AUTHORITATIVE_ONLY`.
- compliance-mode candidates must have `analysis_only = false`, no non-compliance config refs, and null `counterfactual_basis`.
- analysis-mode candidates must have `analysis_only = true` and non-null `counterfactual_basis`.
- `ADJUSTMENT_FACT` must carry a non-null adjustment binding; non-adjustment facts must not.
- duplicate logical candidates in the same partition must collapse through deterministic dedupe rather than persist as semantically identical duplicates.
- extraction that depends on evidence from multiple partitions must fail closed and surface conflict posture later rather than widening scope.

Backend, performance, and security requirements:
- Extraction must be deterministic and replay-safe.
- Candidate persistence must separate draft computation from final schema-valid persistence where sequencing requires it.
- Never store masked/customer-safe projections as candidate truth.
- Keep value-payload refs and normalization decisions auditable and tied to frozen rule refs.

Validation rules:
- Unit-test fact-family classification, adjustment binding, analysis/compliance posture, and partition validation.
- Unit-test identity-hash stability and dedupe behavior.
- Integration-test candidate persistence against fixture conflict-frontier inputs so schema validity is preserved before the next card is finished.
- Validate persisted candidates against `candidate_fact.schema.json`.
- Prove that support-free, masked, or scope-widened candidates are impossible.

Definition of done:
- The repo can persist schema-valid `CandidateFact` artifacts with exact lineage and partition integrity.
- Later conflict detection and canonical promotion can operate on explicit candidate truth rather than reparsing evidence.
- Analysis-only posture is machine-enforced instead of informal.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
