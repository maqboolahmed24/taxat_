---
id: pc_0081
slug: phase_02_seq_081_create_generated_api_schema_and_contract_documentation_site
mode: sequential
phase: "02"
wave: null
track: null
task_number: "081"
block_key: phase_02_seq
original_key: phase_02_seq_081
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0081

## Task Summary

create generated api schema and contract documentation site.

## Metadata

- ID: `pc_0081`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `081`
- Source Slug: `phase_02_seq_081_create_generated_api_schema_and_contract_documentation_site`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0080](./pc_0080.md) `phase_02_seq_080_create_schema_drift_contract_diff_and_migration_readiness_automation`
- Next: [pc_0082](./pc_0082.md) `phase_02_seq_082_create_repository_task_runner_makefile_and_agent_entrypoint_scripts`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0078_to_0085.md](../shared_operating_contract_0078_to_0085.md)
```text
Execute roadmap task `phase_02_seq_081_create_generated_api_schema_and_contract_documentation_site`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0080` if they exist, especially `pc_0001`, `pc_0002`, `pc_0003`, `pc_0004`, `pc_0018`, `pc_0024`, `pc_0028`, `pc_0029`, `pc_0059`, `pc_0060`, `pc_0061`, `pc_0062`, `pc_0065`, `pc_0077`, `pc_0078`, `pc_0079`, and `pc_0080`.

Mission:
Generate the canonical Taxat contract observatory: one premium, search-first documentation site that derives from the authoritative markdown corpus, live schemas, examples, traceability, and generated bindings.
This is not a marketing docs site and not a generic swagger portal.
It is an internal/expert-facing contract atlas that lets engineers, reviewers, and autonomous agents move from prose to schema to examples to validators to dependent tasks without ambiguity.

Source algorithm grounding you must honor:
- `README.md`, especially the authoritative inventory, blueprint coverage map, and validation commands.
- `implementation_conventions.md`, especially support-doc cohesion and constraints that docs, schemas, examples, and traceability move together.
- `architecture_coherence_guardrails.md`, especially anti-drift requirements across artifacts.
- `contract_integrity_requirements.md`, especially token resolution, schema parity, `$id` integrity, and closed-schema expectations.
- `semantic_selector_and_accessibility_contract.md` and `UIUX_DESIGN_SKILL.md`, especially semantic selectors, premium low-noise design, and Playwright-first verification.
- Imported schemas, samples, and generated bindings from `pc_0060` and `pc_0061`.
- The drift/report foundations from `pc_0080`, because the docs site must surface compatibility and source lineage rather than static markdown only.

Deliverables to create:
1. `packages/contracts-docs/src/generate_contract_observatory.ts`.
2. `packages/contracts-docs/src/build_navigation_index.ts`.
3. `packages/contracts-docs/src/build_search_index.ts`.
4. `packages/contracts-docs/src/build_schema_crosslink_graph.ts`.
5. `packages/contracts-docs/src/build_binding_catalog.ts`.
6. `config/docs/contracts_site_manifest.json`.
7. `config/docs/contracts_navigation_schema.json`.
8. `apps/operator-web/src/routes/internal/contracts-observatory/index.tsx`.
9. `apps/operator-web/src/routes/internal/contracts-observatory/[artifact].tsx`.
10. `apps/operator-web/src/components/contracts-observatory/` with the required view components.
11. `docs/contracts/generated_contract_observatory.md`.
12. `tests/integration/docs/contracts_observatory_generation.spec.ts`.
13. `tests/playwright/internal/contracts_observatory.spec.ts`.

Required design decisions you must make explicit:
- Decide whether the observatory lives inside the existing operator shell or as a separate internal docs app behind the same auth/session boundary; prefer the existing shell unless prior architecture forbids it.
- Define the canonical navigation taxonomy: overview, runtime contracts, data model, schemas, samples, bindings, validators, drift/readiness, and support docs.
- Define how schema pages, prose headings, samples, bindings, and downstream prompt cards cross-link to each other.
- Define how the site distinguishes authoritative artifacts from generated companions and explanatory views.
- Define the search model so queries for entities, schema IDs, headings, fields, hashes, and task IDs all resolve predictably.
- Define how large JSON schemas and examples collapse, diff, and deep-link without overwhelming the reader.

Execution steps:
1. Build a generator that parses the authoritative markdown corpus, heading structure, schema inventory, examples, samples, and generated bindings into one typed observatory manifest.
2. Materialize a navigation index, search index, schema-crosslink graph, and binding catalog with stable source references.
3. Generate artifact pages that preserve source file, heading, schema `$id`, required fields, examples, linked validators, linked tasks, and drift/readiness posture where available.
4. Implement a premium internal docs shell that can render prose, structured schema summaries, sample payloads, cross-links, and relationship diagrams without turning into a noisy docs boilerplate site.
5. Expose clear “source of truth” signals on every page so readers can tell whether they are looking at authored prose, generated schema summary, sample payload, binding catalog, or compatibility output.
6. Add generation-time validation for broken links, missing schema tokens, orphan samples, heading collisions, and stale indexes.
7. Add Playwright coverage for search, deep links, anchor-copy, schema-path inspection, keyboard navigation, and reduced-motion behavior.

Visual and interaction specification for `contracts-observatory`:
- Tone: a premium legal-technical library.
  The experience should feel like a disciplined research instrument with product polish, not a generic docs template.
- Layout:
  - fixed `68px` top bar with compact brand sigil, command-search trigger, current artifact type, and source-truth badge.
  - left navigation column `280-304px` with collapsible sections, sticky current-heading marker, and recently visited artifacts.
  - center reading canvas `760-840px` wide for prose, schema summaries, examples, and diagrams.
  - right evidence rail `320-360px` with source metadata, linked schema IDs, required fields, validators, bindings, drift posture, and task links.
- Signature visual language:
  - use a custom “contract seal” glyph built from concentric rules and one split spine.
  - each artifact header gets a restrained inline SVG summary strip: e.g. prose chapter, schema lattice, sample token, or binding bundle.
  - relationship diagrams should use monoline SVG with quiet labels, never colorful node-soup.
- Palette:
  - background `#F7F5F1`
  - reading surface `#FFFFFF`
  - support surface `#EFEDE8`
  - ink `#101418`
  - muted ink `#667079`
  - hairline `rgba(16,20,24,0.08)`
  - accent navy `#465A77`
  - accent sage `#61705C`
  - accent brass `#8A6942`
  - link `#375D8A`
  - success `#17614B`
  - warning `#8B5D1B`
  - danger `#A23A33`
- Typography:
  - editorial, dense, and precise.
  - heading sizes should be generous but not oversized.
  - prose line length target `68-76ch`.
  - monospace is reserved for schema paths, IDs, refs, hashes, and code snippets.
- Dimensional constraints:
  - max app width `1560px`
  - reading column min width `720px`
  - evidence rail cards `56-72px` min height
  - command search panel max width `720px`
- Motion:
  - `120ms` for section expand/collapse.
  - `140ms` for evidence-rail swap and active-link underline.
  - no parallax, no ambient pulses, no bouncing search UI.
  - reduced-motion mode removes transitions and uses instant state changes.
- Interaction:
  - command search must find files, headings, schema IDs, entity names, and task IDs.
  - clicking a schema field should highlight linked prose references and sample payload fragments.
  - copy-link actions must produce stable deep links to the artifact and heading or field.
  - every page must display a “source truth stack” showing file, heading, schema, samples, bindings, and validators.
  - accessible names should read like content relationships, e.g. `principal context schema links to actor and authority model section 3.6`.
  - Playwright must verify search, deep links, heading anchors, focus order, and cross-link highlighting using role-first locators.

Component hierarchy hint:
~~~ts
<InternalShell route="/internal/contracts-observatory">
  <ObservatoryTopBar />
  <NavigationTree />
  <ArtifactCanvas />
  <EvidenceRail />
  <CommandSearchDialog />
</InternalShell>
~~~

Gap handling you must perform:
- Close the gap where the README inventories the corpus but no executable docs site keeps the inventory alive.
- Close the gap where prose, schemas, examples, samples, and generated bindings can drift without one inspectable synthesis surface.
- Close the gap where engineers must grep the repo to answer “what is authoritative for this concept?”
- Close the gap where support docs and traceability are easy to forget during schema changes.
- Close the gap where internal consumers need premium browseability without turning to a generic swagger generator that erases corpus nuance.

Mandatory edge cases:
- two headings with similar slugs in different files
- schema token referenced in prose but missing from the schema bundle
- sample payload present but no matching schema or vice versa
- generated binding catalog stale relative to schema hash
- very large schema requiring collapsed sections and deep linking to one field
- search query matching both prose headings and schema IDs
- reduced-motion and keyboard-only navigation through search and artifact pages
- broken relative source link introduced by moving a support document

Backend, performance, and security requirements:
- Site generation must be deterministic and cacheable.
- Search indexes and page payloads should be optimized for quick internal use but remain build-time generated where possible.
- No artifact page may expose secrets, local file-system paths that do not matter to consumers, or generated transient tokens.
- The site is read-only and must not mutate contract state.
- The generator must fail the build on unresolved authoritative references.

Validation rules:
- Unit-test navigation-index generation, search indexing, cross-link graph resolution, and binding catalog generation.
- Integration-test full-site generation from the imported corpus and detect broken authoritative references.
- Use Playwright to test command search, deep links, field-to-prose highlighting, keyboard navigation, heading hierarchy, copy anchors, and reduced-motion behavior.
- Prove that every rendered artifact exposes source lineage and that broken inputs fail closed rather than disappearing silently.

Definition of done:
- The repo has one generated contract observatory instead of scattered markdown, schemas, and samples with manual navigation.
- Readers can move from concept to schema to sample to binding to validator quickly and accurately.
- The observatory feels premium and purpose-built rather than like a generic AI-generated docs clone.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

