---
id: pc_0167
slug: phase_03_parallel_wave_01_track_backend_northbound_011_implement_problem_envelope_mapping_stale_view_guards_and_if_match_validation
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "011"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_011
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0167

## Task Summary

implement problem envelope mapping stale view guards and if match validation.

## Metadata

- ID: `pc_0167`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `011`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_011_implement_problem_envelope_mapping_stale_view_guards_and_if_match_validation`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0166](./pc_0166.md) `phase_03_parallel_wave_01_track_backend_northbound_010_implement_audit_trail_enquiry_pack_and_investigation_query_endpoints`
- Next: [pc_0168](./pc_0168.md) `phase_03_parallel_wave_01_track_backend_northbound_012_implement_access_rebind_resume_token_and_shell_stability_guard_failure_paths`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0166_to_0173.md](../shared_operating_contract_0166_to_0173.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_011_implement_problem_envelope_mapping_stale_view_guards_and_if_match_validation`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0166` if they exist, especially `pc_0094` through `pc_0101`, `pc_0134` through `pc_0141`, and `pc_0158` through `pc_0166`.

Mission:
Implement the canonical northbound failure-mapping layer for stale-view protection, grouped stale-guard echoes, and HTTP conditional-request validation.
The result must stop individual endpoints from inventing their own stale-write semantics and instead centralize `ProblemEnvelope` construction, stale guard family selection, authoritative `ETag` / `If-Match` comparison, and route-safe recovery hints.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially section 5 (failure and recovery surfaces), section 6 (concurrency and stale-view rules), and the grouped stale-view receipt rules.
- `data_model.md`, especially `ProblemEnvelope`, grouped truth-boundary and stale-view objects, and any route-stability/publication contracts used by manifest, portal, collaboration, or governance routes.
- `frontend_shell_and_interaction_law.md`, especially route-stability primitives and grouped `stability_contract` rules.
- `collaboration_workspace_contract.md`, especially stale-view, view-guard, and `409 VIEW_STALE` behavior.
- `stream_resume_and_catch_up_ordering_contract.md` where grouped recovery semantics must stay coherent with HTTP failures.
- `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially the recovery-family hardening items and any stale-view mirror constraints.

Deliverables to create:
1. `packages/backend-northbound/src/http/problem_envelope_middleware.ts`
2. `packages/backend-northbound/src/http/if_match_precondition_guard.ts`
3. `packages/backend-northbound/src/services/build_problem_envelope.ts`
4. `packages/backend-northbound/src/services/build_stale_view_problem_envelope.ts`
5. `packages/backend-northbound/src/services/build_precondition_failed_problem_envelope.ts`
6. `packages/backend-northbound/src/services/derive_authoritative_etag.ts`
7. `packages/backend-northbound/src/services/validate_if_match_against_authoritative_guard.ts`
8. `packages/backend-northbound/src/services/restrict_problem_recovery_family.ts`
9. `packages/backend-northbound/src/services/select_portal_safe_detail_surface.ts`
10. `packages/backend-northbound/src/tests/problem_envelope_and_if_match.spec.ts`
11. `docs/northbound/problem_envelope_and_precondition_rules.md`
12. Optional but useful: `apps/admin-console-web/src/routes/debug/problem-envelope-lab.tsx`

Required design decisions you must make explicitly:
- define the exact mapping between domain stale-view failures and HTTP transport outcomes: when a mismatch is a domain-level `409 VIEW_STALE`, when it is a conditional transport `412`, and when it is really access drift that belongs to `ACCESS_REBIND_REQUIRED`
- define one authoritative derivation path for `ETag` values from bundle/workspace/policy/approval/request basis contracts rather than route-local hashes or serializer output
- define how grouped stale markers, latest projection refs, and `latest_stability_contract_or_null` are attached to problem envelopes without mixing generations
- define the narrowest lawful `suggested_detail_surface_code` per audience; portal-facing failures must remain portal-safe

Execution requirements:
- build one reusable failure-mapper that endpoint handlers call instead of hand-assembling problem payloads
- compare `If-Match` only against authoritative grouped guards, not weak renderer cache keys or transport-local hashes
- preserve the exact stale guard family that the command or read actually targeted; do not rewrite a request-scoped stale conflict into a manifest-wide stale family
- route wrappers must remain thin; mapping logic belongs in one service layer with deterministic tests
- if an endpoint already emits `ETag`, retrofit it to the shared derivation helper rather than leaving multiple competing formats in the codebase

Gap handling you must close while implementing:
- the algorithm defines rich stale-view envelopes but does not give teams a ready-made middleware boundary; create one canonical mapper so later endpoints inherit it instead of diverging
- earlier route work may already emit partial `ETag` headers; reconcile them into one authoritative derivation helper now
- the source corpus forbids mixing recovery families but leaves room for accidental overpopulation of refs; enforce a strict family-pruning pass before publication
- the customer-safe surface list is narrower than staff tooling naturally wants; clamp `suggested_detail_surface_code` by audience and shell family

Mandatory edge cases:
- only one non-manifest recovery family may appear in one `ProblemEnvelope`
- stale-view failures must echo the exact `stale_guard_family`, the current authoritative `latest_stale_guard_value`, and the latest rebasing ref(s)
- `latest_stability_contract_or_null` must stay coherent with the echoed stale guard, not come from a different publication generation
- portal-facing failures may only suggest `CUSTOMER_ACTIVITY`, `FILES`, or null when customer-safe guidance is needed
- hidden or unauthorized objects must not leak richer existence info through recovery refs
- `If-Match` mismatch must never execute the mutation and then apologize afterwards

Playwright and contract-test requirements:
- use Playwright APIRequestContext for transport-level tests
- cover successful conditional requests, stale-view conflicts, pure precondition failures, portal-safe failures, collaboration stale guards, and governance snapshot mismatches
- if you add a lab page, test it with semantic locators and explicit payload rendering; no CSS-chain selectors and no fixed waits
- capture the exact response status, headers, and problem JSON on failure

Backend, performance, and security requirements:
- compute authoritative precondition hashes from immutable domain or publication contracts, not from pretty-printed JSON payloads
- keep stale-view detection and problem construction server-side and deterministic
- fail closed when a request supplies multiple incompatible stale guards or mixed-generation preconditions
- do not let a looser conditional path bypass stricter domain stale-view checks on approval, filing, override, or remediation mutations

Validation rules:
- validate all error payloads against `problem_envelope.schema.json`
- verify that earlier read endpoints still publish stable `ETag` values after centralization
- unit-test stale-guard-family selection, ref pruning, portal-safe detail-surface selection, and `If-Match` comparison
- integration-test representative manifest, collaboration, portal, and governance routes

Definition of done:
- stale-view and conditional-request behavior is centralized, deterministic, and schema-valid
- northbound routes no longer improvise `ProblemEnvelope` construction or `ETag` derivation
- audience-safe recovery guidance remains precise without crossing truth or visibility boundaries
- stale/conflicting writes fail before mutation and publish enough grouped recovery data for safe rebase or retry.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

