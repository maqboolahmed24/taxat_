---
id: pc_0173
slug: phase_04_parallel_wave_02_track_backend_low_noise_005_implement_experience_delta_publication_coalescing_and_noise_budget_controls
mode: parallel
phase: "04"
wave: "02"
track: "backend_low_noise"
task_number: "005"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_low_noise_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0173

## Task Summary

implement experience delta publication coalescing and noise budget controls.

## Metadata

- ID: `pc_0173`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend low noise`
- Task Number: `005`
- Source Slug: `phase_04_parallel_wave_02_track_backend_low_noise_005_implement_experience_delta_publication_coalescing_and_noise_budget_controls`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0172](./pc_0172.md) `phase_04_parallel_wave_02_track_backend_low_noise_004_implement_low_noise_budget_audit_and_budget_audit_pack_generators`
- Next: [pc_0174](./pc_0174.md) `phase_04_parallel_wave_02_track_backend_low_noise_006_implement_shell_state_taxonomy_contract_and_dominant_question_projection_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0166_to_0173.md](../shared_operating_contract_0166_to_0173.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_low_noise_005_implement_experience_delta_publication_coalescing_and_noise_budget_controls`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0172` if they exist, especially `pc_0070` through `pc_0077` and `pc_0169` through `pc_0172`.

Mission:
Implement the publication layer for `ExperienceDelta` so low-noise shell updates remain ordered, replay-safe, budget-aware, and semantically calm.
This task must turn raw frame changes into deterministic delta artifacts with mirror-safe convenience fields, continuity-cost accounting, and non-material refresh coalescing when the shell would otherwise churn.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md`, especially the non-material refresh continuity-cost formula and publication rules.
- `low_noise_surface_compression_and_noise_budget_audit_contract.md`.
- `modules.md`, especially the `BUILD_LIVE_EXPERIENCE_FRAME(...)` rule set and delta publication notes.
- `stream_resume_and_catch_up_ordering_contract.md` where ordering, idempotency, and catch-up semantics matter.
- `data_model.md`, especially `ExperienceDelta` and related mirror fields.
- `schemas/experience_delta.schema.json` and `schemas/low_noise_experience_frame.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 64-68 and 404-413.

Deliverables to create:
1. `packages/backend-low-noise/src/deltas/build_experience_delta.ts`
2. `packages/backend-low-noise/src/deltas/compute_continuity_cost.ts`
3. `packages/backend-low-noise/src/deltas/coalesce_non_material_refresh.ts`
4. `packages/backend-low-noise/src/deltas/derive_semantic_motion.ts`
5. `packages/backend-low-noise/src/deltas/validate_delta_mirror_contract.ts`
6. `packages/backend-low-noise/src/deltas/publish_experience_delta_batch.ts`
7. `packages/backend-low-noise/src/tests/experience_delta_publication.spec.ts`
8. `docs/low_noise/experience_delta_publication.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/low-noise/delta-inspector.tsx`

Required design decisions you must make explicitly:
- define the exact distinction between material and non-material change for low-noise publication
- define how continuity cost, rank-swap count, prominent motion count, and visible-change counts are computed from consecutive frames
- define how delta sequence issuance and frame-epoch compatibility interact with existing manifest stream infrastructure
- define whether delta publication is a pure byproduct of frame publication or a separately queued step; choose one and keep it replay-safe

Execution requirements:
- publish mirror-safe delta fields that exactly match the underlying frame truth; the client must not need to infer dominant issue, actionability, detail entry points, or suggested detail surface
- maintain strict monotonic ordering and frame-epoch compatibility for deltas
- coalesce or suppress non-material refreshes when continuity cost or visible-change budget would otherwise violate the calm-shell contract
- keep `affected_surface_codes[]` and `surface_updates[]` in exact set agreement
- if you add the optional inspector, make it a before/after comparison instrument with one sequence strip and one diff column, not a live dashboard

Gap handling you must close while implementing:
- earlier manifest stream work may publish low-level events but not a calm-shell delta contract; integrate with the existing stream spine without rewriting transport semantics
- mirror fields are hardened by the patch index but are easy to let drift from the actual surfaces; implement one explicit validator for mirror equality and call it before publication
- non-material refresh coalescing is mathematically defined but often hand-waved in UI code; keep the formula server-authored here
- if prior cards do not yet expose a reusable publication frontier helper, add a narrow adapter instead of embedding stream-sequence logic in delta code

Mandatory edge cases:
- no mixed-generation delta publication across frame-epoch changes
- no more than the frozen visible-change budget for a non-material refresh window; otherwise coalesce
- dominant question and primary action must remain stable across lawful non-material refreshes unless the continuity policy says otherwise
- `affected_surface_codes[]` must equal the actual updated surface set
- duplicate delta delivery must remain idempotent for downstream consumers
- blank reason labels, action labels, waiting copy, blocking copy, or mirror refs are unlawful where the schema/patch rules require them

Playwright and contract-test requirements:
- test the delta builder and publisher directly
- if you add the inspector, use Playwright semantic locators, explicit diff assertions, and reduced-motion coverage; no fixed sleeps
- capture pre-frame, post-frame, delta, and audit payloads on failures
- exercise duplicate delivery, catch-up publication, non-material coalescing, and explicit material change paths

Backend, performance, and security requirements:
- keep delta publication deterministic and replay-safe from immutable frame inputs
- do not let downstream render hints mutate or widen delta meaning after publication
- avoid N^2 diff logic across large hidden structures; compare only the governed low-noise surface payloads and mirrors
- do not leak staff-only or masked-limited objects through convenience mirrors or affected-object lists

Validation rules:
- validate every delta against `experience_delta.schema.json`
- validate embedded or linked frame references against the frame schema where the publisher embeds them
- unit-test continuity-cost arithmetic, mirror equality, affected-surface equality, and coalescing outcomes
- integration-test delta publication against representative calm/warning/blocked/waiting/recovery frame transitions

Definition of done:
- the repo can publish deterministic low-noise `ExperienceDelta` artifacts from consecutive frames
- mirror fields, ordering, and budget-aware coalescing are enforced server-side
- low-noise stream consumers receive calm-shell updates that are replay-safe and semantically stable rather than churn-prone UI diffs
- the delta layer is ready for the next calm-shell tasks without hiding core logic in the frontend.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

