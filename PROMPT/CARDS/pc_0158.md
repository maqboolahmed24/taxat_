---
id: pc_0158
slug: phase_03_parallel_wave_01_track_backend_northbound_002_implement_get_command_receipt_endpoint_with_recovery_anchor_semantics
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "002"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0158

## Task Summary

implement get command receipt endpoint with recovery anchor semantics.

## Metadata

- ID: `pc_0158`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `002`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_002_implement_get_command_receipt_endpoint_with_recovery_anchor_semantics`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0157](./pc_0157.md) `phase_03_parallel_wave_01_track_backend_northbound_001_implement_post_commands_endpoint_and_durable_api_command_receipt_store`
- Next: [pc_0159](./pc_0159.md) `phase_03_parallel_wave_01_track_backend_northbound_003_implement_decision_bundle_snapshot_endpoint_with_etag_support`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0158_to_0165.md](../shared_operating_contract_0158_to_0165.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_002_implement_get_command_receipt_endpoint_with_recovery_anchor_semantics`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0157` if they exist, especially `pc_0064` through `pc_0069`, `pc_0094` through `pc_0101`, `pc_0134` through `pc_0141`, and `pc_0157`.

Mission:
Implement `GET /v1/commands/{command_id}` as the durable recovery surface for lost POST responses, safe retries, automation polling, and app relaunch after write submission.
This endpoint must return the latest lawful `ApiCommandReceipt` for the client-generated `command_id`, preserve the original idempotency lineage, and keep the same recovery-anchor family available even when the receipt has aged into `EXPIRED`.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially sections 1, 2, 4, 5, and 6.
- `data_model.md`, especially the `ApiCommandReceipt` and `ProblemEnvelope` entries.
- `state_machines.md`, especially `ApiCommandReceipt.acceptance_state`.
- `schemas/api_command_receipt.schema.json` and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 69-71, 280-286, and 442-443.
- `invariant_enforcement_and_fail_closed_contract.md`, `replay_and_reproducibility_contract.md`, and `observability_and_audit_contract.md` where receipt recovery must remain replay-safe and auditable.

Deliverables to create:
1. `packages/backend-northbound/src/http/get_command_receipt_endpoint.ts`
2. `packages/backend-northbound/src/http/register_get_command_receipt_route.ts`
3. `packages/backend-northbound/src/query/get_command_receipt_by_command_id.ts`
4. `packages/backend-northbound/src/services/authorize_command_receipt_read.ts`
5. `packages/backend-northbound/src/services/validate_command_receipt_recovery_anchors.ts`
6. `packages/backend-northbound/src/services/build_command_receipt_response.ts`
7. `packages/backend-northbound/src/services/build_command_receipt_problem_envelope.ts`
8. `packages/backend-northbound/src/services/load_latest_receipt_for_duplicate_lineage.ts`
9. `docs/northbound/get_command_receipt.md`
10. `tests/unit/backend_northbound/command_receipt_recovery_anchor_rules.spec.ts`
11. `tests/unit/backend_northbound/command_receipt_read_authorization.spec.ts`
12. `tests/integration/backend_northbound/get_command_receipt_lost_post_recovery.spec.ts`
13. `tests/integration/backend_northbound/get_command_receipt_expired_success_readback.spec.ts`
14. `tests/integration/backend_northbound/get_command_receipt_stale_rejection_readback.spec.ts`
15. `tests/playwright_api/northbound/get_command_receipt_contract.spec.ts`

Optional diagnostic explorer only if it materially accelerates verification:
16. `apps/admin-console-web/src/routes/debug/command-receipts/[commandId].tsx`

Required design decisions you must make explicit:
- Define the canonical lookup boundary for `command_id` so the endpoint remains tenant-, client-, principal-, and scope-safe without breaking safe retry across response loss.
- Define the exact shape of a success-class recovery anchor set and how you reject or quarantine a receipt that has only `latest_projection_ref` and no durable anchor such as `result_ref`, `audit_event_refs[]`, or `activity_refs[]`.
- Define how `EXPIRED` receipts preserve `original_acceptance_state`, `command_id`, `request_hash`, `idempotency_key`, and the same recovery-anchor family they exposed before expiry.
- Define the difference between `404 command not found`, `403/404 scope-hidden`, and typed stale/policy/invalid receipts that do exist and must be returned rather than remapped to transport errors.
- Define how work-item, manifest, and governance receipts are authorized for readback without overloading `manifest_id` as a universal scope field.
- Define whether this endpoint is cacheable; default to `Cache-Control: no-store` unless you can prove safe bounded caching that does not leak actor/session-scoped recovery anchors.

Execution steps:
1. Reuse the `ApiCommandReceipt` model and repository semantics from `pc_0157`; do not fork receipt meaning.
2. Implement receipt lookup by `command_id` plus tenant/scope-safe boundary checks.
3. Re-validate the loaded receipt against `api_command_receipt.schema.json` before emitting it.
4. Enforce success-class recovery-anchor rules server-side so bad historical data cannot be replayed as a valid recovery surface.
5. Emit typed problem envelopes only for true endpoint failures such as not found, hidden scope, or repository corruption; return the receipt itself for stale/policy/invalid command outcomes because those are durable results, not transport failures.
6. Preserve duplicate lineage and semantic action ids in the emitted response so automation can verify safe retry collapsed correctly.
7. Add deterministic contract tests for lost-response recovery, expired-success readback, stale rejection readback, and unauthorized scope isolation.

Gap handling you must perform:
- Close the gap where `GET /v1/commands/{command_id}` could return a success-class receipt with no durable recovery anchor at all.
- Close the gap where expired receipts degrade into timeout markers and lose the original recovery family.
- Close the gap where stale-guard families or governance simulation hashes can drift away from the true target scope.
- Close the gap where command-id lookup leaks cross-scope receipts or silently aliases governance/work-item receipts through `manifest_id`.
- Close the gap where missing durable receipts force clients to blindly replay a write instead of relying on the receipt boundary.

Mandatory edge cases:
- `ACCEPTED` and `DUPLICATE_REPLAY` are success classes and MUST preserve at least one durable recovery anchor in addition to any observational `latest_projection_ref`
- `duplicate_of_receipt_id` MUST NOT equal `receipt_id`
- `expires_at` MUST be later than `accepted_at`
- `REJECTED_STALE_VIEW` MUST preserve `stale_guard_family`, `latest_stale_guard_value`, and the aligned grouped stability contract when applicable
- manifest stale guards MUST NOT appear on work-item or governance receipts
- governance receipts carrying `DEPENDENCY_TOPOLOGY_HASH`, `SIMULATION_BASIS_HASH`, or `MUTATION_BASIS_CONTRACT_HASH` MUST also preserve the matching basis evidence
- a receipt that is hidden by current authorization MUST NOT leak existence through a more informative problem shape than the chosen hidden/not-found policy
- endpoint corruption or schema-invalid receipt rows MUST fail closed and raise typed operational evidence instead of serving partial recovery data

Playwright / contract-test requirements:
- use Playwright APIRequestContext or equivalent deterministic HTTP contract tooling
- no fixed sleeps in polling/retry scenarios
- capture trace or full HTTP logs on contract failure
- prove that a lost POST response can be recovered solely from `GET /v1/commands/{command_id}`
- prove that expired success receipts keep the same recovery anchor family and original acceptance posture

Backend, performance, and security requirements:
- index `command_id` lookups along the exact tenancy/scope boundary you define
- do not rehydrate or recompute legal state from projections when a durable receipt is already present
- fail closed on schema-invalid or anchor-invalid receipts
- do not widen receipt visibility across tenant, client, principal class, or governance/work-item boundaries
- preserve immutable receipt evidence except for the allowed `EXPIRED` terminal projection

Validation rules:
- validate responses against `api_command_receipt.schema.json`
- validate any failure envelopes against `problem_envelope.schema.json`
- unit-test anchor validation, duplicate-lineage safety, hidden-scope handling, and expiry semantics
- integration-test lost-response recovery, expired success, stale rejection, and missing-command handling
- API-test the actual route with Playwright

Definition of done:
- the repo exposes `GET /v1/commands/{command_id}` as a durable recovery surface
- success-class receipts remain meaningfully recoverable even after expiry
- clients and automation can recover from lost POST responses without blind replay
- scope, stale-guard family, and duplicate-lineage semantics remain reproducible and fail closed
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
