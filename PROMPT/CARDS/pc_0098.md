---
id: pc_0098
slug: phase_03_parallel_wave_01_track_backend_manifest_002_implement_manifest_lineage_trace_and_branch_selection_persistence_models
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "002"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0098

## Task Summary

implement manifest lineage trace and branch selection persistence models.

## Metadata

- ID: `pc_0098`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `002`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_002_implement_manifest_lineage_trace_and_branch_selection_persistence_models`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0097](./pc_0097.md) `phase_03_parallel_wave_01_track_backend_manifest_001_implement_run_manifest_repository_and_lifecycle_state_machine`
- Next: [pc_0099](./pc_0099.md) `phase_03_parallel_wave_01_track_backend_manifest_003_implement_config_version_config_change_request_and_config_freeze_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0094_to_0101.md](../shared_operating_contract_0094_to_0101.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_002_implement_manifest_lineage_trace_and_branch_selection_persistence_models`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0097` if they exist, especially `pc_0064`, `pc_0065`, `pc_0067`, `pc_0076`, `pc_0077`, `pc_0080`, and `pc_0097`.

Mission:
Implement the persistence models and factory/validation layer for `ManifestLineageTrace` and the manifest-local branch-decision contract.
This task is about durable narration of why a manifest was reused, returned, or branched.
It must preserve the corpus distinction between the selected manifest's own continuation basis and the request-time branch outcome that may be `RETURN_EXISTING_BUNDLE` or `REUSE_SEALED_MANIFEST`.

Source algorithm grounding you must honor:
- `data_model.md`, especially `ManifestLineageTrace` and the request-time versus manifest-local branch truth split.
- `manifest_branch_selection_contract.md`, especially canonical branch actions, frozen identity inputs, typed branch reasons, and continuation truth.
- `manifest_lineage_explorer_and_reuse_decision_tracer_contract.md`, especially exhaustive candidate evaluation law, mirror parity law, nightly context law, and append-only trace refs on the selected manifest.
- `modules.md`, especially `DECIDE_MANIFEST_REUSE_STRATEGY(...)`, `VALIDATE_REUSE_SEALED_CONTEXT(...)`, `LOAD_AND_VALIDATE_PRIOR_MANIFEST_CONTEXT(...)`, and `EMIT_MANIFEST_LINEAGE_TRACE(...)` as the downstream modules that will consume these persistence shapes.
- `manifest_and_config_freeze_contract.md`, especially idempotency identity and continuation/inheritance mirror rules.
- Existing schemas including `manifest_lineage_trace.schema.json`, `run_manifest.schema.json`, and any sample lineage trace artifacts.

Deliverables to create:
1. `packages/backend-manifest/src/models/manifest_lineage_trace.ts`.
2. `packages/backend-manifest/src/models/manifest_branch_decision_contract.ts`.
3. `packages/backend-manifest/src/repositories/manifest_lineage_trace_repository.ts`.
4. `packages/backend-manifest/src/services/manifest_lineage_trace_factory.ts`.
5. `packages/backend-manifest/src/services/branch_candidate_evaluation_validator.ts`.
6. `packages/backend-manifest/src/services/mirror_consistency_state_evaluator.ts`.
7. `db/migrations/phase03_0098_manifest_lineage_trace.sql`.
8. `apps/operator-web/src/routes/internal/manifest-lineage-trace-viewer.tsx`.
9. `docs/manifest/manifest_lineage_trace_and_branch_decision_contract.md`.
10. `tests/unit/backend_manifest/manifest_lineage_trace_factory.spec.ts`.
11. `tests/integration/backend_manifest/manifest_lineage_trace_persistence.spec.ts`.
12. `tests/playwright/internal/manifest_lineage_trace_viewer.spec.ts`.

Required design decisions you must make explicit:
- Define whether `ManifestBranchDecisionContract` is stored only as an embedded `RunManifest` packet or also receives a write-side helper table/index for lookup and auditing.
  Preserve `RunManifest` as the selected manifest's own truth either way.
- Define the exact canonical ordering of `candidate_evaluations[]`, rejection reason codes, and `mirror_sources[]`.
- Define how `request_identity_hash` is represented and validated now without prematurely implementing the full branch-decision engine from `pc_0102`.
- Define how selected-manifest append-only `manifest_lineage_trace_refs[]` are persisted and updated safely.
- Define how nightly predecessor context is stored so same-window reuse and later-window continuation can never collapse into the same explanation.

Execution steps:
1. Implement typed models for `ManifestLineageTrace` and the embedded manifest-branch decision contract.
2. Create the persistence migration for lineage traces, selected-manifest linkage, and any supporting indexes needed by later branch-selection work.
3. Implement `manifest_lineage_trace_factory.ts` that can construct a schema-valid trace from already-decided inputs without inventing decision logic.
4. Implement validators enforcing exhaustive candidate coverage, exactly one selected candidate, typed rejection codes, and mirror-consistency truth.
5. Append selected trace refs to the chosen manifest in an append-only manner.
6. Build `manifest-lineage-trace-viewer` as an internal inspection surface for one persisted trace at a time.
7. Add tests proving the selected branch outcome and the selected manifest's own continuation basis can differ lawfully without ambiguity.

Visual and interaction specification for `manifest-lineage-trace-viewer`:
- Tone: a premium branch explorer.
  It should feel like an audit-grade path diagram, not a generic activity feed.
- Layout:
  - `64px` top bar with trace id, selected branch action chip, branch-reason chip, and prior-manifest hash chip.
  - left candidate rail `252-268px` listing the canonical branch vocabulary in stable order.
  - center explorer canvas with a branch-ribbon strip on top and selected-candidate detail cards below.
  - right inspector `388-416px` with selected manifest lineage refs, mirror parity, nightly predecessor context, and trace/audit refs.
- Signature visual:
  - use a thin branch-ribbon diagram with one dominant selected path and faint rejected paths.
  - include a small fork sigil near the title built from a single stem splitting into three ordered branches.
- Palette:
  - background `#F6F4F0`
  - primary surface `#FFFFFF`
  - secondary surface `#EEEAE3`
  - ink `#11171C`
  - muted ink `#66707A`
  - hairline `rgba(17,23,28,0.08)`
  - accent blue-slate `#4A6278`
  - accent olive `#68725A`
  - accent brass `#8E6C37`
  - success `#165F49`
  - warning `#885B1C`
  - danger `#A23A34`
- Typography:
  - calm, technical-editorial hierarchy.
  - monospace only for ids, hashes, lineage refs, and reason codes.
- Dimensional constraints:
  - max width `1540px`
  - candidate rail min width `252px`
  - branch-ribbon strip height `124px`
  - decision cards min height `104px`
  - inspector rows `36px`
- Motion:
  - `150ms` branch emphasis on selection.
  - `180ms` inspector swap.
  - reduced-motion mode removes ribbon animation and uses static contrast emphasis only.
- Interaction:
  - selecting a candidate shows why it was selected or rejected with typed disqualifier reasons.
  - selecting nightly context chips highlights predecessor linkage.
  - keyboard navigation across candidate rail is mandatory.
  - accessible names should read as sentences such as `replay child rejected because replay class mismatched`.
  - Playwright must verify candidate ordering, selection determinism, keyboard navigation, and reduced-motion-safe rendering.

Component hierarchy hint:
~~~ts
<InternalShell route="/internal/manifest-lineage-trace-viewer">
  <BranchCandidateRail />
  <BranchRibbonCanvas />
  <TraceDecisionCards />
  <LineageTraceInspector />
</InternalShell>
~~~

Gap handling you must perform:
- Close the gap where request-time branch outcome and manifest-local continuation basis could be collapsed into one ambiguous field.
- Close the gap where rejected branch candidates vanish and later auditors cannot tell why a branch was not chosen.
- Close the gap where nightly predecessor context is inferred from timestamps instead of frozen in the trace.
- Close the gap where selected-manifest lineage mirrors could drift but no durable explorer records which mirrors were actually checked.
- Close the gap where branch narration lives only in logs or spans rather than in a durable artifact.

Mandatory edge cases:
- selected branch action is `RETURN_EXISTING_BUNDLE` while selected manifest continuation basis remains `NEW_MANIFEST`
- selected branch action is `REUSE_SEALED_MANIFEST` and no child lineage refs are fabricated
- nightly same-window reuse stays distinct from `NIGHTLY_WINDOW_ADVANCED`
- candidate list omits a canonical branch action and validation fails closed
- rejected candidate has no typed disqualifier reason and validation fails closed
- selected manifest lineage mirrors disagree with `continuation_set{...}` and trace persistence rejects the snapshot
- returned-decision-bundle hash is present only for bundle-return paths
- selected-manifest trace refs are append-only and do not overwrite earlier trace lineage

Backend, performance, and security requirements:
- Trace persistence must be append-only and deterministic.
- Factories and validators in this task must not implement the full branch-decision engine; they consume already-decided inputs only.
- Do not infer missing lineage fields from timestamps or adjacent manifests.
- Keep the viewer route read-only and driven entirely from persisted trace artifacts.
- Avoid leaking secret or internal transport details in the trace viewer; ids, hashes, and audit refs are enough.

Validation rules:
- Unit-test candidate exhaustiveness, selected-candidate uniqueness, mirror-consistency evaluation, and nightly-context typing.
- Integration-test persistence, append-only selected-manifest trace refs, and lawful divergence between request-time branch action and manifest-local continuation basis.
- Use Playwright to verify the viewer route's candidate ordering, selection behavior, keyboard navigation, and reduced-motion-safe behavior.
- Prove that traces fail closed when mirror parity or typed reason coverage is incomplete.
- Prove that the same decided inputs produce byte-stable trace artifacts across repeated runs.

Definition of done:
- The repo has one durable `ManifestLineageTrace` model and one clear manifest-local branch-decision contract.
- Later branch-selection work can persist and inspect request-time lineage narration without inventing new storage shapes.
- Operators and auditors gain a precise branch story instead of reconstructing reuse decisions from nearby manifests or logs.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
