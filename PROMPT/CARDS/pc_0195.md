---
id: pc_0195
slug: phase_04_parallel_wave_02_track_backend_governance_007_implement_governance_interaction_layer_projection_for_baskets_diff_panels_and_step_up
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "007"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0195

## Task Summary

implement governance interaction layer projection for baskets diff panels and step up.

## Metadata

- ID: `pc_0195`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `007`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_007_implement_governance_interaction_layer_projection_for_baskets_diff_panels_and_step_up`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0194](./pc_0194.md) `phase_04_parallel_wave_02_track_backend_governance_006_implement_audit_investigation_frame_projection_and_correlation_neighborhood_queries`
- Next: [pc_0196](./pc_0196.md) `phase_04_parallel_wave_02_track_backend_governance_008_implement_policy_risk_and_blast_radius_modeling_projection_and_preview_confidence_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0190_to_0197.md](../shared_operating_contract_0190_to_0197.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_007_implement_governance_interaction_layer_projection_for_baskets_diff_panels_and_step_up`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0194` if they exist, especially `pc_0169` through `pc_0178`, `pc_0189` through `pc_0194`, and the earlier portal / operator interaction-layer work.

Mission:
Implement the shared `GovernanceInteractionLayer` projector and the supporting services that freeze chip echo, compaction mode, support-surface precedence, staged diff / basket continuity, focus return, semantic selector profile, and durable receipt / typed failure feedback across governance routes.
This is not a frontend utility and not a CSS-state convention.
It is the server-authored cross-route interaction contract for baskets, diff panels, step-up, drawers, inspectors, trays, and export surfaces.

Source algorithm grounding you must honor:
- `admin_governance_console_architecture.md`, especially the shared governance interaction-layer section after the route descriptions.
- `data_model.md`, especially `GovernanceInteractionLayer` and all governance read models that embed it.
- `frontend_shell_and_interaction_law.md`, especially governance-route compaction, support-surface, focus-return, and stale/recovery behavior.
- `cross_shell_design_token_and_interaction_layer_foundation_contract.md`, especially the governance foundation contract and shared tokens.
- `semantic_selector_and_accessibility_contract.md`, `semantic_selector_and_accessibility_regression_pack_contract.md`, and `focus_restoration_and_return_target_harness_contract.md`.
- `northbound_api_and_session_contract.md`, especially durable receipt and typed failure driven read-side recovery.
- `schemas/governance_interaction_layer.schema.json` and any shared foundation contract schema it imports.
- `scripts/validate_contracts.py`, especially the governance interaction-related drifts already enforced through the policy, authority-link, retention, and audit fixtures.
- `UIUX_DESIGN_SKILL.md`, especially the governance-console skill section on density, explicit hierarchy, and one canonical interaction grammar.

Deliverables to create:
1. `packages/backend-governance/src/projectors/build_governance_interaction_layer.ts`
2. `packages/backend-governance/src/services/derive_governance_filter_chip_echo.ts`
3. `packages/backend-governance/src/services/derive_governance_support_surface_presentation.ts`
4. `packages/backend-governance/src/services/derive_governance_preserved_context.ts`
5. `packages/backend-governance/src/services/derive_governance_focus_and_compaction_state.ts`
6. `packages/backend-governance/src/tests/governance_interaction_layer_projection.spec.ts`
7. `packages/backend-governance/src/tests/governance_interaction_layer_focus_and_compaction.spec.ts`
8. `docs/governance/governance_interaction_layer_and_compaction_rules.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/interaction-layer-atlas.tsx`

You MUST patch every governance route projector touched in this tranche so they reuse this shared layer instead of inventing route-local selector profiles, chip echo grammar, support-surface policy, or compaction semantics.

Required design decisions you must make explicitly:
- define how canonical filter chips are serialized in stable dimension order across all supported governance routes
- define how `compaction_mode` and `auxiliary_surface_presentation` are derived from route shell state without losing same-object context
- define how preserved-context codes are selected per route and how they survive stale rebase or reconnect
- define how `focus_trap_mode` remains `NON_MODAL` for ordinary governance drawers / inspectors while still allowing explicit modal step-up when truly necessary
- define how `feedback_truth_policy` binds durable command receipts and typed problem envelopes back into mounted governance routes
- define how semantic selector ids / labels will be emitted so Playwright and native embodiments share one governance meaning map

Execution requirements:
- publish a schema-valid `GovernanceInteractionLayer`
- keep the exact shared constants:
  `density_profile = GOVERNANCE_DENSITY_PROFILE_V1`
  `inventory_filter_grammar = CANONICAL_ROUTE_FILTER_GRAMMAR`
  `support_surface_policy = ONE_PROMOTED_SUPPORT_SURFACE_MAX`
  `diff_basket_policy = STAGED_DIFF_AND_BASKET_RETAIN_CONTEXT`
  `export_binding_policy = ACTIVE_FILTERED_SLICE_GOVERNS_EXPORT`
  `keyboard_focus_policy = RETURN_FOCUS_ANCHOR_OR_ROVING_SELECTION`
  `selector_profile = GOVERNANCE_SEMANTIC_SELECTORS_V1`
  `selection_persistence_mode = PRESERVE_WHILE_OBJECT_RESOLVES`
  `motion_profile = SUBTLE_CAUSAL_ONLY`
  `feedback_truth_policy = DURABLE_RECEIPT_AND_TYPED_FAILURE_DRIVEN`
- keep `foundation_contract.shell_family = GOVERNANCE_DENSITY_SHELL`
- keep `preserved_context_codes[]` within the governed vocabulary only
- keep chip echo deterministic and stable; route-specific filters must serialize in canonical dimension order
- ordinary governance drawers and inspectors must remain `focus_trap_mode = NON_MODAL`
- step-up, diff panels, baskets, and export / audit surfaces must compete for one promoted support slot instead of stacking parallel promoted surfaces
- keep the layer shared across policy, access, authority-link, retention, and audit routes rather than forking route-local variants

Gap handling you must close while implementing:
- close any gap where filter chips, support surfaces, or focus return are derived from UI-local state instead of server-authored contract
- close any gap where more than one support region can become primary on wide layouts
- close any gap where staged diff or change-basket context is lost during compaction, reconnect, or stale rebase
- close any gap where selector profile drifts between browser routes and future native embodiments
- close any gap where durable receipt or typed failure feedback is not bound back into the mounted route context

Mandatory edge cases:
- `selected_filter_chip_refs[]` must stay unique and deterministic
- `compaction_mode = AUXILIARY_DRAWER` or `AUXILIARY_TRAY` must not discard `PROMOTED_SUPPORT_SURFACE` from preserved context
- `focus_trap_mode = MODAL_EXPLICIT` is not lawful for ordinary governance drawers or inspectors
- no route may promote both blast radius and export eligibility simultaneously as primary support surfaces
- no route may change selector profile by shell width, color theme, or route subtype

Playwright and contract-test requirements:
- add direct tests for canonical constants, filter-chip ordering, preserved-context serialization, compaction transitions, support-surface precedence, and focus-return policy
- if any northbound read routes are touched, add Playwright `APIRequestContext` tests confirming the shared interaction layer is present and route-appropriate
- if you add the optional atlas, add Playwright browser tests that verify chip echo ordering, support-surface exclusivity, focus-return behavior, and semantic locators only
- capture traces and serialized layer payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- make the optional atlas a premium read-only control-plane page, not a devtool-looking matrix wall
- max width `1440px`; palette `#F7F5F1 / #FFFFFF / #F1F3F0 / #171717 / #667085 / #0F766E / #1D4ED8 / #B7791F / #C2410C`
- show one top context bar, one canonical filter-chip echo rail, one compaction preview strip, one support-surface precedence board, and one focus-return explainer rail
- use one subtle transition atlas showing `WIDE -> AUXILIARY_DRAWER -> AUXILIARY_TRAY -> FOCUS_STACK`
- render preserved context as compact labeled chips, not dense JSON
- no dashboard metrics; if a diagram helps, use one route-state ladder or one support-surface precedence stack

Backend, performance, and security requirements:
- keep the layer pure, deterministic, and reusable across governance projectors
- do not let renderers override or repair malformed interaction-layer semantics locally
- keep chip echo and preserved-context derivation cheap and stable
- fail closed on unsupported compaction modes, unsupported preserved-context codes, or selector-profile drift

Validation rules:
- validate every emitted `GovernanceInteractionLayer` against schema
- add regression coverage across policy, authority-link, retention, and audit routes to ensure shared-layer continuity
- keep this task focused on the interaction-layer contract; do not absorb route-specific projector logic beyond what is needed to wire the shared layer in

Definition of done:
- the repo has one authoritative governance interaction-layer projector reused across this tranche’s governance routes
- filter chips, compaction, focus return, support-surface precedence, and durable feedback semantics are now machine-checkable instead of renderer-local convention
- any browser or native governance surface can inherit one premium, low-noise, high-control interaction grammar without forking behavior
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
