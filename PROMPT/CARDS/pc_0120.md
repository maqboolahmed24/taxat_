---
id: pc_0120
slug: phase_03_parallel_wave_01_track_backend_collection_012_implement_artifact_contract_gate_and_validate_artifact_set_services
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "012"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_012
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0120

## Task Summary

implement artifact contract gate and validate artifact set services.

## Metadata

- ID: `pc_0120`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `012`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_012_implement_artifact_contract_gate_and_validate_artifact_set_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0119](./pc_0119.md) `phase_03_parallel_wave_01_track_backend_collection_011_implement_artifact_set_wrapping_hashing_and_contract_reference_recording`
- Next: [pc_0121](./pc_0121.md) `phase_03_parallel_wave_01_track_backend_compute_001_implement_compute_result_formula_engine_with_exact_decimal_handling`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0118_to_0125.md](../shared_operating_contract_0118_to_0125.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_012_implement_artifact_contract_gate_and_validate_artifact_set_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0119` if they exist, especially `pc_0016`, `pc_0028`, `pc_0060`, `pc_0061`, `pc_0065`, `pc_0066`, `pc_0080`, `pc_0097`, `pc_0100`, `pc_0101`, `pc_0118`, and `pc_0119`.

Mission:
Implement the frozen-bundle artifact validation services and the `ARTIFACT_CONTRACT_GATE` decision service for the authoritative pre-seal intake pack.
This task must validate individual authoritative artifacts and intake set artifacts only against the manifest’s frozen schema bundle and reader-window posture, recompute and verify artifact-contract refs/hashes, and emit one schema-valid artifact-contract gate decision without overreaching into the later generic ordered-gate engine.

Source algorithm grounding you must honor:
- `core_engine.md`, especially `LOAD_SCHEMA_BUNDLE(...)`, `VALIDATE_ARTIFACT(...)`, `VALIDATE_ARTIFACT_CONTRACTS_FOR_PRESEAL_SET(...)`, `ARTIFACT_CONTRACT_GATE(...)`, and the ordered pre-seal gate chain.
- `modules.md`, especially `LOAD_SCHEMA_BUNDLE(...)`, `VALIDATE_ARTIFACT_SET(...)`, `VALIDATE_ARTIFACT(...)`, and `ARTIFACT_CONTRACT_GATE(...)`.
- `exact_gate_logic_and_decision_tables.md`, especially section `7.4A ARTIFACT_CONTRACT_GATE`.
- `deployment_and_resilience_contract.md` and `replay_and_reproducibility_contract.md`, especially frozen reader-window and historical bundle rules.
- `data_model.md`, especially `GateDecisionRecord`, `SchemaBundle`, `InputFreeze`, and the authoritative intake family.
- Existing schemas including `schema_bundle.schema.json`, `gate_decision_record.schema.json`, `preseal_gate_evaluation_contract.schema.json`, all intake-boundary schemas, all intake set schemas, `snapshot.schema.json`, and `input_freeze.schema.json`.

Deliverables to create:
1. `packages/backend-collection/src/types/artifact_validation_result.ts`.
2. `packages/backend-collection/src/services/load_intake_schema_bundle.ts`.
3. `packages/backend-collection/src/services/validate_artifact.ts`.
4. `packages/backend-collection/src/services/validate_artifact_set.ts`.
5. `packages/backend-collection/src/services/validate_artifact_contracts_for_preseal_set.ts`.
6. `packages/backend-collection/src/services/artifact_contract_gate.ts`.
7. `packages/backend-collection/src/services/build_artifact_contract_gate_record.ts`.
8. `packages/backend-collection/src/services/resolve_frozen_schema_entry.ts`.
9. `docs/collection/artifact_contract_gate_and_validate_artifact_set_services.md`.
10. `tests/unit/backend_collection/artifact_contract_gate.spec.ts`.
11. `tests/integration/backend_collection/frozen_bundle_validation.spec.ts`.

Required design decisions you must make explicit:
- Define the exact resolution algorithm from `schema_bundle_hash` plus `schema_reader_window_contract{...}` to one active schema entry for a given authoritative artifact type.
- Define how deprecated-but-allowed schema versions become `PASS_WITH_NOTICE` instead of `HARD_BLOCK`.
- Define the exact envelope checks for `schema_id`, `semantic_version`, `dialect_ref`, `schema_bundle_hash`, `artifact_content_hash`, and `writer_build_id`.
- Define how the gate recomputes and verifies `input_freeze.artifact_contract_hash`.
- Define the interop seam with the later generic gate engine.
  This task must emit a schema-valid artifact-contract gate record now, but it must not absorb cross-gate ordering, manifest-level tape assembly, or generic gate repositories from later cards.

Execution steps:
1. Implement bundle resolution helpers that bind schema lookup to the frozen bundle and reader window only.
2. Implement `validate_artifact.ts` and `validate_artifact_set.ts` so authoritative artifacts fail closed on missing schema, invalid closure, unknown enum, undeclared property, or incompatible version mix.
3. Implement `validate_artifact_contracts_for_preseal_set.ts` so the entire intake pack can be validated as one governed unit.
4. Implement `artifact_contract_gate.ts` so it maps validation outcomes and contract-hash checks onto the exact decision table from section `7.4A`.
5. Implement `build_artifact_contract_gate_record.ts` as a narrow gate-record emitter that produces a schema-valid `GateDecisionRecord` without attempting to generalize the whole ordered-gate framework.
6. Add tests for missing schema, deprecated-but-allowed schema, envelope incompleteness, hash mismatch, and clean pass.

Gap handling you must perform:
- Close the roadmap-ordering gap where `ARTIFACT_CONTRACT_GATE` appears before the generic gate-decision engine task.
  Emit one schema-valid gate record through a narrow local builder or injected adapter, but do not fork a second long-term gate framework.
- Close the gap where live schema bundles could be consulted opportunistically.
  Validation must stay frozen to the recorded bundle and reader window.
- Close the gap where `artifact_contract_refs[]` are simple strings and could be interpreted loosely.
  Consume the canonical contract-ref representation from `pc_0119` and reject ambiguous ref forms.
- Close the gap where a compliance-capable run could appear schema-valid even when `artifact_contract_hash` is absent or does not match the authoritative intake pack.

Mandatory edge cases:
- multiple candidate schema entries for one authoritative artifact type inside the frozen bundle must fail as bundle corruption.
- a required authoritative schema missing from the frozen bundle must `HARD_BLOCK`.
- a deprecated but still reader-window-allowed schema may pass only as `PASS_WITH_NOTICE`, never as silent `PASS`.
- `InputFreeze.artifact_contract_refs[]` missing `SourcePlan`, `SourceWindow`, `CollectionBoundary`, or `NormalizationContext` must `HARD_BLOCK`.
- a live-compatible schema outside the frozen bundle must still be rejected.
- unknown enums, undeclared fields after schema closure, or version-incompatible mixes must `HARD_BLOCK`.
- analysis-only runs may have different policy treatment for absent `artifact_contract_hash`; make that rule explicit and test it.
- gate reason-code ordering and `dominant_reason_code` must be deterministic.

Backend, performance, and security requirements:
- Keep validation deterministic, replay-safe, and side-effect free except for explicit gate-artifact emission.
- Cache frozen bundle resolution within the request/run boundary if needed, but never across incompatible bundle hashes or reader windows.
- Do not log full artifact payloads when validation fails; log minimal reason-coded diagnostics and refs.
- Avoid implementing a second schema system; reuse the imported validator and schema bundle primitives.

Optional internal verification surface (only if you add one):
- If you add an internal artifact-contract inspector, place it behind a read-only route such as `/internal/contracts/intake/:manifestId`.
- The UI must be quiet and premium: a two-panel layout with a left contract matrix and right validation trace sheet, one subtle diff stripe for mismatches, no dashboard widgets, and typography-led hierarchy.
- Include one restrained diagram only: a vertical intake pack stack (`SourcePlan`, `SourceWindow`, `CollectionBoundary`, `NormalizationContext`, five set artifacts, `Snapshot`, `InputFreeze`) with status dots bound to persisted validation results.
- Any such surface must be implemented and tested with Playwright using user-facing locators, actionability-safe flows, reduced-motion assertions, and trace capture.

Validation rules:
- Unit-test frozen schema resolution, closure enforcement, envelope validation, deprecated-window handling, and gate mapping.
- Integration-test a full authoritative intake pack from schema-bundle load through gate decision.
- Validate emitted `GateDecisionRecord` payloads against `gate_decision_record.schema.json`.
- Prove that validation never upgrades to a live schema bundle outside the frozen bundle/reader window.
- Re-run the contract validators after the implementation bundle is assembled.

Definition of done:
- The repo can validate the authoritative pre-seal intake pack against the frozen schema bundle.
- Artifact-contract mismatches now become one explicit gate decision instead of ad hoc validation logs.
- Later ordered pre-seal gate work can consume a real artifact-contract gate output without re-implementing schema semantics.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
