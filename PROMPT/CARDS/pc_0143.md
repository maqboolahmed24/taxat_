---
id: pc_0143
slug: phase_03_parallel_wave_01_track_backend_authority_011_implement_filing_notice_step_and_resolution_state_handling
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "011"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_011_implement_filing_notice_step_and_resolution_state_handling
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0143

## Task Summary

filing notice step and resolution state handling.

## Metadata

- ID: `pc_0143`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `011`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_011_implement_filing_notice_step_and_resolution_state_handling`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0142](./pc_0142.md) `phase_03_parallel_wave_01_track_backend_authority_010_implement_out_of_band_authority_correction_and_truth_propagation_services`
- Next: [pc_0144](./pc_0144.md) `phase_03_parallel_wave_01_track_backend_authority_012_implement_authority_reconciliation_analytics_snapshot_and_operator_insight_queries`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0142_to_0149.md](../shared_operating_contract_0142_to_0149.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_011_implement_filing_notice_step_and_resolution_state_handling`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0142` if they exist, especially `pc_0061`, `pc_0097`, `pc_0108`, `pc_0118`, `pc_0126`, `pc_0133`, and `pc_0141`.

Mission:
Implement the full packet-local filing-notice layer.
This card must derive ordered `FilingNoticeStep` artifacts from a prepared filing packet, resolve them into one replay-safe `FilingNoticeResolution`, bind that resolution to packet promotion, and surface the notice posture inside the portal approval route without bloating the UI into a generic warning wall.

Source algorithm grounding you must honor:
- `modules.md`, especially `DERIVE_PACKET_NOTICE_STEPS(...)`, `RESOLVE_FILING_NOTICES(...)`, and the `APPROVE_FILING_PACKET(...)` / packet-promotion rules.
- `exact_gate_logic_and_decision_tables.md`, especially the filing-gate section that treats packet-phase approval and declaration-basis acknowledgement as packet-local, explicit artifacts rather than inferred UI state.
- `data_model.md`, especially `FilingPacket`, `FilingNoticeStep`, and `FilingNoticeResolution`.
- `state_machines.md`, especially the `FilingPacket.lifecycle_state` rules around `PREPARED -> APPROVED_TO_SUBMIT`.
- `schemas/filing_notice_step.schema.json`, `schemas/filing_notice_resolution.schema.json`, and `schemas/filing_packet.schema.json`.
- `customer_client_portal_experience_contract.md`, especially the approval route rules.
- `frontend_shell_and_interaction_law.md`, `low_noise_experience_contract.md`, and the semantic selector / accessibility contracts.
- `PATCH_RESOLUTION_INDEX.md`, especially entries 124 through 129 covering packet-local notice binding and resolution invariants.
- `test_vectors.md`, especially approval / notice / stale-pack vectors and any final-declaration sign-off vectors.

Deliverables to create:
Backend:
1. `packages/backend-authority/src/models/filing_notice_step.ts`
2. `packages/backend-authority/src/models/filing_notice_resolution.ts`
3. `packages/backend-authority/src/repositories/filing_notice_step_repository.ts`
4. `packages/backend-authority/src/repositories/filing_notice_resolution_repository.ts`
5. `packages/backend-authority/src/services/derive_packet_notice_steps.ts`
6. `packages/backend-authority/src/services/validate_packet_notice_step.ts`
7. `packages/backend-authority/src/services/resolve_filing_notices.ts`
8. `packages/backend-authority/src/services/validate_filing_notice_resolution.ts`
9. `packages/backend-authority/src/services/apply_notice_resolution_to_filing_packet.ts`
10. `packages/backend-authority/src/services/project_packet_notice_resolution_view_model.ts`

Frontend / browser verification:
11. `apps/client-portal-web/src/routes/approvals/final-declaration-confirmation.tsx` or the nearest existing approvals route module
12. `apps/client-portal-web/src/components/approvals/PacketNoticeStack.tsx`
13. `apps/client-portal-web/src/components/approvals/NoticeAcknowledgementCard.tsx`
14. `apps/client-portal-web/src/components/approvals/PacketPromotionBlockNotice.tsx`
15. `tests/playwright/client_portal/final_declaration_notice_resolution.spec.ts`
16. `tests/playwright/client_portal/final_declaration_notice_resolution_a11y.spec.ts`

Docs / persistence:
17. `db/migrations/phase03_0143_packet_notice_steps_and_resolution.sql`
18. `docs/authority/filing_notice_step_and_resolution_flow.md`
19. `tests/unit/backend_authority/filing_notice_step_contract.spec.ts`
20. `tests/unit/backend_authority/filing_notice_resolution_contract.spec.ts`
21. `tests/integration/backend_authority/packet_notice_resolution_and_promotion.spec.ts`

Required design decisions you must make explicit:
- Define the canonical ordered derivation of `FilingNoticeStep.step_code in {DECLARED_BASIS_ACK_REQUIRED, DISCLAIMER_ACK_REQUIRED, PACKET_APPROVAL_REQUIRED}` from:
  - packet-phase `approval_state`,
  - packet-phase `declared_basis_ack_state`,
  - any packet-local required approvals,
  - current actor and runtime scope.
- Define stable `notice_step_id` generation and stable step order so replay, packet reuse, and UI rendering cannot reshuffle the notice stack.
- Define the lifecycle of each step:
  - `PENDING` => `resolved_at = null`
  - `SATISFIED` or `UNSATISFIABLE` => typed `resolved_at`
- Define how `FilingNoticeResolution` mirrors the step set:
  `notice_refs[]` must mirror ordered `notice_step_refs[]`; `notice_requirements_satisfied` must agree with component states; unresolved reason codes must be non-empty whenever the resolution is unsatisfied.
- Define the packet promotion rule:
  `APPROVED_TO_SUBMIT` is illegal unless the packet carries resolved packet-phase approval / declaration-basis acknowledgement and a non-null `notice_resolution_ref` whenever notice steps exist.
- Define stale / supersession behavior:
  if the packet, calculation basis, declaration pack, or approval context rebases, the old notice-resolution posture must become read-only history and must not remain signable.
- Define the view-model contract between backend and portal route so the UI never recomputes notice legality locally.

Execution steps:
1. Implement schema-backed models / repositories for `FilingNoticeStep` and `FilingNoticeResolution`.
2. Implement `derive_packet_notice_steps.ts` to emit the exact packet-local steps only **after** `BUILD_FILING_PACKET(...)`.
   Do not allow pre-packet or trust-layer code to mint these steps.
3. Implement `resolve_filing_notices.ts` and `validate_filing_notice_resolution.ts` so the resolution artifact remains the only lawful source of packet-local notice outcome.
4. Implement packet mutation logic so resolved notice posture is copied onto `FilingPacket` fields, including `approval_state`, `declared_basis_ack_state`, ordered `notice_step_refs[]`, and `notice_resolution_ref`.
5. Update the approvals route from `pc_0141` so packet-local notices appear as one compact, readable requirements section instead of scattered inline warnings.
6. Use Playwright from the start to drive:
   - notice-free flows,
   - declared-basis acknowledgement required,
   - disclaimer acknowledgement required,
   - packet approval required,
   - unsatisfiable / denied / blocked states,
   - stale / rebased packet state.

Gap handling you must perform:
- Close the gap where packet-local notices could be recomputed by UI or packet gate instead of coming from persisted artifacts.
- Close the gap where `FilingNoticeStep.packet_refs[]` or `FilingNoticeResolution.notice_refs[]` could drift from the owning packet / ordered step set.
- Close the gap where satisfied and unsatisfied resolution states could serialize contradictory component states.
- Close the gap where a rebased approval context could leave the old route visually signable because notice posture was not tied to packet identity and stale protection.
- Close the gap where the portal route could either hide notice requirements entirely or drown the user in generic banners instead of one deliberate review stack.

Mandatory edge cases:
- no declaration-basis notice exists => `declared_basis_ack_state = NOT_APPLICABLE` must remain legal and explicit
- unsatisfied resolution must carry non-empty `unresolved_reason_codes[]`
- any `approval_state in {REQUIRED_PENDING, UNSATISFIABLE, DENIED}` must force unsatisfied resolution
- any `declared_basis_ack_state in {REQUIRED_PENDING, UNSATISFIABLE}` must force unsatisfied resolution
- a packet with notice steps must not become `APPROVED_TO_SUBMIT` without non-null `notice_resolution_ref`
- stale portal view or rebased packet must block sign-off and surface a calm recovery prompt rather than silently reusing old acknowledgement state

Premium UI and interaction specification you must follow:
- This is a refinement of the premium legal-review workspace created in `pc_0141`, not a new page archetype.
- Insert the notice surface immediately after the approval summary and before the change digest.
- Use one restrained section titled `Review requirements`.
- Render each notice as a horizontally padded card with:
  - left status rail (`3px` to `4px`)
  - compact title
  - one-line explanation
  - optional disclosure for legal detail
  - explicit acknowledgement control when user action is required
- Keep the route calm:
  - no full-width red alert wall
  - no stacked toast pile
  - no modal detours for simple acknowledgement
- Colors:
  - calm informational requirement: surface `#FFFFFF`, info accent `#1D4ED8`
  - acknowledgement pending: warning accent `#B7791F`
  - blocked / unsatisfiable: danger accent `#C2410C`
- Layout:
  - section width follows the dominant content column
  - card radius `18px`
  - gap `12px` between cards
  - sticky summary bar from `pc_0141` remains intact
- Motion:
  - disclosure transitions only
  - `140ms` to `180ms`
  - reduced-motion safe
- If no packet notices exist, keep a compact satisfied state row rather than leaving a visual gap.

Semantic selectors and accessibility requirements:
- add stable semantic hooks / test ids for at least:
  `packet-notice-stack`
  `packet-notice-card`
  `packet-notice-title`
  `packet-notice-detail-toggle`
  `packet-notice-acknowledge`
  `packet-notice-blocked`
  `packet-notice-satisfied`
  `packet-notice-resolution-summary`
  `packet-promotion-block-notice`
- prefer role / label / accessible-name locators over CSS selectors in Playwright
- headings and landmark structure must preserve the dominant legal-review order
- blocked / stale notices must move focus lawfully without stealing focus from active acknowledgement controls

Playwright requirements:
- use Playwright during implementation, not as a final bolt-on
- use semantic locators only
- no fixed sleeps; rely on actionability and auto-waiting
- capture traces and screenshots on retry / failure
- verify desktop and mobile widths
- prove that stale packets and unresolved notice posture keep `approval-submit` disabled

Backend, performance, and security requirements:
- fail closed when step order, packet binding, or resolution logic becomes contradictory
- keep packet mutation and notice-resolution persistence atomic where packet promotion depends on them
- do not let frontend state or query params become the source of truth for acknowledgement legality
- preserve audit lineage for notice derivation, satisfaction, unsatisfiable outcomes, and stale / superseded replacement

Validation rules:
- validate every step and resolution against their schemas
- unit-test derivation order, lifecycle / `resolved_at` legality, resolution truth table, and packet-binding invariants
- integration-test packet build -> notice derivation -> notice resolution -> promotion legality
- Playwright-test the route behavior and accessibility landmarks for required / satisfied / blocked / stale states

Definition of done:
- packet-local notice derivation and resolution now exist as durable first-class artifacts
- packet promotion to `APPROVED_TO_SUBMIT` now consumes persisted notice resolution rather than UI-local or gate-local inference
- the client portal shows notice posture in one calm, premium, replay-safe review section
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
