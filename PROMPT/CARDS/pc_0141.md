---
id: pc_0141
slug: phase_03_parallel_wave_01_track_backend_authority_009_implement_authority_calculation_request_result_and_user_confirmation_flow
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "009"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0141

## Task Summary

implement authority calculation request result and user confirmation flow.

## Metadata

- ID: `pc_0141`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `009`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_009_implement_authority_calculation_request_result_and_user_confirmation_flow`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0140](./pc_0140.md) `phase_03_parallel_wave_01_track_backend_authority_008_implement_authority_interaction_record_and_reconciliation_budget_engine`
- Next: [pc_0142](./pc_0142.md) `phase_03_parallel_wave_01_track_backend_authority_010_implement_out_of_band_authority_correction_and_truth_propagation_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0134_to_0141.md](../shared_operating_contract_0134_to_0141.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_009_implement_authority_calculation_request_result_and_user_confirmation_flow`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0140` if they exist, especially `pc_0014`, `pc_0015`, `pc_0016`, `pc_0028`, `pc_0031`, `pc_0038`, `pc_0040`, `pc_0054`, `pc_0058`, `pc_0061`, `pc_0066`, `pc_0067`, `pc_0068`, `pc_0074`, `pc_0076`, `pc_0082`, `pc_0086` through `pc_0093`, `pc_0097`, `pc_0104`, `pc_0108`, `pc_0118` through `pc_0127`, `pc_0133`, and `pc_0134` through `pc_0140`.

Mission:
Implement the full authority-calculation handshake and the user confirmation flow for filing-capable authority journeys, including typed request/result/basis/confirmation/readiness artifacts and a customer-safe, portal-native final-declaration confirmation route.
This task must make the calculation journey explicit, replay-safe, stale-protected, and ready for packet build without allowing the UI or downstream filing logic to reconstruct readiness from loose refs.

Source algorithm grounding you must honor:
- `authority_calculation_contract.md`, especially `AuthorityCalculationRequest`, `AuthorityCalculationResult`, `CalculationBasis`, `CalculationUserConfirmation`, and `AuthorityCalculationReadinessContext`.
- `modules.md`, especially `EXECUTE_AUTHORITY_CALCULATION_FLOW(...)` and `AUTHORITY_PREFLIGHT(...)`.
- `authority_interaction_protocol.md`, especially the final declaration calculation handshake and authority preflight requirements.
- `data_model.md`, especially the calculation artifacts and the `ClientPortalWorkspace.approval_center` / route-order rules.
- `customer_client_portal_experience_contract.md`, especially the `Approvals` route contract.
- `frontend_shell_and_interaction_law.md`, especially the approval route order and same-shell declaration/sign-off rules.
- `low_noise_experience_contract.md`, `semantic_selector_and_accessibility_contract.md`, and `semantic_selector_and_accessibility_regression_pack_contract.md`.
- `PATCH_RESOLUTION_INDEX.md`, especially entries 369 through 381 covering calculation request/result/basis/confirmation invariants.
- `test_vectors.md`, especially `TV-04` and related final-declaration / parity / readiness vectors.
- current HMRC final declaration guidance only as provider-specific sharpening for trigger/retrieve/show/confirm/submit flow.

Deliverables to create:
Backend:
1. `packages/backend-authority/src/models/authority_calculation_request.ts`
2. `packages/backend-authority/src/models/authority_calculation_result.ts`
3. `packages/backend-authority/src/models/calculation_basis.ts`
4. `packages/backend-authority/src/models/calculation_user_confirmation.ts`
5. `packages/backend-authority/src/models/authority_calculation_readiness_context.ts`
6. `packages/backend-authority/src/repositories/authority_calculation_request_repository.ts`
7. `packages/backend-authority/src/repositories/authority_calculation_result_repository.ts`
8. `packages/backend-authority/src/repositories/calculation_basis_repository.ts`
9. `packages/backend-authority/src/repositories/calculation_user_confirmation_repository.ts`
10. `packages/backend-authority/src/repositories/authority_calculation_readiness_context_repository.ts`
11. `packages/backend-authority/src/services/trigger_authority_calculation.ts`
12. `packages/backend-authority/src/services/retrieve_authority_calculation_result.ts`
13. `packages/backend-authority/src/services/build_calculation_basis.ts`
14. `packages/backend-authority/src/services/record_calculation_user_confirmation.ts`
15. `packages/backend-authority/src/services/build_authority_calculation_readiness_context.ts`
16. `packages/backend-authority/src/services/verify_calculation_handshake_integrity.ts`
17. `packages/backend-authority/src/services/project_calculation_confirmation_view_model.ts`

Frontend / shared UI:
18. `apps/client-portal-web/src/routes/approvals/final-declaration-confirmation.tsx` or the nearest existing approvals route module
19. `apps/client-portal-web/src/components/approvals/ApprovalSummaryPanel.tsx`
20. `apps/client-portal-web/src/components/approvals/ChangeDigestPanel.tsx`
21. `apps/client-portal-web/src/components/approvals/DeclarationPanel.tsx`
22. `apps/client-portal-web/src/components/approvals/CalculationIdentityStrip.tsx`
23. `apps/client-portal-web/src/components/approvals/SignOffPanel.tsx`
24. `packages/shared-ui/src/components/lineage/CalculationLineageRibbon.tsx` if a shared lineage strip is introduced
25. `packages/shared-ui/src/components/feedback/InlineCheckpointNotice.tsx` or reuse an existing pattern
26. `tests/playwright/client_portal/final_declaration_confirmation.spec.ts`
27. `tests/playwright/client_portal/final_declaration_confirmation_a11y.spec.ts`
28. `db/migrations/phase03_0141_authority_calculation_request_result_confirmation.sql`
29. `docs/authority/authority_calculation_request_result_and_user_confirmation_flow.md`

Required design decisions you must make explicit:
- Define the boundary between modeled and live calculation posture.
  `live_authority_call_executed = false` must force modeled posture and clear live authority refs/hashes/confirmation reuse.
- Define the exact legality of `runtime_scope[]` for calculations.
  `submit` and `amendment_submit` are forbidden here.
  filing-preparation and intent-to-amend scopes must remain distinct.
- Define how `CalculationBasis` becomes reusable:
  `parity_reusable` and `filing_reusable` may be true only when `basis_status = CONFIRMED`.
- Define how `CalculationUserConfirmation` behaves:
  `CONFIRMED` requires empty reason codes;
  non-empty reason codes imply `DECLINED`;
  declined/pending confirmation must never serialize a confirmed basis hash.
- Define the exact shape of `AuthorityCalculationReadinessContext` and how it is copied onto `FilingCase` / `FilingPacket`.
- Define the portal route-level stale-protection rules so a superseded calculation, superseded basis, changed declaration pack, or changed approval context blocks sign-off.

Execution steps:
1. Implement the five schema-backed calculation artifacts and repositories.
2. Implement the full trigger/retrieve/basis/confirmation/readiness services with strict schema validation and deterministic exact-decimal hash generation.
3. Implement `verify_calculation_handshake_integrity.ts` so packet build and sign-off both re-check the active request/result/basis/confirmation tuple before treating the calculation as filing-ready.
4. Persist calculation refs plus `readiness_context_ref` on `FilingCase` before packet build and copy the active context onto `FilingPacket` when packet construction occurs.
5. Build a customer-safe approvals route that preserves the canonical portal order:
   `APPROVAL_SUMMARY -> CHANGE_DIGEST -> DECLARATION_PANEL -> SIGN_OFF_PANEL`.
6. Add a compact `CalculationIdentityStrip` or `CalculationLineageRibbon` near the top of the route that shows:
   tax year / period context,
   calculation freshness,
   whether the result is live or modeled,
   confirmation status,
   and stale-protection state.
   Keep it restrained; it is a lineage cue, not a dashboard.
7. Keep declaration text readable, previewable, downloadable, and printable, with current declaration materials clearly separated from superseded or receipt artifacts.
8. Keep sign-off, acknowledgement, inline step-up checkpoint posture, pending signature command state, and receipt state on the same route without route-breaking detours.
9. Verify the route and all interactive states with Playwright from the beginning of implementation, not as a late afterthought.

Premium UI and interaction specification you must follow:
- Route posture: premium utility, calm legal-review workspace, not a generic “AI dashboard”
- Background: `#F7F5F0`
- Surfaces: `#FFFFFF`
- Primary text: `#171717`
- Secondary text: `#667085`
- Accent action color: `#0F766E`
- Informational highlight: `#1D4ED8`
- Warning / stale state: `#B7791F`
- Critical / blocked state: `#C2410C`
- Max content width: `1280px`
- Top context bar:
  - sticky
  - height `60px`
  - contains client / obligation / tax-year context and current calculation freshness chip
- Main body:
  - one dominant content column (`minmax(0, 1fr)`)
  - one restrained side rail (`320px` to `360px`) for status / next-step summary on desktop
  - collapse the rail below the main column on smaller widths
- Reading order must remain exactly:
  1. `APPROVAL_SUMMARY`
  2. `CHANGE_DIGEST`
  3. `DECLARATION_PANEL`
  4. `SIGN_OFF_PANEL`
- `APPROVAL_SUMMARY`:
  - concise plain-language explanation of what the user is being asked to review
  - shows whether this is a final declaration or another authority-calculation-backed approval
  - includes the compact lineage strip
- `CHANGE_DIGEST`:
  - one concise summary card followed by a collapsible detail section
  - must still render a calm “No material changes” posture rather than disappearing
- `DECLARATION_PANEL`:
  - readable declaration text with a premium document feel
  - separate preview/download/print actions
  - current declaration material must be visually distinct from any superseded pack or receipt artifact
- `SIGN_OFF_PANEL`:
  - show acknowledgement progress explicitly
  - show step-up checkpoint inline when required
  - disable final sign/submit action until calculation freshness, acknowledgement, and confirmation posture are all lawful
  - after command submission, show pending posture distinct from final receipt posture
- Use only restrained motion:
  opacity/translate transitions `140ms` to `180ms`, reduced-motion safe
- No carousel, no hero illustration, no metric tiles, no decorative charts
- If you include a diagram at all, use one slim horizontal lineage ribbon only

Semantic selectors and accessibility requirements:
- Add stable semantic hooks / test ids for at least:
  `approval-summary`
  `calculation-identity-strip`
  `change-digest`
  `change-digest-details-toggle`
  `declaration-panel`
  `declaration-download`
  `declaration-print`
  `sign-off-panel`
  `calculation-confirm-checkbox`
  `calculation-stale-notice`
  `calculation-modeled-notice`
  `approval-submit`
  `approval-step-up-inline`
  `approval-pending-receipt`
  `approval-final-receipt`
- Prefer role/label/accessible-name locators over brittle CSS in Playwright.
- Make headings and landmarks explicit.
- Focus must follow visible semantic order.
- Route changes or stale-protection notices must move focus lawfully without stealing focus from active controls.

Playwright requirements:
- Use Playwright during implementation to drive the route in desktop and mobile widths.
- Use semantic locators only.
- No fixed sleeps; rely on auto-waiting / actionability.
- Capture traces and screenshots on retry/failure.
- Test keyboard-only traversal.
- Test reduced-motion mode.
- Test stale-calculation and superseded-pack scenarios.
- Test inline step-up checkpoint appearance/removal.
- Test the difference between pending sign command and final receipt state.
- Test recovery after reconnect / reload so the route returns to the same object context and same surface order.
- If equivalent browser automation is additionally used, Playwright still remains mandatory as the canonical verification suite.

Gap handling you must perform:
- Close the gap where a calculation result could exist without a typed basis, confirmation, and readiness context.
- Close the gap where modeled results could accidentally serialize as filing-ready or authority-cleared.
- Close the gap where superseded calculations or bases could remain selectable in the active confirmation flow.
- Close the gap where customer sign-off UI could become a generic document-signing screen divorced from calculation freshness and readiness.
- Close the gap where declaration preview/download/print could silently target stale or superseded materials.
- Close the gap where the same-shell approval route could fragment into extra detour routes for step-up, acknowledgement, or receipt.

Mandatory edge cases:
- final declaration flow must execute the authority calculation path before filing packet preparation
- calculation request `runtime_scope[]` must never contain `submit` or `amendment_submit`
- live calculation forbidden in analysis mode unless provider contract explicitly allows that read-only path
- `AuthorityCalculationResult.result_state = MODELED` must never serialize `validation_outcome = PASS`
- `CalculationBasis.parity_reusable` / `filing_reusable` must be false unless `basis_status = CONFIRMED`
- `CalculationUserConfirmation.confirmation_state = CONFIRMED` must have empty reason codes
- non-empty confirmation reason codes must imply `DECLINED`
- declined/pending confirmation must never expose a confirmed basis hash to packet build or UI
- `AuthorityCalculationReadinessContext.validation_outcome in {PASS, PASS_WITH_NOTICE}` requires live retrieved calculation + confirmed basis hash + confirmed user confirmation + reusable posture
- if a calculation or approval pack becomes stale while the user is on the page, the sign-off control must lock and an inline stale notice must appear without route-breaking detours
- pending sign-command state must remain distinct from final receipt state

Backend, performance, and security requirements:
- fail closed on stale or inconsistent calculation handshake artifacts
- keep exact-decimal formatting and calculation hashing deterministic
- never expose staff-only diagnostic details or raw provider payloads in the customer-facing route
- keep the route driven by server-authored status / readiness projections rather than client-only inference
- do not let downloads or prints widen masking or historical-selection posture compared with what the route lawfully exposes

Validation rules:
- validate all five calculation artifacts against their canonical schemas
- unit-test modeled vs live posture, runtime-scope legality, exact-decimal calculation hash stability, basis reusability rules, confirmation-state invariants, and readiness-context construction
- integration-test final-declaration calculation trigger/retrieve/confirm/readiness continuity through packet build handoff
- Playwright-test primary happy path, stale path, superseded path, step-up path, pending-sign path, final-receipt path, reload/reconnect path, and reduced-motion accessibility path
- prove that declaration preview/download/print never target superseded materials by accident
- prove that the route preserves canonical surface order and same-shell continuity

Definition of done:
- the repo now has a complete typed authority-calculation handshake plus customer confirmation flow
- filing-capable authority journeys rely on typed request/result/basis/confirmation/readiness artifacts instead of loose refs
- the client portal exposes a premium, calm, same-shell confirmation experience that is Playwright-verified and aligned to the algorithm’s route law
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
