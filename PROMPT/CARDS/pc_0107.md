---
id: pc_0107
slug: phase_03_parallel_wave_01_track_backend_manifest_011_implement_replay_attestation_and_deterministic_outcome_hash_persistence
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "011"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_011
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0107

## Task Summary

implement replay attestation and deterministic outcome hash persistence.

## Metadata

- ID: `pc_0107`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `011`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_011_implement_replay_attestation_and_deterministic_outcome_hash_persistence`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0106](./pc_0106.md) `phase_03_parallel_wave_01_track_backend_manifest_010_implement_release_candidate_identity_contract_and_release_candidate_hashing`
- Next: [pc_0108](./pc_0108.md) `phase_03_parallel_wave_01_track_backend_manifest_012_implement_manifest_reuse_recovery_replay_and_supersession_decision_service`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0102_to_0109.md](../shared_operating_contract_0102_to_0109.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_011_implement_replay_attestation_and_deterministic_outcome_hash_persistence`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0106` if they exist, especially `pc_0010`, `pc_0017`, `pc_0027`, `pc_0061`, `pc_0064`, `pc_0067`, `pc_0076`, `pc_0077`, `pc_0080`, `pc_0084`, `pc_0097`, `pc_0098`, `pc_0100`, `pc_0101`, `pc_0105`, and `pc_0106`.

Mission:
Implement deterministic outcome hashing and replay-attestation persistence so exact replay, audit replay, and limited historical comparison all publish one durable comparison artifact with typed basis validation state.
This task must not invent a fuzzy replay report.
It must persist a machine-usable attestation that later replay, audit, and release logic can trust.

Source algorithm grounding you must honor:
- `modules.md`, especially `COMPUTE_DETERMINISTIC_OUTCOME_HASH(...)`, `COMPARE_REPLAY_OUTCOMES(...)`, and `PERSIST_REPLAY_ATTESTATION(...)`.
- `replay_and_reproducibility_contract.md`, especially exact replay / audit replay semantics, persisted historical basis loading, fail-closed missing/corrupt basis behavior, and comparison posture.
- `manifest_and_config_freeze_contract.md`, especially deterministic outcome hash contract, append-only post-seal projections, and hash exclusion rules.
- `data_model.md`, especially `ReplayAttestation`, `DecisionBundle`, `RunManifest.append_only_outcome_projection`, and release/replay-related fields.
- `verification_and_release_gates.md`, especially where replay comparison outcomes become admissibility evidence.
- Existing schemas including `replay_attestation.schema.json`, `decision_bundle.schema.json`, `run_manifest.schema.json`, and relevant sample payloads.

Deliverables to create:
1. `packages/backend-manifest/src/hash/deterministic_outcome_hash.ts`.
2. `packages/backend-manifest/src/services/normalize_material_outcome_surface.ts`.
3. `packages/backend-manifest/src/services/build_replay_attestation.ts`.
4. `packages/backend-manifest/src/services/persist_replay_attestation.ts`.
5. `packages/backend-manifest/src/services/replay_basis_integrity_contract_builder.ts`.
6. `packages/backend-manifest/src/services/outcome_component_inventory_builder.ts`.
7. `packages/backend-manifest/src/services/replay_comparison_classifier.ts`.
8. `docs/manifest/replay_attestation_and_deterministic_outcome_hash.md`.
9. `tests/unit/backend_manifest/deterministic_outcome_hash.spec.ts`.
10. `tests/integration/backend_manifest/replay_attestation_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the exact normalized material-outcome surface that participates in `deterministic_outcome_hash`.
  It must include every gate and material artifact that affects legal/compliance meaning while excluding persistence-only ids, queue ids, and write-time noise.
- Define how replay comparison posture maps into `comparison_mode`, `basis_validation_state`, and `outcome_class`.
- Define the exact prerequisite set for attestation persistence and when a replay child is allowed to expose replay posture publicly.
- Define how limited historical comparison, counterfactual comparison, missing dependencies, retention limits, schema incompatibility, and build unavailability are represented.
- Define how the manifest and attestation remain synchronized without creating two independent truths.

Execution steps:
1. Implement `normalize_material_outcome_surface.ts` and `outcome_component_inventory_builder.ts` so deterministic outcome hash inputs are canonical and explicitly enumerated.
2. Implement `deterministic_outcome_hash.ts` on top of the canonical hash substrate from earlier cards.
3. Implement `replay_basis_integrity_contract_builder.ts` so replay basis health is assessed before comparison is attempted.
4. Implement `replay_comparison_classifier.ts` and `build_replay_attestation.ts` so comparison results map into the exact schema enums with typed rationale.
5. Implement `persist_replay_attestation.ts` so the attestation is durably stored and the manifest's replay attestation reference updates atomically with any required mirrored outcome fields.
6. Add tests for exact match, expected equivalence, expected difference, limited comparison, missing dependency, corrupt basis, schema incompatibility, build unavailable, and unexpected mismatch.
7. Add tests proving that the same material outcome yields the same deterministic outcome hash despite different persistence ids or write times.

Gap handling you must perform:
- Close the gap where replay outcome comparability exists only as prose and schema enums but not as executable persistence code.
- Close the gap where deterministic outcome hash could accidentally include persistence noise.
- Close the gap where replay posture might be exposed before a durable attestation exists.
- Close the gap where limited historical comparison and basis-corrupt states could be collapsed into generic failure.
- Close the gap where material outcome surface is implicit rather than explicitly inventoried.

Mandatory edge cases:
- exact replay with exact historical basis and identical material outcome produces `EXACT_MATCH`.
- counterfactual or declared-variant replay can produce `EXPECTED_EQUIVALENCE` or `EXPECTED_DIFFERENCE` with explicit rationale.
- missing historical dependency produces `basis_validation_state = MISSING_DEPENDENCY`.
- retention-limited historical basis produces `RETENTION_LIMITED`.
- corrupt retained artifact produces `CORRUPT`.
- schema-incompatible historical basis produces `SCHEMA_INCOMPATIBLE`.
- build-unavailable replay environment produces `BUILD_UNAVAILABLE`.
- changed persistence ids, queue ids, or write timestamps alone must not change `deterministic_outcome_hash`.
- changed material gate or artifact content must change `deterministic_outcome_hash`.
- a replay child must not expose replay posture until `ReplayAttestation` is durably persisted and referenced by the manifest.

Backend, performance, and security requirements:
- Outcome normalization and hashing must be deterministic and pure.
- Never log raw retained artifacts, raw credentials, or full sensitive payload bodies when building attestation errors.
- Keep attestation persistence append-only and audit-friendly.
- Reuse previously loaded frozen and post-seal basis where available; do not trigger ambient live reads in replay comparison code.
- Ensure attestation persistence is transaction-safe and cannot leave the manifest half-synchronized.

Validation rules:
- Unit-test deterministic outcome hash normalization and schema enum mapping.
- Integration-test replay attestation persistence and manifest synchronization.
- Validate persisted attestation payloads against `replay_attestation.schema.json`.
- Prove that persistence-only ids and timestamps do not influence the deterministic outcome hash.
- Prove that replay posture cannot be observed publicly before durable attestation persistence.

Definition of done:
- The repo has one canonical deterministic outcome hashing path and one authoritative replay-attestation persistence path.
- Replay comparability becomes durable machine truth instead of transient controller output.
- Later replay, audit, and release-evidence tasks can trust persisted attestation artifacts and stable outcome hashes.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
