---
id: pc_0125
slug: phase_03_parallel_wave_01_track_backend_compute_005_implement_trust_summary_formula_engine_and_threshold_stability_analysis
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "005"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0125

## Task Summary

implement trust summary formula engine and threshold stability analysis.

## Metadata

- ID: `pc_0125`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `005`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_005_implement_trust_summary_formula_engine_and_threshold_stability_analysis`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0124](./pc_0124.md) `phase_03_parallel_wave_01_track_backend_compute_004_implement_parity_result_computation_and_authority_comparison_logic`
- Next: [pc_0126](./pc_0126.md) `phase_03_parallel_wave_01_track_backend_compute_006_implement_gate_decision_record_model_and_ordered_gate_evaluation_engine`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0118_to_0125.md](../shared_operating_contract_0118_to_0125.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_005_implement_trust_summary_formula_engine_and_threshold_stability_analysis`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0124` if they exist, especially `pc_0010`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0074`, `pc_0076`, `pc_0086` through `pc_0093`, `pc_0097`, `pc_0101`, `pc_0117`, `pc_0121`, `pc_0123`, and `pc_0124`.

Mission:
Implement the `TrustSummary` model, repository, lifecycle helper, trust-input-basis builder, trust synthesis service, and threshold-sensitivity analyzer that convert frozen compute/risk/parity/graph/gate posture into one authoritative trust artifact.
This task must preserve the split between score band and non-score cap band, enforce threshold-stability behavior, persist the exact `trust_input_basis_contract` and `trust_sensitivity_analysis_contract`, and keep automation/readiness ceilings legally aligned with the frozen input basis and upstream gate cap.

Source algorithm grounding you must honor:
- `core_engine.md`, especially `ASSESS_TRUST_INPUT_STATE(...)`, `SYNTHESIZE_TRUST(...)`, and the `TRUST_GATE` handoff.
- `modules.md`, especially the place of trust in the post-seal graph and the explicit dependency on `TrustSummary`.
- `compute_parity_and_trust_formulas.md`, especially section `8.1 TrustSummary artifact binding` and the trust formulas/guard bands/sensitivity analyzer sections.
- `trust_sensitivity_and_threshold_stability_contract.md` in full.
- `exact_gate_logic_and_decision_tables.md`, especially the trust decision table and filing-readiness bridge.
- `data_model.md`, especially `TrustSummary`, `trust_input_basis_contract`, and `trust_sensitivity_analysis_contract`.
- `state_machines.md`, especially section `6.11 TrustSummary.lifecycle_state ...`.
- `test_vectors.md`, especially `TV-56`, `TV-56A`, `TV-56B`, `TV-56C`, `TV-56D`, `TV-56E`, `TV-56F`, and `TV-56G`.
- Existing schemas including `trust_summary.schema.json`, `trust_input_basis_contract.schema.json`, `trust_sensitivity_contract.schema.json`, and `sample_trust_sensitivity_analysis_contract.json`.

Deliverables to create:
1. `packages/backend-compute/src/models/trust_summary.ts`.
2. `packages/backend-compute/src/repositories/trust_summary_repository.ts`.
3. `packages/backend-compute/src/services/transition_trust_summary.ts`.
4. `packages/backend-compute/src/services/assess_trust_input_state.ts`.
5. `packages/backend-compute/src/services/build_trust_input_basis_contract.ts`.
6. `packages/backend-compute/src/services/threshold_stability_analyzer.ts`.
7. `packages/backend-compute/src/services/trust_sensitivity_analyzer.ts`.
8. `packages/backend-compute/src/services/synthesize_trust.ts`.
9. `packages/backend-compute/src/services/build_trust_reason_codes.ts`.
10. `db/migrations/phase03_0125_trust_summary.sql`.
11. `docs/compute/trust_summary_formula_engine_and_threshold_stability_analysis.md`.
12. `tests/unit/backend_compute/trust_summary_formula_engine.spec.ts`.
13. `tests/integration/backend_compute/trust_summary_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the evidence-graph / graph-quality dependency boundary.
  If the evidence graph is not fully implemented yet, trust synthesis must consume a typed injected basis and fail closed when filing-capable trust requires it.
- Define how `trust_input_basis_contract` is assembled from freshness, baseline posture, authority progression, late-data invalidation, override dependency, and required human steps.
- Define how `score_band`, `cap_band`, and `trust_band` are derived and serialized without contradiction.
- Define how `score_cap_alignment_state`, `cap_driver_reason_codes[]`, and `edge_trigger_codes[]` are determined.
- Define exactly how the six canonical perturbation probes are computed and ordered.

Execution steps:
1. Implement the schema-backed `TrustSummary` model, repository, and lifecycle helper.
2. Implement `assess_trust_input_state.ts` and `build_trust_input_basis_contract.ts`.
3. Implement `threshold_stability_analyzer.ts` so guard-band margins and `EDGE_REVIEW` posture are deterministic and reason-coded.
4. Implement `trust_sensitivity_analyzer.ts` so it emits exactly the canonical six probes in canonical order and preserves monotonicity expectations.
5. Implement `build_trust_reason_codes.ts` and `synthesize_trust.ts` so score, cap, automation, readiness, reasons, explainability, and blocking dependencies are derived from frozen inputs only.
6. Add tests for green-score-but-review-cap, stale-basis ceiling, threshold-edge review, invalid override reliance, freshness invalidation, and live-authority guard-band behavior.

Gap handling you must perform:
- Close the gap where trust depends on graph quality and evidence-graph refs that may not yet be fully materialized in the roadmap.
  Use a typed injected graph-quality basis and fail closed when filing-capable trust requires it.
- Close the gap where `score_band` could diverge silently from `cap_band`.
  Persist `score_cap_alignment_state` and non-empty `cap_driver_reason_codes[]` whenever the cap is stricter than the score.
- Close the gap where threshold fragility might otherwise remain implicit.
  Persist exact margins and the exact active `edge_trigger_codes[]`, and emit `TRUST_THRESHOLD_EDGE_REVIEW` when required.
- Close the gap where UI or reviewers could invent local what-if logic.
  Persist the full six-probe `trust_sensitivity_analysis_contract` and require downstream consumers to read it directly.

Mandatory edge cases:
- a numeric green trust score cannot outrank `upstream_gate_cap = REVIEW_ONLY` or `BLOCKED`.
- stale or authority-limited trust input basis must cap automation/readiness even when the score-only inputs are green.
- `threshold_stability_state = EDGE_REVIEW` must cap the final trust posture to at most `AMBER` and prevent `READY_TO_SUBMIT`.
- the six perturbation probes must appear exactly once each and in canonical order.
- `INVALID_OVERRIDE_RELIED_UPON` must collapse to contradicted or insufficient-data posture with blocked automation.
- `FRESHNESS_INVALIDATED` must add `TRUST_INPUT_STALE` and `TRUST_RECALCULATION_REQUIRED`.
- live authority review/block guard bands must populate the corresponding authority margin fields and edge-trigger codes.
- `automation_level` and `filing_readiness` must obey the exact ordinal bridge and the ceilings in `trust_input_basis_contract`.
- `SYNTHESIZED -> SUPERSEDED` transitions must be legal and auditable.

Backend, performance, and security requirements:
- Keep trust synthesis deterministic and side-effect free except for explicit artifact persistence.
- Never recompute or reinterpret trust from UI-only summaries.
- Reuse upstream artifact refs and frozen formulas; do not poll authority state or connector state inside trust synthesis.
- Do not log sensitive financial details, authority payloads, or evidence content beyond bounded reason-coded diagnostics.

Optional internal verification surface (only if you add one):
- If you add a read-only threshold atlas, place it behind an internal route such as `/internal/decision/trust/:trustId/threshold-atlas`.
- The page must be quiet and premium: a narrow left rail for current posture (`score_band`, `cap_band`, `trust_band`, `automation_level`, `filing_readiness`), a center column showing the threshold ladder and current margins, and a right column with six compact probe cards in canonical order.
- Use one restrained diagram only: a horizontal cap-vs-score ladder with active guard-band surfaces called out as small inline markers; no radial gauges, 3D charts, or generic analytics chrome.
- Colors must stay disciplined: neutral ivory background, charcoal copy, blue accent for stable/current, amber for edge review, red for blocking. Motion must be subtle and reduced-motion-safe.
- Any such surface must be implemented and tested with Playwright using user-facing locators, actionability-safe interactions, keyboard support, reduced-motion assertions, and trace capture.

Validation rules:
- Unit-test trust-input-basis assembly, score/cap/trust-band derivation, threshold-stability behavior, six-probe sensitivity generation, and lifecycle legality.
- Integration-test `TrustSummary` persistence from frozen compute/risk/parity/gate inputs and injected graph-quality basis.
- Validate persisted artifacts against `trust_summary.schema.json`, `trust_input_basis_contract.schema.json`, and `trust_sensitivity_contract.schema.json`.
- Compare representative sensitivity output against `sample_trust_sensitivity_analysis_contract.json` structure and semantics.
- Prove that downstream consumers can read threshold fragility and cap explanation without recomputing trust locally.
- Re-run the contract validators after the implementation bundle is assembled.

Definition of done:
- The repo can emit a schema-valid `TrustSummary` with explicit trust-input basis and threshold-sensitivity contracts.
- Score-versus-cap divergence, edge-review posture, and automation/readiness ceilings are now explicit and replay-safe.
- Later trust gating, filing, nightly selection, and explanation surfaces can consume one authoritative trust artifact instead of inventing local heuristics.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
