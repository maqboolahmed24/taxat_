---
id: pc_0076
slug: phase_02_seq_076_create_audit_event_publishing_pipeline_and_append_only_writer_scaffold
mode: sequential
phase: "02"
wave: null
track: null
task_number: "076"
block_key: phase_02_seq
original_key: phase_02_seq_076
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0076

## Task Summary

create audit event publishing pipeline and append only writer scaffold.

## Metadata

- ID: `pc_0076`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `076`
- Source Slug: `phase_02_seq_076_create_audit_event_publishing_pipeline_and_append_only_writer_scaffold`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0075](./pc_0075.md) `phase_02_seq_075_create_open_telemetry_bootstrap_and_correlation_context_propagation_baseline`
- Next: [pc_0077](./pc_0077.md) `phase_02_seq_077_create_synthetic_seed_data_fixture_pack_and_canonical_domain_examples`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0070_to_0077.md](../shared_operating_contract_0070_to_0077.md)
```text
Execute roadmap task `phase_02_seq_076_create_audit_event_publishing_pipeline_and_append_only_writer_scaffold`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0075` if they exist, especially `pc_0020`, `pc_0026`, `pc_0050`, `pc_0054`, `pc_0064`, `pc_0065`, `pc_0067`, `pc_0068`, `pc_0070`, and `pc_0075`.

Mission:
Build the append-only audit pipeline and writer scaffold that later business features can use to publish compliance-significant evidence without mutating or reordering history.
This task must preserve canonical audit ordering by `audit_stream_ref + stream_sequence`, event-payload hashing, prior-hash chaining, visibility / retention posture, and retained explainability boundaries.
The result should let later modules emit durable audit truth with tamper-evident lineage and stable query behavior.

Source algorithm grounding you must honor:
- `observability_and_audit_contract.md`, especially the audit event fields, canonical ordering, required families, retained context rules, tamper-evident support, and separation from telemetry.
- `audit_and_provenance.md`, especially chronology, lineage, append-only posture, and audit anchors.
- `retention_error_and_observability_contract.md`, especially the rule that retention limits do not destroy append-only proof.
- `schemas/audit_event.schema.json`.
- `deployment_and_resilience_contract.md`, especially audit continuity verification during restore and rollback.
- Earlier messaging, control-store, and telemetry groundwork from `pc_0050`, `pc_0067`, `pc_0070`, and `pc_0075`.

Deliverables to create:
1. `packages/audit/src/audit_event_builder.ts`.
2. `packages/audit/src/audit_stream_sequencer.ts`.
3. `packages/audit/src/append_only_audit_writer.ts`.
4. `packages/audit/src/audit_hash_chain.ts`.
5. `packages/audit/src/audit_signature_batcher.ts`.
6. `packages/audit/src/audit_visibility_and_retention.ts`.
7. `config/audit/audit_event_family_catalog.json`.
8. `config/audit/audit_stream_partition_policy.json`.
9. `config/audit/audit_ordering_policy.json`.
10. `config/audit/audit_signature_policy.json`.
11. `docs/audit/append_only_audit_writer_runbook.md`.
12. `apps/operator-web/src/routes/internal/audit-stream-atlas.tsx`.
13. `tests/unit/audit/audit_writer_foundation.spec.ts`.
14. `tests/integration/audit/append_only_hash_chain_flow.spec.ts`.
15. `tests/playwright/internal/audit_stream_atlas.spec.ts`.

Required design decisions you must make explicit:
- Define audit stream partitioning: per tenant, per manifest, per object family, or hybrid, while preserving stable chronology and query ergonomics.
- Define how stream sequence is allocated, fenced, and recovered under concurrent writers.
- Define how `event_payload_hash`, `prev_event_hash`, and `signature_ref` are generated and retained.
- Define how retained explainability, visibility class, and retention class shape what the event stores versus what linked refs store.
- Define how later services publish into audit without writing directly to append-only tables ad hoc.
- Define how restore, replay, and backfill interact with append-only history without mutating prior records.
- Define how duplicate audit publication is prevented or detected.
- Define how audit joins to telemetry and provenance through correlation context without becoming a logging sink.

Execution steps:
1. Create machine-readable audit event family, stream partition, ordering, and signature policy catalogs.
2. Implement an audit event builder that takes typed domain inputs and produces schema-aligned `AuditEvent` objects with explicit retained-context posture.
3. Implement an append-only sequencer that allocates `stream_sequence` lawfully and safely under concurrent writers.
4. Implement hash-chain helpers for `event_payload_hash` and `prev_event_hash`, plus any batch-signature hooks selected by earlier ADRs.
5. Implement the append-only writer with compare-and-swap / single-writer-fence semantics that prevent silent rewrite or reorder behavior.
6. Implement duplicate-publication detection or idempotent no-op posture based on durable lineage and earlier messaging foundations.
7. Write a runbook explaining how services should publish audit evidence and how continuity is verified during restore or investigation.
8. Build `audit-stream-atlas` as a read-only surface that visualizes one audit stream's ordered chain, event families, visibility posture, and hash continuity.
9. Add unit, integration, and Playwright tests for concurrent writers, hash-chain continuity, duplicate publish attempts, and retention-limited explainability.

Visual and interaction specification for `audit-stream-atlas`:
- Tone: a premium ledger chain.
  The page should feel like a forensic ledger, not a logging console or SIEM clone.
- Layout:
  - `64px` top bar with audit-stream selector, continuity-status badge, and visibility-class chip.
  - left family rail `248-264px` listing `AUTH`, `WORKFLOW`, `CONFIG`, `RETENTION`, `FAILURE`, `RELEASE`, `SECURITY`.
  - center ledger canvas with a vertical chain of sequenced audit blocks.
  - right inspector `380-408px` with selected event metadata, retained-context posture, hash links, and signature info.
- Diagram:
  - hero diagram is a vertical chain of slim ledger blocks; each block shows stream sequence, event type, and one continuity mark linking to the previous hash.
  - use a custom monoline chain-link glyph near the heading.
- Palette:
  - background `#F5F4F1`
  - primary surface `#FFFFFF`
  - secondary surface `#F0EEE9`
  - ink `#101418`
  - muted ink `#6A727A`
  - hairline `rgba(16,20,24,0.08)`
  - accent graphite-blue `#4A6076`
  - accent olive `#62705C`
  - accent umber `#866345`
  - success `#17614B`
  - warning `#8C5D1B`
  - danger `#A53A31`
- Typography:
  - serious, ledger-like, and print-minded; monospace only for sequence ids, hashes, refs, and event codes.
- Dimensional constraints:
  - max width `1490px`
  - ledger canvas min height `460px`
  - event block height `56px`
  - inspector row height `34px`
- Motion:
  - `120ms` block focus and inspector reveal.
  - `150ms` continuity-link emphasis only when reduced motion is not preferred.
  - reduced motion uses static contrast and border changes only.
- Interaction:
  - selecting a block must reveal `event_payload_hash`, `prev_event_hash`, `signature_ref`, and retained-context posture.
  - a toggle must switch between strict stream order view and multi-stream merged-view explanation without re-sorting the selected stream itself.
  - accessible names should read like `audit stream sequence 104 records authority interaction resolved with prior hash continuity`.
  - Playwright must verify semantic family rails, keyboard traversal, reduced-motion posture, and immutable ordering presentation.

Gap handling you must perform:
- Close the gap where services might emit audit-like rows without a shared append-only writer.
- Close the gap where canonical ordering and continuity rules remain prose rather than sequencer code and policy.
- Close the gap where duplicate or concurrent audit publication could fork chronology or hash chains.
- Close the gap where restore verification requires audit continuity but the repo lacks reusable continuity tooling.
- Close the gap where operator-facing audit explanation would otherwise require raw SQL or log spelunking.

Mandatory edge cases:
- concurrent writers targeting the same audit stream
- duplicate publication attempt for the same business event
- missing prior hash when appending after a recovery event
- retention-limited explainability where payload availability changes but proof must remain
- restore or replay needing to append new audit evidence without mutating historical rows
- cross-stream query merge where wall-clock timestamps differ from stable stream order
- signature batch failure after events have already been appended

Backend, performance, and security requirements:
- Audit storage must remain append-only to the greatest extent the selected datastore supports; no update-in-place history rewriting.
- Sequencing must be deterministic and safe under concurrent writers.
- Keep sensitive payload material behind refs and retained-context posture; do not dump raw secrets or unnecessary regulated content into the audit row.
- Audit joins to telemetry through correlation context, but telemetry sampling or failure must not drop audit truth.
- The atlas must stay read-only and must not expose hidden privileged payloads beyond the retained explainability contract.
- Restore tooling must be able to verify continuity without rewriting the stream.

Validation rules:
- Unit-test event building, sequencing, hash chaining, duplicate-detection / idempotency posture, and retention / visibility policy application.
- Integration-test append-only writing under concurrency, duplicate publication, and continuity verification.
- Use Playwright to verify semantic family rails, keyboard traversal, reduced-motion behavior, and stable ordering in `audit-stream-atlas`.
- Validate emitted audit events against `audit_event.schema.json`.
- Prove that append-only history remains queryable and continuity-verifiable after restore-like re-entry conditions.

Definition of done:
- The repo has a reusable append-only audit writer and event builder instead of service-local audit rows.
- Audit ordering, hash continuity, and retained explainability posture are explicit and testable.
- Later business features can publish durable evidence without inventing append-only semantics.
- Restore and investigation tooling can verify audit continuity reliably.
- The internal atlas makes audit chronology legible without becoming a generic log viewer.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
