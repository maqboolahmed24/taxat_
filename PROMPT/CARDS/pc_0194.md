---
id: pc_0194
slug: phase_04_parallel_wave_02_track_backend_governance_006_implement_audit_investigation_frame_projection_and_correlation_neighborhood_queries
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "006"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0194

## Task Summary

implement audit investigation frame projection and correlation neighborhood queries.

## Metadata

- ID: `pc_0194`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `006`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_006_implement_audit_investigation_frame_projection_and_correlation_neighborhood_queries`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0193](./pc_0193.md) `phase_04_parallel_wave_02_track_backend_governance_005_implement_retention_governance_frame_projection_legal_hold_register_and_erasure_queues`
- Next: [pc_0195](./pc_0195.md) `phase_04_parallel_wave_02_track_backend_governance_007_implement_governance_interaction_layer_projection_for_baskets_diff_panels_and_step_up`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0190_to_0197.md](../shared_operating_contract_0190_to_0197.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_006_implement_audit_investigation_frame_projection_and_correlation_neighborhood_queries`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0193` if they exist, especially `pc_0128` through `pc_0130`, `pc_0150` through `pc_0168`, and `pc_0193`.

Mission:
Implement the authoritative `AuditInvestigationFrame` projector and the supporting correlation-neighborhood query services for `/v1/governance/tenants/{tenant_id}/audit-investigations`.
This route is not a raw JSON log viewer and not a telemetry screen.
It must preserve append-only audit ordering, explicit query contract, explicit ordering basis, selected event continuity, object neighborhood reconstruction, summary-first diff posture, and export eligibility that is bound to the active filtered slice.

Source algorithm grounding you must honor:
- `admin_governance_console_architecture.md`, especially section `4.6 /governance/audit`, the route reading order, critical surfaces, and export rules.
- `data_model.md`, especially `AuditInvestigationFrame`, `audit_workspace`, `audit_tape`, `object_neighborhood`, `event_diff_inspector`, and `export_eligibility_panel`.
- `audit_and_provenance.md`, especially the proof-boundary rules separating audit, provenance, and telemetry.
- `observability_and_audit_contract.md`, especially canonical audit ordering, correlation keys, export posture, and audit-versus-telemetry separation.
- `provenance_graph_semantics.md` where neighborhood or upstream/downstream lineage affects query assembly.
- `northbound_api_and_session_contract.md`, especially `GET /v1/governance/tenants/{tenant_id}/audit-investigations`.
- `schemas/audit_investigation_frame.schema.json`, `schemas/governance_interaction_layer.schema.json`, and `schemas/externalization_governance_contract.schema.json`.
- `scripts/validate_contracts.py`, especially `audit_investigation_frame_query_contract_alignment`, `audit_investigation_frame_workspace_selection_and_export_alignment`, and `audit_investigation_frame_interaction_layer_compaction_alignment`.
- `PATCH_RESOLUTION_INDEX.md`, especially audit-investigation hardening for empty refs, cursors, and filter arrays.

Deliverables to create:
1. `packages/backend-governance/src/projectors/build_audit_investigation_frame.ts`
2. `packages/backend-governance/src/queries/query_audit_slice.ts`
3. `packages/backend-governance/src/services/build_object_neighborhood.ts`
4. `packages/backend-governance/src/services/build_event_diff_inspector.ts`
5. `packages/backend-governance/src/services/build_export_eligibility_panel.ts`
6. `packages/backend-governance/src/tests/audit_investigation_frame_projection.spec.ts`
7. `packages/backend-governance/src/tests/audit_investigation_ordering_and_export_alignment.spec.ts`
8. `docs/governance/audit_investigation_frame_and_neighborhood_queries.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/audit-investigation-lab.tsx`

You MUST patch any existing audit-investigation route so it uses this frame and query layer rather than deriving event order, object neighborhood, or export posture in the browser.

Required design decisions you must make explicitly:
- define the canonical `query_contract_code` vocabulary you will support initially and how each one determines `query_anchor_ref`
- define which durable fields determine `ordering_basis` and how merged timelines preserve that order without falling back to timestamps
- define how upstream/downstream neighborhoods are reconstructed and bounded for one selected event / object pair
- define how traces and log records may support investigation without becoming the proof source
- define how `export_posture` and `export_eligibility_panel` bind to the active filtered slice and to masking / approval / limitation posture
- define how summary-first diffs are assembled before any raw payload affordance appears

Execution requirements:
- publish a schema-valid `AuditInvestigationFrame`
- preserve the route reading order exactly: `INVENTORY_RAIL -> WORKSPACE_CANVAS -> EVENT_DIFF_INSPECTOR -> AUDIT_SIDECAR`
- preserve the internal canvas module order exactly: `AuditTape -> ObjectNeighborhood`
- keep `ordered_event_refs[]` and `audit_tape.rows[]` in append-only canonical order from the declared `ordering_basis`; do not infer order from raw wall-clock timestamps
- keep one explicit `selected_event_ref`, one `focus_anchor_ref`, and one `selected_object_ref_or_null` stable across refresh, pagination, and compaction while the same event still resolves
- keep `event_diff_inspector.raw_payload_posture = SUMMARY_FIRST` semantics and treat raw payload as a secondary inspection path
- keep `export_eligibility_panel.active_slice_scope_ref` bound to the active filtered slice at invocation time; do not allow detached background export scope
- when export is not `FULL_ALLOWED`, promote the export panel as the support surface and mirror the active slice exactly
- keep audit truth and telemetry separate: trace/log refs may appear as supporting context, but audit event ordering and occurrence truth remain authoritative

Gap handling you must close while implementing:
- close any gap where route order or export posture can be rebuilt from browser state instead of the frame
- close any gap where neighborhood reconstruction depends on local adjacency scans rather than durable query inputs
- close any gap where event order can drift because timestamps outrank stream sequence or declared ordering basis
- close any gap where export can silently detach from the active filtered slice or masking posture
- close any gap where the diff inspector defaults to raw JSON instead of a governed summary-first posture

Mandatory edge cases:
- `window_from` must never exceed `window_to`
- `ordered_event_refs[]`, `correlation_keys[]`, and `object_neighborhood_refs[]` must stay non-empty when the selected query slice claims they exist
- `selected_event_ref` must remain in the ordered slice and in the audit tape rows
- `selected_object_ref_or_null` must align with the neighborhood context when present
- `state = MASKED_ONLY`, `APPROVAL_REQUIRED`, or `DENIED` must prevent `FULL_EXPORT_READY` style or behavior
- no export or print flow may bypass the current slice, current masking posture, or current approval posture through detached URLs or stale cache

Playwright and contract-test requirements:
- add direct tests for query contract alignment, ordering basis, event selection stability, neighborhood reconstruction, diff posture, and export-slice binding
- if a northbound audit-investigations route exists, add Playwright `APIRequestContext` tests for filter echoes, cursor behavior, ordering basis, export eligibility states, and typed failure envelopes
- if you add the optional lab, add Playwright browser tests that verify append-only tape order, neighborhood pivoting, summary-first diffs, export panel promotion, and compaction using semantic locators only
- capture serialized frame payloads and traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a quiet investigation layout with a compact filter rail, one dominant audit tape / neighborhood canvas, one diff inspector band, and one muted sidecar
- max width `1440px`; palette `#F7F5F1 / #FFFFFF / #F1F3F0 / #171717 / #667085 / #0F766E / #1D4ED8 / #B7791F / #C2410C`
- render the tape as an append-only vertical ledger with clear family labels and one selected row highlight
- render the neighborhood as a horizontal upstream / selected / downstream ribbon with explicit arrows and object labels
- render export eligibility as a slim strip with literal state labels (`FULL_ALLOWED`, `MASKED_ONLY`, `APPROVAL_REQUIRED`, `DENIED`) and reason codes
- render diffs as summary-first changed-field nuclei cards with raw payload hidden behind a secondary affordance
- no charts except, at most, one thin chronology rail; never a dashboard

Backend, performance, and security requirements:
- keep the frame read-only and provenance-aware
- query by durable audit keys and stable cursor semantics; do not scan whole audit history for every frame render
- keep export posture bound to masking / approval / limitation state at read time
- fail closed on ambiguous ordering basis, missing selected event, or broken slice anchors

Validation rules:
- validate every emitted `AuditInvestigationFrame` and nested externalization contract against schema
- add regression coverage for the named validator drifts plus slice-bound export semantics and summary-first diff posture
- keep this task limited to audit investigation reads and projector semantics

Definition of done:
- the repo has one authoritative audit-investigation frame and query layer that preserves audit truth, neighborhood context, and slice-bound export posture
- governance clients no longer need to infer event order, diff semantics, or export safety locally
- the audit route becomes a premium forensic surface rather than a generic log explorer
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
