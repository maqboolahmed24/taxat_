---
id: pc_0165
slug: phase_03_parallel_wave_01_track_backend_northbound_009_implement_collaboration_work_item_snapshot_stream_activity_and_attachment_endpoints
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "009"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0165

## Task Summary

implement collaboration work item snapshot stream activity and attachment endpoints.

## Metadata

- ID: `pc_0165`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `009`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_009_implement_collaboration_work_item_snapshot_stream_activity_and_attachment_endpoints`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0164](./pc_0164.md) `phase_03_parallel_wave_01_track_backend_northbound_008_implement_upload_session_allocate_put_blob_and_status_endpoints`
- Next: [pc_0166](./pc_0166.md) `phase_03_parallel_wave_01_track_backend_northbound_010_implement_audit_trail_enquiry_pack_and_investigation_query_endpoints`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0158_to_0165.md](../shared_operating_contract_0158_to_0165.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_009_implement_collaboration_work_item_snapshot_stream_activity_and_attachment_endpoints`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0164` if they exist, especially `pc_0145` through `pc_0156`, `pc_0161`, and `pc_0164`.

Mission:
Implement the collaboration read surface family for work-item detail:
- `GET /v1/work-items/{item_id}/workspace/snapshot`
- `GET /v1/work-items/{item_id}/workspace/stream?resume_token=...`
- `GET /v1/work-items/{item_id}/activity?thread=customer|internal&before_sequence=...`
- `GET /v1/work-items/{item_id}/attachments?visibility=customer|internal`
These endpoints must preserve visibility-scoped `WorkspaceSnapshot` truth, reconnect-safe `WorkspaceCursor` and `WorkspaceStreamEvent` semantics, explicit current-versus-history attachment posture, and strict audience separation between staff and customer-visible lanes.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially sections 2.3, 5, 6, and 7.
- `collaboration_workspace_contract.md`, especially sections 1 through 3, 8.1 through 8.4, 9.1, 11, and 12.
- `frontend_shell_and_interaction_law.md`, especially same object same shell, route continuity, artifact specialization, and automation anchors.
- `data_model.md`, especially `WorkspaceSnapshot`, `CollaborationActivitySlice`, `CollaborationAttachmentSlice`, `WorkspaceCursor`, and `WorkspaceStreamEvent`.
- `schemas/workspace_snapshot.schema.json`, `schemas/workspace_cursor.schema.json`, `schemas/workspace_stream_event.schema.json`, `schemas/collaboration_activity_slice.schema.json`, `schemas/collaboration_attachment_slice.schema.json`, and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 177-183.

Deliverables to create:
1. `packages/backend-northbound/src/http/get_work_item_workspace_snapshot_endpoint.ts`
2. `packages/backend-northbound/src/http/get_work_item_workspace_stream_endpoint.ts`
3. `packages/backend-northbound/src/http/get_collaboration_activity_endpoint.ts`
4. `packages/backend-northbound/src/http/get_collaboration_attachments_endpoint.ts`
5. `packages/backend-northbound/src/http/register_collaboration_read_routes.ts`
6. `packages/backend-northbound/src/query/get_workspace_snapshot.ts`
7. `packages/backend-northbound/src/query/get_collaboration_activity_slice.ts`
8. `packages/backend-northbound/src/query/get_collaboration_attachment_slice.ts`
9. `packages/backend-northbound/src/repositories/workspace_cursor_repository.ts`
10. `packages/backend-northbound/src/streams/serialize_workspace_stream_event.ts`
11. `packages/backend-northbound/src/services/open_or_resume_workspace_cursor.ts`
12. `packages/backend-northbound/src/services/build_workspace_stability_contract.ts`
13. `packages/backend-northbound/src/services/build_workspace_stream_recovery_contract.ts`
14. `packages/backend-northbound/src/services/build_workspace_problem_envelope.ts`
15. `docs/northbound/collaboration_read_surfaces.md`
16. `tests/unit/backend_northbound/workspace_snapshot_contract.spec.ts`
17. `tests/unit/backend_northbound/workspace_stream_event_contract.spec.ts`
18. `tests/integration/backend_northbound/work_item_workspace_snapshot_stream.spec.ts`
19. `tests/integration/backend_northbound/collaboration_activity_and_attachments.spec.ts`
20. `tests/playwright_api/northbound/collaboration_read_routes.spec.ts`
21. `tests/playwright_browser/northbound/workspace_stream_eventsource.spec.ts`

Optional diagnostic explorer only if it materially accelerates verification:
22. `apps/admin-console-web/src/routes/debug/work-item-stream-inspector.tsx`

Required design decisions you must make explicit:
- Define whether and how the collaboration detail endpoints rely on a separate list/bootstrap endpoint. If discovery needs `/v1/work-items`, keep it a narrow dependency and do not widen this card into a second queue system.
- Define the exact route-key and route-context semantics for staff versus customer-visible work-item detail routes.
- Define how `ETag = workspace_version` is applied to snapshot reads and how activity/attachment reads preserve the same route-visible guard spine.
- Define the workspace stream token/cursor lifecycle and heartbeat cadence without breaking the shared stream-recovery grammar.
- Define how attachment current-versus-history posture is represented and how pending placeholders are surfaced without becoming downloadable artifacts.

Execution steps:
1. Materialize `WorkspaceSnapshot` from durable workflow/collaboration truth with exact visibility scoping.
2. Implement `GET /workspace/snapshot` with `ETag = workspace_version`, grouped stability/recovery contracts, and exact route context.
3. Implement `GET /workspace/stream` with persisted `WorkspaceCursor`, visibility-bound resume semantics, catch-up-before-live ordering, and typed rebase/access-rebind failures.
4. Implement activity and attachment slice reads that preserve the mounted workspace guard spine, explicit filters, and latest workspace snapshot ref.
5. Re-validate every emitted artifact against schema before response or event publication.
6. Add deterministic tests for staff/customer visibility separation, stream recovery, backward paging, current-versus-history attachment partitioning, and focus continuity.

Gap handling you must perform:
- Close the gap where `WorkspaceSnapshot.context_bar.item_id` or participants can drift away from the top-level mounted `item_id`.
- Close the gap where customer-visible snapshots still expose staff participants, staff-only module codes, or staff-only thread heads.
- Close the gap where action-strip primary/suggested module codes drift outside mounted modules or available-action sets.
- Close the gap where limited module states lose typed limitation reasons.
- Close the gap where activity/attachment paging or streaming would otherwise force clients to reconstruct visibility posture locally.

Mandatory edge cases:
- customer-visible snapshots MUST set `shell_family = CLIENT_PORTAL_SHELL`, clear `internal_head_sequence_or_null`, and mount only `CUSTOMER_ACTIVITY` and `FILES`
- staff-visible snapshots MUST keep `shell_family = CALM_SHELL` and may mount all lawful modules
- `workspace.snapshot`, `workspace.delta`, `activity.appended`, `audit.appended`, `notification.badge`, and `heartbeat` are the only lawful stream event kinds
- duplicate workspace event delivery MUST be idempotent by `(WORKSPACE, item_id, frame_epoch, workspace_sequence)`
- if history compacts, epoch advances, or route visibility changes, the stream MUST return `REBASE_REQUIRED` rather than inventing missing history
- customer sessions MUST NEVER receive internal thread metadata, internal attachments, or audit refs
- activity slices MUST preserve explicit thread filters and backward-paging posture
- attachment slices MUST preserve explicit current versus historical partitioning and artifact selection/affordance contracts

Playwright / contract-test requirements:
- use Playwright APIRequestContext for the endpoint family
- use a browser EventSource harness for the SSE route if you implement one
- no fixed sleeps; wait on explicit snapshot/event/problem signals
- capture traces or serialized event logs on failure
- prove staff/customer visibility separation, same-shell continuity, and rebase/access-rebind behavior end to end

Backend, performance, and security requirements:
- keep visibility filtering and masking posture enforcement server-side and deterministic
- do not leak customer-safe projection from staff-only stream deltas or activity slices
- use indexed item/thread/sequence lookups for paging and stream catch-up
- fail closed on mixed-audience resume tokens, schema drift, or stale route-context mismatch

Validation rules:
- validate snapshot, cursor, stream-event, activity-slice, and attachment-slice responses against their schemas
- validate rebase/access-rebind failures against `problem_envelope.schema.json`
- unit-test snapshot continuity, stream serialization, and current-versus-history partition rules
- integration-test snapshot reads, stream resume/rebase, activity paging, and attachment reads for both staff and customer audiences
- run Playwright API and browser-harness tests for the shipped routes

Definition of done:
- the repo exposes the collaboration snapshot/stream/activity/attachment read surfaces
- visibility-scoped work-item detail is deterministic, reconnect-safe, and current-versus-history aware
- staff and customer audiences cannot cross lanes through reads, paging, or resume tokens
- collaboration detail recovery remains same-object, same-shell, and fail-closed
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
