---
id: pc_0211
slug: phase_04_parallel_wave_02_track_backend_retention_security_observability_003_implement_secret_version_model_rotation_audit_and_attestation_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_retention_security_observability"
task_number: "003"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_retention_security_observability_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0211

## Task Summary

003 implement secret version model rotation audit and attestation rules.

## Metadata

- ID: `pc_0211`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend retention security observability`
- Task Number: `003`
- Source Slug: `phase_04_parallel_wave_02_track_backend_retention_security_observability_003_implement_secret_version_model_rotation_audit_and_attestation_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0210](./pc_0210.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_002_implement_erasure_proof_legal_hold_and_privacy_minimization_workflows`
- Next: [pc_0212](./pc_0212.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_004_implement_security_runtime_hardening_guards_for_network_headers_cors_and_rate_limits`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0206_to_0213.md](../shared_operating_contract_0206_to_0213.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_retention_security_observability_003_implement_secret_version_model_rotation_audit_and_attestation_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0210` if they exist, especially `pc_0038` through `pc_0045`, `pc_0049` through `pc_0050`, `pc_0134` through `pc_0138`, and `pc_0209` through `pc_0210`.

Mission:
Implement the governed `SecretVersion` model and services that preserve version lineage, activation, rotation, retirement, revocation, attestation, and historical-read windows without ambiguous cutover or silent request rebinding.
This task is not allowed to treat secret rotation as an environment-variable swap.
Secret lineage must remain auditable, chronologically valid, and safe for queued authority work that must revalidate token/client binding immediately before send.

Source algorithm grounding you must honor:
- `security_and_runtime_hardening_contract.md`, especially the secret/key/token handling section.
- `data_model.md`, especially `SecretVersion`.
- `schemas/secret_version.schema.json`.
- `scripts/validate_contracts.py`, especially `secret_version_rotation_state_and_chronology_posture`.
- `tools/forensic_contract_guard.py`, especially the state/time posture rules for `activated_at`, `rotation_started_at`, `retired_at`, `historical_read_window_until`, `revoked_at`, and `superseded_by_secret_version_id`.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially ambiguous cutover or rebinding vectors.

Deliverables to create:
1. `packages/backend-security/src/models/secret_version.ts`
2. `packages/backend-security/src/repositories/secret_version_repository.ts`
3. `packages/backend-security/src/services/rotate_secret_version.ts`
4. `packages/backend-security/src/services/attest_secret_version.ts`
5. `packages/backend-security/src/services/resolve_active_secret_version.ts`
6. `packages/backend-security/src/services/validate_secret_version_cutover.ts`
7. `packages/backend-security/src/tests/secret_version_rotation.spec.ts`
8. `packages/backend-security/src/tests/secret_version_attestation.spec.ts`
9. `docs/security/secret_version_model_rotation_audit_and_attestation_rules.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/security/secret-lineage-lab.tsx`

If `packages/backend-security` does not exist, create it deliberately and record `ASSUMPTION_BACKEND_SECURITY_PACKAGE_CREATED` in the notes or docs you add.

Required design decisions you must make explicitly:
- define the rotation-state machine and exact legal transitions among active, rotating, retired, and revoked postures
- define the attestation payload and how it binds store ref, key-version ref, policy profile, lineage, and timing
- define how active resolution works during cutover and how historical reads are allowed or denied
- define how queued authority sends revalidate token version and binding lineage immediately before send
- define how revocation differs from retirement in read behavior, send behavior, and historical window posture

Execution requirements:
- persist `SecretVersion` as the canonical governed secret/key-version contract for runtime, release, and restore controls
- preserve explicit fields for store ref, key-version ref, policy profile ref, lineage ref, issuance/expiry, attestation ref, activation, rotation start, retirement, revocation, historical read window, and supersession linkage
- fail closed when attestation or version binding is unknown
- ensure queued authority mutation revalidates the usable token or secret version against the persisted binding lineage immediately before send
- ensure rotation is legal only inside the same subject/client/scope lineage and cannot silently rebind a request to a different authority link or subject
- ensure no secret lineage may self-supersede or invert chronology

Gap handling you must close while implementing:
- close any gap where cutover timing is implicit or ambiguous
- close any gap where a “current secret” lookup can skip lineage, attestation, or policy-profile validation
- close any gap where retired versions remain usable without an explicit historical-read window
- close any gap where revoked versions linger as send-capable or silently readable
- close any gap where queued authority sends can proceed after token/client rebinding drift

Mandatory edge cases:
- `activated_at` may be non-null only when state is `ACTIVE`, `ROTATING`, or `RETIRED`
- `rotation_started_at` may be non-null only when state is `ROTATING` or `RETIRED`
- `retired_at`, `historical_read_window_until`, and `superseded_by_secret_version_id` imply `RETIRED`
- `revoked_at` and `revocation_reason_code` imply `REVOKED`
- unknown attestation must fail closed for live use
- no version may supersede itself or create a cyclical lineage

Playwright and contract-test requirements:
- add deterministic tests for activation, rotation, retirement, revocation, historical-read windows, attestation failure, and send-time revalidation
- if you expose or patch a security/admin route, add Playwright `APIRequestContext` coverage for rotation-state reads and revoked-vs-retired posture
- if you add the optional lab, add Playwright browser tests that verify the secret-lineage ladder, cutover/retire/revoke timeline, keyboard navigation, and reduced-motion-safe expansion using semantic locators only
- capture payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a three-zone layout: lineage/attestation ribbon, state-transition timeline canvas, narrow cutover/read-window sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; accent `#0F766E`; info accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- visualize lineage and chronology as a ladder/timeline rather than a generic status board
- do not reveal secret material or bearer tokens anywhere in the UI or tests
- animate only opacity/translate/height with `140ms` to `180ms`; respect reduced motion

Backend, performance, and security requirements:
- never persist raw secrets outside the governed store boundary
- keep rotation and attestation append-only and auditable
- centralize send-time secret-version revalidation so transport code cannot bypass it
- prohibit logging raw secret material, token material, or secret-derivation payloads

Validation rules:
- validate every `SecretVersion` artifact against schema before persistence or response emission
- update fixtures/builders so `secret_version_rotation_state_and_chronology_posture` continues to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one canonical backend path for secret-version creation/rotation/retirement/revocation, one for attestation, and one for send-time active-version resolution
- secret lineage is durable, auditable, chronology-safe, and test-covered
- optional labs, if added, remain read-only projections of persisted secret-version truth
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
