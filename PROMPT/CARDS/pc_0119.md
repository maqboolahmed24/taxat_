---
id: pc_0119
slug: phase_03_parallel_wave_01_track_backend_collection_011_implement_artifact_set_wrapping_hashing_and_contract_reference_recording
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "011"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_011
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0119

## Task Summary

implement artifact set wrapping hashing and contract reference recording.

## Metadata

- ID: `pc_0119`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `011`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_011_implement_artifact_set_wrapping_hashing_and_contract_reference_recording`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0118](./pc_0118.md) `phase_03_parallel_wave_01_track_backend_collection_010_implement_snapshot_assembly_and_snapshot_lifecycle_transitions`
- Next: [pc_0120](./pc_0120.md) `phase_03_parallel_wave_01_track_backend_collection_012_implement_artifact_contract_gate_and_validate_artifact_set_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0118_to_0125.md](../shared_operating_contract_0118_to_0125.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_011_implement_artifact_set_wrapping_hashing_and_contract_reference_recording`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0118` if they exist, especially `pc_0017`, `pc_0028`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0076`, `pc_0080`, `pc_0097`, `pc_0100`, `pc_0101`, `pc_0114`, `pc_0115`, `pc_0116`, `pc_0117`, and `pc_0118`.

Mission:
Implement the generic intake artifact-set wrapping and hashing layer plus canonical artifact-contract reference recording for the pre-seal intake pack.
This task must produce deterministic `SourceRecordSet`, `EvidenceItemSet`, `CandidateFactSet`, `ConflictSet`, and `CanonicalFactSet` artifacts, compute stable `item_identity_hash`, `set_hash`, and `artifact_contract_hash`, and record canonical contract refs for both intake-boundary artifacts and intake-data set artifacts so `InputFreeze` and `ARTIFACT_CONTRACT_GATE` can rely on them without ambiguity.

Source algorithm grounding you must honor:
- `core_engine.md`, especially the set-building sequence before snapshot build.
- `modules.md`, especially `BUILD_ARTIFACT_SET(...)`, `WRAP_AND_HASH(...)`, `RECORD_ARTIFACT_CONTRACT_REF(...)`, and `RECORD_ARTIFACT_CONTRACT_REFS(...)`.
- `data_model.md`, especially `SourceRecordSet`, `EvidenceItemSet`, `CandidateFactSet`, `ConflictSet`, `CanonicalFactSet`, `InputFreeze`, and the artifact contract envelope.
- `contract_integrity_requirements.md`, especially the uniqueness requirement for set-style envelopes.
- `replay_and_reproducibility_contract.md`, especially the requirement for stable artifact identities, canonical decimal strings, and replay-safe basis hashes.
- Existing schemas including `source_record_set.schema.json`, `evidence_item_set.schema.json`, `candidate_fact_set.schema.json`, `conflict_set.schema.json`, `canonical_fact_set.schema.json`, `schema_bundle.schema.json`, and `input_freeze.schema.json`.

Deliverables to create:
1. `packages/backend-collection/src/types/intake_artifact_contract_ref.ts`.
2. `packages/backend-collection/src/services/build_artifact_set.ts`.
3. `packages/backend-collection/src/services/wrap_and_hash.ts`.
4. `packages/backend-collection/src/services/build_source_record_set.ts`.
5. `packages/backend-collection/src/services/build_evidence_item_set.ts`.
6. `packages/backend-collection/src/services/build_candidate_fact_set.ts`.
7. `packages/backend-collection/src/services/build_conflict_set.ts`.
8. `packages/backend-collection/src/services/build_canonical_fact_set.ts`.
9. `packages/backend-collection/src/services/record_artifact_contract_ref.ts`.
10. `packages/backend-collection/src/services/record_artifact_contract_refs.ts`.
11. `packages/backend-collection/src/services/build_artifact_contract_hash.ts`.
12. `db/migrations/phase03_0119_intake_artifact_sets.sql`.
13. `docs/collection/artifact_set_wrapping_hashing_and_contract_reference_recording.md`.
14. `tests/unit/backend_collection/artifact_set_wrapping.spec.ts`.
15. `tests/integration/backend_collection/intake_artifact_contract_refs.spec.ts`.

Required design decisions you must make explicit:
- Define canonical ordering rules for each set family.
  At minimum, ordering must be deterministic under replay, retry, and semantically identical duplicate input.
- Define the exact preimage for `item_identity_hash`, `set_hash`, and `artifact_contract_hash`.
  Hash inputs must use canonical JSON, explicit null-slot preservation where relevant, and canonical decimal-string serialization.
- Define the canonical string representation for one `artifact_contract_ref`.
  The schema only says `string`; you must bind it to the repo’s reference grammar from earlier cards and make it stable across writer builds and replay.
- Define how `ConflictSet.unresolved_conflict_hash`, `resolution_frontier`, `open_conflict_ids[]`, and `blocking_conflict_ids[]` are computed so the schema invariants hold exactly.
- Define whether `artifact_contract_hash` is the ordered digest of contract refs, contract content hashes, or an explicitly structured tuple.
  The decision must remain consistent with later `ARTIFACT_CONTRACT_GATE` recomputation.

Execution steps:
1. Implement the generic `build_artifact_set.ts` / `wrap_and_hash.ts` foundation so all five intake set families use one deterministic wrapping pipeline.
2. Implement family-specific builders for `SourceRecordSet`, `EvidenceItemSet`, `CandidateFactSet`, `ConflictSet`, and `CanonicalFactSet`.
3. Implement canonical contract-ref recording for intake-boundary artifacts (`SourcePlan`, `SourceWindow`, `CollectionBoundary`, `NormalizationContext`) and intake-data set artifacts.
4. Implement the ordered artifact-contract-hash builder used by `InputFreeze` and the later gate.
5. Add tests for deterministic ordering, duplicate suppression, conflict-frontier invariants, and stable contract-ref recording.

Gap handling you must perform:
- Close the gap where `input_freeze.artifact_contract_refs[]` requires canonical strings, but the corpus does not fully spell out the string-level representation.
  Reuse the earlier reference grammar and make the format explicit.
- Close the gap where set-style envelopes previously tolerated non-unique `items[]`.
  Enforce uniqueness and deterministic dedupe rather than trusting upstream callers.
- Close the gap where `ConflictSet` carries extra frontier semantics that simple list wrapping does not provide.
  Build the frontier fields from the unresolved conflict population explicitly.
- Close the gap where snapshot and contract-gate tasks depend on set refs/hashes and contract refs immediately.
  Publish these services now as reusable primitives instead of forcing later cards to recreate hash logic.

Mandatory edge cases:
- semantically duplicate inputs must collapse deterministically rather than changing `set_hash`.
- canonical ordering must not depend on database row ids or insertion order.
- `ConflictSet.resolution_frontier = CLEAR` requires zero open and zero blocking conflicts.
- `ConflictSet.resolution_frontier = MONITORING_ONLY` requires open conflicts but zero blocking conflicts.
- `ConflictSet.resolution_frontier = BLOCKING_PRESENT` requires at least one blocking conflict and one open conflict.
- `dominant_blocking_class` must be null when there are no blocking conflicts and non-null when blocking conflicts exist.
- `artifact_contract_refs[]` ordering must be deterministic and stable across replay.
- `artifact_contract_hash` must change when any authoritative contract ref or contract content changes, and remain stable otherwise.

Backend, performance, and security requirements:
- Keep set wrapping streaming-friendly where feasible, but never trade determinism for throughput.
- Reuse existing canonical serializer and hash utilities instead of introducing per-set bespoke encoders.
- Do not log full artifact payloads or sensitive evidence content when hash mismatches occur.
- Keep family-specific builders thin; generic wrapping, dedupe, hashing, and envelope creation should live in shared services.

Validation rules:
- Unit-test deterministic ordering and dedupe for all five set families.
- Unit-test `ConflictSet` frontier invariants and `unresolved_conflict_hash`.
- Integration-test contract-ref recording and `artifact_contract_hash` recomputation against the same frozen input pack.
- Validate each persisted set artifact against its schema.
- Re-run the contract validators after the implementation bundle is assembled.

Definition of done:
- The repo can wrap authoritative intake families into deterministic first-class set artifacts.
- Canonical contract refs and contract hashes are now explicit, ordered, and reusable.
- Snapshot assembly, `InputFreeze`, and `ARTIFACT_CONTRACT_GATE` can all consume one stable intake-pack identity surface instead of inventing their own hash logic.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
