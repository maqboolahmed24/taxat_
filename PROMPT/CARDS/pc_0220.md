---
id: pc_0220
slug: phase_04_parallel_wave_02_track_backend_release_resilience_002_implement_deployment_release_model_canary_health_summary_and_rollout_state_machine
mode: parallel
phase: "04"
wave: "02"
track: "backend_release_resilience"
task_number: "002"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_release_resilience_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0220

## Task Summary

002 implement deployment release model canary health summary and rollout state machine.

## Metadata

- ID: `pc_0220`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend release resilience`
- Task Number: `002`
- Source Slug: `phase_04_parallel_wave_02_track_backend_release_resilience_002_implement_deployment_release_model_canary_health_summary_and_rollout_state_machine`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0219](./pc_0219.md) `phase_04_parallel_wave_02_track_backend_release_resilience_001_implement_build_artifact_generation_signing_metadata_and_candidate_tuple_persistence`
- Next: [pc_0221](./pc_0221.md) `phase_04_parallel_wave_02_track_backend_release_resilience_003_implement_schema_migration_ledger_and_reader_window_blocking_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0214_to_0221.md](../shared_operating_contract_0214_to_0221.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_release_resilience_002_implement_deployment_release_model_canary_health_summary_and_rollout_state_machine`.

Inherit the shared operating contract and consume outputs from cards `pc_0219` and all prerequisite release / recovery / verification cards if they exist, especially `pc_0027`, `pc_0055`, `pc_0199`, and `pc_0202`.

Mission:
Implement the canonical `DeploymentRelease` persistence model, `CanaryHealthSummary`, and the rollout-state machine that governs canary, serve, abort, rollback, and fail-forward posture.
This task is not allowed to infer rollout state from deployment logs or CI dashboard labels.
It must persist one explicit state machine with explicit rollback / fail-forward boundaries and one first-class canary summary bound to the exact candidate tuple.

Source algorithm grounding you must honor:
- `deployment_and_resilience_contract.md`, especially promotion pipeline, recovery tiers, rollback / fail-forward posture, and reader-window rollback boundaries.
- `release_candidate_identity_and_promotion_evidence_contract.md`, especially shared candidate and compatibility boundaries.
- `verification_and_release_gates.md`, especially blocking gate evidence and canary / restore / client-matrix companion evidence.
- `data_model.md`, especially `DeploymentRelease`, `CanaryHealthSummary`, and the shared `StateTransitionContract` notes.
- `schemas/deployment_release.schema.json`, `schemas/canary_health_summary.schema.json`, `schemas/state_transition_contract.schema.json`, `schemas/release_candidate_identity_contract.schema.json`, and `schemas/schema_bundle_compatibility_gate_contract.schema.json`.
- `scripts/validate_contracts.py`, especially `validate_deployment_release`, `validate_canary_health_summary`, `deployment_release_strategy_state_and_override_posture`, `deployment_release_closed_window_blocks_rollback`, `deployment_release_failed_forward_requires_owner_and_compensating_release`, `deployment_release_state_transition_current_state_mirror`, and `canary_health_summary_reverse_budget_posture`.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially any vectors around closed-window rollback, self-rollback lineage, emergency override expiry, and reverse budget mapping.

Deliverables to create:
1. `packages/backend-release/src/models/canary_health_summary.ts`
2. `packages/backend-release/src/models/deployment_release.ts`
3. `packages/backend-release/src/services/evaluate_canary_health_summary.ts`
4. `packages/backend-release/src/services/advance_deployment_release_state.ts`
5. `packages/backend-release/src/repositories/deployment_release_repository.ts`
6. `packages/backend-release/src/tests/canary_health_summary.spec.ts`
7. `packages/backend-release/src/tests/deployment_release_state_machine.spec.ts`
8. `docs/release/deployment_release_canary_and_rollout_state_machine.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/release/rollout-and-canary-lab.tsx`

You MUST patch any existing rollout logic that currently:
- derives rollout state from external deploy status labels instead of `DeploymentRelease`,
- allows rollback after reader-window closure without an explicit fail-forward boundary,
- or records canary posture without the exact candidate tuple and compatibility boundary.

Required design decisions you must make explicitly:
- define the closed rollout-state machine and its allowed transitions
- define how `rollout_strategy`, `rollout_state`, `health_gate_state`, `rollback_boundary_state`, and `state_transition_contract{...}` interlock
- define the exact reverse mapping from latency/error budgets to `CanaryHealthSummary.health_gate_state` and `abort_recommended`
- define when a release becomes `FAILED_FORWARD` and which compensating release and owner refs become mandatory
- define the emergency override posture, expiry behavior, and how it interacts with rollout state

Execution requirements:
- persist `CanaryHealthSummary` bound to the exact `candidate_identity_hash` and `candidate_identity_contract{...}`
- persist `DeploymentRelease` with nested candidate / compatibility / recovery / reader-window contracts exactly as required by schema
- enforce explicit state transitions via `StateTransitionContract`
- require `deployed_at` for terminal served states
- block self-rollback lineage
- block rollback once `schema_reader_window_contract.window_state = CONTRACT_ELIGIBLE_WINDOW_CLOSED`
- require a compensating release ref plus accountable owner when `rollout_state = FAILED_FORWARD`
- keep emergency overrides scoped and expiring; do not let them become permanent release posture

Gap handling you must close while implementing:
- close any gap where rollout state is reconstructed from deployment provider labels or timestamps instead of persisted release state
- close any gap where canary health is stored as a summary string instead of a candidate-bound first-class artifact
- close any gap where rollback remains available after the schema reader window says it is no longer safe
- close any gap where failed-forward posture lacks a compensating release, owner, or explicit runbook lineage
- close any gap where canary evidence can drift away from the exact candidate tuple or compatibility boundary being judged

Mandatory edge cases:
- both budgets within budget imply green canary posture; budget breach mapping must be explicit and test-covered
- closed reader windows must block rollback
- self-rollback release lineage must be rejected
- emergency override expiry must remain post-deploy and bounded
- failed-forward posture must include compensating release and owner
- terminal reentry and illegal transitions must fail closed through `StateTransitionContract`

Playwright and contract-test requirements:
- add direct tests for canary reverse-budget mapping, rollback blocking on closed windows, failed-forward requirements, and state-transition mirroring
- if you expose or patch a release/debug route, add Playwright `APIRequestContext` coverage for canary summary retrieval, release retrieval, legal transitions, and blocked rollback posture
- if you add the optional lab, add Playwright browser tests for rollout-state ladders, canary rail semantics, semantic-locator navigation, reduced-motion-safe transitions, and sidecar focus behavior using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- use a three-band layout: release tuple ribbon, rollout-state ladder, narrow canary / rollback-boundary sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; info accent `#1D4ED8`; continuity accent `#0F766E`; caution `#B7791F`; danger `#C2410C`
- do not use dashboards or speedometer-style charts; use rails, ladders, and bounded strips only
- animate only opacity / translate / height; respect reduced motion

Backend, performance, and security requirements:
- all rollout transitions must be transactional and idempotent under retried deployment signals
- release state must remain queryable and audit-safe after rollback, fail-forward, or supersession
- canary summaries must remain immutable evidence snapshots once recorded for a candidate/environment slice
- do not let emergency overrides bypass schema compatibility, restore, or signed-build requirements; only model their explicit persisted posture

Validation rules:
- validate every `CanaryHealthSummary`, `DeploymentRelease`, and `StateTransitionContract` payload against schema before persistence
- update fixtures/builders so all relevant validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one authoritative backend path for canary summaries and deployment-release state transitions
- rollback/fail-forward boundaries, canary budget mapping, and candidate-bound evidence are durable and test-covered
- optional labs, if added, are read-only reflections of persisted truth rather than alternate implementations
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
