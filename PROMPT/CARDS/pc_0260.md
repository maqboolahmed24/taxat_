---
id: pc_0260
slug: phase_05_parallel_wave_03_track_frontend_portal_008_Playwright_or_other_appropriate_tooling_implement_contextual_request_detail_routes_and_parent_return_restoration
mode: parallel
phase: "05"
wave: "03"
track: "frontend_portal"
task_number: "008"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_portal_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0260

## Task Summary

008 Playwright or other appropriate tooling implement contextual request detail routes and parent return restoration.

## Metadata

- ID: `pc_0260`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend portal`
- Task Number: `008`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_portal_008_Playwright_or_other_appropriate_tooling_implement_contextual_request_detail_routes_and_parent_return_restoration`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0259](./pc_0259.md) `phase_05_parallel_wave_03_track_frontend_portal_007_Playwright_or_other_appropriate_tooling_implement_portal_activity_route_and_customer_safe_timeline_rendering`
- Next: [pc_0261](./pc_0261.md) `phase_05_parallel_wave_03_track_frontend_portal_009_Playwright_or_other_appropriate_tooling_implement_help_handoff_boundary_crossing_and_return_anchor_restoration`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_portal_008_Playwright_or_other_appropriate_tooling_implement_contextual_request_detail_routes_and_parent_return_restoration`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0259` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement contextual portal request-detail routes with parent return restoration, exact focus anchors, and same-shell customer-safe task continuity.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
`/portal/requests/{item_id}` is a contextual detail derivative inside the portal shell, not a sixth permanent tab or standalone collaboration product. It must keep the parent top-level tab active, restore the named request/approval/support focus, expose a clear return path, render only customer-safe request context, and handle rebase/disappearance through latest lawful object or exact parent return anchor.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md` contextual request-detail routes, shell continuity, route_context, focus restoration, artifact focus bucket, and Playwright validation minimum.
- `collaboration_workspace_contract.md` customer request list/detail rules and customer-visible workspace projection.
- `focus_restoration_and_return_target_harness_contract.md` required rules.
- `schemas/client_portal_workspace.schema.json`, `customer_request_list_snapshot.schema.json`, `focus_restoration_contract.schema.json`, `cross_device_continuity_contract.schema.json`, `customer_safe_projection_contract.schema.json`.

Deliverables to create or patch:
1. `apps/client-portal-web/src/routes/RequestDetailRoute.tsx`
2. `apps/client-portal-web/src/components/requests/RequestDetailFrame.tsx`
3. `apps/client-portal-web/src/components/requests/RequestReturnControl.tsx`
4. `apps/client-portal-web/src/components/requests/CustomerSafeRequestSummary.tsx`
5. `packages/frontend-shell-core/src/portal/request_detail_route_context.ts`
6. `packages/frontend-shell-core/src/portal/portal_return_target_resolver.ts`
7. `tests/unit/frontend-shell-core/request_detail_route_context.spec.ts`
8. `tests/playwright/frontend/client_portal_request_detail_return_restoration.spec.ts`
9. `docs/frontend/client_portal_contextual_request_detail_routes.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Render request detail as a focused task page inside the same portal frame: parent tab remains highlighted, a small `Back to Documents` / `Back to Home task` return chip names the exact parent, then a single request summary card with due label, next action, current file/status, and support entry.
- On narrow screens, keep the same header and return chip; do not convert the route into a modal or detached sheet. Use subdued history/detail accordions below the primary action.

Execution requirements:
- read route_context fields for `context_route`, `return_route`, `focus_anchor_ref`, `return_focus_anchor_ref`, `fallback_target`, `fallback_object_ref`, `fallback_reason`, `artifact_focus_bucket`, and `narrow_screen_mode`
- keep the parent top-level tab active and never materialize contextual detail as a sixth nav tab
- render reply/upload/answer/no-safe-action controls as distinct customer-safe actions derived from authoritative action contracts
- when object rebases or disappears, preserve parent route and explain fallback before moving to latest lawful object or exact parent focus anchor
- filter all request detail content through customer-safe projection and portal language contract

Functional, architectural, and edge-case gaps to close:
- close the gap where notification deep links open a generic list instead of the named request
- close the gap where return controls know only the tab, not the exact parent focus anchor
- close the gap where request detail leaks internal stale/gate/escalation language
- close the gap where narrow-screen detail becomes a separate mobile metaphor

Mandatory edge cases:
- request visible and exact focus anchor valid
- focus anchor invalid but same request still lawful
- request hidden/revoked/rebased after open
- parent route was Home task queue, Documents list, Approvals context, or Help case context
- no safe customer action
- history/current artifact focus preserved across refresh

Playwright, accessibility, and validation requirements:
- unit-test contextual return resolution, fallback dispositions, customer-safe action derivation, and active parent tab rules
- Playwright-test notification deep link, parent tab active, return chip focus, invalid/rebased object fallback, no-safe-action copy, keyboard order, responsive restack, and reduced-motion route transitions
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- contextual request detail remains a portal same-shell route
- return and fallback behavior uses exact governed anchors
- Playwright proves deep link, parent return, safety filtering, and responsive continuity
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
