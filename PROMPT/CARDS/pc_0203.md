---
id: pc_0203
slug: phase_04_parallel_wave_02_track_backend_recovery_005_implement_cache_isolation_secure_reuse_and_native_cache_hydration_contract_support
mode: parallel
phase: "04"
wave: "02"
track: "backend_recovery"
task_number: "005"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_recovery_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0203

## Task Summary

005 implement cache isolation secure reuse and native cache hydration contract support.

## Metadata

- ID: `pc_0203`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend recovery`
- Task Number: `005`
- Source Slug: `phase_04_parallel_wave_02_track_backend_recovery_005_implement_cache_isolation_secure_reuse_and_native_cache_hydration_contract_support`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0202](./pc_0202.md) `phase_04_parallel_wave_02_track_backend_recovery_004_implement_deterministic_golden_pack_generation_null_slot_freezing_and_fixture_hashing`
- Next: [pc_0204](./pc_0204.md) `phase_04_parallel_wave_02_track_backend_recovery_006_implement_native_cache_hydration_automation_pack_generation_and_compatibility_checks`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0198_to_0205.md](../shared_operating_contract_0198_to_0205.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_recovery_005_implement_cache_isolation_secure_reuse_and_native_cache_hydration_contract_support`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0202` if they exist, especially `pc_0161`, `pc_0162`, `pc_0174` through `pc_0181`, `pc_0188`, `pc_0189`, and `pc_0201` through `pc_0202`.

Mission:
Implement the shared `CacheIsolationContract` builder, canonical delivery-binding hash derivation, and `NativeCacheHydrationContract` support across governed read models and native scenes.
This task is not allowed to treat cache identity as an optimization hint.
It must make reuse lawful only when tenant, session, access, masking, route, object, projection version, preview subject, and supported contract window still match exactly.

Source algorithm grounding you must honor:
- `cache_isolation_and_secure_reuse_contract.md`, end to end.
- `native_cache_hydration_purge_and_rebase_contract.md`, especially legality envelope, resume-lineage reuse law, and cache-only restoration action gating.
- `cross_device_continuity_and_restoration_contract.md` where hydration and restoration envelopes meet continuity law.
- `frontend_shell_and_interaction_law.md`, especially the shared shell continuity and hydration boundary.
- `data_model.md`, plus the route-visible read models and native scene artifacts that already publish cache or continuity packets.
- `schemas/cache_isolation_contract.schema.json`, `schemas/native_cache_hydration_contract.schema.json`, `schemas/sample_cache_isolation_contract.json`, and the native scene schemas.
- `scripts/validate_contracts.py`, especially `cache_isolation_contract_customer_safe_visibility_binding`, `cache_isolation_contract_partition_ref_mismatch`, `cache_isolation_contract_secondary_preview_binding`, and `cache_isolation_contract_delivery_binding_hash_drift`, plus the native-hydration payload checks.
- `PATCH_RESOLUTION_INDEX.md`, especially any fixes around visibility partition drift, preview subject leakage, route identity mismatch, or native stale hydration.
- `test_vectors.md`, especially cache isolation, native hydration, and same-object continuity vectors.

Deliverables to create:
1. `packages/backend-recovery/src/services/build_cache_isolation_contract.ts`
2. `packages/backend-recovery/src/services/compute_cache_delivery_binding_hash.ts`
3. `packages/backend-recovery/src/services/build_native_cache_hydration_contract.ts`
4. `packages/backend-recovery/src/services/classify_cache_scope_requirements.ts`
5. `packages/backend-recovery/src/services/classify_native_hydration_compatibility.ts`
6. `packages/backend-recovery/src/services/build_native_local_artifact_purge_plan.ts`
7. `packages/backend-recovery/src/tests/cache_isolation_contract.spec.ts`
8. `packages/backend-recovery/src/tests/native_cache_hydration_contract.spec.ts`
9. `docs/recovery/cache_isolation_secure_reuse_and_native_cache_hydration_contract_support.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/recovery/cache-isolation-atlas.tsx`

You MUST patch any governed read-model or native-scene publisher that currently hand-rolls cache keys, preview reuse, or restore legality instead of emitting the shared contracts.

Required design decisions you must make explicitly:
- define the canonical delivery-binding hash derivation and ensure every relevant scope uses the same function
- define the exact field requirements by `cache_scope_class`, including customer-safe projection and visibility partition rules
- define how governance surfaces clear access, masking, and visibility cache bindings without weakening route identity
- define the exact preview-subject law for native secondary-window scenes and why all non-secondary scopes must clear it
- define the exact native hydration compatibility dimensions and purge-trigger reason codes
- define how cache-only restoration blocks mutation-capable and filing-capable actions until live rebase or access rebind re-establishes legality

Execution requirements:
- publish schema-valid `cache_isolation_contract` and `native_cache_hydration_contract` packets
- enforce exact-match cache identity for shared cache reuse and hydration legality
- preserve `cache_partition_ref == visibility_cache_partition_key_or_null` whenever a visibility partition is present
- require customer-safe portal scopes to retain visibility partition and `customer_safe_projection = true`
- clear governance access/masking/visibility fields when the scope is governance-only as required by the contract
- require non-null `preview_subject_ref_or_null` for native secondary-window scenes and clear it outside those scopes
- preserve `hydration_guard_policy = REJECT_ON_CONTEXT_ROUTE_VERSION_OR_PREVIEW_MISMATCH`
- preserve native first-paint gating and mutation gate posture inside the hydration contract

Gap handling you must close while implementing:
- close any gap where route or scene code computes cache identity ad hoc instead of via one shared contract builder
- close any gap where broader or incompatible cached variants survive access or masking narrowing
- close any gap where preview or export reuse can escape the current route/selection binding
- close any gap where native scenes can restore stale object or privilege context without an explicit legality envelope
- close any gap where cache-only restoration could expose mutation-capable posture before live legality is re-established

Mandatory edge cases:
- `CLIENT_PORTAL_WORKSPACE` and `CUSTOMER_REQUEST_LIST` must always be customer-safe and visibility-partition bound
- governance-only scopes must clear access, masking, and visibility bindings as required by the schema
- native secondary-window scenes must retain a non-null preview subject
- non-secondary scopes must clear preview subject
- delivery-binding hash drift must fail validation
- a native hydration contract must block mutation-capable actions on cache-only restoration
- purge reason codes must include tenant switch, privilege downgrade, session revoked, masking change, and schema incompatibility when applicable

Playwright and contract-test requirements:
- if you expose or patch a northbound route that returns these contracts, add Playwright `APIRequestContext` coverage for scope-specific required fields and typed failures
- add direct tests for portal customer-safe visibility, partition matching, governance field clearing, secondary-window preview binding, delivery-binding hash stability, and native hydration legality posture
- if you add the optional atlas, add Playwright browser tests that verify the identity envelope card, purge-plan ledger, preview subject disclosure, reduced-motion-safe compaction, and semantic locator coverage
- capture serialized payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a calm control-plane atlas: left binding-envelope rail, center scope matrix, right purge/hydration sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717` / `#667085`; continuity accent `#0F766E`; explainability accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- render the delivery-binding hash in monospace within a compact ribbon; do not bury it in JSON viewers
- render scope-specific required fields as a matrix, not as scattered form cards
- if you show a diagram, show one purge decision rail or one identity envelope stack; no charts or metric tiles
- animate only opacity / translate / height in `140ms`–`180ms`; reduced motion must preserve all meaning

Backend, performance, and security requirements:
- keep all contract builders pure and deterministic
- do not log or cache raw preview/export content in diagnostics
- keep purge planning explicit so native and browser clients can prove what was invalidated and why
- index route/scene emitters by canonical object and projection version where relevant
- fail closed when scope class and field posture disagree

Validation rules:
- validate every emitted cache or native hydration contract against schema
- add regression coverage for the named validator cases above
- keep this task limited to shared contract support and emitter patches; do not absorb the full automation-pack generation reserved for the next card

Definition of done:
- governed read models and native scenes now emit canonical cache identity and native hydration legality packets
- preview reuse, customer-safe visibility, governance clearing, and secondary-window preview binding become machine-checkable
- later native automation and continuity-harness work can rely on one shared cache/hydration truth boundary
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
