---
id: pc_0235
slug: phase_05_parallel_wave_03_track_frontend_shared_007_Playwright_or_other_appropriate_tooling_implement_sse_client_resume_rebase_and_access_rebind_experience_runtime
mode: parallel
phase: "05"
wave: "03"
track: "frontend_shared"
task_number: "007"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_shared_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0235

## Task Summary

007 Playwright or other appropriate tooling implement sse client resume rebase and access rebind experience runtime.

## Metadata

- ID: `pc_0235`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend shared`
- Task Number: `007`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_shared_007_Playwright_or_other_appropriate_tooling_implement_sse_client_resume_rebase_and_access_rebind_experience_runtime`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0234](./pc_0234.md) `phase_05_parallel_wave_03_track_frontend_shared_006_Playwright_or_other_appropriate_tooling_implement_command_client_receipt_polling_and_problem_envelope_handling`
- Next: [pc_0236](./pc_0236.md) `phase_05_parallel_wave_03_track_frontend_shared_008_Playwright_or_other_appropriate_tooling_implement_artifact_preview_export_print_and_browser_handoff_components`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_shared_007_Playwright_or_other_appropriate_tooling_implement_sse_client_resume_rebase_and_access_rebind_experience_runtime`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0234` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the shared SSE client/runtime for stream resume, catch-up ordering, rebase posture, and access-rebind experience without letting clients reconstruct missing state locally.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The stream contract makes resume bindings exact-route objects and requires monotonic, gap-free sequence application within one epoch. Catch-up must complete before live delivery is treated as current; compaction, epoch, route, session, access, masking, or schema drift must fail closed into `REBASE_REQUIRED` or `ACCESS_REBIND_REQUIRED`. The frontend runtime must encode those rules before low-noise screens consume live data.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `stream_resume_and_catch_up_ordering_contract.md end to end`.
- `northbound_api_and_session_contract.md stream and reconnect rules`.
- `low_noise_experience_contract.md manifest stream recovery and catch-up`.
- `frontend_shell_and_interaction_law.md state/freshness/recovery presentation`.
- `schemas/stream_recovery_contract.schema.json, experience_stream_event.schema.json, workspace_stream_event.schema.json, route_stability_contract.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/streams/sse_client.ts`
2. `packages/frontend-shell-core/src/streams/stream_recovery_contract.ts`
3. `packages/frontend-shell-core/src/streams/ordered_delta_buffer.ts`
4. `packages/frontend-shell-core/src/streams/rebase_access_rebind_state.ts`
5. `packages/frontend-shell-core/src/streams/stream_cursor_persistence.ts`
6. `packages/shared-ui/src/recovery/InlineStreamRecoveryNotice.tsx`
7. `packages/playwright-kit/src/fixtures/sse_stream_fixture.ts`
8. `tests/unit/frontend-shell-core/stream_recovery_runtime.spec.ts`
9. `tests/playwright/frontend/sse_resume_rebase_access_rebind.spec.ts`
10. `docs/frontend/sse_resume_rebase_and_access_rebind_runtime.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Design the stream recovery UI as a quiet in-shell notice: a narrow context-band segment for `CONNECTED`, `RECONNECTING`, `CATCHING_UP`, `REBASE_REQUIRED`, or `ACCESS_REBIND_REQUIRED`. Add a tiny sequence tape diagram in internal fixtures only: current frontier, missing gap, catch-up window, live frontier. Avoid full-page loading, pulse animations, or celebratory reconnect banners.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- create an SSE abstraction that binds to `stream_recovery_contract` and validates stream scope, route key, subject ref, shell stability token, session, access binding, masking context, generation, epoch, and frontier before applying events
- apply events strictly by `(stream_scope_class, subject_ref, frame_epoch, sequence)` and make duplicates idempotent
- buffer out-of-order arrivals but fail to explicit rebase posture when gaps cannot be filled lawfully
- do not merge live events into state until catch-up delivery has completed
- persist cursors only with their exact route/session/access/masking binding and clear them on drift

Functional, architectural, and edge-case gaps to close:
- close the gap where EventSource resume token is treated as a transport hint rather than contract-bound truth
- close the gap where reconnect can remount the shell or steal focus
- close the gap where duplicate/out-of-order deltas cause repeated side effects
- close the gap where compaction floor or access drift is hidden behind a generic refresh prompt

Mandatory edge cases:
- duplicate delivery must not re-announce or re-apply semantic changes
- sequence gap must keep mounted content and show inline recovery rather than blank shell
- epoch advance, shell token change, or route context change must produce `REBASE_REQUIRED`
- session/access/masking/schema drift must produce `ACCESS_REBIND_REQUIRED` and purge/cache signals

Playwright, accessibility, and validation requirements:
- unit-test duplicate, gap, catch-up-before-live, compaction floor, epoch advance, and access drift behavior
- Playwright-test reconnect/catch-up with focus anchored in the active detail module
- assert reduced-motion reconnect cue does not use spatial displacement
- use API/mock stream fixtures and run validator self-test/forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- SSE runtime enforces exact stream recovery contracts
- inline rebase/access-rebind notices preserve shell identity and focus
- stream ordering, duplicate, compaction, and drift edge cases are test-covered
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
