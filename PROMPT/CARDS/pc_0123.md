---
id: pc_0123
slug: phase_03_parallel_wave_01_track_backend_compute_003_implement_risk_report_derivation_and_materiality_threshold_services
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "003"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0123

## Task Summary

implement risk report derivation and materiality threshold services.

## Metadata

- ID: `pc_0123`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `003`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_003_implement_risk_report_derivation_and_materiality_threshold_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0122](./pc_0122.md) `phase_03_parallel_wave_01_track_backend_compute_002_implement_forecast_set_generation_with_execution_mode_boundary_enforcement`
- Next: [pc_0124](./pc_0124.md) `phase_03_parallel_wave_01_track_backend_compute_004_implement_parity_result_computation_and_authority_comparison_logic`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0118_to_0125.md](../shared_operating_contract_0118_to_0125.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_003_implement_risk_report_derivation_and_materiality_threshold_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0122` if they exist, especially `pc_0010`, `pc_0028`, `pc_0061`, `pc_0074`, `pc_0097`, `pc_0101`, `pc_0118`, `pc_0121`, and `pc_0122`.

Mission:
Implement the `RiskReport` model, repository, and feature-calibrated risk scoring service that turns sealed snapshot/compute inputs into one frozen risk posture.
This task must derive `feature_scores[]`, threshold-aware flags, `risk_score`, and the unresolved material/blocking flags from the frozen threshold profile in a deterministic way.
It must not bury risk meaning in UI heuristics or ad hoc policy comments.

Source algorithm grounding you must honor:
- `core_engine.md`, especially post-seal `SCORE_RISK`.
- `modules.md`, especially the place of risk scoring in the post-seal stage graph.
- `compute_parity_and_trust_formulas.md`, especially section `8.5A Risk scoring formula`.
- `data_model.md`, especially `RiskReport`.
- `test_vectors.md`, especially the trust-edge tests that depend on stable `risk_automation_margin`.
- Existing schemas including `risk_report.schema.json`.

Deliverables to create:
1. `packages/backend-compute/src/models/risk_report.ts`.
2. `packages/backend-compute/src/repositories/risk_report_repository.ts`.
3. `packages/backend-compute/src/services/score_risk.ts`.
4. `packages/backend-compute/src/services/risk_feature_registry.ts`.
5. `packages/backend-compute/src/services/risk_threshold_profile_resolver.ts`.
6. `packages/backend-compute/src/services/risk_flag_deriver.ts`.
7. `db/migrations/phase03_0123_risk_report.sql`.
8. `docs/compute/risk_report_derivation_and_materiality_threshold_services.md`.
9. `tests/unit/backend_compute/risk_report_derivation.spec.ts`.
10. `tests/integration/backend_compute/risk_report_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the feature-registry model that maps frozen feature codes to deterministic extractors.
- Define the canonical ordering of `feature_scores[]` and `flags[]`.
- Define how `feature_resolved` is derived at scoring time.
- Define how invalid threshold profiles, zero total weight, or out-of-range feature values are handled.
- Define whether the service exposes internal material/blocking counts in diagnostics, assumptions, or internal tracing, given that the public schema persists flags and aggregate booleans rather than the counts themselves.

Execution steps:
1. Implement the schema-backed `RiskReport` model and repository.
2. Implement `risk_threshold_profile_resolver.ts` and `risk_feature_registry.ts`.
3. Implement `score_risk.ts` so it follows the frozen weighted-risk formula exactly and emits deterministic feature rows.
4. Implement `risk_flag_deriver.ts` so material/blocking conditions become stable flags and aggregate booleans.
5. Add tests for a clean low-risk case, material threshold breach, blocking threshold breach, resolved-vs-unresolved features, and invalid weight profile fallback.

Gap handling you must perform:
- Close the gap where the corpus defines normalized feature math but not the concrete registry boundary.
  Build an explicit frozen-profile feature registry instead of scattering feature extraction across controllers.
- Close the gap where invalid or empty weight profiles could otherwise look like a soft warning.
  Follow the fail-closed rule and surface `RISK_WEIGHT_PROFILE_INVALID`.
- Close the gap where downstream trust needs stable `risk_automation_margin`.
  Persist risk in a deterministic, canonical form rather than leaving it as transient local math.
- Close the gap where feature ordering or duplicate feature codes could perturb hashes and trust decisions.

Mandatory edge cases:
- zero total active risk weight must produce `risk_score = 100` and unresolved blocking posture.
- `feature_value` must be clamped or rejected exactly as documented by your explicit policy; the behavior must be stable and tested.
- `blocking_threshold` must never be lower than `material_threshold`.
- resolved features must not contribute to unresolved material/blocking flags.
- duplicate feature codes in one report must fail closed or be normalized by one explicit deterministic rule.
- `flags[]` ordering must be deterministic and deduped.
- the same frozen snapshot/compute basis must always produce the same `RiskReport`.

Backend, performance, and security requirements:
- Keep feature extraction deterministic and pure.
- Use one frozen threshold profile resolver; do not let feature extractors reach into live config.
- Avoid logging sensitive fact/evidence payloads when feature extraction fails.
- Keep feature extraction extensible but strongly typed; later feature additions should plug into the registry, not fork the scorer.

Validation rules:
- Unit-test weighted-risk math, threshold behavior, resolved/unresolved semantics, ordering, and invalid-profile handling.
- Integration-test `RiskReport` persistence from a sealed snapshot/compute basis and threshold profile.
- Validate persisted artifacts against `risk_report.schema.json`.
- Verify that trust-margin consumers can read one deterministic `risk_score` and aggregate blocking posture.
- Re-run the contract validators after the implementation bundle is assembled.

Definition of done:
- The repo can emit a schema-valid `RiskReport` from frozen inputs and a frozen threshold profile.
- Material/blocking risk posture is now explicit, deterministic, and reusable.
- Later parity/trust/gating stages can consume one authoritative risk artifact instead of recomputing feature math ad hoc.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
