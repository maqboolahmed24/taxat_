---
id: pc_0127
slug: phase_03_parallel_wave_01_track_backend_compute_007_implement_decision_bundle_assembly_and_terminal_outcome_finalization
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "007"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0127

## Task Summary

implement decision bundle assembly and terminal outcome finalization.

## Metadata

- ID: `pc_0127`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `007`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_007_implement_decision_bundle_assembly_and_terminal_outcome_finalization`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0126](./pc_0126.md) `phase_03_parallel_wave_01_track_backend_compute_006_implement_gate_decision_record_model_and_ordered_gate_evaluation_engine`
- Next: [pc_0128](./pc_0128.md) `phase_03_parallel_wave_01_track_backend_compute_008_implement_provenance_node_edge_and_path_graph_builder`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0126_to_0133.md](../shared_operating_contract_0126_to_0133.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_007_implement_decision_bundle_assembly_and_terminal_outcome_finalization`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0126` if they exist, especially `pc_0010`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0065`, `pc_0067`, `pc_0074`, `pc_0076`, `pc_0094` through `pc_0101`, `pc_0102` through `pc_0108`, and `pc_0118` through `pc_0126`.

Mission:
Implement the schema-backed `DecisionBundle` model, repository, terminal-bundle assembler, and `FINALIZE_TERMINAL_OUTCOME(...)` orchestration that persists every blocked, review-required, or completed post-allocation outcome as one durable replay-safe terminal artifact.
This task must normalize the top-level reason bridge, preserve the artifact-identity spine, synchronize terminal refs into the manifest, compute deterministic hashes, preserve workflow ownership correctly, and fail closed whenever required child refs or authority checkpoint semantics are missing.

Source algorithm grounding you must honor:
- `core_engine.md`, especially the output contract for `DecisionBundle`, the recovery-safe outcome bridge, and the rule that all terminal outcomes route through `FINALIZE_TERMINAL_OUTCOME(...)`.
- `modules.md`, especially `PERSIST_DECISION_BUNDLE(...)`, `FINALIZE_TERMINAL_OUTCOME(...)`, manifest lifecycle update rules, replay-attestation integration, and pre-start blocked behavior.
- `data_model.md`, especially `DecisionBundle`.
- `state_machines.md`, especially the rule that review-required runs still use `RunManifest.lifecycle_state = COMPLETED`.
- `gate_decision_explainability_and_reason_code_compression_contract.md`.
- `authority_truth_and_internal_projection_separation_contract.md` for truth-boundary posture.
- `replay_and_reproducibility_contract.md`.
- `AUDIT_FINDINGS.md`, especially findings around missing compact reason summary, missing artifact identity metadata, and reload-safe outcome bridge semantics.
- `PATCH_RESOLUTION_INDEX.md` where decision-bundle replay, refs, or reason ordering were hardened.
- `test_vectors.md`, especially `TV-19`, `TV-19A`, `TV-20`, and any replay-safe terminal-load vectors.
- Existing schema `schemas/decision_bundle.schema.json` and sample artifact `schemas/sample_decision_bundle.json`.

Deliverables to create:
1. `packages/backend-compute/src/models/decision_bundle.ts`.
2. `packages/backend-compute/src/repositories/decision_bundle_repository.ts`.
3. `packages/backend-compute/src/services/build_terminal_reason_summary.ts`.
4. `packages/backend-compute/src/services/build_decision_explainability_contract.ts`.
5. `packages/backend-compute/src/services/compute_deterministic_outcome_hash.ts`.
6. `packages/backend-compute/src/services/persist_decision_bundle.ts`.
7. `packages/backend-compute/src/services/finalize_terminal_outcome.ts`.
8. `packages/backend-compute/src/services/synchronize_manifest_terminal_projection.ts`.
9. `packages/backend-compute/src/services/reload_terminal_bundle_for_same_request_retry.ts`.
10. `packages/backend-compute/src/services/validate_terminal_ref_set.ts`.
11. `db/migrations/phase03_0127_decision_bundle_and_terminal_finalization.sql`.
12. `docs/compute/decision_bundle_assembly_and_terminal_outcome_finalization.md`.
13. `tests/unit/backend_compute/decision_bundle_contract.spec.ts`.
14. `tests/unit/backend_compute/finalize_terminal_outcome.spec.ts`.
15. `tests/integration/backend_compute/terminal_bundle_reload_and_manifest_sync.spec.ts`.

Required design decisions you must make explicit:
- Define the exact terminal input contract.
  It must distinguish pre-start blocked outcomes from post-allocation outcomes and must require that any referenced child artifact is already durably persisted before finalization.
- Define the top-level reason bridge:
  `decision_reason_codes[]`, `dominant_reason_code`, `reason_codes[]`, `plain_reason`, `decision_explainability_contract`, `outcome_class`, `waiting_on`, `checkpoint_state`, `truth_state`, and actionability fields must all be derived from the persisted gate / trust / submission posture rather than from client heuristics.
- Define how `workflow_item_refs[]` are filtered so only still-open post-terminal work survives on the bundle.
- Define how `filing_case_id`, `amendment_case_id`, `submission_record_id`, `primary_proof_bundle_ref`, `graph_id`, and `twin_id` are synchronized and validated.
- Define the deterministic outcome hash input order and hash material.
- Define the replay-attestation hook interface so later replay work can plug in without mutating the terminal contract.

Execution steps:
1. Implement the schema-backed model, repository, and validation layer for `DecisionBundle`.
2. Implement `build_terminal_reason_summary.ts` and `build_decision_explainability_contract.ts` so top-level reason compression is backend-authored, deterministic, and shared with downstream shells.
3. Implement `validate_terminal_ref_set.ts` so finalization fails closed when required refs are absent, inconsistent, or violate implication rules such as `primary_proof_bundle_ref -> graph_id` and `twin_id -> graph_id + parity_id`.
4. Implement `persist_decision_bundle.ts` so bundle persistence computes `decision_bundle_hash`, stamps `persisted_at`, and writes the append-only outcome projection into the manifest.
5. Implement `finalize_terminal_outcome.ts` so it applies legal manifest lifecycle transitions:
   blocked while frozen -> `seal_blocked`,
   blocked while in progress -> `gate_block`,
   review-required / completed -> `run_completed`,
   while preserving `DecisionBundle.decision_status = REVIEW_REQUIRED` when needed.
6. Implement same-request retry reload through `reload_terminal_bundle_for_same_request_retry.ts` so terminal manifests reload the existing bundle instead of allocating continuations.
7. Add tests for clean-complete, review-required, pre-start blocked, post-start blocked, authority-pending, replay-attestation-ready, and same-request retry reload scenarios.

Gap handling you must perform:
- Close the gap where `DecisionBundle` is richly specified before one authoritative finalization service exists.
  Build one finalizer that owns lifecycle state, reason bridge, truth boundary, and terminal ref validation.
- Close the gap where top-level reason fields could drift from the persisted gate chain.
  Persist one canonical summary and make downstream reload surfaces consume it directly.
- Close the gap where resolved workflow refs might remain attached after authority resolution or completion.
  Filter open work explicitly and reject stale/closed refs.
- Close the gap where pre-start blocked outcomes could reference artifacts that do not exist yet.
  Enforce the documented pre-start constraints and fail closed on impossible refs.
- Close the gap where replay-safe terminal reload could be forced to recompute child artifacts.
  Persist enough bridge fields and hashes so reload stays read-only.

Mandatory edge cases:
- review-required bundles must still leave `RunManifest.lifecycle_state = COMPLETED`.
- `primary_action_code` must appear in `next_action_codes[]` and never in `blocked_action_codes[]` when `actionability_state = ACTION_AVAILABLE`.
- a non-null `primary_proof_bundle_ref` must imply a non-null `graph_id`.
- a non-null `twin_id` must imply non-null `graph_id` and non-null `parity_id`.
- same-request retry against a terminal manifest must reload the persisted bundle rather than allocate a continuation child.
- authority-resolved postures (`CONFIRMED`, `REJECTED`, `UNKNOWN`, `OUT_OF_BAND`, and their authority-prefixed equivalents) must drive terminal status and truth posture explicitly.
- pre-start blocked bundles must not reference snapshot, gate records, or other artifacts forbidden by the corpus.
- `dominant_reason_code` must equal the first emitted compressed summary reason and remain consistent with the explainability contract.
- deterministic hash computation must be byte-stable across replay.

Backend, performance, and security requirements:
- Treat finalization as a transactional write boundary.
- Reuse canonical hashing and ordering helpers from earlier cards.
- Do not let UI code or transport code compute outcome class or truth posture independently.
- Log only bounded ids, statuses, and reason codes; avoid dumping sensitive artifact payloads.

Optional internal verification surface (only if you add one):
- If you add a read-only terminal-outcome inspector, place it behind an internal route such as `/internal/decision/outcome/:decisionBundleId`.
- The UI must feel like a premium decision dossier:
  top header for manifest / outcome / persisted hash,
  left column for status, waiting-on, checkpoint, truth, and actionability,
  large center narrative surface for ordered reasons and child artifact spine,
  narrow right column for open workflows and replay / proof / twin refs.
- Add one restrained “outcome bridge” diagram showing status -> waiting_on -> next_action as a thin semantic rail.
- Use the shared palette and typography contract, `18px` panel radius, `24px` rail spacing, and reduced-motion-safe `160ms` disclosure transitions.
- If this surface exists, implement and verify it with Playwright using semantic locators, keyboard navigation, actionability-safe interactions, and trace capture.

Validation rules:
- Unit-test top-level reason compression, ref implication checks, terminal hash stability, and manifest lifecycle transitions.
- Integration-test blocked, review-required, completed, authority-pending, and same-request retry reload paths.
- Validate persisted artifacts against `decision_bundle.schema.json`.
- Re-run the contract validators after assembling the implementation bundle.

Definition of done:
- Every post-allocation terminal or review outcome persists as one authoritative `DecisionBundle`.
- Same-manifest retries reload the bundle instead of recomputing terminal posture.
- Later shell, notification, replay, and filing stages can consume one durable terminal truth object without reconstructing semantics from the gate chain or UI state.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
