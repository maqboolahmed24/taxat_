---
id: pc_0232
slug: phase_05_parallel_wave_03_track_frontend_shared_004_Playwright_or_other_appropriate_tooling_implement_semantic_selector_and_accessibility_anchor_catalog
mode: parallel
phase: "05"
wave: "03"
track: "frontend_shared"
task_number: "004"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_shared_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0232

## Task Summary

004 Playwright or other appropriate tooling implement semantic selector and accessibility anchor catalog.

## Metadata

- ID: `pc_0232`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend shared`
- Task Number: `004`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_shared_004_Playwright_or_other_appropriate_tooling_implement_semantic_selector_and_accessibility_anchor_catalog`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0231](./pc_0231.md) `phase_05_parallel_wave_03_track_frontend_shared_003_Playwright_or_other_appropriate_tooling_implement_interaction_layer_foundation_and_surface_registry`
- Next: [pc_0233](./pc_0233.md) `phase_05_parallel_wave_03_track_frontend_shared_005_Playwright_or_other_appropriate_tooling_implement_frontend_state_containers_for_etags_view_guards_and_shell_tokens`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_shared_004_Playwright_or_other_appropriate_tooling_implement_semantic_selector_and_accessibility_anchor_catalog`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0231` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the semantic selector and accessibility anchor catalog that binds browser `data-testid`, ARIA landmarks/headings, focus order, live-region policy, and future native identifiers to the same source-of-truth semantics.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The algorithm treats selectors as semantic contract anchors, not test conveniences. `SemanticAccessibilityContract` freezes shell family, selector profile, anchor codes, focus order, live update behavior, artifact handoff policy, and reduced-motion policy. Therefore the frontend must expose a typed anchor catalog and helpers that components consume directly, so automation and accessibility cannot drift from shell meaning.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `semantic_selector_and_accessibility_contract.md end to end`.
- `semantic_selector_and_accessibility_regression_pack_contract.md end to end`.
- `frontend_shell_and_interaction_law.md sections 8 and 10`.
- `low_noise_experience_contract.md minimum semantic selectors and validation additions`.
- `UIUX_DESIGN_SKILL.md selector strategy, governance selector strategy, portal selector strategy`.
- `schemas/semantic_accessibility_contract.schema.json and semantic_accessibility_regression_pack.schema.json`.
- `schemas/focus_restoration_contract.schema.json and focus_restore_return_target_harness.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/accessibility/semantic_anchor_catalog.ts`
2. `packages/frontend-shell-core/src/accessibility/semantic_accessibility_contract_builder.ts`
3. `packages/frontend-shell-core/src/accessibility/focus_order_profiles.ts`
4. `packages/frontend-shell-core/src/accessibility/live_region_policy.ts`
5. `packages/shared-ui/src/accessibility/SemanticAnchor.tsx`
6. `packages/shared-ui/src/accessibility/LandmarkFrame.tsx`
7. `packages/playwright-kit/src/fixtures/semantic_accessibility_fixture.ts`
8. `tests/playwright/frontend/semantic_anchor_catalog.spec.ts`
9. `tests/playwright/frontend/accessibility_keyboard_live_region.spec.ts`
10. `docs/frontend/semantic_selector_and_accessibility_anchor_catalog.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Create an internal “Anchor Catalog” route that looks like a typographic index, not a code dump. Group anchors by shell family and surface, with tiny focus-order arrows and live-region badges. Include a narrow “screen-reader path” column with human-readable labels. Use no charts except a simple focus-order ladder; the visual goal is clarity and auditability.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- mirror required schema anchor codes and focus region codes exactly; do not invent visually named aliases
- create helpers that attach `data-testid`, `aria-label`/`aria-labelledby`, role/landmark, heading level, and optional native identifier names from one catalog entry
- define required anchors for calm shell: `low-noise-shell`, `shell-family`, `object-anchor`, `dominant-question`, `settlement-posture`, `recovery-posture`, `context-bar`, `decision-summary`, `action-strip`, `primary-action`, `no-safe-action`, `detail-drawer`, `detail-entry-{module_code}`
- define portal and governance anchor groups for later compatibility without leaking internal-only anchors into portal routes
- include a regression-pack fixture builder with deterministic cases for keyboard-only, screen-reader, and reduced-motion modalities

Functional, architectural, and edge-case gaps to close:
- close the gap where Playwright selectors are added ad hoc after components exist
- close the gap where browser identifiers and native identifiers drift
- close the gap where live updates announce too much, too little, or steal focus
- close the gap where conditional notices lack stable anchors for stale/recovery/limitation states

Mandatory edge cases:
- duplicated anchor codes in the same semantic scope must fail tests
- hidden/limited content must not be represented by absent anchors when a limitation notice is required
- live updates must not move focus from active composer/editor/file picker/comparison controls
- reduced-motion mode must preserve announcements and focus order

Playwright, accessibility, and validation requirements:
- Playwright-test keyboard traversal through shell family, object anchor, dominant question, action strip, support region, and return controls
- assert required anchors exist by semantic profile and illegal visual selectors are absent from new shared components
- use accessibility-oriented assertions for headings, roles, names, focus visibility, and polite/assertive live-region behavior
- generate or validate sample semantic regression pack fixtures and run validator/forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- one typed anchor catalog drives browser selectors, ARIA, focus order, and native identifier names
- Playwright can verify accessibility semantics without brittle visual selectors
- semantic regression fixtures exist for the shell profiles touched in this tranche
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

### Implementation Notes

- Added the typed semantic accessibility catalog in `packages/frontend-shell-core/src/accessibility/semantic_anchor_catalog.ts`, including required surface inventories, browser/native identifier parity, focus-region mapping, live-region metadata, dynamic calm detail entries, and fail-closed catalog assertions.
- Added `semantic_accessibility_contract_builder.ts`, `focus_order_profiles.ts`, and `live_region_policy.ts` to build schema-valid semantic accessibility contracts, deterministic regression-pack fixtures, keyboard paths, and live-update focus policy.
- Added shared UI accessibility snapshots in `packages/shared-ui/src/accessibility/SemanticAnchor.tsx` and `packages/shared-ui/src/accessibility/LandmarkFrame.tsx`.
- Added the Playwright fixture `packages/playwright-kit/src/fixtures/semantic_accessibility_fixture.ts` and exported it from `packages/playwright-kit/src/index.ts`.
- Added the internal Anchor Catalog route descriptor at `apps/operator-web/src/routes/internal/semantic-anchor-catalog.tsx` and the static browser route under `apps/operator-web/public/internal/semantic-anchor-catalog/`.
- Added unit coverage in `tests/unit/frontend-shell-core/semantic_anchor_catalog.spec.ts` and browser coverage in `tests/playwright/frontend/semantic_anchor_catalog.spec.ts` plus `tests/playwright/frontend/accessibility_keyboard_live_region.spec.ts`.
- Added documentation in `docs/frontend/semantic_selector_and_accessibility_anchor_catalog.md`.

### Verification Evidence

- `pnpm exec playwright test --config=playwright.config.ts --project=unit tests/unit/frontend-shell-core/semantic_anchor_catalog.spec.ts` - pass, 5 tests.
- `pnpm exec playwright test --config=playwright.config.ts --project=browser tests/playwright/frontend/semantic_anchor_catalog.spec.ts tests/playwright/frontend/accessibility_keyboard_live_region.spec.ts` - pass, 6 tests.
- `pnpm exec playwright test --config=playwright.config.ts --project=browser tests/playwright/frontend/shared_workspace_scaffold.spec.ts tests/playwright/frontend/shared_workspace_route_contracts.spec.ts tests/playwright/frontend/cross_shell_theme_runtime.spec.ts tests/playwright/frontend/interaction_layer_surface_registry.spec.ts tests/playwright/frontend/semantic_anchor_catalog.spec.ts tests/playwright/frontend/accessibility_keyboard_live_region.spec.ts` - pass, 31 tests.
- `pnpm exec playwright test --config=playwright.config.ts --project=unit tests/unit/frontend-shell-core/interaction_layer_foundation.spec.ts tests/unit/frontend-shell-core/semantic_anchor_catalog.spec.ts` - pass, 10 tests.
- `PYTHONDONTWRITEBYTECODE=1 ./.venv/bin/python3 Algorithm/scripts/validate_contracts.py --self-test` - pass.
- `PYTHONDONTWRITEBYTECODE=1 ./.venv/bin/python3 Algorithm/tools/forensic_contract_guard.py` - pass.
- `pnpm run validate-contracts` - pass.
- `git diff --check` - pass.
- `pnpm exec tsc --noEmit -p tsconfig.quality.json` - fails only in pre-existing `packages/domain-kernel/src/**` and `packages/generated-models/src/generated/typescript/**`; no `pc_0232` implementation files appear in the TypeScript errors after fixes.
