---
id: pc_0179
slug: phase_04_parallel_wave_02_track_backend_portal_001_implement_client_portal_workspace_route_specific_derivatives_and_view_guards
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "001"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0179

## Task Summary

implement client portal workspace route specific derivatives and view guards.

## Metadata

- ID: `pc_0179`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `001`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_001_implement_client_portal_workspace_route_specific_derivatives_and_view_guards`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0178](./pc_0178.md) `phase_04_parallel_wave_02_track_backend_low_noise_010_implement_semantic_accessibility_contract_data_publication_for_all_governed_surfaces`
- Next: [pc_0180](./pc_0180.md) `phase_04_parallel_wave_02_track_backend_portal_002_implement_client_document_request_and_current_vs_historical_upload_projection_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0174_to_0181.md](../shared_operating_contract_0174_to_0181.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_001_implement_client_portal_workspace_route_specific_derivatives_and_view_guards`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0178` if they exist, especially `pc_0163` through `pc_0165`, `pc_0174` through `pc_0178`, and the cross-device/cache-isolation groundwork from earlier phases.

Mission:
Implement the canonical `ClientPortalWorkspace` projector and its route-specific derivative/view-guard layer so the customer portal stops being a thin composition of ad hoc route fragments.
The output must preserve one `CLIENT_PORTAL_SHELL`, one top-level route, one contextual object when needed, one view guard basis, and one grouped continuity/accessibility/interaction spine.
It must be customer-safe, same-shell, reconnect-safe, and explicit about stale/degraded/review-required posture.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially route architecture, route-context rules, contextual request-detail routes, read-model/API translation requirements, selectors, validation minimum, and cache-isolation note.
- `frontend_shell_and_interaction_law.md`, especially shell ownership, route continuity, stable route keys, focus law, automation anchors, and current-vs-history handling.
- `cross_device_continuity_and_restoration_contract.md`.
- `cache_isolation_and_secure_reuse_contract.md`.
- `modules.md`, especially `BUILD_CLIENT_PORTAL_WORKSPACE(...)`.
- `data_model.md`, especially `ClientPortalWorkspace`, `PortalInteractionLayer`, `CrossDeviceContinuityContract`, `CacheIsolationContract`, and the portal route derivative notes.
- `schemas/client_portal_workspace.schema.json`, `schemas/portal_interaction_layer.schema.json`, `schemas/cross_device_continuity_contract.schema.json`, `schemas/cache_isolation_contract.schema.json`, `schemas/semantic_accessibility_contract.schema.json`, and `schemas/problem_envelope.schema.json` where view-guard failures surface.
- `PATCH_RESOLUTION_INDEX.md`, especially items 244-246 and the portal recovery hardening items.
- `test_vectors.md`, especially portal route, request-focus, stale review, and responsive continuity cases.

Deliverables to create:
1. `packages/backend-portal/src/projectors/build_client_portal_workspace.ts`
2. `packages/backend-portal/src/services/derive_client_portal_route_context.ts`
3. `packages/backend-portal/src/services/derive_client_portal_navigation_tabs.ts`
4. `packages/backend-portal/src/services/derive_client_portal_view_guard_and_stability.ts`
5. `packages/backend-portal/src/services/derive_client_portal_workspace_posture.ts`
6. `packages/backend-portal/src/services/derive_client_portal_reliability_summary.ts`
7. `packages/backend-portal/src/services/derive_client_portal_cache_isolation_contract.ts`
8. `packages/backend-portal/src/services/apply_contextual_route_fallback_rules.ts`
9. `packages/backend-portal/src/tests/client_portal_workspace_route_derivatives.spec.ts`
10. `docs/portal/client_portal_workspace_route_derivatives_and_view_guards.md`
11. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/workspace-preview-lab.tsx`

You MUST also patch any existing northbound portal route/query adapters so they consume this projector layer rather than assembling portal workspace fragments inline.

Required design decisions you must make explicitly:
- define whether route-specific northbound reads reuse the full `ClientPortalWorkspace` or publish tightly governed derivatives that still keep the same shell/stability spine; do not let route derivatives become a second shell language
- define the canonical meaning of `object_anchor_ref` on top-level routes versus contextual request/approval/onboarding/help routes
- define how `workspace_version`, `view_guard_ref`, and grouped `stability_contract` are composed, when they rotate, and how they are threaded through every route derivative
- define the exact `route_context` grammar for `return_route`, `focus_anchor_ref`, `artifact_focus_bucket_or_null`, `artifact_focus_subject_ref_or_null`, `return_focus_anchor_ref_or_null`, and fallback behavior
- define how `workspace_posture`, top-level `settlement_state`, top-level `recovery_posture`, and `reliability_summary` stay derived from durable truth rather than renderer-local guesses

Execution requirements:
- publish one `ClientPortalWorkspace` root projector with `shell_family = CLIENT_PORTAL_SHELL`
- emit one shared spine per workspace: `dominance_contract`, `state_taxonomy_contract`, `cross_device_continuity_contract`, `cache_isolation_contract`, `semantic_accessibility_contract`, `interaction_layer`, `language_contract`, `workspace_version`, `freshness_state`, `view_guard_ref`, and grouped `stability_contract`
- top-level `route` must always name the active portal tab; contextual detail must keep that parent tab while switching `object_anchor_ref` to the focused request/approval/onboarding/help object
- `navigation_tabs[]` must keep canonical labels and at most five destinations, with `Home` and `Help` badge-free
- outside the dedicated `Help` route, promote at most one support region at a time
- when `freshness_state != FRESH`, downgrade unsafe mutation to an explicit review or refresh path and keep the same mounted route visible
- contextual deep-link routes must preserve return route and focus anchor across rebase when the object still exists; otherwise fall back deterministically with explicit reason posture
- if you add the preview lab, build an actual portal-style preview shell: one primary column, `PORTAL_HEADER -> STATUS_HERO -> TASK_QUEUE -> RECENT_ACTIVITY` on `HOME`, quiet typography, no dashboard wall, and one explicit return-path indicator on contextual routes

Gap handling you must close while implementing:
- close active-tab drift so `route` and the single active `navigation_tabs[]` entry cannot disagree
- close contextual return drift so `route_context.return_route` always mirrors the parent tab on contextual routes
- close the gap where portal reliability and stale/review-required posture are reconstructed client-side instead of published by the backend
- close the gap where current support, limitation, and draft-resume posture can overpromote more than one support region at once
- close the gap where route derivatives could overfetch expert/staff-only data and rely on the renderer to hide it

Mandatory edge cases:
- there MUST be exactly one active top-level tab and it MUST match `route`
- `route = HOME` MUST emit `home_surface_order = [PORTAL_HEADER, STATUS_HERO, TASK_QUEUE, RECENT_ACTIVITY]`
- contextual routes MUST preserve the parent top-level tab and exact return focus anchor
- `Home` and `Help` MUST remain badge-free
- stale or degraded portal workspaces MUST keep the focused route/object mounted and downgrade the dominant CTA to review/recovery posture instead of throwing the user back to a generic list
- if the contextual object disappears, fallback must land on the narrowest lawful surviving parent target, not a broad portal root
- customer-safe limitation notices must remain explicit; hidden content may not disappear silently

Playwright and contract-test requirements:
- unit-test the workspace projector and route-context/fallback rules directly
- patch or add Playwright APIRequestContext coverage against the shipped portal workspace reads if they already exist
- if you add the preview lab, add Playwright browser tests using semantic locators, keyboard navigation, reduced-motion mode, and trace capture on failure
- prove that refresh, reconnect, and responsive collapse preserve the same tab, same object, same return path, and same dominant action meaning when the object remains lawful

Backend, performance, and security requirements:
- role-filter and customer-safe-filter everything before serialization
- preserve exact cache isolation and visibility partitioning; do not widen customer-safe reuse across route, object, masking, or access-binding changes
- keep route derivatives deterministic and thin; the projector layer, not the renderer, owns shell continuity
- fail closed on mixed-generation view guards, route-context drift, or visibility-partition mismatch

Validation rules:
- validate `ClientPortalWorkspace` and all embedded contracts against schema
- add regression coverage for active-tab alignment, contextual return route alignment, stale/review-required posture, and support-region singularity
- ensure northbound query adapters only consume this projector layer instead of rebuilding route fragments independently

Definition of done:
- the repo has one canonical `ClientPortalWorkspace` projector and route-derivative spine
- portal route continuity, view-guard rotation, reliability posture, and contextual return semantics become backend-authored
- the customer portal stays one calm shell instead of a set of loosely related route fragments
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
