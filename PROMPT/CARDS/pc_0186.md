---
id: pc_0186
slug: phase_04_parallel_wave_02_track_backend_portal_008_implement_portal_help_request_projection_and_boundary_handoff_tracking
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "008"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0186

## Task Summary

implement portal help request projection and boundary handoff tracking.

## Metadata

- ID: `pc_0186`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `008`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_008_implement_portal_help_request_projection_and_boundary_handoff_tracking`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0185](./pc_0185.md) `phase_04_parallel_wave_02_track_backend_portal_007_implement_client_timeline_event_projection_and_customer_safe_activity_compression`
- Next: [pc_0187](./pc_0187.md) `phase_04_parallel_wave_02_track_backend_portal_009_implement_portal_language_contract_projection_and_banned_internal_vocabulary_filters`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0182_to_0189.md](../shared_operating_contract_0182_to_0189.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_008_implement_portal_help_request_projection_and_boundary_handoff_tracking`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0185` if they exist, especially `pc_0142` through `pc_0149`, `pc_0164`, and `pc_0179`.

Mission:
Implement the `PortalHelpRequest` projection and the support-boundary handoff tracking that lets clients ask for help without losing the exact case context, route, request lineage, or focus anchor they were in when they asked.
This task must prevent support from degrading into generic tickets that force the client to restate governed context.
The resulting artifact needs to preserve whether help came from the general Help route or from a contextual request-detail problem, and it needs to keep the portal return path explicit across acknowledgement, response, and close posture.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially `Help`, `Contextual request-detail routes`, the support-panel rules in `Read-model and API translation requirements`, and the Playwright minimum that requires exact focus-anchor preservation through contextual help flows.
- `northbound_api_and_session_contract.md`, especially `CLIENT_PORTAL_REQUEST_HELP`, `RequestInfoRecord` linkage, stale portal guards, and typed problem-envelope recovery.
- `data_model.md`, especially `PortalHelpRequest`.
- `modules.md`, especially the support-panel and contextual portal-route rules.
- `frontend_shell_and_interaction_law.md`, especially same-object same-shell continuity, promoted-support-surface rules, and route-stable return behavior.
- `schemas/portal_help_request.schema.json`, `schemas/problem_envelope.schema.json`, and any collaboration/request-info schemas already created in earlier tranches.
- `PATCH_RESOLUTION_INDEX.md`, especially the portal help-route hardening items around `REQUEST_DETAIL`, `item_id`, `source_focus_anchor_ref`, request-info lineage, closure timing, and reason-family route alignment.
- `test_vectors.md`, especially contextual help and support continuity regressions.

Deliverables to create:
1. `packages/backend-portal/src/projectors/build_portal_help_request.ts`
2. `packages/backend-portal/src/services/derive_help_request_route_and_reason_binding.ts`
3. `packages/backend-portal/src/services/build_help_case_context_refs.ts`
4. `packages/backend-portal/src/services/track_portal_help_boundary_handoff.ts`
5. `packages/backend-portal/src/tests/portal_help_request_projection.spec.ts`
6. `packages/backend-portal/src/tests/portal_help_request_boundary_handoff.spec.ts`
7. `docs/portal/portal_help_request_projection_and_boundary_handoff.md`
8. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/help-handoff-lab.tsx`

You MUST patch any existing contextual help command or support-panel read adapter so it emits this artifact and its return-path context rather than inventing support routing in the frontend.

Required design decisions you must make explicitly:
- define how `support_channel` and `reason_family` are derived from source route and live request context
- define the exact composition and size limits of `case_context_refs[]` so the artifact carries enough context without becoming an uncontrolled dump of unrelated refs
- define how `request_info_ref` links to the collaboration/request-for-info model when help is contextual to a live request-detail route
- define how acknowledgement, response, and closure on the support side preserve the same portal return target and exact focus anchor when the client reopens the task

Execution requirements:
- publish schema-valid `PortalHelpRequest` artifacts for both dedicated Help-route and contextual request-detail help flows
- keep `source_focus_anchor_ref` non-empty and route-appropriate
- keep `case_context_refs[]` non-empty and tied to the same governed object scope that produced the help request
- `support_channel = CONTEXTUAL_REQUEST` must require `request_info_ref`
- `source_route = REQUEST_DETAIL` must carry a non-null `item_id`
- linked request-for-info help must stay on `source_route = REQUEST_DETAIL`
- closure, acknowledgement, and response timestamps must remain explicit and lifecycle-consistent
- outside the dedicated Help route, keep support subordinate to the active task; do not mount the whole help stack on ordinary routes
- if you add the lab, make it a context strip plus one support option list and one exact return-path explainer; no generic ticket inbox

Gap handling you must close while implementing:
- close any gap where contextual help from a request-detail view loses `item_id`, request lineage, or exact focus anchor
- close any gap where Help and request-detail support are conflated into one channel without route-accurate reason-family semantics
- close any gap where closure or response timing can be present without the correct lifecycle state
- close any gap where clients are forced to restate already-governed context because `case_context_refs[]` or `request_info_ref` are missing

Mandatory edge cases:
- `source_route = REQUEST_DETAIL` requires a non-null `item_id`
- any `request_info_ref` requires `source_route = REQUEST_DETAIL`
- contextual request help requires `request_info_ref`
- `closed_at` may only be present when `lifecycle_state = CLOSED`
- `reason_family = DOCUMENT_HELP` must use `DOCUMENTS` or `REQUEST_DETAIL`
- `reason_family = APPROVAL_HELP` must use `APPROVALS`
- `reason_family = ONBOARDING_HELP` must use `ONBOARDING`
- `reason_family = ACCESS_HELP` must use `HELP` or `ONBOARDING`
- `reason_family = GENERAL_HELP` must use `HELP`

Playwright and contract-test requirements:
- add direct tests for reason-family route validation, request-info linkage, lifecycle chronology, and non-empty context preservation
- if support or request-detail routes exist, add Playwright APIRequestContext tests for `CLIENT_PORTAL_REQUEST_HELP` command behavior and typed stale / recovery failure publication
- if you add the lab, add Playwright browser tests that open contextual help from a request-detail target, submit the request, and confirm the same return route and focus anchor survive the handoff using semantic locators only
- capture traces or serialized help-request payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- help route order must remain `HELP_OPTIONS -> TOP_QUESTIONS -> CASE_CONTEXT_PANEL`
- max width `1100px`, background `#F6F4EE`, surface `#FFFFFF`, muted support surface `#F3F4F6`
- `HELP_OPTIONS` should be a short list of clear channels, not a dense directory
- `TOP_QUESTIONS` must stay bounded and contextual; never a generic FAQ wall
- `CASE_CONTEXT_PANEL` should look like a compact context memo with exact request / approval / onboarding anchors and a visible return target
- on non-Help routes, show at most one promoted support region and keep it subordinate to the active task card
- motion stays subtle and reduced-motion-safe

Backend, performance, and security requirements:
- keep help-request identity and context server-authored; the client may provide intent but not authoritative route context
- do not leak staff-only refs, internal participants, or audit-only counts into portal help artifacts
- keep collaboration linkage append-only and traceable so support handoff can be audited without widening customer-visible scope
- fail closed on cross-tenant, cross-client, or wrong-route linkage attempts

Validation rules:
- validate every emitted `PortalHelpRequest` against schema
- add regression coverage for contextual request-detail help, route / reason-family mismatch, closure timing, and return-path continuity
- keep help-request projection reusable by both portal read routes and collaboration-side handoff tooling

Definition of done:
- the repo has one authoritative `PortalHelpRequest` projector and one support-boundary handoff tracker
- help requests now preserve the same route, same focus, and same governed case context instead of collapsing into generic tickets
- portal support becomes a lawful same-shell continuity feature rather than a route-breaking escape hatch
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
