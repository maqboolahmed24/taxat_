---
id: pc_0239
slug: phase_05_parallel_wave_03_track_frontend_shared_011_Playwright_or_other_appropriate_tooling_implement_focus_keyboard_live_region_and_reduced_motion_utilities
mode: parallel
phase: "05"
wave: "03"
track: "frontend_shared"
task_number: "011"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_shared_011
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0239

## Task Summary

011 Playwright or other appropriate tooling implement focus keyboard live region and reduced motion utilities.

## Metadata

- ID: `pc_0239`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend shared`
- Task Number: `011`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_shared_011_Playwright_or_other_appropriate_tooling_implement_focus_keyboard_live_region_and_reduced_motion_utilities`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0238](./pc_0238.md) `phase_05_parallel_wave_03_track_frontend_shared_010_Playwright_or_other_appropriate_tooling_implement_cache_isolation_storage_partition_and_session_scoped_persistence`
- Next: [pc_0240](./pc_0240.md) `phase_05_parallel_wave_03_track_frontend_shared_012_Playwright_or_other_appropriate_tooling_implement_responsive_shell_continuity_and_return_path_utilities`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_shared_011_Playwright_or_other_appropriate_tooling_implement_focus_keyboard_live_region_and_reduced_motion_utilities`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0238` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement shared focus, keyboard, live-region, and reduced-motion utilities that encode the algorithm’s accessibility/focus law into reusable frontend primitives.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The source algorithm says focus order follows visible semantic order, live updates must not steal focus, support-region close returns focus to invoker/lawful ancestor, everything important is keyboard operable, and reduced motion preserves meaning. These requirements need executable utilities and Playwright harnesses, not scattered component-specific behavior.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `frontend_shell_and_interaction_law.md section 8`.
- `semantic_selector_and_accessibility_contract.md required rules`.
- `semantic_selector_and_accessibility_regression_pack_contract.md coverage requirements`.
- `focus_restoration_and_return_target_harness_contract.md required rules`.
- `low_noise_experience_contract.md validation additions and responsive focus rules`.
- `UIUX_DESIGN_SKILL.md Playwright/XCUITest guidance and selector strategy`.
- `schemas/focus_restoration_contract.schema.json, focus_restore_return_target_harness.schema.json, semantic_accessibility_contract.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/accessibility/focus_manager.ts`
2. `packages/frontend-shell-core/src/accessibility/return_focus_stack.ts`
3. `packages/frontend-shell-core/src/accessibility/keyboard_roving.ts`
4. `packages/frontend-shell-core/src/accessibility/live_region_controller.ts`
5. `packages/frontend-shell-core/src/accessibility/reduced_motion.ts`
6. `packages/shared-ui/src/accessibility/LiveRegionHost.tsx`
7. `packages/shared-ui/src/accessibility/FocusRing.css`
8. `tests/unit/frontend-shell-core/focus_keyboard_live_region.spec.ts`
9. `tests/playwright/frontend/focus_keyboard_live_region_reduced_motion.spec.ts`
10. `docs/frontend/focus_keyboard_live_region_and_reduced_motion_utilities.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Make focus visible and premium: a two-layer ring using the shell accent plus an inner light offset, never relying on color alone. Live-region changes should be visually mirrored as quiet inline text changes, not floating alerts. Reduced-motion should use instant state changes or short fades without spatial travel.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- implement a focus manager that restores exact focus where possible and otherwise follows `EXACT_FOCUS -> REMAPPED_FOCUS -> OBJECT_SUMMARY -> PARENT_RETURN -> INVALIDATED` semantics
- provide return-focus stack helpers for drawers, support surfaces, artifact preview, help handoff, step-up return, and responsive collapse
- implement roving keyboard utilities for tablists/entry lists where appropriate while preserving ordinary tab order for primary shell surfaces
- create polite/assertive live-region controller keyed by announced change kinds, with duplicate-announcement compression
- create `useReducedMotion`/motion token helpers that components must use for all shell transitions

Functional, architectural, and edge-case gaps to close:
- close the gap where focus restoration is implemented per component and loses return targets
- close the gap where live updates announce duplicate posture changes across surfaces
- close the gap where keyboard-only users cannot reach preview/export/recovery controls
- close the gap where reduced motion is a CSS afterthought instead of a semantic runtime flag

Mandatory edge cases:
- closing a collapsed/redocked support region must return focus to the invoking control unless it no longer exists
- live update must not steal focus from composer/editor/file picker/comparison control
- object loss must land on object summary or parent return with reason code
- reduced-motion tests must prove state meaning remains visible without animation

Playwright, accessibility, and validation requirements:
- unit-test focus fallback order, duplicate live-region compression, roving keyboard behavior, and reduced-motion token switching
- Playwright-test keyboard-only traversal and focus restoration across support open/close, rebase notice, artifact handoff, and viewport restack
- assert focus indicators meet visible shape/contrast expectations and use semantic anchors
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- shared utilities govern focus, keyboard, live regions, and reduced motion
- components have no excuse for local focus hacks
- Playwright proves keyboard, focus restore, announcement, and reduced-motion parity
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
