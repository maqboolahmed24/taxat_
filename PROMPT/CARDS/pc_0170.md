---
id: pc_0170
slug: phase_04_parallel_wave_02_track_backend_low_noise_002_implement_context_bar_state_decision_summary_state_and_action_strip_state_projectors
mode: parallel
phase: "04"
wave: "02"
track: "backend_low_noise"
task_number: "002"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_low_noise_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0170

## Task Summary

implement context bar state decision summary state and action strip state projectors.

## Metadata

- ID: `pc_0170`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend low noise`
- Task Number: `002`
- Source Slug: `phase_04_parallel_wave_02_track_backend_low_noise_002_implement_context_bar_state_decision_summary_state_and_action_strip_state_projectors`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0169](./pc_0169.md) `phase_04_parallel_wave_02_track_backend_low_noise_001_implement_low_noise_experience_frame_projector_and_surface_budget_rules`
- Next: [pc_0171](./pc_0171.md) `phase_04_parallel_wave_02_track_backend_low_noise_003_implement_detail_drawer_state_and_support_region_promotion_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0166_to_0173.md](../shared_operating_contract_0166_to_0173.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_low_noise_002_implement_context_bar_state_decision_summary_state_and_action_strip_state_projectors`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0169` if they exist, especially `pc_0169` and the earlier manifest/trust/workflow read-model cards.

Mission:
Implement the three high-salience calm-shell surface projectors that make the shell legible without duplicating meaning across the UI: `ContextBarState`, `DecisionSummaryState`, and `ActionStripState`.
These projectors must compress the richer manifest state into literal, bounded, machine-readable payloads while preserving dominant-question and dominant-action law.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md`, especially the default visible shell semantics, copy-budget limits, action-dominance thresholds, and `NO_SAFE_ACTION` rules.
- `dominant_question_and_single_action_contract.md`.
- `frontend_shell_and_interaction_law.md`, especially one dominant question / one dominant action / one promoted support region.
- `modules.md`, especially `BUILD_CONTEXT_BAR_STATE(...)`, `BUILD_DECISION_SUMMARY_STATE(...)`, and `BUILD_ACTION_STRIP_STATE(...)`.
- `data_model.md`, especially `ContextBarState`, `DecisionSummaryState`, and `ActionStripState`.
- `schemas/context_bar_state.schema.json`, `schemas/decision_summary_state.schema.json`, and `schemas/action_strip_state.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 9-14, 404-410, and the mirror-equality hardening items.

Deliverables to create:
1. `packages/backend-low-noise/src/projectors/build_context_bar_state.ts`
2. `packages/backend-low-noise/src/projectors/build_decision_summary_state.ts`
3. `packages/backend-low-noise/src/projectors/build_action_strip_state.ts`
4. `packages/backend-low-noise/src/services/filter_visible_actions.ts`
5. `packages/backend-low-noise/src/services/score_and_select_primary_action.ts`
6. `packages/backend-low-noise/src/services/normalize_limitation_state.ts`
7. `packages/backend-low-noise/src/services/enforce_low_noise_copy_budgets.ts`
8. `packages/backend-low-noise/src/tests/low_noise_surface_projectors.spec.ts`
9. `docs/low_noise/context_summary_action_projectors.md`
10. Optional but recommended: extend `apps/admin-console-web/src/routes/debug/low-noise/frame-lab.tsx` with a surface inspector

Required design decisions you must make explicitly:
- define one shared attention/action input contract that all three projectors consume so they do not drift from each other or from the frame-level mirrors
- define exact visible-reason selection rules, including ranking, truncation, and `additional_reason_count`
- define how copy budgets are enforced without deleting decisive semantics or legal qualifiers
- define when the primary action may be mutation-capable, when it must downgrade to inspect/review/refresh, and when the shell must publish `NO_SAFE_ACTION`
- define the ownership/waiting/blocked postures so actionability is machine-readable rather than conveyed only in prose labels

Execution requirements:
- `ContextBarState` must carry orientation facts once: manifest identity, period/scope/phase, freshness, truth origin, connection state, owner/handoff posture, and mode posture
- `DecisionSummaryState` must answer what the system believes, why, and what limitation/uncertainty still applies within bounded reason/copy budgets
- `ActionStripState` must expose one dominant safe action, at most two subordinate non-mutating secondary actions, deterministic dominance metrics, and a stable suggested detail entry point
- do not let any projector infer action or salience from client layout; all scores and reason codes must come from server-authored inputs or helpers
- if you add preview UI, it should show one context strip, one summary card, and one restrained action band with exact copy-budget counts visible to engineers

Gap handling you must close while implementing:
- prior contracts make strong mirror-equality promises, but the projectors do not yet exist; implement shared helpers so summary/action/context fields cannot silently drift from frame-level mirrors
- low-noise copy budgets are easy to treat as purely presentational; enforce them in backend helpers and tests
- the mutation-dominance threshold is mathematically defined in the contract but easy to lose in UI code; keep the score and margin serialization server-authored
- if the dedicated follow-up dominant-question projector is not yet extracted, consume the narrow adapter introduced in `pc_0169` rather than recalculating hierarchy separately in each surface

Mandatory edge cases:
- `attention_state = CALM` must imply zero visible reasons and zero visible warnings
- `visible_reasons[].reason_code` must be a subset of `machine_reason_codes[]`
- mutation-capable primary actions require the frozen score threshold and dominance margin; otherwise degrade to least-destructive inspect/review/refresh or `NO_SAFE_ACTION`
- `secondary_actions` must remain a subset of `available_action_codes` and disjoint from `blocked_action_codes`
- `suppressed_secondary_count` must equal the hidden lawful remainder
- when `actionability_state = NO_SAFE_ACTION`, publish deterministic blocking reason and investigation entry point

Playwright and contract-test requirements:
- test projector outputs directly plus any preview lab that renders them
- use Playwright semantic locators if a browser surface exists; no CSS-chain selectors or fixed sleeps
- capture the serialized surface payload alongside screenshot/trace artifacts on failures
- verify reduced-motion rendering if animated disclosures are present in the preview

Backend, performance, and security requirements:
- keep action scoring and copy-budget enforcement server-side and deterministic
- never leak staff-only or masked-limited reasons into customer-safe surface candidates
- avoid recomputing expensive upstream read models; these projectors should consume already prepared truth artifacts
- keep literal status language intact; do not introduce decorative phrasing that obscures legal or workflow posture

Validation rules:
- validate each surface payload against its dedicated schema
- unit-test calm/waiting/review/blocked/limited/no-safe-action branches
- integration-test cross-surface mirror equality and dominance metrics
- verify action-strip invariants from the patch index, including hidden-secondary counts and non-empty waiting/blocking copy where required

Definition of done:
- the repo has deterministic projectors for the three highest-salience low-noise surfaces
- reason, limitation, and actionability semantics are server-authored and machine-readable
- dominant-action and no-safe-action behavior obey the algorithm’s thresholds instead of UI heuristics
- these surfaces are ready to be composed into the calm-shell frame without semantic drift.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

