---
id: pc_0110
slug: phase_03_parallel_wave_01_track_backend_collection_002_implement_source_collection_run_orchestrator_and_fetch_audit_lineage
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "002"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0110

## Task Summary

implement source collection run orchestrator and fetch audit lineage.

## Metadata

- ID: `pc_0110`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `002`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_002_implement_source_collection_run_orchestrator_and_fetch_audit_lineage`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0109](./pc_0109.md) `phase_03_parallel_wave_01_track_backend_collection_001_implement_source_plan_source_window_and_collection_boundary_models`
- Next: [pc_0111](./pc_0111.md) `phase_03_parallel_wave_01_track_backend_collection_003_implement_connector_binding_resolution_and_fetch_dispatch_pipeline`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0110_to_0117.md](../shared_operating_contract_0110_to_0117.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_002_implement_source_collection_run_orchestrator_and_fetch_audit_lineage`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0109` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0028`, `pc_0050`, `pc_0052`, `pc_0066`, `pc_0067`, `pc_0070`, `pc_0076`, `pc_0078`, `pc_0079`, `pc_0085`, `pc_0097`, `pc_0102` through `pc_0108`, and `pc_0109`.

Mission:
Implement the governed `SourceCollectionRun` control object, its named-transition helper, and the orchestration service that owns one manifest-bound collection execution from `NOT_STARTED` through `FETCHED`, `PARTIAL`, `FAILED`, or `ABANDONED`.
This task is about control-plane truth, not provider-specific transport.
It must create and preserve a stable source-window anchor before collection starts, drive lifecycle changes through the shared state-machine rules, aggregate fetch-audit lineage coming back from dispatch, and classify partial versus fatal outcomes without inferring state from queue rows or logs.

Source algorithm grounding you must honor:
- `core_engine.md`, especially the `SourceCollectionStarted` / `SourceCollectionCompleted` section, `BUILD_STAGE_TASK(...)`, `PERSIST_STAGE_WORKSET_AND_OUTBOX(...)`, `AWAIT_STAGE_WORKSET(...)`, fetch-result merge, and collection-boundary handoff.
- `modules.md`, especially `COLLECT_SOURCES(...)`, `MATERIALIZE_SOURCE_WINDOW(...)`, and `TRANSITION_SOURCE_COLLECTION_RUN(...)`.
- `data_model.md`, especially `SourceCollectionRun`, `SourceWindow`, and the collection-boundary family.
- `state_machines.md`, especially `SourceCollectionRun.lifecycle_state` and the named legal transitions.
- `deployment_and_resilience_contract.md`, `observability_and_audit_contract.md`, and `error_model_and_remediation_model.md` for resumability, append-only audit, and typed failure posture.
- Existing schemas including `source_collection_run.schema.json`, `state_transition_contract.schema.json`, `source_window.schema.json`, and `problem_envelope.schema.json` where reusable.

Deliverables to create:
1. `packages/backend-collection/src/models/source_collection_run.ts`.
2. `packages/backend-collection/src/repositories/source_collection_run_repository.ts`.
3. `packages/backend-collection/src/services/source_window_anchor_allocator.ts`.
4. `packages/backend-collection/src/services/create_source_collection_run.ts`.
5. `packages/backend-collection/src/services/transition_source_collection_run.ts`.
6. `packages/backend-collection/src/services/source_collection_run_orchestrator.ts`.
7. `packages/backend-collection/src/services/fetch_result_rollup.ts`.
8. `packages/backend-collection/src/services/fetch_audit_lineage_aggregator.ts`.
9. `packages/backend-collection/src/services/partial_gap_classifier.ts`.
10. `packages/backend-collection/src/types/source_collection_outcome.ts`.
11. `db/migrations/phase03_0110_source_collection_run.sql`.
12. `docs/collection/source_collection_run_orchestrator_and_fetch_audit_lineage.md`.
13. `tests/unit/backend_collection/source_collection_run_orchestrator.spec.ts`.
14. `tests/integration/backend_collection/source_collection_run_lifecycle.spec.ts`.

Required design decisions you must make explicit:
- Define how a non-null `source_window_ref` is allocated before `SourceWindow` is materialized.
  The same ref must be reused byte-for-byte by later `SourceWindow` persistence.
- Define the exact mapping from downstream fetch rollup to `FETCHED`, `PARTIAL`, `FAILED`, and `ABANDONED`.
  Partial gap posture must remain distinct from fatal provider failure and from system fault.
- Define canonical ordering and dedupe rules for `fetch_audit_refs[]` and `partial_gap_refs[]`.
- Define the typed reason-code families for failure and abandonment.
  Reason codes must be stable, log-safe, and reusable by the northbound problem layer.
- Define idempotent resume semantics when the same manifest attempts to start collection more than once.

Execution steps:
1. Create the schema-backed `SourceCollectionRun` model and repository with indexes on `manifest_id`, `lifecycle_state`, and `source_window_ref`.
2. Implement `source_window_anchor_allocator.ts` so collection run creation reserves a stable future source-window ref without prematurely pretending the `SourceWindow` artifact already exists.
3. Implement `transition_source_collection_run.ts` using the shared state-transition machinery from earlier cards rather than ad hoc status mutation.
4. Implement `source_collection_run_orchestrator.ts` so it creates or reuses the collection run, transitions to `FETCHING`, invokes the dispatch seam from `pc_0111`, awaits normalized results, aggregates audit refs, classifies gaps, and persists the correct terminal state.
5. Implement `fetch_result_rollup.ts` and `fetch_audit_lineage_aggregator.ts` so source-domain results are reduced deterministically and the resulting audit lineage remains stable under replay/retry.
6. Implement `partial_gap_classifier.ts` so non-fatal provider gaps become explicit refs rather than disappearing into controller-local warnings.
7. Add tests for first-run start, idempotent restart, successful terminal fetch, partial fetch with gap refs, fatal failure, and operator abandonment from `PARTIAL`.

Gap handling you must perform:
- Close the gap where `SourceCollectionRun` requires `source_window_ref` before `SourceWindow` exists.
  Introduce a durable source-window anchor allocation rule instead of leaving the field null or faking a finished artifact.
- Close the gap where partial collection posture could otherwise be inferred from missing rows, queue retries, or log text.
- Close the gap where failure reasons and partial-gap refs exist only as strings without a stable classification policy.
- Close the gap where repeated start attempts on the same manifest could allocate duplicate collection-run objects or duplicate future source-window refs.

Mandatory edge cases:
- creating a collection run for the same manifest twice must reuse or deterministically reject rather than duplicate the active run.
- `NOT_STARTED` must carry null start/completion timestamps and no gap/reason refs.
- `FETCHING` must carry `started_at_or_null` but not `completed_at_or_null`.
- `PARTIAL` must require at least one `partial_gap_ref` and a completion timestamp.
- `FAILED` must require a typed failure reason and a completion timestamp.
- `ABANDONED` must require a typed abandonment reason and a completion timestamp.
- `completed_at_or_null < started_at_or_null` must fail closed.
- duplicate `fetch_audit_refs[]` or `partial_gap_refs[]` must be deduped deterministically before persistence.
- downstream dispatch returning both fatal failure and successful pages must not be normalized into `FETCHED`; it must stay `FAILED` or `PARTIAL` based on your explicit rule set.

Backend, performance, and security requirements:
- The orchestrator must be deterministic, resumable, and idempotent.
- Persist one manifest-bound collection run unless the model explicitly supports supersession later; do not create controller-local pseudo-runs.
- Keep the orchestration layer provider-neutral; transport details belong to the dispatch seam from `pc_0111`.
- Never log raw payload bodies, credentials, tokens, or provider secrets inside run transitions.
- Use compare-and-swap or equivalent lease-safe writes so concurrent workers cannot both advance the same run illegally.

Validation rules:
- Unit-test every legal and illegal state transition.
- Unit-test source-window-anchor reuse across replayed or restarted collection attempts.
- Integration-test manifest-bound creation, idempotent resume, partial classification, and append-only audit aggregation.
- Validate stored `SourceCollectionRun` payloads against `source_collection_run.schema.json`.
- Prove that no runtime path can produce a terminal collection state without the typed reason/gap fields required by schema and state machine.

Definition of done:
- The repo has a governed `SourceCollectionRun` aggregate with repository, transitions, and orchestration.
- Collection execution no longer relies on controller-local status text or log reconstruction.
- Later cards can plug connector dispatch, boundary freezing, and input-freeze assembly into one stable collection-run spine.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
