---
id: pc_0250
slug: phase_05_parallel_wave_03_track_frontend_low_noise_010_Playwright_or_other_appropriate_tooling_implement_low_noise_budget_audit_overlay_and_developer_diagnostics
mode: parallel
phase: "05"
wave: "03"
track: "frontend_low_noise"
task_number: "010"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_low_noise_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0250

## Task Summary

010 Playwright or other appropriate tooling implement low noise budget audit overlay and developer diagnostics.

## Metadata

- ID: `pc_0250`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend low noise`
- Task Number: `010`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_low_noise_010_Playwright_or_other_appropriate_tooling_implement_low_noise_budget_audit_overlay_and_developer_diagnostics`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0249](./pc_0249.md) `phase_05_parallel_wave_03_track_frontend_low_noise_009_Playwright_or_other_appropriate_tooling_implement_artifact_handoff_return_controls_and_current_vs_historical_views`
- Next: [pc_0251](./pc_0251.md) `phase_05_parallel_wave_03_track_frontend_low_noise_011_Playwright_or_other_appropriate_tooling_implement_same_object_route_continuity_and_back_return_behavior`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_low_noise_010_Playwright_or_other_appropriate_tooling_implement_low_noise_budget_audit_overlay_and_developer_diagnostics`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0249` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the low-noise budget audit overlay and developer diagnostics for `CALM_SHELL`, making the invisible compression contract inspectable without widening the production first view.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The algorithm converts calm-shell compression into a deterministic audit artifact: the shell is lawful only when `LowNoiseExperienceFrame` and `LowNoiseBudgetAudit` agree on surface order, persistent surface count, dominant story count, visible copy footprint, duplicate posture collapse, semantic coverage, and refresh/coalescing posture. The UI therefore needs an internal diagnostic surface that reads the audit and explains why the frame is within or outside budget; it must never become a fifth production surface or a replacement for the algorithmic validator.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `low_noise_surface_compression_and_noise_budget_audit_contract.md`, especially authoritative artifacts, required rules, and failure modes closed.
- `low_noise_experience_contract.md` cognitive budget, copy budget, omission rules, validation additions, and default four-surface shell.
- `frontend_shell_and_interaction_law.md` surface topology, recovery presentation, automation anchors, focus, and motion law.
- `UIUX_DESIGN_SKILL.md` low-noise detail-drawer binding, information hierarchy, microinteraction rules, and anti-patterns.
- `schemas/low_noise_budget_audit.schema.json`, `low_noise_budget_audit_pack.schema.json`, `low_noise_experience_frame.schema.json`, `semantic_accessibility_contract.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/low-noise/low_noise_budget_audit.ts`
2. `packages/frontend-shell-core/src/low-noise/low_noise_budget_diagnostics.ts`
3. `packages/shared-ui/src/low-noise/LowNoiseBudgetAuditOverlay.tsx`
4. `packages/shared-ui/src/low-noise/LowNoiseBudgetLedger.tsx`
5. `packages/shared-ui/src/low-noise/LowNoiseDiagnosticGlyphs.tsx`
6. `apps/operator-web/src/routes/internal/low-noise-budget-lab.tsx` if internal routes are already allowed
7. `tests/unit/frontend-shell-core/low_noise_budget_audit.spec.ts`
8. `tests/playwright/frontend/low_noise_budget_audit_overlay.spec.ts`
9. `docs/frontend/low_noise_budget_audit_overlay_and_diagnostics.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Create a quiet internal “Noise Ledger” overlay launched from a tiny diagnostic chip in development/test builds only. The overlay uses a right-side 392–440px sheet or drawer module, not a modal that obscures the shell. It shows a four-plane surface-order rail, a copy/scan-load ledger, duplicate-posture collapse list, semantic-coverage verdict, and refresh/coalescing tape. Use monochrome micro-bars and labelled chips rather than KPI tiles; an optional small contract diagram may show `CONTEXT_BAR -> DECISION_SUMMARY -> ACTION_STRIP -> DETAIL_DRAWER` and where each budget count is measured.
- Budget failures use inline reason cards with machine reason codes in mono text and plain explanation. Passing states are understated; do not decorate success with celebratory motion.

Execution requirements:
- parse and validate `LowNoiseBudgetAudit` and `LowNoiseBudgetAuditPack` payloads before rendering diagnostics
- compare rendered surface order with audit order and fail closed if they diverge
- compute or verify visible shell character count from actually rendered redacted fixture copy in test fixtures rather than hidden source prose
- separate production render path from internal diagnostics so the overlay cannot count as a peer first-view surface
- show frame-level and pack-level scenario coverage, including first view, high reason pressure, no-safe-action, non-material refresh, reconnect/catch-up, and detail fallback
- serialize diagnostic test fixtures with deterministic redacted data only

Functional, architectural, and edge-case gaps to close:
- close the gap where calm-shell budget is only an editorial convention and not inspectable by developers
- close the gap where duplicate posture, copy-budget drift, and refresh rank swaps can pass through UI review unnoticed
- close the gap where visual tests assert layout but not the source audit semantics
- close the gap where diagnostic surfaces accidentally widen production shell hierarchy

Mandatory edge cases:
- audit says four surfaces but the mounted frame renders more or fewer regions
- copy budget passes per-field lengths but fails aggregate visible shell count
- duplicate masking/analysis/recovery posture appears in multiple surfaces
- non-material refresh produces rank swap without coalesced-change accounting
- diagnostic overlay opened during reduced-motion or keyboard-only navigation
- audit pack missing required scenario coverage or using non-deterministic seed

Playwright, accessibility, and validation requirements:
- unit-test audit/frame consistency, scan-load formatting, failure grouping, duplicate-posture detection, and production-vs-internal gating
- Playwright-test overlay open/close, focus restoration, four-surface rail, budget pass/fail states, reduced-motion behavior, responsive drawer sizing, and no extra production landmark
- assert diagnostic selectors are internal and semantic: `low-noise-budget-overlay`, `budget-surface-rail`, `budget-copy-ledger`, `budget-refresh-tape`
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- developers can inspect budget compliance without widening the production shell
- audit/frame mismatches fail closed and are visible in deterministic diagnostics
- Playwright proves overlay semantics, focus, reduced motion, and internal-only gating
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
