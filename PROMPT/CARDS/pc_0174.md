---
id: pc_0174
slug: phase_04_parallel_wave_02_track_backend_low_noise_006_implement_shell_state_taxonomy_contract_and_dominant_question_projection_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_low_noise"
task_number: "006"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_low_noise_006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0174

## Task Summary

implement shell state taxonomy contract and dominant question projection rules.

## Metadata

- ID: `pc_0174`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend low noise`
- Task Number: `006`
- Source Slug: `phase_04_parallel_wave_02_track_backend_low_noise_006_implement_shell_state_taxonomy_contract_and_dominant_question_projection_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0173](./pc_0173.md) `phase_04_parallel_wave_02_track_backend_low_noise_005_implement_experience_delta_publication_coalescing_and_noise_budget_controls`
- Next: [pc_0175](./pc_0175.md) `phase_04_parallel_wave_02_track_backend_low_noise_007_implement_operator_interaction_layer_and_interaction_layer_foundation_projectors`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0174_to_0181.md](../shared_operating_contract_0174_to_0181.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_low_noise_006_implement_shell_state_taxonomy_contract_and_dominant_question_projection_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0173` if they exist, especially `pc_0169` through `pc_0173`, `pc_0158` through `pc_0165`, and any earlier contract-package work.

Mission:
Implement the reusable server-authored salience layer that publishes `ShellStateTaxonomyContract` and `ShellDominanceContract` from durable state instead of renderer folklore.
This task must make the mounted shell answer three questions deterministically:
1. what absence/limitation/recovery state the user is actually in,
2. what the one dominant question is, and
3. whether there is one safe dominant action or explicit `NO_SAFE_ACTION`.
It must work first for `CALM_SHELL` and be structured so `CLIENT_PORTAL_SHELL` can reuse the same projector rules without rewriting the semantics in the browser.

Source algorithm grounding you must honor:
- `dominant_question_and_single_action_contract.md`, entire document.
- `low_noise_experience_contract.md`, especially the explicit limitation taxonomy, dominant-question, settlement-state, and shell-continuity rules.
- `frontend_shell_and_interaction_law.md`, especially sections 1, 2, 8, and 10.
- `customer_client_portal_experience_contract.md`, especially the `Home`/`Documents`/`Approvals`/`Onboarding`/`Help` route ownership rules and the read-model translation requirements.
- `modules.md`, especially `DERIVE_ATTENTION_POLICY(...)`, `FILTER_VISIBLE_ACTIONS(...)`, `NORMALIZE_LIMITATION_STATE(...)`, `BUILD_LIVE_EXPERIENCE_FRAME(...)`, and `BUILD_CLIENT_PORTAL_WORKSPACE(...)`.
- `data_model.md`, especially `ShellStateTaxonomyContract`, `LowNoiseExperienceFrame`, and `ClientPortalWorkspace`.
- `schemas/shell_state_taxonomy_contract.schema.json` and `schemas/shell_dominance_contract.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 404, 406, 408, 409, and 412 plus the portal-dominance drift items 244-246.
- `test_vectors.md`, especially the low-noise dominance, portal CTA alignment, and continuity scenarios.

Deliverables to create:
1. `packages/backend-low-noise/src/services/project_shell_state_taxonomy_contract.ts`
2. `packages/backend-low-noise/src/services/project_shell_dominance_contract.ts`
3. `packages/backend-low-noise/src/services/derive_dominant_question_and_safe_action.ts`
4. `packages/backend-low-noise/src/services/normalize_shell_limitation_and_empty_state.ts`
5. `packages/backend-low-noise/src/services/validate_shell_salience_alignment.ts`
6. `packages/backend-low-noise/src/tests/shell_state_taxonomy_and_dominance_contract.spec.ts`
7. `docs/low_noise/shell_state_taxonomy_and_dominance_projection.md`
8. Optional but recommended: `apps/admin-console-web/src/routes/debug/low-noise/salience-lab.tsx`

You MUST also patch the existing low-noise frame and surface builders from `pc_0169` through `pc_0173` so they consume this projector layer instead of carrying ad hoc salience logic.
If route-visible portal workspace builders already exist, thread the same projector contracts through them as thin reusable dependencies rather than duplicating route-local dominance code.

Required design decisions you must make explicitly:
- define one canonical projector input model for salience: which fields are authoritative for dominant question, dominant action, limitation state, settlement state, and recovery posture, and which UI-local hints are forbidden
- define the shell-family and route mapping table for `dominant_question_surface_code` and `dominant_action_surface_code`, including portal mappings for `HOME`, `DOCUMENTS`, `APPROVALS`, `ONBOARDING`, and `HELP`
- define when `safe_action_state = NO_SAFE_ACTION` is published and how that differs from merely having no currently visible mutation-capable action
- define how `current_empty_surface_code_or_null` is chosen when absence is mounted in a support or subordinate surface
- define how compare/audit explicitness is surfaced without letting low-noise or portal default first views drift into multi-focus mode

Execution requirements:
- keep the projector pure and deterministic from typed inputs; do not let view code or copy templates determine shell hierarchy
- `summary_action_alignment_policy` must guarantee that the dominant summary and dominant action answer the same question
- `supplemental_queue_policy` must keep portal `TASK_QUEUE` or other worklists subordinate to the published dominant action posture
- `support_surface_role` must remain `SUBORDINATE`, `INVESTIGATION`, or `RECOVERY`; support may never quietly become a second primary
- low-noise default mode may not publish compare or audit multi-focus; portal default mode may not publish competing hero and queue primaries
- stale, degraded, masked, or limited conditions must flow through `ShellStateTaxonomyContract` instead of appearing only in free-form copy or banner flags
- preserve the prior dominant question and action context through `RECEIPT_PENDING` rather than publishing a brand-new shell narrative
- if you add the optional salience lab, it must show one dominant ladder and one taxonomy matrix only; no dashboard wall, no competing counters, no fake analytics

Gap handling you must close while implementing:
- the corpus defines shell-family mappings and dominance invariants, but it does not give you one reusable projector boundary; create it now so future shells do not reinvent salience rules
- earlier low-noise work can still hide limitation and recovery meaning in individual surfaces; normalize those meanings into `ShellStateTaxonomyContract`
- portal hero/queue drift is explicitly called out by the source corpus; stop that drift with one shared dominance projector instead of tab-local conditionals
- current empty-state handling risks collapsing `NOT_REQUESTED`, `NOT_YET_MATERIALIZED`, `LIMITED`, and `NOT_APPLICABLE` into one weak placeholder; make the contract explicit and fail closed when the reason posture is missing

Mandatory edge cases:
- `current_empty_state_or_null = LIMITED` MUST require non-empty typed `limitation_reason_codes[]`
- `current_empty_surface_code_or_null` must be one of the canonical surface codes and must never be an empty string
- `current_recovery_posture = ACCESS_REBIND_REQUIRED` or `READ_ONLY_LIMITED` MUST force `safe_action_state = NO_SAFE_ACTION`
- low-noise shells MUST map dominant question to `DECISION_SUMMARY` and dominant action to `ACTION_STRIP`
- portal `HOME` MUST map dominant question and dominant action to `STATUS_HERO`, while `TASK_QUEUE` remains mirror/support only
- portal `HELP` MUST make `SUPPORT_PANEL` the primary route surface instead of a competing sidecar
- responsive collapse, reconnect, and rebase MUST preserve the same dominant summary/action hierarchy when the same object remains lawful

Playwright and contract-test requirements:
- unit-test the projectors directly with calm-shell, collaboration, and portal-style fixture sets
- if you patch shipped route payloads, add Playwright APIRequestContext assertions against those routes to confirm the new contracts are serialized and stable
- if you add the salience lab, test it with Playwright using semantic locators only and verify reduced-motion behavior
- capture before/after dominance/taxonomy payloads on failures so regressions are diagnosable

Backend, performance, and security requirements:
- compute salience server-side from authoritative ranked inputs, not from client-local sort order or translated strings
- do not leak staff-only issue detail into portal dominance or taxonomy projection
- keep contract publication stable across refresh/reconnect when the same grouped stability basis still holds
- fail closed on ambiguous or mixed-generation inputs rather than publishing a heuristically guessed dominant question

Validation rules:
- validate every emitted taxonomy contract and dominance contract against schema
- add regression tests for summary/action alignment, portal hero/queue mirroring, `NO_SAFE_ACTION` publication, compare/audit explicitness, and limited-state reason preservation
- update or add validator-facing fixture coverage if the existing self-tests do not yet cover these salience contracts end to end

Definition of done:
- the repo has one reusable projector for `ShellStateTaxonomyContract` and one for `ShellDominanceContract`
- low-noise shells stop encoding dominance and limitation semantics in renderer-local logic
- portal and future governance builders can consume the same projector layer without restating shell-law by hand
- stale, limited, and no-safe-action posture is deterministic, typed, and schema-valid
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
