---
id: pc_0111
slug: phase_03_parallel_wave_01_track_backend_collection_003_implement_connector_binding_resolution_and_fetch_dispatch_pipeline
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "003"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0111

## Task Summary

implement connector binding resolution and fetch dispatch pipeline.

## Metadata

- ID: `pc_0111`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `003`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_003_implement_connector_binding_resolution_and_fetch_dispatch_pipeline`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0110](./pc_0110.md) `phase_03_parallel_wave_01_track_backend_collection_002_implement_source_collection_run_orchestrator_and_fetch_audit_lineage`
- Next: [pc_0112](./pc_0112.md) `phase_03_parallel_wave_01_track_backend_collection_004_implement_materialize_source_records_and_evidence_item_persistence_pipeline`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0110_to_0117.md](../shared_operating_contract_0110_to_0117.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_003_implement_connector_binding_resolution_and_fetch_dispatch_pipeline`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0110` if they exist, especially `pc_0022`, `pc_0023`, `pc_0028`, `pc_0031`, `pc_0033`, `pc_0049`, `pc_0051`, `pc_0052`, `pc_0064`, `pc_0067`, `pc_0069`, `pc_0070`, `pc_0074`, `pc_0076`, `pc_0086` through `pc_0093`, `pc_0109`, and `pc_0110`.

Mission:
Implement the provider-binding resolver and controlled fetch-dispatch pipeline that turns one `planned_source` into a sealed fetch request, transmits it through the governed gateway, and returns a normalized dispatch result carrying raw payload refs, audit refs, cursor checkpoints, revision markers, and typed gap/failure posture.
This task must never allow direct provider calls from application code.
It must resolve the correct `ConnectorBinding`, respect source-plan scope and read model, and surface pagination, auth, scope, timeout, schema-drift, and partial-page failures as first-class machine-readable outputs.

Source algorithm grounding you must honor:
- `core_engine.md`, especially `source_plan.planned_sources[]`, fetch task key derivation, `BUILD_STAGE_TASK(...)`, `MERGE_FETCH_RESULTS(...)`, and the rule that collection after `read_cutoff_at` cannot extend the active intake set.
- `modules.md`, especially `COLLECT_SOURCES(...)`, the controlled-gateway rule, and planned-source inputs (`provider/system binding`, `query/filter basis`, `pagination/cursor strategy`, `read_model`, `late_data_policy`).
- `data_model.md`, especially `SourcePlan`, `CollectionBoundary.source_boundaries[]`, and `ConnectorBinding`.
- `connector_delegation_contract.md` and `actor_and_authority_model.md` for binding health, delegation posture, client binding, and provider/environment lineage.
- `deployment_and_resilience_contract.md` and `observability_and_audit_contract.md` for retry, idempotency, timeout, and append-only audit expectations.
- Existing schemas including `connector_binding.schema.json`, `source_plan.schema.json`, `collection_boundary.schema.json`, and `problem_envelope.schema.json` where reusable.

Deliverables to create:
1. `packages/backend-collection/src/services/resolve_connector_binding.ts`.
2. `packages/backend-collection/src/services/build_collection_fetch_request.ts`.
3. `packages/backend-collection/src/services/dispatch_collection_fetch.ts`.
4. `packages/backend-collection/src/services/normalize_fetch_response.ts`.
5. `packages/backend-collection/src/services/extract_cursor_checkpoint.ts`.
6. `packages/backend-collection/src/services/extract_revision_marker.ts`.
7. `packages/backend-collection/src/services/build_fetch_audit_records.ts`.
8. `packages/backend-collection/src/services/classify_fetch_gap.ts`.
9. `packages/backend-collection/src/adapters/controlled_gateway_client.ts`.
10. `packages/backend-collection/src/types/fetch_dispatch_result.ts`.
11. `packages/backend-collection/src/types/fetch_request_envelope.ts`.
12. `docs/collection/connector_binding_resolution_and_fetch_dispatch_pipeline.md`.
13. `tests/unit/backend_collection/connector_binding_resolution.spec.ts`.
14. `tests/integration/backend_collection/fetch_dispatch_pipeline.spec.ts`.

Required design decisions you must make explicit:
- Define the exact binding-resolution key and precedence order across `provider`, `provider_environment`, `client_id`, `subject_ref`, `partition_scope_refs[]`, provider API version, and any required delegated/client posture.
- Define how `SourcePlan.read_model` (`AS_OF`, `WINDOWED`, `POINT_IN_TIME`, `LATEST_ALLOWED`) maps to the gateway request envelope without leaking provider-specific logic into orchestrators.
- Define how cursor and pagination semantics become deterministic `cursor_checkpoint_ref` and page-level audit refs.
- Define the typed fetch-gap taxonomy for rate limits, auth/token problems, client-binding mismatch, schema drift, pagination truncation, timeout, parse failure, revision drift, and “empty but confirmed” success.
- Define how a successful empty response differs from missing-source posture so later cards can distinguish `NO_DATA_CONFIRMED_AT_CUTOFF` from `MISSING_AT_CUTOFF`.

Execution steps:
1. Implement `resolve_connector_binding.ts` so planned-source execution selects exactly one valid binding or fails closed with typed reason codes.
2. Implement `build_collection_fetch_request.ts` so request envelopes are deterministic, provider-neutral at the service boundary, and carry the exact read model, partition scope, query basis, and cursor strategy frozen by `SourcePlan`.
3. Implement `dispatch_collection_fetch.ts` using the controlled gateway client abstraction only; no inline provider SDK calls or ad hoc HTTP clients in application services.
4. Implement `normalize_fetch_response.ts` so raw payload refs, request audit refs, page audit refs, cursor checkpoints, revision markers, and empty/partial/failure posture come back in one normalized result family.
5. Implement `extract_cursor_checkpoint.ts` / `extract_revision_marker.ts` so later `CollectionBoundary` freezing can persist them without reparsing provider-local blobs.
6. Implement `classify_fetch_gap.ts` and `build_fetch_audit_records.ts` so transport problems become typed machine state and audit refs remain deterministic.
7. Add tests for binding selection, environment mismatch, expired/revoked bindings, empty success, paginated success, partial-page failure, timeout, schema drift, and deterministic request-envelope construction.

Gap handling you must perform:
- Close the gap where the corpus requires `request_audit_refs[]` and page-request lineage but does not provide one canonical fetch-exchange artifact family.
  Introduce an internal schema-backed or strongly typed fetch-audit record surface rather than relying on logs only.
- Close the gap where `SourcePlan.read_model` and cursor strategy are named in prose but not translated into an executable transport contract.
- Close the gap where empty successful fetches could be mistaken for missing-source posture.
- Close the gap where provider-specific auth or partition failures could leak out as generic transport exceptions instead of typed collection-gap semantics.

Mandatory edge cases:
- a missing, revoked, expired, superseded, or client-mismatched `ConnectorBinding` must fail closed before transport.
- a binding whose provider environment or API version diverges from the planned source must fail closed.
- an empty but successfully audited response must return a successful normalized result, not a missing-source failure.
- page 1 success plus page 2 timeout or parse failure must produce explicit partial-gap posture, not silent truncation.
- request/page audit refs must be unique and deterministically ordered.
- cursor checkpoint extraction must remain stable across replay of the same provider response.
- schema-version advancement discovered during fetch must surface as typed gap posture and preserve the observed provider schema version for later boundary freeze.
- no transport path may continue collection after the active `read_cutoff_at` as if the result were still part of active intake.

Backend, performance, and security requirements:
- Resolver and dispatcher code must be side-effect free apart from the governed gateway call and persisted audit artifacts.
- Use bounded concurrency and provider-aware backoff policy, but keep backoff logic outside business semantics so replay stays deterministic.
- Never log raw tokens, authorization headers, or full provider payload bodies.
- Reuse secret/vault/KMS choices from earlier cards; transport code must receive binding refs and gateway-issued credentials, not ambient secrets.
- Keep the normalized result shape provider-neutral so later materialization code consumes one stable contract.

Validation rules:
- Unit-test binding resolution precedence and fail-closed posture.
- Unit-test deterministic request-envelope hashing/order.
- Integration-test controlled-gateway dispatch, paginated success, empty success, partial-page gap, timeout, and schema-drift posture.
- Validate any new fetch-audit artifacts or normalized envelopes against their declared schemas if you introduce them.
- Prove that no direct provider call remains reachable from orchestrator/business services.

Definition of done:
- The repo has one governed connector-binding resolver and collection fetch dispatcher.
- Planned-source execution now returns deterministic, typed dispatch results with stable audit lineage and checkpoint extraction.
- Later record/evidence, boundary-freeze, and late-data tasks can consume provider-neutral fetch outputs instead of re-parsing gateway transport details.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
