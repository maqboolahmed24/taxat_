---
id: pc_0094
slug: phase_03_parallel_wave_01_track_backend_access_010_implement_session_revocation_device_binding_and_csrf_protection_services
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "010"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0094

## Task Summary

implement session revocation device binding and csrf protection services.

## Metadata

- ID: `pc_0094`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `010`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_010_implement_session_revocation_device_binding_and_csrf_protection_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0093](./pc_0093.md) `phase_03_parallel_wave_01_track_backend_access_009_implement_access_binding_hash_and_dependency_topology_hash_calculators`
- Next: [pc_0095](./pc_0095.md) `phase_03_parallel_wave_01_track_backend_access_011_implement_role_template_matrix_projector_and_policy_snapshot_hashing`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0094_to_0101.md](../shared_operating_contract_0094_to_0101.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_010_implement_session_revocation_device_binding_and_csrf_protection_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0093` if they exist, especially `pc_0021`, `pc_0040`, `pc_0050`, `pc_0053`, `pc_0068`, `pc_0073`, `pc_0075`, `pc_0076`, `pc_0085`, `pc_0089`, and `pc_0092`.

Mission:
Implement the secure session-boundary services that make browser and native session safety durable rather than framework-local.
This task must turn `ActorSession` from a stored record into an enforceable runtime boundary covering revocation, device binding, anti-CSRF posture, and post-step-up challenge rotation.
The result must honor the corpus rule that session revocation invalidates future command acceptance plus resume, stream, and upload-control capabilities tied to that session lineage.

Source algorithm grounding you must honor:
- `data_model.md`, especially the canonical `ActorSession` field set and the rule that anti-CSRF posture, device-binding state, revocation, and step-up completion remain explicit persisted facts.
- `northbound_api_and_session_contract.md`, especially `8. Session, browser, and native-client rules`, the explicit invalidation of resume and upload-session controls on revocation, and the rule that step-up completion rotates effective session challenge state.
- `security_and_runtime_hardening_contract.md`, especially identity/session trust, anti-CSRF requirements for cookie-based sessions, short-lived session records, revocation audit, and native device-local hygiene.
- `actor_and_authority_model.md`, especially `3.6 Principal context schema`, `3.9 Policy decision model`, and `3.11 Non-delegable and step-up actions` where assurance and step-up posture must remain frozen and replay-safe.
- `cache_isolation_and_secure_reuse_contract.md` and `stream_resume_and_catch_up_ordering_contract.md` where session invalidation affects safe reuse and resume.
- Existing schemas including `actor_session.schema.json`, `problem_envelope.schema.json`, `api_command_receipt.schema.json`, and any cache-isolation or resume-binding schemas already adopted.

Deliverables to create:
1. `packages/backend-access/src/session/session_revocation_service.ts`.
2. `packages/backend-access/src/session/device_binding_service.ts`.
3. `packages/backend-access/src/session/csrf_token_service.ts`.
4. `packages/backend-access/src/session/browser_session_guard.ts`.
5. `packages/backend-access/src/session/revocation_propagation_service.ts`.
6. `packages/backend-access/src/session/session_security_reason_codes.ts`.
7. `config/access/browser_session_security_policy.json`.
8. `config/access/device_binding_policy.json`.
9. `docs/access/session_revocation_device_binding_and_csrf.md`.
10. `tests/unit/backend_access/session_security_services.spec.ts`.
11. `tests/integration/backend_access/session_revocation_and_csrf.spec.ts`.

Required design decisions you must make explicit:
- Choose the anti-CSRF mechanism appropriate for the corpus' cookie-backed, stateful browser sessions.
  Default to a synchronizer-token style design unless an earlier card already fixed a stronger equivalent.
  Explain how `csrf_ref` maps to stored state, issued client material, rotation, expiry, and auditability.
- Define the boundary between strong device binding and heuristic anomaly detection.
  Strong native binding may use platform-backed key material or an equivalent server-verified binding ref; volatile headers, IP shifts, and UA strings are not sufficient as sole authority.
- Define the canonical inputs for `session_binding_hash` and what changes are allowed to rotate the binding versus revoke the session.
- Define the exact propagation model for session revocation to commands, workspace/experience resume tokens, upload-session control paths, and any cached continuation artifacts.
- Define how browser sessions, native sessions, and automation/service sessions differ in anti-CSRF, binding, and revocation expectations without weakening the corpus' explicit principal-class separation.
- Define monotonic timestamp rules for `issued_at`, `step_up_completed_at`, `last_seen_at`, `revoked_at`, and `expires_at` under concurrent updates.

Execution steps:
1. Inventory every runtime path that consumes `ActorSession`, including command acceptance, resume/rebase flows, stream issuance, upload-control operations, and post-step-up command replay protection.
2. Implement `csrf_token_service.ts` so state-changing browser requests bind a per-session CSRF reference and validation flow that is independent from the raw session cookie value.
3. Implement `device_binding_service.ts` so native sessions can verify a strong binding, browser sessions can preserve a bounded session-binding hash, and suspicious signal drift becomes a typed decision instead of an ambient boolean.
4. Implement `session_revocation_service.ts` and `revocation_propagation_service.ts` so revocation updates the durable session record, emits append-only audit lineage, and invalidates related session-bound control artifacts.
5. Integrate with the step-up rotation service from `pc_0092` so successful step-up rotates challenge state and retires pre-step-up CSRF or replay material safely.
6. Publish machine-readable policies for cookie scope, CSRF token rotation, binding validation, suspicion thresholds, and revocation classes.
7. Document exactly how browser, native, and automation callers are expected to present session-bound proofs and how failures map to typed problem codes.

Gap handling you must perform:
- Close the gap where `ActorSession` is durable but not yet authoritative for revocation or anti-CSRF enforcement.
- Close the gap where browser protection could accidentally rely on `SameSite` alone instead of explicit anti-CSRF binding.
- Close the gap where native device invalidation is recorded but does not automatically surface as a revoked or unusable session.
- Close the gap where session revocation might not fan out to resume tokens, upload-session controls, or other continuation artifacts.
- Close the gap where raw session or CSRF material could be stored, logged, or compared directly rather than by stable references and digests.

Mandatory edge cases:
- revoked browser session presents an otherwise valid CSRF token and must still fail closed
- browser state-changing request is missing the CSRF proof even though the session cookie is present
- step-up completes and the old pre-step-up challenge / CSRF material is replayed afterward
- native session has `device_binding_state = INVALIDATED` and must be treated as revoked or unusable immediately
- same principal holds multiple concurrent sessions and revoking one session does not accidentally revoke the others
- last-seen heartbeat arrives after revocation and must not resurrect or time-travel the session record
- a large environment change is detected from heuristic signals; the service must surface typed challenge or revocation posture, not silently widen trust
- repository or middleware code attempts to persist or log raw refresh secrets, raw session cookies, or raw CSRF secrets

Backend, performance, and security requirements:
- Every command and state-changing browser request must validate current session truth server-side.
- Persist references, digests, state, and timestamps; never persist raw bearer material as ordinary domain state.
- Cookie posture must be secure, `HttpOnly`, same-site, and narrowly scoped by origin/path policy.
- Revocation checks must be cheap enough for hot command paths and idempotent under repeated requests.
- Device-binding verification must be explicit and explainable, not a hidden blacklist of heuristics.
- No new product UI is required.
  If you add an internal session-security viewer, keep it read-only, render binding and revocation posture clearly, and verify it with Playwright using semantic locators.

Validation rules:
- Unit-test CSRF issue/validate/rotate flows, device-binding evaluation, revocation no-op semantics, and monotonic timestamp behavior.
- Integration-test browser and native session flows, step-up rotation, resume/upload-control invalidation on revocation, and typed problem responses.
- Prove that state-changing browser requests fail closed without explicit anti-CSRF proof even when the cookie is present.
- Prove that invalidated native bindings surface as unusable sessions and cannot continue command execution.
- Prove that the implementation never relies on ambient cookies or heuristic device signals alone for final authorization.

Definition of done:
- The repo has durable, auditable services for session revocation, device binding, and anti-CSRF protection.
- Session-bound artifacts and continuations become invalid when the governing session truth is revoked or rotated.
- Later command, upload, and stream work can trust one authoritative session boundary instead of scattered middleware assumptions.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
