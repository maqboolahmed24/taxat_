---
id: pc_0187
slug: phase_04_parallel_wave_02_track_backend_portal_009_implement_portal_language_contract_projection_and_banned_internal_vocabulary_filters
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "009"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0187

## Task Summary

implement portal language contract projection and banned internal vocabulary filters.

## Metadata

- ID: `pc_0187`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `009`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_009_implement_portal_language_contract_projection_and_banned_internal_vocabulary_filters`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0186](./pc_0186.md) `phase_04_parallel_wave_02_track_backend_portal_008_implement_portal_help_request_projection_and_boundary_handoff_tracking`
- Next: [pc_0188](./pc_0188.md) `phase_04_parallel_wave_02_track_backend_portal_010_implement_artifact_affordance_contract_for_preview_export_print_and_browser_handoff`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0182_to_0189.md](../shared_operating_contract_0182_to_0189.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_009_implement_portal_language_contract_projection_and_banned_internal_vocabulary_filters`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0186` if they exist, especially `pc_0179`, `pc_0183`, `pc_0184`, `pc_0185`, and `pc_0186`.

Mission:
Implement the runtime `PortalLanguageContract` publisher together with the copy-budget, due-label, dominance, and banned-vocabulary filters that make portal wording deterministic and client-safe.
This task is the contract layer that stops portal copy from drifting by route, renderer, or developer habit.
It must ensure every portal read model in scope publishes the exact same shared language contract and that first-view copy cannot leak manifest, workflow, stale/rebase, operator, queue, staff-role, raw-gate, override, escalation, or internal-only vocabulary into customer-facing surfaces.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially `Content and copy rules`, `Read-model and API translation requirements`, the due-label grammar, first-view budgets, one-dominant-question/one-primary-action rule, and duplicate-support-copy suppression.
- `frontend_shell_and_interaction_law.md`, especially dominant-action law, stale / recovery presentation, and disclosure fencing.
- `dominant_question_and_single_action_contract.md`, especially the client-portal mapping.
- `data_model.md`, especially the requirement that `ClientPortalWorkspace`, `ClientDocumentRequest`, `ClientApprovalPack`, `ClientOnboardingJourney`, and `ClientTimelineEvent` carry the shared `language_contract`.
- `schemas/portal_language_contract.schema.json`.
- `scripts/validate_contracts.py`, especially the canonical `PORTAL_LANGUAGE_CONTRACT`, copy budgets, first-view budgets, and forbidden-fragment detectors used by the validator.
- `PATCH_RESOLUTION_INDEX.md`, especially the portal wording, recovery wording, and customer-safe leak hardening items.
- `UIUX_DESIGN_SKILL.md`, especially the customer/client portal experience rules.

Deliverables to create:
1. `packages/backend-portal/src/contracts/portal_language_contract.ts`
2. `packages/backend-portal/src/services/validate_portal_copy.ts`
3. `packages/backend-portal/src/services/filter_portal_vocabulary.ts`
4. `packages/backend-portal/src/services/measure_portal_first_view_budget.ts`
5. `packages/backend-portal/src/services/derive_clear_due_label.ts`
6. `packages/backend-portal/src/tests/portal_language_contract_and_copy_guard.spec.ts`
7. `docs/portal/portal_language_contract_and_banned_vocabulary_filters.md`
8. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/language-atlas.tsx`

You MUST patch any existing portal projectors in scope so they thread the same shared contract object rather than duplicating route-local wording policies.

Required design decisions you must make explicitly:
- define the exact runtime source of the shared `PortalLanguageContract` object and how you guarantee it stays byte-identical to the validator’s frozen contract
- define how direct text versus governed text refs are allowed and how copy still passes vocabulary / budget checks in both modes
- define when copy is rejected versus rewritten when forbidden terms or over-budget copy are detected
- define how duplicate support or limitation narratives are detected across `STATUS_HERO`, limitation notices, and promoted support regions
- define how due-label grammar is normalized to explicit `Due ...`, `Overdue ...`, or `No deadline yet`

Execution requirements:
- publish the exact shared `PortalLanguageContract` object required by schema and validator
- thread that same contract through `ClientPortalWorkspace`, request-list or request-detail projections in scope, `ClientDocumentRequest`, `ClientApprovalPack`, `ClientOnboardingJourney`, and `ClientTimelineEvent`
- enforce route-level first-view character budgets for `HOME`, `DOCUMENTS`, `APPROVALS`, `ONBOARDING`, `HELP`, and contextual request detail
- enforce field-level budgets for dominant questions, hero text, actions, task labels, request titles, approval summaries, onboarding labels, help text, and timeline headlines
- enforce due-label grammar and explicit current/history/settlement phrasing
- forbid portal-visible terms or fragments associated with manifest language, workflow language, stale/rebase jargon, operator/queue language, assignment state, staff roles, raw gate language, override language, escalation language, audit lineage language, and internal-only language
- keep the one-dominant-question and one-primary-action rule explicit in the language layer, not just implied by layout
- if you add the atlas, make it a quiet typographic spec page with route budgets, allowed / blocked examples, and banned-family chips; no animated marketing page

Gap handling you must close while implementing:
- close any gap where portal routes can publish variant contract objects or omit the shared language contract entirely
- close any gap where error, stale, or degraded posture copy can leak rebase/stale jargon into the portal
- close any gap where role filtering happens after copy generation, allowing hidden affordances or blocked terms to influence customer text
- close any gap where repeated limitation/support narration can appear twice in the first view
- close any gap where due labels or task labels drift into vague or non-literal wording

Mandatory edge cases:
- customer-visible text must not contain fragments like `manifest`, `workflow`, `rebase`, `stale`, `operator`, `queue`, `gate`, `override`, `escalat`, `assignee`, `assigned`, `reviewer`, `staff`, `sla`, `internal-only`, or `internal only`
- `STATUS_HERO.secondary_action` must remain null wherever the governing contract requires one dominant first-view action
- any over-budget first-view route copy must fail the projection pipeline or test suite
- due labels must normalize to `Due ...`, `Overdue ...`, or `No deadline yet`
- current-versus-history wording must remain explicit and calm; no implicit historical selection through copy alone
- support copy must remain subordinate unless the route itself is `HELP`

Playwright and contract-test requirements:
- add direct tests for exact contract equality, forbidden-fragment detection, due-label normalization, and first-view budget measurement
- if portal APIs already ship the shared contract, add Playwright APIRequestContext tests that assert the exact object and verify route-level copy remains within budget
- if you add the language atlas, add Playwright browser tests using user-facing locators that cover budget indicators, blocked-term examples, and route contract display without fixed waits
- capture traces or serialized copy payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- max width `1080px`, background `#F6F4EE`, surface `#FFFFFF`, muted strips `#F3F4F6`
- show one route at a time with typographic specimens rather than a dashboard of all routes
- use slim chips for forbidden term families and monospace inline tokens for policy codes only
- the atlas should include one small budget bar per route and one allowed-vs-blocked phrase pair where helpful
- use restrained iconography only where it clarifies route family or warning posture
- animations limited to subtle reveal/fade, reduced motion mandatory

Backend, performance, and security requirements:
- keep vocabulary filtering and budget validation server-side and deterministic
- do not let frontend copy composition or localization placeholders bypass the contract checks in production builds
- keep the shared contract importable by all portal projectors so drift is impossible without an explicit contract change
- fail closed on contract mismatch or forbidden-vocabulary leakage in customer-visible paths

Validation rules:
- validate the emitted contract against `schemas/portal_language_contract.schema.json`
- add regression coverage for forbidden fragments, due-label drift, duplicate support copy, and per-route first-view budgets
- keep copy validation reusable across portal workspace, detail, approvals, onboarding, timeline, and help projectors

Definition of done:
- the repo has one authoritative runtime `PortalLanguageContract` and one reusable customer-safe copy guard layer
- portal wording, budgets, due labels, and banned-vocabulary rules are now governed backend truth instead of style-guide folklore
- customer-visible portal text stays calm, explicit, and role-safe across every route in scope
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
