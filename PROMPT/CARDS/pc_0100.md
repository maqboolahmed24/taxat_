---
id: pc_0100
slug: phase_03_parallel_wave_01_track_backend_manifest_004_implement_schema_bundle_schema_reader_window_and_backfill_execution_models
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "004"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0100

## Task Summary

implement schema bundle schema reader window and backfill execution models.

## Metadata

- ID: `pc_0100`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `004`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_004_implement_schema_bundle_schema_reader_window_and_backfill_execution_models`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0099](./pc_0099.md) `phase_03_parallel_wave_01_track_backend_manifest_003_implement_config_version_config_change_request_and_config_freeze_models`
- Next: [pc_0101](./pc_0101.md) `phase_03_parallel_wave_01_track_backend_manifest_005_implement_resolve_config_freeze_config_and_materialize_cfg_from_freeze_modules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0094_to_0101.md](../shared_operating_contract_0094_to_0101.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_004_implement_schema_bundle_schema_reader_window_and_backfill_execution_models`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0099` if they exist, especially `pc_0027`, `pc_0060`, `pc_0061`, `pc_0066`, `pc_0077`, `pc_0080`, `pc_0081`, `pc_0097`, and `pc_0099`.

Mission:
Implement the durable models and repositories for `SchemaBundle`, `schema_reader_window_contract`, `SchemaMigrationLedger`, and `BackfillExecutionContract`.
This task makes schema-window compatibility and backfill chronology manifest-consumable truth.
It must let later cards freeze schema identity into manifests and config freezes without hand-waving over migration phase, replay protection, or historical-reader windows.

Source algorithm grounding you must honor:
- `data_model.md`, especially `SchemaBundle`, `SchemaBundleEntry`, `SchemaMigrationLedger`, and `BackfillExecutionContract`.
- `state_machines.md`, especially `SchemaMigrationLedger.phase_state` and its required chronology.
- `deployment_and_resilience_contract.md`, especially schema/datastore migration rules, rollback versus fail-forward posture, and release invariants.
- `verification_and_release_gates.md`, especially migration verification and compatibility evidence expectations.
- `replay_and_reproducibility_contract.md`, especially schema-reader incompatibility handling and historical basis protection.
- `manifest_and_config_freeze_contract.md`, especially `schema_bundle_hash` and `schema_reader_window_contract` as part of the frozen execution basis.
- Existing schemas including `schema_bundle.schema.json`, `schema_reader_window_contract.schema.json`, `schema_migration_ledger.schema.json`, and `backfill_execution_contract.schema.json`.
- The readiness automation from `pc_0080`; do not duplicate its whole diff engine, but do persist the canonical model shapes it needs.

Deliverables to create:
1. `packages/backend-manifest/src/models/schema_bundle.ts`.
2. `packages/backend-manifest/src/models/schema_bundle_entry.ts`.
3. `packages/backend-manifest/src/models/schema_reader_window_contract.ts`.
4. `packages/backend-manifest/src/models/schema_migration_ledger.ts`.
5. `packages/backend-manifest/src/models/backfill_execution_contract.ts`.
6. `packages/backend-manifest/src/repositories/schema_bundle_repository.ts`.
7. `packages/backend-manifest/src/repositories/schema_migration_ledger_repository.ts`.
8. `packages/backend-manifest/src/services/schema_bundle_loader.ts`.
9. `packages/backend-manifest/src/services/schema_reader_window_guard.ts`.
10. `db/migrations/phase03_0100_schema_bundle_and_migration_ledger.sql`.
11. `docs/manifest/schema_bundle_reader_window_and_backfill_models.md`.
12. `tests/unit/backend_manifest/schema_bundle_models.spec.ts`.
13. `tests/integration/backend_manifest/schema_reader_window_and_ledger.spec.ts`.

Required design decisions you must make explicit:
- Define how schema bundles are persisted: by hash only, by embedded bundle metadata, by normalized entry table, or by a hybrid that preserves both integrity and efficient lookup.
- Define how the active reader window is resolved for a given target schema bundle and how that result is consumed by manifest/config-freeze code without live recomputation.
- Define how `SchemaMigrationLedger` points to `BackfillExecutionContract`, verification evidence, rollback class, and compatibility windows.
- Define how historical manifests and replay restore flows are protected when a schema change is still in `EXPAND_ONLY` or `BACKFILL_IN_PROGRESS` posture.
- Define how native-client compatibility and fail-forward-only posture are persisted even if the immediate backend use is manifest-focused.

Execution steps:
1. Implement typed model wrappers for schema bundles, bundle entries, reader-window contracts, migration ledgers, and backfill execution contracts.
2. Create the migration for durable storage, indexes, and phase-state constraints.
3. Implement repositories and loaders that can retrieve the current bundle by hash, enumerate entries, and retrieve the ledger/backfill contract associated with a target bundle.
4. Implement `schema_reader_window_guard.ts` that turns persisted reader-window state into explicit guard results for manifest callers.
5. Ensure the models carry enough source and compatibility lineage to support the `pc_0080` readiness automation without duplicating its full decision engine.
6. Document how manifest allocation and config freezing should consume `schema_bundle_hash` and `schema_reader_window_contract` from these persisted objects.
7. Add tests for phase chronology, reader-window resolution, replay-protection states, and fail-forward/rollback posture.

Gap handling you must perform:
- Close the gap where manifests need schema identity and reader-window truth but no durable runtime model exists.
- Close the gap where migration chronology and backfill posture could be stored only in release notes or CI output instead of in typed control objects.
- Close the gap where historical-manifest and replay guards could drift from the actual schema window state.
- Close the gap where backfill execution policy is not explicitly linked to the migration ledger that requires it.
- Close the gap where native-client or rollback posture is silently ignored even though the compatibility contract models it.

Mandatory edge cases:
- reader window is `EXPAND_ONLY_PREVIOUS_READERS_SUPPORTED` and destructive contraction is still blocked
- reader window is `BACKFILL_IN_PROGRESS_PREVIOUS_READERS_SUPPORTED` and historical manifests remain protected
- window is `CONTRACT_ELIGIBLE_WINDOW_CLOSED` and fail-forward/rollback posture is explicit, not inferred
- migration ledger reaches a verified phase without a verification ref and must fail validation
- backfill execution contract is required but absent
- native client window is blocked even though backend reader window looks otherwise healthy
- target bundle hash exists but schema-entry set is incomplete or mismatched by hash
- replay restore is blocked because schema-reader compatibility has not been proven

Backend, performance, and security requirements:
- Loading schema-window truth must be deterministic, cache-safe, and free from live internet dependency.
- Model/repository code must preserve hash-based identity and never rewrite historical bundle content in place.
- Guard results must be explicit enough that callers can fail closed without reinterpreting phase-state strings.
- No new UI is required in this task.
  Reuse the schema-compatibility atlas from `pc_0080` rather than inventing another dashboard.
- Keep the implementation compatible with later release-candidate hashing work without pre-implementing that later card here.

Validation rules:
- Unit-test model serialization, entry ordering, reader-window guard classification, and ledger/backfill linkage.
- Integration-test persistence, phase-state chronology, replay/historical guard behavior, and hash-based lookup.
- Validate persisted artifacts against the imported schema bundle.
- Prove that destructive schema changes remain blocked until the reader window and ledger phase truly allow contraction.
- Prove that manifests and config freezes can consume persisted schema-window truth without live recomputation.

Definition of done:
- The repo has durable schema-bundle, reader-window, migration-ledger, and backfill-contract models.
- Manifest work can now freeze schema identity and compatibility posture from explicit persisted truth.
- Later release and replay work gains the compatibility substrate it needs without leaning on CI-only artifacts.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
