---
id: pc_0157
slug: phase_03_parallel_wave_01_track_backend_northbound_001_implement_post_commands_endpoint_and_durable_api_command_receipt_store
mode: parallel
phase: '03'
wave: '01'
track: backend_northbound
task_number: '001'
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_001_implement_post_commands_endpoint_and_durable_api_command_receipt_store
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0157

## Task Summary

post commands endpoint and durable api command receipt store.

## Metadata

- ID: `pc_0157`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `001`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_001_implement_post_commands_endpoint_and_durable_api_command_receipt_store`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0156](./pc_0156.md) `phase_03_parallel_wave_01_track_backend_workflow_012_implement_quantitative_queue_health_and_escalation_analytics_services`
- Next: [pc_0158](./pc_0158.md) `phase_03_parallel_wave_01_track_backend_northbound_002_implement_get_command_receipt_endpoint_with_recovery_anchor_semantics`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0150_to_0157.md](../shared_operating_contract_0150_to_0157.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_001_implement_post_commands_endpoint_and_durable_api_command_receipt_store`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0156` if they exist, especially `pc_0064` through `pc_0069`, `pc_0094` through `pc_0101`, `pc_0145` through `pc_0156`.

Mission:
Implement the first northbound write edge and the durable command receipt store that all safe retries, stale-view rejections, and recovery paths depend on.
This card must make `POST /v1/commands` the only product-facing write boundary for manifest-adjacent truth, work-item mutations, and governance mutations handled in this phase, with durable `ApiCommandReceipt` evidence persisted before downstream side effects are published.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially sections 1 through 5.
- `data_model.md`, especially `ApiCommandReceipt` and related truth-boundary semantics.
- `frontend_shell_and_interaction_law.md` and `customer_client_portal_experience_contract.md` where durable command receipts drive pending / recovery copy.
- `admin_governance_console_architecture.md` where writes remain receipt-based.
- `schemas/command_envelope.schema.json`, `schemas/api_command_receipt.schema.json`, and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md` and validator drift cases for command receipt recovery anchors, stale-guard families, duplicate lineage, and mutation-basis hash posture.

Deliverables to create:
1. `packages/backend-northbound/src/models/command_envelope.ts`
2. `packages/backend-northbound/src/models/api_command_receipt.ts`
3. `packages/backend-northbound/src/repositories/api_command_receipt_repository.ts`
4. `packages/backend-northbound/src/http/post_commands_endpoint.ts`
5. `packages/backend-northbound/src/http/register_post_commands_route.ts`
6. `packages/backend-northbound/src/services/validate_command_envelope.ts`
7. `packages/backend-northbound/src/services/hash_command_request.ts`
8. `packages/backend-northbound/src/services/find_duplicate_command_receipt.ts`
9. `packages/backend-northbound/src/services/persist_command_receipt_before_dispatch.ts`
10. `packages/backend-northbound/src/services/dispatch_command_to_domain_handler.ts`
11. `packages/backend-northbound/src/services/build_duplicate_replay_receipt.ts`
12. `packages/backend-northbound/src/services/build_problem_envelope_for_command_rejection.ts`
13. `db/migrations/phase03_0157_api_command_receipt_store.sql`
14. `docs/northbound/post_commands_and_api_command_receipt.md`
15. `tests/unit/backend_northbound/command_envelope_validation.spec.ts`
16. `tests/unit/backend_northbound/api_command_receipt_contract.spec.ts`
17. `tests/integration/backend_northbound/post_commands_idempotency_flow.spec.ts`
18. `tests/integration/backend_northbound/post_commands_stale_guard_flow.spec.ts`
19. `tests/integration/backend_northbound/post_commands_problem_envelope_flow.spec.ts`
20. `tests/playwright_api/northbound/post_commands_contract.spec.ts`

Optional diagnostic harness only if strictly needed for debugging receipts:
21. `apps/admin-console-web/src/routes/debug/command-receipt-preview.tsx`

Required design decisions you must make explicit:
- Define the exact acceptance / duplicate / rejection lifecycle for `POST /v1/commands` and which acceptance states are success-class.
- Define the canonical `request_hash` and duplicate-suppression key semantics, including how `command_id`, `idempotency_key`, actor/session, target scope, and payload shape participate.
- Define how `target_scope_class` chooses the lawful scope field family (`manifest_id`, `work_item_id`, `governance_target_ref`) and which stale guards are legal per command family.
- Define how success-class receipts preserve at least one authoritative recovery anchor (`result_ref`, `audit_event_refs[]`, `activity_refs[]`, `notification_refs[]`, or equivalent) and why `latest_projection_ref` alone is insufficient.
- Define how `mutation_precondition_binding` is preserved from the command envelope into the receipt and the problem envelope.
- Define how the endpoint rejects raw upload bytes, forbidden payload fields, or mismatched stale-guard families.

Execution steps:
1. Implement schema-backed `CommandEnvelope` intake and `ApiCommandReceipt` persistence models.
2. Implement endpoint parsing, validation, request hashing, duplicate lookup, and typed command-family dispatch.
3. Persist the durable receipt before downstream side effects or handler side-effect publication occurs.
4. On safe duplicate retry, return the original / duplicate-replay receipt rather than enqueueing new work.
5. Implement typed stale-view, policy, and invalid-shape rejection mapping to `ProblemEnvelope` with the latest authoritative guard data and recovery anchors.
6. Add API-level contract tests using Playwright’s API test capabilities or an equivalent deterministic HTTP contract harness.
7. Only add a browser receipt preview if that materially accelerates debugging; it is not a product surface.

Gap handling you must perform:
- Close the gap where domain writes could bypass a durable command-edge receipt.
- Close the gap where duplicate POSTs create duplicate activity, audit, or notification side effects.
- Close the gap where a success receipt can survive only as a projection ref with no durable recovery anchor.
- Close the gap where stale-guard families drift across manifest, work-item, request-state, portal-workspace, and governance command families.
- Close the gap where governance simulation stale rejections lose the dependency-topology / basis hashes that made the decision reproducible.

Mandatory edge cases:
- success-class receipts MUST retain at least one authoritative recovery anchor; `latest_projection_ref` alone is invalid
- `duplicate_of_receipt_id` MUST NOT self-reference `receipt_id`
- `expires_at` MUST be later than `accepted_at`
- `REJECTED_STALE_VIEW` MUST preserve the true `stale_guard_family`, current `latest_stale_guard_value`, and the correct grouped stability/recovery mirror when applicable
- manifest-only stale guards MUST NOT appear on `target_scope_class = WORK_ITEM` or `GOVERNANCE`
- `REQUEST_STATE_VERSION` guards MUST appear only for the request-info mutation family
- governance simulation stale receipts MUST preserve aligned `dependency_topology_hash`, `simulation_basis_hash`, and `latest_mutation_basis_contract_or_null` when required
- `EXPIRED` success receipts MUST preserve `original_acceptance_state` and the same recovery-anchor family they had before expiry
- `POST /v1/commands` MUST reject raw upload bytes or upload-session blob payloads; binary transfer remains outside this endpoint

Playwright / contract-test requirements:
- use Playwright APIRequestContext or an equivalent deterministic API contract harness during implementation
- no fixed sleeps in async polling tests
- capture traces or full request/response logs on retry / failure
- prove safe retry returns duplicate-replay posture without duplicate side effects
- prove stale-view failures preserve the right guard family and latest authoritative value

Backend, performance, and security requirements:
- fail closed on schema mismatch, stale-guard / target-scope mismatch, duplicate-lineage drift, or missing recovery anchors
- keep receipt writes, duplicate detection, and downstream dispatch idempotent and transactionally safe
- never let client-provided legal-state flags, raw projection fields, or upload bytes cross the command boundary as authority
- preserve the exact `mutation_precondition_binding` and truth-boundary contract on every receipt

Validation rules:
- validate command envelopes, receipts, and problem envelopes against schema
- unit-test request hashing, duplicate suppression, stale-guard legality, target-scope legality, and recovery-anchor rules
- integration-test accepted, duplicate-replay, stale-view, invalid-shape, and expired-receipt flows
- API-test the actual endpoint contract with Playwright API tooling or equivalent

Definition of done:
- the repo contains a durable `POST /v1/commands` write boundary and `ApiCommandReceipt` store
- safe retries now collapse onto one command receipt / semantic action family
- stale-view, policy, and invalid-shape failures are typed, reproducible, and recovery-aware by construction
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
