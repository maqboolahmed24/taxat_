---
id: pc_0011
slug: phase_00_seq_011_extract_compute_parity_risk_and_trust_formula_requirements
mode: sequential
phase: "00"
wave: null
track: null
task_number: "011"
block_key: phase_00_seq
original_key: phase_00_seq_011
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0011

## Task Summary

extract compute parity risk and trust formula requirements.

## Metadata

- ID: `pc_0011`
- Mode: `sequential`
- Phase: `00`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `011`
- Source Slug: `phase_00_seq_011_extract_compute_parity_risk_and_trust_formula_requirements`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0010](./pc_0010.md) `phase_00_seq_010_extract_gate_order_reason_codes_and_override_rules`
- Next: [pc_0012](./pc_0012.md) `phase_00_seq_012_extract_authority_interaction_and_reconciliation_requirements`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt

Use the shared operating contract first:
[../shared_operating_contract_0006_to_0013.md](../shared_operating_contract_0006_to_0013.md)

```text
Execute roadmap task `phase_00_seq_011_extract_compute_parity_risk_and_trust_formula_requirements`.

Inherit the shared operating contract and consume the outputs from cards `pc_0001` through `pc_0010` if they exist.

Mission:
Convert the formula layer of the Taxat corpus into an implementation-grade formula registry.
This task must make the computation, forecast, risk, parity, evidence-graph quality, trust synthesis,
threshold stability, filing readiness, and adjacent quantitative rules reproducible without forcing later engineers
to scrape dense prose. The output must preserve exact-decimal constraints, mode boundaries, sensitivity hooks,
reason-code emissions, and downstream gate effects.

Algorithm grounding you must honor:
- `compute_parity_and_trust_formulas.md` is the primary formula source.
- `8.1 TrustSummary artifact binding` fixes output contract requirements.
- `8.2 Standard normalization rules` governs clamping, rounding, deterministic arithmetic, and money serialization.
- `8.3 Data-quality and completeness formulas` defines upstream scoring.
- `8.4` through `8.10` define compute, forecast, risk, parity, graph quality, and trust synthesis.
- `8.10A` and `8.11A` define adjacent collaboration and client-flow formula families that still need indexing,
  but they must remain tagged as secondary formula families rather than being folded into filing trust.
- `8.11` and `8.12` define stale-input / recalculation handling and formula reason-code emission.
- `modules.md` procedures such as `COMPUTE_OUTCOME(...)`, `FORECAST(...)`, `SCORE_RISK(...)`,
  `EVALUATE_PARITY(...)`, `SYNTHESIZE_TRUST(...)`, `CHECK_TRUST_CURRENCY(...)`,
  and routing / workflow formula helpers show where formulas bind operationally.
- `exact_gate_logic_and_decision_tables.md` determines how parity/trust/filing outputs are consumed by gates.
- `data_model.md` and schemas for `ComputeResult`, `ForecastSet`, `RiskReport`, `ParityResult`,
  `TrustSummary`, `TrustInputBasisContract`, and `TrustSensitivityAnalysisContract`
  define machine-portable output structure.
- `test_vectors.md` and relevant sample schema payloads must be used as validation aids where applicable.

Deliverables to create:
1. `docs/analysis/11_compute_parity_risk_and_trust_formula_requirements.md`
2. `docs/analysis/11_formula_dependency_and_execution_basis.md`
3. `docs/analysis/11_threshold_edge_case_and_sensitivity_matrix.md`
4. `diagrams/analysis/11_formula_dependency_graph.mmd`
5. `data/analysis/formula_registry.json`
6. `data/analysis/formula_dependencies.csv`
7. `data/analysis/thresholds_and_bands.json`
8. `data/analysis/money_and_rounding_contract.json`
9. `data/analysis/formula_reason_code_map.json`
10. `data/analysis/formula_test_vector_plan.json`
11. `data/analysis/secondary_formula_families.json`
12. `tools/analysis/build_formula_registry.py`
13. `tools/analysis/validate_formula_registry.py`

Each formula record must capture at minimum:
- `formula_id`
- `formula_family`
- `canonical_section`
- `inputs[]`
- `intermediate_terms[]`
- `output_fields[]`
- `output_artifacts[]`
- `mode_applicability`
- `money_profile_requirements`
- `determinism_rules[]`
- `threshold_dependencies[]`
- `reason_code_emissions[]`
- `downstream_gate_consumers[]`
- `sensitivity_hooks[]`
- `source_refs[]`
- `notes`

Execution steps:
1. Parse `compute_parity_and_trust_formulas.md` section by section from `8.1` through `8.13`.
2. Build a formula registry covering at minimum:
   - normalization utilities,
   - data-quality / completeness,
   - record-layer and adjustment-layer compute,
   - forecast,
   - risk,
   - parity comparison-set construction,
   - per-field parity,
   - aggregate parity,
   - evidence-graph quality,
   - trust synthesis,
   - filing-readiness related trust outputs,
   - stale-input / recalculation handling,
   - formula reason-code emission.
3. Index adjacent formula families from `8.10A` and `8.11A`, but tag them as `secondary_formula_family`
   so future engineers do not confuse collaboration or client-flow metrics with core filing trust.
4. Build a dependency graph showing which formulas feed which later artifacts and gates.
5. Extract the canonical money and rounding contract into its own machine-readable pack.
6. Build a thresholds/bands registry covering:
   - score bands,
   - cap bands,
   - trust bands,
   - automation levels,
   - filing readiness,
   - threshold stability,
   - margins and guard bands.
7. Build a reason-code map showing which formulas can emit which reason codes and why.
8. Build a validation/test-vector plan referencing `test_vectors.md`, schema examples, and any source-derived edge cases.
9. Cross-link formulas to their schema-backed output fields and to downstream gate consumers.

Gap handling you must perform:
- Close the “no machine-readable formula registry” gap.
- Close the “no formula dependency graph from compute to trust to gates” gap.
- Close the “exact-decimal money / rounding rules are easy to miss in implementation” gap.
- Close the “threshold/band semantics are spread across prose” gap.
- Close the “decision-side confidence vs projection-side confidence is easy to collapse incorrectly” gap.

Mandatory edge cases:
- money-bearing values must use exact decimal accumulation and canonical decimal-string serialization; binary floating point is forbidden.
- `projection_*` confidence terms are read-side only and must never feed compute, parity, trust, or filing readiness.
- compliance compute requires canonical facts; analysis mode may include provisional facts only under explicit frozen analysis policy.
- zero-sum weight profiles, invalid error budgets, and silent limitation ambiguity are structural failures, not soft warnings.
- trust synthesis must preserve `trust_input_state`, `threshold_stability_state`, `upstream_gate_cap`, and filing-readiness ceilings distinctly.
- amendment freshness and retroactive caps remain part of trust/filing readiness semantics and must not be dropped as late-phase decoration.
- sensitivity analysis is first-class, not optional debugging output.
- reason-code emission rules are normative and must remain linked to formulas.

Backend engineering requirements:
- Use exact decimal libraries/types for any executable test or validation harness.
- Keep formulas as declarative data plus reference implementations where practical; avoid embedding business semantics only in imperative code.
- Use versioned threshold/profile references rather than magic constants scattered across scripts.
- Add defensive validation for illegal ranges, nullability violations, and unknown enum values.
- Keep validation code deterministic and suitable for replay/regression testing.

Validation rules:
- Every numbered formula section must be indexed.
- Every schema-backed output artifact must map to at least one formula family.
- The thresholds/bands registry must account for all score and readiness enums defined in the source.
- The money/rounding contract must include serialization, scale, and aggregation-boundary rules.
- The dependency graph must show how formulas feed parity/trust/final gating.
- Run:
  - `python3 Algorithm/scripts/validate_contracts.py --self-test`
  - `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- A future implementation agent can build exact compute/parity/risk/trust services, property tests,
  and replay-safe evaluators from your outputs without re-deriving the formulas from prose.
```
## Verification

- Prompt authored from direct analysis of the Taxat algorithm corpus and aligned to the authoritative card slug.
- Instructions explicitly preserve card status neutrality; claim and completion state must continue to live only in `Checklist.md`.

### Implementation Outputs

- Created `tools/analysis/build_formula_registry.py`.
- Created `tools/analysis/validate_formula_registry.py`.
- Created `docs/analysis/11_compute_parity_risk_and_trust_formula_requirements.md`.
- Created `docs/analysis/11_formula_dependency_and_execution_basis.md`.
- Created `docs/analysis/11_threshold_edge_case_and_sensitivity_matrix.md`.
- Created `diagrams/analysis/11_formula_dependency_graph.mmd`.
- Created `data/analysis/formula_registry.json`.
- Created `data/analysis/formula_dependencies.csv`.
- Created `data/analysis/thresholds_and_bands.json`.
- Created `data/analysis/money_and_rounding_contract.json`.
- Created `data/analysis/formula_reason_code_map.json`.
- Created `data/analysis/formula_test_vector_plan.json`.
- Created `data/analysis/secondary_formula_families.json`.

### Formula Registry Findings

- The registry now covers all numbered source sections from `8.1` through `8.13` with `19` machine-readable formula records: `13` core formula families, `2` explicitly tagged `secondary_formula_family` records for `8.10A` and `8.11A`, and the remaining contract or summary anchors for artifact binding, reason-code emission, and the section-level closeout.
- The extraction pack indexes `649` formula symbols and `8` schema touchpoints while keeping the schema-backed output spine explicit across `ComputeResult`, `ForecastSet`, `RiskReport`, `ParityResult`, `TrustSummary`, `TrustInputBasisContract`, and `TrustSensitivityAnalysisContract`.
- The dependency layer now makes the compute-to-trust chain explicit instead of implicit prose: normalization feeds every downstream family, compute feeds forecast and parity, data quality / risk / aggregate parity / graph quality all feed trust, trust-input admissibility and upstream gate caps freeze hard ceilings before score math, and the reason-code contract is bound back onto persisted trust artifacts.
- The threshold pack centralizes `30` threshold groups, split into `22` core and `8` secondary groups. It freezes the trust score bands, cap-band rules, automation and filing-readiness enums, the full `EDGE_REVIEW` trigger surface, the canonical `7` edge-trigger codes, and the canonical `6` perturbation probes used by the trust sensitivity contract.
- The money contract now makes the exact-decimal boundary implementation-grade rather than narrative: canonical decimal-string serialization, declared aggregation boundaries, exact scale preservation, deterministic summation order, and the prohibition on binary floating point are all exported in one machine-readable contract.
- The reason-code map now exports `73` distinct formula-layer reason codes across `13` emitting families. It preserves the boundary between decision-side trust reasons and the tagged secondary families, and it keeps prefixes such as `DQ`, `PARITY`, `RISK`, `GRAPH`, `TRUST`, `WORK`, `FLOW`, `UPLOAD`, `APPROVAL`, `FRICTION`, and `COMPLETION` queryable instead of buried in prose.
- The validation plan now links `11` existing trust-era vectors (`TV-55` through `TV-58`) to the formula registry and adds `9` explicit planned vectors for the still-uncovered data-quality, compute, forecast, risk, parity, graph, work-routing, and client-flow edge cases.

### Execution Notes

- `projection_*` confidence terms are indexed but remain explicitly constrained to read-side use only; the registry keeps them out of compute, parity, trust, and filing-readiness dependencies.
- `trust_input_state`, `threshold_stability_state`, and `upstream_gate_cap` are modeled as distinct trust surfaces rather than score-derived decorations, which closes the main implementation ambiguity around stale-input ceilings, edge review, and upstream legal progression caps.
- The `TrustSensitivityAnalysisContract` alias is normalized to the live `trust_sensitivity_contract.schema.json` payload contract so downstream builders can bind to the concrete schema while still preserving the source alias name in notes and touchpoints.

### Verification Evidence

- `python3 tools/analysis/build_formula_registry.py` completed successfully with status `PASS`.
- `python3 tools/analysis/validate_formula_registry.py` completed successfully with status `PASS`.
- `./.venv/bin/python3 Algorithm/scripts/validate_contracts.py --self-test` completed successfully with status `PASS`.
- `./.venv/bin/python3 Algorithm/tools/forensic_contract_guard.py` completed successfully with status `PASS`.
