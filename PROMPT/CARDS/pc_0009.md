---
id: pc_0009
slug: phase_00_seq_009_extract_state_machine_definitions_and_transition_invariants
mode: sequential
phase: "00"
wave: null
track: null
task_number: "009"
block_key: phase_00_seq
original_key: phase_00_seq_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0009

## Task Summary

extract state machine definitions and transition invariants.

## Metadata

- ID: `pc_0009`
- Mode: `sequential`
- Phase: `00`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `009`
- Source Slug: `phase_00_seq_009_extract_state_machine_definitions_and_transition_invariants`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0008](./pc_0008.md) `phase_00_seq_008_inventory_entities_artifacts_and_schema_ownership_boundaries`
- Next: [pc_0010](./pc_0010.md) `phase_00_seq_010_extract_gate_order_reason_codes_and_override_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt

Use the shared operating contract first:
[../shared_operating_contract_0006_to_0013.md](../shared_operating_contract_0006_to_0013.md)

```text
Execute roadmap task `phase_00_seq_009_extract_state_machine_definitions_and_transition_invariants`.

Inherit the shared operating contract and consume the outputs from cards `pc_0001` through `pc_0008` if they exist.

Mission:
Extract the full state-machine layer of the Taxat corpus into a queryable registry.
The result must make every formal state machine, transition event, terminal posture, recovery path,
supersession rule, and cross-state invariant explicit enough that future implementation agents can build
runtime guards, persistence transitions, and UI status mapping without ambiguity.

Algorithm grounding you must honor:
- `state_machines.md` is the primary source of state and transition semantics.
- `state_machines.md -> 6.1 Global state-machine rules` and `-> Persisted coverage map`
  govern all later state sections.
- `state_machines.md -> 6.2` through `6.31` define lifecycle and classification machines spanning manifests,
  nightly batches, cursors, configuration, snapshots, facts, compute outputs, parity, trust, graphs, twins,
  workflow, overrides, authority, filing, drift, amendment, retention, low-noise frames, release/control states,
  and client-facing journeys.
- `core_engine.md` defines where many of those transitions occur operationally.
- `invariant_enforcement_and_fail_closed_contract.md`, `recovery_tier_checkpoint_and_fail_forward_governance_contract.md`,
  `replay_and_reproducibility_contract.md`, and `deployment_and_resilience_contract.md`
  tighten recovery, replay, and fault semantics.
- `frontend_shell_and_interaction_law.md` and `low_noise_experience_contract.md`
  matter for cursor and frame state families, not just backend lifecycle objects.

Deliverables to create:
1. `docs/analysis/09_state_machine_definitions_and_transition_invariants.md`
2. `docs/analysis/09_terminal_recovery_and_supersession_matrix.md`
3. `docs/analysis/09_cross_state_invariant_index.md`
4. `diagrams/analysis/09_state_machine_overview.mmd`
5. `data/analysis/state_machine_registry.json`
6. `data/analysis/state_transition_edges.csv`
7. `data/analysis/state_machine_invariants.jsonl`
8. `data/analysis/state_machine_schema_coverage.json`
9. `data/analysis/compound_state_axes.json`
10. `data/analysis/unmodeled_or_ambiguous_state_postures.json`
11. `tools/analysis/build_state_machine_registry.py`
12. `tools/analysis/validate_state_machines.py`

Each state-machine record must capture at minimum:
- `machine_id`
- `object_family`
- `schema_path`
- `state_field`
- `machine_class` (`lifecycle`, `cursor`, `classification_axis`, `compound_axis`, `release_control`)
- `states[]`
- `events[]`
- `allowed_transitions[]`
- `terminal_states[]`
- `recovery_or_successor_policy`
- `supersession_policy`
- `authority_precedence_rules[]`
- `mode_isolation_rules[]`
- `cross_machine_dependencies[]`
- `authoritative_source_refs[]`
- `notes`

Execution steps:
1. Parse `state_machines.md` section by section from `6.1` through `6.31`.
2. Create a registry entry for every explicitly named machine or state axis.
3. Separate true lifecycle machines from classification axes.
   For example:
   - `RunManifest.lifecycle_state` is a lifecycle machine,
   - `ParityResult.parity_classification` is a classification axis,
   - `TrustSummary` includes multiple coordinated axes that must be represented without collapsing them into one fake lifecycle.
4. Extract transition edges exactly as source strings and normalize them into structured event-driven records.
5. Build a cross-state invariant index that captures global rules and the later `6.29 Cross-state invariants`.
6. Build a terminal/recovery/supersession matrix so later agents can distinguish:
   - terminal but replayable,
   - terminal and retired,
   - terminal but replaceable by successor,
   - recoverable via child/successor allocation rather than in-place reopen.
7. Map each machine to the schema field that persists it.
8. Record where the core engine or modules are expected to trigger each transition.
9. Emit ambiguity records for any object that appears to have stateful behavior in prose but lacks a formal state machine.

Gap handling you must perform:
- Close the “no machine-readable state registry” gap.
- Close the “no unified event-edge list across all machines” gap.
- Close the “compound trust / parity / cursor state axes are easy to flatten incorrectly” gap.
- Close the “no terminal-vs-recovery-vs-supersession matrix” gap.
- Close the “schema coverage for state fields is not centrally indexed” gap.

Mandatory edge cases:
- `RunManifest` pre-start faults differ from post-start faults.
- `ExperienceCursor` / `WorkspaceCursor` terminal posture creates successor cursors rather than reopening the old one.
- review-required runs still use `run_completed`; review posture is expressed through gates and bundles, not a separate manifest state.
- `TrustSummary` has lifecycle and multiple coordinated classification bands; do not collapse them.
- `AuthorityInteractionRecord.lifecycle_state` and `SubmissionRecord.lifecycle_state` are related but not the same machine.
- `LowNoiseExperienceFrame` includes attention, presentation, and recovery posture axes; preserve them separately.
- release/control states in `6.25` are still governed state semantics even if they do not map 1:1 to a single domain entity.
- client-facing journeys such as onboarding, document request, and approval packs are canonical state machines, not just UI copy flows.

Backend engineering requirements:
- Use explicit machine classes and strongly typed transition records.
- Validate that every transition references known states and named events.
- Detect illegal duplicate transitions, conflicting terminal-state declarations, or orphan states.
- Keep extraction code idempotent and diff-friendly.
- Do not introduce hidden default transitions or auto-generated “unknown” states unless the source explicitly names them.

Validation rules:
- Every state machine section in `state_machines.md` must be represented.
- Every allowed transition string must appear exactly once in the edge list for that machine.
- Every machine with a schema-backed state field must map to that schema.
- Every global and cross-state invariant must be indexed with source references.
- Run:
  - `python3 Algorithm/scripts/validate_contracts.py --self-test`
  - `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- A future engineer can build database constraints, service guards, replay/recovery logic,
  and frontend status mapping directly from your outputs without re-parsing the entire state-machine document.
```
## Verification

- Prompt authored from direct analysis of the Taxat algorithm corpus and aligned to the authoritative card slug.
- Instructions explicitly preserve card status neutrality; claim and completion state must continue to live only in `Checklist.md`.

### Implementation Outputs

- Created `tools/analysis/build_state_machine_registry.py`.
- Created `tools/analysis/validate_state_machines.py`.
- Created `docs/analysis/09_state_machine_definitions_and_transition_invariants.md`.
- Created `docs/analysis/09_terminal_recovery_and_supersession_matrix.md`.
- Created `docs/analysis/09_cross_state_invariant_index.md`.
- Created `diagrams/analysis/09_state_machine_overview.mmd`.
- Created `data/analysis/state_machine_registry.json`.
- Created `data/analysis/state_transition_edges.csv`.
- Created `data/analysis/state_machine_invariants.jsonl`.
- Created `data/analysis/state_machine_schema_coverage.json`.
- Created `data/analysis/compound_state_axes.json`.
- Created `data/analysis/unmodeled_or_ambiguous_state_postures.json`.

### State Machine Findings

- The registry now covers `53` canonical machines: `31` lifecycle machines, `13` classification axes, `2` cursor machines, `6` release-control machines, and `1` compound-axis record, with source coverage spanning the global rules, the cross-state invariant section, and all machine sections represented in `state_machines.md`.
- The transition ledger now exports `382` normalized transitions: `375` explicit event edges and `7` prose-rule transitions, with stable per-machine transition ids after deduping merged overlay sections such as `ExperienceCursor.cursor_state`.
- The invariant index now captures all `29` required invariants: `9` global state-machine rules from `6.1` and `20` cross-state invariants from `6.29`.
- Compound and coordinated state families are preserved instead of flattened: the outputs now register explicit compound groups for parity, trust summary, the cursor family, and the three `LowNoiseExperienceFrame` axes.

### Coverage And Ambiguity Notes

- Schema coverage is now centralized for every machine. `46` machines are exact enum matches, with the remaining `7` machine rows made explicit as `2` field-missing cases, `1` no-enum case, `1` non-enumerated counter field, `1` schema-missing case, `1` schema-subset case, and `1` schema-superset case.
- The most important intentional edge-case preservation is `ExperienceCursor.cursor_state`: the registry records `REBASED` as terminal per the base section while also preserving the overlay edge back to `LIVE`, and emits an explicit `conflicting_terminality_overlay` ambiguity row for that implementation decision.
- Overlay and prose-only state families are now machine-registered rather than left implicit. `EvidenceGraph.lifecycle_state` merges the base and filing-proof overlay sections, `ProofBundle.lifecycle_state` is preserved with prose-only transitions and a corresponding note, and `RouteStabilityContract.publication_generation` is tracked as a non-finite release/control counter posture rather than a fake enum machine.
- Residual ambiguity is now machine-readable instead of hidden. The ambiguity register contains `107` rows: `1` terminality conflict, `12` entity-catalog primary state fields without formal machines, and `94` schema state fields without formal machines.

### Verification Evidence

- `python3 tools/analysis/build_state_machine_registry.py` completed successfully with status `PASS`.
- `python3 tools/analysis/validate_state_machines.py` completed successfully with status `PASS`.
- `./.venv/bin/python3 Algorithm/scripts/validate_contracts.py --self-test` completed successfully with status `PASS`.
- `./.venv/bin/python3 Algorithm/tools/forensic_contract_guard.py` completed successfully with status `PASS`.
