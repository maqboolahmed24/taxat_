---
id: pc_0047
slug: phase_01_seq_047_browser_automation_create_malware_scanning_quarantine_service_or_record_self_host_decision
mode: sequential
phase: "01"
wave: null
track: null
task_number: "047"
block_key: phase_01_seq
original_key: phase_01_seq_047
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0047

## Task Summary

browser automation create malware scanning quarantine service or record self host decision.

## Metadata

- ID: `pc_0047`
- Mode: `sequential`
- Phase: `01`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `047`
- Source Slug: `phase_01_seq_047_browser_automation_create_malware_scanning_quarantine_service_or_record_self_host_decision`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0046](./pc_0046.md) `phase_01_seq_046_browser_automation_create_managed_ocr_document_extraction_project_or_record_self_host_decision`
- Next: [pc_0048](./pc_0048.md) `phase_01_seq_048_browser_automation_capture_anti_bot_mfa_or_manual_checkpoint_evidence_for_blocked_portals`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt

Use the shared operating contract first:
[../shared_operating_contract_0046_to_0053.md](../shared_operating_contract_0046_to_0053.md)

```text
Execute roadmap task `phase_01_seq_047_browser_automation_create_malware_scanning_quarantine_service_or_record_self_host_decision`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0046` if they exist,
especially the dependency register, storage/eventing ADR, provisioning workspace, secret policy,
document-extraction selection record, and any earlier upload/object-storage decisions.

Mission:
Create the governed upload-intake safety boundary for binary artifacts.
You must either provision/adopt a managed malware-scanning and quarantine service for the chosen platform,
or record an explicit self-host decision that still preserves the algorithm's upload law and quarantine semantics.
This task is not optional operational polish.
The source algorithm treats transfer, scan, validation, attachment confirmation, and current-request satisfaction as distinct,
so the system needs an executable scanning/quarantine layer that can enforce those distinctions.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially the secure document-upload flow,
  user-visible phase distinctions, typed rejection/quarantine posture, and the rule that success is not implied by transfer alone.
- `northbound_api_and_session_contract.md -> 2.2 Customer/Client portal and upload-session surfaces`
  for upload-session allocation, request-binding, scan/validation exposure, and stale/rebase posture.
- `upload_session_request_binding_and_rebase_contract.md` and `upload_session_recovery_harness_contract.md`
  for frozen request lineage, duplicate prevention, scanner-delay proofs, and attachment-confirmation separation.
- `data_model.md`, especially `ClientUploadSession`, `CollaborationAttachment`, `AuthorityIngressReceipt` quarantine fields where applicable,
  and any upload-state objects that carry `malware_scan_state`, `scan_completed_at`, or quarantine lineage.
- `security_and_runtime_hardening_contract.md`, especially least-privilege ingress/egress, inbox/dedupe, and token/vault boundary rules.
- `deployment_and_resilience_contract.md`, especially queue/object-store/runtime topology and the rule that durable truth is not the broker.
- `retention_and_privacy.md` for evidence-bearing object retention, masking, and export restrictions.
- `release_candidate_identity_and_promotion_evidence_contract.md` wherever admissibility requires quarantine posture `NONE`.
- Prior cards `pc_0018`, `pc_0020`, `pc_0032`, `pc_0046`, and any storage-provider decisions.
- Current official provider documentation for the chosen malware-scanning path.
  If no provider was fixed earlier, prefer the chosen cloud platform's first-party managed object-malware scanning capability.

Deliverables to create:
1. `automation/provisioning/src/providers/malware/flows/create_managed_scanning_service_or_record_self_host_decision.ts`
2. `automation/provisioning/src/providers/malware/contracts/malware_scanning_service_record.schema.json`
3. `automation/provisioning/src/providers/malware/contracts/upload_scan_outcome_mapping.schema.json`
4. `config/uploads/malware_scan_outcome_mapping.json`
5. `config/uploads/scan_coverage_and_rescan_policy.json`
6. `config/uploads/quarantine_lifecycle_policy.json`
7. `config/uploads/quarantine_release_policy.json`
8. `data/provisioning/malware_scanning_selection_record.template.json`
9. `data/provisioning/quarantine_storage_binding.template.json`
10. `docs/provisioning/malware_scanning_and_quarantine_runbook.md`
11. `automation/provisioning/report_viewer/src/routes/upload-intake-safety-board.tsx`
12. `tests/playwright/provisioning/malware/create_scanning_service.spec.ts`
13. `tests/unit/malware_scan_outcome_mapping.spec.ts`
14. `tests/unit/quarantine_lifecycle_policy.spec.ts`

Required design decisions and policy dimensions you must make explicit:
- provider choice or self-host decision, with environment partitioning
- what exact upload classes are covered now and what coverage gaps remain
- size/type limits, archive/compression posture, encrypted/password-protected file posture, and unsupported-format handling
- scan result taxonomy at minimum covering:
  `PENDING`,
  `CLEAN`,
  `MALICIOUS`,
  `SUSPICIOUS_REVIEW_REQUIRED`,
  `UNSUPPORTED`,
  `FAILED_RETRYABLE`,
  and `BLOCKED_BY_POLICY`
- the mapping from provider-specific outcomes to `ClientUploadSession.malware_scan_state`,
  `next_action_code`, `dominant_hazard_code`, and customer-safe portal copy
- quarantine storage/container/bucket binding and whether quarantine implies copy, move, tag, tombstone, or metadata-only state
- preview/download policy for quarantined objects
- rescan triggers such as object-version change, scanner-signature upgrade, manual release, or provider false-positive review
- relationship between clean scan status and later extraction eligibility
- retention and lifecycle rules for clean uploads, malicious uploads, quarantined uploads, and release evidence artifacts

Execution steps:
1. Reconcile earlier storage/provider decisions and determine whether a managed scanning capability is already selected,
   missing, or explicitly rejected.
   If unresolved, produce a typed option matrix instead of silently choosing.
2. Implement an idempotent provisioning/adoption flow for the scanning service.
   It must detect an existing service configuration, adopt when matching, and emit typed drift findings when mismatched.
3. Create `malware_scan_outcome_mapping.json`.
   This file must translate provider-specific outcomes into governed upload states, portal-safe reason families,
   operator-facing hazard codes, and release/evidence posture.
4. Create `scan_coverage_and_rescan_policy.json`.
   It must declare what file sizes/types are scanned, what is blocked before upload, what is accepted but review-required,
   and when rescans occur.
5. Create `quarantine_lifecycle_policy.json` and `quarantine_release_policy.json`.
   These files must define:
   storage isolation,
   allowed viewers,
   retention tags,
   no-preview/no-download rules,
   release authority,
   and the evidence required before any release from quarantine.
6. Bind scanning to the upload lifecycle:
   transfer completion must not imply clean status;
   extraction must not run on pending/quarantined uploads;
   attachment confirmation must remain separate from clean-scan completion.
7. Build `upload-intake-safety-board` in the provisioning report viewer.
   The page must let an operator inspect upload phases, scan coverage, quarantine policy, result mappings, and release rules.
8. Add unit tests for outcome mapping, quarantine policy, unsupported cases, and coverage limits.
9. Add Playwright coverage for the viewer and the provider-flow fixture path.
   Live-provider steps must remain opt-in.

Visual and interaction specification for `upload-intake-safety-board`:
- This page must feel like a premium intake safety conveyor, not a SOC wall.
  It should be calm, precise, and built around state transitions.
- Layout:
  - `64px` top context bar with provider badge, environment selector, and intake safety posture chip.
  - left scenario rail `252-268px` listing upload classes, coverage gaps, and quarantine reason families.
  - central state-conveyor canvas with six ordered columns:
    `Received`,
    `Transferred`,
    `Scan Pending`,
    `Clean`,
    `Rejected`,
    `Quarantined`.
  - right inspector `360-392px` showing mapping rules, retention/lifecycle posture, and release authority for the selected state or scenario.
- Palette:
  background `#F5F6F4`,
  primary surface `#FFFFFF`,
  secondary surface `#EEF1EC`,
  ink `#111418`,
  muted ink `#65707A`,
  hairline `rgba(17,20,24,0.08)`,
  accent indigo `#465C77`,
  accent amber `#8A5C18`,
  accent garnet `#8C4340`,
  success `#16624B`,
  danger `#A63B32`.
- Diagram:
  the state conveyor itself is the primary diagram.
  Use thin connectors and state chips; do not add decorative charts.
- Typography:
  typography-led, with monospace only for policy IDs, queue/topic names, or storage refs.
- Motion:
  120-150ms max for state focus and inspector reveal.
  Reduced-motion mode must use opacity and outline emphasis only.
- Interaction:
  keyboard flow must progress scenario rail -> conveyor -> inspector.
  State chips and reason rows must have stable semantic locators.
  Customer-safe phrasing and internal hazard phrasing must be visibly distinct.

Gap handling you must perform:
- Close the “uploads have scan states in the algorithm, but there is no executable provider/self-host decision” gap.
- Close the “provider scan outcomes are not yet normalized into upload-session and portal-safe semantics” gap.
- Close the “quarantine storage, preview restrictions, and release authority are not yet machine-readable” gap.
- Close the “later OCR or attachment logic would otherwise have to guess when a file is safe to process” gap.
- Close the “coverage limits and unsupported file classes are not yet explicit” gap.

Mandatory edge cases:
- full byte transfer with scanner delay must still leave the upload non-accepted and non-previewable when required
- duplicate scan callbacks or duplicate object events must not produce duplicate state transitions
- password-protected archives, encrypted blobs, nested archives, or unsupported binary formats must surface bounded outcomes and safe next actions
- a provider may report ambiguous or suspicious rather than clearly malicious; the mapping must preserve review-needed posture rather than collapsing to clean/reject heuristics
- object-version updates must trigger rescan or explicit rejection of stale scan reuse
- false-positive review or manual release must preserve auditable lineage and must never silently erase quarantine history
- quarantine artifacts must remain blocked from portal preview/download
- if the provider is not selected and a self-host path is required, the decision record must still define signature update posture, runtime isolation, and throughput assumptions

Backend, performance, and security requirements:
- Treat scanning as event-driven and idempotent.
  Provider/object events may repeat or arrive out of order.
- Keep scan state separate from validation and attachment confirmation in every durable record.
- Preserve least-privilege object access and scoped service credentials through the governed secret boundary.
- Quarantine storage must be explicitly isolated from clean evidence storage and from public/static serving paths.
- Do not use the queue/broker as the durable source of upload truth.
  Persist scan receipts or outcome lineage to durable stores before projecting customer-visible state.
- Provide clear structured error families:
  provider drift,
  callback auth failure,
  unsupported format,
  scanner timeout,
  service unavailable,
  quarantine release rejected,
  and state mismatch.

Validation rules:
- Unit tests must prove every provider outcome maps to one bounded governed outcome and one safe customer/operator posture.
- Unit tests must prove no quarantined artifact is marked previewable or accepted.
- Unit tests must prove coverage gaps are explicit, not implied.
- Playwright must validate semantic locators, reduced-motion parity, keyboard flow, and inspector rendering on the viewer page.
- Live-provider configuration must be opt-in and environment-gated.

Definition of done:
- A later upload, extraction, or portal agent can inspect one authoritative scanning/quarantine selection record,
  know the scan-result taxonomy and lifecycle rules,
  know which storage paths and release authorities exist,
  and implement governed upload intake without inventing malware/quarantine semantics ad hoc.
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm corpus and aligned to the authoritative card slug.
- Instructions explicitly preserve card status neutrality; claim and completion state must continue to live only in `Checklist.md`.
