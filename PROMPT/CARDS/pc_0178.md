---
id: pc_0178
slug: phase_04_parallel_wave_02_track_backend_low_noise_010_implement_semantic_accessibility_contract_data_publication_for_all_governed_surfaces
mode: parallel
phase: "04"
wave: "02"
track: "backend_low_noise"
task_number: "010"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_low_noise_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0178

## Task Summary

implement semantic accessibility contract data publication for all governed surfaces.

## Metadata

- ID: `pc_0178`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend low noise`
- Task Number: `010`
- Source Slug: `phase_04_parallel_wave_02_track_backend_low_noise_010_implement_semantic_accessibility_contract_data_publication_for_all_governed_surfaces`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0177](./pc_0177.md) `phase_04_parallel_wave_02_track_backend_low_noise_009_implement_focus_restoration_return_target_and_recovery_posture_projection_rules`
- Next: [pc_0179](./pc_0179.md) `phase_04_parallel_wave_02_track_backend_portal_001_implement_client_portal_workspace_route_specific_derivatives_and_view_guards`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0174_to_0181.md](../shared_operating_contract_0174_to_0181.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_low_noise_010_implement_semantic_accessibility_contract_data_publication_for_all_governed_surfaces`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0177` if they exist, especially `pc_0169` through `pc_0177`, `pc_0158` through `pc_0165`, and the shell-family registry from `pc_0176`.

Mission:
Publish one authoritative `semantic_accessibility_contract` for every governed shell family and route-visible surface currently materialized in the repo.
This task is about data publication, anchor inventories, focus-order semantics, announcement kinds, and identifier parity. It is not the full regression-pack implementation.
The output must be rich enough that later Playwright/XCUITest suites can consume it directly without inventing selector or announcement grammars.

Source algorithm grounding you must honor:
- `semantic_selector_and_accessibility_regression_pack_contract.md`, entire document.
- `frontend_shell_and_interaction_law.md`, especially sections 2.2, 8, and 10.
- `low_noise_experience_contract.md`, especially semantic selectors and accessibility additions.
- `customer_client_portal_experience_contract.md`, especially minimum semantic selectors, accessibility rules, and Playwright validation minimum.
- `admin_governance_console_architecture.md` and `macos_native_operator_workspace_blueprint.md` for future-facing shell-family coverage where the read models already exist or are seeded.
- `data_model.md`, especially `semantic_accessibility_contract` references on low-noise, portal, and native artifacts.
- `schemas/semantic_accessibility_contract.schema.json` and `schemas/semantic_accessibility_regression_pack.schema.json`.
- `test_vectors.md`, especially TV-39P through TV-39U.

Deliverables to create:
1. `packages/backend-low-noise/src/services/project_semantic_accessibility_contract.ts`
2. `packages/backend-low-noise/src/services/get_shell_anchor_inventory.ts`
3. `packages/backend-low-noise/src/services/get_shell_announcement_profile.ts`
4. `packages/backend-low-noise/src/services/validate_semantic_accessibility_contract.ts`
5. `packages/backend-low-noise/src/services/publish_semantic_accessibility_contract_into_read_models.ts`
6. `packages/backend-low-noise/src/tests/semantic_accessibility_contract_publication.spec.ts`
7. `docs/accessibility/semantic_accessibility_contract_publication.md`
8. Optional but recommended: `apps/admin-console-web/src/routes/debug/accessibility/semantic-anchor-atlas.tsx`

Required design decisions you must make explicitly:
- define the anchor inventory and semantic focus order per shell family and per contextual-route variant, including when `RETURN_PATH_CONTROL`, limitation notices, recovery notices, and artifact handoff anchors must appear
- define the exact browser/native identifier parity model before full native execution suites land; names must be semantic and stable, not visual or layout-derived
- define the announcement-kind taxonomy and how polite versus assertive changes are derived from durable problem/settlement/recovery truth
- define how portal contextual routes extend the base portal contract without becoming a second selector profile

Execution requirements:
- publish `semantic_accessibility_contract` on every route-visible shell artifact that currently exists or is seeded in this tranche: low-noise experience frames, collaboration workspaces if already present, client portal workspaces, and any seeded governance/native wrappers that already carry shell-family semantics
- keep the contract server-authored; renderers may mirror it into `data-testid` and `accessibilityIdentifier`, but may not invent additional primary anchors
- `DOMINANT_QUESTION` must be first in the heading/read-order story, and the semantic focus order must match visible semantic order
- limitation and recovery notices must become addressable anchors when present
- artifact handoff semantics must keep current and history anchors distinct
- reduced-motion semantics must preserve the same recovery story and dominant action meaning
- if you add the atlas, make it one semantic inventory table plus one traversal ladder, not a component zoo

Gap handling you must close while implementing:
- the source corpus mandates semantic contracts, but read models may still publish only ad hoc test ids or none at all; publish the actual grouped contract now
- selector profiles can drift across calm shell, portal, governance, and native seed work; unify them through one contract layer
- live-update announcement posture and return-path anchor presence are easy to forget in route-local code; make them explicit and typed
- do not attempt the full semantic regression pack here; publish only the authoritative data the later suite will consume

Mandatory edge cases:
- `shell_family` and `selector_profile` must agree with the owning shell family mapping
- required anchors for shell root, object anchor, dominant question, dominant action, promoted support region, limitation notice, recovery notice, artifact preview header, artifact-state label, and return-path control must appear whenever the shell/state makes them lawful
- browser and native identifier semantics must mirror the same semantic anchor names; no browser-only nicknames
- `ACTIVITY_DELTA` and `BADGE_DELTA` remain polite; `COMMAND_FAILURE`, `RECOVERY_NOTICE`, and `TERMINAL_SETTLEMENT` remain assertive without stealing active focus
- contextual portal routes must keep `RETURN_PATH_CONTROL` addressable whenever a lawful return path exists

Playwright and contract-test requirements:
- unit-test the contract projector and anchor inventory logic directly
- if any shipped route payload is patched, add Playwright APIRequestContext assertions proving the semantic contract is present and stable
- if you add the atlas, test it with Playwright using semantic locators, keyboard traversal, and reduced-motion coverage
- capture traces on failure and preserve the emitted contract payloads for diagnosis

Backend, performance, and security requirements:
- publish semantic anchors from server-authored meaning, not from rendered DOM shape or CSS class names
- do not leak staff-only or hidden content through anchor naming or announcement payloads
- keep the contract lightweight and deterministic so it can travel with every route-visible read model safely
- fail closed when a shell artifact is missing mandatory semantic anchors for its family/state

Validation rules:
- validate every emitted `semantic_accessibility_contract` against schema
- add regression coverage for anchor presence, focus order, announcement kinds, current-vs-history anchor separation, and return-path control presence
- keep route-specific portal anchor additions and low-noise anchor order deterministic

Definition of done:
- route-visible governed shells publish one authoritative `semantic_accessibility_contract`
- browser and future native automation can consume shared semantic anchor data instead of inventing local selector grammars
- accessibility semantics, live announcements, and return-path anchors become part of backend truth rather than frontend convention
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
