---
id: pc_0198
slug: phase_04_parallel_wave_02_track_backend_governance_010_implement_gate_decision_explainability_projection_and_reason_code_compression
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "010"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0198

## Task Summary

010 implement gate decision explainability projection and reason code compression.

## Metadata

- ID: `pc_0198`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `010`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_010_implement_gate_decision_explainability_projection_and_reason_code_compression`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0197](./pc_0197.md) `phase_04_parallel_wave_02_track_backend_governance_009_implement_manifest_lineage_explorer_and_reuse_decision_tracer_queries`
- Next: [pc_0199](./pc_0199.md) `phase_04_parallel_wave_02_track_backend_recovery_001_implement_recovery_checkpoint_model_and_tiered_checkpoint_open_close_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0198_to_0205.md](../shared_operating_contract_0198_to_0205.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_010_implement_gate_decision_explainability_projection_and_reason_code_compression`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0197` if they exist, especially `pc_0125`, `pc_0126`, `pc_0127`, `pc_0159`, `pc_0160`, `pc_0189`, and `pc_0190` through `pc_0197`.

Mission:
Implement the authoritative read-side explainability projection for persisted gate, trust, and terminal decision artifacts and harden the deterministic reason-code compression bridge.
This task is not allowed to invent a second decision grammar.
It must project the persisted `DecisionExplainabilityContract` into governance, audit, workflow, and low-noise shell reads without letting UI code reconstruct dominant reason, summary reason order, or semantic qualifiers from metrics.

Source algorithm grounding you must honor:
- `gate_decision_explainability_and_reason_code_compression_contract.md`, end to end.
- `exact_gate_logic_and_decision_tables.md`, especially `7.1 Gate result contract`, the maximum-rank reason-family rules, and the invariant that final decision semantics cannot depend on container iteration order.
- `data_model.md`, especially `GateDecisionRecord`, `TrustSummary`, and `DecisionBundle`.
- `modules.md`, especially the earlier gate explainability and terminal-bundle persistence helpers you already established, plus any projection hooks that consume persisted decision artifacts.
- `frontend_shell_and_interaction_law.md`, `low_noise_experience_contract.md`, and `dominant_question_and_single_action_contract.md` for how compressed explanation must support primary action and no-safe-action posture.
- `northbound_api_and_session_contract.md` if you expose or patch a read route.
- `schemas/decision_explainability_contract.schema.json`, `schemas/gate_decision_record.schema.json`, `schemas/trust_summary.schema.json`, `schemas/decision_bundle.schema.json`, and any sample artifacts that show the persisted compressed prefix shape.
- `scripts/validate_contracts.py`, especially `decision_explainability_contract_prefix_and_qualifier_ok`, `decision_explainability_contract_prefix_alignment`, and `decision_explainability_contract_qualifier_order`, plus the parent/contract drift checks inside gate, trust, and decision-bundle validation.
- `PATCH_RESOLUTION_INDEX.md`, especially any fixes around dominant reason drift, qualifier omission, or summary-source divergence.
- `test_vectors.md`, especially the vectors around compressed terminal reasons, trust qualifier disclosure, and stable backend-authored explanation under reload and replay.

Deliverables to create:
1. `packages/backend-compute/src/services/project_decision_explainability_view.ts`
2. `packages/backend-compute/src/services/validate_persisted_decision_explainability_alignment.ts`
3. `packages/backend-governance/src/projectors/build_gate_decision_explainability_projection.ts`
4. `packages/backend-governance/src/queries/get_gate_decision_explainability.ts`
5. `packages/backend-governance/src/queries/list_decision_explainability_rows.ts`
6. `packages/backend-governance/src/tests/gate_decision_explainability_projection.spec.ts`
7. `packages/backend-governance/src/tests/gate_decision_explainability_route.spec.ts`
8. `docs/governance/gate_decision_explainability_projection_and_reason_compression.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/decision-explainability-lab.tsx`

You MUST patch any existing governance, workflow, or investigation read surface that still rebuilds reason summaries from raw `reason_codes[]`, raw metrics, or ad hoc ranking logic. All such surfaces must consume the persisted contract or fail closed with a typed integrity problem.

Required design decisions you must make explicitly:
- define the exact projection family grammar for `GATE_DECISION`, `TRUST_SUMMARY`, and `DECISION_BUNDLE` so read surfaces can mount them without hiding which parent artifact remains authoritative
- define how `compressed_reason_codes[]` and `suppressed_reason_count` are mirrored into thin projection rows without creating a second source of truth
- define how `semantic_qualifiers[]` map into badge, legend, and assistive disclosure while preserving canonical ordering
- define how `action_projection_state` controls whether the projection exposes a primary-action hint, a review-only hint, or a no-safe-action warning
- define the fail-closed behavior for historical rows that are missing `decision_explainability_contract` or whose parent fields drift from the contract; do not allow UI-side reconstruction as a fallback
- define which bounded plain-text field is projected (`plain_explanation`, `plain_summary`, or `plain_reason`) and how the route distinguishes it by artifact family

Execution requirements:
- treat the parent artifact as authoritative and project only persisted explainability fields
- preserve `ordered_reason_codes[]` exactly as persisted; do not sort or deduplicate them in projection code
- preserve `dominant_reason_code = ordered_reason_codes[0]`
- preserve `compressed_reason_codes[]` as the canonical prefix of the ordered list with cap `3`
- preserve `suppressed_reason_count = len(ordered_reason_codes) - len(compressed_reason_codes)`
- preserve the canonical qualifier order from the schema and validator logic
- ensure `DecisionBundle.decision_reason_codes[]` exactly mirrors the compressed prefix from the contract
- ensure route or query consumers cannot mount explanation without knowing which artifact family they are reading
- surface typed drift/integrity failures when parent reason fields no longer match the contract

Gap handling you must close while implementing:
- close any gap where governance or audit routes reconstruct dominant reason from raw reason arrays or metrics
- close any gap where trust, gate, and terminal bundles can drift on qualifier ordering or compressed prefix content
- close any gap where safe-next-action posture can be hidden because verbose reasons displaced action semantics
- close any gap where replay, queue reload, or stale browser refresh could choose a different summary reason for the same persisted posture
- close any gap where missing historical explainability contracts would tempt renderer-owned backfill rather than a typed integrity failure or an explicit migration/backfill job

Mandatory edge cases:
- a parent artifact with four or more ordered reasons must still project only the first three compressed reasons plus exact suppressed count
- `dominant_reason_code` must equal the first ordered reason and the first compressed reason whenever the compressed list is non-empty
- authority-sensitive postures must include `AUTHORITY_STATE`
- limitation-sensitive postures must include `LIMITATION_STATE`
- override-governed postures must include `OVERRIDE_STATE`
- action-bearing or no-safe-action postures must include `ACTIONABILITY_STATE`
- no projection may emit a bounded summary text longer than the persisted explainability budget
- no projection route may infer a dominant reason from severity buckets or metric values when persisted reasons exist

Playwright and contract-test requirements:
- if you expose or patch a northbound explainability route, add Playwright `APIRequestContext` coverage for artifact family, ordered reasons, compressed prefix, suppressed count, qualifier order, and typed drift failures
- add direct tests proving that gate, trust, and decision-bundle rows project the same compressed prefix and dominant reason that were persisted
- if you add the optional lab, add Playwright browser tests that verify the reason ladder, compressed-prefix rail, qualifier badges, keyboard focus order, reduced-motion-safe expansion, and no-safe-action disclosure using semantic locators only
- capture serialized payloads and traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a premium two-column composition: left reason ladder and artifact identity rail, right detail canvas for plain language, qualifiers, and projected action posture
- max width `1360px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717` / `#667085`; explainability accent `#1D4ED8`; continuity accent `#0F766E`; caution `#B7791F`; danger `#C2410C`
- place a thin compressed-prefix ribbon directly below the identity header showing dominant reason, compressed reasons, and suppressed count in one line
- render semantic qualifiers as ordered chips with concise labels and precise assistive descriptions; never as re-sorted freeform tags
- if you show a diagram, show one vertical reason ladder with the dominant reason pinned at top; do not use metric tiles or ranking charts
- animate only opacity / translate / height in `140ms`–`180ms`; reduced motion must preserve all meaning with no ornamental transitions

Backend, performance, and security requirements:
- projector code must be pure read-side logic; it must not mutate or recompute decision truth
- keep integrity checks cheap and indexable by artifact family plus artifact id / manifest id
- log only ids, artifact family, dominant reason, and typed integrity codes; never dump sensitive input or raw metric maps
- if you add migration/backfill for missing explainability contracts, keep it explicit, bounded, and separately auditable

Validation rules:
- validate all emitted or backfilled `DecisionExplainabilityContract` packets against schema
- add regression coverage for the named validator cases above
- prove parent/contract alignment for gate, trust, and decision-bundle artifacts
- keep this task limited to explainability projection and integrity hardening; do not absorb new gate-evaluation or terminal-finalization semantics beyond the minimum patches needed for reuse

Definition of done:
- the repo has one authoritative explainability projection path for gate, trust, and terminal decision artifacts
- governance and audit surfaces now consume persisted compressed reasons and qualifiers instead of reconstructing them
- dominant reason, compressed prefix, semantic qualifiers, and action posture remain byte-stable across reload, replay, and low-noise shell rendering
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
