---
id: pc_0122
slug: phase_03_parallel_wave_01_track_backend_compute_002_implement_forecast_set_generation_with_execution_mode_boundary_enforcement
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "002"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0122

## Task Summary

implement forecast set generation with execution mode boundary enforcement.

## Metadata

- ID: `pc_0122`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `002`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_002_implement_forecast_set_generation_with_execution_mode_boundary_enforcement`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0121](./pc_0121.md) `phase_03_parallel_wave_01_track_backend_compute_001_implement_compute_result_formula_engine_with_exact_decimal_handling`
- Next: [pc_0123](./pc_0123.md) `phase_03_parallel_wave_01_track_backend_compute_003_implement_risk_report_derivation_and_materiality_threshold_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0118_to_0125.md](../shared_operating_contract_0118_to_0125.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_002_implement_forecast_set_generation_with_execution_mode_boundary_enforcement`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0121` if they exist, especially `pc_0017`, `pc_0028`, `pc_0061`, `pc_0074`, `pc_0077`, `pc_0097`, `pc_0101`, and `pc_0121`.

Mission:
Implement the analysis-only `ForecastSet` generator that derives deterministic point forecasts and optional Monte Carlo scenarios from the frozen compute basis and forecast profile.
This task must enforce the execution-mode boundary, derive deterministic seeds, preserve canonical scenario ordering, and ensure forecast artifacts never mutate or overwrite the compliance compute result in place.

Source algorithm grounding you must honor:
- `core_engine.md`, especially post-seal `FORECAST`.
- `modules.md`, especially `FORECAST(...)` and `SEED(...)`.
- `compute_parity_and_trust_formulas.md`, especially section `8.5 Forecast formula`.
- `data_model.md`, especially `ForecastSet`.
- `replay_and_reproducibility_contract.md`, especially deterministic seed and canonical decimal-string rules.
- `test_vectors.md`, especially `TV-10` and `TV-44F`.
- Existing schemas including `forecast_set.schema.json`.

Deliverables to create:
1. `packages/backend-compute/src/models/forecast_set.ts`.
2. `packages/backend-compute/src/repositories/forecast_set_repository.ts`.
3. `packages/backend-compute/src/services/generate_forecast_set.ts`.
4. `packages/backend-compute/src/services/forecast_baseline_series_builder.ts`.
5. `packages/backend-compute/src/services/forecast_seed_deriver.ts`.
6. `packages/backend-compute/src/services/forecast_scenario_generator.ts`.
7. `packages/backend-compute/src/services/enforce_forecast_execution_boundary.ts`.
8. `db/migrations/phase03_0122_forecast_set.sql`.
9. `docs/compute/forecast_set_generation_with_execution_mode_boundary_enforcement.md`.
10. `tests/unit/backend_compute/forecast_set_generation.spec.ts`.
11. `tests/integration/backend_compute/forecast_execution_boundary.spec.ts`.

Required design decisions you must make explicit:
- Define the baseline time-series source for each forecast category.
  Make the source deterministic and tied to frozen inputs rather than live reads.
- Define the canonical horizon ordering and `scenario_id` ordering.
- Define the exact seed-derivation algorithm so `SEED(...)` in the modules layer and `forecast_seed` in the formula layer resolve to one implementation.
- Define how forecast profiles, floors, caps, and residual scales are materialized from frozen config.
- Define the compliance-vs-analysis rejection behavior.
  A compliance run that requests forecasting must fail closed with typed diagnostics rather than quietly omitting the forecast.

Execution steps:
1. Implement the schema-backed `ForecastSet` model and repository.
2. Implement `enforce_forecast_execution_boundary.ts` so only analysis-capable runs can emit forecasts and all emitted forecast artifacts remain `analysis_only = true`.
3. Implement `forecast_baseline_series_builder.ts` so baseline totals per category/horizon are derived from the frozen compute basis and frozen forecast profile.
4. Implement `forecast_seed_deriver.ts` and `forecast_scenario_generator.ts` so point-only and Monte Carlo modes are deterministic and replay-safe.
5. Implement `generate_forecast_set.ts` so it emits point forecasts, optional scenarios, seeds, and canonical contract metadata.
6. Add tests for point-only mode, Monte Carlo mode, no-baseline-step omission, deterministic seed reuse, and compliance-mode rejection.

Gap handling you must perform:
- Close the gap where the corpus names a forecast profile and residual generator but does not fully spell out the in-repo baseline-series adapter.
  Create one deterministic builder tied to frozen compute outputs.
- Close the gap where `SEED(...)` and the formula-layer `forecast_seed` could diverge.
  Publish one canonical seed-derivation service and reuse it everywhere.
- Close the gap where categories with zero baseline steps could be handled inconsistently.
  Make the omission rule explicit and test it.
- Close the gap where forecast output might otherwise leak into compliance truth.
  Enforce one-way analysis-only artifact creation.

Mandatory edge cases:
- the same frozen compute basis and forecast profile must always produce the same `ForecastSet`.
- categories with `baseline_steps = 0` must not emit point forecasts.
- `scenario_mode = POINT_ONLY` must keep `scenarios[]` and `seeds[]` empty or otherwise explicitly consistent with the schema and your documented policy.
- `scenario_mode = MONTE_CARLO` must persist stable `scenario_id -> seed` bindings.
- normalized seasonality must remain well-defined; zero or invalid seasonal mean must fail closed.
- forecast floors and caps must be respected after residual application.
- no forecast result may mutate or supersede the compliance `ComputeResult` artifact.

Backend, performance, and security requirements:
- Keep forecast generation deterministic and pure except for explicit artifact persistence.
- Reuse existing deterministic-seed helpers where available.
- Avoid large in-memory scenario explosions; stream or chunk scenario generation if needed without changing order.
- Do not log raw sensitive financial rows when reporting forecast diagnostics.

Validation rules:
- Unit-test seed derivation, point-forecast math, Monte Carlo scenario stability, and execution-boundary enforcement.
- Integration-test `ForecastSet` persistence from a frozen `ComputeResult` and forecast profile.
- Validate persisted artifacts against `forecast_set.schema.json`.
- Prove that compliance runs cannot emit forecast artifacts and that forecast artifacts never mutate compute truth.
- Re-run the contract validators after the implementation bundle is assembled.

Definition of done:
- The repo can emit deterministic `ForecastSet` artifacts for analysis-capable runs only.
- Forecast seed derivation is canonical and replay-safe.
- Forecasting is now a governed analysis artifact rather than a side-effecting compute extension.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
