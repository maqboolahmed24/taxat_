---
id: pc_0207
slug: phase_04_parallel_wave_02_track_backend_recovery_009_implement_nightly_autopilot_scheduler_sharding_reclaim_and_operator_morning_digest_derivation
mode: parallel
phase: "04"
wave: "02"
track: "backend_recovery"
task_number: "009"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_recovery_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0207

## Task Summary

009 implement nightly autopilot scheduler sharding reclaim and operator morning digest derivation.

## Metadata

- ID: `pc_0207`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend recovery`
- Task Number: `009`
- Source Slug: `phase_04_parallel_wave_02_track_backend_recovery_009_implement_nightly_autopilot_scheduler_sharding_reclaim_and_operator_morning_digest_derivation`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0206](./pc_0206.md) `phase_04_parallel_wave_02_track_backend_recovery_008_implement_nightly_batch_identity_batch_run_and_selection_disposition_persistence`
- Next: [pc_0208](./pc_0208.md) `phase_04_parallel_wave_02_track_backend_recovery_010_implement_nightly_portfolio_what_if_simulation_and_release_admissibility_counterfactuals`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0206_to_0213.md](../shared_operating_contract_0206_to_0213.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_recovery_009_implement_nightly_autopilot_scheduler_sharding_reclaim_and_operator_morning_digest_derivation`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0206` if they exist, especially `pc_0206`, `pc_0102` through `pc_0109`, `pc_0150` through `pc_0157`, `pc_0166` through `pc_0169`, and `pc_0198` through `pc_0205`.

Mission:
Implement the nightly autopilot control plane that plans stable execution shards from persisted selection rows, advances durable batch heartbeat and lifecycle state, performs lawful stale-batch reclaim with successor lineage, and publishes the next-morning operator digest only from settled persisted truth.
This task is not allowed to infer overnight posture from worker memory, logs, or queue leftovers.
Autopilot, reclaim, and digest publication must all run on the persisted `NightlyBatchRun` and related read-side truth you established in `pc_0206`.

Source algorithm grounding you must honor:
- `nightly_autopilot_contract.md`, especially sections covering stable shard planning, shard failure isolation, heartbeat ownership, stale reclaim, successor linkage, digest publication state, digest publication QA, and non-mutating simulation boundary.
- `nightly_selection_disposition_and_batch_isolation_contract.md`, especially shard-isolation law and recovery-reclaim law.
- `modules.md`, especially `PLAN_NIGHTLY_SHARDS(...)`, `EXECUTE_NIGHTLY_CLIENT_ATTEMPT(...)`, `RESOLVE_STALE_NIGHTLY_BATCH(...)`, `AGGREGATE_NIGHTLY_BATCH_OUTCOME(...)`, and `PUBLISH_OPERATOR_MORNING_DIGEST(...)`.
- `data_model.md`, especially `NightlyBatchRun`, `OperatorMorningDigest`, and any linked workflow/notification publication fields.
- `schemas/nightly_batch_run.schema.json`, `schemas/operator_morning_digest.schema.json`, and `schemas/operator_digest_derivation_contract.schema.json`.
- `scripts/validate_contracts.py`, especially the nightly digest and publication QA expectations.
- `tools/forensic_contract_guard.py`, especially the pinned `OperatorDigestDerivationContract` constants and execution-boundary requirements.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially stale-worker reclaim, publication supersession, and partial-failure isolation vectors.

Deliverables to create:
1. `packages/backend-recovery/src/services/plan_nightly_shards.ts`
2. `packages/backend-recovery/src/services/claim_nightly_shard_lease.ts`
3. `packages/backend-recovery/src/services/advance_nightly_batch_heartbeat.ts`
4. `packages/backend-recovery/src/services/resolve_stale_nightly_batch.ts`
5. `packages/backend-recovery/src/services/aggregate_nightly_batch_outcome.ts`
6. `packages/backend-recovery/src/models/operator_digest_derivation_contract.ts`
7. `packages/backend-recovery/src/models/operator_morning_digest.ts`
8. `packages/backend-recovery/src/services/publish_operator_morning_digest.ts`
9. `packages/backend-recovery/src/tests/nightly_autopilot_scheduler.spec.ts`
10. `packages/backend-recovery/src/tests/operator_morning_digest_derivation.spec.ts`
11. `docs/recovery/nightly_autopilot_scheduler_sharding_reclaim_and_operator_morning_digest_derivation.md`
12. Optional but recommended: `apps/admin-console-web/src/routes/debug/recovery/nightly-autopilot-lab.tsx`

You MUST patch any existing worker or scheduler path that claims shards, emits heartbeats, reclaims stale batches, or derives morning operator summaries without going through these canonical services.

Required design decisions you must make explicitly:
- define the stable shard-assignment algorithm from frozen execution-capable selection rows
- define shard lease ownership, heartbeat cadence, stale threshold, reclaim preconditions, and successor-link recording
- define which lifecycle transitions are legal between `PLANNED`, `RUNNING`, `QUIESCING`, `COMPLETED`, `COMPLETED_WITH_FAILURES`, `BLOCKED`, `FAILED`, and `ABANDONED`
- define the exact `OperatorDigestDerivationContract` hash inputs and publication QA checkpoints
- define how workflow item publication and notification publication settle before `operator_digest_publication_state` can become `PUBLISHED_COMPLETE`
- define how successor recovery batches supersede predecessors without duplicating unresolved-handoff rows or highlight partitions

Execution requirements:
- derive shard assignment only from persisted execution-capable `selection_entries[]` and preserve stable ownership keys per frozen batch
- keep reuse/defer/escalate/skip rows off-shard and maintain explicit blocked-entry lists and reason codes on failed or blocked shards
- advance `last_heartbeat_at` from persisted worker ownership rather than ephemeral process-local timers alone
- reclaim stale batches only after proving stale heartbeat, durable ownership break, and lawful successor intent; record predecessor/successor linkage explicitly
- ensure successor reclaim preserves or lawfully reshapes the earlier nightly window without fabricating a new universe
- derive `OperatorMorningDigest` only from persisted `NightlyBatchRun`, `RunManifest`, `DecisionBundle`, workflow, notification, and error/remediation truth
- publish one digest per tenant coverage date only after the batch reaches a quiescence-safe posture, every selected entry has an explicit `outcome_bucket`, and publication QA is complete
- keep `operator_digest_publication_state` explicit: `NOT_READY`, `WORKFLOW_PUBLICATION_PENDING`, `NOTIFICATION_PUBLICATION_PENDING`, `PUBLISHED_COMPLETE`
- preserve unresolved handoff counts, queue partition hashes, highlight order hashes, and backlog/tail-risk metrics as durable digest truth

Gap handling you must close while implementing:
- close any gap where shard planning consults transient queue position or current worker load instead of the frozen selection ledger
- close any gap where a stale batch can be reclaimed without durable heartbeat proof, successor lineage, or explicit resume posture
- close any gap where worker crashes trigger blind resends or duplicate digest publication instead of lawful successor recovery
- close any gap where the morning digest is reconstructed from logs or ad hoc queue inspection instead of persisted objects and a derivation contract
- close any gap where digest publication can reach `PUBLISHED_COMPLETE` before workflow/notification publication is settled and QA completed
- close any gap where quiescing is treated as “all succeeded” rather than “no more lawful autonomous work remains for this batch”

Mandatory edge cases:
- duplicate scheduler deliveries must reuse the same active batch and must not create a second shard plan
- shard failure must leave unrelated candidates with intact persisted disposition and outcome buckets
- stale reclaim must preserve predecessor linkage and successor law; non-stale in-flight ownership must reject reclaim
- a batch in `QUIESCING` with unsettled publication state must not publish a complete digest yet
- workflow publication failure must keep digest publication pending rather than emitting a complete handoff artifact
- notification publication failure must remain distinct from workflow publication failure
- recovery supersession must keep one coherent source batch set and lineage for the digest
- publication QA must fail closed if queue partition hash, highlight order, or coverage refs drift from the derivation contract

Playwright and contract-test requirements:
- add deterministic tests for shard-plan stability, shard failure isolation, heartbeat advancement, stale reclaim gating, successor linkage, digest summary derivation, and publication-QA state transitions
- if you expose or patch an admin/nightly-read route, add Playwright `APIRequestContext` tests for shard-state reads, reclaim results, and digest publication-state transitions
- if you add the optional lab, add Playwright browser tests that verify the shard/reclaim timeline, digest-publication strip, highlight partition ladder, keyboard navigation, and reduced-motion-safe expansion using semantic locators only
- capture payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a three-zone layout: batch/shard timeline, digest publication summary canvas, narrow quiescence/reclaim sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; accent `#0F766E`; info accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- visualize shard and digest lineage with ordered rails or ladders, never with donuts or aggregate-only KPI tiles
- show unresolved handoffs and publication blockers as explicit typed rows, not implied iconography
- animate only opacity/translate/height with `140ms` to `180ms`; respect reduced motion

Backend, performance, and security requirements:
- shard planning and reclaim must be idempotent and safe under duplicate scheduler delivery and worker retry
- protect heartbeat and lease claims with durable compare-and-swap / transactional semantics so split-brain workers cannot both own one shard
- keep digest derivation read-only relative to source objects; it may publish workflow/notification artifacts only through explicit publication steps
- prevent worker-local caches from becoming digest truth or stale-reclaim authority
- keep all publication and supersession lineage append-only and queryable for later audit/analytics work

Validation rules:
- validate all `NightlyBatchRun`, `OperatorDigestDerivationContract`, and `OperatorMorningDigest` artifacts against schema before persistence or response emission
- update fixtures/builders so all nightly digest validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one canonical backend path for shard planning, heartbeat advancement, stale reclaim, batch aggregation, and morning digest derivation/publication
- stale-batch recovery and morning handoff are durable, idempotent, and test-covered
- optional labs, if added, remain read-only projections of persisted nightly truth
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
