---
id: pc_0108
slug: phase_03_parallel_wave_01_track_backend_manifest_012_implement_manifest_reuse_recovery_replay_and_supersession_decision_service
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "012"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_012
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0108

## Task Summary

implement manifest reuse recovery replay and supersession decision service.

## Metadata

- ID: `pc_0108`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `012`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_012_implement_manifest_reuse_recovery_replay_and_supersession_decision_service`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0107](./pc_0107.md) `phase_03_parallel_wave_01_track_backend_manifest_011_implement_replay_attestation_and_deterministic_outcome_hash_persistence`
- Next: [pc_0109](./pc_0109.md) `phase_03_parallel_wave_01_track_backend_collection_001_implement_source_plan_source_window_and_collection_boundary_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0102_to_0109.md](../shared_operating_contract_0102_to_0109.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_012_implement_manifest_reuse_recovery_replay_and_supersession_decision_service`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0107` if they exist, especially `pc_0010`, `pc_0016`, `pc_0028`, `pc_0067`, `pc_0076`, `pc_0084`, `pc_0097`, `pc_0098`, `pc_0102`, `pc_0103`, `pc_0104`, `pc_0105`, `pc_0106`, and `pc_0107`.

Mission:
Implement the higher-level manifest decision orchestrator that composes prior-context loading, branch selection, start/recovery eligibility, trace assembly inputs, replay/recovery guards, and supersession-aware continuation handling into one service boundary.
This task should not duplicate lower-level modules.
It should integrate them into one authoritative decision service that later collection and execution cards can call.

Source algorithm grounding you must honor:
- `core_engine.md`, especially the request analysis, prior-manifest lookup, branch selection, child allocation handoff, and supersession-related lifecycle behavior.
- `modules.md`, especially the orchestration around prior-manifest loading, continuation decisions, trace emission, replay/recovery guards, and post-terminal continuation.
- `manifest_branch_selection_contract.md`, especially branch action semantics, reason codes, request identity inputs, and candidate evaluation rules.
- `manifest_lineage_explorer_and_reuse_decision_tracer_contract.md`, especially exhaustive candidate evaluation, selected action semantics, and mirror-consistency explanation.
- `replay_and_reproducibility_contract.md`, especially exact replay rerun idempotency, stale recovery versus active lease, and historical post-seal basis reuse.
- `manifest_and_config_freeze_contract.md`, especially continuation basis, supersession lineage, nightly-window logic, and exact reuse modes.
- `state_machines.md`, especially terminal retry versus new continuation and superseded/replay-only transitions.
- Existing schemas including `manifest_lineage_trace.schema.json`, `manifest_branch_decision_contract.schema.json`, `run_manifest.schema.json`, and `replay_attestation.schema.json`.

Deliverables to create:
1. `packages/backend-manifest/src/services/manifest_decision_orchestrator.ts`.
2. `packages/backend-manifest/src/services/manifest_reuse_recovery_replay_supersession_service.ts`.
3. `packages/backend-manifest/src/services/build_manifest_branch_decision_contract.ts`.
4. `packages/backend-manifest/src/services/build_manifest_lineage_trace.ts`.
5. `packages/backend-manifest/src/services/replay_or_recovery_path_guard.ts`.
6. `packages/backend-manifest/src/services/supersession_decision_guard.ts`.
7. `packages/backend-manifest/src/types/manifest_decision_service_result.ts`.
8. `docs/manifest/manifest_reuse_recovery_replay_and_supersession_decision_service.md`.
9. `tests/unit/backend_manifest/manifest_decision_orchestrator.spec.ts`.
10. `tests/integration/backend_manifest/reuse_recovery_replay_supersession_paths.spec.ts`.

Required design decisions you must make explicit:
- Define the public service boundary for manifest decision orchestration.
  Explain what lower-level modules it calls, what it returns, and what it intentionally defers to later allocation / collection / execution steps.
- Define the exact precedence between terminal bundle return, sealed reuse, replay child reuse, stale recovery, continuation child, new request child, and fresh root manifest.
- Define how supersession is detected or prepared without rewriting historical manifest truth.
- Define how the orchestrator returns exhaustive candidate evaluation data for later `ManifestLineageTrace` persistence.
- Define how the service integrates existing replay-attestation or decision-bundle availability into idempotent return paths.
- Define how nightly-window advancement and request-identity drift remain distinct branches.

Execution steps:
1. Implement `manifest_decision_orchestrator.ts` as the top-level request-to-decision service that composes lower-level modules from `pc_0102` through `pc_0107`.
2. Implement `replay_or_recovery_path_guard.ts` so replay/recovery-only constraints stay explicit and cannot be bypassed by generic continuation logic.
3. Implement `supersession_decision_guard.ts` so post-terminal continuation and supersession-prep semantics remain explicit and append-only.
4. Implement `build_manifest_branch_decision_contract.ts` and `build_manifest_lineage_trace.ts` so decision results are ready for persistence and explainability without duplicating business logic.
5. Add tests for same-request terminal return, pre-start sealed reuse, replay rerun reuse, stale recovery, post-terminal continuation, request-identity change, nightly-window advancement, and supersession-prep flows.
6. Ensure the orchestrator returns selected action, typed reason code, candidate evaluations, selected manifest snapshot or refs, and any required continuation basis or inheritance-mode metadata for later child allocation.

Gap handling you must perform:
- Close the gap where lower-level branch modules exist but there is no one authoritative orchestration boundary.
- Close the gap where supersession preparation could be implicit or scattered across controller code.
- Close the gap where lineage trace inputs might be assembled inconsistently by different call sites.
- Close the gap where replay and recovery safeguards could be bypassed by generic continuation orchestration.
- Close the gap where nightly-window advancement and request-identity drift might collapse into one generic “different request” branch.

Mandatory edge cases:
- exact same-request terminal manifest with missing `decision_bundle_hash` must fail closed rather than pretending a bundle exists.
- exact same-request replay rerun returns existing replay result when available instead of allocating a duplicate child.
- active lease blocks recovery-child path even when other attributes match.
- stale recovery path preserves exact recovery inheritance modes and attempt-lineage continuity.
- post-terminal continuation path remains distinct from request-identity-changed new-request child.
- nightly-window advancement remains distinct from generic request drift.
- supersession preparation must be append-only and must not rewrite old manifest lineage or outcome truth.
- branch decision contract and lineage trace candidate evaluations must cover the full canonical action vocabulary and exactly one selected action.

Backend, performance, and security requirements:
- Keep orchestration deterministic and side-effect aware: decision assembly may prepare allocation inputs, but it must not silently mutate unrelated records.
- Reuse prior loaded data and lower-level computed hashes rather than recomputing request identity or historical basis repeatedly.
- Never leak raw authority or credential material in decision or trace output.
- Keep the public result type narrow, typed, and stable so later orchestration layers cannot reinterpret branch semantics ad hoc.
- Ensure the orchestrator is safe under idempotent retries and concurrent duplicate requests.

Validation rules:
- Unit-test decision precedence, typed reason mapping, and full candidate evaluation coverage.
- Integration-test the end-to-end decision paths using persisted manifest fixtures from prior cards.
- Validate emitted decision and trace payloads against the imported schemas.
- Prove that replay/recovery/supersession guards cannot be bypassed through the orchestrator.
- Prove that two identical requests against the same manifest history yield the same selected action and candidate evaluation set.

Definition of done:
- The repo has one authoritative manifest decision orchestrator that composes reuse, replay, recovery, continuation, and supersession semantics.
- Later cards can call one service boundary instead of reconstructing branch logic from scattered helpers.
- Decision and trace semantics become stable, typed, and auditable across the manifest pipeline.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
