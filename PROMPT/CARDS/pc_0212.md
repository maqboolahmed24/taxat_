---
id: pc_0212
slug: phase_04_parallel_wave_02_track_backend_retention_security_observability_004_implement_security_runtime_hardening_guards_for_network_headers_cors_and_rate_limits
mode: parallel
phase: "04"
wave: "02"
track: "backend_retention_security_observability"
task_number: "004"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_retention_security_observability_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0212

## Task Summary

004 implement security runtime hardening guards for network headers cors and rate limits.

## Metadata

- ID: `pc_0212`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend retention security observability`
- Task Number: `004`
- Source Slug: `phase_04_parallel_wave_02_track_backend_retention_security_observability_004_implement_security_runtime_hardening_guards_for_network_headers_cors_and_rate_limits`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0211](./pc_0211.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_003_implement_secret_version_model_rotation_audit_and_attestation_rules`
- Next: [pc_0213](./pc_0213.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_005_implement_authority_ingress_correlation_proof_and_reconciliation_control_contract_binding`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0206_to_0213.md](../shared_operating_contract_0206_to_0213.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_retention_security_observability_004_implement_security_runtime_hardening_guards_for_network_headers_cors_and_rate_limits`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0211` if they exist, especially `pc_0039` through `pc_0041`, `pc_0054` through `pc_0057`, `pc_0094` through `pc_0101`, `pc_0158` through `pc_0169`, and `pc_0174` through `pc_0181`.

Mission:
Implement the runtime hardening guard layer for HTTP security headers, CSP, CORS, anti-clickjacking, anti-content-sniffing, safe download/export posture, deep-link/import origin validation, and per-session/per-principal command rate limiting.
This task is not allowed to scatter security headers or origin/rate-limit policy piecemeal across route handlers.
The runtime boundary must become explicit, typed, centrally enforced, and consistent with the algorithm’s session, stale-view, masking, and authority-security law.

Source algorithm grounding you must honor:
- `security_and_runtime_hardening_contract.md`, especially browser/native/API/transport hardening, service-to-service/network hardening, and security invariants.
- `northbound_api_and_session_contract.md`, especially session, stale-view, idempotency, masking, and route-family posture.
- `data_model.md` and earlier session/access cards where they define the persisted session/access basis you must not bypass.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially duplicate-submit, origin-validation, and export/download safety cases.
- any existing route contract or edge/cache law from earlier cards that you must preserve.

Deliverables to create:
1. `packages/backend-security/src/services/build_runtime_hardening_policy.ts`
2. `packages/backend-security/src/services/apply_http_security_headers.ts`
3. `packages/backend-security/src/services/validate_cors_origin.ts`
4. `packages/backend-security/src/services/enforce_command_rate_limits.ts`
5. `packages/backend-security/src/services/validate_deep_link_and_upload_origin.ts`
6. `apps/api/src/http/register_runtime_hardening_guards.ts`
7. `packages/backend-security/src/tests/runtime_hardening_headers.spec.ts`
8. `packages/backend-security/src/tests/cors_and_rate_limits.spec.ts`
9. `docs/security/security_runtime_hardening_guards_for_network_headers_cors_and_rate_limits.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/security/runtime-hardening-atlas.tsx`

Required design decisions you must make explicitly:
- define the canonical runtime-hardening policy object and where route families inherit or override it
- define the exact security-header set, CSP posture, `frame-ancestors` posture, anti-clickjacking posture, anti-content-sniffing posture, and safe download/export posture
- define the CORS allowlist resolution model, credential posture, and preflight handling
- define per-session and per-principal rate-limit dimensions and the stricter profiles for approval, step-up, and transmit actions
- define origin/type/tenant validation for deep links, drag/drop intake, imported files, and embedded web content

Execution requirements:
- centralize runtime hardening into one reusable policy + registration layer rather than ad hoc middleware fragments
- enforce deny-by-default CSP posture and `frame-ancestors 'none'` unless an approved embedding model is explicitly registered
- enforce restrictive CORS with explicit origin allowlists only where cross-origin access is truly required; do not use wildcard origins with credentials
- apply anti-clickjacking, anti-content-sniffing, and safe download/export headers to the appropriate route families
- preserve secure cookie posture where cookies are used and ensure browser-origin write actions continue to require authenticated session plus anti-CSRF protection
- enforce command rate limits per session and per principal, especially on approval, step-up, and transmit endpoints
- validate deep links, drag/drop intake, imported files, and embedded web content for origin, type, and tenant binding before any state transition or artifact adoption
- preserve stale-view and idempotency replay windows so accidental or malicious duplicate submits remain suppressed

Gap handling you must close while implementing:
- close any gap where headers or CORS policy differ by route because one handler forgot to opt in
- close any gap where CSP exceptions can be added without explicit registry and documentation
- close any gap where rate limiting keys only on IP address and ignores session/principal identity
- close any gap where export/download endpoints can bypass masking or safe delivery posture through a direct object-store URL pattern
- close any gap where browser and native embodiments diverge from the same hardening policy without explicit approved variance

Mandatory edge cases:
- preflight requests must not accidentally grant broader origin access than the main request would allow
- credentialed CORS requests must reject wildcard-origin posture
- approval, step-up, and transmit routes must hit stricter command-rate profiles than ordinary reads
- download/export responses must receive safe delivery headers and must not bypass masking policy
- deep links or imports with wrong tenant binding must fail closed before adoption
- stale-view or duplicate-submit suppression must continue to work after the hardening layer is inserted

Playwright and contract-test requirements:
- add deterministic tests for security-header emission, CSP posture, blocked framing, blocked disallowed origins, allowed explicit origins, rate-limit exhaustion, and safe export/download headers
- add Playwright `APIRequestContext` coverage for preflight/main request behavior, blocked origins, and command-rate-limit responses
- if you add the optional atlas, add Playwright browser tests that verify the hardening-policy cards, keyboard navigation, and reduced-motion-safe section expansion using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser atlas or real route patch is touched:
- use a three-zone layout: hardening-policy ribbon, route-family policy atlas canvas, narrow exception/override sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; accent `#0F766E`; info accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- show explicit policy values and exception registries as ordered cards or matrices, not as marketing-style security score widgets
- animate only opacity/translate/height with `140ms` to `180ms`; respect reduced motion

Backend, performance, and security requirements:
- keep the hardening layer cheap, deterministic, and safe under high request concurrency
- centralize policy resolution so route handlers cannot silently drift
- do not log raw secrets, tokens, or unredacted sensitive payloads while emitting blocked-origin or rate-limit errors
- preserve compatibility with existing stale-view, idempotency, masking, and tenant-binding semantics

Validation rules:
- add explicit automated coverage for headers, CORS, and rate-limit posture
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one canonical runtime hardening registration path and one canonical policy object for headers/origin/rate-limit behavior
- blocked, allowed, and exception paths are explicit and test-covered
- optional atlases, if added, remain read-only projections of policy truth rather than control surfaces
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
