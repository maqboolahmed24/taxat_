---
id: pc_0144
slug: phase_03_parallel_wave_01_track_backend_authority_012_implement_authority_reconciliation_analytics_snapshot_and_operator_insight_queries
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "012"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_012_implement_authority_reconciliation_analytics_snapshot_and_operator_insight_queries
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0144

## Task Summary

authority reconciliation analytics snapshot and operator insight queries.

## Metadata

- ID: `pc_0144`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `012`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_012_implement_authority_reconciliation_analytics_snapshot_and_operator_insight_queries`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0143](./pc_0143.md) `phase_03_parallel_wave_01_track_backend_authority_011_implement_filing_notice_step_and_resolution_state_handling`
- Next: [pc_0145](./pc_0145.md) `phase_03_parallel_wave_01_track_backend_workflow_001_implement_workflow_item_aggregate_and_state_machine_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0142_to_0149.md](../shared_operating_contract_0142_to_0149.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_012_implement_authority_reconciliation_analytics_snapshot_and_operator_insight_queries`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0143` if they exist, especially `pc_0034`, `pc_0054`, `pc_0055`, `pc_0061`, `pc_0074`, `pc_0086` through `pc_0093`, `pc_0136`, `pc_0138`, and `pc_0140`.

Mission:
Implement the replay-safe reconciliation analytics layer and the operator insight query surface that sits on top of it.
This card must aggregate durable reconciliation-control truth into `AuthorityReconciliationAnalyticsSnapshot` and expose stable query APIs / read models for operators without letting logs, retry counters, or browser heuristics masquerade as authoritative analytics inputs.

Source algorithm grounding you must honor:
- `authority_interaction_protocol.md`, especially `9.13A` and `9.13B`, including the rule that analytics derive from persisted grouped reconciliation-control contracts, not from retry logs.
- `data_model.md`, especially `AuthorityReconciliationAnalyticsSnapshot` and adjacent authority interaction / submission / workflow linkage.
- `observability_and_audit_contract.md`, especially the operational metrics and query-contract guidance.
- `admin_governance_console_architecture.md`, especially internal operator workspace posture and density rules.
- `low_noise_experience_contract.md`, `frontend_shell_and_interaction_law.md`, and the semantic selector / accessibility contracts.
- `PATCH_RESOLUTION_INDEX.md` and `AUDIT_FINDINGS.md` where reconciliation lineage, resend refusal, escalation timing, or audit coverage issues are discussed.
- `test_vectors.md`, especially reconciliation, escalation, ambiguity, and replay-resume vectors.

Deliverables to create:
Backend:
1. `packages/backend-authority/src/models/authority_reconciliation_analytics_snapshot.ts`
2. `packages/backend-authority/src/repositories/authority_reconciliation_analytics_snapshot_repository.ts`
3. `packages/backend-authority/src/services/build_authority_reconciliation_analytics_snapshot.ts`
4. `packages/backend-authority/src/services/query_reconciliation_snapshots_by_profile.ts`
5. `packages/backend-authority/src/services/query_reconciliation_hotspots.ts`
6. `packages/backend-authority/src/services/query_reconciliation_resume_and_restore_posture.ts`
7. `packages/backend-authority/src/services/derive_reconciliation_tuning_recommendations.ts`
8. `packages/backend-authority/src/services/project_reconciliation_insight_view_model.ts`
9. `packages/backend-authority/src/services/project_reconciliation_reason_matrix.ts`

Internal browser surface:
10. `apps/admin-console-web/src/routes/operations/reconciliation-insights.tsx`
11. `apps/admin-console-web/src/components/reconciliation/ReconciliationProfileHeader.tsx`
12. `apps/admin-console-web/src/components/reconciliation/ReconciliationBudgetBand.tsx`
13. `apps/admin-console-web/src/components/reconciliation/ReasonMatrixTable.tsx`
14. `apps/admin-console-web/src/components/reconciliation/ResumeAndEscalationStrip.tsx`
15. `apps/admin-console-web/src/components/reconciliation/ReconciliationWindowQueryPanel.tsx`
16. `tests/playwright/admin_console/reconciliation_insights.spec.ts`
17. `tests/playwright/admin_console/reconciliation_insights_a11y.spec.ts`

Docs / persistence:
18. `db/migrations/phase03_0144_authority_reconciliation_analytics_snapshot.sql`
19. `docs/authority/reconciliation_analytics_snapshot_and_operator_queries.md`
20. `tests/unit/backend_authority/authority_reconciliation_analytics_snapshot_contract.spec.ts`
21. `tests/integration/backend_authority/reconciliation_analytics_aggregation.spec.ts`

Required design decisions you must make explicit:
- Define the snapshot windowing policy:
  exact window start / end, inclusion of only lineage-deduplicated authoritative interactions, and how superseded / replayed / resumed interactions contribute.
- Honor the schema’s source-of-truth rule:
  `source_policy` is `DURABLE_RECONCILIATION_CONTROL_CONTRACTS_ONLY`.
  Do not fold retry-worker telemetry, raw broker redeliveries, or transient logs into snapshot authority.
- Define the exact derivation of:
  `budget_state_counts`,
  `outcome_class_counts`,
  `resend_refusal_reason_counts`,
  `escalation_reason_counts`,
  `unresolved_ambiguity_count`,
  `deadline_expiry_count`,
  `replay_resume_count`,
  `average_attempts_consumed`,
  `max_attempts_consumed`,
  `escalation_latency_seconds_p95_or_null`,
  `tuning_recommendation_codes[]`.
- Define the operator query contract:
  by provider environment, operation family, profile, time window, unresolved-only, escalation-only, resume-heavy, or ambiguity-heavy slices.
- Define how the read-only operator surface consumes snapshots and query slices without becoming the source of truth itself.

Execution steps:
1. Implement the schema-backed model / repository for `AuthorityReconciliationAnalyticsSnapshot`.
2. Build a snapshot assembler that reads persisted `AuthorityInteractionRecord.reconciliation_control_contract{...}` lineage and deduplicates by exact interaction meaning before aggregation.
3. Implement explicit hotspot queries:
   - highest ambiguity profiles
   - highest resend-refusal clusters
   - highest escalation-latency clusters
   - highest replay-resume frequency clusters
4. Implement deterministic tuning recommendation derivation from persisted metrics only.
   Recommendation codes must be explainable from the persisted snapshot and query result, not from human guesswork.
5. Build one read-only internal operator surface that lets staff inspect one profile / window at a time without turning the page into a dashboard wall.
6. Use Playwright from the start to verify filters, query panel interactions, chart / table rendering, empty states, and accessibility.

Gap handling you must perform:
- Close the gap where reconciliation analytics could otherwise depend on logs or retry-worker counters instead of durable contracts.
- Close the gap where operators can see transport noise but not the actual budget / escalation economics defined by the protocol.
- Close the gap where tuning recommendations are implicit in prose but not emitted as durable, queryable codes.
- Close the gap where the internal surface could become a generic metric board instead of a precise explanation tool for one chosen profile / window.

Mandatory edge cases:
- zero-interaction snapshot window
- profile with escalations but no terminal outcomes yet
- profile with high ambiguity and blocked resend but zero duplicate sends
- replay / restore heavy profile where replay-resume count is materially high but success rate is acceptable
- mixed window containing superseded or deduplicated interaction lineage
- very small sample size where p95 latency is null or must be explicitly marked low confidence

Premium UI and interaction specification you must follow:
- Surface posture: premium internal instrument, calm and investigative, not a wall of KPI tiles
- Background: `#F7F5F0`
- Surface cards: `#FFFFFF`
- Primary text: `#171717`
- Secondary text: `#667085`
- Accent: `#0F766E`
- Info accent: `#1D4ED8`
- Warning: `#B7791F`
- Danger: `#C2410C`
- Max width: `1440px`
- Layout:
  - top profile header with environment / operation family / window context
  - one dominant insights column (`minmax(0, 1fr)`)
  - one restrained query rail (`320px` to `360px`) for filters and saved query presets
  - below the header, use a vertical stack:
    1. dominant insight summary
    2. budget state band
    3. reason matrix
    4. resume / escalation strip
    5. supporting drill table
- Charts / diagrams:
  - `ReconciliationBudgetBand`: horizontal segmented band or stacked strip only
  - `ReasonMatrixTable`: ordered numeric table with restrained sparklines if useful
  - `ResumeAndEscalationStrip`: one slim trend strip or interval band; no oversized chart
  - no donut charts, no gauge charts, no twelve-tile executive dashboard
- Motion:
  - only panel disclosure / filter-apply transitions
  - `140ms` to `180ms`
  - reduced-motion safe
- Empty state:
  - calm explanatory copy plus active filter echo
  - no false-zero celebratory state

Semantic selectors and accessibility requirements:
- add stable semantic hooks / test ids for at least:
  `reconciliation-profile-header`
  `reconciliation-query-panel`
  `reconciliation-dominant-insight`
  `reconciliation-budget-band`
  `reconciliation-reason-matrix`
  `reconciliation-resume-escalation-strip`
  `reconciliation-drill-table`
  `reconciliation-window-filter`
  `reconciliation-empty-state`
- prefer role / label / accessible-name locators in Playwright
- expose explicit headings and landmarks
- tables must have accessible headers and summary captions
- filter application must preserve focus lawfully

Playwright requirements:
- use Playwright during implementation
- semantic locators only
- no fixed sleeps
- traces and screenshots on retry / failure
- verify desktop and compact widths
- verify that drill-down preserves the same route / shell rather than causing a hard navigation reset

Backend, performance, and security requirements:
- fail closed if a snapshot tries to aggregate sources other than persisted reconciliation-control contracts
- support indexed window / profile lookup without full log scans
- treat snapshots as replay-safe derived artifacts with explicit source-policy echo
- do not leak secrets, raw response payloads, or customer-sensitive data into the internal analytics surface
- preserve audit lineage for snapshot generation and operator query execution where the existing audit spine allows it

Validation rules:
- validate each snapshot against `schemas/authority_reconciliation_analytics_snapshot.schema.json`
- prove that hotspot queries and operator route projections remain consistent with the persisted snapshot
- integration-test aggregation under escalated, blocked, unresolved, deadline-expired, and replay-resume-heavy datasets
- Playwright-test filters, charts / tables, empty states, and accessibility landmarks

Definition of done:
- the repo contains a durable reconciliation analytics snapshot model and repeatable operator query layer
- operators can inspect reconciliation economics without reading raw logs
- the internal surface remains calm, precise, and clearly subordinate to the persisted snapshot truth
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
