---
id: pc_0136
slug: phase_03_parallel_wave_01_track_backend_authority_004_implement_authority_request_identity_idempotency_and_duplicate_meaning_key_logic
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "004"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0136

## Task Summary

implement authority request identity idempotency and duplicate meaning key logic.

## Metadata

- ID: `pc_0136`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `004`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_004_implement_authority_request_identity_idempotency_and_duplicate_meaning_key_logic`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0135](./pc_0135.md) `phase_03_parallel_wave_01_track_backend_authority_003_implement_authority_operation_binding_request_and_response_envelope_models`
- Next: [pc_0137](./pc_0137.md) `phase_03_parallel_wave_01_track_backend_authority_005_implement_hmrc_oauth_token_client_binding_and_send_time_revalidation_service`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0134_to_0141.md](../shared_operating_contract_0134_to_0141.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_004_implement_authority_request_identity_idempotency_and_duplicate_meaning_key_logic`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0135` if they exist, especially `pc_0058`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0067`, `pc_0068`, `pc_0069`, `pc_0074`, `pc_0076`, `pc_0086` through `pc_0093`, `pc_0097`, `pc_0102`, `pc_0108`, and `pc_0135`.

Mission:
Implement the exact request-identity, idempotency, and duplicate-meaning logic for authority traffic.
This card must make the hash lineage from canonical request material to `identity_namespace_hash`, `duplicate_meaning_key`, `request_hash`, and request-level `idempotency_key` deterministic, collision-aware, and reusable across transport, recovery, ingress correlation, and request-backed settlement.

Source algorithm grounding you must honor:
- `authority_interaction_protocol.md`, especially section `9.8 Request hashing and idempotency` and the surrounding rules on duplicate buckets and request identity.
- `modules.md`, especially `DERIVE_AUTHORITY_REQUEST_HASHES(...)`, `BUILD_AUTHORITY_REQUEST_ENVELOPE(...)`, duplicate/pending collision checks, and request-backed recovery / reconciliation helpers.
- `data_model.md`, especially `AuthorityRequestEnvelope`, `AuthorityIngressReceipt`, `AuthorityInteractionRecord`, and `SubmissionRecord` fields that reuse `request_hash`, `idempotency_key`, `identity_namespace_hash`, `duplicate_meaning_key`, and `request_identity_contract{...}`.
- `state_machines.md`, especially rules around duplicate-bucket consultation before send and stronger-truth blocking.
- `PATCH_RESOLUTION_INDEX.md`, including the calculation and ordering patches that reinforce deterministic arrays and explicit request identity.
- `test_vectors.md`, especially the vectors around duplicate vs exact identity, ambiguous ingress, and authority recovery continuity.

Deliverables to create:
1. `packages/backend-authority/src/services/derive_authority_request_hashes.ts`
2. `packages/backend-authority/src/services/build_request_identity_contract.ts`
3. `packages/backend-authority/src/services/normalize_authority_request_identity_inputs.ts`
4. `packages/backend-authority/src/services/assert_authority_request_identity_stability.ts`
5. `packages/backend-authority/src/services/detect_duplicate_meaning_conflict.ts`
6. `packages/backend-authority/src/services/detect_request_identity_collision.ts`
7. `packages/backend-authority/src/services/resolve_authority_duplicate_bucket.ts`
8. `packages/backend-authority/src/repositories/authority_request_identity_lookup_repository.ts`
9. `db/migrations/phase03_0136_authority_request_identity_and_duplicate_bucket.sql`
10. `docs/authority/authority_request_identity_idempotency_and_duplicate_meaning_logic.md`
11. `tests/unit/backend_authority/request_identity_hash_derivation.spec.ts`
12. `tests/unit/backend_authority/duplicate_meaning_and_collision_rules.spec.ts`
13. `tests/integration/backend_authority/request_identity_replay_and_collision_flow.spec.ts`

Required design decisions you must make explicit:
- Define the canonical normalization algorithm for every input to the hash chain:
  ordered header profile refs,
  ordered business partitions,
  canonical path,
  canonical query,
  canonical body bytes,
  normalized obligation ref,
  normalized basis type,
  delegation grant sentinel,
  null-body sentinel,
  and any required Unicode normalization.
- Define the exact boundaries between:
  `identity_namespace_hash`,
  `duplicate_meaning_key`,
  `request_hash`,
  and request-level `idempotency_key`.
- Define the collision/error taxonomy for:
  exact request collision,
  duplicate-meaning collision,
  namespace collision,
  body mismatch under same duplicate bucket,
  conflicting access-binding hash,
  and stale duplicate bucket after stronger external truth appears.
- Define the exact persisted grouped `request_identity_contract{...}` and prohibit downstream code from rebuilding it out of neighboring models.

Execution steps:
1. Implement `normalize_authority_request_identity_inputs.ts` so every array, sentinel, and canonical string is normalized before hashing.
2. Implement `derive_authority_request_hashes.ts` exactly from the algorithm:
   - `identity_profile_version = AUTHORITY_REQUEST_IDENTITY_V2`
   - derive `attempt_lineage_manifest_id`
   - derive `identity_namespace_hash`
   - derive `duplicate_meaning_key`
   - derive `request_hash`
   - derive request-level `idempotency_key`
3. Implement `build_request_identity_contract.ts` so all load-bearing request identity fields are persisted together in one byte-stable grouped contract.
4. Implement duplicate-bucket lookup and conflict detection helpers that make it impossible to confuse:
   “same exact request”
   vs
   “same business meaning but different exact request”
   vs
   “same namespace shape but materially different operation”
5. Add repository/index support for fast lookup by `duplicate_meaning_key`, `request_hash`, `idempotency_key`, and `identity_namespace_hash`.
6. Ensure every downstream caller reuses the persisted grouped contract rather than recalculating from partial operation / binding state.

Gap handling you must perform:
- Close the gap where a request-level idempotency key could be mistaken for the manifest-level idempotency key.
- Close the gap where caller-local ordering differences could change duplicate bucket identity.
- Close the gap where live transport, callback normalization, or settlement code could rebuild request identity from loose neighboring refs instead of from the grouped contract.
- Close the gap where exact request identity and duplicate-meaning identity are conflated.
- Close the gap where lawful within-lineage token rotation or recovery replay might accidentally widen into a different namespace or exact request identity.

Mandatory edge cases:
- `identity_profile_version` must remain fixed at `AUTHORITY_REQUEST_IDENTITY_V2`
- empty business partition scope must normalize to `["<NONE>"]` only when the operation family lawfully allows global/account-level read posture
- missing `obligation_ref` or `basis_type` must normalize to explicit sentinels, not omitted fields
- `duplicate_meaning_key` must remain stable across lawful transport replay and within-lineage token refresh when executable meaning is unchanged
- `request_hash` must change when ordered header profile refs, token binding ref, authority binding ref, authority link ref, delegation grant ref, subject/acting-party tuple, or policy snapshot hash materially change
- request-level `idempotency_key` must be derived from `duplicate_meaning_key`, not invented independently
- same duplicate bucket with a materially different request body must fail closed as a collision / conflict
- same exact request hash under a different namespace, subject, or access-binding posture must fail closed
- downstream request-backed settlement and ingress correlation must reuse the persisted `request_identity_contract{...}` instead of reconstructing it

Backend, performance, and security requirements:
- use only deterministic serializers and hash helpers already established earlier in the roadmap
- fail closed on missing canonicalized inputs, illegal empty sentinels, or any attempt to hash raw unordered structures
- store indexes that support hot-path duplicate consultation and exact request lookup without full-table scans
- do not expose raw payload bytes through lookup APIs when only hashes are required
- preserve cross-language compatibility for future bindings and replay

Validation rules:
- unit-test every element of the exact hash formulas and every normalization rule
- property-test ordering invariance where appropriate
- integration-test replay, crash/reload, lawful token rotation within lineage, duplicate-bucket conflict, and stale stronger-truth blocks
- prove that the same logical request input yields byte-identical grouped request identity output across runs
- prove that malformed or ambiguous inputs are rejected before authority transport or settlement code can proceed

Definition of done:
- the repo contains one exact, deterministic implementation of authority request identity and duplicate bucket semantics
- downstream authority transport, ingress, reconciliation, and settlement services can rely on a persisted grouped request identity contract
- exact request identity and business-meaning identity are clearly separated and collision-safe
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
