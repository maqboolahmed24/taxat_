---
id: pc_0133
slug: phase_03_parallel_wave_01_track_backend_authority_001_implement_obligation_mirror_filing_case_and_filing_packet_models
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "001"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0133

## Task Summary

implement obligation mirror, filing case, and filing packet models.

## Metadata

- ID: `pc_0133`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `001`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_001_implement_obligation_mirror_filing_case_and_filing_packet_models`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0132](./pc_0132.md) `phase_03_parallel_wave_01_track_backend_compute_012_implement_twin_reconciliation_state_interpretation_state_and_portfolio_summary_services`
- Next: [pc_0134](./pc_0134.md) `phase_03_parallel_wave_01_track_backend_authority_002_implement_submission_record_lifecycle_and_authority_truth_projection`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0126_to_0133.md](../shared_operating_contract_0126_to_0133.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_001_implement_obligation_mirror_filing_case_and_filing_packet_models`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0132` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0067`, `pc_0069`, `pc_0074`, `pc_0076`, `pc_0086` through `pc_0093`, `pc_0097`, `pc_0108`, `pc_0126`, `pc_0127`, `pc_0129`, and `pc_0131` through `pc_0132`.

Mission:
Implement the first authority-domain foundation models and guarded builders for `ObligationMirror`, `FilingCase`, and `FilingPacket`.
This task must establish strict schema-backed persistence, legal state transitions, readiness and proof lineage binding, and authority-truth separation without prematurely implementing live submission transport, `SubmissionRecord` lifecycle, request-envelope generation, callback normalization, or settlement projection.

Source algorithm grounding you must honor:
- `data_model.md`, especially `ObligationMirror`, `FilingCase`, `FilingPacket`, and the nearby `SubmissionRecord` description used to understand what must *not* be absorbed here.
- `state_machines.md`, especially `ObligationMirror.lifecycle_state`, `FilingPacket.lifecycle_state`, `FilingCase.lifecycle_state`, and cross-state invariants around filing packet / submission / case interplay.
- `modules.md`, especially `UPSERT_OBLIGATION_MIRROR(...)`, `UPSERT_FILING_CASE(...)`, `BUILD_FILING_PACKET(...)`, and the explicit later separation of `BEGIN_SUBMISSION_RECORD(...)`.
- `authority_truth_and_internal_projection_separation_contract.md`.
- `authority_interaction_protocol.md`.
- `authority_calculation_contract.md`, especially readiness-context and calculation-lineage persistence rules.
- `PATCH_RESOLUTION_INDEX.md`, especially fixes around `ready_manifest_ref`, `current_submission_ref`, `last_confirmed_submission_ref`, packet-phase legality, and packet proof lineage.
- `test_vectors.md`, especially any authority truth, ready-to-file, packet preparation, or settlement-boundary vectors relevant to these models.
- Existing schemas:
  `obligation_mirror.schema.json`,
  `filing_case.schema.json`,
  `filing_packet.schema.json`,
  and `submission_record.schema.json` for boundary awareness only.

Deliverables to create:
1. `packages/backend-authority/src/models/obligation_mirror.ts`.
2. `packages/backend-authority/src/models/filing_case.ts`.
3. `packages/backend-authority/src/models/filing_packet.ts`.
4. `packages/backend-authority/src/repositories/obligation_mirror_repository.ts`.
5. `packages/backend-authority/src/repositories/filing_case_repository.ts`.
6. `packages/backend-authority/src/repositories/filing_packet_repository.ts`.
7. `packages/backend-authority/src/services/transition_obligation_mirror.ts`.
8. `packages/backend-authority/src/services/transition_filing_case.ts`.
9. `packages/backend-authority/src/services/transition_filing_packet.ts`.
10. `packages/backend-authority/src/services/upsert_obligation_mirror.ts`.
11. `packages/backend-authority/src/services/upsert_filing_case.ts`.
12. `packages/backend-authority/src/services/build_filing_packet.ts`.
13. `packages/backend-authority/src/services/validate_authority_domain_refs.ts`.
14. `db/migrations/phase03_0133_obligation_mirror_filing_case_and_packet_foundation.sql`.
15. `docs/authority/obligation_mirror_filing_case_and_filing_packet_models.md`.
16. `tests/unit/backend_authority/obligation_mirror_contract.spec.ts`.
17. `tests/unit/backend_authority/filing_case_and_packet_state_rules.spec.ts`.
18. `tests/integration/backend_authority/foundation_authority_domain_model_flow.spec.ts`.

Required design decisions you must make explicit:
- Define the exact authority-truth boundary.
  `ObligationMirror` is a normalized internal mirror and may reflect readiness and authority-grounded outcome posture, but it must never replace `SubmissionRecord` as the legal settlement spine.
- Define the ref legality matrix for:
  `ready_manifest_ref`,
  `current_submission_ref`,
  `last_confirmed_submission_ref`,
  `calculation_basis_ref`,
  `authority_calculation_ref`,
  `readiness_context_ref`,
  `controlling_proof_bundle_ref`,
  `filing_gate_ref`,
  `notice_step_refs[]`,
  and `notice_resolution_ref`.
- Define the minimal `FilingCase` identity and readiness-lineage rules so packet construction can bind to persisted calculation and proof posture deterministically.
- Define packet build legality:
  which proof, parity, trust, readiness, approval, declaration-basis, and notice conditions are required for `DRAFT`, `PREPARED`, and `APPROVED_TO_SUBMIT`.
- Define explicit non-goals for this card:
  no live send, no request hashing, no OAuth, no fraud-prevention header generation, no callback / poll normalization, no `SubmissionRecord` lifecycle transitions.

Execution steps:
1. Implement schema-backed models and repositories for obligation mirror, filing case, and filing packet.
2. Implement state transition helpers for each model using the authoritative state-machine rules.
3. Implement `validate_authority_domain_refs.ts` so illegal ref combinations fail closed before persistence.
4. Implement `upsert_obligation_mirror.ts` so the mirror reflects window state, internal readiness, and authority-grounded posture without masquerading as confirmed settlement.
5. Implement `upsert_filing_case.ts` so the case persists calculation refs, readiness context, baseline / authority context, amendment eligibility hooks, and review posture needed before packet build.
6. Implement `build_filing_packet.ts` so packet construction freezes manifest binding, payload hash lineage, calculation basis / authority calculation refs, controlling proof bundle, approval posture, declaration-basis acknowledgement, notice-step posture, and filing-gate linkage.
7. Add tests for obligation window opening / due-soon / ready-to-file / pending / confirmed transitions, packet preparation legality, and separation from later submission-record behavior.

Gap handling you must perform:
- Close the gap where authority-domain models appear before one guarded implementation boundary exists.
  Build the models and transition rules now, but keep live submission and settlement behavior explicitly out of scope.
- Close the gap where internal readiness could masquerade as authority confirmation.
  Reserve `MET_CONFIRMED` and `last_confirmed_submission_ref` for authority-grounded posture only.
- Close the gap where packet legality could be reconstructed loosely from neighboring artifacts.
  Freeze packet-phase approval, declared-basis acknowledgement, notice resolution, proof closure, and filing gate posture on the packet itself.
- Close the gap where readiness context and calculation lineage might be lost between compute and authority domains.
  Persist them on `FilingCase` before packet build and copy the active context into the packet.
- Close the gap where packet / case / mirror refs might violate state-specific legality.
  Enforce ref legality before persistence.

Mandatory edge cases:
- `ready_manifest_ref` must be non-null only in `READY_TO_FILE`.
- `current_submission_ref` must be non-null only in `SUBMITTED_PENDING`.
- `last_confirmed_submission_ref` must be non-null only in `MET_CONFIRMED`.
- the engine may show `READY_TO_FILE` before submission, but not `MET_CONFIRMED`.
- `PREPARED` packets may persist while approval or declaration-basis acknowledgement is still pending, but `APPROVED_TO_SUBMIT` must enforce the stronger packet legality set.
- `APPROVED_TO_SUBMIT` requires trust and parity gates satisfied or a valid override, plus a bound filing gate, `proof_closure_state = CLOSED`, and resolved notice posture where notice steps exist.
- packet content must never mutate in place after `SUBMITTED`.
- no service in this card may create or transition a `SubmissionRecord`; only refs to already-existing submission artifacts may be recognized where the schema allows them.

Backend, performance, and security requirements:
- Keep authority-domain foundation services deterministic and repository-driven.
- Reuse canonical state-transition, schema-validation, and reference-grammar helpers from earlier cards.
- Do not let browser or API callers claim authority confirmation by writing mirror state directly.
- Avoid logging sensitive payload bytes or provider-specific secrets; log ids, lifecycle states, and reason codes only.

Optional internal verification surface (only if you add one):
- If you add a read-only authority-state inspector, place it behind internal routes such as `/internal/authority/obligation/:obligationRef`, `/internal/authority/case/:filingCaseId`, and `/internal/authority/packet/:packetId`.
- The UI must be minimalist premium and intentionally structured:
  top identity bar for obligation / case / packet ids,
  left `300px` state rail for lifecycle and readiness posture,
  central dossier surface for manifest, calculation, proof, notice, and approval lineage,
  right `320px` truth-boundary rail that clearly separates internal readiness, packet legality, and authority-grounded settlement posture.
- Use the shared palette and typography contract, with no playful status widgets.
- Add one restrained state-timeline strip only: a thin horizontal progression with semantic chips for `OPEN`, `READY_TO_FILE`, `SUBMITTED_PENDING`, `MET_CONFIRMED`, packet states, and case posture.
- Motion must stay subtle and reduced-motion safe.
- If this surface exists, implement and verify it with Playwright using semantic locators, keyboard traversal, actionability-safe interactions, reduced-motion assertions, and trace capture.

Validation rules:
- Unit-test lifecycle transitions, ref legality, readiness-context persistence, and packet build legality.
- Integration-test mirror -> case -> packet foundation flow, including authority-confirmed versus merely-ready distinction.
- Validate persisted artifacts against the relevant authority schemas.
- Re-run the contract validators after assembling the implementation bundle.

Definition of done:
- The repo can persist strict `ObligationMirror`, `FilingCase`, and `FilingPacket` artifacts with state-legal refs and lineage.
- Later submission, transport, and authority-reconciliation tasks can build on these foundations without retrofitting truth-boundary or packet-legality semantics.
- Internal readiness, packet legality, and authority-grounded settlement remain machine-separable.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
