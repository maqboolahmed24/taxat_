---
id: pc_0267
slug: phase_05_parallel_wave_03_track_frontend_collaboration_001_Playwright_or_other_appropriate_tooling_implement_work_inbox_layout_filters_sorting_and_focus_stability
mode: parallel
phase: "05"
wave: "03"
track: "frontend_collaboration"
task_number: "001"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_collaboration_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0267

## Task Summary

001 Playwright or other appropriate tooling implement work inbox layout filters sorting and focus stability.

## Metadata

- ID: `pc_0267`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend collaboration`
- Task Number: `001`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_collaboration_001_Playwright_or_other_appropriate_tooling_implement_work_inbox_layout_filters_sorting_and_focus_stability`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0266](./pc_0266.md) `phase_05_parallel_wave_03_track_frontend_portal_014_Playwright_or_other_appropriate_tooling_implement_portal_stale_view_rejection_refresh_and_graceful_rebase_experience`
- Next: [pc_0268](./pc_0268.md) `phase_05_parallel_wave_03_track_frontend_collaboration_002_Playwright_or_other_appropriate_tooling_implement_staff_workspace_shell_and_dual_lane_activity_layout`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_collaboration_001_Playwright_or_other_appropriate_tooling_implement_work_inbox_layout_filters_sorting_and_focus_stability`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0266` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the staff collaboration work inbox layout, filters, sorting, live updates, quick actions, and focus stability using authoritative queue and routing contracts.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The staff inbox is a triage surface, not a generic ticket grid. Rows have three bands—identity, triage signals, and action—split unread badges for customer-visible and internal-only lanes, route-stable filters, live updates in place, and deferred reordering/removal while row focus or action menus are active. Sorting and quick actions must derive from persisted routing/authoritative action contracts, not websocket arrival order or client heuristics.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `collaboration_workspace_contract.md` staff work inbox, core invariants, queue projection, routing formulas, live update and focus stability rules, accessibility/responsive rules, and Playwright scenarios.
- `frontend_shell_and_interaction_law.md` queue/inbox continuity, same-object shell, focus law, automation anchors.
- `northbound_api_and_session_contract.md` collaboration read surfaces, command families, stale-view and stream rules.
- `schemas/collaboration_queue_projection_contract.schema.json`, `collaboration_routing_contract.schema.json`, `workspace_stream_event.schema.json`, `operator_interaction_layer.schema.json`, `semantic_accessibility_contract.schema.json`.

Deliverables to create or patch:
1. `apps/operator-web/src/routes/work/WorkInboxRoute.tsx`
2. `apps/operator-web/src/components/work/WorkInboxShell.tsx`
3. `apps/operator-web/src/components/work/WorkItemRow.tsx`
4. `apps/operator-web/src/components/work/WorkInboxFilterTray.tsx`
5. `apps/operator-web/src/components/work/WorkInboxQuickActions.tsx`
6. `packages/frontend-shell-core/src/collaboration/work_inbox_presenter.ts`
7. `packages/frontend-shell-core/src/collaboration/work_inbox_focus_stability.ts`
8. `tests/unit/frontend-shell-core/work_inbox_presenter.spec.ts`
9. `tests/playwright/frontend/collaboration_work_inbox_focus_stability.spec.ts`
10. `docs/frontend/collaboration_work_inbox.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Build a premium triage list, not a table-first admin grid. Each row is a white card or row slab with three horizontal bands: title/client/period, waiting/due/split unread/escalation signals, then one dominant quick action plus up to two subordinate actions.
- Filters are compact route-stable chips (`mine`, `unassigned`, `escalated`, `waiting on customer`, `overdue`, `blocked`, `resolved recently`). Use a small queue-health/routing reason strip only when it clarifies ordering; avoid KPI tiles. Split unread badges must say `Customer` and `Internal`, not just numbers.

Execution requirements:
- render one row per active workflow item from `WorkInboxSnapshot`/queue projection; do not join arbitrary workspace payloads in the browser
- preserve filter chip route state and authoritative `active_filters` object
- derive row quick actions from the same authoritative action/stale-view basis as the workspace action strip
- apply live deltas in place; if a focused row would reorder or leave the filter, keep a pending reorder/removal placeholder until focus leaves or action completes
- selecting a row transitions into `/work/items/{item_id}` inside the same SPA shell and restores row focus on return

Functional, architectural, and edge-case gaps to close:
- close the gap where customer/internal unread counts are blended
- close the gap where live updates reorder rows under keyboard focus
- close the gap where quick actions drift from workspace legality
- close the gap where sorting is inferred from websocket arrival order instead of routing contract

Mandatory edge cases:
- focused row changes sort rank or leaves active filter
- assignment/escalation/due update while action menu open
- split unread changes only in hidden/non-expanded lane
- stale quick action from cached row menu
- filter chips on narrow screen
- return from workspace restores row focus

Playwright, accessibility, and validation requirements:
- unit-test row band derivation, filter membership, sort/deferred reorder, split unread, quick action legality, and return focus anchors
- Playwright-test filter keyboard navigation, live row update, pending reorder while focused, action menu stale rejection, row open/return focus, responsive list, reduced motion, and semantic selectors like `work-item-row-{item_id}`
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- work inbox is a contract-bound triage surface with stable focus
- routing/quick actions are authoritative and lane-aware
- Playwright proves filters, live updates, focus stability, and same-shell row navigation
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
