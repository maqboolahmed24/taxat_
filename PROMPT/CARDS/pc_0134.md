---
id: pc_0134
slug: phase_03_parallel_wave_01_track_backend_authority_002_implement_submission_record_lifecycle_and_authority_truth_projection
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "002"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0134

## Task Summary

implement submission record lifecycle and authority truth projection.

## Metadata

- ID: `pc_0134`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `002`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_002_implement_submission_record_lifecycle_and_authority_truth_projection`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0133](./pc_0133.md) `phase_03_parallel_wave_01_track_backend_authority_001_implement_obligation_mirror_filing_case_and_filing_packet_models`
- Next: [pc_0135](./pc_0135.md) `phase_03_parallel_wave_01_track_backend_authority_003_implement_authority_operation_binding_request_and_response_envelope_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0134_to_0141.md](../shared_operating_contract_0134_to_0141.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_002_implement_submission_record_lifecycle_and_authority_truth_projection`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0133` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0050`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0067`, `pc_0068`, `pc_0069`, `pc_0074`, `pc_0076`, `pc_0086` through `pc_0093`, `pc_0097`, `pc_0102` through `pc_0109`, `pc_0118` through `pc_0127`, and `pc_0133`.

Mission:
Implement the full `SubmissionRecord` lifecycle, persistence model, transition guardrail layer, and authority-truth projection services.
This card must make `SubmissionRecord` the durable legal settlement spine for one authority meaning and must guarantee that mirror, workflow, and client-safe projections stay subordinate to that truth rather than inventing settlement from internal readiness or transport observations.

Source algorithm grounding you must honor:
- `data_model.md`, especially `SubmissionRecord`, `AuthorityInteractionRecord`, `AuthorityIngressReceipt`, and the nearby grouped contracts on request identity, ingress proof, reconciliation control, and authority truth.
- `state_machines.md`, especially the `SubmissionRecord` state machine, the `ObligationMirror` state rules, and the interaction between `current_submission_ref`, `last_confirmed_submission_ref`, `baseline_type`, `reconciliation_deadline_at`, and supersession.
- `modules.md`, especially `BEGIN_SUBMISSION_RECORD(...)`, `TRANSITION_SUBMISSION_RECORD(...)`, `RECONCILE_AUTHORITY_STATE(...)`, and `UPSERT_OBLIGATION_MIRROR(...)`.
- `authority_truth_and_internal_projection_separation_contract.md`.
- `authority_interaction_protocol.md`, especially the sections that distinguish settlement truth from ingress/runtime truth and the rules for `PENDING_ACK`, `UNKNOWN`, `OUT_OF_BAND`, and late authority correction.
- `PATCH_RESOLUTION_INDEX.md`, especially the entries that corrected stale `reconciliation_deadline_at`, missing `REJECTED` authority evidence, non-authoritative `baseline_type`, and `INTENT_RECORDED` packet-lineage omissions.
- `test_vectors.md`, especially `TV-70I` through `TV-70N` and the vectors that cover unresolved, confirmed, rejected, late-corrected, and out-of-band authority truth.

Deliverables to create:
1. `packages/backend-authority/src/models/submission_record.ts`
2. `packages/backend-authority/src/repositories/submission_record_repository.ts`
3. `packages/backend-authority/src/services/validate_submission_record_transition.ts`
4. `packages/backend-authority/src/services/begin_submission_record.ts`
5. `packages/backend-authority/src/services/transition_submission_record.ts`
6. `packages/backend-authority/src/services/build_submission_authority_truth_contract.ts`
7. `packages/backend-authority/src/services/project_submission_truth_to_obligation_mirror.ts`
8. `packages/backend-authority/src/services/project_submission_truth_to_workflow.ts`
9. `packages/backend-authority/src/services/project_submission_truth_to_client_timeline.ts`
10. `packages/backend-authority/src/services/project_authority_truth_bundle.ts` or an equivalent orchestration helper that updates all projections from one authoritative submission mutation
11. `packages/backend-authority/src/services/classify_submission_truth_mutation_gate.ts`
12. `db/migrations/phase03_0134_submission_record_lifecycle_and_truth_projection.sql`
13. `docs/authority/submission_record_lifecycle_and_truth_projection.md`
14. `tests/unit/backend_authority/submission_record_contract.spec.ts`
15. `tests/unit/backend_authority/submission_record_transition_rules.spec.ts`
16. `tests/integration/backend_authority/submission_truth_projection_flow.spec.ts`

Required design decisions you must make explicit:
- Define the authoritative boundary: `SubmissionRecord` is the settlement ledger; `AuthorityInteractionRecord` is runtime exchange truth; `AuthorityIngressReceipt` is ingress checkpoint truth; `ObligationMirror`, workflow items, and client timeline events are projections only.
- Define the complete transition matrix for:
  `INTENT_RECORDED`,
  `TRANSMIT_PENDING`,
  `TRANSMITTED`,
  `PENDING_ACK`,
  `CONFIRMED`,
  `REJECTED`,
  `UNKNOWN`,
  `OUT_OF_BAND`,
  `SUPERSEDED`.
- Define which refs and grouped contracts are mandatory, optional, or forbidden in each state:
  `packet_ref`,
  `request_envelope_ref`,
  `request_hash`,
  `idempotency_key`,
  `identity_namespace_hash`,
  `duplicate_meaning_key`,
  `request_identity_contract{...}`,
  `proof_bundle_ref`,
  `proof_bundle_hash`,
  `authority_interaction_ref`,
  `authority_evidence_ref`,
  `authority_ingress_proof_contract{...}`,
  `reconciliation_control_contract_or_null{...}`,
  `baseline_type`,
  `reconciliation_deadline_at`,
  `superseded_by_submission_id`.
- Define the projection policy from submission truth to:
  - `ObligationMirror.authority_truth_state`
  - `ObligationMirror.current_submission_ref`
  - `ObligationMirror.last_confirmed_submission_ref`
  - any workflow waiting/blocked/resolved posture
  - any client-safe timeline headline or settlement label
- Define how late corrections reopen projections without rewriting historical submission lineage.

Execution steps:
1. Implement the schema-backed `SubmissionRecord` model and repository with canonical serialization, strict schema validation, and durable indexing by manifest, client, obligation scope, duplicate meaning, and active supersession lineage.
2. Implement `begin_submission_record.ts` to create the initial settlement object from a packet / request-envelope intent without allowing a submission intent to exist detached from packet or proof lineage.
3. Implement `validate_submission_record_transition.ts` and `transition_submission_record.ts` so every lifecycle change is explicit, monotonic, and schema-valid before persistence.
4. Implement a reusable authority-truth contract builder for the submission surface so projection layers consume one frozen interpretation rather than re-decoding settlement posture ad hoc.
5. Implement projection helpers that update `ObligationMirror`, workflow read models, and customer-safe timeline projections from the authoritative submission mutation.
6. Ensure projections are always derived from the submission surface role and never from raw ingress or transport state.
7. Add explicit supersession handling so only one non-superseded active submission remains authoritative for one exact meaning, while older records remain queryable.
8. Ensure late-correction or contradiction paths reopen mirrors and workflows from authoritative submission truth rather than leaving prior resolved projections frozen.

Gap handling you must perform:
- Close the gap where `SubmissionRecord` exists in the corpus before one implementation prevents `ObligationMirror` or workflow closure from impersonating settlement.
- Close the gap where unresolved or out-of-band truth can otherwise be flattened into generic “done” or “updated” projection copy.
- Close the gap where `INTENT_RECORDED` could lose request or packet lineage.
- Close the gap where state-specific ref legality is otherwise scattered between schemas, prose, and downstream renderer assumptions.
- Close the gap where reconciliation ownership on unresolved submissions is only implicit.
  Persist and preserve `reconciliation_control_contract_or_null{...}` whenever unresolved settlement posture requires it.
- Close the gap where late authority corrections could reopen projections without a clearly authoritative mutation source.

Mandatory edge cases:
- `INTENT_RECORDED` must already freeze `packet_ref`, `request_envelope_ref`, `request_hash`, `idempotency_key`, `identity_namespace_hash`, `duplicate_meaning_key`, `request_identity_contract{...}`, `proof_bundle_ref`, and `proof_bundle_hash`.
- only request-backed, authority-grounded evidence may move a submission to `CONFIRMED` or `REJECTED`
- `baseline_type` must be non-null only in `CONFIRMED` or `OUT_OF_BAND`
- `reconciliation_deadline_at` must be non-null only in `PENDING_ACK` or `UNKNOWN`
- non-terminal unresolved settlement (`PENDING_ACK` or `UNKNOWN`) must preserve an open reconciliation control packet rather than nulling it out
- only `OUT_OF_BAND` may represent legal settlement without packet-origin transmit lineage
- `SUPERSEDED` must retain historical lineage and set `superseded_by_submission_id`
- `ObligationMirror.current_submission_ref` may be non-null only in `SUBMITTED_PENDING`
- `ObligationMirror.last_confirmed_submission_ref` may be non-null only in `MET_CONFIRMED`
- internal completion, accepted-risk posture, or workflow override must never republish unresolved or rejected authority truth as confirmed
- late authority correction must reopen previously resolved workflow / timeline projections without mutating the historical `SubmissionRecord` chain into a false single-state narrative

Backend, performance, and security requirements:
- default fail-closed on missing authority evidence, illegal ref combinations, chronology regressions, or contradictory state-specific fields
- maintain transaction boundaries so submission mutation plus downstream projection update is atomic where legal consistency requires it
- index for exact-scope lookup, active chain lookup, current unresolved lookup, and authority-evidence lookup
- preserve replay safety: identical authoritative inputs must reconstruct the same submission state and grouped truth contract
- do not persist transport secrets, provider credentials, or raw unbounded callback blobs on the submission model
- emit auditable domain events or append-only journal entries for every state transition if the existing event spine from earlier cards exists

Validation rules:
- validate every persisted `SubmissionRecord` against `schemas/submission_record.schema.json`
- unit-test state transition legality, state-specific ref legality, baseline/ref guards, and supersession rules
- integration-test confirmed, rejected, unknown, out-of-band, and late-correction flows, including projection behavior into mirror/workflow/client timeline
- prove that unresolved authority truth cannot publish a resolved customer-safe projection
- prove that reloading from storage preserves byte-stable grouped contracts and does not re-derive truth from neighboring artifacts

Definition of done:
- the repo contains a durable, schema-backed `SubmissionRecord` implementation with explicit transition rules and authoritative truth projection
- settlement truth is now clearly separated from runtime transport and ingress checkpoint truth
- downstream mirror/workflow/client projections consume authoritative submission truth without inventing legal confirmation
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
