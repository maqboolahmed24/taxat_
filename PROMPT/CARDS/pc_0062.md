---
id: pc_0062
slug: phase_02_seq_062_configure_code_formatting_linting_static_analysis_and_commit_hooks
mode: sequential
phase: "02"
wave: null
track: null
task_number: "062"
block_key: phase_02_seq
original_key: phase_02_seq_062
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0062

## Task Summary

configure code formatting linting static analysis and commit hooks.

## Metadata

- ID: `pc_0062`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `062`
- Source Slug: `phase_02_seq_062_configure_code_formatting_linting_static_analysis_and_commit_hooks`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0061](./pc_0061.md) `phase_02_seq_061_generate_language_models_and_type_bindings_from_json_schemas`
- Next: [pc_0063](./pc_0063.md) `phase_02_seq_063_establish_environment_configuration_loader_and_secret_injection_abstractions`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0062_to_0069.md](../shared_operating_contract_0062_to_0069.md)
```text
Execute roadmap task `phase_02_seq_062_configure_code_formatting_linting_static_analysis_and_commit_hooks`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0061` if they exist, especially `pc_0019`, `pc_0028`, `pc_0059`, `pc_0060`, and `pc_0061`.

Mission:
Turn the repository skeleton into a governed engineering surface with deterministic formatting, linting, static analysis, and commit-time guardrails.
The purpose of this task is not to install fashionable tooling.
It is to make corpus drift, schema/code mismatch, locator brittleness, and generated-code confusion hard to introduce.
The finished setup must protect the imported schemas, generated bindings, runtime boundary packages, and browser automation suite without making normal development miserable.

Source algorithm grounding you must honor:
- `README.md`, `implementation_conventions.md`, `architecture_coherence_guardrails.md`, and `contract_integrity_requirements.md`, especially the requirements that prose, schemas, examples, and executable validators move together.
- `modules.md` and `data_model.md` where naming patterns, exact-decimal posture, and hash-bearing objects imply type- and serialization-sensitive code paths that quality gates must protect.
- `frontend_shell_and_interaction_law.md`, `customer_client_portal_experience_contract.md`, and `collaboration_workspace_contract.md` wherever browser-facing code needs durable accessibility, locator stability, and route-safe interaction behavior.
- `verification_and_release_gates.md`, `deployment_and_resilience_contract.md`, and `security_and_runtime_hardening_contract.md` where repo checks become part of release admission and fail-closed runtime posture.
- The imported schema / validator bundle from `pc_0060` and the generated bindings from `pc_0061`; generated artifacts must be recognized explicitly rather than treated like hand-authored source.
- Current official docs for the selected formatter/linter/type-check tools, pre-commit orchestration, and Playwright locator/actionability guidance.

Deliverables to create:
1. Root formatter/linter/type-check configuration files for the adopted stack, including JS/TS and Python coverage.
2. `.pre-commit-config.yaml` with staged-file-safe hooks and a clearly documented ordering policy.
3. `config/tooling/code_quality_matrix.json` mapping every repo area to formatter, linter, type checker, and hook coverage.
4. `config/tooling/generated_fixture_and_external_code_policy.json` declaring which paths are generated, vendored, fixtures, snapshot-like, or fully governed source.
5. `tools/repository/verify_code_quality_coverage.ts` or the ADR-approved equivalent that fails if new packages/routes appear without declared quality coverage.
6. Root package / task-runner script updates so `format`, `lint`, `typecheck`, `check`, `playwright`, and `validate-contracts` families are deterministic and documented.
7. `docs/repository/code_quality_runbook.md` explaining tool roles, local-vs-CI scope, generated-code posture, and how the setup maps back to the corpus.
8. `apps/operator-web/src/routes/internal/code-quality-atlas.tsx`.
9. `tests/unit/repository/code_quality_matrix.spec.ts`.
10. `tests/playwright/internal/code_quality_atlas.spec.ts`.

Required design decisions you must make explicit:
- Choose and serialize the default JS/TS quality stack. If no prior ADR says otherwise, prefer Biome for formatting/linting plus language-native type checking.
- Choose and serialize the default Python quality stack. If unresolved, prefer Ruff for formatting/linting and one explicit type checker with clear strictness policy.
- Define what runs on save, on staged files, on commit, and in CI. Commit hooks must stay fast and deterministic; full browser suites do not belong in every commit hook.
- Define the exemption and ownership posture for generated code, imported schema fixtures, snapshots, sample payloads, and large binary test assets.
- Define which rule classes are blocking errors versus ratcheted warnings, and how temporary suppressions are recorded and expired.
- Define how Playwright tests are linted and how locator/trace discipline is enforced so browser automation remains semantic rather than CSS-fragile.
- Define how cross-language quality evidence is surfaced in machine-readable form so later packages cannot silently escape the gate set.

Execution steps:
1. Inventory the monorepo from `pc_0059` and the imported/generated contract surfaces from `pc_0060` and `pc_0061`.
2. Create the code-quality matrix first. Every top-level workspace and path family must be declared before tool configs are finalized.
3. Install and configure the chosen JS/TS formatter/linter and Python formatter/linter/type checker in a way that matches the stack ADR and does not duplicate responsibilities unnecessarily.
4. Set up `pre-commit` as the outer hook runner. Hooks must run only the scoped checks appropriate to staged files and must fail closed on config drift.
5. Create the generated/fixture policy so imported schemas, sample payloads, generated bindings, and hand-authored source have intentionally different rule treatments.
6. Wire root scripts and task-graph entries so the same commands work locally and in CI with predictable output and exit codes.
7. Implement `verify_code_quality_coverage.ts` so new packages, apps, routes, or Python modules cannot appear without declared quality ownership.
8. Build the internal `code-quality-atlas` page from the machine-readable matrix rather than hard-coded UI lists.
9. Add unit tests that prove every workspace is covered and every generated/external exemption is explicit.
10. Add Playwright tests for the atlas, including keyboard traversal, reduced-motion behavior, heading hierarchy, and semantic locator stability.

Visual and interaction specification for `code-quality-atlas`:
- Tone: a premium quality loom, not a CI dashboard.
  The page should feel like a quiet editorial control surface that explains how source moves through guardrails.
- Layout:
  - `64px` top bar with repository badge, current branch or workspace context chip, and a single status sentence.
  - left tool-family rail `248-264px` listing `FORMAT`, `LINT`, `TYPECHECK`, `HOOKS`, `PLAYWRIGHT`.
  - central loom canvas with four fixed vertical lanes: `SAVE`, `STAGED`, `COMMIT`, `CI`.
  - right inspector drawer `360-392px` showing selected tool, covered paths, ignored paths, autofix posture, and blocking policy.
- Diagram:
  - the main diagram is a woven lane chart where each tool appears as one horizontal band crossing the stages it participates in.
  - add one thin dependency ribbon showing `source → generated → gated → releasable`.
  - include a tiny monoline emblem built from four interlocking rails; do not use mascots or stock icons.
- Palette:
  - background `#F5F6F3`
  - primary surface `#FFFFFF`
  - secondary surface `#EDF0EA`
  - ink `#101317`
  - muted ink `#69727A`
  - hairline `rgba(16,19,23,0.08)`
  - accent slate `#3F5E73`
  - accent moss `#5B725E`
  - accent amber `#8B6735`
  - success `#16624B`
  - warning `#8A5A18`
  - danger `#A63B32`
- Typography:
  - use a calm sans family for all UI copy.
  - reserve monospace for commands, globs, package names, rule IDs, and test IDs only.
  - keep section titles in the `15-18px` range and dense matrix rows at `13px`.
- Dimensional constraints:
  - overall max width `1480px`
  - central lane min width `720px`
  - matrix row height `40px`
  - rail item height `36px`
  - inspector code blocks max height `240px` before internal scroll
- Motion:
  - `120-150ms` ease-out for drawer reveal and band hover.
  - reduced motion must replace sliding with opacity and outline emphasis only.
- Interaction:
  - keyboard users must be able to move rail → matrix → inspector without trap.
  - every lane cell needs an accessible name describing tool, stage, and posture.
  - Playwright coverage must use `getByRole` / `getByLabel` / `getByText` first and only use `data-testid` where semantic names would be unstable.

Gap handling you must perform:
- Close the gap where the roadmap now has many packages and internal viewers but no declared quality ownership map.
- Close the gap where imported schemas and generated bindings could either be over-linted destructively or under-validated silently.
- Close the gap where Playwright tests could drift into CSS-selector fragility without a repository-wide locator policy.
- Close the gap where commit hooks often become unusably slow because stage scope, CI scope, and browser-suite scope are not separated.
- Close the gap where docs/schema/code parity is normative in the corpus but not enforced by the engineering toolchain.

Mandatory edge cases:
- a newly added package or route folder that is not represented in the quality matrix
- generated files accidentally hand-edited and formatted as if they were owned source
- sample payload JSON files reordered or mutated in ways that break deterministic validator expectations
- Playwright tests using brittle CSS selectors or force-clicking through actionability problems
- Python helper modules introduced under `python/` without static-analysis coverage
- developers bypassing a broken hook by disabling it locally because the repo did not distinguish fast hook scope from full CI scope

Backend, performance, and security requirements:
- Keep the quality setup deterministic and cross-platform; avoid shell snippets that assume one OS.
- Prefer one source of truth per responsibility. Do not let multiple tools fight over the same formatting domain.
- Hook execution must fail closed on missing config and fail open only where explicitly recorded in machine-readable exemption policy.
- Do not allow the internal atlas to mutate config or invoke repository commands.
- Keep secret-bearing files and local environment files out of linter output, traces, screenshots, and UI datasets.

Validation rules:
- Run formatter, linter, and type-check commands against representative workspaces and ensure the root scripts are stable.
- Prove that `verify_code_quality_coverage.ts` fails when a synthetic uncovered workspace is introduced.
- Validate the generated/exemption policy against its schema or create the schema as part of this task.
- Use Playwright to verify the atlas renders semantic headings, keyboard reachability, reduced-motion-safe interactions, and accurate stage coverage for at least one JS/TS package, one Python path, and one generated path.
- Keep `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py` runnable after the repo-quality setup lands.

Definition of done:
- Every governed source area is covered by explicit formatter/linter/type-check ownership.
- Generated and imported artifacts are handled intentionally rather than by broad ignore globs.
- Commit hooks are fast, deterministic, and clearly separated from CI-only work.
- Playwright/browser automation code is held to semantic locator and actionability-safe standards.
- The internal atlas lets later agents inspect the policy without reverse-engineering config files.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
