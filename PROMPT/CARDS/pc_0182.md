---
id: pc_0182
slug: phase_04_parallel_wave_02_track_backend_portal_004_implement_upload_session_recovery_harness_generation_and_cross_device_resume_support
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "004"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0182

## Task Summary

implement upload session recovery harness generation and cross device resume support.

## Metadata

- ID: `pc_0182`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `004`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_004_implement_upload_session_recovery_harness_generation_and_cross_device_resume_support`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0181](./pc_0181.md) `phase_04_parallel_wave_02_track_backend_portal_003_implement_client_upload_session_identity_freeze_request_binding_and_rebase_logic`
- Next: [pc_0183](./pc_0183.md) `phase_04_parallel_wave_02_track_backend_portal_005_implement_client_approval_pack_projection_and_stale_view_signoff_guards`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0182_to_0189.md](../shared_operating_contract_0182_to_0189.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_004_implement_upload_session_recovery_harness_generation_and_cross_device_resume_support`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0181` if they exist, especially `pc_0180` and `pc_0181`.

Mission:
Implement the deterministic `UploadSessionRecoveryHarness` generator and the supporting recovery-proof services that show lawful reconnect, reload, stale request rebase, duplicate retry, scanner delay, attachment confirmation, and cross-device continuation all preserve one governed upload identity.
This task is not a UI retry gimmick and not generic blob resumability.
It is the proof layer that demonstrates the runtime upload/session logic built in earlier cards is actually safe under hostile continuation paths.
The harness must prove that the same `upload_session_id` and `storage_ref` survive every lawful continuation path, that live request drift is published explicitly instead of silently rebinding bytes, and that transfer success alone never masquerades as request completion.

Source algorithm grounding you must honor:
- `upload_session_recovery_harness_contract.md`, all sections, especially `Required case matrix`, `Required rules`, and `Failure modes closed`.
- `customer_client_portal_experience_contract.md`, especially `Secure document-upload flow`, `Route stability across devices`, `Read-model and API translation requirements`, and the Playwright validation minimum covering reconnect, stale rebase, and mobile continuity.
- `northbound_api_and_session_contract.md`, especially the upload-session surfaces, grouped `upload_request_binding_contract`, stale portal recovery refs, and the problem-envelope rules for `latest_upload_session_ref` and stale-guard publication.
- `data_model.md`, especially `ClientUploadSession`, `UploadSessionRecoveryHarness`, `ClientDocumentRequest`, and the distinction between `latest_upload_ref` and `current_request_upload_ref_or_null`.
- `modules.md`, especially `BUILD_CLIENT_DOCUMENT_CENTER(...)` and the explicit callout that deterministic upload recovery must validate through `schemas/upload_session_recovery_harness.schema.json`.
- `state_machines.md`, especially the `ClientDocumentRequest` rules and the cross-state invariants prohibiting attached or accepted current-request posture when binding is stale or confirmation is incomplete.
- `glossary.md`, especially the FE-87 `Upload Session Recovery Harness` definition.
- `schemas/upload_session_recovery_harness.schema.json`, `schemas/client_upload_session.schema.json`, `schemas/upload_request_binding_contract.schema.json`, and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially the portal and upload hardening items around request-version binding, duplicate-session prevention, recovery posture drift, and confidence / completion separation.
- `test_vectors.md`, especially the upload reconnect/rebase cases already added by earlier tranche work.

Deliverables to create:
1. `packages/backend-portal/src/services/build_upload_session_recovery_harness.ts`
2. `packages/backend-portal/src/services/materialize_upload_recovery_case.ts`
3. `packages/backend-portal/src/services/derive_upload_recovery_post_request_projection.ts`
4. `packages/backend-portal/src/services/assert_upload_recovery_invariants.ts`
5. `packages/backend-portal/src/services/derive_cross_device_resume_policy.ts`
6. `packages/backend-portal/src/tests/upload_session_recovery_harness.spec.ts`
7. `packages/backend-portal/src/tests/upload_session_recovery_problem_envelope.spec.ts`
8. `docs/portal/upload_session_recovery_harness_and_cross_device_resume.md`
9. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/upload-recovery-lab.tsx`

You MUST patch any existing upload-session read or portal document-center adapter so it can emit the stable inputs the harness needs rather than rebuilding them inside test-only fixtures.

Required design decisions you must make explicitly:
- define how `deterministic_seed` is chosen and how it stays stable across schema-compatible code changes
- define the exact pre/post snapshot payload for each case so the harness can compare frozen identity, live request drift, resumability, attachment posture, and current-request projection without depending on renderer-local state
- define how cross-device continuation proves lawful session reuse while still honoring session/access binding and visibility partition rules
- define how stale request rebase is represented without mutating `frozen_request_version_ref`
- define how duplicate-session/storage creation is detected and asserted, rather than assumed
- define how the harness consumes the existing request-binding services from `pc_0181` instead of reimplementing them

Execution requirements:
- cover all required scenarios: `MOBILE_RECONNECT`, `BROWSER_RELOAD`, `STALE_REQUEST_REBASE`, `DUPLICATE_ALLOCATION_RETRY`, `CHECKSUM_OR_SCANNER_DELAY`, `ATTACHMENT_CONFIRMATION`, and `CROSS_DEVICE_CONTINUATION`
- keep `pre_session.upload_session_id`, `storage_ref`, `tenant_id`, `client_id`, `request_id`, and `frozen_request_version_ref` identical to `post_session` in every lawful continuation case
- keep `post_request_projection.request_version_ref` aligned to `post_session.live_request_version_ref`, never to the frozen version when live drift exists
- for stale-request-rebase cases, clear `current_request_upload_ref_or_null` until explicit reconfirmation occurs
- for duplicate retry and cross-device continuation cases, prove `duplicate_session_created = false` and `duplicate_storage_ref_created = false`
- for checksum/scanner delay cases, prove that full byte transfer still leaves request completion unready until scan / validation / confirmation semantics say otherwise
- for attachment-confirmation cases, prove `CONFIRM_ATTACHMENT` is distinct from transfer or validation success and that only explicit confirmation produces `READY_CURRENT_REQUEST_SATISFIED`
- if an upload-recovery debug lab is added, make it a quiet three-column explainer at most: pre-session, post-session, and post-request projection, plus one slim recovery ladder diagram; no dashboard grid

Gap handling you must close while implementing:
- close any remaining gap where upload recovery depends on browser-local history rather than durable governed upload state
- close any gap where cross-device continuation would allocate a new `upload_session_id` or `storage_ref`
- close any gap where a request rebase mutates the frozen upload identity instead of publishing live drift and reconfirmation posture
- close any gap where problem envelopes fail to publish the narrowest authoritative recovery ref (`latest_upload_session_ref`) plus stale-guard basis for upload-session failures
- close any gap where runtime reads cannot generate the exact harness snapshots because they omit live request version, binding-resolution basis, or resumability metadata

Mandatory edge cases:
- `CROSS_DEVICE_CONTINUATION` must reuse the same governed session when `resume_identity_policy = RESUME_EXISTING_SESSION_ONLY`
- `STALE_REQUEST_REBASE` must preserve `frozen_request_version_ref` while advancing `live_request_version_ref`
- `CHECKSUM_OR_SCANNER_DELAY` must keep request completion not-ready even when `bytes_transferred = byte_count`
- `ATTACHMENT_CONFIRMATION` must keep attachment distinct from transfer acceptance and validation acceptance
- duplicate retry or reload must never create a second current upload row for the same frozen identity scope
- no case may report a current satisfied request when `request_binding_state` is stale or superseded
- no case may widen tenant/client/request scope during recovery

Playwright and contract-test requirements:
- add pure unit tests for the harness generator and every scenario enumerator
- if upload-session routes already exist, add Playwright APIRequestContext coverage that verifies recovery failures publish `latest_upload_session_ref`, stale guard data, and problem-envelope recovery hints correctly
- if you add the debug lab, add Playwright browser tests that prove scenario switching, side-by-side identity comparison, and cross-device continuation visualization using semantic locators only
- capture request/session payloads and traces on failure so harness regressions are diagnosable

Backend, performance, and security requirements:
- keep the harness deterministic and side-effect-free; it must not mutate production upload rows as part of proof generation
- do not let recovery proof code become a second source of runtime truth; runtime truth remains `ClientUploadSession` plus `upload_request_binding_contract`
- make duplicate detection and storage/session reuse checks indexed and cheap; no broad scans over unrelated uploads
- fail closed on tenant/client/request scope mismatch, stale identity drift, or ambiguous continuation inputs

Validation rules:
- validate every emitted harness payload against `schemas/upload_session_recovery_harness.schema.json`
- validate every embedded `pre_session` / `post_session` snapshot and post-request projection against the corresponding schema expectations you already own
- ensure the harness remains reusable by validators, forensic guardrails, and future debug or replay tooling

Definition of done:
- the repo contains one authoritative upload recovery-harness generator that proves all required continuation scenarios
- cross-device continuation, reload, reconnect, duplicate retry, stale rebase, and attachment confirmation are machine-checkable instead of implied by UX prose
- upload recovery now has durable proof that frozen identity, storage reuse, explicit stale reconfirmation, and completion boundaries survive every lawful resume path
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
