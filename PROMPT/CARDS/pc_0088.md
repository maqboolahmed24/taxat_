---
id: pc_0088
slug: phase_03_parallel_wave_01_track_backend_access_004_implement_delegation_grant_authority_link_and_exceptional_authority_grant_repositories
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "004"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0088

## Task Summary

implement delegation grant authority link and exceptional authority grant repositories.

## Metadata

- ID: `pc_0088`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `004`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_004_implement_delegation_grant_authority_link_and_exceptional_authority_grant_repositories`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0087](./pc_0087.md) `phase_03_parallel_wave_01_track_backend_access_003_implement_authority_layer_boundary_contract_model_and_validations`
- Next: [pc_0089](./pc_0089.md) `phase_03_parallel_wave_01_track_backend_access_005_implement_authorize_module_with_scope_partition_and_masking_logic`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0086_to_0093.md](../shared_operating_contract_0086_to_0093.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_004_implement_delegation_grant_authority_link_and_exceptional_authority_grant_repositories`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0087` if they exist, especially `pc_0022`, `pc_0023`, `pc_0034`, `pc_0038`, `pc_0039`, `pc_0040`, `pc_0049`, `pc_0050`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0067`, `pc_0068`, `pc_0076`, `pc_0085`, `pc_0086`, and `pc_0087`.

Mission:
Implement the durable repositories and lifecycle services for `DelegationGrant`, `AuthorityLink`, and `ExceptionalAuthorityGrant`.
This is the persistence and lineage substrate that makes external authority, client authorization, and bounded exception handling explicit and auditable.
The result must preserve lifecycle state, freshness posture, partition coverage, token-client binding health, grant lineage, and approval evidence instead of treating these artifacts as opaque connector metadata.

Source algorithm grounding you must honor:
- `data_model.md`, especially the field definitions and invariants for `DelegationGrant`, `AuthorityLink`, and `ExceptionalAuthorityGrant`.
- `actor_and_authority_model.md`, especially `3.10 Delegation rules`, `3.11 Non-delegable and step-up actions`, and `3.14 Invariants`.
- `connector_delegation_contract.md`, especially the persistence boundary rules, explicit edge semantics, no-silent-rebind requirement, and bounded exceptional-authority constraints.
- `authority_truth_and_internal_projection_separation_contract.md`, especially the warning that internal acceptance never fabricates external authority truth.
- `northbound_api_and_session_contract.md`, especially the rules that delegation basis and authority posture are resolved before command acceptance.
- Existing schemas including `delegation_grant.schema.json`, `authority_link.schema.json`, and `exceptional_authority_grant.schema.json`.

Deliverables to create:
1. `packages/backend-access/src/models/delegation_grant.ts`.
2. `packages/backend-access/src/models/authority_link.ts`.
3. `packages/backend-access/src/models/exceptional_authority_grant.ts`.
4. `packages/backend-access/src/repositories/delegation_grant_repository.ts`.
5. `packages/backend-access/src/repositories/authority_link_repository.ts`.
6. `packages/backend-access/src/repositories/exceptional_authority_grant_repository.ts`.
7. `packages/backend-access/src/services/authority_edge_resolution_service.ts`.
8. `packages/backend-access/src/services/delegation_freshness_service.ts`.
9. `packages/backend-access/src/services/exceptional_authority_budget_service.ts`.
10. `packages/backend-access/src/services/authority_link_binding_health_service.ts`.
11. `db/migrations/phase03_0003_delegation_authority_exception_repositories.sql`.
12. `config/access/delegation_freshness_policy.json`.
13. `config/access/exceptional_authority_policy.json`.
14. `docs/access/delegation_authority_link_and_exceptional_authority_repositories.md`.
15. `tests/unit/backend_access/delegation_and_authority_lifecycle.spec.ts`.
16. `tests/integration/backend_access/delegation_authority_exception_repository.spec.ts`.

Required design decisions you must make explicit:
- Define primary keys, lineage keys, supersession keys, and unique constraints for grants, links, and exceptional grants.
- Define how grant lineage relates to connector bindings and token-binding profiles without persisting raw secrets in the repository rows.
- Define whether exceptional-authority usage counts are decremented through compare-and-swap, separate usage ledgers, or both; preserve race safety either way.
- Define how imported-evidence freshness timestamps, client scope, partition scope, and reporting-subject / authorised-party relationships are indexed for fast authorization evaluation.
- Define the policy boundary between repository state transitions and approval / step-up / human review services.

Execution steps:
1. Implement typed models and repositories using the canonical schemas as the first line of contract enforcement.
2. Create migrations that preserve lifecycle chronology, supersession history, expiry, revocation, and linkage to tenant, principal, client, reporting subject, authorised party, and connector-binding references.
3. Implement lifecycle transition guards that reject silent rebinds, illegal supersession, illegal revival of expired or revoked grants, and self-approved exceptional authority.
4. Implement freshness services that compute `CURRENT`, `REVALIDATION_REQUIRED`, `STALE`, or equivalent governed posture strictly from source timestamps and policy, never from ad hoc time windows buried in handlers.
5. Implement binding-health services for `AuthorityLink` so client-binding mismatches, delegation gaps, and limited-scope situations are explicit and queryable.
6. Implement bounded-use exceptional-authority accounting with incident linkage, distinct requester / approver identities, expiry, and scope ceilings.
7. Preserve cross-artifact lineage so `PrincipalContext`, `AuthorityLayerBoundaryContract`, and `AuthorizationDecision` can point to exact snapshot refs rather than just current mutable rows.

Gap handling you must perform:
- Close the gap where external delegation and authority readiness are hidden inside connector tokens or transient OAuth callbacks.
- Close the gap where imported client authorization freshness is ambiguous or not durable.
- Close the gap where exceptional authority could be granted without distinct requester / approver identities, incident evidence, expiry, or bounded usage.
- Close the gap where authority-link lineage can silently drift when client bindings or connector profiles change.
- Close the gap where main-agent versus supporting-agent capability boundaries are not preserved as durable, queryable facts.

Mandatory edge cases:
- `authorised_party_ref != reporting_subject_ref` but `delegation_grant_ref` is null and must fail closed
- imported or handshake-based delegation has no `imported_evidence_fresh_until` and must fail validation
- authority link is `AUTHORIZED_ACTIVE` but token-client binding state is mismatch
- authority link says delegation not required while a delegated-state field is populated
- authority link transitions from healthy to revoked while a prior snapshot is still referenced by a frozen authorization decision
- exceptional authority requester and approver are the same principal
- exceptional authority remaining uses underflow under concurrent consumption
- expired or revoked exceptional authority is presented as satisfying override posture
- human-facing copy is generated using the phrase `digital handshake` instead of the HMRC-facing phrase `authorisation link`
- supporting-agent posture tries to authorize a final-declaration or equivalent main-agent-only action

Backend, performance, and security requirements:
- Never persist raw OAuth refresh tokens, access tokens, session cookies, or raw authority credentials in these repositories.
  Persist references, encrypted handles, hashes, profile refs, and lineage ids only, according to the earlier vault and secret-root policies.
- Repository operations must be race-safe, idempotent where appropriate, and audit-friendly.
- Index freshness, lifecycle, grant lineage, reporting subject, authorised party, tenant, and connector-binding references for hot-path authorization lookups.
- Default to fail-closed when freshness, binding health, or scope ceilings are ambiguous.
- No new product UI is required.
  If an internal lifecycle inspector is added for QA, render it as a narrow evidence ledger rather than a generic table wall, and verify it with Playwright.

Validation rules:
- Validate all persisted objects against the canonical schemas.
- Unit-test every lifecycle reverse guard from the schemas, including delegated/not-delegated pairings, authority-link binding health transitions, imported freshness requirements, and exceptional-authority approval separation.
- Integration-test supersession, expiry, revocation, stale-import refresh, concurrent exceptional-authority consumption, and snapshot lookup by lineage.
- Prove that frozen authorization records can keep referencing old snapshot ids safely even after newer current rows exist.
- Prove that supporting-agent or limited-delegation posture fails closed before later access logic attempts a live authority call.

Definition of done:
- The repo has durable, schema-governed repositories for `DelegationGrant`, `AuthorityLink`, and `ExceptionalAuthorityGrant`.
- Later authorization and authority-request tasks can query explicit lifecycle, freshness, and lineage facts instead of digging into connector-specific state.
- Bounded exceptions remain bounded, auditable, and structurally incapable of silently widening client or authority scope.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
