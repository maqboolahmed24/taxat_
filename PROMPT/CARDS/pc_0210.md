---
id: pc_0210
slug: phase_04_parallel_wave_02_track_backend_retention_security_observability_002_implement_erasure_proof_legal_hold_and_privacy_minimization_workflows
mode: parallel
phase: "04"
wave: "02"
track: "backend_retention_security_observability"
task_number: "002"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_retention_security_observability_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0210

## Task Summary

002 implement erasure proof legal hold and privacy minimization workflows.

## Metadata

- ID: `pc_0210`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend retention security observability`
- Task Number: `002`
- Source Slug: `phase_04_parallel_wave_02_track_backend_retention_security_observability_002_implement_erasure_proof_legal_hold_and_privacy_minimization_workflows`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0209](./pc_0209.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_001_implement_retention_tag_artifact_retention_and_expiry_limitation_propagation_services`
- Next: [pc_0211](./pc_0211.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_003_implement_secret_version_model_rotation_audit_and_attestation_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0206_to_0213.md](../shared_operating_contract_0206_to_0213.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_retention_security_observability_002_implement_erasure_proof_legal_hold_and_privacy_minimization_workflows`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0209` if they exist, especially `pc_0209`, `pc_0199` through `pc_0200`, `pc_0152` through `pc_0156`, and `pc_0166` through `pc_0169`.

Mission:
Implement the lawful erasure / pseudonymisation workflow, legal-hold application and release controls, append-only erasure proof creation, and privacy-minimization defaults that prevent hidden-data re-exposure in runtime, exports, diagnostics, and restores.
This task is not allowed to treat deletion as “best-effort cleanup.”
Every erasure request must either produce a typed blocked posture or a durable completed proof with lawful surviving-derived-artifact semantics.

Source algorithm grounding you must honor:
- `retention_and_privacy.md`, especially erasure and legal-hold workflow, privacy/minimization defaults, proof-bundle retention semantics, and restore privacy reconciliation boundaries.
- `data_model.md`, especially `ArtifactRetention` and `ErasureProof`.
- `schemas/erasure_proof.schema.json`, `schemas/retention_tag.schema.json`, and `schemas/artifact_retention.schema.json`.
- `modules.md`, especially the retention-policy and failure/remediation hooks that must stay bound to the same retention object.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially legal-hold blocking, minimum-retention blocking, surviving-derived-artifact, and restore re-materialization scenarios.

Deliverables to create:
1. `packages/backend-retention/src/services/derive_erasure_eligibility.ts`
2. `packages/backend-retention/src/services/apply_legal_hold.ts`
3. `packages/backend-retention/src/services/release_legal_hold.ts`
4. `packages/backend-retention/src/services/execute_erasure_or_pseudonymisation.ts`
5. `packages/backend-retention/src/services/build_erasure_proof.ts`
6. `packages/backend-retention/src/services/apply_privacy_minimization_transform.ts`
7. `packages/backend-retention/src/tests/erasure_workflow.spec.ts`
8. `packages/backend-retention/src/tests/privacy_minimization.spec.ts`
9. `docs/retention/erasure_proof_legal_hold_and_privacy_minimization_workflows.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/retention/erasure-and-hold-lab.tsx`

Required design decisions you must make explicitly:
- define the erasure-eligibility decision ladder and typed failure/blocking codes
- define when the lawful action is delete, pseudonymise, retain-with-limitation, or block
- define the legal-hold register model, release behavior, and explicit release-preview semantics for previously blocked items
- define the `ErasureProof` payload and how it binds request, basis, target, action, proof hash, and completion timing
- define the privacy-minimization transform library for diagnostics, exports, support tools, and restored data re-entry paths

Execution requirements:
- implement the workflow `ErasureRequested -> basis/hold/eligibility evaluation -> delete or pseudonymise lawful targets -> preserve lawful surviving derived artifacts with explicit limitation behavior -> emit completed proof only after durable action result`
- fail closed for unresolved legal hold, unresolved authority ambiguity, unmet minimum retention, or missing proof-preservation preconditions
- preserve append-only proof of request, basis, action, and outcome; no erasure path may destroy that proof lineage
- ensure legal-hold release can preview the queue items that would become eligible after release instead of collapsing directly into destructive action
- ensure privacy defaults remain least-data by default for reads, exports, diagnostics, and support tooling
- ensure restore or recovery flows that re-materialize already-erased or newly-out-of-policy data open compensating re-erasure / limitation handling rather than silently re-exposing the data

Gap handling you must close while implementing:
- close any gap where “erase” means hard delete without proof preservation
- close any gap where legal-hold release bypasses an explicit release preview and destructive confirmation posture
- close any gap where surviving derived artifacts remain untyped or silently retain broader data than lawful
- close any gap where diagnostics, exports, or support tools can reintroduce hidden data that runtime views would otherwise mask or minimize
- close any gap where restore/recovery work can rehydrate erased data without opening a compensating privacy workflow

Mandatory edge cases:
- unresolved legal hold must block erasure even when all other conditions look eligible
- unmet minimum retention must block erasure even if policy-expiry time has passed
- a request may resolve to pseudonymisation rather than deletion; the proof must say so explicitly
- completed erasure proof must not be emitted before the durable action result exists
- surviving-derived-artifact states must keep limitation behavior and retained basis explicit
- restored or replayed data that crosses privacy boundaries must trigger compensating re-erasure or limitation handling

Playwright and contract-test requirements:
- add deterministic tests for erasure-eligibility blocking, legal-hold apply/release, delete vs pseudonymise branching, proof creation, privacy minimization transforms, and compensating re-erasure posture
- if you expose or patch a retention or admin route, add Playwright `APIRequestContext` coverage for blocked, preview-only, and completed-proof workflows
- if you add the optional lab, add Playwright browser tests that verify the hold/erasure state rail, release preview, proof sidecar, keyboard navigation, and reduced-motion-safe reveal behavior using semantic locators only
- capture payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a three-zone layout: eligibility/hold ribbon, request/action timeline canvas, narrow proof/minimization sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; accent `#0F766E`; info accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- visualize hold release as a preview-first ladder or timeline, not as a destructive primary button in a metrics card
- surface delete vs pseudonymise outcomes as explicit typed steps with basis and proof refs
- animate only opacity/translate/height with `140ms` to `180ms`; respect reduced motion

Backend, performance, and security requirements:
- keep destructive actions gated by durable eligibility evaluation and explicit legal state
- prohibit raw sensitive payloads from leaking into logs, traces, or error strings during minimization or erasure
- keep proof generation deterministic and append-only
- centralize privacy-minimization transforms so diagnostics and exports cannot diverge from runtime policy

Validation rules:
- validate `ErasureProof`, `RetentionTag`, and `ArtifactRetention` artifacts against schema before persistence or response emission
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one canonical backend path for eligibility, one for legal hold, one for destructive action, one for proof creation, and one for minimization transforms
- blocked and completed paths are durable, typed, and test-covered
- optional labs, if added, remain read-only projections of persisted truth
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
