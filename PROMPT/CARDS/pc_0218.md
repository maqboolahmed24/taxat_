---
id: pc_0218
slug: phase_04_parallel_wave_02_track_backend_retention_security_observability_010_implement_operator_morning_digest_and_audit_hotspot_analytics_queries
mode: parallel
phase: "04"
wave: "02"
track: "backend_retention_security_observability"
task_number: "010"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_retention_security_observability_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0218

## Task Summary

010 implement operator morning digest and audit hotspot analytics queries.

## Metadata

- ID: `pc_0218`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend retention security observability`
- Task Number: `010`
- Source Slug: `phase_04_parallel_wave_02_track_backend_retention_security_observability_010_implement_operator_morning_digest_and_audit_hotspot_analytics_queries`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0217](./pc_0217.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_009_implement_failure_lifecycle_dashboard_and_lineage_read_model_queries`
- Next: [pc_0219](./pc_0219.md) `phase_04_parallel_wave_02_track_backend_release_resilience_001_implement_build_artifact_generation_signing_metadata_and_candidate_tuple_persistence`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0214_to_0221.md](../shared_operating_contract_0214_to_0221.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_retention_security_observability_010_implement_operator_morning_digest_and_audit_hotspot_analytics_queries`.

Inherit the shared operating contract and consume outputs from cards `pc_0189` through `pc_0217` if they exist, especially `pc_0194`, `pc_0206`, `pc_0207`, `pc_0208`, `pc_0214`, `pc_0215`, and `pc_0217`.

Mission:
Implement the authoritative query layer for published `OperatorMorningDigest` artifacts and the analytics/feed layer for governance-facing audit hotspots.
This task is not allowed to reconstruct overnight posture from raw queues, ephemeral worker state, or logs.
It must read persisted nightly truth and publish stable query surfaces that preserve supersession lineage, partition integrity, worklist refs, and hotspot ordering.

Source algorithm grounding you must honor:
- `nightly_autopilot_contract.md`, especially section `12.2 OperatorMorningDigest`, the publication rule, and the what-if exclusions that reinforce persisted-truth-only posture.
- `observability_and_audit_contract.md`, especially `get_operator_morning_digest(...)` and the related slice/query requirements.
- `admin_governance_console_architecture.md`, especially the governance overview / risk-ledger / audit-hotspot tape requirements.
- `data_model.md`, especially `OperatorMorningDigest` and `TenantGovernanceSnapshot`.
- `modules.md`, especially `PUBLISH_OPERATOR_MORNING_DIGEST(...)` and the governance dominant-family scoring rule.
- `northbound_api_and_session_contract.md`, especially route-stable governance read surfaces.
- `schemas/operator_morning_digest.schema.json`, `schemas/tenant_governance_snapshot.schema.json`, and `schemas/audit_investigation_frame.schema.json` where hotspot slices reuse investigation-envelope semantics.
- `scripts/validate_contracts.py`, especially `validate_operator_morning_digest`, `operator_morning_digest_workflow_publication_and_summary_alignment`, `operator_morning_digest_publication_qa_requires_settled_publication`, `operator_morning_digest_publication_qa_queue_partition_hash_mirror`, `operator_morning_digest_recovery_supersession_lineage`, `validate_tenant_governance_snapshot`, `tenant_governance_snapshot_attention_queue_alignment`, and `tenant_governance_snapshot_widget_ledger_alignment`.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially vectors around digest supersession, queue-partition drift, unresolved handoff publication, and audit-hotspot prominence drift.

Deliverables to create:
1. `packages/backend-observability/src/queries/get_operator_morning_digest.ts`
2. `packages/backend-observability/src/queries/list_operator_morning_digests.ts`
3. `packages/backend-observability/src/queries/list_audit_hotspots.ts`
4. `packages/backend-observability/src/services/build_audit_hotspot_analytics.ts`
5. `packages/backend-observability/src/tests/operator_morning_digest_query.spec.ts`
6. `packages/backend-observability/src/tests/audit_hotspot_analytics.spec.ts`
7. `docs/observability/operator_morning_digest_and_audit_hotspot_queries.md`
8. Optional but recommended: `apps/admin-console-web/src/routes/debug/observability/operator-digest-and-hotspots-lab.tsx`

You MUST patch any existing morning-digest or governance-hotspot surface that still:
- derives counts from queue snapshots instead of persisted published-workflow refs,
- loses digest supersession lineage,
- or renders hotspot counts without concrete worklist or object refs.

Required design decisions you must make explicitly:
- define the query contract and cache key for `(tenant_id, coverage_date)` digest reads and any list filters
- define how superseded digests are returned, paged, and distinguished from the current authoritative digest
- define the exact audit-hotspot ranking and grouping inputs you will use, ensuring they remain grounded to persisted audit / failure / governance truth and not UI-local heuristics
- define how hotspot analytics bind back to concrete worklist refs and object refs for governance overview reuse
- define how queue summaries and highlighted outcomes are surfaced without re-partitioning or re-ranking published workflow truth client-side

Execution requirements:
- query `OperatorMorningDigest` from persisted digest artifacts only
- preserve `derivation_contract{...}`, `covered_selection_entry_refs[]`, `summary_counts{...}`, `outcome_entry_refs{...}`, `queue_summaries[]`, `highlighted_client_outcomes[]`, `waiting_on_authority_refs[]`, `late_data_hold_refs[]`, `published_workflow_item_refs[]`, `published_notification_refs[]`, `generated_at`, `published_at`, and `supersedes_digest_id`
- ensure digest reads distinguish current publication from superseded publication without deleting prior audit history
- ensure hotspot analytics emit concrete worklist refs and affected object refs, not decorative counts
- keep hotspot ordering stable and explainable; when feeding governance overview, remain compatible with the dominant-family scoring / hysteresis rules already defined by the algorithm
- preserve worklist-backed posture so hotspots remain actionable rather than decorative KPI tiles

Gap handling you must close while implementing:
- close any gap where queue summaries or highlights are rebuilt from live queue state instead of persisted published workflow refs
- close any gap where digest publication appears complete even though workflow or notification publication never settled and QA never froze the exact partition hashes
- close any gap where a recovery digest supersedes an earlier digest but the earlier lineage becomes invisible to queries
- close any gap where audit hotspot counts appear without a concrete worklist ref or object anchor
- close any gap where governance overview or hotspot analytics can drift because the query layer reranks using ad hoc UI logic instead of frozen backend rules

Mandatory edge cases:
- `covered_selection_entry_refs[]` must equal the exact union of `outcome_entry_refs{...}`
- `queue_summaries[].item_refs[]` must partition the persisted `published_workflow_item_refs[]` without duplication
- `publication_qa_completed_at` must not predate settled workflow or notification publication
- superseding digests must preserve `supersedes_digest_id` and root lineage
- hotspot analytics must remain action-backed even when open counts are zeroed by non-material churn suppression

Playwright and contract-test requirements:
- add tests proving digest queries preserve supersession lineage, queue partition integrity, highlight ordering, and waiting-on-authority / late-data coverage
- add tests proving hotspot analytics always surface concrete worklist refs and object anchors
- if you expose or patch routes, add Playwright `APIRequestContext` coverage for digest retrieval, superseded digest retrieval, hotspot query filtering, and governance-summary compatibility
- if you add the optional lab, add Playwright browser tests for semantic-locator navigation, digest supersession badges, hotspot row selection, reduced-motion-safe expansion, and sidecar focus retention using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- use a three-band layout: overnight digest ribbon, queue/hotspot ledger, narrow lineage/explanation sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; info accent `#1D4ED8`; continuity accent `#0F766E`; caution `#B7791F`; danger `#C2410C`
- make the digest feel like a calm operator handoff, not a dashboard wall
- represent audit hotspots as an ordered tape or ledger with concrete worklist anchors; do not use donut charts or KPI tiles
- animate only opacity / translate / height; respect reduced motion

Backend, performance, and security requirements:
- keep digest queries and hotspot analytics read-only and deterministic
- never widen visibility beyond the stored worklist / route contract
- avoid expensive ad hoc rescans over raw audit logs on every query; precompute or index responsibly without changing the authoritative source of truth
- ensure stale or superseded digests remain queryable for audit/history without being confused with the live digest

Validation rules:
- validate every returned `OperatorMorningDigest` and any reused `TenantGovernanceSnapshot` / `AuditInvestigationFrame` artifacts against schema before returning or persisting them
- update fixtures/builders so all relevant validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one authoritative query layer for operator-morning digests and audit-hotspot analytics
- supersession lineage, queue partitions, hotspot worklist bindings, and governance compatibility are durable and test-covered
- optional labs, if added, are read-only reflections of persisted truth rather than alternate implementations
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
