---
id: pc_0164
slug: phase_03_parallel_wave_01_track_backend_northbound_008_implement_upload_session_allocate_put_blob_and_status_endpoints
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "008"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0164

## Task Summary

implement upload session allocate put blob and status endpoints.

## Metadata

- ID: `pc_0164`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `008`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_008_implement_upload_session_allocate_put_blob_and_status_endpoints`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0163](./pc_0163.md) `phase_03_parallel_wave_01_track_backend_northbound_007_implement_client_portal_workspace_documents_approvals_onboarding_and_activity_endpoints`
- Next: [pc_0165](./pc_0165.md) `phase_03_parallel_wave_01_track_backend_northbound_009_implement_collaboration_work_item_snapshot_stream_activity_and_attachment_endpoints`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0158_to_0165.md](../shared_operating_contract_0158_to_0165.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_008_implement_upload_session_allocate_put_blob_and_status_endpoints`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0163` if they exist, especially `pc_0110` through `pc_0117`, `pc_0134` through `pc_0141`, and `pc_0163`.

Mission:
Implement the governed upload surface family:
- `POST /v1/uploads/sessions`
- `PUT /v1/uploads/sessions/{upload_session_id}/blob`
- `GET /v1/uploads/sessions/{upload_session_id}`
These endpoints must freeze request identity and request version at allocation time, prevent duplicate session creation for the same frozen request identity, keep transfer/scan/validation/request-binding/resumability/attachment posture distinct, and preserve the same session/storage lineage through reconnect and request rebase.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially section 2.2, the portal command rules in 3.2, and session rules in section 8.
- `upload_session_request_binding_and_rebase_contract.md` in full.
- `customer_client_portal_experience_contract.md`, especially the secure document-upload flow and read-model/API translation requirements.
- `data_model.md`, especially `ClientUploadSession` and `UploadSessionRecoveryHarness`.
- `state_machines.md`, especially `ClientDocumentRequest.lifecycle_state`, the upload-related rules, and cross-state invariants 12 and 18.
- `schemas/client_upload_session.schema.json`, `schemas/upload_request_binding_contract.schema.json`, `schemas/upload_session_recovery_harness.schema.json`, and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 57-61, 265-266, and 417-418.

Deliverables to create:
1. `packages/backend-northbound/src/models/client_upload_session.ts`
2. `packages/backend-northbound/src/repositories/client_upload_session_repository.ts`
3. `packages/backend-northbound/src/http/post_upload_sessions_endpoint.ts`
4. `packages/backend-northbound/src/http/put_upload_blob_endpoint.ts`
5. `packages/backend-northbound/src/http/get_upload_session_status_endpoint.ts`
6. `packages/backend-northbound/src/http/register_upload_session_routes.ts`
7. `packages/backend-northbound/src/services/allocate_upload_session.ts`
8. `packages/backend-northbound/src/services/reuse_duplicate_upload_session.ts`
9. `packages/backend-northbound/src/services/store_upload_blob_and_update_progress.ts`
10. `packages/backend-northbound/src/services/verify_upload_integrity_and_checksum.ts`
11. `packages/backend-northbound/src/services/build_upload_request_binding_contract.ts`
12. `packages/backend-northbound/src/services/transition_client_upload_session_state.ts`
13. `packages/backend-northbound/src/services/build_upload_session_problem_envelope.ts`
14. `docs/northbound/upload_session_surfaces.md`
15. `tests/unit/backend_northbound/client_upload_session_contract.spec.ts`
16. `tests/unit/backend_northbound/upload_request_binding_contract.spec.ts`
17. `tests/integration/backend_northbound/upload_session_allocate_put_get.spec.ts`
18. `tests/integration/backend_northbound/upload_session_rebase_and_reconfirm.spec.ts`
19. `tests/integration/backend_northbound/upload_session_recovery_harness.spec.ts`
20. `tests/playwright_api/northbound/upload_session_routes.spec.ts`

Optional diagnostic explorer only if it materially accelerates verification:
21. `apps/client-portal-web/src/routes/debug/upload-session-inspector.tsx`

Required design decisions you must make explicit:
- Define one exact wire profile for `PUT /blob` because the algorithm freezes lifecycle semantics but leaves chunking grammar open. Choose one deterministic profile and document it precisely rather than supporting multiple upload protocols.
- Define how duplicate-session suppression works for the same frozen `(tenant, client, request, request_version, file identity)` tuple and when a lawful resume returns the existing session instead of creating a new one.
- Define how checksum verification, scan state, validation state, request binding state, attachment state, and next action are updated and observed without collapsing them into one status badge.
- Define how cross-device continuation reuses the same `upload_session_id`, `storage_ref`, and request-binding contract when lawful.
- Define how in-flight request rebase is represented without silently rebinding the session to the newer request version.

Execution steps:
1. Implement governed upload-session allocation that freezes tenant/client/request/request-version identity and emits the grouped `upload_request_binding_contract`.
2. Implement one deterministic `PUT /blob` profile that updates progress idempotently and can lawfully resume the same session.
3. Keep `ClientUploadSession` state transitions explicit across transfer, integrity, scan, validation, resumability, request-binding, attachment, and recovery posture.
4. Implement `GET /v1/uploads/sessions/{upload_session_id}` as the authoritative status read, preserving the frozen request lineage even after rebase.
5. Re-validate emitted upload sessions against schema before response.
6. Add deterministic tests driven by the `upload_session_recovery_harness` cases for reconnect, reload, duplicate retry, stale rebase, scanner delay, attachment confirmation, and cross-device continuation.

Gap handling you must perform:
- Close the gap where duplicate allocation mints a second session or second storage lineage for the same frozen request identity.
- Close the gap where transfer success is mistaken for accepted attachment completion.
- Close the gap where stale bytes can silently satisfy the current request after rebase.
- Close the gap where chronology across transfer/scan/validation/finalize/reconfirm timestamps can move backward.
- Close the gap where upload confidence and recovery posture are treated as UI-only guesses instead of governed artifact state.

Mandatory edge cases:
- `ClientUploadSession.request_binding_state` MUST stay one of `ORIGINAL_CURRENT`, `RECONFIRMED_CURRENT`, `RECONFIRMATION_REQUIRED`, or `SUPERSEDED`
- duplicate session allocation for the same frozen request identity MUST be forbidden
- in-flight rebase MUST preserve the same governed session and storage lineage; it may not mutate the session onto a newer request version
- `GET` status MUST preserve the original frozen tenant/client/request/request-version fields from allocation time
- `ATTACHED` MUST NOT be reachable unless integrity is verified, bytes transferred equals byte count, request binding is current or reconfirmed, upload confidence is high enough, and recovery posture is `NONE`
- stale uploads may surface `REBIND_REQUIRED` and `RECONFIRM_REQUEST`, but they MUST NOT silently become current-request satisfaction
- quarantined or validation-rejected uploads MUST remain non-downloadable and recovery-explicit
- chronology fields MUST remain monotonic

Playwright / contract-test requirements:
- use Playwright APIRequestContext for allocate/put/get flows
- no fixed sleeps around scan/validation delay; use deterministic fixtures or explicit state transitions
- capture full request/response and state-transition logs on failure
- prove duplicate allocation returns or references the existing lawful session instead of minting a new one
- prove stale rebase requires explicit reconfirmation before current-request satisfaction

Backend, performance, and security requirements:
- keep blob-write operations idempotent and resumable under the one documented wire profile
- never let raw upload bytes traverse `POST /v1/commands`
- preserve frozen request-lane lineage and fail closed on cross-tenant, cross-client, or stale-request allocation attempts
- isolate storage refs, resume tokens, and status reads by authenticated session/access boundary

Validation rules:
- validate `ClientUploadSession` responses against schema
- validate grouped request-binding contracts against `upload_request_binding_contract.schema.json`
- validate deterministic recovery-harness fixtures against `upload_session_recovery_harness.schema.json`
- unit-test state transitions, duplicate suppression, chronology monotonicity, and stale rebase handling
- integration-test allocate/put/get, duplicate retry, rebase/reconfirm, and cross-device continuation
- API-test the shipped routes with Playwright

Definition of done:
- the repo exposes governed upload allocation/blob/status routes
- upload sessions preserve frozen request identity, resumability, and recovery posture deterministically
- duplicate session/storage creation and silent stale rebinding are prevented
- portal and automation clients can resume the same lawful upload session across reconnect and rebase
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
