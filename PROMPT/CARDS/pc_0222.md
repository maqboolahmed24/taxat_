---
id: pc_0222
slug: phase_04_parallel_wave_02_track_backend_release_resilience_004_implement_schema_bundle_compatibility_gate_contract_and_release_verification_manifest_assembly
mode: parallel
phase: "04"
wave: "02"
track: "backend_release_resilience"
task_number: "004"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_release_resilience_0004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0222

## Task Summary

004 implement schema bundle compatibility gate contract and release verification manifest assembly.

## Metadata

- ID: `pc_0222`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend release resilience`
- Task Number: `004`
- Source Slug: `phase_04_parallel_wave_02_track_backend_release_resilience_004_implement_schema_bundle_compatibility_gate_contract_and_release_verification_manifest_assembly`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0221](./pc_0221.md) `phase_04_parallel_wave_02_track_backend_release_resilience_003_implement_schema_migration_ledger_and_reader_window_blocking_rules`
- Next: [pc_0223](./pc_0223.md) `phase_04_parallel_wave_02_track_backend_release_resilience_005_implement_verification_suite_result_and_gate_admissibility_record_persistence`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0222_to_0229.md](../shared_operating_contract_0222_to_0229.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_release_resilience_004_implement_schema_bundle_compatibility_gate_contract_and_release_verification_manifest_assembly`.

Inherit the shared operating contract and consume outputs from cards `pc_0219`, `pc_0220`, and `pc_0221`, plus any earlier replay/recovery work if it exists.

Mission:
Implement the canonical backend models, canonical hash builders, deterministic gate-order logic, and assembly services for `SchemaBundleCompatibilityGateContract` and `ReleaseVerificationManifestAssemblyContract`.
This task is not allowed to treat schema safety as a suite-local status bit, to assemble promotion evidence from CI dashboards, or to let a stable `candidate_identity_hash` masquerade as proof that the reader window and native client window are still safe.

Source algorithm grounding you must honor:
- `release_candidate_identity_and_promotion_evidence_contract.md`, end to end.
- `deployment_and_resilience_contract.md`, especially sections 2, 3, 5, and 6.
- `verification_and_release_gates.md`, especially section 2, gate admissibility rules, and evidence required for promotion.
- `data_model.md`, especially `SchemaBundleCompatibilityGateContract`, `ReleaseVerificationManifestAssemblyContract`, `ReleaseVerificationManifest`, `DeploymentRelease`, and `SchemaReaderWindowContract`.
- `modules.md`, especially the compatibility-boundary notes and `ASSEMBLE_RELEASE_VERIFICATION_MANIFEST(...)`.
- `schemas/schema_bundle_compatibility_gate_contract.schema.json`, `schemas/release_verification_manifest_assembly_contract.schema.json`, `schemas/release_candidate_identity_contract.schema.json`, `schemas/schema_reader_window_contract.schema.json`, and the sample payloads for gate and assembly contracts.
- `scripts/validate_contracts.py`, especially `validate_schema_bundle_compatibility_gate_contract`, `derive_schema_bundle_compatibility_gate_hash`, `validate_release_verification_manifest_assembly_contract`, `derive_release_verification_manifest_assembly_contract_hash`, and the custom drift checks named in the shared operating contract.
- `tools/forensic_contract_guard.py` anywhere it pins gate-order or contract-binding posture.
- `test_vectors.md`, especially `TV-80`, `TV-80A`, `TV-80B`, `TV-80C`, and `TV-81` through `TV-81D`.

Deliverables to create:
1. `packages/backend-release/src/models/schema_bundle_compatibility_gate_contract.ts`
2. `packages/backend-release/src/models/release_verification_manifest_assembly_contract.ts`
3. `packages/backend-release/src/services/derive_schema_bundle_compatibility_gate_hash.ts`
4. `packages/backend-release/src/services/derive_release_verification_manifest_assembly_contract_hash.ts`
5. `packages/backend-release/src/services/derive_release_gate_bindings.ts`
6. `packages/backend-release/src/services/assemble_schema_bundle_compatibility_gate_contract.ts`
7. `packages/backend-release/src/services/assemble_release_verification_manifest_assembly_contract.ts`
8. `packages/backend-release/src/repositories/release_verification_manifest_assembly_repository.ts`
9. `packages/backend-release/src/tests/schema_bundle_compatibility_gate_contract.spec.ts`
10. `packages/backend-release/src/tests/release_verification_manifest_assembly_contract.spec.ts`
11. `docs/release/schema_bundle_compatibility_gate_and_manifest_assembly.md`
12. Optional but recommended: `apps/admin-console-web/src/routes/debug/release/verification-manifest-assembly-lab.tsx`

You MUST patch any existing code path that currently:
- derives schema-safety posture from `candidate_identity_hash` alone,
- assembles promotion evidence from loose suite results without one deterministic gate-order tape,
- fails to mirror gate rows between top-level manifest rows and the assembly contract,
- or treats native client compatibility as independent from the shared compatibility gate.

Required design decisions you must make explicitly:
- define the canonical hash input for `compatibility_gate_hash`
- define the canonical blocking gate order and represent it as one reusable constant
- define how `reader_window_state`, `historical_manifest_guard_state`, `replay_restore_guard_state`, `native_client_window_state`, `migration_chronology_state`, `destructive_contract_state`, and `rollback_boundary_state` are derived from existing persisted release/migration facts
- define how green supporting gates force companion refs (`canary_summary_ref`, `restore_drill_ref`, `restore_checkpoint_ref`, `client_compatibility_matrix_ref`, `deterministic_golden_pack_ref_or_null`, `authority_sandbox_coverage_hash_or_null`) into the assembly basis
- define how the assembly contract records approval, deployment, and supersession posture without consulting deploy logs or operator summaries

Execution requirements:
- persist `SchemaBundleCompatibilityGateContract` as the shared schema-safety boundary across suite results, admissibility records, manifests, client matrices, and deployment releases
- derive `compatibility_gate_hash` from the exact candidate contract plus compatibility-window contract and not from any looser subset
- persist `ReleaseVerificationManifestAssemblyContract` as the deterministic release-assembly tape even before the final top-level manifest is serialized
- encode canonical gate order exactly as the algorithm sample does: `schema_compatibility`, `deterministic_and_state_machine`, `northbound_api`, `authority_sandbox`, `operator_client`, `security`, `performance_and_canary`, `restore_drill`, `migration_verification`, `supply_chain`, `suite_admissibility`
- mirror every gate row field the validator expects, including suite family, candidate hash, compatibility gate hash, authority sandbox coverage hash, result ref, admissibility ref, status, admissibility state, quarantine state, manual waiver state, and `executed_at`
- ensure blocked assembly posture cannot coexist with an all-green gate list
- ensure green operator-client, performance/canary, restore-drill, deterministic/state-machine, and authority-sandbox rows each keep their required companion evidence refs or hashes

Gap handling you must close while implementing:
- close the gap where a stable binary candidate could reuse stale schema-compatibility evidence after the reader window narrowed
- close the gap where promotion evidence is assembled from inconsistent gate order or mismatched `result_ref` / `admissibility_ref` pairs
- close the gap where native supported-client posture can drift away from the shared compatibility gate
- close the gap where approval/supersession posture is inferred after the fact instead of frozen in the assembly contract

Mandatory edge cases:
- the same build/config/provider-profile tuple with a different reader-window state MUST produce a different `compatibility_gate_hash`
- green schema, migration, and operator-client gate bindings MUST keep a non-null `compatibility_gate_hash_or_null`
- green authority-sandbox gate bindings MUST keep a non-null `authority_sandbox_coverage_hash_or_null`
- green operator-client, performance/canary, restore-drill, and deterministic/state-machine rows MUST fail closed if their companion evidence refs are missing
- a blocked assembly contract MUST retain at least one blocking red/inadmissible gate
- supersession MUST be explicit and never a post-hoc overwrite

Playwright and contract-test requirements:
- add direct tests for canonical gate order, hash determinism, companion-evidence enforcement, and blocked/all-green rejection
- if you expose or patch an internal release/debug route, add Playwright `APIRequestContext` coverage for compatibility-gate retrieval and manifest-assembly retrieval
- if you add the optional lab, add Playwright browser tests for semantic navigation, ordered gate rendering, reduced-motion-safe expansion, and sidecar focus using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- use a three-band layout: candidate-and-compatibility ribbon, ordered gate assembly tape, narrow companion-evidence sidecar
- emphasize causality and exact binding, not generic release KPIs
- render gate order as a ledger/tape, not a chart wall
- respect the shared palette, typography, and motion rules from the tranche contract

Backend, performance, and security requirements:
- hashing must be deterministic, side-effect free, and canonicalized for ordered arrays
- compatibility and assembly contracts must remain immutable once published for a given evidence snapshot; supersession happens through new artifacts, not mutation in place
- repositories must preserve chronology and evidence lineage instead of overwriting prior assembly states
- do not trust deploy-time environment strings, CI summaries, or dashboard state as release truth

Validation rules:
- validate every `SchemaBundleCompatibilityGateContract` and `ReleaseVerificationManifestAssemblyContract` payload against schema before persistence
- update fixtures/builders so all relevant validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test`
- rerun `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one authoritative compatibility-gate model and one authoritative manifest-assembly tape
- hash derivation, gate order, companion-evidence rules, and decision posture are durable and test-covered
- any optional lab is read-only and mirrors persisted truth rather than implementing release logic in the browser
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, the relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.