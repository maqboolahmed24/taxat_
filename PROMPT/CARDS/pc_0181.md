---
id: pc_0181
slug: phase_04_parallel_wave_02_track_backend_portal_003_implement_client_upload_session_identity_freeze_request_binding_and_rebase_logic
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "003"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0181

## Task Summary

implement client upload session identity freeze request binding and rebase logic.

## Metadata

- ID: `pc_0181`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `003`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_003_implement_client_upload_session_identity_freeze_request_binding_and_rebase_logic`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0180](./pc_0180.md) `phase_04_parallel_wave_02_track_backend_portal_002_implement_client_document_request_and_current_vs_historical_upload_projection_rules`
- Next: [pc_0182](./pc_0182.md) `phase_04_parallel_wave_02_track_backend_portal_004_implement_upload_session_recovery_harness_generation_and_cross_device_resume_support`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0174_to_0181.md](../shared_operating_contract_0174_to_0181.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_003_implement_client_upload_session_identity_freeze_request_binding_and_rebase_logic`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0180` if they exist, especially `pc_0180`, `pc_0179`, `pc_0164`, and the continuity/cache-isolation/stale-guard work from earlier phases.

Mission:
Implement the governed `ClientUploadSession` projector plus the reusable `UploadRequestBindingContract` logic that freezes request identity, preserves same-session lineage through lawful reconnect/rebase, and makes stale or superseded request binding explicit instead of silent.
This task is the semantic heart of portal upload continuity.
It must keep staged bytes, request meaning, resumability, attachment legality, confidence, and next-action posture separate and deterministic.
It must not swallow the later dedicated recovery-harness generation card.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially the `Documents` route upload rules and the validation minimum around mobile reconnect, rebased uploads, and current-vs-history posture.
- `modules.md`, especially the upload/session rules around request version, grouped binding contract, next action, confidence, current-request pointer, in-flight rebase, and session reuse.
- `data_model.md`, especially `ClientUploadSession`, `UploadRequestBindingContract`, and the portal workspace document-route semantics.
- `state_machines.md`, especially the client upload rules and the cross-state invariants involving `ClientUploadSession` and `ClientDocumentRequest`.
- `compute_parity_and_trust_formulas.md`, especially the frozen `upload_confidence_score` formula and recovery-mode derivation.
- `schemas/client_upload_session.schema.json`, `schemas/upload_request_binding_contract.schema.json`, `schemas/upload_session_recovery_harness.schema.json`, and `schemas/problem_envelope.schema.json` when northbound failures are touched.
- `PATCH_RESOLUTION_INDEX.md`, especially items 57-61, 265-266, and 417-420.
- `test_vectors.md`, especially TV-26, TV-27A, TV-27B, TV-27C, and TV-27F.

Deliverables to create:
1. `packages/backend-portal/src/projectors/build_client_upload_session.ts`
2. `packages/backend-portal/src/services/derive_upload_request_binding_contract.ts`
3. `packages/backend-portal/src/services/derive_upload_confidence_score.ts`
4. `packages/backend-portal/src/services/derive_upload_recovery_posture_and_next_action.ts`
5. `packages/backend-portal/src/services/reconcile_inflight_request_rebase.ts`
6. `packages/backend-portal/src/services/validate_upload_session_chronology_and_scope.ts`
7. `packages/backend-portal/src/tests/client_upload_session_binding_and_rebase.spec.ts`
8. `docs/portal/client_upload_session_identity_freeze_and_rebase.md`
9. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/upload-rebase-lab.tsx`

You MUST patch any existing upload-control northbound adapters or document-route builders so they consume these projectors/services rather than rebuilding next-action, recovery, or binding posture locally.

Required design decisions you must make explicitly:
- define the exact composition of `frozen_binding_scope_hash` and the minimal stable identity basis for duplicate-session prevention
- define when `request_binding_state` becomes `ORIGINAL_CURRENT`, `RECONFIRMED_CURRENT`, `RECONFIRMATION_REQUIRED`, or `SUPERSEDED`
- define how in-flight request rebase preserves the same upload session and storage lineage without silently rebinding bytes to the newer request version
- define where the frozen confidence formula lives and how you prevent duplicate implementations in UI or route layers
- define how `next_action_code` and `recovery_posture` are derived from durable blocker state rather than from previously published values

Execution requirements:
- publish a valid `UploadRequestBindingContract` for every governed client upload session
- keep frozen request identity, frozen request version, live request version, binding-resolution basis, duplicate-session posture, duplicate-file posture, in-flight rebase posture, stale-completion prohibition, attachment authority, and cross-device resume posture explicit on that grouped contract
- preserve the same `upload_session_id` and `storage_ref` through lawful reconnect, retry, or cross-device resume when the contract requires session reuse
- compute `upload_confidence_score` from the frozen formula and hard fail-closed overrides; do not leave it as UI guesswork
- derive `recovery_posture` from concrete blocker state and `next_action_code`, not from a previously stored recovery string
- keep transfer, integrity, malware scan, validation, request binding, resumability, and attachment state distinct
- accepted stale uploads may advance only to explicit rebind-required posture after transfer settles; in-flight rebase must preserve staged transfer continuity
- do not implement the full `UploadSessionRecoveryHarness` suite here; publish only the core session and binding logic the later harness card will consume
- if you add the lab, make it a quiet lineage and rebase explainer with one upload session timeline and one binding strip; no dashboard wall

Gap handling you must close while implementing:
- close empty-string and weak-identity drift across tenant/client/request/version/storage fields
- close chronology drift across transfer, scan, validation, finalize, attachment confirmation, and reconfirmation timestamps
- close the gap where accepted or attached posture can coexist with unverified integrity, incomplete bytes, low confidence, stale request binding, or the wrong next action
- close duplicate allocation drift so refresh or cross-device resume does not mint a second current-row upload session
- close recovery posture drift so the portal never has to guess whether to resume, reconfirm, restart, or contact support

Mandatory edge cases:
- no `ClientUploadSession` may become attached while `request_binding_state in {RECONFIRMATION_REQUIRED, SUPERSEDED}`
- `upload_confidence_score` MUST be zero on integrity failure or quarantined scan
- `request_binding_state = SUPERSEDED` MUST cap confidence and forbid stale bytes from satisfying the current request
- `attachment_state = ATTACHED` MUST imply `integrity_state = VERIFIED`, `bytes_transferred = byte_count`, confidence above the governed threshold, and no recovery posture blocker
- `recovery_posture` MUST be derived from `next_action_code` and binding state, not copied forward blindly
- in-flight rebase must preserve the same governed session and storage lineage until transfer terminates
- cross-device resume must reuse the existing session when `resume_identity_policy = RESUME_EXISTING_SESSION_ONLY`
- all timestamp sequences must remain monotonic where the source contract requires chronology

Playwright and contract-test requirements:
- unit-test binding projection, confidence calculation, recovery-mode derivation, chronology validation, and in-flight rebase handling directly
- patch or add Playwright APIRequestContext assertions against any shipped upload-control endpoints if they now expose this logic
- if you add the upload-rebase lab, add Playwright browser tests that use file chooser or synthetic upload input with semantic locators, and prove rebase/reconnect posture without fixed sleeps
- capture request/session payloads and traces on failures so binding regressions are diagnosable

Backend, performance, and security requirements:
- keep request-binding, duplicate-session, and attachment-authority logic server-side and deterministic
- do not let client-local cache, retry wrappers, or transport retries mint a second authoritative upload meaning
- keep session lookups indexed by tenant/client/request/frozen-version/session identity so resume paths do not degrade into broad scans
- fail closed on identity-scope mismatch, stale binding drift, or ambiguous attachment legality

Validation rules:
- validate every emitted `ClientUploadSession` and `UploadRequestBindingContract` against schema
- add regression coverage for in-flight rebase, explicit reconfirmation, supersession, duplicate retry reuse, confidence thresholds, and chronology rules
- keep the session projector reusable by the later `UploadSessionRecoveryHarness` card rather than embedding test-only logic here

Definition of done:
- the repo has one authoritative client-upload-session projector and one reusable request-binding contract projector
- request rebases, reconnects, and cross-device resume preserve lawful session lineage without silent rebinding
- confidence, next action, and recovery posture become governed backend truth instead of UI guesswork
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

## Implementation Notes

- Added the authoritative portal upload session projector in `packages/backend-portal/src/projectors/build_client_upload_session.ts`.
- Added reusable services for upload request binding, confidence scoring, recovery/next-action derivation, in-flight rebase reconciliation, and chronology/scope validation.
- Aligned `UploadRequestBindingContract` identity with the canonical validator: `request_identity_ref` mirrors `request_id`, and `frozen_binding_scope_hash` is the literal `tenant_id|client_id|request_id|frozen_request_version_ref` tuple.
- Patched the domain upload binding reader and northbound upload-control adapters so allocation, duplicate retry, blob progress, status rebase, and attachment confirmation consume the portal-owned binding/posture logic instead of locally rebuilding recovery state.
- Patched document upload-row projection and the portal publication guard so stale/rebased historical rows may retain their frozen upload request version while current/reconfirmed rows must mirror the parent request version.
- Added `packages/backend-portal/src/tests/client_upload_session_binding_and_rebase.spec.ts` and the unit wrapper under `tests/unit/backend_portal/`.
- Added `docs/portal/client_upload_session_identity_freeze_and_rebase.md`.
- Did not add the optional upload-rebase browser lab; this card is backend-first and the later recovery-harness card owns the broader continuation proof surface.

Design decisions:

- `ORIGINAL_CURRENT` means frozen and live request versions match.
- `RECONFIRMATION_REQUIRED` means live request version advanced while the upload remains frozen to the original request version.
- `RECONFIRMED_CURRENT` requires explicit reconfirmation and a non-null `reconfirmed_at`.
- `SUPERSEDED` keeps the upload as stale history, caps confidence, and cannot satisfy the current request.
- In-flight rebase preserves `upload_session_id`, `storage_ref`, and frozen `request_version_ref`; only live request version and binding posture change.
- `upload_confidence_score` lives in `derive_upload_confidence_score.ts`; route/UI layers consume the score and do not recompute it.
- `next_action_code`, `recovery_posture`, `attachment_state`, and `dominant_hazard_code` are derived together from durable blocker state in `derive_upload_recovery_posture_and_next_action.ts`.

Verification evidence:

- `node --experimental-strip-types -e "import('./packages/backend-portal/src/index.ts').then((m)=>{ const s=m.buildClientUploadSession({tenantId:'tenant.taxat-upload',clientId:'client.taxat-upload',requestId:'request.upload.bank-statement',frozenRequestVersionRef:'request-version.bank-statement.v1',uploadSessionId:'upload-session.demo',storageRef:'storage.upload-staging.demo',filename:'bank-statement.pdf',mediaType:'application/pdf',byteCount:12,checksum:'checksum.demo',now:'2026-05-04T09:00:00Z'}); console.log(s.request_binding_state, s.next_action_code, s.upload_request_binding_contract.frozen_binding_scope_hash, s.upload_confidence_score); })"` passed.
- `pnpm exec playwright test --config=playwright.config.ts --project=unit tests/unit/backend_portal/client_upload_session_binding_and_rebase.spec.ts` passed: 7 tests.
- `pnpm exec playwright test --config=playwright.config.ts --project=unit tests/unit/backend_portal/client_document_request_projection.spec.ts tests/unit/backend_portal/client_portal_workspace_route_derivatives.spec.ts tests/unit/backend_portal/client_upload_session_binding_and_rebase.spec.ts` passed: 16 tests.
- `pnpm exec playwright test --config=playwright.config.ts --project=unit tests/unit/backend_northbound/upload_request_binding_contract.spec.ts tests/unit/backend_northbound/client_upload_session_contract.spec.ts` passed: 8 tests.
- `pnpm exec playwright test --config=playwright.config.ts --project=api tests/playwright_api/northbound/upload_session_routes.spec.ts` passed: 2 tests.
- `pnpm exec playwright test --config=playwright.config.ts --project=api tests/playwright_api/northbound/client_portal_read_routes.spec.ts` passed: 2 tests.
- `./.venv/bin/python3 ./packages/contracts-core/python/validate_contracts.py --self-test` passed.
- `./.venv/bin/python3 ./Algorithm/scripts/validate_contracts.py --self-test` passed.
- `./.venv/bin/python3 ./Algorithm/tools/forensic_contract_guard.py` passed.
- Attempted `pnpm exec playwright test --config=playwright.config.ts --project=unit tests/integration/backend_northbound/upload_session_recovery_harness.spec.ts`; Playwright reported no tests found because `tests/integration/**` is outside this config's active project globs.
