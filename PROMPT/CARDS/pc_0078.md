---
id: pc_0078
slug: phase_02_seq_078_create_local_development_stack_with_database_object_store_queue_and_cache
mode: sequential
phase: "02"
wave: null
track: null
task_number: "078"
block_key: phase_02_seq
original_key: phase_02_seq_078
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0078

## Task Summary

create local development stack with database object store queue and cache.

## Metadata

- ID: `pc_0078`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `078`
- Source Slug: `phase_02_seq_078_create_local_development_stack_with_database_object_store_queue_and_cache`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0077](./pc_0077.md) `phase_02_seq_077_create_synthetic_seed_data_fixture_pack_and_canonical_domain_examples`
- Next: [pc_0079](./pc_0079.md) `phase_02_seq_079_create_ephemeral_test_environment_bootstrap_and_data_reset_workflows`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0078_to_0085.md](../shared_operating_contract_0078_to_0085.md)
```text
Execute roadmap task `phase_02_seq_078_create_local_development_stack_with_database_object_store_queue_and_cache`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0077` if they exist, especially `pc_0019`, `pc_0020`, `pc_0021`, `pc_0028`, `pc_0029`, `pc_0030`, `pc_0031`, `pc_0033`, `pc_0049`, `pc_0050`, `pc_0051`, `pc_0052`, `pc_0053`, `pc_0054`, `pc_0056`, `pc_0057`, `pc_0059`, `pc_0063`, `pc_0067`, `pc_0068`, `pc_0069`, `pc_0070`, `pc_0071`, `pc_0072`, `pc_0073`, `pc_0074`, `pc_0075`, `pc_0076`, and `pc_0077`.

Mission:
Create the canonical local Taxat runtime so one developer command can boot a faithful, security-aware, semantically correct development environment with a control store, append-only audit store, object storage, queue/broker, cache/resume store, and seeded fixture basis.
This is not just Docker plumbing.
You must encode the corpus' durable-versus-disposable boundary, local secret posture, bootstrap ordering, seed loading, health gating, and rebuild safety.
The result should let later agents run the product locally without inventing infrastructure assumptions from memory.

Source algorithm grounding you must honor:
- `deployment_and_resilience_contract.md`, especially `1. Reference runtime topology`, `4. Recovery tiers and targets`, `5. Backup, restore, and DR rules`, and `8. Release and resilience invariants`.
- `security_and_runtime_hardening_contract.md`, especially threat classes, secret and token handling, cache safety, and network hardening requirements.
- `replay_and_reproducibility_contract.md`, especially immutable execution basis, deterministic seed basis, mutation isolation, and deterministic golden-fixture boundaries.
- `observability_and_audit_contract.md` and `audit_and_provenance.md`, especially the separation between operational telemetry and append-only audit truth.
- `manifest_and_config_freeze_contract.md`, `config_freeze_inheritance_and_consumption_contract.md`, and `manifest_start_claim_protocol.md`, especially the rule that workers consume frozen execution truth and that local dev shortcuts must not bypass freeze semantics.
- `cache_isolation_and_secure_reuse_contract.md` and `native_cache_hydration_purge_and_rebase_contract.md`, especially exact isolation and purge behavior.
- The previously established queue, object-store, config, telemetry, and fixture foundations from `pc_0049` through `pc_0077`.

Deliverables to create:
1. `infra/local/compose.yaml`.
2. `infra/local/local_runtime_topology.json`.
3. `infra/local/service_boot_order.json`.
4. `infra/local/service_health_contract.json`.
5. `config/runtime/local/provider_overrides.json`.
6. `config/runtime/local/local_seed_profiles.json`.
7. `scripts/local/bootstrap_local_stack.sh`.
8. `scripts/local/wait_for_local_stack_ready.py`.
9. `scripts/local/seed_local_runtime.py`.
10. `scripts/local/smoke_local_runtime.py`.
11. `docs/runtime/local_development_stack.md`.
12. `apps/operator-web/src/routes/internal/local-runtime-observatory.tsx`.
13. `tests/integration/runtime/local_stack_smoke.spec.ts`.
14. `tests/playwright/internal/local_runtime_observatory.spec.ts`.

Required design decisions you must make explicit:
- Define which services are mandatory locally and which remain optional compatibility adapters behind feature flags.
- Preserve the corpus distinction between durable truth and disposable helpers: control-store records, append-only audit records, object-store artifacts, manifests, inbox/outbox rows, and session/access records are durable; queue state, cache entries, projections, and generated indexes are disposable.
- Decide whether the control store and audit store are separate databases, separate logical schemas, or separate clusters locally; justify the choice against prior ADRs.
- Define the local object-store bucket taxonomy, queue namespaces, cache namespaces, and telemetry endpoints so they match earlier cards instead of drifting.
- Define a deterministic bootstrap order that brings up stores, applies migrations, seeds baseline fixtures, verifies contracts, and only then starts app processes and workers.
- Define a secret posture for local development that never embeds live HMRC or production credentials in compose files or seed scripts.
- Decide how local health checks prove semantic readiness, not just open TCP ports.
- Define how a developer can rebuild disposable services safely without destroying durable local truth unless they intentionally request a full reset.

Execution steps:
1. Build a machine-readable topology file that labels every local service by role, provider family, persistence class, secret class, port exposure, and rebuild posture.
2. Implement a compose stack that boots the agreed local database, object store, queue/broker, cache/resume store, and any required app-side support services.
3. Implement bootstrap helpers that verify prerequisites, materialize env defaults, wait for readiness, apply migrations, create buckets/queues/cache namespaces, and load the deterministic seed pack from `pc_0077`.
4. Implement semantic health checks that confirm schema compatibility, bucket existence, queue reachability, cache isolation, audit appendability, and seed-profile integrity.
5. Implement a local smoke runner that exercises one sealed command path, one queue path, one object-store write path, one cache/write-read invalidation path, and one audit append path.
6. Build `local-runtime-observatory` as a read-only runtime topology desk showing service classes, durable/disposable posture, health, seed profile, and rebuild guidance.
7. Add integration tests for cold boot, warm boot, missing-volume recovery, queue/cache rebuild, and deterministic reseeding.
8. Add Playwright coverage for the observatory route using seeded local data and semantic locators only.

Visual and interaction specification for `local-runtime-observatory`:
- Tone: a premium machine-room field guide.
  The page should feel like a technical operations folio, not a Kubernetes dashboard clone.
- Layout:
  - `64px` top bar with environment badge, seed-profile chip, validator status, and a subtle stack glyph.
  - left topology rail `248-264px` listing `CONTROL STORE`, `AUDIT STORE`, `OBJECT STORAGE`, `QUEUE`, `CACHE`, `APP`, `WORKERS`.
  - center canvas showing a quiet service-map diagram with directional flows and durable/disposable badges.
  - right inspection rail `360-400px` showing selected service contract, bootstrap dependencies, storage class, and rebuild instructions.
- Diagram and visual motif:
  - use a monoline “topology loom” graphic: boxes aligned on a soft grid with thin connective routes and arrowheads only where causality matters.
  - durable nodes should use solid rule emphasis; disposable nodes should use lighter rule weight and a reusable icon treatment.
  - include a small custom stack sigil near the heading, built from nested rectangles and one anchor dot.
- Palette:
  - background `#F5F4EF`
  - primary surface `#FFFFFF`
  - secondary surface `#ECEEE8`
  - ink `#11161A`
  - muted ink `#67717A`
  - hairline `rgba(17,22,26,0.08)`
  - accent slate `#4D5C6A`
  - accent moss `#5A6B59`
  - accent brass `#8A6A44`
  - success `#17614B`
  - warning `#8C5B1B`
  - danger `#A63B31`
- Typography:
  - typography-led and restrained.
  - use the product sans stack for all prose and labels.
  - reserve monospace for ports, queue names, bucket refs, schema bundle hashes, and readiness codes.
- Dimensional constraints:
  - max page width `1480px`
  - topology row height `68px`
  - service card min width `220px`
  - inspector property rows `34px`
- Motion:
  - `120ms` service selection fade/slide.
  - `160ms` path emphasis animation on selection only.
  - no ambient animation loops.
  - reduced-motion mode swaps path animation for instant outline and background changes.
- Interaction:
  - selecting a service must update the right rail with exact dependencies, bootstrap order, persistence class, and safe-rebuild instructions.
  - selecting a connection should explain whether the edge is durable truth, derived transport, or disposable acceleration.
  - accessible names should read as statements such as `queue service is disposable delivery fabric and rebuilds from outbox truth`.
  - Playwright must assert that health chips, topology labels, and rebuild instructions render deterministically under seeded local state.

Component hierarchy hint:
~~~ts
<InternalShell route="/internal/local-runtime-observatory">
  <TopologyRail />
  <ServiceMapCanvas />
  <RuntimeInspector />
</InternalShell>
~~~

Gap handling you must perform:
- Close the gap where the corpus defines runtime topology and recovery law but not a portable developer bootstrap.
- Close the gap where a local stack could accidentally treat queue state or cache state as durable truth.
- Close the gap where local seed loading is undocumented or non-deterministic.
- Close the gap where developers can boot ports successfully but still miss schema drift, bucket setup, or audit appendability.
- Close the gap where local secrets are scattered across ad hoc env files without lineage or safety rules.

Mandatory edge cases:
- first boot on a clean machine with no volumes
- warm boot with durable data present and disposable services emptied
- broker unavailable while control store and object store are healthy
- cache namespace drift requiring purge and rebuild
- object-store bucket missing after partial reset
- local schema bundle mismatch after a contract update
- developer requests full reset but forgets to re-seed deterministic fixtures

Backend, performance, and security requirements:
- Every local secret must come from env injection, local secret wrappers, or generated dev-only placeholders; never commit live credentials.
- The compose stack must support repeated up/down cycles without corrupting durable tables or object-store metadata.
- Local runtime helpers must fail closed when the environment profile is not `local`, `devcontainer`, or an explicitly whitelisted sandbox profile.
- App and worker containers must not start until bootstrap preconditions and migrations are satisfied.
- Queue, cache, and local projections may be purged and rebuilt; control-store, audit-store, and object-store reset must require explicit confirmation flags.
- The observatory route is read-only and must not expose live secret values, signed URLs, raw tokens, or container-console controls.

Validation rules:
- Unit-test topology parsing, health-contract evaluation, and durable-versus-disposable classification.
- Integration-test cold bootstrap, warm restart, missing service recovery, bucket creation, queue namespace creation, and deterministic reseed behavior.
- Run the authoritative corpus validators as part of the local bootstrap smoke flow.
- Use Playwright to verify the observatory route, keyboard traversal, reduced-motion behavior, and semantic labels.
- Prove that queue and cache purges do not destroy durable truth, and that a durable reset path requires explicit opt-in.

Definition of done:
- One canonical bootstrap command can start a faithful local Taxat stack.
- The stack respects the corpus' topology and durable/disposable boundary.
- Developers can seed, smoke-test, inspect, and safely rebuild the environment without handcrafting infra steps.
- The observatory route explains the runtime clearly without becoming a generic ops dashboard.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

