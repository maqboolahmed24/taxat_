---
id: pc_0169
slug: phase_04_parallel_wave_02_track_backend_low_noise_001_implement_low_noise_experience_frame_projector_and_surface_budget_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_low_noise"
task_number: "001"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_low_noise_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0169

## Task Summary

implement low noise experience frame projector and surface budget rules.

## Metadata

- ID: `pc_0169`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend low noise`
- Task Number: `001`
- Source Slug: `phase_04_parallel_wave_02_track_backend_low_noise_001_implement_low_noise_experience_frame_projector_and_surface_budget_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0168](./pc_0168.md) `phase_03_parallel_wave_01_track_backend_northbound_012_implement_access_rebind_resume_token_and_shell_stability_guard_failure_paths`
- Next: [pc_0170](./pc_0170.md) `phase_04_parallel_wave_02_track_backend_low_noise_002_implement_context_bar_state_decision_summary_state_and_action_strip_state_projectors`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0166_to_0173.md](../shared_operating_contract_0166_to_0173.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_low_noise_001_implement_low_noise_experience_frame_projector_and_surface_budget_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0168` if they exist, especially `pc_0070` through `pc_0077`, `pc_0118` through `pc_0127`, `pc_0159` through `pc_0160`, and `pc_0168`.

Mission:
Implement the first server-authored `LowNoiseExperienceFrame` projector that turns already-published manifest truth, trust, workflow, and recovery state into one schema-valid calm-shell frame with explicit surface order, grouped continuity markers, and low-noise budget rules.
This task is the top-level frame assembly boundary. It must not let the frontend derive shell shape heuristically, and it must not dissolve the calm shell into ad hoc cards or dashboard widgets.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md`, especially the default visible shell, budget formulas, non-material refresh continuity cost, and publication rules.
- `low_noise_surface_compression_and_noise_budget_audit_contract.md`.
- `dominant_question_and_single_action_contract.md`.
- `frontend_shell_and_interaction_law.md`, especially route continuity, promoted support-region law, and grouped stability primitives.
- `modules.md`, especially `BUILD_LIVE_EXPERIENCE_FRAME(...)`.
- `data_model.md`, especially `LowNoiseExperienceFrame` and related shell/read-side contracts.
- `schemas/low_noise_experience_frame.schema.json`, `schemas/low_noise_budget_audit.schema.json`, `schemas/shell_state_taxonomy_contract.schema.json`, and `schemas/experience_delta.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 404-406 and 411-413.

Deliverables to create:
1. `packages/backend-low-noise/src/projectors/build_low_noise_experience_frame.ts`
2. `packages/backend-low-noise/src/services/derive_surface_budget_contract.ts`
3. `packages/backend-low-noise/src/services/validate_published_surface_order.ts`
4. `packages/backend-low-noise/src/services/build_minimal_dominance_contract_adapter.ts`
5. `packages/backend-low-noise/src/services/build_minimal_shell_state_taxonomy_adapter.ts`
6. `packages/backend-low-noise/src/services/validate_low_noise_frame_publication.ts`
7. `packages/backend-low-noise/src/tests/low_noise_experience_frame_projector.spec.ts`
8. `docs/low_noise/experience_frame_projector.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/low-noise/frame-lab.tsx`

Required design decisions you must make explicitly:
- define the canonical upstream inputs to the frame projector: which bundle, trust, workflow, queue, recovery, and continuity artifacts are authoritative, and which renderer-local hints are forbidden
- define how the frame obtains `dominance_contract` and `state_taxonomy_contract` right now without swallowing the full dedicated follow-up task; if the later card owns the reusable abstraction, create a narrow adapter seam here and make that seam explicit
- define the canonical cache key or partition key for frame publication so stale/masked/access-incompatible frames never hydrate into the wrong session or shell
- define how recovery posture, settlement state, and route continuity are surfaced in-frame rather than through extra peer surfaces

Execution requirements:
- publish only `CONTEXT_BAR`, `DECISION_SUMMARY`, `ACTION_STRIP`, and `DETAIL_DRAWER` as peer top-level surfaces when `experience_profile = LOW_NOISE`
- keep `shell_route_key`, `object_anchor_ref`, `focus_anchor_ref`, `shell_stability_token`, grouped continuity contracts, and truth-boundary contracts explicit on the frame
- compute and attach `low_noise_budget_audit` from the published payload before release
- if a frame candidate exceeds budget or violates four-surface discipline, collapse lower-ranked warnings/actions/detail entries rather than publishing an over-budget frame
- if you add the optional lab, make it a server-truth preview shell, not a divergent frontend mock

Gap handling you must close while implementing:
- the schema expects `dominance_contract` and `state_taxonomy_contract` before the dedicated follow-up card exists; introduce narrow, replaceable adapters rather than ad hoc booleans or UI-only enums
- earlier manifest read work may publish the underlying truth artifacts but not a calm-shell assembly boundary; create that boundary now so the frontend stops composing shell semantics locally
- the low-noise contract freezes budget constants in prose and schema; surface them in one backend helper instead of scattering them across tests and UI previews
- compare/audit/detail richness can easily leak into the default first view; enforce the default calm-shell topology at the frame boundary

Mandatory edge cases:
- exact peer surface order must be `CONTEXT_BAR -> DECISION_SUMMARY -> ACTION_STRIP -> DETAIL_DRAWER`
- `shell_family` and `shell_route_key` must preserve same-shell continuity across refresh/reconnect/rebase
- the frame must tolerate inline recovery posture (`RECEIPT_PENDING`, `FRESHENING`, `RECOVERY_REQUIRED`, etc.) without remounting a second shell
- one promoted support region only by default; detail is subordinate, not a peer competitor
- no route-local heuristic salience or client-side dominant-question derivation is allowed

Playwright and contract-test requirements:
- unit/integration-test the projector directly and any northbound frame publication path that consumes it
- if you add the frame lab, test it with Playwright using semantic locators and reduced-motion coverage
- capture before/after frame payloads and budget audits on failures
- no fixed sleeps in refresh or preview tests

Backend, performance, and security requirements:
- keep the projector pure and deterministic from typed inputs; do not let view code mutate frame semantics post-publication
- make cache partitioning respect tenant, manifest, access scope, masking scope, and grouped stability boundaries
- do not join staff-only or masked-limited facts into customer-facing frame candidates
- keep projector cost bounded by reusing prior read artifacts instead of re-querying deep truth graphs for every render

Validation rules:
- validate every frame against `low_noise_experience_frame.schema.json`
- validate embedded `low_noise_budget_audit` against its schema
- unit-test four-surface ordering, continuity markers, recovery posture publication, and budget-threshold collapse
- integration-test frame generation from representative calm, warning, blocked, waiting, and degraded cases

Definition of done:
- the repo has one server-authored `LowNoiseExperienceFrame` projector
- calm-shell topology, grouped continuity, and budget rules stop being renderer folklore
- frame publication is deterministic, schema-valid, and ready for the next tranche of low-noise surface-specific projectors
- optional preview surfaces, if added, mirror backend truth instead of inventing their own shell logic.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

