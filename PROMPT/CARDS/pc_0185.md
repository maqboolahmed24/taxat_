---
id: pc_0185
slug: phase_04_parallel_wave_02_track_backend_portal_007_implement_client_timeline_event_projection_and_customer_safe_activity_compression
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "007"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0185

## Task Summary

implement client timeline event projection and customer safe activity compression.

## Metadata

- ID: `pc_0185`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `007`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_007_implement_client_timeline_event_projection_and_customer_safe_activity_compression`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0184](./pc_0184.md) `phase_04_parallel_wave_02_track_backend_portal_006_implement_client_onboarding_journey_projection_and_one_step_at_a_time_progression_rules`
- Next: [pc_0186](./pc_0186.md) `phase_04_parallel_wave_02_track_backend_portal_008_implement_portal_help_request_projection_and_boundary_handoff_tracking`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0182_to_0189.md](../shared_operating_contract_0182_to_0189.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_007_implement_client_timeline_event_projection_and_customer_safe_activity_compression`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0184` if they exist, especially `pc_0179`, `pc_0183`, and `pc_0184`.

Mission:
Implement the `ClientTimelineEvent` projector and the customer-safe activity-compression layer that powers recent activity in the portal without leaking staff-only reasoning, noisy churn, or false authority reassurance.
This task must turn raw internal workflow and authority movement into a bounded, plain-language client activity rail.
The timeline must stay newest-first, explicit about authority truth when authority is involved, and quiet enough that it supports the dominant task instead of becoming a social feed or an audit log.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially `Home`, `Read-model and API translation requirements`, the `activity_timeline[]` rules, customer-safe projection rules, and the Playwright validation minimum around request focus, recent activity ordering, and stale/reconnect stability.
- `data_model.md`, especially `ClientTimelineEvent` and the nested `authority_truth_contract` boundary.
- `modules.md`, especially `BUILD_CLIENT_PORTAL_WORKSPACE(...)` and `BUILD_CLIENT_TASK_QUEUE(...)` where recent activity remains subordinate to the dominant task.
- `frontend_shell_and_interaction_law.md`, especially dominant-action law, current-vs-history posture, and disclosure fencing.
- `schemas/client_timeline_event.schema.json`, `schemas/portal_language_contract.schema.json`, and any shared authority-truth schemas those events consume.
- `PATCH_RESOLUTION_INDEX.md`, especially the portal and customer-safe projection hardening entries that prevent internal-only state and noisy status churn from leaking into client flows.
- `test_vectors.md`, especially portal activity ordering, customer-safe wording, and authority-truth regressions.

Deliverables to create:
1. `packages/backend-portal/src/projectors/build_client_timeline_event.ts`
2. `packages/backend-portal/src/services/compress_client_activity_timeline.ts`
3. `packages/backend-portal/src/services/derive_client_timeline_headline.ts`
4. `packages/backend-portal/src/services/derive_client_timeline_authority_truth_posture.ts`
5. `packages/backend-portal/src/tests/client_timeline_event_projection.spec.ts`
6. `packages/backend-portal/src/tests/client_timeline_compression_and_ordering.spec.ts`
7. `docs/portal/client_timeline_event_projection_and_activity_compression.md`
8. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/activity-timeline-lab.tsx`

You MUST patch any existing portal home or route-level timeline builder so it consumes these services instead of composing recent activity in UI code.

Required design decisions you must make explicitly:
- define the deterministic mapping from internal event families to the limited `ClientTimelineEvent.event_kind` vocabulary
- define compression rules for repetitive upload churn, status churn, and closely spaced same-object updates so the client timeline remains bounded and meaningful
- define when `authority_truth_state` is `NOT_APPLICABLE`, `PENDING_ACK`, `UNKNOWN`, `CONFIRMED`, `REJECTED`, or `OUT_OF_BAND` in customer-visible events
- define how generic headline wording such as `Status updated` is permitted or forbidden depending on authority truth posture

Execution requirements:
- publish schema-valid `ClientTimelineEvent` artifacts for the visible portal timeline
- keep the event vocabulary bounded to the schema set and the customer-safe portal language contract
- keep the timeline newest-first by `occurred_at` and ensure `event_id` uniqueness
- ensure non-authority events retain `authority_truth_state = NOT_APPLICABLE`
- ensure `event_kind = SUBMISSION_SENT` retains `authority_truth_state = PENDING_ACK` until genuine confirmation or rejection is observed
- prevent customer-visible headlines from implying confirmed authority truth when the truth state is not confirmed
- keep detail refs and related-object refs anchored so timeline interactions reopen the same portal object rather than generic feeds or dead-end rows
- if you add the lab, make it a quiet chronology rail with grouped timestamps, subtle icons, and no more than one small supporting filter strip

Gap handling you must close while implementing:
- close any gap where raw internal activity or staff-only reasoning can leak into the client timeline
- close any gap where reconnect or repeated polling causes duplicate client events or unstable ordering
- close any gap where authority-related events collapse unresolved or out-of-band states into generic reassuring text
- close any gap where timeline detail rows can outgrow the bounded copy budget or compete with the dominant task surface

Mandatory edge cases:
- `UPLOAD_RECEIVED`, `UPLOAD_REJECTED`, `APPROVAL_READY`, `APPROVAL_SIGNED`, and `ONBOARDING_STEP_COMPLETED` must keep `authority_truth_state = NOT_APPLICABLE`
- `SUBMISSION_SENT` must keep `authority_truth_state = PENDING_ACK` until authority truth changes for real
- `STATUS_UPDATED` or `SUBMISSION_SENT` headlines must not contain confirming wording unless `authority_truth_state = CONFIRMED`
- `OUT_OF_BAND` authority updates must not collapse into a generic `Status updated` headline
- duplicate or reordered events under reconnect must compress deterministically and preserve stable newest-first order
- the visible timeline must stay customer-safe and must not publish audit, assignment, escalation, queue, or staff-role vocabulary

Playwright and contract-test requirements:
- add direct tests for authority-truth copy gating, event compression, ordering stability, and headline budget enforcement
- if timeline or workspace read routes exist, add Playwright APIRequestContext tests showing newest-first ordering and correct authority-truth serialization
- if you add the lab, add Playwright browser tests that verify grouped chronology, explicit authority state labels, and deep-link reopening using semantic locators only
- capture traces or serialized timeline payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- recent activity should render as a restrained chronology rail, not a feed wall or KPI module
- use background `#F6F4EE`, surface `#FFFFFF`, subtle dividers, and at most one accent per event row
- row layout: timestamp column or pill, headline, optional one-line detail, and one optional route affordance
- keep the home surface capped to a bounded event count; the timeline must support the dominant task rather than outrank it
- use explicit text labels for authority posture; color alone is not enough
- motion stays subtle and optional, 140-160ms max, reduced motion mandatory

Backend, performance, and security requirements:
- perform customer-safe filtering and compression server-side
- keep raw internal activity and audit truth elsewhere; the client timeline is a projection, not the source log
- make compression deterministic and idempotent so the same input stream produces the same visible timeline
- do not widen visibility or leak staff-only attachment or participant state into timeline detail

Validation rules:
- validate every emitted `ClientTimelineEvent` and any embedded authority-truth contract against schema
- add regression coverage for authority confirmation wording, out-of-band updates, repeated upload churn, and reconnect ordering stability
- keep the timeline builder reusable by the broader portal workspace projector

Definition of done:
- the repo has one authoritative client timeline-event projector and one deterministic compression layer
- recent activity remains customer-safe, bounded, newest-first, and explicit about authority truth
- the portal timeline now supports trust and continuity without becoming a noisy audit surface
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
