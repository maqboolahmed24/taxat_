---
id: pc_0227
slug: phase_04_parallel_wave_02_track_backend_release_resilience_009_implement_release_candidate_identity_and_promotion_evidence_query_surfaces
mode: parallel
phase: "04"
wave: "02"
track: "backend_release_resilience"
task_number: "009"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_release_resilience_0009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0227

## Task Summary

009 implement release candidate identity and promotion evidence query surfaces.

## Metadata

- ID: `pc_0227`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend release resilience`
- Task Number: `009`
- Source Slug: `phase_04_parallel_wave_02_track_backend_release_resilience_009_implement_release_candidate_identity_and_promotion_evidence_query_surfaces`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0226](./pc_0226.md) `phase_04_parallel_wave_02_track_backend_release_resilience_008_implement_recovery_tier_fail_forward_governance_and_post_incident_checkpoint_rules`
- Next: [pc_0228](./pc_0228.md) `phase_04_parallel_wave_02_track_backend_release_resilience_010_implement_forensic_contract_guard_and_constraint_traceability_register_ci_gate`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0222_to_0229.md](../shared_operating_contract_0222_to_0229.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_release_resilience_009_implement_release_candidate_identity_and_promotion_evidence_query_surfaces`.

Inherit the shared operating contract and consume outputs from cards `pc_0219` through `pc_0226` if they exist.

Mission:
Implement the internal read/query surfaces that let operators, automation, and release tooling retrieve the full candidate-identity and promotion-evidence bundle from persisted truth.
This task is not allowed to make operators reconstruct release proof from multiple tables, CI dashboards, or log searches.

Source algorithm grounding you must honor:
- `release_candidate_identity_and_promotion_evidence_contract.md`, end to end.
- `deployment_and_resilience_contract.md`, especially release topology, rollback/fail-forward, and promotion-evidence posture.
- `verification_and_release_gates.md`, especially the release-gate and evidence sections.
- `data_model.md`, especially `BuildArtifact`, `ReleaseCandidateIdentityContract`, `SchemaBundleCompatibilityGateContract`, `VerificationSuiteResult`, `GateAdmissibilityRecord`, `ClientCompatibilityMatrix`, `RestoreDrillResult`, `ReleaseVerificationManifest`, and `DeploymentRelease`.
- `northbound_api_and_session_contract.md` if you expose internal northbound routes.
- `schemas/release_verification_manifest.schema.json` and related release-evidence schemas.
- `scripts/validate_contracts.py` and any relevant ETag/shape checks on query envelopes you touch.

Deliverables to create:
1. `packages/backend-release/src/queries/get_release_candidate_identity_bundle.ts`
2. `packages/backend-release/src/queries/get_release_verification_manifest_bundle.ts`
3. `packages/backend-release/src/queries/get_deployment_release_bundle.ts`
4. `packages/backend-release/src/routes/internal/release_candidate_evidence_routes.ts`
5. `packages/backend-release/src/tests/release_candidate_identity_bundle_query.spec.ts`
6. `packages/backend-release/src/tests/release_candidate_evidence_routes.spec.ts`
7. `docs/release/release_candidate_identity_and_promotion_evidence_queries.md`
8. Optional but recommended: `apps/admin-console-web/src/routes/debug/release/candidate-evidence-explorer.tsx`

You MUST patch any existing code path that currently:
- forces operators to fetch build artifact, compatibility gate, suite evidence, client matrix, restore evidence, and deployment state separately without one coherent bundle,
- or recomputes candidate or compatibility hashes in the query layer instead of reading persisted truth.

Required design decisions you must make explicitly:
- define the canonical read keys for internal release evidence (`candidate_identity_hash`, `verification_manifest_id`, `release_id`) and when each is authoritative
- define how superseded manifests and superseded candidate evidence are represented without losing history
- define the ETag/currentness posture for any internal route you expose
- define how missing companion evidence is surfaced without manufacturing green or complete posture
- define what is a read-model DTO versus a first-class persisted contract so query surfaces do not create a second domain layer

Execution requirements:
- expose read-only internal query surfaces that materialize persisted release truth into coherent evidence bundles
- include at minimum: candidate tuple, compatibility gate contract, suite results, admissibility records, canary summary ref, deterministic golden pack ref, restore drill/checkpoint refs, client compatibility matrix ref, release verification manifest, deployment release, and supersession lineage where present
- keep candidate and compatibility hashes as persisted top-level fields; do not recalculate them in the browser
- preserve missing-evidence posture explicitly in the response instead of hiding absent artifacts
- if you expose northbound routes, keep them internal/admin-only and typed, with explicit problem-envelope handling on missing or stale bundles
- preserve read chronology and supersession rather than mutating older bundles in place

Gap handling you must close while implementing:
- close the gap where release proof exists only as individually queryable artifacts rather than a coherent read surface
- close the gap where superseded or blocked release evidence is hard to inspect without manual joins
- close the gap where internal tools could accidentally recompute or reinterpret candidate/compatibility hashes
- close the gap where missing companion evidence is mistaken for an all-green release posture

Mandatory edge cases:
- superseded manifests must remain inspectable and explicitly marked as superseded
- mixed-candidate evidence must fail closed rather than partially rendering
- missing client matrix, restore pair, or canary companion evidence must remain explicit in the query bundle
- blocked manifests with red gates must render as blocked even if other evidence is present
- a query by `candidate_identity_hash` must not silently return evidence from another compatibility gate hash

Playwright and contract-test requirements:
- add direct tests for candidate-hash lookups, verification-manifest lookups, superseded-manifest lineage, and companion-evidence inclusion
- add Playwright `APIRequestContext` coverage for any internal release-evidence routes you expose
- if you add the optional explorer, add Playwright browser tests for semantic navigation, hash/ribbon rendering, reduced-motion-safe inspector behavior, and keyboard flow using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- create a quiet release evidence explorer with a candidate ribbon, ordered evidence ledger, and narrow lineage/decision sidecar
- emphasize exact binding and supersession lineage, not summary analytics
- keep the explorer read-only and clearly internal
- respect the tranche visual language

Backend, performance, and security requirements:
- query surfaces must be read-only and must not mutate promotion posture
- bundle assembly must reuse persisted truth and avoid N+1 patterns where practical
- restrict internal routes to the appropriate internal/admin boundary and typed authorization checks
- do not expose raw sensitive logs or opaque blobs inline when a durable ref suffices

Validation rules:
- validate any newly introduced bundle/query DTOs against the existing underlying contract families and route contracts you use
- update fixtures/builders and route tests so validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test`
- rerun `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- internal consumers can retrieve a coherent candidate-identity and promotion-evidence bundle from persisted truth
- supersession, missing companion evidence, and mixed-candidate rejection are durable and test-covered
- optional explorers, if added, are read-only viewers over persisted release truth
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, the relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.