---
id: pc_0091
slug: phase_03_parallel_wave_01_track_backend_access_007_implement_governance_mutation_simulation_service_and_hazard_basis_persistence
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "007"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0091

## Task Summary

implement governance mutation simulation service and hazard basis persistence.

## Metadata

- ID: `pc_0091`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `007`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_007_implement_governance_mutation_simulation_service_and_hazard_basis_persistence`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0090](./pc_0090.md) `phase_03_parallel_wave_01_track_backend_access_006_implement_validate_effective_scope_binding_and_access_scope_enforcement_services`
- Next: [pc_0092](./pc_0092.md) `phase_03_parallel_wave_01_track_backend_access_008_implement_step_up_approval_and_authentication_level_policy_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0086_to_0093.md](../shared_operating_contract_0086_to_0093.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_007_implement_governance_mutation_simulation_service_and_hazard_basis_persistence`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0090` if they exist, especially `pc_0017`, `pc_0023`, `pc_0027`, `pc_0040`, `pc_0050`, `pc_0054`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0067`, `pc_0068`, `pc_0074`, `pc_0075`, `pc_0076`, `pc_0083`, `pc_0086`, `pc_0087`, `pc_0089`, and `pc_0090`.

Mission:
Implement `SIMULATE_GOVERNANCE_MUTATION(...)`, dependency-topology assembly, hazard and basis contract persistence, and the frozen coupling between authorization and simulation for governance mutations.
This task is where the platform stops pretending governance changes are simple RBAC edits and starts treating them as risk-bearing system mutations with deterministic blast-radius and approval posture.

Source algorithm grounding you must honor:
- `modules.md`, especially `SIMULATE_GOVERNANCE_MUTATION(...)`, the hazard formulas, the basis formulas, the `commit_authority_posture` thresholds, and the approval requirement mapping.
- `data_model.md`, especially `GovernanceMutationHazardContract`, `GovernanceMutationBasisContract`, and `GovernanceAccessSimulation` semantics.
- `actor_and_authority_model.md`, especially `3.9 Policy decision model` and the requirement that governance mutation previews materialize `GovernanceMutationBasisContract`.
- `admin_governance_console_architecture.md`, especially the simulator route, stale-view protection, `simulator_posture`, and the rule that `AUTHORIZE(...)` and `SIMULATE_GOVERNANCE_MUTATION(...)` must operate on the same frozen cut.
- `invariants_and_gates.md`, especially the audit and approval invariants for governance mutation and override paths.
- Existing schemas including `governance_mutation_hazard_contract.schema.json`, `governance_mutation_basis_contract.schema.json`, and `governance_access_simulation.schema.json`.

Deliverables to create:
1. `packages/backend-access/src/models/governance_mutation_hazard_contract.ts`.
2. `packages/backend-access/src/models/governance_mutation_basis_contract.ts`.
3. `packages/backend-access/src/models/governance_access_simulation.ts`.
4. `packages/backend-access/src/services/governance_mutation_simulator.ts`.
5. `packages/backend-access/src/services/dependency_topology_builder.ts`.
6. `packages/backend-access/src/services/mutation_hazard_scoring_service.ts`.
7. `packages/backend-access/src/services/simulation_staleness_guard.ts`.
8. `packages/backend-access/src/repositories/governance_access_simulation_repository.ts`.
9. `db/migrations/phase03_0004_governance_simulation_and_hazard_basis.sql`.
10. `config/governance/simulation_profile_catalog.json`.
11. `docs/access/governance_mutation_simulation.md`.
12. `tests/unit/backend_access/governance_mutation_simulator.spec.ts`.
13. `tests/integration/backend_access/governance_simulation_persistence_and_staleness.spec.ts`.

Required design decisions you must make explicit:
- Define how dependency nodes, edges, profile refs, and object-version refs are represented so `dependency_topology_hash` is stable and replayable.
- Define the simulation profile catalog and how different governance mutation classes map to node weights, edge weights, and risk scoring posture.
- Define how the simulator stores interval blast-radius values, blocker arrays, confidence, predictability, trigger sets, and posture transitions.
- Define how stale-view protection compares `policy_snapshot_hash`, `dependency_topology_hash`, `simulation_basis_hash`, impacted approval path, and inventory slices before a later commit is allowed.
- Define how read-only decision simulations differ from mutation-capable governance simulations without inventing incompatible payload shapes.

Execution steps:
1. Implement topology builders that accept a frozen policy cut and produce stable ordered node and edge vectors.
2. Implement the exact hazard and basis formulas from `modules.md`, including bounded-safe classification, approval requirement mapping, and advisory-only downgrade when confidence or predictability falls below the corpus thresholds.
3. Implement a simulation service that first resolves or receives a frozen `AuthorizationDecision`, then computes hazard and basis contracts on that same snapshot, and finally emits a schema-valid `GovernanceAccessSimulation`.
4. Persist the simulation record, hazard contract, basis contract, and all relevant stale-guard fields so later approval and commit stages can compare against them directly.
5. Implement stale-check helpers that fail closed when policy snapshot, topology, impacted approvals, or inventory slices have changed.
6. Add source-lineage and hash-lineage documentation so later agents know exactly which mutable inventories feed the simulator.

Gap handling you must perform:
- Close the gap where governance changes are previewed without a frozen authorization / simulation pair.
- Close the gap where topology, blast radius, or approval requirement is recomputed differently between preview and commit.
- Close the gap where low-confidence or low-predictability scenarios are not downgraded to advisory posture.
- Close the gap where stale policy or stale dependency information can masquerade as a still-committable preview.
- Close the gap where hazard and basis contracts are only explained in prose, not durably persisted.

Mandatory edge cases:
- confidence < 80 or predictability < 75 and posture must downgrade to `ADVISORY_ONLY`
- `bounded_safe_mutation=1` but approval requirement is not `NOT_REQUIRED`
- topology hash changes because a referenced object version changed even though node ids stayed the same
- policy snapshot changes after preview and stale guard must block commit
- mixed staged mutations try to present different simulation bases as one atomic action
- zero-impact or empty-topology mutation still needs deterministic hashes and posture classification
- simulator is asked to run on a mutation-capable request that lacks a frozen `AuthorizationDecision`
- authority chain layers or boundary packet differ between authorization and simulation on the same request and must be treated as a conflict

Backend, performance, and security requirements:
- The simulator must be deterministic and side-effect free with respect to domain state.
  It predicts; it does not mutate live policy.
- Persist enough detail for audit and stale detection, but do not dump secrets, raw payload bodies, or large opaque graphs where stable references suffice.
- Use indexes that support correlation by `policy_snapshot_hash`, `simulation_basis_hash`, `dependency_topology_hash`, and request / approval ids.
- No new product UI is required.
  If you add an internal-only simulator inspection route, it should present a minimal premium evidence desk with a topology sparkline or interval diagram, explicit hashes, and reason-led detail panels, and it must be validated with Playwright.

Validation rules:
- Validate all emitted hazard, basis, and access-simulation objects against their canonical schemas.
- Unit-test formula thresholds, advisory-only downgrade, approval mapping, bounded-safe classification, topology hashing inputs, and stale-guard comparisons.
- Integration-test preview persistence, replay-safe lookup, stale detection after policy or topology drift, and coupling to a frozen authorization decision.
- Prove that the same frozen input set yields byte-stable hazard and basis artifacts.
- Prove that commit posture cannot be inferred without the persisted basis contract.

Definition of done:
- The repo has an executable governance-mutation simulator that matches the corpus formulas and frozen-snapshot rules.
- Hazard and basis contracts are durable, queryable, and reusable by later approval and commit flows.
- Governance preview no longer depends on informal reasoning or ephemeral UI-side calculations.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
