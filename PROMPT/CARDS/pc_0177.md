---
id: pc_0177
slug: phase_04_parallel_wave_02_track_backend_low_noise_009_implement_focus_restoration_return_target_and_recovery_posture_projection_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_low_noise"
task_number: "009"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_low_noise_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0177

## Task Summary

implement focus restoration return target and recovery posture projection rules.

## Metadata

- ID: `pc_0177`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend low noise`
- Task Number: `009`
- Source Slug: `phase_04_parallel_wave_02_track_backend_low_noise_009_implement_focus_restoration_return_target_and_recovery_posture_projection_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0176](./pc_0176.md) `phase_04_parallel_wave_02_track_backend_low_noise_008_implement_cross_shell_design_token_binding_and_interaction_semantics_projection`
- Next: [pc_0178](./pc_0178.md) `phase_04_parallel_wave_02_track_backend_low_noise_010_implement_semantic_accessibility_contract_data_publication_for_all_governed_surfaces`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0174_to_0181.md](../shared_operating_contract_0174_to_0181.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_low_noise_009_implement_focus_restoration_return_target_and_recovery_posture_projection_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0176` if they exist, especially `pc_0078` through `pc_0085`, `pc_0163` through `pc_0165`, and `pc_0174` through `pc_0176`.

Mission:
Implement the reusable projection rules that make focus restore, return-target selection, and recovery fallback deterministic across calm-shell, collaboration, and portal contextual routes.
This task must stop close/back/help-return/rebase fallback behavior from degrading into browser-history heuristics.
It must publish the exact data needed for later harness tasks, but it must not swallow the later dedicated regression-suite implementation.

Source algorithm grounding you must honor:
- `focus_restoration_and_return_target_harness_contract.md`, entire document.
- `frontend_shell_and_interaction_law.md`, especially route continuity, focus law, automation anchors, and motion/recovery sections.
- `cross_device_continuity_and_restoration_contract.md`.
- `customer_client_portal_experience_contract.md`, especially contextual route return-path and help-handoff rules.
- `collaboration_workspace_contract.md`, especially same-shell return and focused detail recovery.
- `data_model.md`, especially route-context, cross-device continuity, focus-restore, and harness-related fields.
- `schemas/focus_restoration_contract.schema.json`, `schemas/focus_restore_return_target_harness.schema.json`, and `schemas/cross_device_continuity_contract.schema.json`.
- `test_vectors.md`, especially TV-39I through TV-39O and the contextual portal return-path cases.

Deliverables to create:
1. `packages/backend-low-noise/src/services/project_focus_restoration_contract.ts`
2. `packages/backend-low-noise/src/services/project_return_target_and_fallback.ts`
3. `packages/backend-low-noise/src/services/project_recovery_navigation_posture.ts`
4. `packages/backend-low-noise/src/services/validate_return_target_narrowness.ts`
5. `packages/backend-low-noise/src/services/build_focus_restore_case_seeds.ts`
6. `packages/backend-low-noise/src/tests/focus_restoration_and_return_target_projection.spec.ts`
7. `docs/low_noise/focus_restoration_return_target_and_recovery_projection.md`
8. Optional but recommended: `apps/admin-console-web/src/routes/debug/focus/focus-return-lab.tsx`

Required design decisions you must make explicitly:
- define the canonical projector inputs for requested focus, resolved focus, parent return, fallback route, fallback object, focus lock, and recovery posture
- define how the same logic is reused by calm-shell detail drawers, collaboration contextual routes, portal request-detail/help routes, and parent-bound support windows without flattening their differences
- define the exact contract between `route_context`, `cross_device_continuity_contract`, and `focus_restoration_contract` so a route does not publish conflicting return anchors
- define what minimal case-seed artifact is lawful now for later harness tasks, and how to keep it obviously narrower than the future full harness suites

Execution requirements:
- publish one deterministic restore contract whenever a route or support surface can close, back out, or recover to a narrower target
- preserve the exact invoker or parent-return anchor when that target remains lawful
- when the exact target is no longer lawful, fallback order MUST remain: same governed object remap -> same governed object summary -> serialized parent return -> narrowest surviving list target
- live updates MUST preserve focus locks for active composers, pickers, file selectors, and compare controls
- help-handoff return MUST restore the serialized source anchor instead of a generic help root
- contextual portal routes MUST keep the parent top-level tab active while restoring the exact request, approval, or support focus anchor
- if you add the optional lab, it must show one ordered fallback ladder and one route-state panel only; do not turn it into a generic router debugger

Gap handling you must close while implementing:
- route-visible models can already carry focus and return fields, but the repo may still lack one reusable narrow-target selection service; create it now
- stale or rebased contextual routes often fall back too broadly; enforce the narrowest surviving lawful target ordering explicitly
- help handoff and support-surface dismissal can lose the original focus anchor; make that failure impossible by projection rules instead of UI callbacks
- provide deterministic case-seed generation for later harness tasks without pretending to ship the entire regression suite now

Mandatory edge cases:
- closing a support region MUST restore focus to the exact invoker when that invoker is still lawful
- `restoration_disposition = INVALIDATED` MUST be used when neither exact target nor lawful fallback survives; do not silently select a broad shell root
- portal contextual routes MUST restore `return_route` and `return_focus_anchor_ref_or_null` together; never recover only the tab and lose the item anchor
- help-return cases MUST restore the carried case-context anchor rather than the top of `TOP_QUESTIONS`
- live-update focus locks must survive activity deltas and polite announcements without stealing active input focus
- parent-bound secondary-window close must restore the parent anchor, not just the parent scene root

Playwright and contract-test requirements:
- unit-test the return-target and focus projector logic directly with deterministic fixture matrices
- if existing route payloads are patched, add Playwright APIRequestContext assertions proving the projected focus/return data is serialized on those payloads
- if you add the focus-return lab, test it with Playwright using keyboard-only interaction, semantic locators, and trace capture on failure
- no fixed sleeps; wait on explicit focus or recovery-state signals

Backend, performance, and security requirements:
- keep focus and fallback decisions server-authored from the grouped stability/visibility basis, not from browser history or DOM ancestry
- do not widen visibility or object scope during fallback; narrower lawful recovery always outranks broad convenience routing
- avoid storing raw browser implementation detail in the contract; store semantic anchors and governed route refs instead
- fail closed on mixed-generation route context or stale parent-return anchors

Validation rules:
- validate any emitted `focus_restoration_contract` against schema
- add regression tests for exact target restore, parent return, narrowest-list fallback, help-return, and live-update focus-lock preservation
- if you generate case seeds, ensure they remain deterministic and typed rather than test-framework-specific blobs

Definition of done:
- the repo has one reusable projector for focus restoration and one for return-target/fallback selection
- stale/rebased contextual routes stop falling back to generic shell roots
- later harness tasks can consume typed case seeds and projected contracts instead of reverse-engineering focus rules from UI code
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
