---
id: pc_0201
slug: phase_04_parallel_wave_02_track_backend_recovery_003_implement_replay_and_reproducibility_pipeline_with_execution_basis_freeze_loading
mode: parallel
phase: "04"
wave: "02"
track: "backend_recovery"
task_number: "003"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_recovery_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0201

## Task Summary

003 implement replay and reproducibility pipeline with execution basis freeze loading.

## Metadata

- ID: `pc_0201`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend recovery`
- Task Number: `003`
- Source Slug: `phase_04_parallel_wave_02_track_backend_recovery_003_implement_replay_and_reproducibility_pipeline_with_execution_basis_freeze_loading`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0200](./pc_0200.md) `phase_04_parallel_wave_02_track_backend_recovery_002_implement_restore_privacy_reconciliation_contract_and_post_restore_limitation_pass`
- Next: [pc_0202](./pc_0202.md) `phase_04_parallel_wave_02_track_backend_recovery_004_implement_deterministic_golden_pack_generation_null_slot_freezing_and_fixture_hashing`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0198_to_0205.md](../shared_operating_contract_0198_to_0205.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_recovery_003_implement_replay_and_reproducibility_pipeline_with_execution_basis_freeze_loading`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0200` if they exist, especially `pc_0102` through `pc_0109`, `pc_0117`, `pc_0126`, `pc_0127`, `pc_0158`, `pc_0159`, `pc_0166`, and `pc_0199` through `pc_0200`.

Mission:
Implement the replay/recovery orchestration pipeline that validates exact replay preconditions, loads historical frozen basis and post-seal basis, executes replay against historical truth, preserves idempotent rerun behavior, and publishes attestation-compatible outputs without live-state pollution.
This task is not allowed to fake replay by recollecting mutable inputs.
It must make exact replay and same-attempt recovery execute against retained historical truth or fail closed.

Source algorithm grounding you must honor:
- `replay_and_reproducibility_contract.md`, especially exact replay preconditions, recovery and continuation semantics, idempotent rerun guarantees, historical post-seal basis, and implementation shape.
- `manifest_and_config_freeze_contract.md`, especially `ConfigFreeze`, `InputFreeze`, `schema_reader_window_contract`, and frozen execution binding semantics.
- `modules.md`, especially `COMPUTE_EXECUTION_BASIS_HASH(...)`, `COMPARE_REPLAY_OUTCOMES(...)`, and the replay orchestration helpers implied by the implementation shape.
- `data_model.md`, especially `ReplayAttestation`, `ReplayBasisIntegrityContract`, `RunManifest` replay fields, and `DeterministicGoldenPack`.
- `verification_and_release_gates.md`, especially replay evidence and deterministic release evidence interactions.
- `schemas/replay_attestation.schema.json`, `schemas/replay_basis_integrity_contract.schema.json`, and any relevant manifest / config freeze schemas already in the corpus.
- `scripts/validate_contracts.py`, especially `replay_attestation_basis_dimension_order`, `replay_attestation_observable_component_ref`, `replay_attestation_confidence_projection`, and `replay_attestation_counterfactual_posture`.
- `PATCH_RESOLUTION_INDEX.md`, especially fixes around exact replay without basis hashes, missing post-seal basis, or rerun duplication.
- `test_vectors.md`, especially exact replay, same-attempt recovery, missing dependency, non-persisted outcome material, and deterministic rerun vectors.

Deliverables to create:
1. `packages/backend-recovery/src/services/validate_replay_preconditions.ts`
2. `packages/backend-recovery/src/services/load_historical_config_freeze.ts`
3. `packages/backend-recovery/src/services/load_historical_input_freeze.ts`
4. `packages/backend-recovery/src/services/load_historical_preseal_gate_context.ts`
5. `packages/backend-recovery/src/services/load_historical_post_seal_basis.ts`
6. `packages/backend-recovery/src/services/execute_replay_against_historical_basis.ts`
7. `packages/backend-recovery/src/services/orchestrate_replay_pipeline.ts`
8. `packages/backend-recovery/src/services/resolve_idempotent_replay_rerun.ts`
9. `packages/backend-recovery/src/tests/replay_preconditions.spec.ts`
10. `packages/backend-recovery/src/tests/replay_pipeline_exactness.spec.ts`
11. `docs/recovery/replay_and_reproducibility_pipeline_with_execution_basis_freeze_loading.md`
12. Optional but recommended: `apps/admin-console-web/src/routes/debug/recovery/replay-basis-lab.tsx`

You MUST patch any existing replay entrypoint that currently recollects config, intake, or authority state without first proving exact historical basis availability.

Required design decisions you must make explicitly:
- define the exact precondition checklist and failure-code mapping for exact replay vs same-attempt recovery vs counterfactual replay
- define how historical `ConfigFreeze`, `InputFreeze`, pre-seal gate tape, schema reader window, and post-seal basis are located, verified, and bound into one replay execution context
- define the idempotency key for exact same-request replay reruns and same-attempt recovery reruns
- define how replay class, execution mode, and analysis posture constrain live mutation tokens and legal-effect boundaries
- define how missing, corrupt, schema-incompatible, retention-limited, or undecryptable historical artifacts are surfaced without silently substituting live state
- define when the pipeline may return an existing persisted replay child / attestation pair instead of creating new lineage

Execution requirements:
- validate all exact replay preconditions before execution begins
- load historical frozen config, historical frozen input, historical pre-seal gate context, and historical post-seal basis explicitly
- preserve `execution_basis_hash` for exact replay and same-attempt recovery
- fail closed if the historical basis is missing, corrupt, schema-incompatible, or otherwise insufficient for the requested exactness class unless policy explicitly allows limited comparison
- keep transport recovery metadata (`resume_token`, `frame_epoch`, `shell_stability_token`, route snapshots, etc.) out of replay legality and execution-basis decisions
- preserve the distinction between `manifest_branch_decision.branch_action` and selected-manifest continuation basis
- return existing persisted `DecisionBundle` for exact same-request terminal reruns and existing replay child + `ReplayAttestation` for exact same-request replay reruns when idempotency requires it

Gap handling you must close while implementing:
- close any gap where exact replay could silently recollect inputs or authority state and still claim exactness
- close any gap where historical post-seal basis is omitted and later compared heuristically
- close any gap where rerun idempotency is handled by controller code instead of one canonical recovery/replay service
- close any gap where replay legality depends on current live schema or deploy bundle rather than retained reader-window truth
- close any gap where replay posture can become visible before a durable attestation-compatible pipeline result exists

Mandatory edge cases:
- exact replay without `execution_basis_hash` must fail closed
- exact replay with fresh source collection, fresh authority reads, or fresh late-data scans must fail closed
- same-attempt recovery must reuse exact frozen config and input basis and must not recast the attempt as a fresh child
- `COUNTERFACTUAL_ANALYSIS` must keep explicit declared basis differences and must not publish exact-match posture
- missing schema-reader compatibility must produce typed failure rather than silent substitute deserialization
- historical artifact loads must verify presence, artifact type, content hash, and decryptability when relevant
- replay comparison rows for observable components must keep non-null `component_ref`
- non-persisted outcome material must not pollute deterministic comparison claims

Playwright and contract-test requirements:
- if you expose or patch replay diagnostics or a northbound replay route, add Playwright `APIRequestContext` coverage for exact-vs-limited posture, idempotent rerun return, and typed basis failures
- add tests for exact replay, same-attempt recovery, counterfactual replay, missing dependency, corrupt basis, schema incompatibility, retention limitation, and build unavailable conditions
- if you add the optional lab, add Playwright browser tests that verify the execution-basis ribbon, basis-dimension table, post-seal basis disclosure, keyboard navigation, and reduced-motion-safe expansion using semantic locators only
- capture payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a structured three-band layout: execution-basis ribbon, historical basis ledger, comparison/result sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717` / `#667085`; explainability accent `#1D4ED8`; continuity accent `#0F766E`; caution `#B7791F`; danger `#C2410C`
- show basis dimensions in fixed canonical order; never sort by score or mismatch count
- render post-seal basis as its own explicit section, not folded into the pre-seal execution-basis ribbon
- if you show a diagram, show one basis lineage rail from frozen config/input to post-seal basis to comparison outcome; no gauges or spark lines
- animate only opacity / translate / height in `140ms`–`180ms`; reduced motion must preserve the same causal structure

Backend, performance, and security requirements:
- keep historical artifact loading deterministic, side-effect free, and auditable
- never log raw retained artifacts or decrypted payload bodies; log ids, hash refs, replay class, and typed failure codes only
- separate precondition validation from actual execution so operators can inspect typed blockers without triggering side effects
- index replay lookups by replay target, replay class, execution mode, and idempotency key
- fail closed on live mutation scope in replay requests

Validation rules:
- reuse the attestation and basis-integrity validators from earlier replay work
- add regression coverage for the named validator cases above
- prove exact replay and same-attempt recovery preserve the historical execution basis rather than recomputing a mutated one
- keep this task limited to replay/reproducibility orchestration and basis loading; do not absorb nightly batch scheduling work

Definition of done:
- the repo has one authoritative replay/recovery pipeline that loads historical frozen basis explicitly
- exact replay and same-attempt recovery can no longer drift into live-state substitution
- later deterministic-golden-pack and nightly-recovery tasks can rely on a strict, typed replay foundation
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
