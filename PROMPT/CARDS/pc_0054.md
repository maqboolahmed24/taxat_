---
id: pc_0054
slug: phase_01_seq_054_provision_opentelemetry_collection_trace_metric_and_log_backends
mode: sequential
phase: "01"
wave: null
track: null
task_number: "054"
block_key: phase_01_seq
original_key: phase_01_seq_054
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0054

## Task Summary

provision opentelemetry collection trace metric and log backends.

## Metadata

- ID: `pc_0054`
- Mode: `sequential`
- Phase: `01`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `054`
- Source Slug: `phase_01_seq_054_provision_opentelemetry_collection_trace_metric_and_log_backends`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0053](./pc_0053.md) `phase_01_seq_053_provision_cache_and_stream_resume_store_with_partition_isolation_controls`
- Next: [pc_0055](./pc_0055.md) `phase_01_seq_055_provision_container_registry_build_signing_and_artifact_attestation_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt

Use the shared operating contract first:
[../shared_operating_contract_0054_to_0061.md](../shared_operating_contract_0054_to_0061.md)

```text
Execute roadmap task `phase_01_seq_054_provision_opentelemetry_collection_trace_metric_and_log_backends`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0053` if they exist,
especially the monitoring-provider workspace from `pc_0044`, the datastore/object/broker/cache substrates from `pc_0050` through `pc_0053`,
the environment/provider catalog from `pc_0031`, and the secret policy from `pc_0033`.

Mission:
Provision the OpenTelemetry collection and backend topology for traces, metrics, and logs without collapsing Taxat's audit law into a telemetry tool.
This task must turn the corpus' observability contract into an executable, environment-scoped substrate:
collectors,
resource-attribute rules,
signal routes,
sampling posture,
retention posture,
backend inventories,
and correlation keys that let runtime behavior be understood without weakening append-only audit evidence.
The result must let later runtime agents instrument the system once and know exactly where every signal family goes, how it is correlated,
what is sampled, what is retained, and what must remain outside telemetry systems entirely.

Source algorithm grounding you must honor:
- `observability_and_audit_contract.md`, especially:
  `14.2 Separation of concerns`,
  `14.3 Signal model`,
  `14.4 Mandatory correlation keys`,
  `14.7 Trace contract`,
  `14.8 Metric contract`,
  `14.9 Logging contract`,
  `14.10 Audit versus telemetry retention`,
  the query-contract sections,
  and the invariant list near the end.
- `retention_error_and_observability_contract.md` and `retention_and_privacy.md`
  for retention classes, masking posture, omission law, and security-sensitive signal handling.
- `deployment_and_resilience_contract.md`, especially the reference runtime topology, release promotion expectations,
  and the rule that read models/caches are disposable while audit evidence is durable.
- `security_and_runtime_hardening_contract.md`, especially secret handling, least-privilege egress, supply-chain hardening,
  and the rule that runtime controls must not leak sensitive content into logs or third-party backends.
- `verification_and_release_gates.md`, especially the required test families, release evidence expectations,
  and the rule that blocking suites are bound to tested build digests / schema bundles.
- `release_candidate_identity_and_promotion_evidence_contract.md` wherever release candidate hashes, compatibility-gate binding,
  and tested-build identity need observability correlation.
- Prior cards `pc_0017`, `pc_0020`, `pc_0031`, `pc_0033`, `pc_0044`, `pc_0050`, `pc_0051`, `pc_0052`, and `pc_0053`.
- Current official OpenTelemetry documentation and current official docs for any selected backend vendor.

Deliverables to create:
1. `infra/observability/bootstrap/provision_otel_collection_and_backends.ts`
2. `infra/observability/contracts/telemetry_topology.schema.json`
3. `config/observability/signal_backend_catalog.json`
4. `config/observability/otlp_export_matrix.json`
5. `config/observability/sampling_and_retention_policy.json`
6. `config/observability/correlation_key_policy.json`
7. `config/observability/log_scrub_and_field_allowlist.json`
8. `data/provisioning/observability_inventory.template.json`
9. `docs/provisioning/opentelemetry_stack_runbook.md`
10. `automation/provisioning/report_viewer/src/routes/telemetry-signal-atlas.tsx`
11. `tests/unit/correlation_key_policy.spec.ts`
12. `tests/unit/sampling_and_retention_policy.spec.ts`
13. `tests/integration/observability/otel_stack_bootstrap.spec.ts`
14. `tests/playwright/provisioning/observability/telemetry_signal_atlas.spec.ts`

Required design decisions and policy dimensions you must make explicit:
- whether the topology uses one shared collector tier, per-service sidecars/agents, or a hybrid gateway pattern,
  and why that fits the runtime topology
- environment partitioning for local, preview, sandbox, staging, production, and any authority-sandbox lanes
- backend selection by signal family:
  traces,
  metrics,
  logs,
  security telemetry,
  privacy telemetry,
  and export bridge to any downstream monitoring vendor
- exact resource-attribute conventions for:
  service name,
  service namespace,
  environment,
  tenant/posture-safe dimensions,
  release candidate hash,
  build digest,
  schema bundle hash when useful,
  and correlation to manifest/run/workspace lineages
- correlation keys required across signals and their mapping to audit evidence,
  including `trace_id`, request hash/binding refs, manifest refs, release refs, authority correlation refs, and manual-checkpoint refs where relevant
- sampling posture, unsampled fallback posture, retention windows, redaction posture, and backend-specific storage class
- cardinality and label/attribute budget policy so the system remains queryable and predictable at scale
- exporter failure posture and backpressure posture; runtime must stay safe if a downstream telemetry sink degrades
- which log fields are allowed, which are forbidden, and which are hash/redaction-only

Execution steps:
1. Reconcile earlier provider and monitoring decisions.
   If a monitoring vendor workspace exists from `pc_0044`, treat it as a downstream integration target rather than the whole observability system.
   If provider selection is unresolved, emit a typed option matrix and still produce the policy pack and viewer.
2. Implement an idempotent provisioning/adoption flow for collectors, backend projects/datasets/buckets/streams, API creds refs, and routing config placeholders.
3. Create `signal_backend_catalog.json`.
   It must declare each signal family, its canonical collector entrypoint, primary backend, optional secondary/export backends,
   retention class, and whether it is vendor-exportable.
4. Create `otlp_export_matrix.json`.
   It must map service families and runtime components to OTLP pipelines with explicit processors, batching posture, and export targets.
5. Create `sampling_and_retention_policy.json`.
   It must distinguish mandatory audit evidence from telemetry sampling.
   No sampling decision may erase a required audit trail.
   Include default, elevated-debug, incident, and privacy-constrained posture if applicable.
6. Create `correlation_key_policy.json`.
   It must define mandatory keys per signal family and prove how telemetry can be joined back to durable audit evidence without re-sorting audit history by wall-clock guesswork.
7. Create `log_scrub_and_field_allowlist.json`.
   It must make raw secrets, raw authority tokens, one-time codes, unmasked personal data, and unsafe document contents structurally impossible to emit.
8. Build `telemetry-signal-atlas` in the provisioning report viewer.
   The page must let an operator inspect the signal families, routes, sampling/retention posture, correlation keys, and telemetry-vs-audit boundary in one quiet surface.
9. Add unit/integration tests for policy completeness, redaction rules, and bootstrap idempotency.
10. Add Playwright coverage for the atlas.

Visual and interaction specification for `telemetry-signal-atlas`:
- This page must feel like a premium signal atelier, not a metrics dashboard.
  Think “observability atlas” rather than “NOC wall”.
- Layout:
  - `64px` top context bar with environment selector, collector-topology badge, and telemetry posture chip.
  - left signal-family rail `252-268px` listing `TRACES`, `METRICS`, `LOGS`, `SECURITY`, `PRIVACY`, and `AUDIT-LINKS`.
  - central atlas canvas split into four vertical planes:
    `Emission`,
    `Collector / Processors`,
    `Backends / Sinks`,
    `Correlation To Audit`.
  - right inspector drawer `360-392px` showing retention class, sampling posture, required keys, and scrub policy for the selected family.
- Palette:
  background `#F5F6F4`,
  primary surface `#FFFFFF`,
  secondary surface `#EEF1EC`,
  ink `#111418`,
  muted ink `#65707A`,
  hairline `rgba(17,20,24,0.08)`,
  accent slate `#476275`,
  accent olive `#5C7263`,
  accent bronze `#86663A`,
  success `#16624B`,
  warning `#8A5C18`,
  danger `#A63B32`.
- Typography:
  typography-led, with monospace reserved for signal IDs, backend names, and correlation-key identifiers.
- Diagram:
  the four-plane atlas is the diagram.
  Include one thin lineage strip showing `runtime signal -> collector -> backend -> audit join keys`.
  Do not render KPI counters, throughput charts, or sales-style observability widgets.
- Motion:
  120-150ms max for family-switch and inspector reveal.
  Reduced-motion mode must replace movement with opacity/focus transitions only.
- Interaction:
  family rows, pipeline edges, and retention badges must all be keyboard reachable.
  Hover must never be the only way to discover the telemetry/audit boundary.
  Every meaningful atlas element must expose stable semantic locators.

Gap handling you must perform:
- Close the “the corpus defines telemetry law, but there is no executable OpenTelemetry topology” gap.
- Close the “mandatory correlation keys exist in prose, but there is no machine-readable signal/correlation policy pack” gap.
- Close the “sampling and retention posture are not yet normalized across traces/metrics/logs/security/privacy signals” gap.
- Close the “current monitoring workspace setup does not yet describe the collector-to-backend path” gap.
- Close the “later runtime work would otherwise guess what may be exported to vendors and what must remain first-party only” gap.

Mandatory edge cases:
- an exporter outage or degraded backend must not break runtime correctness or mandatory audit capture
- high-cardinality tenant/client/session labels must be constrained or normalized; do not let cardinality explode silently
- unsampled traces must still leave required audit evidence intact for filing-critical, approval, or authority-significant actions
- privacy-sensitive views, masked content, and erasure workflows must not leak raw data into logs or third-party sinks
- log correlation must remain possible even when the primary trace backend is unavailable
- preview / sandbox / staging / production signals must not co-mingle in one ambiguous namespace
- debug/incident escalation must remain explicit, time-bounded, and auditable if it changes sampling or retention posture

Backend, performance, and security requirements:
- Use OTLP as the canonical ingest/interchange contract for traces, metrics, and logs unless a prior ADR forbids it.
- Make collectors and exporters idempotently provisionable and environment scoped.
- Keep credentials and backend secrets outside repo-tracked files and outside general traces/logs.
- Prefer batching, bounded queues, and failure-aware backpressure policy over naive firehose behavior.
- Keep telemetry schemas and policy in typed JSON so runtime instrumentation can consume a shared truth.
- Do not let viewer pages mutate collector or backend infrastructure.

Validation rules:
- Unit tests must prove every signal family maps to a backend, retention class, scrub policy, and correlation-key set.
- Unit tests must prove forbidden secret and sensitive-field classes are rejected by the log allowlist/scrub policy.
- Integration tests must prove the bootstrap can create/adopt the collector/backend topology without destructive duplication.
- Playwright must validate semantic locators, keyboard flow, reduced-motion parity, inspector rendering, and lineage-strip visibility on the atlas page.

Definition of done:
- A later runtime agent can inspect one authoritative telemetry-topology pack,
  know exactly how traces/metrics/logs are collected and routed,
  know which correlation keys and redaction rules are mandatory,
  and instrument services without weakening Taxat's audit, privacy, or release law.
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm corpus and aligned to the authoritative card slug.
- Instructions explicitly preserve card status neutrality; claim and completion state must continue to live only in `Checklist.md`.

## Execution Summary

- Added the canonical OpenTelemetry topology builder in [infra/observability/bootstrap/provision_otel_collection_and_backends.ts](/Users/test/Code/taxat_/infra/observability/bootstrap/provision_otel_collection_and_backends.ts), including deterministic builders, validators, dry-run adoption logic, checked-in artifact emission, and sample viewer payload generation.
- Added the inventory schema in [infra/observability/contracts/telemetry_topology.schema.json](/Users/test/Code/taxat_/infra/observability/contracts/telemetry_topology.schema.json).
- Emitted the machine-readable pack in [signal_backend_catalog.json](/Users/test/Code/taxat_/config/observability/signal_backend_catalog.json), [otlp_export_matrix.json](/Users/test/Code/taxat_/config/observability/otlp_export_matrix.json), [sampling_and_retention_policy.json](/Users/test/Code/taxat_/config/observability/sampling_and_retention_policy.json), [correlation_key_policy.json](/Users/test/Code/taxat_/config/observability/correlation_key_policy.json), [log_scrub_and_field_allowlist.json](/Users/test/Code/taxat_/config/observability/log_scrub_and_field_allowlist.json), [observability_inventory.template.json](/Users/test/Code/taxat_/data/provisioning/observability_inventory.template.json), and [opentelemetry_stack_runbook.md](/Users/test/Code/taxat_/docs/provisioning/opentelemetry_stack_runbook.md).
- Added the viewer route descriptor in [telemetry-signal-atlas.tsx](/Users/test/Code/taxat_/automation/provisioning/report_viewer/src/routes/telemetry-signal-atlas.tsx) and wired the interactive atlas into [app.js](/Users/test/Code/taxat_/automation/provisioning/report_viewer/app.js), [styles.css](/Users/test/Code/taxat_/automation/provisioning/report_viewer/styles.css), and [sample_run.json](/Users/test/Code/taxat_/automation/provisioning/report_viewer/data/sample_run.json).
- Added focused verification in [correlation_key_policy.spec.ts](/Users/test/Code/taxat_/automation/provisioning/tests/unit/correlation_key_policy.spec.ts), [sampling_and_retention_policy.spec.ts](/Users/test/Code/taxat_/automation/provisioning/tests/unit/sampling_and_retention_policy.spec.ts), [otel_stack_bootstrap.spec.ts](/Users/test/Code/taxat_/tests/integration/observability/otel_stack_bootstrap.spec.ts), and [telemetry_signal_atlas.spec.ts](/Users/test/Code/taxat_/tests/playwright/provisioning/observability/telemetry_signal_atlas.spec.ts).

## Outcome Notes

- The resulting posture is explicit and fail-closed:
  - `selection_status = PROVIDER_SELECTION_REQUIRED`
  - `managed_default_status = BLOCKED_BY_PLATFORM_PROVIDER_SELECTION`
  - `topology_mode = HYBRID_DIRECT_SDK_AND_WORKLOAD_AGENT_TO_ENVIRONMENT_GATEWAY_WITH_SIGNAL_SPECIFIC_FIRST_PARTY_BACKENDS`
- The pack freezes `6` signal families, `4` collector tiers, `7` backend rows, `5` OTLP pipelines, `8` runtime service families, `6` retention classes, `8` cardinality budgets, and `4` allowed log families.
- The Sentry-compatible monitoring workspace from `pc_0044` is preserved as a secondary vendor overlay only. First-party trace, metric, log, security, privacy, and audit-link truth remains separate and explicit.
- `deployment.environment.name`, `service.name`, `service.namespace`, release candidate hash, build digest, schema bundle hash, and workspace lineage are now part of the shared observability identity contract rather than later-runtime guesswork.
- The atlas page delivers the required four-plane signal view and keeps the inspector persistent so the telemetry-vs-audit boundary, correlation keys, and sampling posture stay keyboard discoverable.
- One verification note is environment-local rather than product-local: the first concurrent `npm test` run collided with another active viewer bind on port `4328` while `npm run test:smoke` was running in parallel. The authoritative rerun used `PROVISIONING_VIEWER_PORT=4332` and passed cleanly.

## Verification Evidence

- `node --check /Users/test/Code/taxat_/automation/provisioning/report_viewer/app.js`
- `node --experimental-strip-types /Users/test/Code/taxat_/infra/observability/bootstrap/provision_otel_collection_and_backends.ts --emit`
- `npm run typecheck --workspaces=false` in `/Users/test/Code/taxat_/automation/provisioning`
- `npm run test:unit --workspaces=false -- automation/provisioning/tests/unit/correlation_key_policy.spec.ts automation/provisioning/tests/unit/sampling_and_retention_policy.spec.ts` (`4 passed`)
- `npm exec --workspaces=false -- playwright test --config=/Users/test/Code/taxat_/tests/integration/playwright.config.ts /Users/test/Code/taxat_/tests/integration/observability/otel_stack_bootstrap.spec.ts` (`1 passed`)
- `npm exec --workspaces=false -- playwright test /Users/test/Code/taxat_/tests/playwright/provisioning/observability/telemetry_signal_atlas.spec.ts` (`2 passed`)
- `npm run test:smoke --workspaces=false` in `/Users/test/Code/taxat_/automation/provisioning` (`16 passed`)
- `PROVISIONING_VIEWER_PORT=4332 npm test --workspaces=false` in `/Users/test/Code/taxat_/automation/provisioning` (`93 passed`)
- `./.venv/bin/python3` schema-validation snippet against [telemetry_topology.schema.json](/Users/test/Code/taxat_/infra/observability/contracts/telemetry_topology.schema.json) and [observability_inventory.template.json](/Users/test/Code/taxat_/data/provisioning/observability_inventory.template.json) (`validated observability inventory schema`)
- `./.venv/bin/python3 /Users/test/Code/taxat_/Algorithm/scripts/validate_contracts.py --self-test` (`PASS: schemas, bundled samples, and custom invariants validated.`)
- `./.venv/bin/python3 /Users/test/Code/taxat_/Algorithm/tools/forensic_contract_guard.py` (`PASS`)

## Current Provider Grounding

Revalidated on `2026-04-18` against current official docs:

- OpenTelemetry gateway deployment: [opentelemetry.io/docs/collector/deploy/gateway/](https://opentelemetry.io/docs/collector/deploy/gateway/)
- OpenTelemetry agent deployment: [opentelemetry.io/docs/collector/deploy/agent/](https://opentelemetry.io/docs/collector/deploy/agent/)
- OpenTelemetry agent-to-gateway deployment: [opentelemetry.io/docs/collector/deploy/other/agent-to-gateway/](https://opentelemetry.io/docs/collector/deploy/other/agent-to-gateway/)
- OpenTelemetry Collector resiliency: [opentelemetry.io/docs/collector/resiliency/](https://opentelemetry.io/docs/collector/resiliency/)
- OpenTelemetry resources and `deployment.environment.name`: [opentelemetry.io/docs/concepts/resources/](https://opentelemetry.io/docs/concepts/resources/) and [opentelemetry.io/docs/specs/semconv/registry/attributes/deployment/](https://opentelemetry.io/docs/specs/semconv/registry/attributes/deployment/)
- OpenTelemetry resource semantic conventions: [opentelemetry.io/docs/specs/semconv/resource/](https://opentelemetry.io/docs/specs/semconv/resource/)
- Sentry OpenTelemetry support: [docs.sentry.io/platforms/node/performance/instrumentation/opentelemetry](https://docs.sentry.io/platforms/node/performance/instrumentation/opentelemetry)
