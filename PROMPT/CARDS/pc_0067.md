---
id: pc_0067
slug: phase_02_seq_067_create_outbox_inbox_event_envelope_and_idempotency_foundation_libraries
mode: sequential
phase: "02"
wave: null
track: null
task_number: "067"
block_key: phase_02_seq
original_key: phase_02_seq_067
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0067

## Task Summary

create outbox inbox event envelope and idempotency foundation libraries.

## Metadata

- ID: `pc_0067`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `067`
- Source Slug: `phase_02_seq_067_create_outbox_inbox_event_envelope_and_idempotency_foundation_libraries`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0066](./pc_0066.md) `phase_02_seq_066_create_database_migration_framework_and_schema_bundle_versioning_baseline`
- Next: [pc_0068](./pc_0068.md) `phase_02_seq_068_create_command_envelope_problem_envelope_and_receipt_contract_runtime_scaffold`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0062_to_0069.md](../shared_operating_contract_0062_to_0069.md)
```text
Execute roadmap task `phase_02_seq_067_create_outbox_inbox_event_envelope_and_idempotency_foundation_libraries`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0066` if they exist, especially `pc_0020`, `pc_0052`, `pc_0063`, `pc_0064`, `pc_0065`, and `pc_0066`.

Mission:
Build the messaging foundation that allows side effects and async propagation without losing durable truth, idempotency, or correlation.
This task must distinguish authoritative records from broker packets, preserve the corpus' request and duplicate semantics, and make inbox/outbox processing replayable and safe under at-least-once delivery.
The result should give later services a lawful foundation for internal async work, authority-bound dispatch, and recovery instrumentation.

Source algorithm grounding you must honor:
- `authority_interaction_protocol.md`, especially the logical blocks and formulas around `duplicate_meaning_key`, `request_hash`, `idempotency_key`, dispatch lineage, and authenticated intake/dedupe boundaries.
- `manifest_and_config_freeze_contract.md`, especially the canonical idempotency basis and frozen execution inputs.
- `data_model.md` where authority submission/response, dispatch, packet, and correlation objects preserve hash and identity lineage.
- `security_and_runtime_hardening_contract.md`, especially the rule that queues, outboxes, and logs carry opaque refs instead of raw secrets or oversized business payloads.
- `deployment_and_resilience_contract.md`, `observability_and_audit_contract.md`, and `replay_and_reproducibility_contract.md` where retries, recovery, and evidence interact with messaging.
- Earlier eventing/storage/control-store decisions from `pc_0020`, `pc_0050`, `pc_0052`, `pc_0064`, `pc_0065`, and `pc_0066`.

Deliverables to create:
1. `packages/domain-kernel/src/messaging/event_envelope.ts`.
2. `packages/domain-kernel/src/messaging/outbox_record.ts`.
3. `packages/domain-kernel/src/messaging/inbox_record.ts`.
4. `packages/domain-kernel/src/messaging/idempotency.ts`.
5. `packages/domain-kernel/src/messaging/correlation.ts`.
6. `config/messaging/event_envelope_catalog.json`.
7. `config/messaging/idempotency_scope_matrix.json`.
8. `config/messaging/retry_and_dead_letter_policy.json`.
9. `config/messaging/message_redaction_policy.json`.
10. `docs/messaging/outbox_inbox_and_idempotency_runbook.md`.
11. `apps/operator-web/src/routes/internal/event-envelope-atlas.tsx`.
12. `tests/unit/messaging/idempotency_foundation.spec.ts`.
13. `tests/integration/messaging/outbox_inbox_flow.spec.ts`.
14. `tests/playwright/internal/event_envelope_atlas.spec.ts`.

Required design decisions you must make explicit:
- Define the envelope shape for internal async messages without replacing the authoritative domain records they point at.
- Define how `duplicate_meaning_key`, `request_hash`, and `idempotency_key` are computed and reused across inbox/outbox workflows.
- Define dedupe scopes for producer side, consumer side, and authority-interaction side.
- Define retry, visibility timeout, poison/dead-letter, and replay posture without making the broker itself authoritative.
- Define redaction rules so queue-visible payloads remain small, opaque, and secret-safe.
- Define causation/correlation lineage and how it maps to durable audit versus transient telemetry.
- Define how inbox processing proves exactly-what-was-seen even when transport is at-least-once.

Execution steps:
1. Create a machine-readable event-envelope catalog covering the internal message families the current repo needs, keyed to authoritative source record families rather than vague event names.
2. Implement outbox and inbox record helpers that preserve record refs, idempotency basis, causation/correlation, attempt metadata, and redaction posture.
3. Implement canonical idempotency helpers using the primitives from `pc_0064` and the corpus formulas from authority-interaction and manifest contracts.
4. Implement retry and dead-letter policy files that separate broker delivery concerns from durable domain state.
5. Implement redaction policy so payloads carry only what later processing lawfully needs; prefer refs and hashes over duplicated business blobs.
6. Add a runbook explaining how outbox/inbox coordination differs from authoritative records and how replay/recovery should re-drive effects safely.
7. Build `event-envelope-atlas` as an internal lane view from emit → outbox → broker → inbox → effect/audit.
8. Add unit, integration, and Playwright tests covering resend, dedupe, partial failure, and dead-letter posture.

Visual and interaction specification for `event-envelope-atlas`:
- Tone: a premium packet-lane map.
  The page should feel like inspecting disciplined traffic flow, not a message-broker admin console.
- Layout:
  - `64px` top bar with envelope catalog badge, queue fabric badge, and selected message-family chip.
  - left family rail `248-264px` listing `EMIT`, `OUTBOX`, `BROKER`, `INBOX`, `EFFECT`, `AUDIT`.
  - center lane canvas with one horizontal transport band per selected message family.
  - right inspector `364-396px` with idempotency basis, retry posture, redaction class, and dead-letter policy.
- Diagram:
  - hero diagram is a five-lane packet flow.
  - each packet is represented by a compact pill that expands into refs and hashes on selection.
  - include a small monoline packet glyph with a checksum slash.
- Palette:
  - background `#F4F5F3`
  - primary surface `#FFFFFF`
  - secondary surface `#EEF0EC`
  - ink `#101418`
  - muted ink `#68717A`
  - hairline `rgba(16,20,24,0.08)`
  - accent navy `#3D5876`
  - accent moss `#5B715C`
  - accent ochre `#8A6640`
  - success `#17624B`
  - warning `#8C5D1B`
  - danger `#A53A31`
- Typography:
  - compact and ledger-like.
  - monospace only for hashes, ids, refs, and queue/topic names.
- Dimensional constraints:
  - max width `1500px`
  - lane min height `72px`
  - packet pill height `28px`
  - inspector policy rows `36px`
- Motion:
  - `120ms` packet selection and inspector reveal.
  - reduced motion removes packet travel animation and uses opacity/outline only.
- Interaction:
  - selecting a packet must reveal the authoritative record family it points to and the dedupe policy in force.
  - accessible names must read as transport statements such as `outbox packet for authority submission uses duplicate meaning scope`.
  - Playwright tests must use semantic text/role locators and verify no hidden payload blob appears in the DOM.

Gap handling you must perform:
- Close the gap where async work might treat broker packets as authoritative truth instead of pointers to durable records.
- Close the gap where idempotency semantics exist in the corpus but not in shared runtime helpers.
- Close the gap where retries and dead letters are often operationally defined but not serialized into product-safe policy files.
- Close the gap where redaction/sizing rules for queued payloads are unspecified and secrets or oversized blobs could leak into transports.
- Close the gap where inbox processing cannot prove what was seen and what was deduped.

Mandatory edge cases:
- same semantic request resent with a different transport packet id
- same packet delivered twice by the broker
- consumer crash after side effect but before ack
- dead-letter routing after repeated safe retries
- authority dispatch needing identity-bound idempotency separate from generic internal event dedupe
- stale or missing source record ref when an inbox packet arrives

Backend, performance, and security requirements:
- Outbox writes and authoritative state changes should be coordinated transactionally where the datastore allows.
- Inbox handlers must be idempotent and explicit about dedupe scopes.
- Never store raw secrets or overly broad business payloads inside packet envelopes.
- Separate audit records from transient transport metadata even when both share correlation ids.
- The atlas must visualize policy and lineage, not live-broker secrets or uncontrolled payload dumps.

Validation rules:
- Unit-test idempotency helpers, dedupe scopes, redaction classes, and retry-policy parsing.
- Integration-test an outbox → broker stub → inbox flow with duplicate delivery and partial failure recovery.
- Prove that a dead-lettered packet retains enough refs/hashes for investigation without needing raw secret-bearing payload copies.
- Use Playwright to verify the atlas' semantic lane structure, keyboard reachability, reduced-motion posture, and redaction-safe rendering.
- Ensure messaging helpers align with authority-interaction formulas and earlier event-fabric policy artifacts.

Definition of done:
- The repo has shared outbox/inbox/idempotency foundations rather than ad hoc queue code.
- Authoritative truth stays in durable records while packets remain transport artifacts.
- Retry, dead-letter, and redaction posture are serialized and testable.
- Later async features can compose on a lawful messaging substrate.
- The internal atlas explains flow and lineage without becoming a broker console.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
