---
id: pc_0168
slug: phase_03_parallel_wave_01_track_backend_northbound_012_implement_access_rebind_resume_token_and_shell_stability_guard_failure_paths
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "012"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_012
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0168

## Task Summary

implement access rebind resume token and shell stability guard failure paths.

## Metadata

- ID: `pc_0168`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `012`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_012_implement_access_rebind_resume_token_and_shell_stability_guard_failure_paths`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0167](./pc_0167.md) `phase_03_parallel_wave_01_track_backend_northbound_011_implement_problem_envelope_mapping_stale_view_guards_and_if_match_validation`
- Next: [pc_0169](./pc_0169.md) `phase_04_parallel_wave_02_track_backend_low_noise_001_implement_low_noise_experience_frame_projector_and_surface_budget_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0166_to_0173.md](../shared_operating_contract_0166_to_0173.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_012_implement_access_rebind_resume_token_and_shell_stability_guard_failure_paths`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0167` if they exist, especially `pc_0070` through `pc_0077`, `pc_0158` through `pc_0165`, and `pc_0167`.

Mission:
Implement the canonical failure-path services for `ACCESS_REBIND_REQUIRED`, `REBASE_REQUIRED`, stale shell-stability guards, and resume-token invalidation across manifest and collaboration snapshot/stream routes plus any northbound mutation that reuses those grouped markers.
This task is not about inventing a second stream transport; it is about making all routes speak one recovery language when session/access/masking/schema drift or route/epoch/history drift breaks continuity.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially the manifest stream rules, failure/recovery rules, and stale receipt guidance.
- `stream_resume_and_catch_up_ordering_contract.md`.
- `collaboration_workspace_contract.md`, especially grouped recovery semantics, `VIEW_STALE`, and access/scope drift language.
- `frontend_shell_and_interaction_law.md`, especially route-stability primitives and grouped `stability_contract` usage.
- `data_model.md`, especially cursor/recovery/problem artifacts and route-stability fields.
- `schemas/problem_envelope.schema.json`, `schemas/stream_recovery_contract.schema.json`, `schemas/experience_cursor.schema.json`, and `schemas/workspace_cursor.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially the recovery-family separation and mixed-generation hardening items.

Deliverables to create:
1. `packages/backend-northbound/src/services/build_access_rebind_required_problem.ts`
2. `packages/backend-northbound/src/services/build_rebase_required_problem.ts`
3. `packages/backend-northbound/src/services/detect_route_or_epoch_rebase_requirement.ts`
4. `packages/backend-northbound/src/services/detect_access_binding_or_masking_rebind_requirement.ts`
5. `packages/backend-northbound/src/services/invalidate_resume_token_on_binding_drift.ts`
6. `packages/backend-northbound/src/services/issue_replacement_resume_anchor.ts`
7. `packages/backend-northbound/src/services/validate_shell_stability_guard.ts`
8. `packages/backend-northbound/src/http/stream_recovery_error_mapper.ts`
9. `packages/backend-northbound/src/tests/recovery_and_rebind_failure_paths.spec.ts`
10. `docs/northbound/recovery_and_rebind_failure_paths.md`
11. Optional but useful: `apps/admin-console-web/src/routes/debug/recovery-lab.tsx`

Required design decisions you must make explicitly:
- define the exact discriminant between `REBASE_REQUIRED` and `ACCESS_REBIND_REQUIRED`
- define how replacement snapshot refs, replacement resume anchors, and grouped stability contracts are emitted together without creating two competing recovery bases
- define when a stale shell-stability token belongs in a command failure versus a stream failure versus a snapshot-read failure
- decide whether resume-token invalidation records are persisted as first-class audit artifacts or derived on demand from grouped contract drift; choose one and keep it deterministic

Execution requirements:
- reuse the grouped `stream_recovery_contract` as the authority; never treat raw `resume_token` as the primary continuity object
- make route/session/access/masking/schema drift detection reusable by both manifest and collaboration flows
- wire the shared failure helpers into existing northbound snapshot/stream endpoints instead of leaving old ad hoc branches alive
- preserve same-object and same-shell continuity when recovery is possible; only escalate to broader rebase when the algorithm requires it
- if you add the optional lab, keep it diagnostic and render one ordered recovery ladder, one grouped guard table, and one narrow event strip rather than a metrics dashboard

Gap handling you must close while implementing:
- the corpus defines grouped recovery semantics but does not hand teams one shared implementation boundary; create that boundary now so snapshot, stream, and mutation code stop diverging
- older stream work may only validate raw resume tokens; retrofit grouped route/session/access/masking binding checks
- access drift is easy to misreport as plain stale view; explicitly separate those code paths and test them against customer/staff/masked audiences
- collaboration and manifest routes can drift apart in replacement-snapshot behavior; unify them around one service grammar while preserving route-specific refs

Mandatory edge cases:
- epoch advance, route drift, shell-stability drift, and history compaction must force `REBASE_REQUIRED`
- session drift, access-binding drift, masking drift, and schema drift must force `ACCESS_REBIND_REQUIRED`
- mixed-audience or mixed-mask resume tokens must fail closed
- collaboration stale-view failures must keep the latest snapshot ref and a fresh resume anchor coherent when the algorithm allows both
- customer-visible sessions must never regain internal-lane history or refs through a rebind path
- no route may silently auto-rebase a destructive or filing-capable action

Playwright and contract-test requirements:
- use Playwright APIRequestContext for snapshot/stream failure tests
- if a browser or EventSource harness is needed, drive it with Playwright and explicit event-state assertions; no fixed sleeps
- cover manifest and collaboration snapshot reads, stream reconnect, stale command failures, route drift, access drift, masking drift, and schema drift
- capture ordered event logs / recovery payloads on failure

Backend, performance, and security requirements:
- bind resume tokens and shell-stability tokens to the full grouped recovery contract, not just object id plus sequence
- keep drift detection and failure construction server-side and deterministic
- reject mixed-generation marker sets before any mutation or stream catch-up continues
- prefer indexed frontier lookups over scanning event history to decide rebase posture

Validation rules:
- validate all recovery failures against `problem_envelope.schema.json`
- verify experience/workspace cursor invariants continue to hold after the refactor
- unit-test route-drift, epoch-drift, session-drift, access-drift, masking-drift, and schema-drift detection
- integration-test manifest and collaboration routes with both same-object recovery and terminal failure cases

Definition of done:
- the repo has one canonical implementation boundary for rebase vs access-rebind failure handling
- resume-token invalidation and shell-stability-guard rejection are deterministic and route-safe
- existing northbound snapshot/stream/mutation routes reuse the new grouped recovery helpers instead of hand-coded divergence
- customer/staff/masked continuity remains fail-closed and same-shell aware under reconnect and drift pressure.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

