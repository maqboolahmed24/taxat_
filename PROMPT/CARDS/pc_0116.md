---
id: pc_0116
slug: phase_03_parallel_wave_01_track_backend_collection_008_implement_canonical_fact_promotion_and_partition_isolation_pipeline
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "008"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0116

## Task Summary

implement canonical fact promotion and partition isolation pipeline.

## Metadata

- ID: `pc_0116`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `008`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_008_implement_canonical_fact_promotion_and_partition_isolation_pipeline`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0115](./pc_0115.md) `phase_03_parallel_wave_01_track_backend_collection_007_implement_conflict_detection_and_conflict_set_persistence`
- Next: [pc_0117](./pc_0117.md) `phase_03_parallel_wave_01_track_backend_collection_009_implement_late_data_indicators_bindings_and_input_freeze_persistence`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0110_to_0117.md](../shared_operating_contract_0110_to_0117.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_008_implement_canonical_fact_promotion_and_partition_isolation_pipeline`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0115` if they exist, especially `pc_0010`, `pc_0011`, `pc_0017`, `pc_0028`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0077`, `pc_0080`, `pc_0114`, and `pc_0115`.

Mission:
Implement the canonical-promotion pipeline that turns eligible candidates into schema-valid `CanonicalFact` artifacts without leaking partition scope, support lineage, or masked projection input.
This task must preserve exact promotion lineage, enforce blocking-conflict rules, and emit deterministic canonical identities.
It must keep `PROVISIONAL`, `CANONICAL`, and `CONTESTED` posture explicit rather than treating promotion as a boolean.

Source algorithm grounding you must honor:
- `modules.md`, especially `PROMOTE_CANONICAL_FACTS(...)`.
- `core_engine.md`, especially candidate/canonical exact partition rule, no-cross-partition-promotion rule, and promotion fail-closed posture.
- `canonical_fact_promotion_and_partition_isolation_contract.md`.
- `canonical_source_and_evidence_taxonomy.md`, especially fact-family separation and adjustment-fact discipline.
- `data_model.md`, especially `CanonicalFact` and `CanonicalFactSet`.
- Existing schemas including `canonical_fact.schema.json`, `canonical_fact_set.schema.json`, and `sample_canonical_fact.json`.

Deliverables to create:
1. `packages/backend-collection/src/models/canonical_fact.ts`.
2. `packages/backend-collection/src/repositories/canonical_fact_repository.ts`.
3. `packages/backend-collection/src/services/promote_canonical_facts.ts`.
4. `packages/backend-collection/src/services/canonical_identity_hash.ts`.
5. `packages/backend-collection/src/services/build_promotion_record.ts`.
6. `packages/backend-collection/src/services/select_promotion_state.ts`.
7. `packages/backend-collection/src/services/partition_promotion_guard.ts`.
8. `packages/backend-collection/src/services/candidate_to_canonical_mapper.ts`.
9. `packages/backend-collection/src/services/resolve_canonical_freshness_state.ts`.
10. `docs/collection/canonical_fact_promotion_and_partition_isolation.md`.
11. `tests/unit/backend_collection/canonical_fact_promotion.spec.ts`.
12. `tests/integration/backend_collection/canonical_fact_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the exact rule that selects `PROVISIONAL`, `CANONICAL`, or `CONTESTED` from conflict frontier and promotion policy.
  Be explicit about `MONITORING_ONLY` posture and when it may still yield `CANONICAL`.
- Define the canonical identity and dedupe preimage for promoted facts.
- Define how freshness state is selected or inherited during promotion.
- Define how `promotion_record{...}` is assembled and how it preserves the upstream conflict frontier, rule ref, override ref, and promotion activity ref.
- Define how duplicate logical candidates in one partition collapse into one canonical truth without dropping lineage.

Execution steps:
1. Implement schema-backed `CanonicalFact` model and repository.
2. Implement `select_promotion_state.ts` and `build_promotion_record.ts` so promotion outcome is deterministic, explicit, and schema-coherent.
3. Implement `candidate_to_canonical_mapper.ts` and `canonical_identity_hash.ts` so promoted facts preserve exact source/evidence lineage, exact partition binding, and deterministic identity/dedupe.
4. Implement `partition_promotion_guard.ts` so any partition widening or mixed-partition lineage fails closed.
5. Implement `resolve_canonical_freshness_state.ts` so freshness remains explicit and not silently upgraded by promotion.
6. Add tests for clear-frontier promotion, monitoring-only promotion, blocking conflict prohibition, contested posture, duplicate logical candidates, and cross-partition contamination.

Gap handling you must perform:
- Close the gap where the corpus requires exact promotion lineage but does not provide one executable promotion-state selector.
- Close the gap where `MONITORING_ONLY` conflict posture could be interpreted inconsistently across implementations.
- Close the gap where canonical dedupe could drop support lineage or merge incompatible candidate populations.
- Close the gap where cross-partition contamination might otherwise be normalized into widened canonical scope.

Mandatory edge cases:
- `promotion_state = CANONICAL` must require zero blocking conflicts at promotion time.
- `promotion_state = CONTESTED` must carry non-empty `conflict_membership_refs[]`.
- canonical facts must preserve at least one supporting evidence ref and exact lineage hashes.
- `partition_scope_refs[]` must contain exactly one scope and match the scalar partition field.
- masked or customer-safe projections must never be accepted as promotion input.
- analysis-mode canonical facts must remain `analysis_only = true` with non-null counterfactual basis.
- `ADJUSTMENT_FACT` must preserve exact adjustment binding and `analysis_mode_treatment` requirements.
- duplicate logical candidates within one partition must not produce unstable canonical duplicates.

Backend, performance, and security requirements:
- Promotion must be deterministic and replay-safe.
- Keep candidate lineage immutable; promotion creates new canonical artifacts instead of mutating source/evidence truth.
- Never allow support-free or masked input to reach canonical promotion.
- Use canonical hashing and stable ordering utilities from earlier cards instead of ad hoc string concatenation.

Validation rules:
- Unit-test promotion-state selection, promotion-record construction, canonical identity hashing, and partition guards.
- Integration-test candidate-to-canonical persistence across clear, monitoring-only, and blocked frontiers.
- Validate persisted canonical facts against `canonical_fact.schema.json`.
- Prove that cross-partition promotion and support-free canonical facts are impossible.

Definition of done:
- The repo can persist first-class `CanonicalFact` artifacts with exact promotion lineage and partition integrity.
- Canonical promotion no longer depends on informal heuristics; it is an auditable, deterministic service.
- Later compute, parity, trust, graph, and filing tasks can consume explicit canonical truth rather than deriving it ad hoc.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
