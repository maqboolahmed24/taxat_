---
id: pc_0112
slug: phase_03_parallel_wave_01_track_backend_collection_004_implement_materialize_source_records_and_evidence_item_persistence_pipeline
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "004"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0112

## Task Summary

implement materialize source records and evidence item persistence pipeline.

## Metadata

- ID: `pc_0112`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `004`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_004_implement_materialize_source_records_and_evidence_item_persistence_pipeline`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0111](./pc_0111.md) `phase_03_parallel_wave_01_track_backend_collection_003_implement_connector_binding_resolution_and_fetch_dispatch_pipeline`
- Next: [pc_0113](./pc_0113.md) `phase_03_parallel_wave_01_track_backend_collection_005_implement_freeze_normalization_context_declared_exclusions_and_missing_source_tracking`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0110_to_0117.md](../shared_operating_contract_0110_to_0117.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_004_implement_materialize_source_records_and_evidence_item_persistence_pipeline`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0111` if they exist, especially `pc_0011`, `pc_0028`, `pc_0046`, `pc_0047`, `pc_0048`, `pc_0051`, `pc_0060`, `pc_0061`, `pc_0069`, `pc_0076`, `pc_0080`, `pc_0109`, `pc_0110`, and `pc_0111`.

Mission:
Implement the raw-fetch-to-artifact materialization pipeline that persists first-class `SourceRecord` and `EvidenceItem` artifacts from normalized dispatch results.
This task must preserve exact raw origin, extraction lineage, strength tier, freshness, retention, erasure posture, and exact partition identity.
It must not collapse raw payloads into facts.
Documentary or low-confidence extraction may remain weak, but it must still be explicit, attributable, and replay-safe.

Source algorithm grounding you must honor:
- `modules.md`, especially `MATERIALIZE_SOURCE_RECORDS(...)` and `MATERIALIZE_EVIDENCE_ITEMS(...)`.
- `canonical_source_and_evidence_taxonomy.md`, especially source classes, fact families, strength tiers, confidence model, freshness model, and the rule that raw payload alone is never canonical fact.
- `data_model.md`, especially `SourceRecord`, `EvidenceItem`, and their set artifacts.
- `provenance_graph_semantics.md`, especially raw-source entry and evidence attachment rules.
- `retention_and_privacy.md`, `audit_and_provenance.md`, and `observability_and_audit_contract.md` for retention tags, erasure posture, and lineage retention.
- Existing schemas including `source_record.schema.json`, `evidence_item.schema.json`, `source_record_set.schema.json`, and `evidence_item_set.schema.json`.

Deliverables to create:
1. `packages/backend-collection/src/models/source_record.ts`.
2. `packages/backend-collection/src/models/evidence_item.ts`.
3. `packages/backend-collection/src/repositories/source_record_repository.ts`.
4. `packages/backend-collection/src/repositories/evidence_item_repository.ts`.
5. `packages/backend-collection/src/services/materialize_source_records.ts`.
6. `packages/backend-collection/src/services/materialize_evidence_items.ts`.
7. `packages/backend-collection/src/services/source_strength_tier_resolver.ts`.
8. `packages/backend-collection/src/services/evidence_kind_classifier.ts`.
9. `packages/backend-collection/src/services/evidence_lineage_ref_builder.ts`.
10. `packages/backend-collection/src/services/content_ref_allocator.ts`.
11. `db/migrations/phase03_0112_source_record_and_evidence_item.sql`.
12. `docs/collection/source_record_and_evidence_item_materialization.md`.
13. `tests/unit/backend_collection/source_record_and_evidence_item_materialization.spec.ts`.
14. `tests/integration/backend_collection/source_record_and_evidence_item_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the canonical mapping from `source_class` to `source_strength_tier` and any default freshness posture.
- Define the closed internal vocabulary for `capture_method` and `evidence_kind` even though the current schemas allow free-form strings.
- Define how raw payload refs and extracted content refs are stored so retention, quarantine, and replay can reopen the exact binary or payload later.
- Define how `business_partition` and `period_partition` are derived and validated from planned-source scope and provider payload identity.
- Define the dedupe policy for repeated identical raw payloads within one manifest versus separate retained source records across different partitions or providers.

Execution steps:
1. Implement schema-backed `SourceRecord` and `EvidenceItem` models plus repositories with indexes on `manifest_id`, partition, source class, and collection-boundary ref.
2. Implement `materialize_source_records.ts` so every normalized fetch result becomes one or more `SourceRecord` artifacts preserving raw payload hash, provider identity, provider account ref, capture method, period, tenant/client, partition, retention tag, and erasure state.
3. Implement `materialize_evidence_items.ts` so retained evidence is produced from source records and extraction rules without skipping attribution; every evidence item must preserve `content_ref`, extraction method/confidence, freshness, strength tier, and lineage refs.
4. Implement `source_strength_tier_resolver.ts`, `evidence_kind_classifier.ts`, and `evidence_lineage_ref_builder.ts` so strength/confidence/lineage are deterministic and stable across replay.
5. Integrate malware/quarantine and OCR/manual-extraction seams from earlier cards without letting blocked or unexamined content masquerade as a normal evidence item.
6. Add tests for structured feeds, documentary evidence, declared assertions, duplicate payloads, quarantine/low-confidence posture, and cross-partition validation failures.

Gap handling you must perform:
- Close the gap where schemas require `capture_method`, `evidence_kind`, and lineage fidelity but the corpus does not provide one closed implementation vocabulary for them.
- Close the gap where documentary evidence with low-confidence extraction could otherwise disappear instead of persisting as explicit weak evidence.
- Close the gap where quarantine/retention-limited payloads could lose lineage or be silently skipped.
- Close the gap where raw payload dedupe could accidentally merge different provider accounts, partitions, or manifest scopes.

Mandatory edge cases:
- every `SourceRecord` must preserve non-empty raw hash and raw payload ref.
- every `EvidenceItem` must preserve non-null `extraction_method`, `extraction_confidence`, and at least one lineage ref.
- documentary evidence with no successful text extraction must still remain explicit as retained evidence with a typed extraction method or review-needed posture; it must not vanish.
- identical payload bytes from different provider accounts or partitions must not dedupe into one source record.
- a source/evidence artifact whose derived partition does not exactly match the frozen planned scope must fail closed.
- quarantine or malware-blocked payloads must not become normal evidence support without explicit limited/quarantined posture.
- retention-limited or erasure-pending posture must remain machine-readable on persisted artifacts.

Backend, performance, and security requirements:
- Preserve replay-safe raw payload lineage; do not normalize away the exact raw origin.
- Do not expose raw payload bodies or binary blobs in logs or exception messages.
- Keep storage references object-store friendly and content-addressable where possible.
- Materialization must be batch-capable and idempotent for retried normalized fetch results.
- Reuse retention-tag and erasure-state machinery from earlier cards rather than inventing collection-local retention fields.

Validation rules:
- Unit-test source-strength mapping, evidence-kind classification, lineage-ref stability, and cross-partition guards.
- Integration-test round-trip persistence for structured feeds, documentary evidence, and duplicate-but-not-equivalent payloads.
- Validate `SourceRecord` and `EvidenceItem` payloads against their schemas.
- Prove that weak extraction remains explicit and attributable instead of being dropped or auto-promoted.

Definition of done:
- The repo can persist first-class `SourceRecord` and `EvidenceItem` artifacts from normalized fetch results.
- Raw and evidential layers remain distinct, replay-safe, and retention-aware.
- Later candidate-extraction and provenance-graph tasks can consume explicit retained artifacts rather than re-reading transport payloads.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
