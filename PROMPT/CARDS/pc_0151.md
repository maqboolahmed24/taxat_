---
id: pc_0151
slug: phase_03_parallel_wave_01_track_backend_workflow_007_implement_work_inbox_snapshot_and_work_inbox_delta_projectors
mode: parallel
phase: '03'
wave: '01'
track: backend_workflow
task_number: '007'
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_workflow_007_implement_work_inbox_snapshot_and_work_inbox_delta_projectors
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0151

## Task Summary

work inbox snapshot and work inbox delta projectors.

## Metadata

- ID: `pc_0151`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend workflow`
- Task Number: `007`
- Source Slug: `phase_03_parallel_wave_01_track_backend_workflow_007_implement_work_inbox_snapshot_and_work_inbox_delta_projectors`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0150](./pc_0150.md) `phase_03_parallel_wave_01_track_backend_workflow_006_implement_workspace_snapshot_customer_request_list_snapshot_and_activity_slice_projectors`
- Next: [pc_0152](./pc_0152.md) `phase_03_parallel_wave_01_track_backend_workflow_008_implement_customer_safe_projection_and_visibility_partition_contract_enforcement`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0150_to_0157.md](../shared_operating_contract_0150_to_0157.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_workflow_007_implement_work_inbox_snapshot_and_work_inbox_delta_projectors`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0150` if they exist, especially `pc_0145`, `pc_0146`, `pc_0149`, and `pc_0150`.

Mission:
Implement the authoritative staff triage inbox snapshot and live-update delta projectors.
This card must make `WorkInboxSnapshot` and `WorkInboxDelta` the only lawful queue-bearing read models for the staff inbox so row order, queue health, unread badges, selection continuity, and focus-safe live updates never depend on websocket arrival order or renderer-local sorting.

Source algorithm grounding you must honor:
- `data_model.md`, especially `WorkInboxSnapshot`, `WorkInboxDelta`, queue projections, row actions, and queue-health fields.
- `collaboration_workspace_contract.md`, especially section `7.3 Read models`, row contract rules, delta rules, and inbox read-surface requirements.
- `frontend_shell_and_interaction_law.md`, especially same-shell route continuity for queue/list read models.
- `low_noise_experience_contract.md` and `semantic_selector_and_accessibility_contract.md`.
- `compute_parity_and_trust_formulas.md`, especially queue-health and collaboration-priority formulas.
- `schemas/work_inbox_snapshot.schema.json`, `schemas/work_inbox_delta.schema.json`, `schemas/collaboration_queue_projection_contract.schema.json`, and `schemas/work_queue_health_contract.schema.json`.
- validator drift cases for inbox snapshots / deltas in `scripts/validate_contracts.py`.

Deliverables to create:
1. `packages/backend-workflow/src/projectors/build_work_inbox_snapshot.ts`
2. `packages/backend-workflow/src/projectors/build_work_inbox_delta.ts`
3. `packages/backend-workflow/src/projectors/coalesce_work_inbox_delta.ts`
4. `packages/backend-workflow/src/projectors/build_work_queue_health_contract.ts`
5. `packages/backend-workflow/src/projectors/derive_work_inbox_selection_state.ts`
6. `packages/backend-workflow/src/repositories/work_inbox_snapshot_repository.ts`
7. `packages/backend-workflow/src/repositories/work_inbox_delta_repository.ts`
8. `packages/backend-workflow/src/services/project_work_inbox_for_viewer.ts`
9. `packages/backend-workflow/src/services/project_incremental_work_inbox_delta.ts`
10. `docs/workflow/work_inbox_snapshot_and_delta_rules.md`
11. `tests/unit/backend_workflow/work_inbox_snapshot_projector.spec.ts`
12. `tests/unit/backend_workflow/work_inbox_delta_projector.spec.ts`
13. `tests/integration/backend_workflow/work_inbox_live_update_flow.spec.ts`
14. `tests/playwright/admin_console/work_inbox_preview.spec.ts`

Optional verification harness if the real inbox route is not yet executable:
15. `apps/admin-console-web/src/routes/debug/work-inbox-preview.tsx`
16. `apps/shared-ui/src/components/debug/WorkInboxPreview.tsx`

Required design decisions you must make explicit:
- Define the exact projector boundary between snapshot and delta, including when to emit `delivery_class in {LIVE, CATCH_UP, SNAPSHOT}`.
- Define the canonical selected-row contract: `selected_item_ref`, `selected_focus_anchor_ref_or_null`, and how focus-safe reorders/removals behave when the selected row is active.
- Define how `queue_health_contract{...}` is built, versioned, and mirrored into row-level routing contracts.
- Define the exact mapping from routing-contract fields to top-level row fields so the same order and recommendation posture survives inbox, workspace, notification-open, and replay.
- Define delta rules for `row_upserts[]`, `row_removals[]`, `badge_updates[]`, `order_changed`, and `defer_reorder_until_focus_exit`.
- Define action-availability and mutation-precondition-binding rules for inbox row actions.

Execution steps:
1. Implement the snapshot projector from persisted workflow, routing, unread, escalation, due, and notification lineage only.
2. Implement one canonical queue-health-contract builder that consumes the frozen analytics basis rather than raw backlog counts.
3. Implement the delta projector keyed by stable `causal_semantic_action_id` and row `queue_projection_basis_hash`.
4. Implement coalescing and replay-safe dedupe so repeated downstream publishes do not produce contradictory row upserts / removals for the same semantic action.
5. Stamp every row with the exact mirrored routing / queue / badge fields required by the schemas and contracts.
6. If the real inbox route is not yet available, add a thin preview harness inside `CALM_SHELL` to render the persisted snapshot and apply deltas in-browser.
7. Use Playwright to verify ordering, selection continuity, focus-safe reorder deferral, and badge updates.

Gap handling you must perform:
- Close the gap where the staff inbox is reconstructed from workspace snapshots or local sorting instead of one dedicated snapshot / delta pair.
- Close the gap where live updates can reorder or remove the focused row mid-triage with no persisted deferral posture.
- Close the gap where split unread badges and module badge mirrors can drift from the row’s queue projection.
- Close the gap where filter chips, queue-health posture, and action bindings can diverge from the routing contract that justified the row’s placement.

Mandatory edge cases:
- snapshot row order MUST follow the persisted canonical sort tuple, not local row insertion order
- `selected_item_ref` and `selected_focus_anchor_ref_or_null` MUST point at a currently returned row when non-null
- `primary_action_code` MUST be a member of `available_action_codes[]` and MUST NOT also appear in `blocked_action_codes[]`
- row-level `authoritative_action{...}` MUST match visible action posture, route, and blocking reason
- mutating row actions MUST carry the right `mutation_precondition_binding_or_null`
- delta payloads MUST NOT both upsert and remove the same item in one batch
- split unread badge values in `badge_updates[]` MUST match the row’s queue-projection basis
- when a focused row changes order, `defer_reorder_until_focus_exit` MUST follow the routing contract’s focus continuity posture
- `FILTER_EXIT` removals that preserve the focused row MUST emit the required placeholder / continuity posture rather than disappearing immediately

Optional harness UI specification if you create a preview surface:
- keep the inbox surface list-first and quiet; no dense table chrome and no dashboard card wall
- top band may show one compact queue-health strip and one dominant question only
- selected row preview may open as one restrained contextual rail, not a second full page
- expose selectors:
  - `work-inbox-preview`
  - `work-inbox-row`
  - `work-inbox-selected-row`
  - `work-queue-health-band`
  - `customer-unread-badge`
  - `internal-unread-badge`
  - `delta-application-log`

Playwright requirements:
- use Playwright during implementation and verification
- semantic locators only
- no fixed sleeps
- capture traces on retry / failure
- prove that applying deltas preserves canonical ordering, badge integrity, and focus-safe deferral rules
- prove that the selected row survives lawful filter-exit and reorder scenarios without shell or focus drift

Backend, performance, and security requirements:
- fail closed on ordering drift, queue-projection hash mismatch, action-binding mismatch, or schema mismatch
- keep delta publication idempotent by `causal_semantic_action_id`
- keep queue-health and row-order calculations deterministic and replay-safe
- do not let unread totals or local clocks outrank the persisted routing basis

Validation rules:
- validate snapshots and deltas against their schemas before persistence / publication
- unit-test row ordering, filter-chip alignment, action membership, binding alignment, badge alignment, and delta overlap rejection
- integration-test snapshot-plus-delta flows under focus, rebase, and retry conditions
- Playwright-test inbox preview or real inbox route for visual order, semantic selectors, and focused-row continuity

Definition of done:
- the repo contains canonical `WorkInboxSnapshot` and `WorkInboxDelta` projectors
- the staff inbox can be rebuilt or incrementally updated from persisted backend-authored contracts alone
- queue health, routing posture, unread badges, and focus-safe live updates now remain exact by construction
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
