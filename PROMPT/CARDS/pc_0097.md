---
id: pc_0097
slug: phase_03_parallel_wave_01_track_backend_manifest_001_implement_run_manifest_repository_and_lifecycle_state_machine
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "001"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0097

## Task Summary

implement run manifest repository and lifecycle state machine.

## Metadata

- ID: `pc_0097`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `001`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_001_implement_run_manifest_repository_and_lifecycle_state_machine`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0096](./pc_0096.md) `phase_03_parallel_wave_01_track_backend_access_012_implement_principal_access_view_read_model_and_query_service`
- Next: [pc_0098](./pc_0098.md) `phase_03_parallel_wave_01_track_backend_manifest_002_implement_manifest_lineage_trace_and_branch_selection_persistence_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0094_to_0101.md](../shared_operating_contract_0094_to_0101.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_001_implement_run_manifest_repository_and_lifecycle_state_machine`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0096` if they exist, especially `pc_0027`, `pc_0050`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0066`, `pc_0067`, `pc_0074`, `pc_0076`, `pc_0077`, `pc_0078`, `pc_0079`, `pc_0080`, `pc_0084`, and `pc_0085`.

Mission:
Implement the durable `RunManifest` aggregate, repository, and lifecycle state machine.
This task establishes the manifest spine itself.
It must preserve lineage mirrors, frozen execution envelope boundaries, append-only outcome posture, and explicit state transitions so later tasks can layer branch selection, start claims, pre-seal gates, replay, and release evidence on top of one trustworthy control object.

Source algorithm grounding you must honor:
- `data_model.md`, especially `RunManifest`, `append_only_outcome_projection`, `frozen_execution_binding`, `preseal_gate_evaluation`, and mirrored lineage fields.
- `state_machines.md`, especially `6.2 RunManifest.lifecycle_state` and the rules for `BLOCKED` versus `FAILED`, terminalization, and replay designation.
- `manifest_and_config_freeze_contract.md`, especially `5.3 RunManifest required field groups`, `5.9 Hash contract`, and the prohibition on floating config or mutable pre-seal basis.
- `modules.md`, especially `LOAD_MANIFEST(...)`, `TRANSITION_MANIFEST(...)`, `UPDATE_MANIFEST_OUTPUTS(...)`, `COMPUTE_EXECUTION_BASIS_HASH(...)`, and the manifest-bound invariants around outputs and append-only projections.
- `contract_integrity_requirements.md`, especially the manifest/lineage/gate integrity items and replay integrity items that close ambiguities around replay class, lineage mirrors, and execution basis.
- `replay_and_reproducibility_contract.md`, especially immutable execution basis and temporal integrity.
- Existing schemas including `run_manifest.schema.json`, `preseal_gate_evaluation_contract.schema.json`, `manifest_start_claim_contract.schema.json`, and `decision_bundle.schema.json`.

Deliverables to create:
1. `packages/backend-manifest/src/models/run_manifest.ts`.
2. `packages/backend-manifest/src/repositories/run_manifest_repository.ts`.
3. `packages/backend-manifest/src/state/run_manifest_lifecycle_machine.ts`.
4. `packages/backend-manifest/src/state/manifest_transition_validator.ts`.
5. `packages/backend-manifest/src/services/load_manifest.ts`.
6. `packages/backend-manifest/src/services/transition_manifest.ts`.
7. `packages/backend-manifest/src/services/manifest_mirror_consistency_validator.ts`.
8. `packages/backend-manifest/src/services/output_ref_projection_normalizer.ts`.
9. `db/migrations/phase03_0097_run_manifest_core.sql`.
10. `docs/manifest/run_manifest_repository_and_lifecycle.md`.
11. `tests/unit/backend_manifest/run_manifest_lifecycle_machine.spec.ts`.
12. `tests/integration/backend_manifest/run_manifest_repository.spec.ts`.

Required design decisions you must make explicit:
- Define the physical storage strategy for `RunManifest` and its nested contracts.
  Explain what remains in the root row versus child tables versus typed JSON columns, while preserving one authoritative aggregate shape.
- Define compare-and-swap or optimistic concurrency rules for lifecycle transitions, mirror updates, and append-only outcome synchronization.
- Define how `append_only_outcome_projection{...}` and its top-level mirrored fields are persisted and synchronized without allowing the mirrors to become independent truth.
- Define how `manifest_start_claim{...}` and `preseal_gate_evaluation{...}` are carried as typed placeholder-compatible fields now without silently implementing later-card orchestration in this task.
- Define how `output_refs{...}` are normalized and reloaded as structured entries instead of alias strings.
- Define how invariant failures record `invariant_enforcement_contract{...}` and typed reason codes on blocked or failed transitions.

Execution steps:
1. Materialize typed model wrappers for `RunManifest` and its nested control objects using imported/generated bindings where available.
2. Create the initial manifest-core migration with required identity, scope, access-binding, environment/build, config/input, hash, continuation, and lifecycle columns plus storage for nested contracts and append-only projections.
3. Implement `run_manifest_repository.ts` with explicit load, create, update, and compare-and-swap transition APIs.
4. Implement `run_manifest_lifecycle_machine.ts` and `manifest_transition_validator.ts` from `state_machines.md`, including typed rejection reasons for illegal transitions.
5. Implement `load_manifest.ts` so mirror parity is verified on every load and mismatches fail closed with typed invariant evidence.
6. Implement `output_ref_projection_normalizer.ts` so structured output-link entries survive reload and do not collapse into plain ref strings.
7. Add tests for legal and illegal transitions, mirror parity failures, append-only outcome synchronization, and replay-class / run-kind invariants.

Gap handling you must perform:
- Close the gap where `RunManifest` exists only as schema truth and prose but not as a durable aggregate repository.
- Close the gap where lifecycle transitions could be performed by arbitrary row updates rather than a state machine.
- Close the gap where top-level lineage mirrors or outcome mirrors could drift from their nested authoritative packets.
- Close the gap where pre-start failures might disappear as abandoned manifests instead of explicit `BLOCKED` terminal state.
- Close the gap where output refs could degrade into untyped strings, breaking downstream replay and lineage explanation.

Mandatory edge cases:
- `ALLOCATED --freeze_blocked--> BLOCKED` and `ALLOCATED --system_fault--> BLOCKED` must remain distinct from post-start failure
- `FROZEN --seal_success--> SEALED` requires a typed `preseal_gate_evaluation` placeholder or aligned null posture, not an implicit assumption
- `SEALED` manifest reload shows outputs/submissions already attached and must fail closed because pre-start sealed state drifted
- non-`REPLAY` manifest carries `replay_class` and must fail validation
- live or amendment-capable manifest omits required provider environment refs and must fail validation
- top-level lineage mirrors diverge from `continuation_set{...}` and loader must reject `MANIFEST_LINEAGE_PROJECTION_MISMATCH`
- top-level outcome mirrors diverge from `append_only_outcome_projection{...}` and loader must reject rather than guessing
- `output_refs{...}` preserve dependency identity refs on reload and do not collapse into alias strings

Backend, performance, and security requirements:
- Repository operations must be deterministic, idempotent where possible, and safe under concurrent transition attempts.
- Aggregate loads must validate mirror parity before returning business objects.
- Manifest writes must never backfill post-seal outcome material into the frozen execution basis.
- No ambient live-config or live-authority lookup is allowed inside manifest load/transition logic.
- Keep the implementation prepared for later start-claim and branch-selection tasks without pre-emptively inventing their orchestration logic here.

Validation rules:
- Unit-test lifecycle transitions, blocked-vs-failed distinction, mirror validation, and output-ref normalization.
- Integration-test migration application, repository create/load/update flows, compare-and-swap transition safety, and fail-closed reload of corrupted mirrors.
- Validate persisted manifest payloads against `run_manifest.schema.json`.
- Prove that illegal state transitions never partially mutate the manifest.
- Prove that append-only outcome mirrors remain synchronized and subordinate to their authoritative nested carrier.

Definition of done:
- The repo has a durable `RunManifest` aggregate and one authoritative lifecycle machine.
- Later manifest tasks can build on explicit state, mirror parity, and append-only outcome posture instead of ad hoc row mutations.
- Replay and release work gain the control spine they need before branch selection and start-claim logic arrive.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
