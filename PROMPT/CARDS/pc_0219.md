---
id: pc_0219
slug: phase_04_parallel_wave_02_track_backend_release_resilience_001_implement_build_artifact_generation_signing_metadata_and_candidate_tuple_persistence
mode: parallel
phase: "04"
wave: "02"
track: "backend_release_resilience"
task_number: "001"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_release_resilience_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0219

## Task Summary

001 implement build artifact generation signing metadata and candidate tuple persistence.

## Metadata

- ID: `pc_0219`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend release resilience`
- Task Number: `001`
- Source Slug: `phase_04_parallel_wave_02_track_backend_release_resilience_001_implement_build_artifact_generation_signing_metadata_and_candidate_tuple_persistence`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0218](./pc_0218.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_010_implement_operator_morning_digest_and_audit_hotspot_analytics_queries`
- Next: [pc_0220](./pc_0220.md) `phase_04_parallel_wave_02_track_backend_release_resilience_002_implement_deployment_release_model_canary_health_summary_and_rollout_state_machine`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0214_to_0221.md](../shared_operating_contract_0214_to_0221.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_release_resilience_001_implement_build_artifact_generation_signing_metadata_and_candidate_tuple_persistence`.

Inherit the shared operating contract and consume outputs from cards `pc_0055`, `pc_0059` through `pc_0061`, `pc_0199` through `pc_0202`, and `pc_0212` through `pc_0218` if they exist.

Mission:
Implement the canonical persistence layer for `BuildArtifact` plus the shared `ReleaseCandidateIdentityContract` / `candidate_identity_hash` tuple used by every promotion-evidence artifact.
This task is not allowed to leave build provenance, signature lineage, notarization evidence, distribution targets, or candidate-tuple hashing as CI-only metadata or ad hoc YAML glue.

Source algorithm grounding you must honor:
- `deployment_and_resilience_contract.md`, especially the promotion-pipeline and topology rules.
- `release_candidate_identity_and_promotion_evidence_contract.md`, end to end.
- `verification_and_release_gates.md`, especially evidence required for promotion.
- `data_model.md`, especially `BuildArtifact` and the release-evidence object family.
- `modules.md`, especially the shared candidate-identity and compatibility boundary notes around release verification.
- `schemas/build_artifact.schema.json` and `schemas/release_candidate_identity_contract.schema.json`.
- `scripts/validate_contracts.py`, especially `validate_build_artifact`, `build_artifact_distribution_target_order`, `build_artifact_desktop_evidence_posture`, `release_candidate_identity_contract_provider_order_drift`, and `release_candidate_identity_contract_hash_drift`.
- `tools/forensic_contract_guard.py` for any pinned candidate-identity or bundle-hash policies.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially any vectors around mixed-candidate evidence, array-order hash drift, and macOS desktop evidence posture.

Deliverables to create:
1. `packages/backend-release/src/models/build_artifact.ts`
2. `packages/backend-release/src/models/release_candidate_identity_contract.ts`
3. `packages/backend-release/src/repositories/build_artifact_repository.ts`
4. `packages/backend-release/src/services/derive_candidate_identity_hash.ts`
5. `packages/backend-release/src/services/persist_build_artifact_and_candidate_tuple.ts`
6. `packages/backend-release/src/tests/build_artifact_model.spec.ts`
7. `packages/backend-release/src/tests/release_candidate_identity_hash.spec.ts`
8. `docs/release/build_artifact_and_candidate_tuple_persistence.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/release/candidate-tuple-lab.tsx`

You MUST patch any existing build/release evidence path that currently:
- computes candidate hashes from non-canonical array order,
- stores only a digest string without the first-class candidate tuple,
- or forgets to gate desktop-only evidence on the actual presence of `MACOS_DESKTOP` in `distribution_targets[]`.

Required design decisions you must make explicitly:
- define the exact canonical tuple hashed into `candidate_identity_hash`
- define the canonical sorting and uniqueness rules for `distribution_targets[]` and `enabled_provider_profile_refs[]`
- define how build provenance, SBOM, signature, notarization, hardened-runtime evidence, and artifact-registry location are persisted and cross-linked
- define how missing desktop evidence is represented for non-desktop builds versus invalid desktop builds
- define how this persisted candidate tuple is exposed to downstream release-verification, canary, restore-drill, and deployment objects without rehashing from loose fields

Execution requirements:
- persist `BuildArtifact` as a first-class artifact with digest, SBOM, provenance, signature, registry ref, release channel, build time, canonical distribution targets, and desktop-only evidence fields
- persist `ReleaseCandidateIdentityContract` as the authoritative candidate tuple with canonical hash
- ensure arrays used in candidate hashing are sorted and unique before hashing
- keep `candidate_identity_hash` as a queryable top-level field on downstream release-evidence objects when you touch them
- ensure macOS notarization and hardened-runtime evidence are required whenever `MACOS_DESKTOP` is shipped and absent on non-desktop candidates

Gap handling you must close while implementing:
- close any gap where build provenance is only implicit in CI metadata rather than durable backend truth
- close any gap where release evidence can mix results from different build artifacts because the candidate tuple was not persisted as a first-class object
- close any gap where provider-profile order or target order changes candidate hashes without semantic change
- close any gap where desktop-only evidence can leak onto non-desktop builds or be omitted from real desktop candidates

Mandatory edge cases:
- semantically identical provider-profile or distribution-target sets must hash identically regardless of discovery order
- a desktop build missing notarization or hardened-runtime evidence must fail closed
- a non-desktop build must clear desktop-only refs rather than leaving stale values populated
- persisted candidate tuples must remain stable across re-read and replay; no runtime-generated timestamps or transient CI IDs may enter the hash

Playwright and contract-test requirements:
- add direct tests for canonical target/profile ordering, candidate hash stability, and desktop evidence posture
- if you expose or patch a release/debug route, add Playwright `APIRequestContext` coverage for build artifact retrieval and candidate tuple retrieval
- if you add the optional lab, add Playwright browser tests for semantic-locator navigation, tuple field ordering, evidence chips, reduced-motion-safe expansion, and sidecar focus behavior using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- use a three-band layout: candidate tuple ribbon, artifact/evidence ladder, narrow hash/provenance sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; info accent `#1D4ED8`; continuity accent `#0F766E`; caution `#B7791F`; danger `#C2410C`
- use ordered ladders and chips instead of generic release dashboards
- animate only opacity / translate / height; respect reduced motion

Backend, performance, and security requirements:
- candidate hashing must be deterministic and side-effect free
- provenance, signature, and attestation refs must remain immutable once recorded for a build artifact
- do not trust mutable CI environment strings as release truth; persist only the canonical contract fields
- keep repositories append-only or versioned where mutation would alter historical promotion evidence

Validation rules:
- validate every `BuildArtifact` and `ReleaseCandidateIdentityContract` payload against schema before persistence
- update fixtures/builders so all relevant validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one authoritative backend path for persisting build artifacts and canonical candidate tuples
- candidate hash stability, target/profile ordering, and desktop-evidence posture are durable and test-covered
- optional labs, if added, are read-only reflections of persisted truth rather than alternate implementations
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
