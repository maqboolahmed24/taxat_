---
id: pc_0095
slug: phase_03_parallel_wave_01_track_backend_access_011_implement_role_template_matrix_projector_and_policy_snapshot_hashing
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "011"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_011
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0095

## Task Summary

implement role template matrix projector and policy snapshot hashing.

## Metadata

- ID: `pc_0095`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `011`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_011_implement_role_template_matrix_projector_and_policy_snapshot_hashing`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0094](./pc_0094.md) `phase_03_parallel_wave_01_track_backend_access_010_implement_session_revocation_device_binding_and_csrf_protection_services`
- Next: [pc_0096](./pc_0096.md) `phase_03_parallel_wave_01_track_backend_access_012_implement_principal_access_view_read_model_and_query_service`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0094_to_0101.md](../shared_operating_contract_0094_to_0101.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_011_implement_role_template_matrix_projector_and_policy_snapshot_hashing`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0094` if they exist, especially `pc_0040`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0074`, `pc_0083`, `pc_0089`, `pc_0091`, `pc_0092`, `pc_0093`, and `pc_0094`.

Mission:
Implement the canonical governance-policy snapshot hash and the `RoleTemplateMatrix` projector so the governance access workspace can render role truth from one deterministic backend contract.
This task is not a decorative matrix exercise.
It is the read-side proof that policy state, authn rules, approvals, masking defaults, and role-template cell decisions can be re-opened, hashed, and compared without reconstructing them from UI-local state.

Source algorithm grounding you must honor:
- `data_model.md`, especially `GovernancePolicySnapshot` and `RoleTemplateMatrix`, including the distinction between committed policy hash versus read-model interaction state.
- `admin_governance_console_architecture.md`, especially `/governance/access`, stale-view protection, minimum route read models, grouped row/column layout, authority-chain ordering, and mounted selected-cell behavior.
- `actor_and_authority_model.md`, especially `3.9 Policy decision model` and `3.15 Frontend and governance-console rendering contract` so read models preserve exact decision vocabulary and authority-layer explanation.
- `modules.md`, especially `AUTHORIZE(...)` and `SIMULATE_GOVERNANCE_MUTATION(...)`, because the projector must surface current committed policy truth without inventing an alternative decision grammar.
- `frontend_shell_and_interaction_law.md` and `semantic_selector_and_accessibility_contract.md` if you build the internal preview route.
- Existing schemas including `governance_policy_snapshot.schema.json` and `role_template_matrix.schema.json`.

Deliverables to create:
1. `packages/backend-access/src/models/governance_policy_snapshot.ts`.
2. `packages/backend-access/src/read_models/role_template_matrix.ts`.
3. `packages/backend-access/src/hash/policy_snapshot_hash.ts`.
4. `packages/backend-access/src/hash/role_template_version_hash.ts`.
5. `packages/backend-access/src/projectors/governance_policy_snapshot_projector.ts`.
6. `packages/backend-access/src/projectors/role_template_matrix_projector.ts`.
7. `packages/backend-access/src/repositories/governance_policy_snapshot_repository.ts`.
8. `packages/backend-access/src/repositories/role_template_matrix_repository.ts`.
9. `apps/operator-web/src/routes/internal/role-template-matrix-preview.tsx`.
10. `docs/access/governance_policy_snapshot_and_role_template_matrix.md`.
11. `tests/unit/backend_access/policy_snapshot_hash_and_role_matrix.spec.ts`.
12. `tests/integration/backend_access/role_template_matrix_projection.spec.ts`.
13. `tests/playwright/internal/role_template_matrix_preview.spec.ts`.

Required design decisions you must make explicit:
- Define the exact boundary of `policy_snapshot_hash`.
  It must bind committed tenant policy truth such as environment bindings, session security posture, step-up rules, approval rules, masking defaults, and material config lineage.
  It must not be polluted by query-local selection, focus anchors, preview-only chips, or pending UI-only animation state.
- Define the exact boundary of `version_hash` for `RoleTemplateMatrix`.
  It should represent the committed role matrix content and any pending staged edit references that the corpus says must remain visible as governance facts, while still staying deterministic under ordering normalization.
- Define canonical ordering for row groups, columns, cells, reason codes, approvals, and policy-path references so matrix hashes and projections do not drift on replay-equivalent inputs.
- Define how pending role-editor changes, latest simulation refs, and selected-cell state are represented without contaminating the committed policy hash.
- Define how stale-view detection compares current `policy_snapshot_hash` against the hash the operator relied on when staging or reviewing access changes.

Execution steps:
1. Inventory every policy object, config surface, and committed rule family that contributes to `GovernancePolicySnapshot` truth.
2. Implement `policy_snapshot_hash.ts` using the shared canonical hashing substrate from `pc_0093` and the deterministic-serialization rules from `pc_0064`.
3. Implement `governance_policy_snapshot_projector.ts` so the snapshot read model preserves the committed policy slice plus the route-ready governance workspace contract described in `admin_governance_console_architecture.md`.
4. Implement `role_template_matrix_projector.ts` so grouped rows, columns, cells, mounted inspector detail, and pending change refs are derived from backend policy truth rather than from front-end re-interpretation.
5. Persist published snapshot/matrix records with lineage back to source policy/config refs and stable hashes.
6. Build the internal preview route so developers and operators can inspect one role template at a time against its exact `policy_snapshot_hash` and `version_hash`.
7. Add tests proving equivalent logical policy inputs produce identical hashes and identical projected matrix content regardless of incidental ordering.

Visual and interaction specification for `role-template-matrix-preview`:
- Tone: a premium policy atelier, not a spreadsheet clone.
  The screen should feel like a quiet rules surface where each decision cell is deliberate and inspectable.
- Layout:
  - `64px` top context bar with route title, role chip, `policy_snapshot_hash` chip, `version_hash` chip, and stale-view status chip.
  - left filter rail `248-264px` with grouped filters for resource class, action family, outcome, and pending-change presence.
  - center matrix canvas with frozen row headers, compact grouped column headers, and a persistent selected-cell focus ring.
  - right inspector `380-404px` with selected cell decision, reason codes, effective scope, masking rules, required approvals, policy-path ref, and pending-change ref when present.
- Signature visual:
  - place a thin monoline “rule weave” glyph near the title: two vertical strands crossed by ordered horizontal rule bars to suggest policy layering.
  - do not use heatmaps, pie charts, or giant status numerals.
- Palette:
  - background `#F5F4F1`
  - primary surface `#FFFFFF`
  - secondary surface `#EEECE7`
  - ink `#11161B`
  - muted ink `#65707A`
  - hairline `rgba(17,22,27,0.08)`
  - accent indigo `#4C5F7B`
  - accent sage `#66735C`
  - accent bronze `#8C6937`
  - success `#165F49`
  - warning `#875A1B`
  - danger `#A13A34`
- Typography:
  - editorial and restrained.
  - use monospace only for hashes, policy-path refs, cell refs, and scope tokens.
  - cell labels must remain readable at a glance without giant badges.
- Dimensional constraints:
  - max width `1540px`
  - row height `44-48px`
  - column header band `72px`
  - inspector key/value rows `36px`
- Motion:
  - `140ms` cell-focus transition.
  - `180ms` inspector content swap.
  - reduced-motion mode removes animated grid emphasis and keeps only contrast/focus changes.
- Interaction:
  - keyboard navigation across cells is mandatory and must preserve `focus_anchor_ref`.
  - selecting a cell updates the inspector without rerendering the whole matrix.
  - filters must preserve the currently selected cell when still visible; otherwise the route must surface a typed recovery posture.
  - accessible names should read as statements such as `role accountant allows prepare_submission and requires approval`.
  - Playwright must verify keyboard navigation, filter determinism, selected-cell persistence, and reduced-motion behavior.

Component hierarchy hint:
~~~ts
<InternalShell route="/internal/role-template-matrix-preview">
  <RoleMatrixFilters />
  <RoleMatrixGrid />
  <RoleCellInspector />
</InternalShell>
~~~

Gap handling you must perform:
- Close the gap where policy snapshot truth exists only as loose config references instead of one canonical digest.
- Close the gap where the role matrix could be rebuilt differently by different callers because ordering, cell refs, or policy-path assembly are not frozen.
- Close the gap where pending change refs or latest simulation refs disappear on refresh even though the corpus requires them to remain explicit governance context.
- Close the gap where front-end code might collapse `ALLOW_MASKED`, `REQUIRE_STEP_UP`, and `REQUIRE_APPROVAL` into a generic “allowed” badge.
- Close the gap where stale-view checks cannot prove which committed policy slice the operator reviewed.

Mandatory edge cases:
- same logical policy slice with reordered source objects yields the same `policy_snapshot_hash`
- same logical matrix with reordered cell-generation inputs yields the same `version_hash`
- `ALLOW_MASKED` remains distinct from `ALLOW` in both cells and inspector
- step-up and approval obligations both remain visible for a selected action family
- pending role-editor change refs render without altering the committed snapshot hash
- empty filter results preserve route explanation instead of collapsing into an untyped blank grid
- selected cell becomes filtered out and the route must surface deterministic recovery posture
- stale policy hash mismatch is visible and queryable without re-deriving policy client-side

Backend, performance, and security requirements:
- Hashing and projection must be deterministic and cheap enough to publish or refresh on routine governance reads.
- The preview route must render from backend read models, not from ad hoc client-side policy recomputation.
- Do not expose secrets or raw connector credentials in any projected policy structure.
- Keep the projector side-effect free; publishing and persistence should be explicit steps.
- Prefer append-only publication or versioned upsert patterns over in-place mutation with hidden drift.

Validation rules:
- Unit-test hash canonicalization, row/column ordering, cell ref stability, and policy-path projection.
- Integration-test snapshot publication, matrix publication, stale-view hash comparison, and persistence reload.
- Use Playwright to verify the preview route with role-first locators, keyboard traversal, inspector updates, and reduced-motion-safe behavior.
- Prove that `policy_snapshot_hash` stays stable across equivalent policy orderings and changes when a committed rule materially changes.
- Prove that matrix cells preserve exact decision vocabulary and authority-layer detail.

Definition of done:
- The repo has one authoritative `policy_snapshot_hash` and one deterministic `RoleTemplateMatrix` projector.
- Governance access tooling can inspect committed role truth without rebuilding it differently in each client.
- Stale-view protection for access and role workspaces gains a durable backend basis instead of ambient UI memory.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
