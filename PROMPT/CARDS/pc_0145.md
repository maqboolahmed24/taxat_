---
id: pc_0145
slug: phase_03_parallel_wave_01_track_backend_workflow_001_implement_workflow_item_aggregate_and_state_machine_rules
mode: parallel
phase: "03"
wave: "01"
track: "backend_workflow"
task_number: "001"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_workflow_001_implement_workflow_item_aggregate_and_state_machine_rules
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0145

## Task Summary

workflow item aggregate and state machine rules.

## Metadata

- ID: `pc_0145`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend workflow`
- Task Number: `001`
- Source Slug: `phase_03_parallel_wave_01_track_backend_workflow_001_implement_workflow_item_aggregate_and_state_machine_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0144](./pc_0144.md) `phase_03_parallel_wave_01_track_backend_authority_012_implement_authority_reconciliation_analytics_snapshot_and_operator_insight_queries`
- Next: [pc_0146](./pc_0146.md) `phase_03_parallel_wave_01_track_backend_workflow_002_implement_collaboration_routing_contract_queue_scoring_and_assignment_recommendations`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0142_to_0149.md](../shared_operating_contract_0142_to_0149.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_workflow_001_implement_workflow_item_aggregate_and_state_machine_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0144` if they exist, especially `pc_0061`, `pc_0064` through `pc_0069`, `pc_0086` through `pc_0093`, `pc_0102` through `pc_0109`, and `pc_0134` through `pc_0144`.

Mission:
Implement the durable `WorkflowItem` aggregate, its state machine, its authority-truth boundary packet, and its lifecycle / projection rules.
This card must make `WorkflowItem` the one deterministic coordination artifact for module-planned work without letting internal progress, assignment, or customer copy overrule unresolved authority truth.

Source algorithm grounding you must honor:
- `data_model.md`, especially `WorkflowItem` and adjacent queue / workspace / notification fields.
- `state_machines.md`, especially `6.14 WorkflowItem.lifecycle_state`.
- `modules.md`, especially `PLAN_WORKFLOW(...)` and `UPSERT_WORKFLOW_ITEMS(...)`.
- `collaboration_workspace_contract.md`.
- `authority_truth_and_internal_projection_separation_contract.md`.
- `error_model_and_remediation_model.md`, especially remediation classes that spawn or reopen workflow.
- `observability_and_audit_contract.md`, especially audit-correlation expectations for workflow-linked events.
- `PATCH_RESOLUTION_INDEX.md` and `AUDIT_FINDINGS.md` where workflow visibility, authority truth, or customer projection drift was corrected.

Deliverables to create:
1. `packages/backend-workflow/src/models/workflow_item.ts`
2. `packages/backend-workflow/src/repositories/workflow_item_repository.ts`
3. `packages/backend-workflow/src/services/validate_workflow_item_transition.ts`
4. `packages/backend-workflow/src/services/build_workflow_truth_boundary_contract.ts`
5. `packages/backend-workflow/src/services/open_or_reuse_workflow_item.ts`
6. `packages/backend-workflow/src/services/advance_workflow_item.ts`
7. `packages/backend-workflow/src/services/close_or_supersede_workflow_item.ts`
8. `packages/backend-workflow/src/services/sync_workflow_item_authority_truth.ts`
9. `packages/backend-workflow/src/services/sync_workflow_item_customer_projection.ts`
10. `packages/backend-workflow/src/services/bump_workflow_workspace_versions.ts`
11. `db/migrations/phase03_0145_workflow_item_aggregate_and_state_machine.sql`
12. `docs/workflow/workflow_item_aggregate_and_state_machine.md`
13. `tests/unit/backend_workflow/workflow_item_contract.spec.ts`
14. `tests/unit/backend_workflow/workflow_item_transition_rules.spec.ts`
15. `tests/integration/backend_workflow/workflow_item_lifecycle_flow.spec.ts`

Required design decisions you must make explicit:
- Define the full lifecycle matrix for:
  `OPEN`, `IN_PROGRESS`, `WAITING_ON_CLIENT`, `WAITING_ON_AUTHORITY`, `BLOCKED`, `DONE`, `CANCELLED`, `STALE`.
- Define the exact relationship between:
  `lifecycle_state`,
  `customer_status_projection`,
  `authority_truth_state`,
  `assignment_state`,
  `waiting_on_actor`,
  `active_request_info_ref`,
  `closed_at`,
  `customer_workspace_version`,
  `staff_workspace_version`.
- Define dedupe / reuse policy:
  which dimensions form the active-item uniqueness key, when a task should reopen an existing item, and when it must supersede into a new item.
- Define customer-shared versus internal-only rules:
  when `customer_thread_ref` is mandatory, when `customer_status_projection` must be null, and when customer-visible status may lawfully be `RESOLVED` or `CLOSED`.
- Define exact authority-truth propagation onto workflow items so unresolved authority truth blocks illegal `DONE` / `RESOLVED` posture.

Execution steps:
1. Implement the schema-backed `WorkflowItem` model / repository with canonical serialization and indexed lookup by tenant, client, dedupe key, lifecycle state, assignee, queue ref, authority truth state, and due posture.
2. Implement `validate_workflow_item_transition.ts` so lifecycle changes are explicit and fail closed.
3. Implement one canonical truth-boundary builder that stamps:
   - `truth_boundary_contract{...}`
   - `authority_truth_contract{...}`
   on every workflow item.
4. Implement open / reuse / supersede helpers that keep exactly one lawful active item per active dedupe meaning unless deterministic supersession is required.
5. Implement customer-projection synchronization so customer-visible status stays subordinate to authority truth and lifecycle.
6. Implement workspace-version bump rules so later workspace/projector tasks can consume stable monotonic versions without guessing.
7. Emit workflow audit events / domain events where the earlier event spine already exists.

Gap handling you must perform:
- Close the gap where workflow lifecycle, customer projection, and authority truth are defined in multiple places but not enforced by one aggregate.
- Close the gap where customer-shared work could be marked resolved while authority truth remains unresolved.
- Close the gap where open-request identity could drift from `WAITING_ON_CLIENT` posture.
- Close the gap where workspace-version fields exist but no canonical mutation rule increments them.
- Close the gap where dedupe / reuse policy is implicit and could create duplicate active items.

Mandatory edge cases:
- `WAITING_ON_CLIENT` requires `collaboration_visibility = CUSTOMER_SHARED`, `waiting_on_actor = CUSTOMER`, and non-null `active_request_info_ref`
- `WAITING_ON_AUTHORITY` must publish `waiting_on_actor = AUTHORITY`; if customer-shared, customer projection must be `WAITING_ON_CONFIRMATION`
- `DONE`, `CANCELLED`, and `STALE` require `closed_at`; non-terminal states must have `closed_at = null`
- `customer_status_projection = RESOLVED` requires `lifecycle_state = DONE`
- `customer_status_projection = ACTION_REQUIRED` requires `WAITING_ON_CLIENT`
- `customer_status_projection = WAITING_ON_CONFIRMATION` requires `WAITING_ON_AUTHORITY` plus unresolved authority truth
- `customer_status_projection = null` implies `INTERNAL_ONLY`
- unresolved authority truth in `{UNKNOWN, PENDING_ACK, PARTIAL_ACK, OUT_OF_BAND}` must block illegal “resolved” customer or internal completion semantics

Backend, performance, and security requirements:
- fail closed on illegal projection combinations, stale workspace-version regressions, or duplicate active dedupe keys
- keep lifecycle mutation and version bump atomic
- preserve immutable history for completed / cancelled / stale items
- do not serialize customer-visible projection text that is not deterministically derived from canonical fields

Validation rules:
- validate every persisted workflow item against `schemas/workflow_item.schema.json`
- unit-test lifecycle, assignment, visibility, authority-truth, and customer-projection rules
- integration-test reuse vs supersede, authority-truth reopen, and workspace-version increment behavior
- prove that internal completion cannot masquerade as authority confirmation

Definition of done:
- the repo contains a durable, schema-backed `WorkflowItem` aggregate with one authoritative state machine
- authority truth, customer projection, and collaboration visibility now remain consistent by construction
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
