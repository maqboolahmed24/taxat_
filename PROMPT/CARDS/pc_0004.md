---
id: pc_0004
slug: phase_00_seq_004_normalize_glossary_and_domain_ubiquitous_language_map
mode: sequential
phase: "00"
wave: null
track: null
task_number: "004"
block_key: phase_00_seq
original_key: phase_00_seq_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0004

## Task Summary

normalize glossary and domain ubiquitous language map.

## Metadata

- ID: `pc_0004`
- Mode: `sequential`
- Phase: `00`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `004`
- Source Slug: `phase_00_seq_004_normalize_glossary_and_domain_ubiquitous_language_map`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0003](./pc_0003.md) `phase_00_seq_003_build_contract_schema_script_cross_reference_index`
- Next: [pc_0005](./pc_0005.md) `phase_00_seq_005_extract_invention_boundary_and_system_boundary_requirements`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt

Use the shared operating contract first:
[../shared_operating_contract_0001_to_0005.md](../shared_operating_contract_0001_to_0005.md)

```text
Execute roadmap task `phase_00_seq_004_normalize_glossary_and_domain_ubiquitous_language_map`.

Inherit the shared operating contract and consume the outputs from tasks `pc_0001` through `pc_0003`.

Mission:
Normalize the Taxat corpus into one authoritative ubiquitous-language map. The goal is not to rewrite
the product voice. The goal is to ensure that human-facing terms, machine field names, contract names,
and cross-domain concepts all resolve to one stable semantic system that downstream agents can use without drift.

Algorithm grounding you must honor:
- `glossary.md` says it is the authoritative vocabulary layer for the pack.
- `README.md -> Shared Spine Vocabulary` introduces canonical shell-family, interaction-layer, route,
  workspace, visibility, and automation-regression vocabulary that must align with specialized contracts.
- `implementation_conventions.md -> 1. Corpus authoring conventions` requires terms to stay consistent across
  `glossary.md`, `data_model.md`, and the contracts that serialize or interpret them.
- `architecture_coherence_guardrails.md -> 0. Foundation spine coherence` and
  `architecture_coherence_guardrails.md -> 5. Frontend shell and interaction coherence`
  require one stable shared vocabulary across README, glossary, data model, schemas, shell law, and specialized surfaces.
- `data_model.md`, `actor_and_authority_model.md`, `frontend_shell_and_interaction_law.md`,
  `collaboration_workspace_contract.md`, `customer_client_portal_experience_contract.md`,
  and `admin_governance_console_architecture.md` all define domain-specific language that must not silently drift.
- `canonical_source_and_evidence_taxonomy.md` and `invention_and_system_boundary.md` define important distinctions
  (source vs evidence vs canonical fact; inside-engine vs outside-engine) that must remain crisp in the language map.

Deliverables to create:
1. `docs/analysis/04_glossary_normalization_and_ubiquitous_language_map.md`
2. `docs/analysis/04_prohibited_synonyms_and_term_drift_rules.md`
3. `data/analysis/glossary_normalized.json`
4. `data/analysis/ubiquitous_language_map.csv`
5. `data/analysis/term_alias_conflicts.json`
6. `data/analysis/field_to_term_map.json`
7. `tools/analysis/build_ubiquitous_language_map.py`
8. `tools/analysis/lint_ubiquitous_language.py`

Each term record must capture at minimum:
- `term_id`
- `canonical_term`
- `term_category` (`core_engine`, `lineage`, `evidence`, `authority`, `workflow`, `shell_route`, `portal`, `governance`, `collaboration`, `retention`, `release`, `observability`, etc.)
- `definition`
- `authoritative_source_refs[]`
- `machine_field_names[]`
- `canonical_contract_names[]`
- `allowed_aliases[]`
- `prohibited_aliases[]`
- `related_terms[]`
- `visibility_or_audience_notes`
- `drift_risk_level`
- `notes`

Execution steps:
1. Start with every bold glossary entry in `glossary.md` and treat it as the initial canonical vocabulary seed.
2. Expand the vocabulary using:
   - `README.md -> Shared Spine Vocabulary`
   - `data_model.md` object names and critical field families
   - `actor_and_authority_model.md` actor and authority-layer terms
   - `frontend_shell_and_interaction_law.md` shell, route, and recovery posture terms
   - `collaboration_workspace_contract.md`, `customer_client_portal_experience_contract.md`,
     and `admin_governance_console_architecture.md` for specialized surface language
   - `canonical_source_and_evidence_taxonomy.md` for evidence/source terminology
   - `invention_and_system_boundary.md` for boundary-language precision
3. Normalize spelling, plurality, casing, and phrase form while preserving the exact canonical machine terms where the corpus requires closed vocabulary.
4. Build a field-to-term map so that backticked field names and human-readable concepts are traceably connected.
5. Detect alias collisions and overloaded language, especially around:
   - `RunManifest` vs manifest vs execution envelope
   - source vs evidence vs candidate fact vs canonical fact vs derived value
   - authority acknowledgement vs authority reference
   - replay vs recovery vs continuation vs supersession
   - shell vs route vs surface vs workspace vs module
   - customer-safe vs customer-visible vs internal-only vs staff-full vs governance-controlled vs authority-facing
   - approval vs override vs gate decision
   - drift vs amendment vs correction
6. Create a prohibited-synonym list where casual shorthand would cause semantic drift or architectural bugs.
7. Add lint logic that can detect future vocabulary drift in markdown prose or machine-readable support files.
8. Preserve audience nuance. Some terms may need a technical canonical name plus a safe human-facing explanation; do not collapse those into one ambiguous label.

Gap handling you must perform:
- Close the “glossary is authoritative but not machine-readable” gap by exporting a structured ubiquitous-language map.
- Close the “shared-spine vocabulary is split between glossary, README, and specialized contracts” gap by unifying those terms.
- Close the “machine fields and human concepts are linked only informally” gap by producing a field-to-term map.
- Close the “alias drift is easy to introduce” gap by generating prohibited-synonym and lint rules.
- Close the “audience-safe language vs internal technical language” gap by explicitly tracking audience/visibility notes where needed.

Mandatory edge cases:
- Closed vocabularies such as shell families and certain execution/gate/state enums must remain exact; do not prettify them into looser synonyms.
- A single human phrase may refer to different technical concepts in different contexts; when that happens, split the concept into distinct canonical terms rather than merging them.
- UI-facing plain language must not erase legally or technically necessary distinctions such as authority acknowledgement vs internal submission intent.
- Do not let portal-friendly wording redefine internal operator, governance, or authority-protocol semantics.

Validation rules:
- Every shared-spine field named in `README.md -> Shared Spine Vocabulary` must appear in the normalized map.
- Every bold glossary term must appear exactly once as a canonical term or as a documented alias of a canonical term.
- Every prohibited alias must name the canonical replacement and rationale.
- No term may silently collapse `COMPLIANCE` and `ANALYSIS` posture or raw source and canonical fact posture.
- The lint tool must be able to flag unknown high-risk aliases deterministically.

Definition of done:
- A later autonomous agent can use your language map as the single interpretation layer for prompts,
  architecture docs, schemas, and UI copy constraints without inventing new vocabulary or conflating distinct concepts.
```

### Implementation Outputs

- Created `tools/analysis/build_ubiquitous_language_map.py`.
- Created `tools/analysis/lint_ubiquitous_language.py`.
- Created `docs/analysis/04_glossary_normalization_and_ubiquitous_language_map.md`.
- Created `docs/analysis/04_prohibited_synonyms_and_term_drift_rules.md`.
- Created `data/analysis/glossary_normalized.json`.
- Created `data/analysis/ubiquitous_language_map.csv`.
- Created `data/analysis/term_alias_conflicts.json`.
- Created `data/analysis/field_to_term_map.json`.

### Vocabulary Normalization Findings

- The normalized map now contains `144` machine-readable terms spanning glossary seeds, shared-spine fields, shell/read-model vocabulary, source/evidence taxonomy objects and source classes, authority-model concepts, execution-mode terms, and explicit system-boundary language.
- Shared-spine normalization is complete: all `9` README shared-spine fields appear in the term map and the shell-family enum vocabulary (`CALM_SHELL`, `CLIENT_PORTAL_SHELL`, `GOVERNANCE_DENSITY_SHELL`) plus the native embodiment marker (`NATIVE_OPERATOR`) were exported as first-class canonical terms.
- Glossary coverage is deterministic and clean: all `92` bold glossary entries now resolve exactly once as canonical terms or documented aliases, with `0` coverage failures.
- The field bridge is explicit: `data/analysis/field_to_term_map.json` maps canonical machine field names back to normalized human terms and surfaces `13` ambiguous machine-token cases where one field family participates in multiple closely related concepts.
- The drift layer is explicit: `docs/analysis/04_prohibited_synonyms_and_term_drift_rules.md` and the JSON outputs now encode `12` prohibited high-risk alias rules with named canonical replacements and rationales.

### Residual Drift Risks

- `data/analysis/term_alias_conflicts.json` reports `30` alias-collision clusters that remain semantically close enough to require disciplined wording rather than silent merging.
- The highest-signal collision families are exactly the ones called out by the task brief: execution mode markers (`execution_mode`, `analysis_only`, `counterfactual_basis`), shell/read-side stability markers (`shell_family`, `guard_vector_hash`, `publication_generation`), route/workspace interactions (`queue_projection`), and authority-control lineage tokens (`access_binding_hash`, `AuthorityRequestEnvelope`, `AuthorityInteractionRecord`).
- These collisions are not treated as unresolved schema bugs in this task; they are preserved as deliberate disambiguation requirements so later agents know where shorthand becomes dangerous.

### Verification Evidence

- `python3 tools/analysis/build_ubiquitous_language_map.py` completed successfully with status `PASS`.
- `python3 tools/analysis/lint_ubiquitous_language.py` completed successfully with status `PASS`.
- `./.venv/bin/python3 Algorithm/scripts/validate_contracts.py --self-test` completed successfully with `PASS: schemas, bundled samples, and custom invariants validated.`.
- `./.venv/bin/python3 Algorithm/tools/forensic_contract_guard.py` completed successfully with `forensic contract guard: PASS`.
- The generated summary confirms `0` shared-spine omissions and `0` glossary canonicalization failures.

## Verification

- Prompt authored from direct analysis of the `Algorithm/` corpus and aligned to the authoritative card slug.
- This card remains status-neutral; claim and completion state must continue to live only in `Checklist.md`.
