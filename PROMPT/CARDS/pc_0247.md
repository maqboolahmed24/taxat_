---
id: pc_0247
slug: phase_05_parallel_wave_03_track_frontend_low_noise_007_Playwright_or_other_appropriate_tooling_implement_inline_refresh_rebase_and_recovery_posture_banners
mode: parallel
phase: "05"
wave: "03"
track: "frontend_low_noise"
task_number: "007"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_low_noise_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0247

## Task Summary

007 Playwright or other appropriate tooling implement inline refresh rebase and recovery posture banners.

## Metadata

- ID: `pc_0247`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend low noise`
- Task Number: `007`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_low_noise_007_Playwright_or_other_appropriate_tooling_implement_inline_refresh_rebase_and_recovery_posture_banners`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0246](./pc_0246.md) `phase_05_parallel_wave_03_track_frontend_low_noise_006_Playwright_or_other_appropriate_tooling_implement_dominant_question_dominant_action_and_support_region_promotion_behaviors`
- Next: [pc_0248](./pc_0248.md) `phase_05_parallel_wave_03_track_frontend_low_noise_008_Playwright_or_other_appropriate_tooling_implement_provenance_twin_and_audit_support_modules_inside_calm_shell_budget`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_low_noise_007_Playwright_or_other_appropriate_tooling_implement_inline_refresh_rebase_and_recovery_posture_banners`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0246` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement inline refresh, rebase, catch-up, stale-view, degraded-state, and recovery-posture banners for the low-noise calm shell without remounting or replacing the four-surface hierarchy.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The algorithm requires pending, stale, frozen, blocked, degraded, and recovery postures to render inside existing shell surfaces. Reconnect and catch-up must keep content mounted; `REBASE_REQUIRED` and `ACCESS_REBIND_REQUIRED` are typed stream/session states, not generic errors. Inline notices must be durable-receipt, typed-problem, stream-contract, or frame-posture driven.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md settlement-state, shell continuity, stream recovery, edge-case handling, validation additions`.
- `frontend_shell_and_interaction_law.md state/freshness/recovery presentation and loading without semantic loss`.
- `stream_resume_and_catch_up_ordering_contract.md required rules`.
- `northbound_api_and_session_contract.md problem response and reconnect rules`.
- `schemas/stream_recovery_contract.schema.json, api_command_receipt.schema.json, low_noise_experience_frame.schema.json, context_bar_state.schema.json`.

Deliverables to create or patch:
1. `packages/shared-ui/src/low-noise/InlineRecoveryBanner.tsx`
2. `packages/shared-ui/src/low-noise/InlineRefreshNotice.tsx`
3. `packages/frontend-shell-core/src/low-noise/recovery_posture_presenter.ts`
4. `packages/frontend-shell-core/src/low-noise/refresh_coalescer.ts`
5. `packages/frontend-shell-core/src/low-noise/rebase_action_model.ts`
6. `tests/unit/frontend-shell-core/recovery_posture_presenter.spec.ts`
7. `tests/playwright/frontend/low_noise_inline_refresh_rebase_recovery_banners.spec.ts`
8. `docs/frontend/inline_refresh_rebase_and_recovery_posture_banners.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Notices should be slim, contextual, and surface-bound: context bar for connection/rebase/access rebind; decision summary for hard limitation/block; action strip for stale mutation; drawer for module-specific refresh. Use one amber notice at a time for recoverable issues and red only for hard fail-closed states. Motion is 140ms fade/height; no skeleton takeover once meaningful content exists.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- map frame `connection_state`, `settlement_state`, `recovery_posture`, stream delivery state, command receipt state, and typed problem envelopes into one inline notice model
- coalesce non-material refreshes within the low-noise refresh window and visible-change budget
- keep mounted summary/action/detail context visible during pending propagation, awaiting approval, catch-up, stale review, rebase required, access rebind, and degraded state
- provide explicit next actions for rebase/access rebind when lawful, otherwise no-safe-action/investigation entry
- ensure notices have semantic anchors and live-region announcements without duplicate noise

Functional, architectural, and edge-case gaps to close:
- close the gap where reconnect creates a full-screen spinner
- close the gap where stale-view rejection appears as a generic error toast
- close the gap where pending command acceptance is mistaken for completion
- close the gap where multiple simultaneous problems create warning stacks

Mandatory edge cases:
- catching up must not accept live deltas as current until catch-up completes
- compaction floor breach must show explicit rebase and not fabricate missed history
- access rebind must purge/disable unsafe cached actions
- multiple simultaneous problems must collapse into one dominant notice plus count/disclosure

Playwright, accessibility, and validation requirements:
- unit-test notice priority, coalescing, and problem/receipt/stream mapping
- Playwright-test reconnect/catch-up, stale mutation rejection, degraded state, access rebind, multiple-problem collapse, focus preservation, and reduced motion
- assert no route-wide loader appears after first meaningful paint
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- recovery and refresh stay inline within the calm shell
- typed stream/receipt/problem states drive copy and actions
- Playwright proves focus, budget, no-spinner, and edge-state behavior
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
