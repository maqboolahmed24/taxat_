---
id: pc_0190
slug: phase_04_parallel_wave_02_track_backend_governance_002_implement_governance_policy_snapshot_projector_and_environment_binding_views
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "002"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0190

## Task Summary

implement governance policy snapshot projector and environment binding views.

## Metadata

- ID: `pc_0190`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `002`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_002_implement_governance_policy_snapshot_projector_and_environment_binding_views`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0189](./pc_0189.md) `phase_04_parallel_wave_02_track_backend_governance_001_implement_tenant_governance_snapshot_projector_and_dominant_attention_summary`
- Next: [pc_0191](./pc_0191.md) `phase_04_parallel_wave_02_track_backend_governance_003_implement_governance_access_simulation_projection_and_basis_contract_hash_reuse_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0190_to_0197.md](../shared_operating_contract_0190_to_0197.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_002_implement_governance_policy_snapshot_projector_and_environment_binding_views`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0189` if they exist, especially `pc_0189`, `pc_0094` through `pc_0101`, `pc_0158` through `pc_0168`, and `pc_0174` through `pc_0181`.

Mission:
Implement the authoritative `GovernancePolicySnapshot` projector and the environment-binding read stack for `/v1/governance/tenants/{tenant_id}/policy-snapshot`.
This is not a generic admin-settings DTO and not a controller-shaped bundle of form defaults.
It is the stale-view anchor and same-shell control-plane contract for tenant policy changes.
It must publish the exact policy hash, environment bindings, session-security posture, step-up rules, approval rules, masking defaults, section-nav posture, staged basket posture, approval-composer posture, blast-radius review, and config-history timeline needed to keep high-risk changes same-object and same-basis from preview through submission.

Source algorithm grounding you must honor:
- `admin_governance_console_architecture.md`, especially section `4.2 /governance/tenant`, the fixed section order, the semantic reading path, and the direct-submission / blast-radius / change-basket rules.
- `data_model.md`, especially `GovernancePolicySnapshot` and the distinction between this route and `TenantGovernanceSnapshot`.
- `modules.md`, especially any governance snapshot or config-read assembly used by the tenant route and the mutation-basis semantics reused later by approvals and commands.
- `policy_risk_and_blast_radius_modeling_contract.md`, especially atomic basket and hazard/basis reuse law.
- `frontend_shell_and_interaction_law.md`, `cross_shell_design_token_and_interaction_layer_foundation_contract.md`, and `dominant_question_and_single_action_contract.md`, especially the governance shell and interaction rules.
- `northbound_api_and_session_contract.md`, especially the governance policy-snapshot read surface and stale-view expectations.
- `schemas/governance_policy_snapshot.schema.json`, `schemas/governance_interaction_layer.schema.json`, `schemas/governance_mutation_hazard_contract.schema.json`, `schemas/governance_mutation_basis_contract.schema.json`, `schemas/problem_envelope.schema.json`, and any nested shell / cache / continuity contracts required by the snapshot.
- `scripts/validate_contracts.py`, especially `governance_policy_snapshot_fe31_shared_interaction_policy`, `governance_policy_snapshot_workspace_and_staged_change_posture`, `governance_policy_snapshot_interaction_layer_non_modal_compaction`, `governance_policy_snapshot_atomic_submission_and_blast_alignment`, `governance_policy_snapshot_approval_and_history_timeline_alignment`, `governance_policy_snapshot_atomic_basis_contract_hash_continuity`, `governance_policy_snapshot_approval_composer_basis_contract_alignment`, and `governance_policy_snapshot_blast_radius_basis_contract_alignment`.
- `PATCH_RESOLUTION_INDEX.md`, especially the governance policy snapshot hardening work around empty ids, basis drift, and shell alignment.
- `test_vectors.md`, especially TV-65A, TV-65B, TV-65C, and FE-51.

Deliverables to create:
1. `packages/backend-governance/src/projectors/build_governance_policy_snapshot.ts`
2. `packages/backend-governance/src/services/build_tenant_config_workspace.ts`
3. `packages/backend-governance/src/services/build_governance_change_basket.ts`
4. `packages/backend-governance/src/services/build_governance_approval_composer.ts`
5. `packages/backend-governance/src/services/build_governance_blast_radius_panel.ts`
6. `packages/backend-governance/src/services/build_config_history_timeline.ts`
7. `packages/backend-governance/src/tests/governance_policy_snapshot_projection.spec.ts`
8. `packages/backend-governance/src/tests/governance_policy_snapshot_basis_continuity.spec.ts`
9. `docs/governance/governance_policy_snapshot_and_environment_bindings.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/policy-snapshot-lab.tsx`

You MUST patch any existing governance tenant-policy read endpoint so it consumes this projector rather than composing section order, basket posture, or blast-radius semantics inside controllers or frontend code.

Required design decisions you must make explicitly:
- define the durable source for `environment_bindings[]` and how provider environment, authority defaults, and connector posture are normalized into one route-safe binding view
- define how `active_section_code` and `visible_form_section_refs[]` are chosen so inline policy help remains in-form and same-object during rebase
- define how `change_basket.active_mutation_hazard_or_null` and `change_basket.active_mutation_basis_contract_or_null` are populated, cleared, or left null when the basket is mixed-basis, stale, or empty
- define the exact readiness rules for `approval_composer.composer_state`, especially when rationale, step-up, or approval scope is required but the basket is not yet atomic
- define how `blast_radius_panel.panel_state` becomes `EMPTY`, `ACTIVE`, or `STALE`
- define how `config_history_timeline.selected_change_ref` and `last_material_change_ref` stay same-object during historical review and stale rebase
- define how stale-view rejection will publish the latest `policy_snapshot_hash` / mutation-basis information in problem envelopes without letting the browser infer what changed

Execution requirements:
- publish a schema-valid `GovernancePolicySnapshot`
- preserve the fixed section-nav order exactly:
  `TENANT_PROFILE -> SECURITY_POSTURE -> AUTHORITY_AND_ENVIRONMENTS -> CONNECTOR_POLICY -> APPROVAL_AND_CHANGE_CONTROL -> NOTIFICATIONS_AND_EVIDENCE`
- preserve the route reading path exactly:
  `SECTION_NAV -> CONFIG_FORM -> INLINE_POLICY_HELP -> BLAST_RADIUS_PANEL -> CHANGE_BASKET -> APPROVAL_COMPOSER -> CONFIG_HISTORY_TIMELINE`
- keep `inline_policy_help` inside the active section context; do not detach it into tooltip-only or documentation-only affordances
- preserve one shared `interaction_layer = GovernanceInteractionLayer` with non-modal compaction and one promoted support surface max
- keep `change_basket.simulation_atomicity` authoritative; `submission_enabled = true` is lawful only when the basket is atomic on one live `simulation_basis_hash`, one live `dependency_topology_hash`, one reviewed `mutation_basis_contract.basis_contract_hash`, and one visible blast panel that still matches that same reviewed basis
- keep each staged change explicit with `before`, `after`, `effective_scope_label`, `reason_required`, `approval_required`, `audit_event_families[]`, and `input_commit_mode`
- keep `active_mutation_hazard_or_null.hazard_contract_hash`, `active_mutation_basis_contract_or_null.hazard_contract_hash`, `approval_composer.mutation_basis_contract_or_null.hazard_contract_hash`, and `blast_radius_panel.mutation_basis_contract_or_null.hazard_contract_hash` identical whenever a reviewed hazard is active
- clear direct-commit posture whenever `commit_authority_posture = PREVIEW_ONLY`, whenever confidence or predictability is below threshold, or whenever the basket is mixed-basis
- keep `config_history_timeline` visible beside current staged diffs so stale rebase and current-vs-history comparison remain same-object
- keep `policy_snapshot_hash` and `cache_isolation_contract` explicit so same-tenant refresh, reconnect, and browser restore cannot mount an unguarded policy shell

Gap handling you must close while implementing:
- close any gap where the policy route composes environment bindings from controller-local branching instead of one durable projector
- close any gap where submission readiness can be inferred from local form dirtiness rather than atomic basket / blast alignment
- close any gap where approval composer, blast panel, and change basket can drift to different basis hashes or hazard hashes
- close any gap where history review and current staged diffs become detached views or modal chains
- close any gap where stale-view failures omit the latest authoritative basis and therefore force browser guesswork

Mandatory edge cases:
- `simulation_atomicity = MIXED_BASIS_BLOCKED` or `STALE_BASIS_BLOCKED` MUST force `submission_enabled = false`
- if the basket is empty, `active_mutation_hazard_or_null` and `active_mutation_basis_contract_or_null` must stay null
- `timeline_state = REBASE_REQUIRED` must not erase the selected historical anchor
- `approval_composer.composer_state = NOT_REQUIRED` is lawful only when the preserved mutation basis says approval is not required
- `blast_radius_panel.panel_state = STALE` must be used when the visible hazard no longer matches the live policy / topology / simulation basis
- no route state may present a direct-save-on-blur illusion for mutable high-risk fields
- no responsive collapse may remount a different active section, different focus anchor, or different selected historical change while the same object still resolves

Playwright and contract-test requirements:
- add direct tests for section order, surface order, active section stability, inline help placement, basket atomicity rules, approval-composer readiness, blast-panel basis reuse, and timeline continuity
- if a northbound policy-snapshot route already exists, add Playwright `APIRequestContext` coverage for JSON shape, `policy_snapshot_hash`, continuity contracts, and typed stale-view failures
- if you add the optional lab, add Playwright browser tests that verify the basis ribbon, section-nav selection, staged-change ledger, non-modal support-surface behavior, and reduced-motion-safe compaction using semantic locators only
- capture serialized snapshot payloads and traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- create a quiet three-zone composition inside the governance shell: slim section-nav rail, dominant form canvas, narrow blast / approval support zone
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; supporting surface `#F1F3F0`; text `#171717` / `#667085`; accents `#0F766E`, `#1D4ED8`, `#B7791F`, `#C2410C`
- place a thin basis ribbon above the canvas showing `policy_snapshot_hash`, current simulation-basis state, and stale posture; keep it typographic and compact
- render staged changes as a ledger with precise before/after rows and reason / approval flags; never as generic toggle cards
- use one slim blast-radius interval band, not a scorecard wall
- animate only opacity / translate / height in `140ms`–`180ms`; reduced motion must preserve all meaning with no ornamental transitions
- if you show a diagram, use one narrow section-nav stack or one basis continuity ribbon; no donut charts, no decorative hero illustrations

Backend, performance, and security requirements:
- keep the projector strictly read-side; it must never be mistaken for live mutation authority
- derive environment bindings and policy posture from durable governance truth, not from browser-local staged state
- keep basis and hazard reuse canonical and deterministic; do not recompute or narrow reviewed risk in downstream adapters
- index the policy-snapshot read path by tenant and latest policy hash so the route stays fast under tenant scale
- fail closed on missing basis identity, missing policy hash, mixed-basis staging, or ambiguous current-vs-history selection

Validation rules:
- validate every emitted `GovernancePolicySnapshot` against schema, including nested interaction-layer, hazard, and basis packets
- add regression coverage for all named validator drift checks listed above
- keep this task limited to the policy snapshot route; do not swallow access simulation, authority-link inventory, retention, or audit routes

Definition of done:
- the repo has one authoritative `GovernancePolicySnapshot` projector with exact section-nav, same-shell basket / blast / approval continuity, and environment-binding truth
- submission readiness is now machine-checkable and basis-safe instead of UI-local guesswork
- the tenant policy route becomes a premium governance surface that feels deliberate, high-trust, and impossible to misread as instant-commit settings
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
