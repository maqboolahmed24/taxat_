---
id: pc_0225
slug: phase_04_parallel_wave_02_track_backend_release_resilience_007_implement_client_compatibility_matrix_generation_for_web_and_native_surface_versions
mode: parallel
phase: "04"
wave: "02"
track: "backend_release_resilience"
task_number: "007"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_release_resilience_0007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0225

## Task Summary

007 implement client compatibility matrix generation for web and native surface versions.

## Metadata

- ID: `pc_0225`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend release resilience`
- Task Number: `007`
- Source Slug: `phase_04_parallel_wave_02_track_backend_release_resilience_007_implement_client_compatibility_matrix_generation_for_web_and_native_surface_versions`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0224](./pc_0224.md) `phase_04_parallel_wave_02_track_backend_release_resilience_006_implement_restore_drill_result_capture_and_release_promotion_evidence_binding`
- Next: [pc_0226](./pc_0226.md) `phase_04_parallel_wave_02_track_backend_release_resilience_008_implement_recovery_tier_fail_forward_governance_and_post_incident_checkpoint_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0222_to_0229.md](../shared_operating_contract_0222_to_0229.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_release_resilience_007_implement_client_compatibility_matrix_generation_for_web_and_native_surface_versions`.

Inherit the shared operating contract and consume outputs from cards `pc_0219`, `pc_0222`, `pc_0223`, and `pc_0224` if they exist.

Mission:
Implement the canonical `ClientCompatibilityMatrix` generator and persistence layer for browser and macOS operator/client windows.
This task is not allowed to treat compatibility promises as release notes, spreadsheets, or manually curated tables detached from the candidate and compatibility gate.

Source algorithm grounding you must honor:
- `verification_and_release_gates.md`, especially section 1D and section 2.
- `deployment_and_resilience_contract.md`, especially sections 2, 3, and 6.
- `release_candidate_identity_and_promotion_evidence_contract.md`, especially supported-client window binding.
- `data_model.md`, especially `ClientCompatibilityMatrix`, `SchemaBundleCompatibilityGateContract`, and `ReleaseVerificationManifest`.
- `frontend_shell_and_interaction_law.md` and `macos_native_operator_workspace_blueprint.md` where browser/native operator surfaces and compatibility promises are materially relevant.
- `schemas/client_compatibility_matrix.schema.json`, `schemas/schema_bundle_compatibility_gate_contract.schema.json`, and `schemas/release_candidate_identity_contract.schema.json`.
- `scripts/validate_contracts.py`, especially `validate_client_compatibility_matrix` and the matrix drift checks named in the shared operating contract.
- `test_vectors.md`, especially `TV-78B`, `TV-80B`, and the supporting-client-window vectors.

Deliverables to create:
1. `packages/backend-release/src/models/client_compatibility_matrix.ts`
2. `packages/backend-release/src/repositories/client_compatibility_matrix_repository.ts`
3. `packages/backend-release/src/services/generate_client_compatibility_matrix.ts`
4. `packages/backend-release/src/services/derive_client_matrix_state.ts`
5. `packages/backend-release/src/tests/client_compatibility_matrix.spec.ts`
6. `packages/backend-release/src/tests/client_window_and_native_gate_alignment.spec.ts`
7. `docs/release/client_compatibility_matrix_generation.md`
8. Optional but recommended: `apps/admin-console-web/src/routes/debug/release/client-compatibility-matrix-lab.tsx`

You MUST patch any existing code path that currently:
- stores compatibility evidence without the candidate-bound supported-client window,
- omits one of the two required compatibility scenarios,
- or allows a green matrix to survive while the shared compatibility gate says the native client window is blocked.

Required design decisions you must make explicitly:
- define the canonical row ordering for `browser_rows[]` and `macos_rows[]`
- define how browser and macOS versions are sourced from the supported-client window contract or release inputs
- define how `matrix_state` is derived from row outcomes plus `schema_bundle_compatibility_gate_contract.native_client_window_state`
- define how candidates that do not lawfully publish a supported-client window are handled without generating misleading green matrices
- define how matrix generation binds to both the candidate tuple and the shared compatibility gate boundary

Execution requirements:
- persist `ClientCompatibilityMatrix` as a first-class artifact, not an attached spreadsheet or CI snippet
- require exact candidate binding and exact supported-client window binding
- generate and preserve the two required compatibility scenarios for every tested client family: `OLDEST_SUPPORTED_TO_CURRENT_SERVER` and `CURRENT_CLIENT_TO_ROLLBACK_SAFE_SERVER`
- keep row order canonical by `client_version` then scenario
- reject duplicate client/scenario rows
- require `matrix_state = GREEN` only when there are no incompatible rows and the shared compatibility gate says `native_client_window_state = VERIFIED_COMPATIBLE`
- require `matrix_state = RED` only when there is at least one incompatible row and the shared compatibility gate says `native_client_window_state = BLOCKED`
- keep browser and macOS rows distinct so browser-only and native-only failures remain explainable

Gap handling you must close while implementing:
- close the gap where supported-client compatibility is promised but not represented as a durable artifact
- close the gap where operator-client evidence can be attached to the wrong supported-client window
- close the gap where native-client compatibility can stay green after reader-window or client-window posture changes
- close the gap where one compatibility scenario is silently omitted and rollback-safe posture is therefore unproven

Mandatory edge cases:
- a matrix must fail closed when its supported-client window differs from the candidate contract
- duplicate client/scenario rows must fail closed
- missing required scenarios must fail closed whenever rows are present
- a green matrix with `native_client_window_state != VERIFIED_COMPATIBLE` must fail closed
- a red matrix without an incompatible row must fail closed
- a candidate without a lawful supported-client window must not fabricate a green matrix

Playwright and contract-test requirements:
- add direct tests for row ordering, duplicate rejection, required-scenario coverage, supported-window binding, and matrix-state derivation
- if you expose or patch internal release/debug routes, add Playwright `APIRequestContext` coverage for matrix retrieval and supported-window alignment
- if you add the optional lab, add Playwright browser tests for semantic filtering, row sorting, reduced-motion-safe inspector reveal, and keyboard navigation using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- render browser and macOS matrices as ordered tables with one scenario ribbon, not as charts
- surface the bound candidate hash and compatibility gate hash together
- use explicit compatible/incompatible language and preserve green/red posture only from server-authored truth
- respect the tranche visual language and keep the page quiet and exact

Backend, performance, and security requirements:
- generation must be deterministic for the same candidate and client-window inputs
- matrix artifacts must be immutable once published; newer evaluations supersede through new artifacts
- do not infer compatibility from semver ranges alone without the governed supported-client window inputs
- keep browser and native evidence queryable separately without widening release truth into UI-only summaries

Validation rules:
- validate every `ClientCompatibilityMatrix` payload against schema before persistence
- update fixtures/builders so validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test`
- rerun `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one authoritative, candidate-bound compatibility-matrix generator and repository
- required scenarios, row ordering, native-gate alignment, and window binding are durable and test-covered
- optional labs, if added, remain read-only viewers over persisted truth
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, the relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.