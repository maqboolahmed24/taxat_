---
id: pc_0213
slug: phase_04_parallel_wave_02_track_backend_retention_security_observability_005_implement_authority_ingress_correlation_proof_and_reconciliation_control_contract_binding
mode: parallel
phase: "04"
wave: "02"
track: "backend_retention_security_observability"
task_number: "005"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_retention_security_observability_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0213

## Task Summary

005 implement authority ingress correlation proof and reconciliation control contract binding.

## Metadata

- ID: `pc_0213`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend retention security observability`
- Task Number: `005`
- Source Slug: `phase_04_parallel_wave_02_track_backend_retention_security_observability_005_implement_authority_ingress_correlation_proof_and_reconciliation_control_contract_binding`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0212](./pc_0212.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_004_implement_security_runtime_hardening_guards_for_network_headers_cors_and_rate_limits`
- Next: [pc_0214](./pc_0214.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_006_implement_audit_event_trace_span_metric_event_and_log_record_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0206_to_0213.md](../shared_operating_contract_0206_to_0213.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_retention_security_observability_005_implement_authority_ingress_correlation_proof_and_reconciliation_control_contract_binding`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0212` if they exist, especially `pc_0139` through `pc_0140`, `pc_0166` through `pc_0169`, `pc_0189`, and `pc_0212`.

Mission:
Implement the authenticated authority-ingress checkpoint, ingress-proof contract binding, response-correlation contract, duplicate suppression, quarantine posture, normalization gating, and reconciliation control persistence that turns provider callbacks/polls/recovered responses into governed evidence before any legal-state mutation is even considered.
This task is not allowed to correlate or normalize provider ingress from transport-local memory or a lone authority reference.
Correlation must stay typed, conservative, auditable, and legally non-mutating until reconciliation control says otherwise.

Source algorithm grounding you must honor:
- `security_and_runtime_hardening_contract.md`, especially the authenticated ingress, dedupe, request-lineage, and authority-ingress proof sections.
- `modules.md`, especially `CHECKPOINT_AUTHORITY_INGRESS(...)`, `NORMALIZE_AUTHORITY_RESPONSE(...)`, `MERGE_AUTHORITY_RESPONSE_OBSERVATION(...)`, `PERSIST_AUTHORITY_RECONCILIATION_CONTROL(...)`, and `PROJECT_AUTHORITY_INGRESS_INVESTIGATION(...)`.
- `data_model.md`, especially `AuthorityIngressReceipt`, `AuthorityResponseEnvelope`, `AuthorityInteractionRecord`, and `AuthorityIngressInvestigationSnapshot`.
- `schemas/authority_ingress_correlation_contract.schema.json`, `schemas/authority_ingress_receipt.schema.json`, `schemas/authority_ingress_investigation_snapshot.schema.json`, and `schemas/authority_response_envelope.schema.json`.
- `scripts/validate_contracts.py`, especially the authority ingress receipt/correlation/investigation expectations.
- `tools/forensic_contract_guard.py`, especially the pinned `AUTHORITY_INGRESS_CORRELATION_CONTRACT_V1` policies and the receipt-state posture rules.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially duplicate-suppression, partial-bind quarantine, weak authority-reference-only match, and ambiguous multi-match vectors.

Deliverables to create:
1. `packages/backend-authority/src/models/authority_ingress_correlation_contract.ts`
2. `packages/backend-authority/src/services/checkpoint_authority_ingress.ts`
3. `packages/backend-authority/src/services/build_authority_ingress_correlation_contract.ts`
4. `packages/backend-authority/src/services/normalize_authority_response.ts`
5. `packages/backend-authority/src/services/merge_authority_response_observation.ts`
6. `packages/backend-authority/src/services/persist_authority_reconciliation_control.ts`
7. `packages/backend-authority/src/queries/project_authority_ingress_investigation.ts`
8. `packages/backend-authority/src/tests/authority_ingress_correlation.spec.ts`
9. `packages/backend-authority/src/tests/authority_ingress_receipt_states.spec.ts`
10. `docs/authority/authority_ingress_correlation_proof_and_reconciliation_control_contract_binding.md`
11. Optional but recommended: `apps/admin-console-web/src/routes/debug/authority/ingress-correlation-lab.tsx`

You MUST patch any existing callback, polling, or recovery ingress path that normalizes or mutates legal state before persisting `AuthorityIngressReceipt` and correlation/proof truth.

Required design decisions you must make explicitly:
- define the authenticated-ingress checkpoint and proof-contract binding fields that every ingress path must publish
- define the comparison-set construction rules for request lineage, extracted provider keys, and duplicate-meaning comparison
- define the exact behavior for `BOUND`, `BOUND_WITH_AUTHORITY_REFERENCE_ONLY`, `AMBIGUOUS`, `UNBOUND`, and duplicate-suppressed postures
- define when normalization is legal, when quarantine is mandatory, and how reconciliation owner assignment is persisted
- define the investigation snapshot projection that exposes only safe next-action codes and correlation explainability

Execution requirements:
- persist `AuthorityIngressReceipt` before response normalization or any legal-state mutation
- authenticate the provider channel, compute the provider-delivery dedupe key, and bind the payload to expected request lineage before correlation proceeds
- publish one grouped `authority_ingress_proof_contract{...}` reused by the receipt, normalized response envelope, and later authority-settling artifacts
- build `AuthorityIngressCorrelationContract` from persisted request-lineage truth only; do not use transport-local memory as a correlation source
- keep `legal_mutation_policy = NO_DIRECT_LEGAL_STATE_MUTATION_FROM_CORRELATION`
- quarantine weak or ambiguous matches rather than treating them as exact legal correlation
- allow `NORMALIZED` only when authenticated ingress, exact bound correlation, and all required request/hash/idempotency/identity/duplicate keys plus `normalized_response_ref` are present
- allow `DUPLICATE_SUPPRESSED` only when a canonical ingress receipt ref exists and no second normalized response is emitted
- ensure `AuthorityIngressInvestigationSnapshot` surfaces safe next-action codes and keeps duplicate/ambiguous posture distinct

Gap handling you must close while implementing:
- close any gap where a lone `authority_reference` match can be treated as exact legal correlation
- close any gap where normalization occurs before authenticated ingress proof and comparison-set construction are durable
- close any gap where duplicate suppression lacks a canonical receipt ref or emits a second normalized response anyway
- close any gap where weak/unresolved correlation clears quarantine posture or loses reconciliation ownership
- close any gap where correlation directly mutates legal state instead of feeding governed reconciliation control

Mandatory edge cases:
- missing provider keys must remain distinct from an explicit no-match result
- `AMBIGUOUS` requires multi-match posture and at least two candidate lineages
- `UNBOUND` requires zero candidates, not merely low confidence
- `BOUND_WITH_AUTHORITY_REFERENCE_ONLY` must stay weak and must not become full normalization or legal mutation authority
- `QUARANTINED` receipts require `reconciliation_owner_ref`
- `DUPLICATE_SUPPRESSED` receipts require `canonical_ingress_receipt_ref` and must clear normalization fields
- `NORMALIZED` receipts require authenticated ingress, exact bound correlation, and non-null required request-binding fields

Playwright and contract-test requirements:
- add deterministic tests for authenticated ingress checkpointing, exact vs weak vs ambiguous correlation, duplicate suppression, normalization gating, investigation snapshot projection, and reconciliation-owner persistence
- if you expose or patch an authority/admin route, add Playwright `APIRequestContext` coverage for quarantined, duplicate-suppressed, exact-bound, and ambiguous outcomes
- if you add the optional lab, add Playwright browser tests that verify the correlation table, quarantine/duplicate state band, safe-next-action ladder, keyboard navigation, and reduced-motion-safe reveal behavior using semantic locators only
- capture payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a three-zone layout: ingress-proof/correlation ribbon, candidate-lineage table canvas, narrow reconciliation/safe-next-action sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; accent `#0F766E`; info accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- present exact/weak/ambiguous/duplicate posture as explicit typed bands or rows, not as confidence-only chips
- render candidate-lineage evidence and next-action codes as ordered ledgers or tables rather than charts
- animate only opacity/translate/height with `140ms` to `180ms`; respect reduced motion

Backend, performance, and security requirements:
- keep the authenticated ingress checkpoint and correlation path idempotent under provider retries and recovery replay
- prohibit raw response bodies or raw authority payloads from leaking into logs or unredacted diagnostics
- centralize normalization gating so no callback/poll/recovery path can bypass it
- persist dedupe, proof, and correlation lineage append-only so later observability and investigation cards can query them without reprocessing raw transport state

Validation rules:
- validate every `AuthorityIngressCorrelationContract`, `AuthorityIngressReceipt`, `AuthorityIngressInvestigationSnapshot`, and `AuthorityResponseEnvelope` against schema before persistence or response emission
- update fixtures/builders so all authority ingress validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one canonical backend path for authenticated ingress checkpointing, one for correlation, one for normalization gating, and one for reconciliation-control persistence
- exact, weak, ambiguous, duplicate, and quarantine postures are durable, safe, and test-covered
- optional labs, if added, remain read-only projections of persisted ingress truth
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
