---
id: pc_0193
slug: phase_04_parallel_wave_02_track_backend_governance_005_implement_retention_governance_frame_projection_legal_hold_register_and_erasure_queues
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "005"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0193

## Task Summary

implement retention governance frame projection legal hold register and erasure queues.

## Metadata

- ID: `pc_0193`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `005`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_005_implement_retention_governance_frame_projection_legal_hold_register_and_erasure_queues`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0192](./pc_0192.md) `phase_04_parallel_wave_02_track_backend_governance_004_implement_authority_link_inventory_item_projection_and_health_rollups`
- Next: [pc_0194](./pc_0194.md) `phase_04_parallel_wave_02_track_backend_governance_006_implement_audit_investigation_frame_projection_and_correlation_neighborhood_queries`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0190_to_0197.md](../shared_operating_contract_0190_to_0197.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_005_implement_retention_governance_frame_projection_legal_hold_register_and_erasure_queues`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0192` if they exist, especially `pc_0094` through `pc_0101`, `pc_0130` through `pc_0133`, and `pc_0190`.

Mission:
Implement the authoritative `RetentionGovernanceFrame` projector, including the retention policy matrix, legal-hold register, erasure queue, and impact preview for `/v1/governance/tenants/{tenant_id}/retention`.
This route is not a generic settings matrix and not a hidden destructive back-office queue.
It must keep statutory baseline, tenant override, effective minimum, limitation behavior, pseudonymisation mode, legal holds, erasure readiness, blocker reasons, and previewed provenance limitations explicit and same-shell.

Source algorithm grounding you must honor:
- `admin_governance_console_architecture.md`, especially section `4.5 /governance/retention`, the policy-matrix rules, legal-hold rules, erasure-workbench rules, and critical surfaces.
- `data_model.md`, especially `RetentionGovernanceFrame`, `retention_workspace`, `retention_policy_matrix`, `legal_hold_register`, `erasure_queue`, and `retention_impact_preview`.
- `retention_and_privacy.md` and `retention_limited_explainability_and_audit_sufficiency_contract.md`, especially statutory minimums, limitation posture, and privacy / provenance constraints.
- `northbound_api_and_session_contract.md`, especially `GET /v1/governance/tenants/{tenant_id}/retention`.
- `schemas/retention_governance_frame.schema.json`, `schemas/governance_interaction_layer.schema.json`, and any nested externalization / limitation contracts involved in the impact preview.
- `scripts/validate_contracts.py`, especially `retention_governance_frame_policy_matrix_order_and_warning_alignment`, `retention_governance_frame_legal_hold_selection_and_release_preview_alignment`, and `retention_governance_frame_erasure_queue_and_destructive_review_posture`.
- `PATCH_RESOLUTION_INDEX.md`, especially the retention-frame empty-field hardening items.

Deliverables to create:
1. `packages/backend-governance/src/projectors/build_retention_governance_frame.ts`
2. `packages/backend-governance/src/services/build_retention_policy_matrix.ts`
3. `packages/backend-governance/src/services/build_legal_hold_register.ts`
4. `packages/backend-governance/src/services/build_erasure_queue.ts`
5. `packages/backend-governance/src/services/build_retention_impact_preview.ts`
6. `packages/backend-governance/src/tests/retention_governance_frame_projection.spec.ts`
7. `packages/backend-governance/src/tests/retention_hold_release_and_erasure_posture.spec.ts`
8. `docs/governance/retention_governance_frame_and_erasure_review.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/retention-lab.tsx`

You MUST patch any existing retention or privacy governance read route so it emits this frame instead of stitching matrix rows, legal holds, and erasure items together in controllers.

Required design decisions you must make explicitly:
- define how statutory baseline, tenant override, and effective minimum are resolved for each artifact class and how missing overrides are represented
- define how limitation behavior and pseudonymisation mode are surfaced in the policy matrix and impact preview
- define how `warning_posture` is derived for the overall workspace and for the preview panel
- define how legal-hold release preview is computed so operators can see which blocked erasure items would become eligible after release
- define how erasure readiness is partitioned into `ELIGIBLE`, `BLOCKED`, and `PENDING_REVIEW`
- define how affected-client count, affected-artifact count, and projected provenance limitations are computed for `retention_impact_preview`

Execution requirements:
- publish a schema-valid `RetentionGovernanceFrame`
- preserve the fixed workspace reading order exactly: `INVENTORY_RAIL -> WORKSPACE_CANVAS -> RETENTION_IMPACT_PREVIEW -> AUDIT_SIDECAR`
- preserve the policy-matrix column order exactly:
  `ARTIFACT_CLASS -> STATUTORY_BASELINE -> TENANT_OVERRIDE -> EFFECTIVE_MINIMUM -> LIMITATION_BEHAVIOR -> PSEUDONYMISATION_MODE -> EXPORT_POSTURE`
- keep `editing_posture = EXPLICIT_STAGE_ONLY` and `inline_blocker_visibility = ALWAYS_VISIBLE`
- preserve sticky headers with `sticky_header_mode = ROW_AND_COLUMN_HEADERS`
- preserve the legal-hold register column order exactly:
  `CLIENT -> OBJECT_REF -> HOLD_REASON -> RELEASE_ELIGIBILITY -> BLOCKED_ERASURE_COUNT -> LAST_CHANGED_AT`
- preserve the erasure-queue section order exactly: `ELIGIBLE -> BLOCKED -> PENDING_REVIEW`
- keep `destructive_flow_mode = CHANGE_BASKET_ONLY`
- make legal-hold release preview explicit: if a release action is previewable or stageable, both `selected_hold_ref_or_null` and `release_preview_ref_or_null` must be populated lawfully
- keep blocked rows and blocked erasure items visibly tied to explicit blocker refs; do not hide the statutory or hold basis in a sidecar-only explanation
- when an erasure candidate is selected, promote `RetentionImpactPreview` as the support region until the operator stages or leaves the destructive path

Gap handling you must close while implementing:
- close any gap where retention policy, legal holds, and erasure readiness are flattened into one table or one count wall
- close any gap where legal-hold release can be initiated without a preview of newly eligible erasure items
- close any gap where erasure posture hides blocker reasons or provenance limitations until too late
- close any gap where destructive actions can appear one-click or immediate
- close any gap where current selection and warning posture drift during refresh, compaction, or rebase

Mandatory edge cases:
- `release_action_posture = NONE_SELECTED` must force both `selected_hold_ref_or_null = null` and `release_preview_ref_or_null = null`
- `release_action_posture = PREVIEW_ONLY` or `CHANGE_BASKET_REQUIRED` must require both a selected hold and a non-null preview ref
- if `primary_blocker_ref_or_null` is non-null, `blocked_item_refs[]` must be non-empty
- `action_posture = BLOCKED` must keep destructive actions unavailable even when an item is selected
- no tenant override may reduce effective minimum below statutory minimum
- no erasure queue may collapse blocked and pending-review items into one undifferentiated list

Playwright and contract-test requirements:
- add direct tests for matrix ordering, warning posture, hold selection / release preview coupling, erasure partitioning, destructive-flow gating, and impact-preview counts
- if a northbound retention route exists, add Playwright `APIRequestContext` tests for active filters, selected-row continuity, hold preview posture, and erasure queue partitioning
- if you add the optional lab, add Playwright browser tests that verify sticky matrix semantics, legal-hold preview behavior, erasure swimlane promotion, and reduced-motion-safe compaction using semantic locators only
- capture serialized frame payloads and traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- create one quiet governance composition with filter rail, dominant matrix / register canvas, one promoted preview sidecar, and a subdued audit sidecar
- max width `1440px`; palette `#F7F5F1 / #FFFFFF / #F1F3F0 / #171717 / #667085 / #0F766E / #1D4ED8 / #B7791F / #C2410C`
- render the policy matrix as a precise, typography-led table with sticky headers and inline blocker chips; avoid oversized cards
- render legal holds as a compact register with one mounted release-preview drawer or sidecar
- render erasure readiness as a three-lane swimlane with literal section labels and one selected-item preview band
- render impact preview as a structured prose + count panel, not as a destructive confirmation modal
- any diagram should be a policy-change impact strip or erasure flow rail only; no dashboard charts

Backend, performance, and security requirements:
- keep the frame strictly read-side and review-oriented; destructive mutation remains elsewhere
- derive legal-hold and erasure readiness from durable privacy / retention truth, not from UI-local state
- keep policy-matrix row refs, hold refs, and erasure item refs stable and indexable
- fail closed on ambiguous statutory baseline, ambiguous hold state, or missing limitation posture

Validation rules:
- validate every emitted `RetentionGovernanceFrame` against schema
- add regression coverage for the named validator drifts plus selected-object continuity and destructive-flow gating
- keep this task limited to the retention governance frame and read-side preview semantics

Definition of done:
- the repo has one authoritative retention governance frame with policy matrix, hold register, erasure queue, and impact preview truth
- destructive retention work is now visibly staged, previewed, and blocker-aware instead of hidden or one-click
- governance clients can render the route as a premium, trustworthy control-plane surface without inventing privacy semantics locally
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
