---
id: pc_0103
slug: phase_03_parallel_wave_01_track_backend_manifest_007_implement_begin_manifest_begin_child_manifest_and_manifest_start_claim_protocol
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "007"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0103

## Task Summary

implement begin manifest begin child manifest and manifest start claim protocol.

## Metadata

- ID: `pc_0103`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `007`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_007_implement_begin_manifest_begin_child_manifest_and_manifest_start_claim_protocol`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0102](./pc_0102.md) `phase_03_parallel_wave_01_track_backend_manifest_006_implement_load_and_validate_prior_manifest_context_and_reuse_decision_modules`
- Next: [pc_0104](./pc_0104.md) `phase_03_parallel_wave_01_track_backend_manifest_008_implement_update_manifest_preseal_context_and_scope_execution_binding_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0102_to_0109.md](../shared_operating_contract_0102_to_0109.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_007_implement_begin_manifest_begin_child_manifest_and_manifest_start_claim_protocol`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0102` if they exist, especially `pc_0010`, `pc_0016`, `pc_0028`, `pc_0066`, `pc_0067`, `pc_0076`, `pc_0084`, `pc_0085`, `pc_0097`, `pc_0098`, `pc_0099`, `pc_0100`, `pc_0101`, and `pc_0102`.

Mission:
Implement root-manifest allocation, child-manifest allocation, and the authoritative start-claim / reclaim protocol that moves a sealed manifest into active execution without ambiguity.
This task must preserve lineage, generation, attempt-lineage continuity, first-publication atomicity, and single-writer start semantics.
It is the bridge between request-time branch selection and durable execution ownership.

Source algorithm grounding you must honor:
- `core_engine.md`, especially root or child allocation, start-claim timing, and first stage/outbox publication semantics.
- `modules.md`, especially `BEGIN_MANIFEST(...)`, `BEGIN_CHILD_MANIFEST(...)`, and `CLAIM_MANIFEST_START(...)`.
- `manifest_start_claim_protocol.md`, especially claim outcomes, durable start lease semantics, stale-reclaim rules, same-attempt recovery, and atomic commit requirements.
- `manifest_and_config_freeze_contract.md`, especially single-writer start rules, lineage inheritance, nightly launch context, and exact recovery attempt-lineage reuse.
- `replay_and_reproducibility_contract.md`, especially recovery-child lineage and basis preservation.
- `state_machines.md`, especially `SEALED -> IN_PROGRESS`, terminal result recording, and failure modes around start and reclaim.
- `data_model.md`, especially `RunManifest`, `manifest_start_claim{...}`, `continuation_set{...}`, and top-level lineage mirrors.
- Existing schemas including `run_manifest.schema.json`, `manifest_start_claim_contract.schema.json`, `manifest_lineage_trace.schema.json`, and `decision_bundle.schema.json`.

Deliverables to create:
1. `packages/backend-manifest/src/services/begin_manifest.ts`.
2. `packages/backend-manifest/src/services/begin_child_manifest.ts`.
3. `packages/backend-manifest/src/services/claim_manifest_start.ts`.
4. `packages/backend-manifest/src/services/reclaim_manifest_start.ts`.
5. `packages/backend-manifest/src/services/attempt_lineage_ref_service.ts`.
6. `packages/backend-manifest/src/services/prestart_manifest_target_validator.ts`.
7. `packages/backend-manifest/src/services/start_claim_atomic_publication.ts`.
8. `packages/backend-manifest/src/types/manifest_start_claim_outcome.ts`.
9. `db/migrations/phase03_0103_manifest_start_claim_protocol.sql`.
10. `docs/manifest/begin_manifest_and_start_claim_protocol.md`.
11. `tests/unit/backend_manifest/begin_manifest_and_start_claim.spec.ts`.
12. `tests/integration/backend_manifest/start_claim_atomicity_and_reclaim.spec.ts`.

Required design decisions you must make explicit:
- Define the storage and repository mutation pattern for `manifest_start_claim{...}`.
  Explain how claim state, lease token, lease expiry, holder identity, epoch counters, and first publication refs are updated atomically with the manifest lifecycle transition.
- Define how root versus child manifests initialize lineage refs, generation counters, nightly launch context, and parent-hash-at-branch.
- Define how replay child, recovery child, continuation child, and new-request child differ in lineage and `attempt_lineage_ref` handling.
- Define compare-and-swap rules for `SEALED -> IN_PROGRESS` and reclaim transitions.
- Define how first stage publication / outbox refs are written in the same durable boundary as a successful claim without forcing later stage orchestration into this card.
- Define how stale reclaim proofs are represented and what evidence is required before granting a reclaim.

Execution steps:
1. Implement `begin_manifest.ts` so root manifests are allocated with correct root lineage, generation zero, nightly launch context capture, and typed placeholder-compatible claim state.
2. Implement `begin_child_manifest.ts` so child manifests inherit and persist lineage, continuation basis, config/input inheritance modes, parent manifest hash at branch, and exact replay/recovery lineage semantics.
3. Implement `attempt_lineage_ref_service.ts` so started-run recovery children reuse the original attempt lineage while other child types allocate or preserve the correct lineage shape.
4. Implement `claim_manifest_start.ts` with compare-and-swap semantics that only grant start on a truly pre-start sealed manifest and commit `opened_at`, claim state, lease token, and first-publication refs together.
5. Implement `reclaim_manifest_start.ts` so stale reclaim can succeed only when durable expiry proof exists and there is no still-active lease.
6. Implement `prestart_manifest_target_validator.ts` so claim targets fail closed when the manifest has already started, already terminalized, or drifted from pre-start sealed invariants.
7. Add tests for root manifest allocation, replay child allocation, recovery-child allocation, granted claim, already-active rejection, already-terminal rejection, stale-recovery required, reclaim granted, and reclaim rejected due to active lease.

Gap handling you must perform:
- Close the gap where start ownership could be implied by transient worker state rather than durable manifest truth.
- Close the gap where recovery-child lineage and attempt-lineage reuse remain prose rather than enforced executable behavior.
- Close the gap where first publication could happen outside the same transaction as start claim.
- Close the gap where stale reclaim might be granted without durable expiry evidence.
- Close the gap where replay or recovery children could be allocated with broken parent/root lineage mirrors.

Mandatory edge cases:
- root manifest allocates with `root_manifest_id = manifest_id`, `parent_manifest_id = null`, and generation zero.
- replay child persists `parent_manifest_id = replay_of_manifest_id` and correct continuation basis / inheritance modes.
- recovery child reuses the original `attempt_lineage_ref`.
- no recovery child may be created while the source manifest is still `ACTIVE_LEASED`.
- `CLAIM_GRANTED`, `ALREADY_ACTIVE`, `ALREADY_TERMINAL`, `INVALID_PRESTART_STATE`, `RECOVERY_REQUIRED`, `RECLAIM_GRANTED`, and `RECLAIM_REJECTED_ACTIVE_LEASE` must be explicit typed outcomes.
- a claim target with `opened_at` already set, outputs present, or start-claim drift must fail closed.
- claim success must write `opened_at`, `IN_PROGRESS`, claim token, claim state, and first-publication refs in one durable boundary.
- reclaim must preserve the same `attempt_lineage_ref` and must not invent a fresh attempt lineage for same-attempt recovery.
- terminal result recording must transition claim state to `TERMINAL_RESULT_RECORDED`; it must not leave a terminal manifest looking active.

Backend, performance, and security requirements:
- Start-claim logic must be concurrency-safe under duplicate worker start attempts.
- Lease tokens and holder identities are sensitive control data; never log raw secrets or credentials.
- Use explicit transaction boundaries and optimistic locking or compare-and-swap so partial start claims cannot leak.
- Keep allocation helpers deterministic and side-effect free until persistence is intentionally invoked.
- Do not widen access or execution scope here; consume the branch decision and frozen envelope rather than re-deriving them.

Validation rules:
- Unit-test lineage generation, attempt-lineage reuse, and claim outcome mapping.
- Integration-test compare-and-swap safety under racing claim attempts and stale reclaim attempts.
- Validate persisted `manifest_start_claim{...}` shapes against the imported schema.
- Prove that successful start claim and first publication references commit together or not at all.
- Prove that claim rejection never leaves a half-open manifest.

Definition of done:
- The repo has executable services that can allocate root and child manifests and claim start ownership with one authoritative durable protocol.
- Later execution cards can rely on exact lineage, attempt continuity, and claim-state truth instead of worker-local conventions.
- Recovery and reclaim semantics are enforceable, auditable, and aligned to the corpus instead of inferred from timestamps alone.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
