---
id: pc_0172
slug: phase_04_parallel_wave_02_track_backend_low_noise_004_implement_low_noise_budget_audit_and_budget_audit_pack_generators
mode: parallel
phase: "04"
wave: "02"
track: "backend_low_noise"
task_number: "004"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_low_noise_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0172

## Task Summary

implement low noise budget audit and budget audit pack generators.

## Metadata

- ID: `pc_0172`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend low noise`
- Task Number: `004`
- Source Slug: `phase_04_parallel_wave_02_track_backend_low_noise_004_implement_low_noise_budget_audit_and_budget_audit_pack_generators`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0171](./pc_0171.md) `phase_04_parallel_wave_02_track_backend_low_noise_003_implement_detail_drawer_state_and_support_region_promotion_rules`
- Next: [pc_0173](./pc_0173.md) `phase_04_parallel_wave_02_track_backend_low_noise_005_implement_experience_delta_publication_coalescing_and_noise_budget_controls`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0166_to_0173.md](../shared_operating_contract_0166_to_0173.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_low_noise_004_implement_low_noise_budget_audit_and_budget_audit_pack_generators`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0171` if they exist, especially `pc_0169` through `pc_0171`.

Mission:
Implement the deterministic budget-evidence layer that proves calm-shell compression remains within the algorithm’s frozen cognitive limits under first view, refresh, reconnect, and fallback pressure.
This task must make `LowNoiseBudgetAudit` and `LowNoiseBudgetAuditPack` backend artifacts, not optional test-only afterthoughts.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md`, especially the budget audit artifact rules and minimum scenario coverage.
- `low_noise_surface_compression_and_noise_budget_audit_contract.md`.
- `modules.md`, especially `BUILD_LOW_NOISE_BUDGET_AUDIT(...)` and `RUN_LOW_NOISE_BUDGET_AUDIT_PACK(...)`.
- `data_model.md`, especially `LowNoiseBudgetAudit` and `LowNoiseBudgetAuditPack`.
- `schemas/low_noise_budget_audit.schema.json` and `schemas/low_noise_budget_audit_pack.schema.json`.
- `test_vectors.md`, especially the non-material refresh and reconnect/budget vectors.
- `PATCH_RESOLUTION_INDEX.md`, especially items 404-413.

Deliverables to create:
1. `packages/backend-low-noise/src/audit/build_low_noise_budget_audit.ts`
2. `packages/backend-low-noise/src/audit/run_low_noise_budget_audit_pack.ts`
3. `packages/backend-low-noise/src/audit/compute_scan_load.ts`
4. `packages/backend-low-noise/src/audit/detect_duplicate_posture_codes.ts`
5. `packages/backend-low-noise/src/audit/count_secondary_mutation_actions.ts`
6. `packages/backend-low-noise/src/audit/build_budget_scenario_matrix.ts`
7. `packages/backend-low-noise/src/tests/low_noise_budget_audit.spec.ts`
8. `docs/low_noise/budget_audit_and_pack_generation.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/low-noise/budget-audit-lab.tsx`

Required design decisions you must make explicitly:
- define the deterministic seed and run mode for `LowNoiseBudgetAuditPack` generation so the same fixture suite produces the same pack id and case ordering
- define exactly how `visible_shell_char_count` is computed from rendered copy rather than hidden disclosures or source prose
- define how `scan_load` is computed from the frozen formula and where that formula lives so both publication and regression suites reuse it
- define how the pack binds each scenario case back to a concrete frame/input lineage so the audit evidence is not detached from published shell truth

Execution requirements:
- compute `LowNoiseBudgetAudit` directly from the published shell payload, not from hypothetical pre-collapse content
- require exact rendered surface order, `persistent_surface_count = 4`, `concurrent_primary_count = 1`, and `secondary_mutation_action_count = 0`
- compute `duplicate_posture_codes[]` and fail publication or regression when it is non-empty
- produce an audit pack that covers at minimum first view, high reason pressure, `NO_SAFE_ACTION`, non-material refresh, reconnect/catch-up, and typed detail fallback
- if you add the optional lab, make it a restrained evidence table or ladder view; no dashboard charts, no vanity metrics

Gap handling you must close while implementing:
- the low-noise contract demands budget evidence but does not ship an implementation skeleton; create one canonical audit layer now
- visible copy counts are easy to measure from source strings instead of rendered strings; tie counting to the actual surface payloads
- regression evidence can drift from publication evidence if they use different formulas or case sets; reuse one shared audit core for both
- if the scenario matrix needs fixture inputs not yet normalized, create stable fixture adapters rather than letting tests construct bespoke shell payloads by hand

Mandatory edge cases:
- `rendered_surface_order[]` must be exactly `CONTEXT_BAR -> DECISION_SUMMARY -> ACTION_STRIP -> DETAIL_DRAWER`
- `duplicate_posture_codes[]` must be empty on published frames
- `secondary_mutation_action_count` must be `0`
- `FIRST_VIEW`, `NON_MATERIAL_REFRESH`, and `RECOVERY_RECONNECT` must behave differently where the schema requires refresh-only fields or fallback/coalescing fields
- include at least one case where non-material change is coalesced to preserve budget
- include at least one case for each typed detail fallback posture when lawful fixtures exist

Playwright and contract-test requirements:
- test the audit generators directly
- if you add the lab, use Playwright semantic locators and explicit table-row assertions; no CSS-chain selectors or fixed sleeps
- capture the serialized audit and pack payloads on failures
- make the regression suite replayable from deterministic fixtures or golden-case manifests

Backend, performance, and security requirements:
- keep audit generation pure, deterministic, and cheap enough to run during publication and regression
- never let the audit layer mutate or re-rank shell state to make metrics look better; it must measure the published truth, not repair it silently
- do not expose internal-only case fixtures or hidden copy through the optional lab
- keep pack generation isolated from production request latency if it becomes heavier than per-frame audits

Validation rules:
- validate every `LowNoiseBudgetAudit` and `LowNoiseBudgetAuditPack` against their schemas
- unit-test scan-load computation, posture deduplication, secondary-mutation detection, and scenario coverage
- integration-test audit generation from real frame payloads emitted by the projector stack
- prove scenario-pack determinism by repeating generation from the same seeded inputs

Definition of done:
- the repo can generate schema-valid per-frame budget audits and deterministic regression audit packs
- calm-shell publication has machine-checkable evidence for surface order, copy budget, duplicate-posture compression, and secondary-mutation absence
- regression cases are tied to actual shell truth rather than ad hoc handcrafted payloads
- later low-noise work can rely on the audit layer as the authoritative budget oracle.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

