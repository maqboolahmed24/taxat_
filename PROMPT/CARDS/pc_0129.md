---
id: pc_0129
slug: phase_03_parallel_wave_01_track_backend_compute_009_implement_proof_bundle_generation_and_defensible_filing_graph_closure
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "009"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0129

## Task Summary

implement proof bundle generation and defensible filing graph closure.

## Metadata

- ID: `pc_0129`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `009`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_009_implement_proof_bundle_generation_and_defensible_filing_graph_closure`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0128](./pc_0128.md) `phase_03_parallel_wave_01_track_backend_compute_008_implement_provenance_node_edge_and_path_graph_builder`
- Next: [pc_0130](./pc_0130.md) `phase_03_parallel_wave_01_track_backend_compute_010_implement_enquiry_pack_assembly_and_retention_limited_explainability_support`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0126_to_0133.md](../shared_operating_contract_0126_to_0133.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_009_implement_proof_bundle_generation_and_defensible_filing_graph_closure`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0128` if they exist, especially `pc_0010`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0074`, `pc_0076`, `pc_0097`, `pc_0108`, `pc_0118`, `pc_0121`, `pc_0124`, `pc_0125`, `pc_0126`, `pc_0127`, and `pc_0128`.

Mission:
Implement the deterministic proof-closure and `ProofBundle` assembly layer that turns a persisted `EvidenceGraph` plus filing-capable target assessments into one decisive, replay-safe, queryable, and historically preserved proof artifact.
This task must compute support / authority / contradiction / replay closure, select one primary path deterministically, preserve materially distinct rejected paths and their rejection bases, persist replay recipes and render refs, and ensure stale or superseded proof remains historical rather than controlling.

Source algorithm grounding you must honor:
- `defensible_filing_graph_contract.md`, especially the closure formula, support-state taxonomy, admissible support-path grammar, primary-path ranking ladder, contradiction isolation, replay closure, proof bundle contract, and historical / controlling bundle distinction.
- `provenance_graph_semantics.md`, especially target assessment continuity and proof-bundle linkage rules.
- `modules.md`, especially graph/proof assembly, target-assessment requirements, and filing-gate proof prerequisites.
- `data_model.md`, especially `ProofBundle` and `EvidenceGraph`.
- `state_machines.md`, especially `EvidenceGraph.lifecycle_state` and `ProofBundle.lifecycle_state`.
- `retention_limited_explainability_and_audit_sufficiency_contract.md` where proof limitations interact with later enquiry surfaces.
- `PATCH_RESOLUTION_INDEX.md`, especially fixes around `primary_path_ref`, `rejected_path_refs[]`, stale proof lineage, and decisive-path containment.
- `test_vectors.md`, especially `TV-59`, `TV-60`, `TV-60C`, `TV-61`, `TV-61A`, `TV-61B`, and `TV-61C`.
- Existing schema `schemas/proof_bundle.schema.json`.

Deliverables to create:
1. `packages/backend-provenance/src/models/proof_bundle.ts`.
2. `packages/backend-provenance/src/repositories/proof_bundle_repository.ts`.
3. `packages/backend-provenance/src/services/build_target_assessments.ts`.
4. `packages/backend-provenance/src/services/select_primary_proof_path.ts`.
5. `packages/backend-provenance/src/services/build_proof_closure_contract.ts`.
6. `packages/backend-provenance/src/services/build_proof_bundle.ts`.
7. `packages/backend-provenance/src/services/transition_proof_bundle.ts`.
8. `packages/backend-provenance/src/services/validate_proof_bundle.ts`.
9. `packages/backend-provenance/src/services/reconstruct_proof_bundle_for_replay.ts`.
10. `db/migrations/phase03_0129_proof_bundle_and_graph_closure.sql`.
11. `docs/provenance/proof_bundle_generation_and_defensible_filing_graph_closure.md`.
12. `tests/unit/backend_provenance/proof_closure_contract.spec.ts`.
13. `tests/unit/backend_provenance/primary_and_rejected_path_ordering.spec.ts`.
14. `tests/integration/backend_provenance/proof_bundle_generation_and_staleness.spec.ts`.

Required design decisions you must make explicit:
- Define the exact target-assessment input contract.
  It must bind one filing-capable target to one comparison of decisive admissible support, authority closure, contradiction state, replay closure, and limitation posture.
- Define the primary-path selector with the exact ranking ladder from the contract:
  admissible and contradiction-free first,
  then authority-linked prerequisite satisfaction without inferred decisive support,
  then highest weakest decisive-edge confidence,
  then fewest limitation codes,
  then fewest stale / tombstoned decisive segments,
  then shortest hop count,
  then lexical `path_id`.
- Define how materially distinct rejected paths are detected and persisted.
- Define the replay recipe contract so proof can be reconstructed without live provider reads or viewer-specific heuristics.
- Define the historical / controlling distinction.
  Older limited, stale, or superseded bundles must remain queryable without masquerading as current proof.

Execution steps:
1. Implement the schema-backed `ProofBundle` model, repository, and lifecycle transition helper.
2. Implement `build_target_assessments.ts` so every filing-capable target receives explicit support state, contradiction posture, authority closure, replay closure, and limitation notes.
3. Implement `select_primary_proof_path.ts` using the exact ranking ladder from the contract and preserve rejected materially distinct paths in deterministic order.
4. Implement `build_proof_closure_contract.ts` so `support_closed`, `authority_closed`, `contradiction_isolated`, and `replay_closed` combine into the required closure formula and `closure_state`.
5. Implement `build_proof_bundle.ts` so the bundle persists primary path, rejected paths, replay recipe, render refs, limitation notes, retention binding, and deterministic hash.
6. Implement `validate_proof_bundle.ts` so `primary_path_ref` must resolve inside the decisive set, `rejected_path_refs[]` must not overlap the primary controlling path set, and stale / contradiction / limitation states fail closed where required.
7. Implement `reconstruct_proof_bundle_for_replay.ts` so replay consumers can verify the bundle from persisted graph inputs and replay recipe without live graph mutation.
8. Add tests for contradiction-driven open closure, stale proof from late data, deterministic primary-path selection, rejected-path ordering, and historical supersession behavior.

Gap handling you must perform:
- Close the gap where topology exists before one authoritative closure/bundle service does.
  Build one closure service that reads persisted graph truth and emits one decisive bundle per target purpose.
- Close the gap where primary-path selection could drift from replay due to unstable ordering.
  Canonicalize ranking inputs and tie-breakers before bundle persistence.
- Close the gap where rejected paths might overlap the decisive set or disappear entirely.
  Preserve materially distinct rejected paths explicitly with deterministic order and rejection basis.
- Close the gap where silent limitation ambiguity might otherwise look like support.
  Fail closed and persist explicit limitation / unsupported posture instead.
- Close the gap where stale or superseded proof could still be treated as current filing proof by later stages.
  Preserve history but mark control posture explicitly.

Mandatory edge cases:
- if `proof_closure = 1`, then `closure_state = CLOSED`; otherwise `closure_state = OPEN`.
- decisive contradiction that remains unresolved must prevent proof closure.
- stale decisive support caused by late data or authority correction must retain `staleness_dependency_refs[]`.
- `primary_path_ref` must always resolve to one assessed current target path inside the decisive set.
- `rejected_path_refs[]`, rejected entries, and replay recipe order must preserve the same deterministic primary-plus-rejected order.
- historically queryable stale or superseded bundles must not remain controlling proof.
- silent limitation ambiguity must surface as explicit unsupported or open-closure posture rather than optimistic support.
- replay reconstruction must reproduce the same ordering and hash from persisted inputs.

Backend, performance, and security requirements:
- Reuse the persisted graph topology; do not rebuild graph edges ad hoc inside proof selection.
- Keep ranking and closure math deterministic and side-effect free until persistence.
- Persist only bounded render refs and replay recipes; do not duplicate full sensitive evidence payloads inside the bundle.
- Avoid path explosions by applying admissibility and target-purpose filtering before ranking all candidates.

Optional internal verification surface (only if you add one):
- If you add a read-only proof inspector, place it behind an internal route such as `/internal/provenance/proof/:proofBundleId`.
- The UI must feel like a premium legal-proof dossier:
  left rail for target identity and closure state,
  wide center column for the primary proof ladder,
  tabbed secondary path drawer for rejected materially distinct paths,
  right column for limitation notes, replay recipe, and historical / controlling posture.
- Show one restrained path-ranking strip that lists the selector criteria in order, with the winning criterion emphasized from persisted values only.
- Use the shared palette and typography contract; keep cards tall, sparse, and editorial rather than analytic.
- If this surface exists, implement and verify it with Playwright using semantic locators, keyboard path switching, reduced-motion checks, and trace capture.

Validation rules:
- Unit-test closure formula, selector ordering, rejected-path determinism, and lifecycle transitions.
- Integration-test graph -> target assessment -> proof bundle build, stale proof supersession, and replay reconstruction.
- Validate persisted artifacts against `proof_bundle.schema.json`.
- Re-run the contract validators after assembling the implementation bundle.

Definition of done:
- The repo can emit one authoritative `ProofBundle` per target purpose from a persisted graph.
- Primary and rejected paths remain deterministic, replay-safe, and historically queryable.
- Later filing, enquiry, and authority work can consume one decisive proof artifact rather than re-deriving proof closure locally.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
