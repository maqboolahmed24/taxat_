---
id: pc_0128
slug: phase_03_parallel_wave_01_track_backend_compute_008_implement_provenance_node_edge_and_path_graph_builder
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "008"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0128

## Task Summary

implement provenance node, edge, and path graph builder.

## Metadata

- ID: `pc_0128`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `008`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_008_implement_provenance_node_edge_and_path_graph_builder`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0127](./pc_0127.md) `phase_03_parallel_wave_01_track_backend_compute_007_implement_decision_bundle_assembly_and_terminal_outcome_finalization`
- Next: [pc_0129](./pc_0129.md) `phase_03_parallel_wave_01_track_backend_compute_009_implement_proof_bundle_generation_and_defensible_filing_graph_closure`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0126_to_0133.md](../shared_operating_contract_0126_to_0133.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_008_implement_provenance_node_edge_and_path_graph_builder`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0127` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0069`, `pc_0075`, `pc_0076`, `pc_0097`, `pc_0108`, `pc_0110` through `pc_0119`, `pc_0121`, `pc_0124`, `pc_0125`, `pc_0126`, and `pc_0127`.

Mission:
Implement the manifest-scoped provenance graph builder that persists `ProvenanceNode`, `ProvenanceEdge`, `ProvenancePath`, and the root `EvidenceGraph` topology needed by later proof, enquiry, twin, and filing-capable work.
This task must produce a directed labelled multigraph with stable graph addresses, typed lineage boundaries, traversable support paths, contradiction edges, replay-safe path identities, and one persisted graph root that later proof closure logic can consume without rebuilding topology ad hoc.

Source algorithm grounding you must honor:
- `provenance_graph_semantics.md`, especially the node families, activity families, support-path grammar, filing-field continuity rules, contradiction isolation, and replay-safe traversability rules.
- `defensible_filing_graph_contract.md`, especially admissible path grammar, target-level support posture prerequisites, graph integrity expectations, and the distinction between topology build and proof closure.
- `modules.md`, especially graph/proof/twin build placement, graph builder inputs, and the rule that decisive artifacts must have generating activities and support paths.
- `data_model.md`, especially `ProvenanceNode`, `ProvenanceEdge`, `ProvenancePath`, and `EvidenceGraph`.
- `state_machines.md`, especially `EvidenceGraph.lifecycle_state`.
- `audit_and_provenance.md` and `observability_and_audit_contract.md`.
- `PATCH_RESOLUTION_INDEX.md`, especially any fixes around path identity, node families, contradiction handling, or cross-manifest lineage.
- `test_vectors.md`, especially graph / proof continuity and replay / stale / contradiction vectors that topology must satisfy before closure.
- Existing schemas `schemas/provenance_node.schema.json`, `schemas/provenance_edge.schema.json`, and `schemas/provenance_path.schema.json` plus any `EvidenceGraph` schema / contract artifacts already imported.

Deliverables to create:
1. `packages/backend-provenance/src/models/provenance_node.ts`.
2. `packages/backend-provenance/src/models/provenance_edge.ts`.
3. `packages/backend-provenance/src/models/provenance_path.ts`.
4. `packages/backend-provenance/src/models/evidence_graph.ts`.
5. `packages/backend-provenance/src/repositories/provenance_node_repository.ts`.
6. `packages/backend-provenance/src/repositories/provenance_edge_repository.ts`.
7. `packages/backend-provenance/src/repositories/provenance_path_repository.ts`.
8. `packages/backend-provenance/src/repositories/evidence_graph_repository.ts`.
9. `packages/backend-provenance/src/services/derive_graph_address.ts`.
10. `packages/backend-provenance/src/services/build_lineage_boundaries.ts`.
11. `packages/backend-provenance/src/services/register_provenance_nodes.ts`.
12. `packages/backend-provenance/src/services/register_provenance_edges.ts`.
13. `packages/backend-provenance/src/services/enumerate_candidate_paths.ts`.
14. `packages/backend-provenance/src/services/build_evidence_graph.ts`.
15. `packages/backend-provenance/src/services/validate_graph_integrity.ts`.
16. `db/migrations/phase03_0128_provenance_graph_foundation.sql`.
17. `docs/provenance/provenance_node_edge_and_path_graph_builder.md`.
18. `tests/unit/backend_provenance/provenance_topology_contract.spec.ts`.
19. `tests/unit/backend_provenance/path_enumeration_and_ordering.spec.ts`.
20. `tests/integration/backend_provenance/evidence_graph_build_and_replay.spec.ts`.

Required design decisions you must make explicit:
- Define the graph identity contract:
  how `graph_id`, `graph_address`, `manifest_id`, `tenant_id`, `client_id`, business partition, and scope are derived and frozen.
- Define the node-family and activity-family registry as data, not hard-coded switch statements scattered across builders.
- Define the cross-manifest lineage rule for `from_manifest_id`, `to_manifest_id`, and lineage boundaries.
- Define path hashing and canonical path ordering.
  Path identities must be deterministic and independent of insertion order.
- Define how contradiction edges and stale / tombstone / limitation semantics are preserved on topology without prematurely deciding proof closure.
- Define the minimum graph root fields that must exist now so later `ProofBundle`, `TwinView`, and `DecisionBundle.graph_id` references can bind to one durable artifact.

Execution steps:
1. Implement schema-backed models and repositories for nodes, edges, paths, and the graph root.
2. Implement `derive_graph_address.ts` and `build_lineage_boundaries.ts` so graph identity is stable, replay-safe, and explicit about cross-manifest boundaries.
3. Implement node and edge registration services that materialize required entity families and generating activity families for decisive artifacts such as gate decisions, trust, evidence, twin artifacts, proof bundles, filing fields, and authority responses as they become available.
4. Implement `enumerate_candidate_paths.ts` so admissible support-path candidates are persisted with ordered node / edge refs, path role, path hash, closure posture, and replayability metadata.
5. Implement `build_evidence_graph.ts` so the root graph artifact binds topology refs, lineage boundaries, integrity summary inputs, and placeholder target-assessment hooks required by later proof closure work.
6. Implement `validate_graph_integrity.ts` so missing generating activity, missing support continuity, illegal cross-manifest jumps, or malformed contradiction topology fail closed before the graph becomes controlling.
7. Add tests for deterministic node/edge/path ordering, graph-address stability, contradiction edge persistence, cross-manifest lineage boundaries, and replay-safe path hashing.

Gap handling you must perform:
- Close the gap where `DecisionBundle`, `ProofBundle`, and `TwinView` all need a durable `graph_id` before a reusable graph root builder exists.
  Persist `EvidenceGraph` now as the authoritative topology root rather than leaving graph identity implicit.
- Close the gap where topology and proof closure could be fused into one opaque service.
  Keep topology build separate from target assessment / closure so later proof services can consume persisted graph truth.
- Close the gap where cross-manifest lineage could drift or silently flatten into one manifest-local graph.
  Preserve explicit lineage boundaries and per-edge manifest relation semantics.
- Close the gap where path identity could depend on repository insertion order.
  Canonicalize node and edge ordering before hashing / persistence.
- Close the gap where contradiction evidence might only survive in UI-level annotations.
  Persist contradiction relations as first-class edges and references.

Mandatory edge cases:
- every decisive artifact entering the graph must have an inbound generating activity or the build fails closed.
- a `GateDecisionRecord` must become an `EN_GATE_DECISION` node generated by `AC_EVALUATE_GATE`.
- cross-manifest lineage jumps must carry explicit `lineage_relation` and boundary annotations.
- contradictory decisive evidence must survive as explicit contradiction edges and cannot be collapsed into a summary boolean.
- path hashes must remain stable even when repository insertion order changes.
- stale or tombstoned segments must remain queryable and typed rather than deleted from topology.
- graph rebuild under late data or authority correction must be able to mark the older graph stale / rebuild-required without losing replayable history.

Backend, performance, and security requirements:
- Use bulk persistence and stable sorting so large graphs do not degrade into N+1 writes.
- Keep topology services free of renderer-specific fields.
- Never let client-provided graph paths or viewer ordering override canonical persisted order.
- Avoid logging full sensitive evidence payloads; log typed refs, path ids, node families, and integrity failures only.

Optional internal verification surface (only if you add one):
- If you add a read-only provenance explorer, place it behind an internal route such as `/internal/provenance/graph/:graphId`.
- The UI must be a premium graph atlas, not a generic node-link playground:
  a narrow left rail for target selection and path filters,
  a central vertical lineage canvas that renders one selected path as a stacked ladder,
  and a right rail for node / edge contracts, limitations, and cross-manifest boundaries.
- Show only one main path at a time by default; additional paths appear as layered tabs, not as a noisy all-at-once graph.
- Use the shared palette and typography contract with a slightly denser mono label scale for ids and lineage relations.
- Motion must be restrained to `160ms` pane transitions and focus highlights only, with full reduced-motion support.
- If this surface exists, implement and verify it with Playwright using semantic locators, keyboard path switching, reduced-motion assertions, and trace capture.

Validation rules:
- Unit-test graph address derivation, node / edge family registration, path hashing, and integrity validation.
- Integration-test graph build from a frozen manifest with compute / gate / trust artifacts and deterministic rebuild from the same inputs.
- Validate persisted node, edge, and path artifacts against their schemas.
- Re-run the contract validators after assembling the implementation bundle.

Definition of done:
- The repo can persist one authoritative manifest-scoped provenance topology that later proof, enquiry, twin, and filing stages can consume directly.
- Graph identity, path hashes, contradiction edges, and lineage boundaries are deterministic and replay-safe.
- Later proof closure work does not need to improvise topology or re-derive generating activity continuity.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
