---
id: pc_0228
slug: phase_04_parallel_wave_02_track_backend_release_resilience_010_implement_forensic_contract_guard_and_constraint_traceability_register_ci_gate
mode: parallel
phase: "04"
wave: "02"
track: "backend_release_resilience"
task_number: "010"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_release_resilience_0010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0228

## Task Summary

010 implement forensic contract guard and constraint traceability register ci gate.

## Metadata

- ID: `pc_0228`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend release resilience`
- Task Number: `010`
- Source Slug: `phase_04_parallel_wave_02_track_backend_release_resilience_010_implement_forensic_contract_guard_and_constraint_traceability_register_ci_gate`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0227](./pc_0227.md) `phase_04_parallel_wave_02_track_backend_release_resilience_009_implement_release_candidate_identity_and_promotion_evidence_query_surfaces`
- Next: [pc_0229](./pc_0229.md) `phase_05_parallel_wave_03_track_frontend_shared_001_Playwright_or_other_appropriate_tooling_create_shared_web_frontend_workspace_and_route_contract_scaffold`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0222_to_0229.md](../shared_operating_contract_0222_to_0229.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_release_resilience_010_implement_forensic_contract_guard_and_constraint_traceability_register_ci_gate`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0227` where relevant, especially `pc_0059`, `pc_0060`, and `pc_0222` through `pc_0227`.

Mission:
Turn the live contract validator, forensic guard, and live constraint traceability register into a merge-blocking CI gate with coherent local developer entrypoints and documentation.
This task is not allowed to leave coherence enforcement as a manual reviewer checklist.

Source algorithm grounding you must honor:
- `architecture_coherence_guardrails.md`, especially sections 4 and 5.
- `constraint_coverage_index.md`, end to end.
- `constraint_traceability_register.json` and `schemas/constraint_traceability_register.schema.json`.
- `contract_integrity_requirements.md`.
- `README.md`, especially validation and reference-document roles.
- `implementation_conventions.md` where schema/prose synchronization rules live.
- `AUDIT_FINDINGS.md` and `PATCH_RESOLUTION_INDEX.md` only as forensic-history companions, never as the live register.
- `scripts/validate_contracts.py` and `tools/forensic_contract_guard.py`, especially the live-register checks and README/path checks.

Deliverables to create:
1. `.github/workflows/contract-integrity.yml` or the ADR-approved CI workflow file
2. `scripts/ci/run_contract_integrity_gate.sh`
3. root `package.json` script additions for contract-integrity verification
4. `docs/repository/contract_integrity_and_traceability_ci_gate.md`
5. `tests/ci/contract_integrity_gate.spec.ts` or the ADR-approved equivalent smoke test for the local entrypoint
6. patch `README.md` validation / reference sections if needed so the guard’s documented entrypoint matches the real runnable path

You MUST patch any existing code path or documentation that currently:
- references the wrong forensic guard path,
- allows the live constraint register and human index to drift,
- or treats historical forensic notes as if they were the live machine register.

Required design decisions you must make explicitly:
- define when the CI gate runs (PR, main, release branches) and what is blocking versus advisory
- define the local developer entrypoint and whether it wraps both validator self-test and forensic guard in one command
- define how CI surfaces failures as actionable annotations rather than opaque log dumps
- define how same-change-set coherence is enforced when live constraint rows change
- define how Python runtime setup is handled so the gate is deterministic on clean checkout

Execution requirements:
- wire `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py` into one deterministic local and CI entrypoint
- fail closed if `constraint_traceability_register.json` is unordered, duplicates ids, omits required traceability spine fields, omits required validator/forensic refs, or drifts from `constraint_coverage_index.md`
- fail closed if `README.md` names the wrong validator/guard entrypoint or drifts from the live validation commands the guard expects
- keep historical findings and patch history out of the live register while still preserving them in the forensic documents
- make the CI job easy to run locally so contributors can fix drift before opening a PR

Gap handling you must close while implementing:
- close the gap where validator and forensic guard are runnable but not CI-blocking
- close the gap where the machine register, human index, and downstream refs can drift independently
- close the gap where README validation instructions can silently point at a non-runnable path
- close the gap where live constraint rows can accumulate stale-defect language or incomplete traceability spines

Mandatory edge cases:
- duplicate or out-of-order `constraint_id` rows must fail closed
- missing `scripts/validate_contracts.py` or `tools/forensic_contract_guard.py` enforcement refs must fail closed
- missing required terms in any traced path must fail closed
- stale-defect phrasing in a live constraint name must fail closed
- README references to the nonexistent bare `tools/forensic_contract_guard.py` path must fail closed
- partial same-change-set updates where the live register changes but the coverage index does not must fail closed

Playwright and contract-test requirements:
- Playwright is not required for the CI gate itself
- if you introduce any browser-visible internal integrity viewer (not recommended for this task), it must stay read-only and semantic-locator driven, with Playwright coverage and trace capture
- otherwise focus on shell/unit/CI smoke coverage around the contract-integrity entrypoint

Backend, performance, and security requirements:
- the CI gate must be deterministic and private by default
- do not fetch live internet resources during the gate; operate on repo truth only
- keep failure output concise but actionable; surface the exact broken path or rule
- avoid parallel scripts that duplicate validator or forensic-guard semantics in a drift-prone wrapper

Validation rules:
- the implementation itself must pass `python3 Algorithm/scripts/validate_contracts.py --self-test`
- the implementation itself must pass `python3 Algorithm/tools/forensic_contract_guard.py`
- verify the local wrapper and CI workflow call the same underlying commands

Definition of done:
- contract/coherence drift blocks merge through one deterministic local and CI path
- the live traceability register, human coverage index, README validation instructions, validator, and forensic guard remain synchronized
- contributors have one clear local command that reproduces the CI result
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, the relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.