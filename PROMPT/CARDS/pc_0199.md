---
id: pc_0199
slug: phase_04_parallel_wave_02_track_backend_recovery_001_implement_recovery_checkpoint_model_and_tiered_checkpoint_open_close_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_recovery"
task_number: "001"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_recovery_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0199

## Task Summary

001 implement recovery checkpoint model and tiered checkpoint open close rules.

## Metadata

- ID: `pc_0199`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend recovery`
- Task Number: `001`
- Source Slug: `phase_04_parallel_wave_02_track_backend_recovery_001_implement_recovery_checkpoint_model_and_tiered_checkpoint_open_close_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0198](./pc_0198.md) `phase_04_parallel_wave_02_track_backend_governance_010_implement_gate_decision_explainability_projection_and_reason_code_compression`
- Next: [pc_0200](./pc_0200.md) `phase_04_parallel_wave_02_track_backend_recovery_002_implement_restore_privacy_reconciliation_contract_and_post_restore_limitation_pass`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0198_to_0205.md](../shared_operating_contract_0198_to_0205.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_recovery_001_implement_recovery_checkpoint_model_and_tiered_checkpoint_open_close_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0198` if they exist, especially `pc_0050`, `pc_0051`, `pc_0052`, `pc_0060`, `pc_0097`, `pc_0107`, `pc_0156`, and `pc_0157`.

Mission:
Implement the authoritative `RecoveryCheckpoint` model, repository, transition service, reopen-readiness classifier, and shared recovery-governance contract builder.
This task is not allowed to treat restoreability as dashboard metadata.
It must make checkpoint legality machine-bound so the platform can prove when a control-plane restore is merely inventoried, when it is verified, when it is quarantined, and when reopen is still blocked.

Source algorithm grounding you must honor:
- `recovery_tier_checkpoint_and_fail_forward_governance_contract.md`, end to end.
- `deployment_and_resilience_contract.md`, especially checkpoint cadence, inventory linkage, and restore-drill evidence posture.
- `verification_and_release_gates.md`, especially where restore drills and checkpoints become release evidence.
- `data_model.md`, especially `RecoveryCheckpoint` and the shared `recovery_governance_contract` boundary.
- `state_machines.md`, especially section `F. RecoveryCheckpoint.checkpoint_state` and its allowed transitions.
- `audit_and_provenance.md` and `observability_and_audit_contract.md` where restore evidence and audit continuity remain append-only.
- `schemas/recovery_checkpoint.schema.json`, `schemas/restore_privacy_reconciliation_contract.schema.json`, `schemas/state_transition_contract.schema.json`, and `schemas/sample_recovery_checkpoint.json`.
- `scripts/validate_contracts.py`, especially `state_transition_contract_recovery_checkpoint_illegal_tuple` and `recovery_checkpoint_restore_evidence_and_privacy_posture`.
- `PATCH_RESOLUTION_INDEX.md`, especially any fixes around restore drill lineage, privacy outcome refs, reopen readiness, and quarantine evidence.
- `test_vectors.md`, especially the checkpoint and restore-drill vectors around verified and blocked reopen posture.

Deliverables to create:
1. `packages/backend-recovery/src/models/recovery_checkpoint.ts`
2. `packages/backend-recovery/src/repositories/recovery_checkpoint_repository.ts`
3. `packages/backend-recovery/src/services/build_recovery_governance_contract.ts`
4. `packages/backend-recovery/src/services/register_recovery_checkpoint.ts`
5. `packages/backend-recovery/src/services/transition_recovery_checkpoint.ts`
6. `packages/backend-recovery/src/services/compute_reopen_readiness_state.ts`
7. `packages/backend-recovery/src/services/bind_restore_drill_evidence.ts`
8. `packages/backend-recovery/src/services/quarantine_recovery_checkpoint.ts`
9. `packages/backend-recovery/src/tests/recovery_checkpoint_state_machine.spec.ts`
10. `packages/backend-recovery/src/tests/recovery_checkpoint_reopen_readiness.spec.ts`
11. `docs/recovery/recovery_checkpoint_model_and_tiered_open_close_rules.md`
12. Optional but recommended: `apps/admin-console-web/src/routes/debug/recovery/checkpoint-atlas.tsx`

Required design decisions you must make explicitly:
- define the canonical mapping from `protected_workload_class` to `recovery_tier_class`, `rpo_class`, and `rto_class`, and fail closed when a weaker tier is attempted
- define the exact event-to-state transition mapping for `snapshot_complete`, `restore_drill_passed`, `restore_drill_failed`, `privacy_reconciliation_failed`, `remediation_and_redrill_passed`, and `retention_elapsed`
- define how `reopen_readiness_state` is derived and which blocker codes win when more than one blocker is present
- define how bound restore-drill evidence is stored so `restore_drill_ref`, `restore_verification_hash`, and `snapshot_time` cannot drift apart
- define the quarantine policy and the exact `quarantine_reason_code` vocabulary you need without weakening the schema
- define how expired checkpoints remain ledger-visible even after underlying backup artifacts age out

Execution requirements:
- implement `RecoveryCheckpoint` exactly as a schema-backed control object, not a loose DTO
- preserve the state machine exactly:
  `REQUESTED -> CREATED -> VERIFIED / QUARANTINED -> EXPIRED`
  with only the allowed event transitions from the source corpus
- preserve one bound `recovery_governance_contract` and one bound `privacy_reconciliation_contract` on every checkpoint row
- compute `reopen_readiness_state` explicitly and persist it on every write
- enforce that `checkpoint_state = VERIFIED` is lawful only when:
  restore evidence is bound,
  privacy reconciliation is complete,
  audit continuity is verified,
  queue rebuild is verified,
  authority rebuild is verified,
  authority binding revalidation is verified,
  replay-safe and enquiry-safe limitation posture is verified,
  and `reopen_readiness_state = READY_FOR_REOPEN`
- preserve `QUARANTINED` checkpoints as inventory-visible, typed failure objects; do not collapse them into staleness
- preserve `EXPIRED` checkpoints as ledger-visible historical entries

Gap handling you must close while implementing:
- close any gap where checkpoint posture is represented only in prose or deployment notes instead of one schema-valid model
- close any gap where verified restore evidence can exist without a bound privacy reconciliation contract
- close any gap where reopen safety is inferred from booleans without one authoritative typed blocker field
- close any gap where failed drills lose the exact failing basis hash or restore evidence lineage
- close any gap where weaker workload tiers can be serialized for control-plane truth stores

Mandatory edge cases:
- `restore_tested_at` must not predate `snapshot_time`
- `checkpoint_state = VERIFIED` with any missing verification boolean must fail closed
- `checkpoint_state = VERIFIED` with non-final restore privacy posture must fail closed
- `checkpoint_state = QUARANTINED` must preserve failing drill or privacy evidence rather than nulling it out
- `checkpoint_state = EXPIRED` must not erase inventory linkage or audit visibility
- retention-elapsed transitions may occur from `CREATED`, `VERIFIED`, or `QUARANTINED` only
- reopen blockers for legal hold, proof preservation, authority ambiguity, compensating re-erasure, limitation reconciliation, audit continuity, queue rebuild, and authority revalidation must remain distinguishable

Playwright and contract-test requirements:
- if you expose or patch a northbound checkpoint route, add Playwright `APIRequestContext` coverage for state, bound restore evidence, reopen blocker posture, and typed failures
- add tests that prove the exact state machine rejects illegal tuples and rejects premature verified posture
- if you add the optional atlas, add Playwright browser tests for lifecycle spine order, blocker ladder visibility, inventory visibility for quarantined/expired checkpoints, keyboard focus order, and reduced-motion-safe expansion using semantic locators only
- capture payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a quiet three-zone layout: checkpoint identity and tier rail, dominant lifecycle spine, and narrow evidence/blocker sidecar
- max width `1400px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717` / `#667085`; continuity accent `#0F766E`; caution `#B7791F`; danger `#C2410C`
- render the lifecycle as a vertical spine with exact states in canonical order and the active state visibly pinned
- render reopen blockers as an ordered ladder with one row per blocker family; no generic health badges
- if you show a diagram, show one checkpoint lifecycle spine or one evidence-binding ribbon; no KPI tiles or pie charts
- animate only opacity / translate / height in `140ms`–`180ms`; reduced motion must preserve meaning without ornament

Backend, performance, and security requirements:
- keep checkpoint writes append-only/audit-friendly around state transitions
- do not couple reopen legality to deployment UI state or transient dashboard refresh timing
- avoid logging full backup metadata or restored data content; log ids, states, hashes, and typed blockers only
- index checkpoint queries by `datastore_ref`, `snapshot_time`, and `checkpoint_state`
- fail closed on ambiguous workload class, missing restore evidence, or mismatched privacy contract lineage

Validation rules:
- validate every emitted checkpoint against `recovery_checkpoint.schema.json`
- validate the bound privacy contract before a checkpoint may transition to `VERIFIED`
- add regression coverage for the named validator cases above
- keep this task limited to checkpoint model and open/close law; do not swallow later nightly/autopilot scheduling work

Definition of done:
- the repo has one authoritative `RecoveryCheckpoint` model and state machine
- restoreability, quarantine, and reopen blocker posture are now machine-bound instead of dashboard-inferred
- later restore-privacy, replay, and release-evidence tasks can bind to checkpoints without ambiguity
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
