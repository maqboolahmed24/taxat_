---
id: pc_0109
slug: phase_03_parallel_wave_01_track_backend_collection_001_implement_source_plan_source_window_and_collection_boundary_models
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "001"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0109

## Task Summary

implement source plan source window and collection boundary models.

## Metadata

- ID: `pc_0109`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `001`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_001_implement_source_plan_source_window_and_collection_boundary_models`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0108](./pc_0108.md) `phase_03_parallel_wave_01_track_backend_manifest_012_implement_manifest_reuse_recovery_replay_and_supersession_decision_service`
- Next: [pc_0110](./pc_0110.md) `phase_03_parallel_wave_01_track_backend_collection_002_implement_source_collection_run_orchestrator_and_fetch_audit_lineage`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0102_to_0109.md](../shared_operating_contract_0102_to_0109.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_001_implement_source_plan_source_window_and_collection_boundary_models`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0108` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0028`, `pc_0031`, `pc_0046`, `pc_0048`, `pc_0050`, `pc_0051`, `pc_0052`, `pc_0053`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0069`, `pc_0070`, `pc_0072`, `pc_0073`, `pc_0074`, `pc_0076`, `pc_0080`, `pc_0085`, and `pc_0097` through `pc_0108`.

Mission:
Implement the first durable collection-boundary control objects: `SourcePlan`, `SourceWindow`, and `CollectionBoundary`.
This task does not yet fetch data from providers.
It establishes the machine-readable objects that declare what sources are in scope, when legal reads occurred, and how every planned domain ended at cutoff.

Source algorithm grounding you must honor:
- `modules.md`, especially `PLAN_SOURCE_COLLECTION(...)`, `MATERIALIZE_SOURCE_WINDOW(...)`, `FREEZE_COLLECTION_BOUNDARY(...)`, and the collection-to-input-freeze boundary rules.
- `input_boundary_and_cutoff_contract.md`, especially hard read cutoff, post-cutoff late-data-only posture, and the requirement that every planned domain land in exactly one boundary disposition.
- `manifest_and_config_freeze_contract.md`, especially `InputFreeze` references to `source_plan_ref`, `source_window_ref`, `collection_boundary_ref`, explicit missing/stale/exclusion declarations, and late-data bindings.
- `canonical_source_and_evidence_taxonomy.md`, especially source-domain vocabulary, provider/environment identity, and evidence-class consistency.
- `late_data_policy_contract.md`, especially how post-cutoff observations are represented separately from active boundary truth.
- `data_model.md`, especially `SourcePlan`, `SourceWindow`, `CollectionBoundary`, and their field-level semantics.
- Existing schemas including `source_plan.schema.json`, `source_window.schema.json`, `collection_boundary.schema.json`, `input_freeze.schema.json`, and any sample fixtures tied to source boundaries.

Deliverables to create:
1. `packages/backend-collection/src/models/source_plan.ts`.
2. `packages/backend-collection/src/models/source_window.ts`.
3. `packages/backend-collection/src/models/collection_boundary.ts`.
4. `packages/backend-collection/src/repositories/source_plan_repository.ts`.
5. `packages/backend-collection/src/repositories/source_window_repository.ts`.
6. `packages/backend-collection/src/repositories/collection_boundary_repository.ts`.
7. `packages/backend-collection/src/services/source_plan_factory.ts`.
8. `packages/backend-collection/src/services/source_window_factory.ts`.
9. `packages/backend-collection/src/services/collection_boundary_factory.ts`.
10. `packages/backend-collection/src/services/plan_coverage_validator.ts`.
11. `packages/backend-collection/src/services/boundary_disposition_validator.ts`.
12. `db/migrations/phase03_0109_source_plan_window_boundary.sql`.
13. `docs/collection/source_plan_window_and_boundary_models.md`.
14. `tests/unit/backend_collection/source_plan_window_boundary_models.spec.ts`.
15. `tests/integration/backend_collection/source_plan_window_boundary_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the physical storage strategy for planned sources, required domains, per-domain source boundaries, audit refs, cursor/checkpoint refs, and provider/env/schema version metadata.
- Define exact canonical ordering and uniqueness rules for `required_domains[]`, `planned_sources[]`, and `source_boundaries[]`.
- Define how `boundary_coverage_state` is computed and validated.
- Define how `collection_started_at`, `collection_completed_at`, and `read_cutoff_at` are validated and persisted.
- Define how `connector_profile_ref`, `connector_build_id`, provider environment refs, API version refs, schema version refs, and request/page audit refs are carried without later normalization drift.
- Define how later input-freeze code will consume these models without needing to reinterpret omission as “no data”.

Execution steps:
1. Create typed wrappers for `SourcePlan`, `SourceWindow`, and `CollectionBoundary` using generated bindings where available.
2. Implement repository and migration support for the three control objects with explicit child-table or typed-JSON strategy that preserves one authoritative aggregate shape.
3. Implement `source_plan_factory.ts` and `plan_coverage_validator.ts` so every required domain is covered by at least one planned source and no planned source falls outside the required domain set.
4. Implement `source_window_factory.ts` so collection start/completion/cutoff times are validated and the window enforces `HARD_CLOSED_AT_READ_CUTOFF` and `LATE_DATA_ONLY`.
5. Implement `collection_boundary_factory.ts` and `boundary_disposition_validator.ts` so every planned domain ends in exactly one explicit disposition and provider/version/audit metadata are persisted with the boundary.
6. Add tests for valid plan/window/boundary creation, required-domain coverage failures, ambiguous or conflicting boundary dispositions, omitted planned domains, and invalid timestamp ordering.
7. Ensure the factories are designed so later collection/fetch cards can extend them without rewriting the canonical model shape.

Gap handling you must perform:
- Close the gap where source-plan, source-window, and collection-boundary semantics exist only as schema truth and prose but not as durable models and repositories.
- Close the gap where omission could be mistaken for “no data confirmed”.
- Close the gap where cutoff timing could remain implicit rather than explicit machine state.
- Close the gap where provider/build/version lineage for collection boundaries could drift or disappear before input freeze.
- Close the gap where later late-data handling might mutate active collection boundaries instead of using explicit late-data posture.

Mandatory edge cases:
- any `required_domains[]` entry lacking a covering planned source must fail validation.
- any planned source whose `source_domain` is outside `required_domains[]` must fail validation.
- duplicate or ambiguous planned sources for the same domain and partition must be handled explicitly; do not allow silent ambiguous coverage.
- `collection_started_at > collection_completed_at` must fail.
- `collection_completed_at > read_cutoff_at` must fail.
- `cutoff_enforcement_state` must be `HARD_CLOSED_AT_READ_CUTOFF`.
- `post_cutoff_observation_mode` must be `LATE_DATA_ONLY`.
- every planned domain must land in exactly one disposition from `IN_SCOPE_COLLECTED`, `NO_DATA_CONFIRMED_AT_CUTOFF`, `EXCLUDED_BY_POLICY`, `MISSING_AT_CUTOFF`, or `STALE_AT_CUTOFF`.
- omission must never be interpreted as `NO_DATA_CONFIRMED_AT_CUTOFF`.
- post-cutoff observations must not mutate the active collection boundary; they belong to later late-data pathways.

Backend, performance, and security requirements:
- Keep the model and repository layer deterministic and strongly typed.
- Preserve append-only or audit-friendly lineage for request/page audit refs and provider/build/version metadata.
- Never log raw provider credentials or raw evidence payloads in validation failures.
- Make repository APIs efficient for lookup by source-plan ref, window ref, and manifest/input-freeze linkage that later cards will need.
- Keep factories provider-neutral so connector-specific later work plugs into them without changing the control-object semantics.

Validation rules:
- Unit-test coverage validation, timestamp ordering, disposition exactness, and canonical ordering rules.
- Integration-test migration application and round-trip persistence for all three control objects.
- Validate stored payloads against the imported schemas.
- Prove that omission cannot pass as no-data-confirmed and that every planned domain gets exactly one explicit disposition.
- Prove that post-cutoff timestamps cannot be recorded as active-window completion state.

Definition of done:
- The repo has durable `SourcePlan`, `SourceWindow`, and `CollectionBoundary` control objects with repositories and validators.
- Later collection and input-freeze tasks can build on explicit source-plan and cutoff truth instead of connector-local guesses.
- The collection boundary becomes a governed, auditable artifact rather than an implicit side effect of fetch code.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
