---
id: pc_0117
slug: phase_03_parallel_wave_01_track_backend_collection_009_implement_late_data_indicators_bindings_and_input_freeze_persistence
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "009"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0117

## Task Summary

implement late data indicators bindings and input freeze persistence.

## Metadata

- ID: `pc_0117`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `009`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_009_implement_late_data_indicators_bindings_and_input_freeze_persistence`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0116](./pc_0116.md) `phase_03_parallel_wave_01_track_backend_collection_008_implement_canonical_fact_promotion_and_partition_isolation_pipeline`
- Next: [pc_0118](./pc_0118.md) `phase_03_parallel_wave_01_track_backend_collection_010_implement_snapshot_assembly_and_snapshot_lifecycle_transitions`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0110_to_0117.md](../shared_operating_contract_0110_to_0117.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_009_implement_late_data_indicators_bindings_and_input_freeze_persistence`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0116` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0074`, `pc_0076`, `pc_0080`, `pc_0097`, `pc_0099`, `pc_0101`, `pc_0109`, `pc_0113`, `pc_0114`, `pc_0115`, and `pc_0116`.

Mission:
Implement the late-data binding and indicator layer plus the final `InputFreeze` persistence service that freezes the authoritative pre-seal intake population for downstream computation.
This task must project ordered `LateDataPolicyBinding[]`, detect and persist explicit late-data indicators (and any immediate finding/monitor artifacts required by the core engine), assemble per-domain intake posture, and persist a schema-valid `InputFreeze` with exact refs, hashes, counts, and declarations.
It must never permit silent adoption of post-cutoff data.

Source algorithm grounding you must honor:
- `modules.md`, especially `LATE_DATA_INDICATORS(...)`, `COLLECTION_LATE_DATA_BINDINGS(...)`, and `FREEZE_INPUT_SET(...)`.
- `core_engine.md`, especially `CLASSIFY_LATE_DATA(collection_boundary, runtime_scope)`, the requirement to persist a `LateDataIndicatorSet`, `LateDataFinding` artifacts, and a `LateDataMonitorResult` before consequences are applied, and later `COLLECTION_LATE_DATA_BINDINGS(collection_boundary, runtime_scope)` consumption.
- `input_boundary_and_cutoff_contract.md`, especially explicit per-domain accounting and `FROZEN_INPUT_ONLY` posture.
- `late_data_policy_contract.md`, especially ordered bindings, persisted indicator/finding/monitor artifacts, temporal classification, and trust/proof/amendment invalidation posture.
- `data_model.md`, especially `LateDataPolicyBinding`, `LateDataIndicator`, `LateDataIndicatorSet`, `LateDataFinding`, `LateDataMonitorResult`, and `InputFreeze`.
- `exact_gate_logic_and_decision_tables.md`, especially `INPUT_BOUNDARY_GATE` and filing-gate late-data handoff.
- Existing schemas including `late_data_policy_binding.schema.json`, `late_data_indicator.schema.json`, `late_data_indicator_set.schema.json`, `late_data_finding.schema.json`, `late_data_monitor_result.schema.json`, `late_data_temporal_contract.schema.json`, `late_data_consequence_summary.schema.json`, and `input_freeze.schema.json`.

Deliverables to create:
1. `packages/backend-collection/src/models/input_freeze.ts`.
2. `packages/backend-collection/src/repositories/input_freeze_repository.ts`.
3. `packages/backend-collection/src/models/late_data_indicator.ts`.
4. `packages/backend-collection/src/models/late_data_indicator_set.ts`.
5. `packages/backend-collection/src/models/late_data_finding.ts`.
6. `packages/backend-collection/src/models/late_data_monitor_result.ts`.
7. `packages/backend-collection/src/repositories/late_data_indicator_set_repository.ts`.
8. `packages/backend-collection/src/repositories/late_data_finding_repository.ts`.
9. `packages/backend-collection/src/repositories/late_data_monitor_result_repository.ts`.
10. `packages/backend-collection/src/services/collection_late_data_bindings.ts`.
11. `packages/backend-collection/src/services/late_data_indicator_builder.ts`.
12. `packages/backend-collection/src/services/classify_collection_late_data.ts`.
13. `packages/backend-collection/src/services/source_domain_posture_builder.ts`.
14. `packages/backend-collection/src/services/freeze_input_set.ts`.
15. `packages/backend-collection/src/services/input_set_hash_builder.ts`.
16. `docs/collection/late_data_bindings_and_input_freeze_persistence.md`.
17. `tests/unit/backend_collection/late_data_bindings_and_input_freeze.spec.ts`.
18. `tests/integration/backend_collection/input_freeze_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the precedence order for late-data bindings across domain-wide, source-class, partition-scoped, and runtime-scoped matches.
- Define the exact mapping from collection-boundary drift signals to `LateDataIndicator.indicator_type`, `detection_basis`, severity, and temporal-classification inputs.
- Define whether and how collection-time classification persists `LateDataFinding` / `LateDataMonitorResult` immediately, given the core engine’s requirement, while still allowing later post-seal monitoring to reuse the same primitives.
- Define the canonical `InputFreeze.input_set_hash` preimage and ordering rules.
- Define how `source_domain_postures[]` are assembled from boundary disposition, declarations, bindings, and artifact counts without double counting.
- Define how final `InputFreeze` assembly receives `artifact_contract_refs[]` / `artifact_contract_hash` without weakening schema rules even though generic set-wrapping and artifact-contract tasks land next in the roadmap.

Execution steps:
1. Implement the schema-backed `InputFreeze`, `LateDataIndicator`, `LateDataIndicatorSet`, `LateDataFinding`, and `LateDataMonitorResult` models plus repositories.
2. Implement `collection_late_data_bindings.ts` so ordered bindings are projected from the frozen collection boundary and current runtime scope with deterministic precedence and unique binding ids.
3. Implement `late_data_indicator_builder.ts` and `classify_collection_late_data.ts` so post-cutoff records, cursor advancement, revision advancement, schema-version advancement, and freshness SLO breaches become explicit indicators and policy-resolved outcomes.
4. Implement `source_domain_posture_builder.ts` so every required domain acquires one explicit posture with per-artifact counts.
5. Implement `freeze_input_set.ts` and `input_set_hash_builder.ts` so the final `InputFreeze` can be assembled once all required artifacts and contract refs are available; do not permit partial runtime persistence that violates the schema.
6. Add tests for zero-late-data posture, excluded-only late data, review-required late data, child-manifest-required late data, disjoint source-domain posture, and fully populated `InputFreeze` persistence.

Gap handling you must perform:
- Close the gap where the core engine requires immediate persistence of `LateDataIndicatorSet`, `LateDataFinding`, and `LateDataMonitorResult`, but the roadmap title foregrounds only indicators/bindings.
  Implement the minimum monitor/finding persistence needed by the algorithm rather than leaving the core-engine requirement unimplemented.
- Close the gap where the roadmap orders `InputFreeze` before snapshot/set-wrapping/artifact-contract tasks even though the schema requires `artifact_contract_refs[]` and `artifact_contract_hash`.
  Build a final assembler with explicit dependency injection; use fixture refs in tests if needed, but never persist a weakened schema shape in runtime.
- Close the gap where `no_data_confirmed_declarations[]` are required by `InputFreeze` but the module list does not name a helper for them.
  Consume the declaration artifact family from `pc_0113` and keep confirmed-empty posture distinct.
- Close the gap where collection-time late-data classification and later post-seal monitoring could diverge.
  Reuse one shared binding/indicator/temporal-classification primitive layer.

Mandatory edge cases:
- identical inputs must produce stable binding order, indicator ids, set hashes, and `input_set_hash`.
- a run with no late data must still be able to persist a `LateDataIndicatorSet` / monitor posture consistent with `NO_LATE_DATA` if the core engine path requires it.
- binding precedence ties must fail closed; do not silently pick one ambiguous policy.
- every required domain must appear in exactly one `source_domain_postures[]` entry.
- `InputFreeze` counts/frontier fields must cohere: zero open conflicts => `CLEAR`; open/non-blocking => `MONITORING_ONLY`; blocking => `BLOCKING_PRESENT`.
- no domain may vanish from `InputFreeze` without either collected posture or an explicit declaration.
- post-cutoff observations must not be adopted into active intake; they must stay late-data artifacts only.
- compliance-capable runtime persistence must fail closed if required contract refs/hashes are absent.

Backend, performance, and security requirements:
- Keep late-data and input-freeze assembly deterministic, replay-safe, and strongly typed.
- Do not recalculate active intake from fresh connector state during or after freeze.
- Keep temporal-classification logic auditable and reason-coded; do not hide legal-effect basis in free-form comments.
- Never log raw provider payloads, secrets, or sensitive client data inside late-data detection.
- Use append-only persistence for indicator/finding/monitor artifacts so later replay/amendment logic can audit them.

Validation rules:
- Unit-test binding precedence, indicator-type mapping, temporal-classification invariants, and input-set hash stability.
- Unit-test `source_domain_postures[]` exactness and disjointness.
- Integration-test full `InputFreeze` persistence with fixture contract refs, plus monitor persistence for no-late-data and review-required cases.
- Validate late-data artifacts and `InputFreeze` against their schemas.
- Prove that no post-cutoff data can silently enter active intake and that downstream freeze consumers can rely on one exact frozen input basis.

Definition of done:
- The repo can persist ordered late-data bindings, explicit late-data artifacts, and a schema-valid `InputFreeze`.
- Active intake is now frozen in one governed object instead of being reconstructed from live connector state.
- Later snapshot, contract-gate, compute, filing, amendment, and replay work can consume a deterministic frozen intake basis.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
