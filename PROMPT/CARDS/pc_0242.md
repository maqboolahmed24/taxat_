---
id: pc_0242
slug: phase_05_parallel_wave_03_track_frontend_low_noise_002_Playwright_or_other_appropriate_tooling_implement_context_bar_state_rendering_and_status_badges
mode: parallel
phase: "05"
wave: "03"
track: "frontend_low_noise"
task_number: "002"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_low_noise_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0242

## Task Summary

002 Playwright or other appropriate tooling implement context bar state rendering and status badges.

## Metadata

- ID: `pc_0242`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend low noise`
- Task Number: `002`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_low_noise_002_Playwright_or_other_appropriate_tooling_implement_context_bar_state_rendering_and_status_badges`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0241](./pc_0241.md) `phase_05_parallel_wave_03_track_frontend_low_noise_001_Playwright_or_other_appropriate_tooling_implement_low_noise_experience_shell_layout_and_route_mounting`
- Next: [pc_0243](./pc_0243.md) `phase_05_parallel_wave_03_track_frontend_low_noise_003_Playwright_or_other_appropriate_tooling_implement_decision_summary_state_rendering_and_limitation_notices`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_low_noise_002_Playwright_or_other_appropriate_tooling_implement_context_bar_state_rendering_and_status_badges`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0241` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement `ContextBarState` rendering and status badges for identity, scope, phase, freshness, truth origin, ownership, mode, limitation, and connection posture in the low-noise shell.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The context bar is the calm shell’s single place for stable identity, freshness, owner handoff, mode, limitation, and reconnect status. The algorithm explicitly forbids duplicating recovery/mode warnings across persistent surfaces. Therefore the context bar must compress status semantics into one readable band while preserving full-text disclosure and semantic anchors.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md default visible shell, shell continuity/recovery, masking/limits, projection lag, reconnect, analysis-only posture`.
- `frontend_shell_and_interaction_law.md state/freshness/recovery and low-noise topology`.
- `schemas/context_bar_state.schema.json and low_noise_experience_frame.schema.json`.
- `semantic_selector_and_accessibility_contract.md conditional notice anchors and live regions`.

Deliverables to create or patch:
1. `packages/shared-ui/src/low-noise/ContextBar.tsx`
2. `packages/shared-ui/src/low-noise/ContextBadge.tsx`
3. `packages/frontend-shell-core/src/low-noise/context_bar_presenter.ts`
4. `packages/frontend-shell-core/src/low-noise/context_bar_copy.ts`
5. `tests/unit/frontend-shell-core/context_bar_presenter.spec.ts`
6. `tests/playwright/frontend/low_noise_context_bar_status_badges.spec.ts`
7. `docs/frontend/context_bar_state_rendering_and_status_badges.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Render the context bar as a slim, premium identity rail: manifest/case label, period/scope, phase, owner, freshness, truth origin, mode, and one limitation/recovery chip. Use compact rounded chips, 12–14px type, mono for refs, blue for fresh/live, amber for pending/stale/catching-up, red only for degraded/blocking. On wide screens it can be a left rail header; on compact screens it becomes the first stacked card.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- map every required `ContextBarState` field to visible text, semantic anchor, and full-text disclosure ref
- show at most one promoted limitation/recovery/mode narrative in the context bar while preserving collapsed additional context in full-text disclosure
- bind connection states `CONNECTED`, `RECONNECTING`, `CATCHING_UP`, `STALE`, `DEGRADED` to text+shape+aria labels, never color alone
- show `truth_origin` and owner handoff posture distinctly so authority-pending/out-of-band truth cannot look like internal completion
- drive live-region announcements only for meaningful freshness/recovery changes

Functional, architectural, and edge-case gaps to close:
- close the gap where identity/status metadata is scattered across several components
- close the gap where stale/recovery mode banners duplicate across context, summary, and action surfaces
- close the gap where truth origin and connection state are represented only as colors
- close the gap where limitation copy lacks a stable full-text path

Mandatory edge cases:
- masked/limited mode must state limited visibility without implying data absence
- analysis-only/replay-only/read-only modes must be visible once and reflected in action legality
- owner waiting on customer/authority/review/approval must not compete with dominant action copy
- catch-up must keep mounted context and not trigger full shell loader

Playwright, accessibility, and validation requirements:
- unit-test copy mapping for every enum and limitation/mode combination
- Playwright-test badge rendering, live-region announcement, keyboard focus, reduced-motion, and compact restack
- assert only one context-level limitation/recovery chip is promoted at a time
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- context bar compresses identity/currentness truth into one stable surface
- badge semantics are accessible and not color-only
- edge states for limited, stale, degraded, waiting, and non-live posture are tested
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
