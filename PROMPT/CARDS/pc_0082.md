---
id: pc_0082
slug: phase_02_seq_082_create_repository_task_runner_makefile_and_agent_entrypoint_scripts
mode: sequential
phase: "02"
wave: null
track: null
task_number: "082"
block_key: phase_02_seq
original_key: phase_02_seq_082
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0082

## Task Summary

create repository task runner makefile and agent entrypoint scripts.

## Metadata

- ID: `pc_0082`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `082`
- Source Slug: `phase_02_seq_082_create_repository_task_runner_makefile_and_agent_entrypoint_scripts`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0081](./pc_0081.md) `phase_02_seq_081_create_generated_api_schema_and_contract_documentation_site`
- Next: [pc_0083](./pc_0083.md) `phase_02_seq_083_create_baseline_access_control_matrix_and_default_role_seed_data`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0078_to_0085.md](../shared_operating_contract_0078_to_0085.md)
```text
Execute roadmap task `phase_02_seq_082_create_repository_task_runner_makefile_and_agent_entrypoint_scripts`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0081` if they exist, especially `pc_0028`, `pc_0029`, `pc_0030`, `pc_0059`, `pc_0060`, `pc_0062`, `pc_0063`, `pc_0066`, `pc_0078`, `pc_0079`, `pc_0080`, and `pc_0081`.

Mission:
Create the one canonical operator and agent command surface for the repo.
A human developer, CI worker, or autonomous coding agent should be able to bootstrap the workspace, validate contracts, start the local stack, provision ephemeral test environments, run Playwright and integration suites, generate the observatory, check drift, and collect evidence through a stable task grammar instead of memorizing scattered scripts.

Source algorithm grounding you must honor:
- `README.md`, especially the validator commands and blueprint acceptance map.
- `architecture_coherence_guardrails.md` and `implementation_conventions.md`, especially anti-drift and support-doc coupling.
- `verification_and_release_gates.md`, because task surfaces must expose the actual required test and gate families.
- `manifest_and_config_freeze_contract.md` and `config_freeze_inheritance_and_consumption_contract.md`, because task execution must not quietly bypass frozen config semantics.
- Prior repo-structure and execution decisions from `pc_0028`, `pc_0029`, `pc_0059`, `pc_0062`, `pc_0063`, `pc_0066`, `pc_0078`, `pc_0079`, `pc_0080`, and `pc_0081`.

Deliverables to create:
1. `Makefile`.
2. `scripts/tasks/task_catalog.json`.
3. `scripts/tasks/task_graph.py`.
4. `scripts/agent/bootstrap_agent_workspace.sh`.
5. `scripts/agent/run_card_task.sh`.
6. `scripts/agent/verify_card_evidence.sh`.
7. `scripts/agent/resolve_environment_profile.py`.
8. `docs/repository/task_runner_and_agent_entrypoints.md`.
9. `tests/integration/repository/task_runner_smoke.sh`.
10. `tests/integration/repository/agent_entrypoint_contract.spec.ts`.

Required design decisions you must make explicit:
- Choose the canonical public interface for tasks: `make`, a secondary task tool, or both; if both, document which is authoritative.
- Define stable namespaces for commands such as `bootstrap`, `validate`, `local`, `ephemeral`, `docs`, `drift`, `test`, `release`, and `agent`.
- Define dry-run, verbose, and fail-fast semantics so agents can inspect commands safely before executing destructive operations.
- Define how environment profile resolution works and how dangerous profiles are blocked unless explicitly acknowledged.
- Define how task outputs, logs, and evidence directories are organized so later automation can consume them deterministically.
- Define how card-aware agent entrypoints map a task card ID to the commands it should run, the evidence it should emit, and the checklist or verification state it may inspect.

Execution steps:
1. Build a machine-readable task catalog that lists canonical targets, aliases, required tools, environment profile expectations, destructive posture, and expected outputs.
2. Implement a task graph resolver that can explain dependencies, perform dry runs, and guard against impossible target combinations.
3. Create a top-level `Makefile` that wraps the authoritative scripts for bootstrap, validation, docs generation, drift checks, local stack, ephemeral environments, tests, and release metadata.
4. Implement agent entrypoints that can bootstrap a clean workspace, execute a specific card task or named task family, and verify evidence without assuming human-interactive terminals.
5. Add profile-resolution logic that forces explicit acknowledgment for destructive commands and refuses ambiguous environments.
6. Add smoke tests that prove the task runner can discover commands, resolve dependencies, run validators, and surface clear failures.

Gap handling you must perform:
- Close the gap where the repo has many foundational scripts but no single canonical command surface.
- Close the gap where autonomous agents would otherwise guess which commands to run or how to capture evidence.
- Close the gap where local, CI, and ephemeral workflows diverge because of ad hoc script entrypoints.
- Close the gap where dangerous environment selection could happen via defaulted env vars or shell history.
- Close the gap where task definitions exist only as README prose and not as machine-readable inventory.

Mandatory edge cases:
- missing required toolchain binary
- unsupported environment profile for a destructive command
- request to run docs generation before contract import or binding generation completed
- task alias colliding with another task namespace
- agent entrypoint called with an unknown card ID
- one task depends on local stack but the stack is not healthy
- dry-run output for a destructive command must remain non-destructive
- CI invocation with a reduced toolchain that can still run validation-only targets

Backend, performance, and security requirements:
- Every command must have deterministic exit codes and machine-parseable log markers.
- Environment resolution must be explicit, reproducible, and secret-safe.
- Agent entrypoints must be non-interactive by default and accept structured arguments.
- Dangerous commands must require explicit flags, profile matches, and typed confirmation markers.
- Validation and docs tasks should support incremental execution where safe, but not by skipping authoritative checks.

Validation rules:
- Unit-test task catalog parsing, task graph resolution, environment profile gating, and card ID mapping.
- Smoke-test the top-level task runner from a clean checkout.
- Integration-test the agent entrypoints under both happy-path and blocked-path scenarios.
- Prove that validator commands from the README remain reachable through the canonical task surface.
- Prove that dry-run mode never mutates resources.

Definition of done:
- The repo has one canonical human-and-agent task interface.
- Local, CI, docs, drift, and test workflows are discoverable and scriptable from the same surface.
- Agents no longer need to reverse-engineer the repo's command grammar before working.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

