---
id: pc_0196
slug: phase_04_parallel_wave_02_track_backend_governance_008_implement_policy_risk_and_blast_radius_modeling_projection_and_preview_confidence_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "008"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0196

## Task Summary

implement policy risk and blast radius modeling projection and preview confidence rules.

## Metadata

- ID: `pc_0196`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `008`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_008_implement_policy_risk_and_blast_radius_modeling_projection_and_preview_confidence_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0195](./pc_0195.md) `phase_04_parallel_wave_02_track_backend_governance_007_implement_governance_interaction_layer_projection_for_baskets_diff_panels_and_step_up`
- Next: [pc_0197](./pc_0197.md) `phase_04_parallel_wave_02_track_backend_governance_009_implement_manifest_lineage_explorer_and_reuse_decision_tracer_queries`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0190_to_0197.md](../shared_operating_contract_0190_to_0197.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_008_implement_policy_risk_and_blast_radius_modeling_projection_and_preview_confidence_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0195` if they exist, especially `pc_0190`, `pc_0191`, and the earlier authority / approval foundations.

Mission:
Implement the reusable governance mutation hazard / basis modeling service and the preview confidence rules that feed simulator, basket, blast-radius panel, approval composer, and later commit boundaries.
This is not a UI-only scoring layer and not an advisory helper.
It is the canonical deterministic engine for bounded blast-radius intervals, policy risk, approval necessity, bounded-safe posture, commit authority posture, and hazard / basis hash reuse.

Source algorithm grounding you must honor:
- `policy_risk_and_blast_radius_modeling_contract.md`, all sections.
- `modules.md`, especially `SIMULATE_GOVERNANCE_MUTATION(...)`, the exact formulas, hashes, and bounded-safe conditions.
- `data_model.md`, especially the hazard / basis fields embedded in `GovernanceAccessSimulation` and `GovernancePolicySnapshot`.
- `admin_governance_console_architecture.md`, especially the simulator and tenant-policy blast-radius rules.
- `schemas/governance_mutation_hazard_contract.schema.json` and `schemas/governance_mutation_basis_contract.schema.json`.
- `scripts/validate_contracts.py`, especially the deterministic formula checks and FE-51 continuity assertions.
- `test_vectors.md`, especially TV-65A, TV-65B, TV-65C, and FE-51.

Deliverables to create:
1. `packages/backend-governance/src/services/build_governance_mutation_hazard_contract.ts`
2. `packages/backend-governance/src/services/build_governance_mutation_basis_contract.ts`
3. `packages/backend-governance/src/services/derive_policy_risk_score.ts`
4. `packages/backend-governance/src/services/derive_approval_necessity_score.ts`
5. `packages/backend-governance/src/services/derive_commit_authority_posture.ts`
6. `packages/backend-governance/src/services/derive_governance_count_classes.ts`
7. `packages/backend-governance/src/tests/governance_mutation_hazard_contract.spec.ts`
8. `packages/backend-governance/src/tests/governance_mutation_basis_contract_continuity.spec.ts`
9. `docs/governance/policy_risk_and_blast_radius_modeling.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/blast-radius-lab.tsx`

You MUST refactor any duplicated risk or approval math in policy, access, or approval codepaths to reuse this service layer instead of preserving route-local copies.

Required design decisions you must make explicitly:
- define the canonical serialization order used to hash the hazard contract and basis contract
- define the count-class bucketing profile for impacted principals, clients, authority operations, workflows, and limitations
- define how `risk_driver_codes[]`, `approval_trigger_codes[]`, `confidence_limiter_codes[]`, and `bounded_safety_blocker_codes[]` are derived deterministically from the same frozen inputs
- define the shared rounding helper used by `policy_risk_score` and `approval_necessity_score`
- define how mixed staged change groups are recognized as non-atomic when hazard hash, basis hash, approval posture, or required approvals differ

Execution requirements:
- implement the exact formulas from `modules.md`:
  `policy_risk_score = round_score(100 * (1 - ((1 - privilege_gain_score/100)^0.40 * (1 - scope_expansion_score/100)^0.25 * (1 - masking_relaxation_score/100)^0.20 * (1 - impact_radius_upper_score/100)^0.15)))`
  `approval_necessity_score = round_score(100 * (1 - ((1 - policy_risk_score/100)^0.70 * (1 - impact_radius_upper_score/100)^0.30)))`
  `bounded_safe_mutation = 1` iff privilege, scope, masking scores are all zero, upper impact radius `< 5`, policy risk `< 15`, confidence `>= 90`, and predictability `>= 85`
- implement the exact commit posture ladder:
  `PREVIEW_ONLY` iff confidence `< 80` or predictability `< 75`
  `BOUNDED_SAFE` iff not preview-only and bounded-safe is proven
  `APPROVAL_GATED` otherwise
- implement the exact approval ladder:
  `NOT_REQUIRED` iff bounded-safe is proven
  `CHANGE_ADVISORY_QUORUM` iff approval necessity `>= 80`
  `SECURITY_REVIEW` iff approval necessity `>= 55`
  `DUAL_APPROVER` iff approval necessity `>= 30`
  `SINGLE_APPROVER` otherwise
- ensure `hazard_contract_hash` covers the full hazard packet and `basis_contract_hash` covers the exact basis packet that points back to that hazard hash
- ensure mixed governance baskets never collapse into one active hazard or basis packet
- ensure reviewed hazard/basis packets are reusable without semantic drift across simulator, basket, approval composer, and blast-radius panel

Gap handling you must close while implementing:
- close any gap where policy-risk math is duplicated or route-local
- close any gap where reviewed hazard can be silently recomputed, narrowed, or re-ranked downstream
- close any gap where low-confidence previews can surface direct commit styling or `NOT_REQUIRED` approval posture
- close any gap where mixed staged groups collapse into a fake atomic hazard packet
- close any gap where reason codes are hand-authored text instead of deterministic derived posture

Mandatory edge cases:
- `approval_requirement = NOT_REQUIRED` must be impossible when any bounded-safe condition fails
- `risk_driver_codes[]` must reflect only the contributor families actually driving risk
- `confidence_limiter_codes[]` must be non-empty whenever posture becomes `PREVIEW_ONLY`
- `bounded_safety_blocker_codes[]` must name every failing family when bounded-safe is not proven
- atomic basket hazard / basis reuse must fail when any group differs on `hazard_contract_hash`, `basis_contract_hash`, `approval_requirement`, or `required_approvals[]`

Playwright and contract-test requirements:
- add direct tests for formula outputs, approval ladder transitions, preview-only posture, bounded-safe gates, count bucketing, and mixed-basis non-atomic baskets
- if simulation or policy routes are touched, add Playwright `APIRequestContext` tests that confirm the same reviewed hazard / basis hash survives from preview into route projections
- if you add the optional lab, add Playwright browser tests that verify interval-band rendering, confidence rails, reason-code stability, and semantic locators only
- capture serialized hazard / basis packets and traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- create one quiet analysis surface with a compact input summary rail, a dominant interval band, two slim confidence rails, and one reason-code ledger
- max width `1440px`; palette `#F7F5F1 / #FFFFFF / #F1F3F0 / #171717 / #667085 / #0F766E / #1D4ED8 / #B7791F / #C2410C`
- render blast radius as a bounded interval band with lower / upper values and uncertainty emphasis when spread is material
- render risk and approval posture as literal labeled rails or rows, not gauges or circular charts
- render reason-code families as compact categorized ledgers with no decorative badge cloud
- animate only subtle opacity / translate transitions; reduced-motion mode must preserve meaning without ornamental movement

Backend, performance, and security requirements:
- keep the service deterministic, pure, and reusable
- canonicalize all hash inputs; no non-deterministic map iteration or locale-sensitive formatting
- keep count derivation indexed and cheap; do not scan unrelated governance objects per preview
- fail closed on missing topology hash, missing access binding hash, malformed counts, or impossible score ranges

Validation rules:
- validate hazard and basis outputs against schema
- add regression coverage for TV-65A/B/C and FE-51
- keep this task focused on reusable hazard / basis modeling; do not absorb unrelated route projector work

Definition of done:
- the repo has one canonical service layer for governance mutation hazard / basis derivation
- blast-radius, risk, approval, and preview-confidence posture are deterministic and hash-stable across all consumer routes
- governance previews can no longer drift into route-local scoring or silent recomputation
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
