---
id: pc_0208
slug: phase_04_parallel_wave_02_track_backend_recovery_010_implement_nightly_portfolio_what_if_simulation_and_release_admissibility_counterfactuals
mode: parallel
phase: "04"
wave: "02"
track: "backend_recovery"
task_number: "010"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_recovery_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0208

## Task Summary

010 implement nightly portfolio what-if simulation and release admissibility counterfactuals.

## Metadata

- ID: `pc_0208`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend recovery`
- Task Number: `010`
- Source Slug: `phase_04_parallel_wave_02_track_backend_recovery_010_implement_nightly_portfolio_what_if_simulation_and_release_admissibility_counterfactuals`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0207](./pc_0207.md) `phase_04_parallel_wave_02_track_backend_recovery_009_implement_nightly_autopilot_scheduler_sharding_reclaim_and_operator_morning_digest_derivation`
- Next: [pc_0209](./pc_0209.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_001_implement_retention_tag_artifact_retention_and_expiry_limitation_propagation_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0206_to_0213.md](../shared_operating_contract_0206_to_0213.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_recovery_010_implement_nightly_portfolio_what_if_simulation_and_release_admissibility_counterfactuals`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0207` if they exist, especially `pc_0206` through `pc_0207`, `pc_0190` through `pc_0197`, and `pc_0198` through `pc_0205`.

Mission:
Implement the non-mutating nightly portfolio what-if simulation that replays persisted nightly batch truth and digest truth under counterfactual release/policy/concurrency inputs to estimate backlog, tail-risk, queue partitions, and highlight movement without touching live legal state.
This task is not allowed to re-query current eligibility, recompute the nightly universe from live state, or downgrade blocking semantics for authority ambiguity, step-up, or approval.
The resulting artifact must be a persisted `NightlyPortfolioWhatIfSimulation` bound to an exact `NightlyPortfolioSimulationBasisContract`.

Source algorithm grounding you must honor:
- `nightly_autopilot_contract.md`, especially the what-if simulation section, the source-batch-set rules, and the non-execution boundary.
- `modules.md`, especially `SIMULATE_NIGHTLY_PORTFOLIO_WHAT_IF(...)` and any referenced summary/ranking helpers.
- `data_model.md`, especially `NightlyPortfolioWhatIfSimulation` and linked digest/batch references.
- `schemas/nightly_portfolio_what_if_simulation.schema.json` and `schemas/nightly_portfolio_simulation_basis_contract.schema.json`.
- `scripts/validate_contracts.py`, especially `nightly_portfolio_what_if_simulation_digest_impact_comparison`, `nightly_portfolio_what_if_simulation_retry_budget_reason_binding`, `nightly_portfolio_what_if_simulation_authority_ambiguity_stays_blocking`, and `nightly_portfolio_what_if_simulation_highlight_diff_partition`.
- `tools/forensic_contract_guard.py`, especially the pinned basis-contract constants such as `truth_source_policy`, `selection_projection_policy`, `digest_projection_policy`, `non_execution_boundary_policy`, and `diff_explainability_policy`.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially counterfactual-release, same-window successor-chain, and highlight-diff scenarios.

Deliverables to create:
1. `packages/backend-recovery/src/models/nightly_portfolio_simulation_basis_contract.ts`
2. `packages/backend-recovery/src/models/nightly_portfolio_what_if_simulation.ts`
3. `packages/backend-recovery/src/services/load_nightly_simulation_source_batch_set.ts`
4. `packages/backend-recovery/src/services/replay_nightly_selection_entries.ts`
5. `packages/backend-recovery/src/services/simulate_nightly_portfolio_what_if.ts`
6. `packages/backend-recovery/src/services/derive_nightly_portfolio_diff_explainability.ts`
7. `packages/backend-recovery/src/tests/nightly_portfolio_what_if_simulation.spec.ts`
8. `packages/backend-recovery/src/tests/nightly_portfolio_basis_contract.spec.ts`
9. `docs/recovery/nightly_portfolio_what_if_simulation_and_release_admissibility_counterfactuals.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/recovery/nightly-portfolio-what-if-lab.tsx`

You MUST patch any existing simulation or analytics helper that currently re-derives the nightly portfolio from live read models instead of replaying the persisted source batch set.

Required design decisions you must make explicitly:
- define how the source batch set is selected and constrained to one nightly window or one lawful successor chain
- define the canonical basis-contract hash inputs, including execution-boundary hash, source-batch set hash, covered selection-entry set hash, baseline release/policy/schema/build/env, and counterfactual overrides
- define how counterfactual concurrency, retry budgets, and release admissibility differences are expressed without mutating source truth
- define how queue summaries, highlight partitions, and entry-level diffs are generated from replayed selection truth rather than live recomputation
- define how blocking postures such as authority ambiguity, approval requirement, and step-up requirement remain blocking in the simulation result

Execution requirements:
- load a persisted source batch set and baseline digest only from nightly batches within one lawful nightly window context
- replay persisted `selection_entries[]` and baseline digest partitions without live re-query of eligibility or authority state
- keep the simulation non-mutating: do not create or patch `NightlyBatchRun`, `RunManifest`, workflow items, notifications, or authority-facing artifacts
- persist `NightlyPortfolioSimulationBasisContract` with exact source-batch refs, set hash, selection-entry coverage, baseline release/policy/schema/build/env, counterfactual overrides, and typed source policies
- produce `NightlyPortfolioWhatIfSimulation` with baseline/simulated summary counts, backlog pressure, tail risk, stability state, highlighted refs, entry diffs, and highlight diffs
- preserve the non-execution boundary: authority ambiguity, approval gaps, step-up requirements, and similar block conditions remain blocking in the simulated outcome
- emit explicit reason-code diffs for every bucket movement, order movement, or highlighted-client movement

Gap handling you must close while implementing:
- close any gap where what-if simulation re-runs live selection queries instead of replaying persisted selection truth
- close any gap where a simulation spanning multiple windows or unrelated successor chains can be assembled without an explicit basis-contract failure
- close any gap where simulated highlight order or queue partitions are emitted without durable diff explainability
- close any gap where authority ambiguity or approval barriers disappear under counterfactual replay even though the contract says they remain blocking
- close any gap where simulation output can be mistaken for a live digest or live operator handoff artifact

Mandatory edge cases:
- missing baseline digest is legal only if the basis contract and simulation artifact explicitly capture null-baseline posture rather than backfilling one from live reads
- a successor recovery chain may be included only when it still represents one nightly window and one lawful source batch set
- changing release admissibility or concurrency profile must surface diff reason codes rather than rewriting baseline truth
- highlight movement must preserve source and simulated partition ordering with explicit movement reasons
- retry-budget differences must bind to explicit reason codes and must not silently reshape unrelated queue partitions
- authority ambiguity must remain blocking in the simulated result even if a hypothetical policy would otherwise accelerate execution

Playwright and contract-test requirements:
- add deterministic contract tests for basis-contract hashing, source-batch-set loading, selection replay without live requery, diff explainability, and blocking-posture preservation
- if you expose or patch a simulation-read route, add Playwright `APIRequestContext` coverage for counterfactual requests, null-baseline posture, and authority-ambiguity blocking retention
- if you add the optional lab, add Playwright browser tests that verify the counterfactual diff matrix, highlight-movement ladder, backlog/tail-risk rails, keyboard navigation, and reduced-motion-safe reveal behavior using semantic locators only
- capture payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a three-zone layout: counterfactual basis ribbon, diff matrix / movement ladder canvas, narrow stability sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; accent `#0F766E`; info accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- prefer ordered rails, matrices, and ranked ladders over summary-only tiles
- clearly label baseline vs simulated posture without using loud split-screen color blocks or gimmick charts
- animate only opacity/translate/height with `140ms` to `180ms`; respect reduced motion

Backend, performance, and security requirements:
- keep simulation compute deterministic and side-effect free
- cache only by exact basis-contract hash and counterfactual override tuple; never cache across tenant/window/source-batch-set drift
- prohibit simulation from reading mutable queue state, browser state, or transient worker-local metrics as truth
- store reason-code diffs and order hashes so later governance/analytics work can inspect them without re-running simulation

Validation rules:
- validate every `NightlyPortfolioSimulationBasisContract` and `NightlyPortfolioWhatIfSimulation` artifact against schema before persistence or response emission
- update fixtures/builders so all nightly simulation validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one canonical backend path for loading source batch sets, replaying nightly selection truth, and persisting counterfactual portfolio simulations
- simulation is deterministic, non-mutating, exact about its basis, and test-covered
- optional labs, if added, remain read-only projections of persisted simulation artifacts
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
