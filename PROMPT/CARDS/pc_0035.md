---
id: pc_0035
slug: phase_01_seq_035_browser_automation_register_hmrc_sandbox_application_for_obligations_calculations_and_submissions
mode: sequential
phase: "01"
wave: null
track: null
task_number: "035"
block_key: phase_01_seq
original_key: phase_01_seq_035
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0035

## Task Summary

browser automation register hmrc sandbox application for obligations calculations and submissions.

## Metadata

- ID: `pc_0035`
- Mode: `sequential`
- Phase: `01`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `035`
- Source Slug: `phase_01_seq_035_browser_automation_register_hmrc_sandbox_application_for_obligations_calculations_and_submissions`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0034](./pc_0034.md) `phase_01_seq_034_browser_automation_create_hmrc_developer_hub_account_and_project_workspace`
- Next: [pc_0036](./pc_0036.md) `phase_01_seq_036_browser_automation_configure_hmrc_redirect_uris_scopes_and_callback_endpoints`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt

Use the shared operating contract first:
[../shared_operating_contract_0030_to_0037.md](../shared_operating_contract_0030_to_0037.md)

```text
Execute roadmap task `phase_01_seq_035_browser_automation_register_hmrc_sandbox_application_for_obligations_calculations_and_submissions`.

Inherit the shared operating contract and consume the outputs from cards `pc_0001` through `pc_0034` if they exist,
especially the environment/profile catalog, provisioning workspace, secret policy, and HMRC Developer Hub workspace record.

Mission:
Create or adopt the canonical HMRC sandbox application for Taxat's obligations, calculation, and submission flows,
subscribe it to the required MTD Income Tax APIs,
and persist a sanitized application record plus subscription matrix.
This task stops at sandbox application registration and API subscription.
It must not yet export client credentials into the vault; that belongs to later cards.

Source algorithm grounding you must honor:
- `authority_interaction_protocol.md` operation families and provider-profile requirements.
- `manifest_and_config_freeze_contract.md -> 5.12 Provider and environment capture`.
- `release_candidate_identity_and_promotion_evidence_contract.md` and `verification_and_release_gates.md`
  wherever provider-profile sets and sandbox evidence matter.
- `deployment_and_resilience_contract.md` for controlled gateway / runtime topology implications.
- `invention_and_system_boundary.md` to keep provisioning outside runtime product semantics.
- The outputs from `pc_0031`, `pc_0032`, `pc_0033`, and `pc_0034`.
- Current HMRC guidance for:
  creating sandbox applications,
  subscribing them to APIs,
  getting sandbox credentials,
  and the MTD Income Tax minimum-functionality API set.
- Current HMRC API documentation / roadmap for the current versions and statuses of the relevant APIs.

External-provider grounding you must honor at execution time:
Current observed API-version baseline at prompt-authoring time:
- `Business Details (MTD)` latest observed `2.0` (beta)
- `Obligations (MTD)` latest observed `3.0`
- `Individual Calculations (MTD)` latest observed `8.0`
- `Self-Employment Business (MTD)` latest observed `5.0`
- `Property Business (MTD)` latest observed `6.0`
Use these only as a starting baseline; re-check live HMRC docs immediately before acting.

- HMRC “Getting started” and “Testing in the sandbox” say you create a sandbox application,
  subscribe it to APIs, get sandbox credentials, and test against sandbox endpoints.
- HMRC MTD Income Tax integration guidance identifies the core API families required for in-year and end-of-year journeys.
- Current HMRC docs expose live versions for Business Details, Obligations, Individual Calculations,
  Self-Employment Business, and Property Business; those versions can change and must be revalidated before subscription work.
- HMRC application naming guidance allows sandbox applications to be named freely while production naming is stricter.
- HMRC development practices say one production application is typically sufficient and customer traffic isolation comes from OAuth tokens, not one app per customer.

Deliverables to create:
1. `automation/provisioning/src/providers/hmrc/flows/register_sandbox_application.ts`
2. `automation/provisioning/src/providers/hmrc/flows/subscribe_required_mtd_apis.ts`
3. `automation/provisioning/src/providers/hmrc/selectors/application_console.selectors.json`
4. `automation/provisioning/src/providers/hmrc/contracts/sandbox_application_record.schema.json`
5. `docs/provisioning/hmrc_sandbox_application_runbook.md`
6. `data/provisioning/hmrc_sandbox_application_record.template.json`
7. `data/provisioning/hmrc_sandbox_api_subscription_matrix.template.json`
8. `data/provisioning/hmrc_required_api_set_baseline.json`
9. `tests/playwright/provisioning/hmrc/register_sandbox_application.spec.ts`
10. `tests/playwright/provisioning/hmrc/subscribe_required_mtd_apis.spec.ts`

Required API-set reasoning you must perform:
- Derive the minimal required sandbox API subscriptions from the algorithm's operation families and HMRC's current MTD Income Tax minimum-functionality guidance.
- At minimum evaluate:
  `Business Details (MTD)`,
  `Obligations (MTD)`,
  `Individual Calculations (MTD)`,
  `Self-Employment Business (MTD)`,
  and `Property Business (MTD)`.
- Also explicitly evaluate whether the current Taxat scope implies additional APIs such as
  `Business Source Adjustable Summary (MTD)` or `Individual Losses (MTD)`.
  If they are not required for this card's narrow obligations/calculations/submissions title,
  record that as a typed deferred-scope decision rather than ignoring them.
- Persist both:
  a “required now for this roadmap slice” list,
  and a “likely required later for broader end-of-year/full-product scope” list.

Execution steps:
1. Revalidate current HMRC API versions and statuses before acting.
   Record the observed versions and the verification timestamp in `hmrc_required_api_set_baseline.json`.
2. Use the authenticated Developer Hub workspace from `pc_0034` to reach the application-management area.
3. Detect whether the canonical sandbox application already exists.
   If it does, adopt it and verify its settings rather than creating a duplicate.
4. If it does not exist, create it using a naming convention that is sandbox-safe now and production-safe in spirit,
   while still complying with HMRC's current naming guidance.
5. Navigate to API subscription management and subscribe the app to the required API set.
6. Re-read the application page and subscription page to verify the final state.
7. Persist a sanitized application record and subscription matrix with aliases / refs only, never live secrets.
8. Capture typed gaps if HMRC's current portal organization or API labels differ from the algorithm's conceptual families.

Gap handling you must perform:
- Close the “there is no canonical HMRC sandbox application record for Taxat” gap.
- Close the “required HMRC subscriptions for the current roadmap slice are not yet normalized into one machine-readable set” gap.
- Close the “later cards need one adopted-or-created sandbox app rather than ambiguous multiple apps” gap.
- Close the “full-product versus this-slice API scope is easy to blur” gap.

Mandatory edge cases:
- The application may already exist but be missing one or more subscriptions.
- HMRC may rename menu labels or move subscription controls.
- The current API version or status may change between authored prompt time and execution time.
- A sandbox application may have been auto-deleted after inactivity; the flow must handle recreate-versus-adopt decisions explicitly.
- Subscription pages may show API names that do not match Taxat operation-family labels one-to-one; preserve the mapping artifact.
- Do not create multiple near-identical sandbox apps merely because prior runs were partial.
- Do not export or persist client credentials yet, even if the portal reveals them on the page.

Architecture and security requirements:
- Use Playwright for implementation and testing.
- Keep app creation and subscription logic idempotent and state-aware.
- Persist only sanitized records plus evidence refs.
- Align required API subscriptions with the environment/provider-profile catalog from `pc_0031`.
- Keep naming decisions, observed versions, and subscription rationale machine-readable.

Validation rules:
- Fixture tests must cover:
  create-new app,
  adopt-existing app,
  partial-subscription remediation,
  and API-label drift.
- The sanitized application record and subscription matrix must validate against their schemas/templates.
- The required API baseline file must include verification timestamps and source-rationale fields.
- Live mode must remain opt-in and must not run by default in CI.

Definition of done:
- A later agent can deterministically locate or create the canonical HMRC sandbox application,
  subscribe it to the exact required API set for this roadmap slice, and rely on one sanitized application record plus one subscription matrix
  instead of reconstructing the provider setup manually.
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm corpus and aligned to the authoritative card slug.
- Instructions explicitly preserve card status neutrality; claim and completion state must continue to live only in `Checklist.md`.
