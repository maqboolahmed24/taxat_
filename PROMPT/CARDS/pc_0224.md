---
id: pc_0224
slug: phase_04_parallel_wave_02_track_backend_release_resilience_006_implement_restore_drill_result_capture_and_release_promotion_evidence_binding
mode: parallel
phase: "04"
wave: "02"
track: "backend_release_resilience"
task_number: "006"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_release_resilience_0006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0224

## Task Summary

006 implement restore drill result capture and release promotion evidence binding.

## Metadata

- ID: `pc_0224`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend release resilience`
- Task Number: `006`
- Source Slug: `phase_04_parallel_wave_02_track_backend_release_resilience_006_implement_restore_drill_result_capture_and_release_promotion_evidence_binding`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0223](./pc_0223.md) `phase_04_parallel_wave_02_track_backend_release_resilience_005_implement_verification_suite_result_and_gate_admissibility_record_persistence`
- Next: [pc_0225](./pc_0225.md) `phase_04_parallel_wave_02_track_backend_release_resilience_007_implement_client_compatibility_matrix_generation_for_web_and_native_surface_versions`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0222_to_0229.md](../shared_operating_contract_0222_to_0229.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_release_resilience_006_implement_restore_drill_result_capture_and_release_promotion_evidence_binding`.

Inherit the shared operating contract and consume outputs from cards `pc_0200`, `pc_0219`, `pc_0222`, and `pc_0223` if they exist.

Mission:
Implement the canonical persistence, binding, and verification path for `RestoreDrillResult` so restore evidence becomes lawful release evidence only when it is checkpoint-bound, candidate-bound, privacy-reconciliation-bound, and queue/audit/authority-safe.
This task is not allowed to equate byte-level restore success with lawful production readiness.

Source algorithm grounding you must honor:
- `deployment_and_resilience_contract.md`, especially sections 4, 5, and 6.
- `recovery_tier_checkpoint_and_fail_forward_governance_contract.md`, especially checkpoint law and restore privacy reconciliation law.
- `verification_and_release_gates.md`, especially restore-drill gate rules and evidence required for promotion.
- `data_model.md`, especially `RestoreDrillResult`, `RecoveryCheckpoint`, `RestorePrivacyReconciliationContract`, `ReleaseVerificationManifest`, and `VerificationSuiteResult`.
- `schemas/restore_drill_result.schema.json`, `schemas/recovery_checkpoint.schema.json`, `schemas/restore_privacy_reconciliation_contract.schema.json`, and `schemas/release_verification_manifest.schema.json`.
- `scripts/validate_contracts.py`, especially `validate_restore_drill_result`, `validate_recovery_checkpoint`, and the restore/privacy drift checks named in the shared operating contract.
- `test_vectors.md`, especially `TV-79`, `TV-79G`, and the release-evidence restore vectors.

Deliverables to create:
1. `packages/backend-release/src/models/restore_drill_result.ts`
2. `packages/backend-release/src/repositories/restore_drill_result_repository.ts`
3. `packages/backend-release/src/services/record_restore_drill_result.ts`
4. `packages/backend-release/src/services/validate_restore_drill_promotion_readiness.ts`
5. `packages/backend-release/src/services/bind_restore_drill_into_release_evidence.ts`
6. `packages/backend-release/src/tests/restore_drill_result.spec.ts`
7. `packages/backend-release/src/tests/restore_drill_promotion_binding.spec.ts`
8. `docs/release/restore_drill_result_and_promotion_binding.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/release/restore-drill-evidence-lab.tsx`

You MUST patch any existing code path that currently:
- records restore success without binding the exact checkpoint tested,
- records restore outcomes without the candidate-bound runtime tuple,
- promotes restore evidence without a final privacy reconciliation contract,
- or treats queue/audit/authority rebuild checks as optional narrative notes.

Required design decisions you must make explicitly:
- define how `drill_scope` distinguishes `CURRENT_RELEASE_CANDIDATE` from `DR_FAILOVER_FAILBACK`
- define how a passed drill derives `restore_verification_hash` / promotion eligibility from checkpoint, candidate, and privacy-contract evidence
- define the canonical ordering and uniqueness rules for `enabled_provider_profile_refs[]` and `failure_reason_codes[]`
- define how restore evidence binds forward into suite results, admissibility records, manifest assembly, and the final verification manifest
- define how `QUARANTINED` versus `FAILED` posture is represented when restore evidence exists but cannot lawfully reopen or promote

Execution requirements:
- persist `RestoreDrillResult` as a first-class evidence artifact with the exact candidate-bound runtime tuple
- require both `restore_drill_ref` and `checkpoint_ref` lineage to stay explicit
- require passed results to verify audit continuity, privacy reconciliation, queue rebuild, authority rebuild, authority binding revalidation, and a final ready privacy contract
- require failed or quarantined results to retain explicit `failure_reason_codes[]`
- ensure provider-profile refs and failure-reason codes remain canonically ordered and unique
- make restore evidence queryable by checkpoint, candidate hash, and verification manifest lineage
- if you touch manifest assembly code, ensure green restore posture always keeps both `restore_drill_ref` and `restore_checkpoint_ref`

Gap handling you must close while implementing:
- close the gap where restore evidence is stored as a report attachment instead of a typed contract
- close the gap where a passed restore drill can outlive a blocked or stale privacy reconciliation contract
- close the gap where restore evidence is not candidate-bound and can be silently swapped across releases
- close the gap where rebuild and revalidation checks remain implied instead of typed proof

Mandatory edge cases:
- non-passed results cannot claim every verification basis succeeded
- passed results must fail closed if the privacy reconciliation contract is blocked, incomplete, or lacks final reopen-safe posture
- restore evidence must fail closed if the provider-profile set is serialized in non-canonical order
- restore evidence must fail closed if failure-reason codes are unsorted or present on a passed result
- a green restore gate must never exist without both the restore drill ref and checkpoint ref

Playwright and contract-test requirements:
- add direct tests for candidate identity binding, restore/checkpoint pairing, privacy-contract readiness, canonical ordering, and failure-basis posture
- if you expose or patch internal release/debug routes, add Playwright `APIRequestContext` coverage for restore-drill retrieval and checkpoint-bound release evidence retrieval
- if you add the optional lab, add Playwright browser tests for checkpoint-to-drill lineage display, reduced-motion-safe evidence expansion, and semantic keyboard navigation using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- use a quiet evidence ladder: checkpoint card, restore drill result, privacy reconciliation status, and release-binding sidecar
- current promotion-eligible posture must be visibly distinct from quarantine or blocked posture
- keep limitation and blocker language explicit; do not collapse it into a generic warning banner

Backend, performance, and security requirements:
- restore evidence must remain immutable once published for a drill execution
- keep report bodies or forensic blobs referenced, not inlined into the core contract object
- preserve append-only lineage; do not mutate older drill results in place when a newer drill supersedes them
- ensure privacy-reconciliation and restore evidence cross-links remain durable and queryable

Validation rules:
- validate every `RestoreDrillResult` payload against schema before persistence
- update fixtures/builders so validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test`
- rerun `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- restore-drill evidence is first-class, candidate-bound, checkpoint-bound, privacy-bound, and promotion-bound
- passed/failed/quarantined posture is durable and test-covered
- optional labs, if added, remain read-only viewers over persisted truth
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, the relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.