---
id: pc_0175
slug: phase_04_parallel_wave_02_track_backend_low_noise_007_implement_operator_interaction_layer_and_interaction_layer_foundation_projectors
mode: parallel
phase: "04"
wave: "02"
track: "backend_low_noise"
task_number: "007"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_low_noise_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0175

## Task Summary

implement operator interaction layer and interaction layer foundation projectors.

## Metadata

- ID: `pc_0175`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend low noise`
- Task Number: `007`
- Source Slug: `phase_04_parallel_wave_02_track_backend_low_noise_007_implement_operator_interaction_layer_and_interaction_layer_foundation_projectors`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0174](./pc_0174.md) `phase_04_parallel_wave_02_track_backend_low_noise_006_implement_shell_state_taxonomy_contract_and_dominant_question_projection_rules`
- Next: [pc_0176](./pc_0176.md) `phase_04_parallel_wave_02_track_backend_low_noise_008_implement_cross_shell_design_token_binding_and_interaction_semantics_projection`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0174_to_0181.md](../shared_operating_contract_0174_to_0181.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_low_noise_007_implement_operator_interaction_layer_and_interaction_layer_foundation_projectors`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0174` if they exist, especially `pc_0169` through `pc_0174` and the route-continuity work from `pc_0158` through `pc_0168`.

Mission:
Implement the explicit `InteractionLayerFoundationContract` projector and the `OperatorInteractionLayer` projector for `CALM_SHELL` surfaces so spacing, recovery, preview, history, notification, motion, and continuity grammar stop living in renderer wrappers.
The output must be server-authored, schema-valid, and directly consumable by browser and future native embodiments.

Source algorithm grounding you must honor:
- `cross_shell_design_token_and_interaction_layer_foundation_contract.md`, entire document.
- `frontend_shell_and_interaction_law.md`, especially sections 2.2A, 8, and 10.
- `low_noise_experience_contract.md`, especially shell continuity, current-vs-history, operator interaction layer, motion, and artifact-handoff rules.
- `data_model.md`, especially `InteractionLayerFoundationContract` and `OperatorInteractionLayer`.
- `modules.md`, especially the `BUILD_LIVE_EXPERIENCE_FRAME(...)` rules that require `interaction_layer` and `foundation_contract` emission.
- `schemas/interaction_layer_foundation_contract.schema.json` and `schemas/operator_interaction_layer.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially the low-noise budget/salience hardening items and any interaction-layer drift findings.

Deliverables to create:
1. `packages/backend-low-noise/src/services/project_interaction_layer_foundation_contract.ts`
2. `packages/backend-low-noise/src/services/project_operator_interaction_layer.ts`
3. `packages/backend-low-noise/src/services/derive_calm_shell_preview_notification_and_history_posture.ts`
4. `packages/backend-low-noise/src/services/derive_calm_shell_motion_and_recovery_presentation.ts`
5. `packages/backend-low-noise/src/services/validate_operator_interaction_layer_contract.ts`
6. `packages/backend-low-noise/src/tests/operator_interaction_layer_projection.spec.ts`
7. `docs/low_noise/operator_interaction_layer_and_foundation.md`
8. Optional but recommended: `apps/admin-console-web/src/routes/debug/low-noise/interaction-atlas.tsx`

You MUST patch the existing low-noise frame projector so every published `LowNoiseExperienceFrame` now emits one grouped `interaction_layer` with one grouped `foundation_contract`.

Required design decisions you must make explicitly:
- define whether the foundation projector is a shell-family registry, a typed factory, or both; make it easy for portal and governance builders to reuse later without copying constant tables
- choose the exact calm-shell `recovery_notice_surface` and document when `IDENTITY_HEADER` is lawful versus when ordinary inline recovery stays in `CONTEXT_BAR`
- choose the exact notification posture for browser versus parent-bound mirrored support windows without violating the shared `notification_surface_policy`
- define how `artifact_preview_surface` and `history_presentation` remain summary-first and parent-bound through secondary-window or redocked support embodiments
- define how `unsafe_action_policy` is derived from stale/limited/no-safe-action posture so renderer wrappers cannot keep unsafe mutations visually live

Execution requirements:
- publish one `foundation_contract` whose values for `CALM_SHELL` exactly match the source mapping for density, spacing, support spacing, responsive compaction, selector profile, continuity, recovery surface, history posture, preview posture, notification posture, secondary-window posture, motion profile, and feedback-truth policy
- publish one `OperatorInteractionLayer` whose fields are derived from that grouped foundation instead of duplicating token bindings manually
- preserve `CURRENT_PRIMARY_HISTORY_SECONDARY` posture for file, receipt, and declaration surfaces
- preserve `SAME_OBJECT_SAME_SHELL_INLINE_RECOVERY` continuity and `INLINE_EXPLICIT_REBASE` recovery posture
- keep support windows parent-bound and support-only; they may not become a second authoritative action surface
- keep motion subtle and causal only; do not encode decorative or route-specific animation classes in the interaction layer
- if you add the optional atlas, make it a calm-shell semantics explainer with one token matrix and one embodiment ladder; no dashboard wall, no arbitrary component gallery

Gap handling you must close while implementing:
- the algorithm freezes the cross-shell foundation values in prose and schema, but the repo may still lack a single projector that emits them for live frames; create that projector now
- preview, notification, and history posture often drift into route-local wrappers; centralize them in the operator interaction layer
- calm-shell renderers can still infer unsafe-action posture from stale banners instead of typed contract data; make the unsafe-action posture explicit in the interaction layer

Mandatory edge cases:
- `foundation_contract.shell_family` MUST equal `CALM_SHELL`
- `selector_profile` MUST equal `OPERATOR_SEMANTIC_SELECTORS_V1`
- `continuity_policy` MUST remain `SAME_OBJECT_SAME_SHELL_INLINE_RECOVERY`
- `history_presentation_policy` MUST remain `CURRENT_PRIMARY_HISTORY_SECONDARY`
- `preview_surface_policy` MUST remain `DETAIL_DRAWER_OR_PARENT_BOUND_SECONDARY_WINDOW`
- `secondary_window_policy` MUST remain support-only and parent-bound; do not let a detached window become a new shell family
- degraded or stale postures MUST stay inline; they may not remount a different shell or full-screen recovery product

Playwright and contract-test requirements:
- unit-test the foundation and operator-layer projectors directly
- if any shipped route payload changes, add Playwright APIRequestContext checks proving the emitted interaction layer is present and stable
- if you add the interaction atlas, test it with Playwright using semantic locators, reduced-motion coverage, and trace capture on failure

Backend, performance, and security requirements:
- keep token and interaction semantics server-authored and typed; the browser may style them but must not reinterpret their meaning
- do not widen portal or governance semantics into the calm-shell operator interaction layer
- derive interaction posture from grouped truth and stability contracts rather than from presentation-layer state
- fail closed on incomplete or cross-family-invalid foundations

Validation rules:
- validate every emitted `InteractionLayerFoundationContract` and `OperatorInteractionLayer` against schema
- add regression coverage for preview surface, notification surface, history posture, and unsafe-action posture
- ensure the live frame builder consumes the shared projector rather than hard-coded constants

Definition of done:
- every calm-shell frame publishes one valid `InteractionLayerFoundationContract` and one valid `OperatorInteractionLayer`
- preview/history/notification/recovery semantics stop drifting into renderer-local wrappers
- the calm shell has one reusable interaction contract ready for browser and native embodiments
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
