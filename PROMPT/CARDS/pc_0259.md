---
id: pc_0259
slug: phase_05_parallel_wave_03_track_frontend_portal_007_Playwright_or_other_appropriate_tooling_implement_portal_activity_route_and_customer_safe_timeline_rendering
mode: parallel
phase: "05"
wave: "03"
track: "frontend_portal"
task_number: "007"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_portal_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0259

## Task Summary

007 Playwright or other appropriate tooling implement portal activity route and customer safe timeline rendering.

## Metadata

- ID: `pc_0259`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend portal`
- Task Number: `007`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_portal_007_Playwright_or_other_appropriate_tooling_implement_portal_activity_route_and_customer_safe_timeline_rendering`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0258](./pc_0258.md) `phase_05_parallel_wave_03_track_frontend_portal_006_Playwright_or_other_appropriate_tooling_implement_portal_onboarding_route_and_one_step_at_a_time_progression`
- Next: [pc_0260](./pc_0260.md) `phase_05_parallel_wave_03_track_frontend_portal_008_Playwright_or_other_appropriate_tooling_implement_contextual_request_detail_routes_and_parent_return_restoration`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_portal_007_Playwright_or_other_appropriate_tooling_implement_portal_activity_route_and_customer_safe_timeline_rendering`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0258` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the portal Activity route or activity surface with customer-safe timeline rendering, authority truth posture, newest-first ordering, and hidden-staff-context isolation.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
Portal activity is a client-safe projection of uploads, review results, approvals, submissions, and support/request events. It is not an audit log, not a staff timeline, and not a mixed hidden chronology. `ClientTimelineEvent` and customer-safe projection govern what can be shown; authority-derived states must not be rendered as confirmed when pending, unknown, rejected, out-of-band, or correction-in-flight.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md` Recent Activity, read-model/API translation, activity timeline rules, authority truth binding, customer-safe projection, and copy rules.
- `collaboration_workspace_contract.md` customer-visible lane separation and hidden activity side-channel rules where request activity overlaps collaboration.
- `authority_truth_and_internal_projection_separation_contract.md` and `authority_interaction_protocol.md` where authority truth states shape customer-facing timeline copy.
- `schemas/client_timeline_event.schema.json`, `customer_safe_projection_contract.schema.json`, `portal_language_contract.schema.json`, `client_portal_workspace.schema.json`, `authority_truth_contract.schema.json`.

Deliverables to create or patch:
1. `apps/client-portal-web/src/routes/ActivityRoute.tsx` if the repository has a route; otherwise `apps/client-portal-web/src/components/activity/PortalActivitySurface.tsx` used by Home/detail routes
2. `apps/client-portal-web/src/components/activity/ActivityTimeline.tsx`
3. `apps/client-portal-web/src/components/activity/ActivityEventRow.tsx`
4. `apps/client-portal-web/src/components/activity/AuthorityTruthLabel.tsx`
5. `packages/frontend-shell-core/src/portal/client_timeline_presenter.ts`
6. `tests/unit/frontend-shell-core/client_timeline_presenter.spec.ts`
7. `tests/playwright/frontend/client_portal_activity_customer_safe_timeline.spec.ts`
8. `docs/frontend/client_portal_activity_timeline.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Activity should be a calm paper trail: newest-first rows in a recessed white card, each with event kind, plain-language headline, date, related task link, and authority/settlement posture if relevant. Use a small vertical timeline rule only when it clarifies order; avoid audit-tape density.
- For pending authority or submission states, show a discrete `Waiting for confirmation` style chip and explanation, not a green success state. Hidden or limited activity uses an explicit limitation row rather than a missing gap.

Execution requirements:
- render only `ClientTimelineEvent.visible_to_client = true` entries and customer-safe fields
- sort newest-first by `occurred_at` with unique `event_id`; dedupe reconnect/live replay by event identity
- translate authority truth states into customer-safe labels without claiming final resolution until truth confirms
- link event rows back to the owning portal route and focus anchor using same-shell return rules
- make hidden/limited activity explicit through content limitation notices only when the published contract says a limitation is visible

Functional, architectural, and edge-case gaps to close:
- close the gap where portal activity can become an internal audit trail
- close the gap where hidden staff activity perturbs customer timeline counts/order
- close the gap where pending authority acknowledgements are rendered as final success
- close the gap where activity deep links lose the originating focus anchor

Mandatory edge cases:
- authority pending, unknown, rejected, out-of-band, or correction in flight
- reconnect replays duplicate timeline events
- activity event points to object now hidden or superseded
- masked/withheld activity exists
- same timestamp tie-breaking
- mobile/narrow route from an activity row into request detail

Playwright, accessibility, and validation requirements:
- unit-test timeline ordering, dedupe, authority truth label mapping, customer-safe field filtering, and route link derivation
- Playwright-test newest-first timeline, limited activity notice, authority pending/rejected labels, event deep-link focus restoration, reconnect replay dedupe, keyboard navigation, and reduced-motion row insertion
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- portal activity is customer-safe, bounded, and authority-truth-aware
- hidden staff context cannot leak through order, labels, or counts
- Playwright proves timeline rendering, deep links, and reconnect behavior
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
