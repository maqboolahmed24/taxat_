---
id: pc_0221
slug: phase_04_parallel_wave_02_track_backend_release_resilience_003_implement_schema_migration_ledger_and_reader_window_blocking_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_release_resilience"
task_number: "003"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_release_resilience_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0221

## Task Summary

003 implement schema migration ledger and reader window blocking rules.

## Metadata

- ID: `pc_0221`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend release resilience`
- Task Number: `003`
- Source Slug: `phase_04_parallel_wave_02_track_backend_release_resilience_003_implement_schema_migration_ledger_and_reader_window_blocking_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0220](./pc_0220.md) `phase_04_parallel_wave_02_track_backend_release_resilience_002_implement_deployment_release_model_canary_health_summary_and_rollout_state_machine`
- Next: [pc_0222](./pc_0222.md) `phase_04_parallel_wave_02_track_backend_release_resilience_004_implement_schema_bundle_compatibility_gate_contract_and_release_verification_manifest_assembly`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0214_to_0221.md](../shared_operating_contract_0214_to_0221.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_release_resilience_003_implement_schema_migration_ledger_and_reader_window_blocking_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0066`, `pc_0219`, and `pc_0220` plus any earlier schema / replay / restore work if they exist.

Mission:
Implement the canonical persistence and enforcement layer for `SchemaMigrationLedger`, `SchemaReaderWindowContract`, and the grouped reader-window blocking rules that govern destructive change, rollback safety, fail-forward posture, replay/restore readability, and protected historical manifests.
This task is not allowed to treat schema migration chronology as runbook prose or to leave reader-window closure as an implied operational convention.

Source algorithm grounding you must honor:
- `deployment_and_resilience_contract.md`, especially schema/datastore migration rules and rollback boundaries.
- `release_candidate_identity_and_promotion_evidence_contract.md`, especially the compatibility-gate contract boundary.
- `verification_and_release_gates.md`, especially schema compatibility and migration verification evidence.
- `data_model.md`, especially `SchemaMigrationLedger`, `SchemaReaderWindowContract`, and `BackfillExecutionContract`.
- `modules.md`, especially the schema-evolution notes requiring callers to respect migration-ledger and reader-window contracts.
- `schemas/schema_migration_ledger.schema.json`, `schemas/schema_reader_window_contract.schema.json`, `schemas/backfill_execution_contract.schema.json`, `schemas/state_transition_contract.schema.json`, and `schemas/schema_bundle_compatibility_gate_contract.schema.json`.
- `scripts/validate_contracts.py`, especially `validate_schema_migration_ledger`, `validate_schema_reader_window_contract`, `validate_backfill_execution_contract`, `schema_migration_ledger_phase_and_chronology_posture`, `schema_migration_ledger_reader_window_and_backfill_posture`, `schema_reader_window_contract_writer_not_supported`, `schema_reader_window_contract_protected_not_subset`, and `backfill_execution_contract_required_without_affected_artifacts`.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially vectors around destructive change before window closure, invalid protected historical bundles, and rollback after compatibility closure.

Deliverables to create:
1. `packages/backend-release/src/models/schema_reader_window_contract.ts`
2. `packages/backend-release/src/models/backfill_execution_contract.ts`
3. `packages/backend-release/src/models/schema_migration_ledger.ts`
4. `packages/backend-release/src/services/validate_schema_reader_window_rules.ts`
5. `packages/backend-release/src/services/advance_schema_migration_phase.ts`
6. `packages/backend-release/src/repositories/schema_migration_ledger_repository.ts`
7. `packages/backend-release/src/tests/schema_reader_window_contract.spec.ts`
8. `packages/backend-release/src/tests/schema_migration_ledger.spec.ts`
9. `docs/release/schema_migration_ledger_and_reader_window_rules.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/release/reader-window-and-migration-lab.tsx`

You MUST patch any existing migration or release code that currently:
- treats reader-window safety as a string flag without the first-class contract,
- allows contract/destructive phases to proceed without lawful window closure,
- or allows rollback/fail-forward posture to drift outside the grouped compatibility contract.

Required design decisions you must make explicitly:
- define the legal `phase_state` chronology for `SchemaMigrationLedger`
- define how `contract_phase_required`, `halted_subphase`, `verified_at`, and `compatibility_window_closed_at` interlock
- define the exact semantics of `supported_reader_schema_bundle_hashes[]` and `protected_historical_schema_bundle_hashes[]`
- define how `destructive_change_policy`, `rollback_boundary_policy`, `fail_forward_policy`, and `replay_restore_policy` are evaluated at runtime
- define how the migration ledger reuses or mirrors the shared `SchemaBundleCompatibilityGateContract` rather than drifting into a second compatibility grammar
- define how required versus non-required backfill is represented and validated

Execution requirements:
- persist `SchemaReaderWindowContract` as the authoritative grouped compatibility-window law
- persist `BackfillExecutionContract` as the authoritative execution / idempotency / lineage law for backfills
- persist `SchemaMigrationLedger` with explicit chronology, state-transition contract, reader-window contract, and backfill execution contract
- require `writer_schema_bundle_hash` to be included in `supported_reader_schema_bundle_hashes[]`
- require `protected_historical_schema_bundle_hashes[]` to remain a subset of the supported-reader set and to exclude the current writer bundle
- block destructive / contract phases until the reader window is lawfully closed
- block rollback once the reader-window policy says rollback is no longer allowed and fail-forward becomes mandatory
- preserve replay/restore readability posture explicitly; do not infer it from bundle names or timestamps

Gap handling you must close while implementing:
- close any gap where reader-window safety is tracked in notes or deployment config but not persisted as a first-class contract
- close any gap where destructive change can proceed before lawful closure of the compatibility window
- close any gap where optional-contract migrations drift into contracting/contracted phases illegitimately
- close any gap where protected historical bundles are not validated against the supported-reader set
- close any gap where rollback remains available after the window has closed and the system should fail forward instead
- close any gap where backfill requirements are ambiguous or missing affected artifact types

Mandatory edge cases:
- the writer bundle must always be in the supported-reader set
- protected historical bundles must be historical only and must remain a subset of the supported-reader set
- `verified_at` must stay aligned with post-verification phases only
- optional-contract migrations must not drift into `CONTRACTING` or `CONTRACTED`
- reader-window closure must be explicit before destructive contract posture becomes legal
- backfill required posture must carry affected artifact types and lawful execution state

Playwright and contract-test requirements:
- add direct tests for reader-window subset rules, rollback boundary behavior, fail-forward posture, and migration-phase chronology
- if you expose or patch a release/debug route, add Playwright `APIRequestContext` coverage for reader-window retrieval, migration-ledger retrieval, blocked destructive posture, and blocked rollback posture
- if you add the optional lab, add Playwright browser tests for semantic-locator navigation, phase-rail ordering, reader-window matrix rendering, reduced-motion-safe expansion, and sidecar focus behavior using semantic locators only
- capture traces on failure

UI / route-consumption requirements if a browser lab or route patch is touched:
- use a three-band layout: compatibility-window ribbon, migration phase rail, narrow destructive/rollback blocker sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; info accent `#1D4ED8`; continuity accent `#0F766E`; caution `#B7791F`; danger `#C2410C`
- emphasize chronology, protection sets, and blocking rules rather than generic metrics or charts
- animate only opacity / translate / height; respect reduced motion

Backend, performance, and security requirements:
- migration and reader-window transitions must be transactional and replay-safe
- compatibility-window state must remain queryable for release, replay, restore, and historical-manifest checks
- repositories must preserve chronology and explicit halt/failure evidence rather than overwriting it
- never infer legal historical readability from “latest bundle wins” logic

Validation rules:
- validate every `SchemaReaderWindowContract`, `BackfillExecutionContract`, and `SchemaMigrationLedger` payload against schema before persistence
- update fixtures/builders so all relevant validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one authoritative backend path for reader-window law and migration chronology
- destructive-change blocking, rollback/fail-forward boundaries, and backfill posture are durable and test-covered
- optional labs, if added, are read-only reflections of persisted truth rather than alternate implementations
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
