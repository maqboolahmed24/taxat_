---
id: pc_0257
slug: phase_05_parallel_wave_03_track_frontend_portal_005_Playwright_or_other_appropriate_tooling_implement_portal_approval_pack_route_and_stale_view_signoff_guard
mode: parallel
phase: "05"
wave: "03"
track: "frontend_portal"
task_number: "005"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_portal_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0257

## Task Summary

005 Playwright or other appropriate tooling implement portal approval pack route and stale view signoff guard.

## Metadata

- ID: `pc_0257`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend portal`
- Task Number: `005`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_portal_005_Playwright_or_other_appropriate_tooling_implement_portal_approval_pack_route_and_stale_view_signoff_guard`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0256](./pc_0256.md) `phase_05_parallel_wave_03_track_frontend_portal_004_Playwright_or_other_appropriate_tooling_implement_resumable_upload_flow_with_checksum_scan_and_rebase_states`
- Next: [pc_0258](./pc_0258.md) `phase_05_parallel_wave_03_track_frontend_portal_006_Playwright_or_other_appropriate_tooling_implement_portal_onboarding_route_and_one_step_at_a_time_progression`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_portal_005_Playwright_or_other_appropriate_tooling_implement_portal_approval_pack_route_and_stale_view_signoff_guard`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0256` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the portal Approval Pack route and stale-view sign-off guard so clients review, acknowledge, step up, sign, and receive receipts inside one same-shell flow.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The Approvals route answers four questions in order: what the client is reviewing, what changed, what they are declaring, and how they sign/what happens next. Signing is lawful only when the current approval pack, view guard, acknowledgements, declaration, step-up freshness, and readiness score meet the frozen threshold. A stale or superseded pack must never keep `Sign now` promoted.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md` Approvals route, approval/sign-off flow, artifact/print/browser handoff, content/copy, trust moments, and read-model/API translation.
- `frontend_shell_and_interaction_law.md` artifact preview/export/print, external handoff, focus, and motion law.
- `northbound_api_and_session_contract.md` portal command families, command receipts, stale-view rules, and session/step-up rules.
- `schemas/client_approval_pack.schema.json`, `client_portal_workspace.schema.json`, `artifact_selection_contract.schema.json`, `artifact_affordance_contract.schema.json`, `command_envelope.schema.json`, `api_command_receipt.schema.json`.

Deliverables to create or patch:
1. `apps/client-portal-web/src/routes/ApprovalsRoute.tsx`
2. `apps/client-portal-web/src/components/approvals/ApprovalSummaryPanel.tsx`
3. `apps/client-portal-web/src/components/approvals/ChangeDigestPanel.tsx`
4. `apps/client-portal-web/src/components/approvals/DeclarationPanel.tsx`
5. `apps/client-portal-web/src/components/approvals/SignOffPanel.tsx`
6. `packages/frontend-shell-core/src/portal/approval_pack_presenter.ts`
7. `packages/frontend-shell-core/src/portal/approval_signoff_guard.ts`
8. `tests/unit/frontend-shell-core/approval_signoff_guard.spec.ts`
9. `tests/playwright/frontend/client_portal_approval_pack_signoff_guard.spec.ts`
10. `docs/frontend/client_portal_approval_pack_route.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Build a compact review dossier: `Approval summary` card, `What changed` digest, `Declaration` reader, and `Sign-off` card. Use a narrow step rail on desktop and a stacked numbered flow on mobile.
- Declaration preview/download/print controls sit in the declaration panel header and are labelled `Current declaration`, `Historical pack`, or `Signed receipt` as appropriate. Step-up appears as an inline checkpoint within `SIGN_OFF_PANEL`, not a route-breaking modal unless the external provider mandates it.

Execution requirements:
- preserve route order exactly: `APPROVAL_SUMMARY -> CHANGE_DIGEST -> DECLARATION_PANEL -> SIGN_OFF_PANEL`
- derive sign-off readiness from current pack freshness, view guard, material-change acknowledgement, declaration acknowledgement, and step-up freshness; never from local checkbox state alone
- bind declaration preview/download/print and final receipt targets to `artifact_selection` and `artifact_affordance`
- use command envelopes and durable receipts for signature submission; show pending settlement separately from final receipt
- handle contained step-up checkpoint and external identity handoff return without losing approval-pack context

Functional, architectural, and edge-case gaps to close:
- close the gap where declaration exports can masquerade as signed receipts
- close the gap where stale pack history still promotes `Sign now`
- close the gap where local acknowledgements survive a pack rebase as if still signable
- close the gap where step-up opens a detached flow and loses declaration context

Mandatory edge cases:
- no material changes but digest still needs compact summary
- stale/superseded/expired pack opened from notification
- step-up required, expired, failed, or returned from system browser
- signature command receipt pending, failed, or settled
- historical approval pack preview/download
- readiness score below threshold

Playwright, accessibility, and validation requirements:
- unit-test readiness guard, stale protection, artifact target selection, receipt states, and step-up freshness
- Playwright-test route order, acknowledgement sequence, stale rejection, latest-pack redirect, inline step-up, pending signature receipt, final receipt, print/download labels, keyboard-only signing, reduced motion, and mobile stack
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- approval route supports safe current-pack review and signing
- stale/superseded packs fail closed with clear recovery
- Playwright proves sign-off guard, receipts, handoff return, accessibility, and current/history separation
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
