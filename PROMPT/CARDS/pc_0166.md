---
id: pc_0166
slug: phase_03_parallel_wave_01_track_backend_northbound_010_implement_audit_trail_enquiry_pack_and_investigation_query_endpoints
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "010"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0166

## Task Summary

implement audit trail enquiry pack and investigation query endpoints.

## Metadata

- ID: `pc_0166`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `010`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_010_implement_audit_trail_enquiry_pack_and_investigation_query_endpoints`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0165](./pc_0165.md) `phase_03_parallel_wave_01_track_backend_northbound_009_implement_collaboration_work_item_snapshot_stream_activity_and_attachment_endpoints`
- Next: [pc_0167](./pc_0167.md) `phase_03_parallel_wave_01_track_backend_northbound_011_implement_problem_envelope_mapping_stale_view_guards_and_if_match_validation`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0166_to_0173.md](../shared_operating_contract_0166_to_0173.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_010_implement_audit_trail_enquiry_pack_and_investigation_query_endpoints`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0165` if they exist, especially `pc_0126` through `pc_0130`, `pc_0134` through `pc_0141`, and `pc_0158` through `pc_0165`.

Mission:
Implement the read-only manifest and governance investigation surfaces that let operators and automation recover deterministic audit slices and retention-aware explanations without mutating truth.
At minimum this task must make `GET /v1/manifests/{manifest_id}/audit-trail`, `GET /v1/manifests/{manifest_id}/enquiry-pack?target_ref=...`, and `GET /v1/governance/tenants/{tenant_id}/audit-investigations` real northbound contracts backed by schema-valid `AuditInvestigationFrame` and `EnquiryPack` artifacts or by a tightly governed equivalent wrapper that still embeds those canonical artifacts unchanged.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially sections 1, 2, 5, and the route list that names the audit-trail and enquiry-pack surfaces.
- `observability_and_audit_contract.md`, especially `get_audit_trail(...)`, `get_run_timeline(...)`, and `get_filing_evidence_ledger(...)` plus the requirement that investigation slices freeze query contract, ordering basis, event refs, and supporting trace/log refs.
- `provenance_graph_semantics.md`, especially the deterministic enquiry-pack path and limitation rules.
- `retention_limited_explainability_and_audit_sufficiency_contract.md`.
- `data_model.md`, especially `AuditInvestigationFrame`, `EnquiryPack`, and related audit/export objects.
- `modules.md`, especially `QUERY_OBSERVABILITY_SLICE(...)` and the enquiry/proof generation logic.
- `schemas/audit_investigation_frame.schema.json`, `schemas/enquiry_pack.schema.json`, and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 76-80 and the problem-envelope recovery-family hardening items.

Deliverables to create:
1. `packages/backend-northbound/src/http/get_manifest_audit_trail_endpoint.ts`
2. `packages/backend-northbound/src/http/get_manifest_enquiry_pack_endpoint.ts`
3. `packages/backend-northbound/src/http/get_governance_audit_investigations_endpoint.ts`
4. `packages/backend-northbound/src/http/register_audit_and_enquiry_routes.ts`
5. `packages/backend-northbound/src/query/load_manifest_audit_investigation_frame.ts`
6. `packages/backend-northbound/src/query/load_manifest_enquiry_pack.ts`
7. `packages/backend-northbound/src/query/load_governance_audit_investigation_frame.ts`
8. `packages/backend-northbound/src/services/build_audit_query_contract.ts`
9. `packages/backend-northbound/src/services/authorize_audit_or_enquiry_read.ts`
10. `packages/backend-northbound/src/services/validate_enquiry_externalization_governance.ts`
11. `packages/backend-northbound/src/services/map_audit_query_filters_and_cursor.ts`
12. `packages/backend-northbound/src/tests/audit_and_enquiry_endpoints.spec.ts`
13. `docs/northbound/audit_and_enquiry_surfaces.md`
14. Optional but recommended: `apps/admin-console-web/src/routes/debug/audit-investigation-explorer.tsx`

Required design decisions you must make explicitly:
- define the exact query grammar for actor/event-family/client/manifest/object/authority filters, cursor pagination, focus anchors, and export posture without inventing a second, UI-only filter language
- choose when each surface returns `AUDIT_TRAIL` versus `RUN_TIMELINE` versus `FILING_EVIDENCE_LEDGER` in `query_contract_code`, and freeze that choice in one shared mapper
- define whether manifest audit-trail and governance audit-investigations share one frame shape plus route-specific policy wrappers, or whether route wrappers differ while the embedded `AuditInvestigationFrame` stays identical
- choose a lawful caching posture for audit and enquiry reads; default to fail-safe freshness over aggressive caching because masking, retention, or access posture may change independently of object identity
- define how enquiry-pack retrieval behaves when proof closure is absent, stale, or retention-limited: either return a schema-valid limited/failed pack or a typed `ProblemEnvelope` according to the algorithm branch, but never improvise a partial success

Execution requirements:
- route handlers must remain thin and push all query assembly into deterministic services/query modules
- keep audit evidence ordering server-authored; clients must never infer ordered events locally from timestamps or trace spans
- manifest audit-trail must remain manifest-scoped and visibility-scoped
- governance audit-investigations must remain control-plane/staff-scoped
- enquiry-pack retrieval must preserve masking posture, omission entries, limitation notes, lineage boundaries, authority refs, config refs, and override refs exactly as the algorithm defines them
- if you add the optional explorer, make it read-only and diagnostic; use one dominant event/pack column plus one restrained context rail, not a dashboard wall

Gap handling you must close while implementing:
- the source corpus assumes deterministic audit slices but does not give you a single HTTP query grammar; create one canonical filter/cursor contract and reuse it across manifest and governance routes
- the source corpus requires retention-aware enquiry packs but leaves room for teams to drop omission/limitation details during externalization; enforce them in one shared validator
- investigation routes risk drifting into telemetry timelines; explicitly keep `supporting_log_record_refs[]` secondary to `ordered_event_refs[]` and never let logs replace audit evidence
- if earlier cards did not expose a reusable proof/provenance lookup service, introduce one narrow adapter here instead of duplicating graph traversal logic in the endpoint

Mandatory edge cases:
- `AuditInvestigationFrame.frame_id`, `tenant_id`, `ordered_event_refs[]`, filter arrays, export posture reason codes, and `next_cursor` must reject empty-string values
- `ordering_basis` must be explicit and deterministic; do not let the frontend choose between stream sequence and recorded-at ordering
- `EnquiryPack.primary_path_ref` must appear in `critical_path_refs[]`
- `explanation_status = AVAILABLE` is unlawful if retention/omission/masking conditions materially limit the explanation
- customer-safe or masked audiences must not receive governance investigation frames or staff-only supporting refs
- any read failure must map to a typed `ProblemEnvelope` rather than a framework-default error page

Playwright and contract-test requirements:
- use Playwright APIRequestContext for route tests
- assert schema validity, filter/cursor stability, and deterministic ordering
- if you add the explorer, test it with semantic locators only; no CSS-chain selectors, no fixed sleeps
- capture request/response payloads and trace logs on test failure

Backend, performance, and security requirements:
- keep access control, masking, and retention logic server-side and deterministic
- index the event and pack lookups by tenant/manifest/object/filter frontier so paging does not degrade into full scans
- do not join unrelated manifests or authority packets into the same slice just because correlation keys happen to overlap; correlation neighborhoods must remain contract-governed
- do not permit export posture escalation from a read query parameter; export eligibility is derived from governance policy and the frame contract

Validation rules:
- validate every audit frame against `audit_investigation_frame.schema.json`
- validate every enquiry pack against `enquiry_pack.schema.json`
- validate every read failure against `problem_envelope.schema.json`
- unit-test query-contract mapping, cursor encoding/decoding, retention-limited pack rendering, and export-posture gating
- integration-test manifest audit, governance investigations, and enquiry-pack reads across staff/masked/forbidden audiences

Definition of done:
- the repo exposes deterministic audit-trail, enquiry-pack, and governance-investigation read surfaces
- audit slices preserve query contract, order, focus, cursor, and export posture without UI-local inference
- enquiry packs remain provenance-aware, masking-aware, and retention-aware under real failure pressure
- all three surfaces are read-only, replay-safe, schema-valid, and backed by one canonical HTTP contract rather than endpoint-by-endpoint improvisation.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

