---
id: pc_0183
slug: phase_04_parallel_wave_02_track_backend_portal_005_implement_client_approval_pack_projection_and_stale_view_signoff_guards
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "005"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0183

## Task Summary

implement client approval pack projection and stale view signoff guards.

## Metadata

- ID: `pc_0183`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `005`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_005_implement_client_approval_pack_projection_and_stale_view_signoff_guards`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0182](./pc_0182.md) `phase_04_parallel_wave_02_track_backend_portal_004_implement_upload_session_recovery_harness_generation_and_cross_device_resume_support`
- Next: [pc_0184](./pc_0184.md) `phase_04_parallel_wave_02_track_backend_portal_006_implement_client_onboarding_journey_projection_and_one_step_at_a_time_progression_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0182_to_0189.md](../shared_operating_contract_0182_to_0189.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_005_implement_client_approval_pack_projection_and_stale_view_signoff_guards`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0182` if they exist, especially `pc_0159`, `pc_0166`, `pc_0179`, and `pc_0182`.

Mission:
Implement the authoritative `ClientApprovalPack` projector plus the stale-view, signability, and externalization guard logic that keeps client review and sign-off lawful under rebase, supersession, step-up expiry, and receipt settlement.
This task is where the portal stops being a page of buttons and becomes a governed legal review surface.
The projector must state, in one place, whether the current pack is reviewable, signable, stale, superseded, step-up blocked, or receipt-complete.
It must prevent signing a superseded pack, prevent declaration exports from masquerading as receipts, and keep preview / download / print posture aligned to the current legal pack state.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially `Approvals`, `Approval and sign-off flow`, `Artifact, print, and browser-handoff rules`, `Read-model and API translation requirements`, and the Playwright validation minimum around stale-view rejection, step-up continuity, and current-vs-history targets.
- `northbound_api_and_session_contract.md`, especially portal command families, `if_match_approval_pack_hash`, stale-view failure handling, `latest_approval_pack_ref`, and typed stale-guard responses.
- `data_model.md`, especially `ClientApprovalPack`, `externalization_governance_contract`, and the distinction between declaration targets and issued receipt targets.
- `state_machines.md`, especially `ClientApprovalPack.lifecycle_state` and the cross-state invariants requiring current stale posture, readiness `>= 85`, and fresh step-up proof before signed states.
- `modules.md`, especially `BUILD_CLIENT_APPROVAL_CENTER(...)`.
- `frontend_shell_and_interaction_law.md`, especially stale posture law, artifact preview/export/print/handoff, and dominant-action hierarchy.
- `schemas/client_approval_pack.schema.json`, `schemas/artifact_selection_contract.schema.json`, `schemas/artifact_affordance_contract.schema.json`, `schemas/externalization_governance_contract.schema.json`, and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially the portal approval hardening items covering chronology, stale signability, readiness scoring, receipt separation, and stale-guard command semantics.
- `test_vectors.md`, especially approval stale-view, step-up, and receipt-state regressions.

Deliverables to create:
1. `packages/backend-portal/src/projectors/build_client_approval_pack.ts`
2. `packages/backend-portal/src/services/derive_approval_readiness_score.ts`
3. `packages/backend-portal/src/services/derive_approval_recovery_posture.ts`
4. `packages/backend-portal/src/services/build_approval_artifact_targets.ts`
5. `packages/backend-portal/src/services/validate_approval_pack_chronology.ts`
6. `packages/backend-portal/src/tests/client_approval_pack_projection_and_stale_guards.spec.ts`
7. `packages/backend-portal/src/tests/client_approval_commands_stale_signoff.spec.ts`
8. `docs/portal/client_approval_pack_projection_and_stale_signoff_guards.md`
9. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/approval-pack-lab.tsx`

You MUST patch any shipped portal approval-center builder or approval read endpoint so it consumes these services instead of deriving signability, step-up posture, or artifact targets locally.

Required design decisions you must make explicitly:
- define the exact boundary between `approval_pack_hash` and `view_guard_ref`, and how each participates in stale-view protection
- define how `stale_protection_state` becomes `CURRENT`, `REBASE_REQUIRED`, `SUPERSEDED`, or `EXPIRED`
- define when declaration preview/download/print is lawful, when receipt preview/download/print takes over, and how standalone artifact refs differ from workspace-embedded targets
- define the single source of truth for `approval_readiness_score` and its frozen formula inputs so no renderer recomputes signability heuristically
- define how step-up freshness, supersession, cancellation, and currentness map to `recovery_posture`

Execution requirements:
- publish a schema-valid `ClientApprovalPack` for every current portal approval artifact
- derive and persist `approval_readiness_score`, `stale_protection_state`, `recovery_posture`, and `dominant_hazard_code` from blocker state rather than from previously published UI posture
- keep review order exact: `APPROVAL_SUMMARY -> CHANGE_DIGEST -> DECLARATION_PANEL -> SIGN_OFF_PANEL`
- keep `change_digest_acknowledged_at`, `declaration_acknowledged_at`, `acknowledged_at`, `step_up_verified_at`, `step_up_expires_at`, and `signed_at` chronologically coherent
- signed or countersigned packs must require `stale_protection_state = CURRENT`, readiness `>= 85`, digest and declaration acknowledgement lineage, and required unexpired step-up proof
- stale, expired, superseded, or cancelled packs must demote sign actions and publish explicit recovery posture instead of leaving the portal to guess whether to reopen, rebase, retry step-up, or show receipt history
- preserve strict current-vs-history posture for declaration text, signed receipts, and superseded packs
- if you add the lab, make it a quiet single-column review page with one sticky action band at most; no split-screen compliance dashboard

Gap handling you must close while implementing:
- close any gap where declaration export and issued receipt export can be confused or share the same default target accidentally
- close any gap where stale-view rejection returns only generic failure text without the latest approval-pack ref and stale-guard basis
- close any gap where step-up expiry can leave the pack looking signable
- close any gap where `approval_acknowledged` or signed posture can outrun digest/declaration acknowledgement chronology
- close any gap where workspace-embedded preview/download/print targets drift from the visible primary subject and current-vs-history header posture

Mandatory edge cases:
- `CANCELLED` packs must clear `viewed_at` and `acknowledged_at`
- `stale_protection_state in {SUPERSEDED, EXPIRED}` must force readiness to `0`
- `SIGNED` and `COUNTERSIGNED` must keep `approval_readiness_score >= 85`
- `SIGNED` must not survive expired step-up proof when `requires_step_up = true`
- `recovery_posture` must be `NONE` only when the pack is truly actionable or fully settled
- non-current packs must not remain the default declaration preview or signing target
- issue the latest receipt target only after receipt state actually transitions to issued / settled posture
- if the pack is rebased after view or acknowledgement, preserve read-only review context but block stale mutation and stale sign-off

Playwright and contract-test requirements:
- add direct unit tests for readiness scoring, stale-protection mapping, chronology validation, externalization alignment, and receipt-vs-declaration target selection
- if northbound approval commands exist, add Playwright APIRequestContext tests showing stale sign-off rejection includes `latest_approval_pack_ref`, stale-guard metadata, and one typed recovery surface code
- if you add the debug lab, add Playwright browser tests that cover review progression, stale rebase, step-up retry, and receipt-state takeover using user-facing locators only
- capture traces and serialized approval payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- max width `1180px`, background `#F6F4EE`, primary surface `#FFFFFF`, muted surface `#F3F4F6`
- typography-led hierarchy with a 12-column grid but one dominant content column
- the header must show plain-language title, current/history posture chip, and one due / timing line if present
- `CHANGE_DIGEST` should be concise first and expandable second; never a second hero card
- the declaration preview should read like a quiet document sheet, not an embedded code viewer
- the sign-off panel may be sticky at desktop but must remain subordinate until the pack becomes signable
- use restrained icons only where they disambiguate `review`, `step-up`, `receipt`, or `history`
- animations: 160ms, opacity / translate only, reduced motion mandatory

Backend, performance, and security requirements:
- keep signability, stale-view, and step-up legality server-side; the client may only render the published posture
- do not let sign-off commands infer currentness from the most recently fetched card alone; use authoritative pack hash and stale guards
- make the projector cheap to recompute from durable approval truth and current policy / step-up state
- fail closed on stale pack hashes, missing acknowledgement lineage, or ambiguous externalization targets

Validation rules:
- validate every emitted `ClientApprovalPack`, `artifact_selection`, `artifact_affordance`, and `externalization_governance_contract` against schema
- add regression coverage for cancellation cleanup, stale pack rebase, expired step-up, receipt takeover, and declaration / receipt target drift
- keep standalone artifact semantics and workspace-embedded target semantics explicit; do not blur the two surfaces

Definition of done:
- the repo has one authoritative approval-pack projector and one stale-signoff guard path
- signability, preview/download/print targets, and recovery posture are now governed backend truth rather than renderer guesswork
- the portal can review, rebase, step-up, sign, and settle inside one lawful same-shell approval flow
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
