---
id: pc_0083
slug: phase_02_seq_083_create_baseline_access_control_matrix_and_default_role_seed_data
mode: sequential
phase: "02"
wave: null
track: null
task_number: "083"
block_key: phase_02_seq
original_key: phase_02_seq_083
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0083

## Task Summary

create baseline access control matrix and default role seed data.

## Metadata

- ID: `pc_0083`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `083`
- Source Slug: `phase_02_seq_083_create_baseline_access_control_matrix_and_default_role_seed_data`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0082](./pc_0082.md) `phase_02_seq_082_create_repository_task_runner_makefile_and_agent_entrypoint_scripts`
- Next: [pc_0084](./pc_0084.md) `phase_02_seq_084_create_release_branching_versioning_and_artifact_naming_conventions`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0078_to_0085.md](../shared_operating_contract_0078_to_0085.md)
```text
Execute roadmap task `phase_02_seq_083_create_baseline_access_control_matrix_and_default_role_seed_data`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0082` if they exist, especially `pc_0010`, `pc_0011`, `pc_0015`, `pc_0016`, `pc_0017`, `pc_0021`, `pc_0024`, `pc_0064`, `pc_0065`, `pc_0077`, `pc_0081`, and `pc_0082`.

Mission:
Create the first governed access-control substrate for the product: default role seed data, a machine-readable resource/action matrix, policy snapshot and seed loaders, and the first premium governance access route that makes layered authority legible.
This task must not implement “RBAC only.”
It must preserve the Taxat actor model: tenant operational authority, client delegation, external authority links, exceptional authority, and optional authority-of-record precedence all remain distinct.

Source algorithm grounding you must honor:
- `actor_and_authority_model.md`, especially `3.4 Authority layers`, `3.6 Principal context schema`, `3.7 Resource classes`, `3.8 Action families`, `3.9 Policy decision model`, `3.11 Non-delegable and step-up actions`, `3.12 Authority precedence rules`, and `3.15 Frontend and governance-console rendering contract`.
- `modules.md`, especially the `AUTHORIZE(...)` and governance mutation semantics.
- `data_model.md`, especially `PrincipalAccessView`, `RoleTemplateMatrix`, and `AuthorizationDecision` expectations.
- `admin_governance_console_architecture.md`, especially `4.3 /governance/access`, `5. Domain translation rules`, `6. Shared interaction and mutation rules`, and `10. Minimum semantic selectors`.
- `northbound_api_and_session_contract.md`, especially the rules that effective principal, delegation basis, and masking posture are resolved before route composition.
- Existing schemas including `principal_access_view.schema.json`, `role_template_matrix.schema.json`, `principal_context.schema.json`, and `authorization_decision.schema.json`.

Deliverables to create:
1. `config/access/default_roles.json`.
2. `config/access/resource_action_catalog.json`.
3. `config/access/access_control_matrix.json`.
4. `config/access/step_up_policy.json`.
5. `config/access/approval_policy.json`.
6. `packages/access-control/src/role_seed_loader.ts`.
7. `packages/access-control/src/access_matrix_compiler.ts`.
8. `packages/access-control/src/principal_access_projection_builder.ts`.
9. `packages/access-control/src/governance_access_simulator.ts`.
10. `docs/access/baseline_access_control_matrix.md`.
11. `apps/operator-web/src/routes/governance/access/index.tsx`.
12. `apps/operator-web/src/routes/governance/access/simulator.tsx`.
13. `tests/unit/access/access_matrix_compiler.spec.ts`.
14. `tests/integration/access/default_role_seed_and_projection.spec.ts`.
15. `tests/playwright/governance/access_matrix_and_simulator.spec.ts`.

Required design decisions you must make explicit:
- Define the baseline internal role templates and justify each one against the admin-governance roles documented in the corpus.
- Define the complete resource-class and action-family catalog used by the matrix and ensure it traces to the actor model and later workflow surfaces.
- Decide how matrix cells encode `ALLOW`, `ALLOW_MASKED`, `REQUIRE_STEP_UP`, `REQUIRE_APPROVAL`, and `DENY` while preserving reason codes and effective scope lineage.
- Define which actions are non-delegable, which require step-up, which require approval, and which always fail closed without fresh authority-link or delegation evidence.
- Define how default role seed data versioning works and how policy snapshot hashes are computed deterministically.
- Define how the matrix distinguishes internal tenant permissions from external delegation and authority-link readiness instead of collapsing them into one checkbox grid.

Execution steps:
1. Materialize a machine-readable catalog of resource classes, action families, masking rules, partition-scope rules, step-up policies, and approval requirements.
2. Encode baseline role templates in seed files with deterministic ordering, version hashes, and source lineage.
3. Build a compiler that turns role templates and policy files into a deterministic access matrix plus supporting read-side projections.
4. Implement projection builders for `PrincipalAccessView`, `RoleTemplateMatrix`, and simulator payloads consistent with the corpus schemas and UI route expectations.
5. Build the `/governance/access` route and simulator so operators can inspect one principal or role, see the stacked authority layers, read the matrix, and understand why a cell resolves to its posture.
6. Add tests for role seeding, projection generation, masking rules, step-up and approval semantics, and route rendering.

Visual and interaction specification for `/governance/access`:
- Tone: a premium governance instrument panel.
  It should feel like a precise legal-control surface, not an admin CRUD table.
- Layout:
  - `68px` top bar with route title, policy snapshot hash chip, and current authority-stack summary.
  - left principal / role rail `272-296px` with segmented tabs for `PRINCIPALS`, `ROLES`, `SIMULATOR`.
  - center workspace with a layered authority ribbon on top, an action matrix in the middle, and a selected-cell detail panel below.
  - right sidecar `360-400px` for reason codes, effective scope, masking rules, required approvals, and policy path.
- Signature visual language:
  - the hero motif is a five-band authority ribbon stacked horizontally: legal subject, delegated client, tenant operational authority, exceptional authority, optional authority-of-record override.
  - matrix cells use restrained semantic fills and thin rule emphasis, not saturated red/green blocks.
  - include a subtle “lattice key” glyph near the heading built from a grid and one rotated key tooth.
- Palette:
  - background `#F6F4F0`
  - primary surface `#FFFFFF`
  - secondary surface `#EEECE7`
  - ink `#0F1418`
  - muted ink `#67707A`
  - hairline `rgba(15,20,24,0.08)`
  - accent indigo `#495C7A`
  - accent sage `#62715D`
  - accent amber `#8B693A`
  - allow `#1F6A53`
  - masked `#567063`
  - step-up `#87612A`
  - approval `#7A5A8C`
  - deny `#A63B31`
- Typography:
  - data-dense but elegant.
  - upper labels small and widely tracked.
  - cell text concise; detailed reasoning lives in the inspector.
  - monospace only for policy hashes, role IDs, scope refs, and snapshot identifiers.
- Dimensional constraints:
  - max width `1540px`
  - matrix viewport min height `520px`
  - cell min size `44x44px`
  - selected-cell detail panel min height `180px`
- Motion:
  - `120ms` cell focus transition.
  - `160ms` authority-ribbon emphasis on selected cell.
  - reduced-motion mode uses static contrast and border changes only.
- Interaction:
  - selecting a role or principal must preserve focus in the matrix and update all three reading zones.
  - selecting a cell must reveal exact decision posture, reason codes, effective scope, partition scope, masking, approval rules, and required authn level.
  - simulator inputs must let the operator change role set, delegation presence, authority-link state, and requested action, then see the layered explanation update.
  - accessible names should read as statements such as `tenant admin may submit filing only with step up and linked authority evidence`.
  - Playwright must prove keyboard navigation across the matrix, ribbon, and simulator using role- and label-first locators.

Component hierarchy hint:
~~~ts
<GovernanceShell route="/governance/access">
  <PrincipalRoleRail />
  <AuthorityRibbon />
  <ActionMatrix />
  <SelectedActionDetail />
  <ReasonCodeSidecar />
</GovernanceShell>
~~~

Gap handling you must perform:
- Close the gap where the actor model is specified but there is no concrete baseline role seed and matrix artifact.
- Close the gap where UI or downstream APIs might otherwise flatten layered authority into generic RBAC.
- Close the gap where step-up and approval semantics are described but not serialized into reusable policy files.
- Close the gap where governance operators have no inspectable surface for why a permission posture exists.
- Close the gap where default roles drift informally across tenants or environments.

Mandatory edge cases:
- role allows action but client delegation is missing
- action is allowed only in masked form
- action requires approval despite role membership
- action requires step-up because of exceptional sensitivity
- authority link exists but is stale or missing required partition scope
- service principal evaluated in the simulator with human-only action family
- imported authorisation stale relative to policy snapshot hash
- principal has multiple roles whose combined outcome must still fail closed

Backend, performance, and security requirements:
- Default posture is deny unless explicit source-truth policies say otherwise.
- Seed data must be deterministic, diff-friendly, and hashable.
- Matrix compilation must preserve reason-code lineage and should not rely on UI-only logic.
- Governance routes are inspection and simulation surfaces; they must not silently commit policy mutations.
- No route may render raw secrets, signed tokens, or unsafe principal attributes.

Validation rules:
- Unit-test role seeding, matrix compilation, decision posture precedence, masking, step-up, approval, and partition-scope logic.
- Integration-test projection building for `PrincipalAccessView` and `RoleTemplateMatrix`.
- Validate any generated read models against the imported schemas.
- Use Playwright to verify the access route, simulator route, keyboard matrix navigation, reduced-motion behavior, and semantic selectors.
- Prove that default deny holds when any required authority layer or policy input is absent.

Definition of done:
- The repo has a governed default role and access-matrix baseline.
- Layered authority semantics are preserved end to end.
- Governance operators can inspect and simulate access outcomes through a premium, low-noise route instead of generic admin tables.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

