---
id: pc_0021
slug: phase_00_seq_021_author_architecture_decision_record_for_identity_step_up_and_session_model
mode: sequential
phase: "00"
wave: null
track: null
task_number: "021"
block_key: phase_00_seq
original_key: phase_00_seq_021
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0021

## Task Summary

author architecture decision record for identity step up and session model.

## Metadata

- ID: `pc_0021`
- Mode: `sequential`
- Phase: `00`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `021`
- Source Slug: `phase_00_seq_021_author_architecture_decision_record_for_identity_step_up_and_session_model`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0020](./pc_0020.md) `phase_00_seq_020_author_architecture_decision_record_for_storage_and_eventing_topology`
- Next: [pc_0022](./pc_0022.md) `phase_00_seq_022_author_architecture_decision_record_for_authority_integration_boundary`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt

Use the shared operating contract first:
[../shared_operating_contract_0014_to_0021.md](../shared_operating_contract_0014_to_0021.md)

```text
Execute roadmap task `phase_00_seq_021_author_architecture_decision_record_for_identity_step_up_and_session_model`.

Inherit the shared operating contract and consume the outputs from cards `pc_0001` through `pc_0020` if they exist.

Mission:
Author the ADR that selects the identity, step-up, and session model for Taxat.
The source corpus is unusually explicit about actor classes, delegation, authority binding, non-delegable actions,
browser versus native session posture, anti-CSRF, device-local storage, invite/deep-link upgrade rules, and step-up invalidation.
You must compare real alternatives and choose a secure, usable, contract-aligned model that protects authority interactions,
preserves same-object continuity, and works across browser, native macOS, and machine automation clients.

Source algorithm grounding you must honor:
- `actor_and_authority_model.md -> Actor classes`, `Authority layers`, `Actor-to-authority relationships`,
  `Principal context schema`, `Action families`, `Policy decision model`, `Delegation rules`,
  `Non-delegable and step-up actions`, `Exceptional authority controls`, `Authority precedence rules`,
  `Machine-actor rules`, and `Frontend and governance-console rendering contract`.
- `northbound_api_and_session_contract.md -> Required northbound surfaces`, `Command envelope`,
  `Command receipt`, `Concurrency and stale-view rules`, `Stream and reconnect rules`,
  and `Session, browser, and native-client rules`.
- `security_and_runtime_hardening_contract.md -> Identity, session, and command trust`,
  `Secret, key, and token handling`, `Browser, native-client, API, and transport hardening`,
  and `Operational security release gates`.
- `customer_client_portal_experience_contract.md -> Approval and sign-off flow`, `Security and trust moments`,
  and `Playwright validation minimum`.
- `macos_native_operator_workspace_blueprint.md -> Authentication and session strategy`,
  `Persistence model`, `Security and runtime posture for the desktop client`,
  and `Native UX opportunities that should replace browser habits`.
- `authority_interaction_protocol.md -> AuthorityBinding`, `AuthorityRequestEnvelope`,
  `Token and client binding rule`, `Fraud-prevention header rule`, and `Submission-state write rules`.
- `PROMPT/Checklist.md` roadmap tasks covering IdP setup, step-up/session policies, and later authority-boundary work.

Deliverables to create:
1. `docs/architecture/adr/ADR-003-identity-step-up-and-session-model.md`
2. `docs/architecture/adr/ADR-003-identity-step-up-and-session-model-comparison.md`
3. `docs/architecture/adr/ADR-003-identity-step-up-and-session-model-scorecard.json`
4. `data/analysis/session_flow_matrix.json`
5. `data/analysis/step_up_trigger_and_invalidation_matrix.json`
6. `data/analysis/browser_native_automation_identity_boundary.json`
7. `data/analysis/deep_link_invite_and_resume_rules.json`
8. `diagrams/analysis/ADR-003-identity-session-topology.mmd`

Required alternatives:
1. Secure browser session model using modern OIDC/OAuth-style human authentication,
   secure server-side or equivalent protected sessions with anti-CSRF, system-browser native auth,
   and distinct short-lived machine credentials for automation.
2. Bearer-token-in-browser-storage or SPA-centric client-token model with minimal server session state.
3. Embedded-webview or blended token model for native/browser flows.
You may add more, but do not evaluate fewer than three.

Expected recommended direction to test rigorously:
- Browser humans authenticate through a standards-based identity provider flow and operate through secure session cookies
  or an equally protected server-mediated session posture with anti-CSRF.
- Native macOS uses system-browser authentication (`ASWebAuthenticationSession`-style posture) and device-local secure storage
  such as Keychain for allowed session material, never raw authority credentials.
- Automation clients use distinct, short-lived machine credentials and are not interchangeable with human interactive sessions.
- Step-up rotates effective challenge state and invalidates affected commands, cursors, or resume artifacts as the contract requires.
If the evidence rejects any of this, explain why.

Execution steps:
1. Derive decision criteria from the corpus:
   actor/authority fit, delegation, step-up posture, browser security, native security, deep-link continuity,
   invite onboarding, authority-binding safety, machine-actor separation, revocation behavior, DX/operability, and auditability.
2. Enumerate the exact step-up and non-delegable actions from the source corpus and turn them into a trigger matrix.
3. Build the session-flow matrix covering:
   - browser human operator,
   - browser portal user,
   - native macOS operator,
   - machine automation client,
   - invite/deep-link recipient before upgrade,
   - upload-session or limited-context flows if the corpus allows them.
4. Build the invalidation matrix showing what must be rotated, purged, or revalidated on:
   - step-up completion,
   - session revocation,
   - logout,
   - tenant switch,
   - authority rebinding,
   - stale-view rebase,
   - deep-link re-entry after expiry.
5. Compare alternatives with weighted criteria and explicit rejection reasons.
6. State how the chosen model supports browser, native, and automation simultaneously without leaking credentials or collapsing trust boundaries.
7. Fence deferred decisions that belong to later ADRs or implementation tasks rather than smuggling them in.

Gap handling you must perform:
- Close the “identity/session posture is implied across many docs but not centrally decided” gap.
- Close the “step-up triggers and invalidation fallout are not in one machine-readable matrix” gap.
- Close the “browser, native, and automation identities can be conflated” gap.
- Close the “deep-link, invite, upload-session, and resume flows need exact upgrade rules” gap.
- Close the “later authority-boundary and UI implementation work needs a stable identity model” gap.

Mandatory edge cases:
- Non-delegable or high-trust actions named by the corpus must explicitly require step-up or exceptional handling.
- Invite, deep-link, upload, acknowledgement, and approval entry flows may begin in limited posture but must upgrade into the proper authenticated session before sensitive mutation.
- Step-up completion must rotate effective challenge state and invalidate any commands, resume tokens, upload-session artifacts,
  or cursors the contract says can no longer be trusted.
- Browser writes must not rely on bearer tokens in unsafe browser storage if the corpus requires secure session posture and anti-CSRF.
- Native clients must not store raw authority credentials on device.
- Machine credentials must remain distinct from human sessions and must not bypass human step-up rules for human-only actions.
- Tenant switch, logout, and authority rebinding must purge incompatible caches and session-derived state.
- Same-object / same-shell continuity must survive step-up handoff where the corpus requires it; provider-owned checkpoints may hand off externally,
  but the user must return to the same object context.
- Revocation and stale-view behavior must compose cleanly with reconnect and recovery rules.

Architecture, backend, and security requirements:
- Use a professional ADR structure with weighted criteria and explicit consequences.
- Discuss browser security (XSS/CSRF posture), native security, automation posture, and operational complexity honestly.
- Keep session and token terminology precise.
- Make the chosen posture auditable and implementable with common identity-provider capabilities, but do not let vendor-specific features override the source contract.

Validation rules:
- At least three serious alternatives must be compared.
- Every actor/channel flow must appear in the session matrix.
- Every step-up trigger must map to an invalidation or revalidation consequence where applicable.
- The chosen model must clearly separate browser human, native human, and machine automation identity postures.
- Deferred identity decisions must be explicit.

Definition of done:
- A later security, frontend, backend, or native agent can read ADR-003 and know exactly how interactive sessions,
  step-up, machine credentials, deep links, and revocation should work across Taxat without re-deriving the model from scattered prose.
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm corpus and aligned to the authoritative card slug.
- Instructions explicitly preserve card status neutrality; claim and completion state must continue to live only in `Checklist.md`.
