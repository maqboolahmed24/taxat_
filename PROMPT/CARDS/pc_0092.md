---
id: pc_0092
slug: phase_03_parallel_wave_01_track_backend_access_008_implement_step_up_approval_and_authentication_level_policy_services
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "008"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0092

## Task Summary

implement step up approval and authentication level policy services.

## Metadata

- ID: `pc_0092`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `008`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_008_implement_step_up_approval_and_authentication_level_policy_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0091](./pc_0091.md) `phase_03_parallel_wave_01_track_backend_access_007_implement_governance_mutation_simulation_service_and_hazard_basis_persistence`
- Next: [pc_0093](./pc_0093.md) `phase_03_parallel_wave_01_track_backend_access_009_implement_access_binding_hash_and_dependency_topology_hash_calculators`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0086_to_0093.md](../shared_operating_contract_0086_to_0093.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_008_implement_step_up_approval_and_authentication_level_policy_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0091` if they exist, especially `pc_0021`, `pc_0039`, `pc_0040`, `pc_0050`, `pc_0060`, `pc_0061`, `pc_0063`, `pc_0064`, `pc_0067`, `pc_0068`, `pc_0074`, `pc_0075`, `pc_0076`, `pc_0085`, `pc_0086`, `pc_0087`, `pc_0088`, `pc_0089`, `pc_0090`, and `pc_0091`.

Mission:
Implement the policy services that decide required authentication level, step-up posture, approval obligations, and post-step-up session challenge rotation for sensitive actions.
This task must preserve the corpus rule that step-up, approval, and delegated-authority requirements are distinct and that step-up can surface before approval while approval obligations still remain frozen.

Source algorithm grounding you must honor:
- `actor_and_authority_model.md`, especially `3.9 Policy decision model`, `3.11 Non-delegable and step-up actions`, and the service-principal and human-gate restrictions.
- `northbound_api_and_session_contract.md`, especially the rules that step-up completion rotates effective session challenge state, that fresh step-up proof is required when policy says so, and that pre-step-up commands cannot be replayed afterward.
- `security_and_runtime_hardening_contract.md`, especially MFA / step-up, privilege elevation, session rotation, anti-CSRF posture, and short-lived session records.
- `modules.md`, especially the decision posture requirements consumed by `AUTHORIZE(...)` and runtime-binding enforcement.
- `data_model.md`, especially `ActorSession`, `PrincipalContext`, `AuthorizationDecision`, and governance mutation basis concepts.
- Existing schemas including `actor_session.schema.json`, `principal_context.schema.json`, and `authorization_decision.schema.json`.

Deliverables to create:
1. `packages/backend-access/src/services/authentication_level_policy_service.ts`.
2. `packages/backend-access/src/services/step_up_policy_service.ts`.
3. `packages/backend-access/src/services/approval_resolution_policy_service.ts`.
4. `packages/backend-access/src/services/session_challenge_rotation_service.ts`.
5. `packages/backend-access/src/services/approval_capability_resolver.ts`.
6. `config/access/authentication_level_policy.json`.
7. `config/access/approval_requirement_resolution.json`.
8. `config/access/non_delegable_action_family_catalog.json`.
9. `docs/access/step_up_approval_and_authentication_policy.md`.
10. `tests/unit/backend_access/step_up_and_approval_policy.spec.ts`.
11. `tests/integration/backend_access/step_up_rotation_and_approval_resolution.spec.ts`.

Required design decisions you must make explicit:
- Define the policy table or catalog that maps action families, resource classes, principal classes, delegation basis, governance mutation posture, and exceptional-authority usage into `required_authn_level` and `required_approvals[]`.
- Define how session challenge rotation is represented so a completed step-up invalidates prior pre-step-up command attempts and resume tokens.
- Define how approval obligations remain attached to a request when step-up is completed later, instead of being lost or recomputed loosely.
- Define how service principals, browser human principals, native human principals, and automation principals differ in allowed authn-level transitions.
- Define how fresh step-up windows, proof timestamps, and expiry are stored and compared without hiding policy in controller code.

Execution steps:
1. Materialize machine-readable policy files for authentication levels, non-delegable action families, and approval-resolution rules.
2. Implement services that derive required authn level and approval posture from frozen access context plus action semantics and governance simulation outputs.
3. Implement session challenge rotation so successful step-up updates `ActorSession` safely, invalidates stale command admission artifacts, and records audit lineage.
4. Implement approval capability resolution based on `PrincipalContext.approval_capabilities[]`, action family, governance posture, and segregation-of-duties rules.
5. Ensure `AUTHORIZE(...)` can ask these services for consistent answers rather than inlining policy logic.
6. Document exact mappings for filing, authority-linking, override approval, retention exception, export of unmasked evidence, governance mutation, and similar sensitive actions.

Gap handling you must perform:
- Close the gap where step-up policy is implied in prose but not executable in one service.
- Close the gap where approvals are treated as a generic workflow add-on instead of part of frozen access posture.
- Close the gap where post-step-up session rotation could be forgotten, allowing pre-step-up command replay.
- Close the gap where service principals or automation actors might accidentally satisfy human-only gates.
- Close the gap where governance-mutation approval requirement is recomputed inconsistently with the simulator basis.

Mandatory edge cases:
- same request requires both step-up and approval; step-up surfaces first but approval obligation must remain frozen and visible afterward
- service principal requests a human-only action and must fail closed rather than escalate authn level
- step-up proof expires between authorization and command execution
- pre-step-up command token or resume artifact is replayed after session challenge rotation
- approval capability exists but approver is also the requester where segregation-of-duties rules forbid it
- governance mutation simulator returns `ADVISORY_ONLY`; approval service must not pretend approval can convert it into bounded safe
- exceptional authority is active but still requires fresh human step-up for the specific non-delegable action family
- native device binding has been invalidated and any step-up satisfaction tied to that session must no longer count

Backend, performance, and security requirements:
- Authn-level and approval resolution must be deterministic and cheap enough to sit on hot authorization paths.
- Keep step-up proof material and provider details out of general domain objects; persist references, timestamps, assurance posture, and reason lineage instead.
- Session challenge rotation must be race-safe and append-only in audit terms.
- Do not allow policy files or runtime flags to lower required authn level for action families the corpus marks as non-delegable or step-up sensitive without explicit governed change.
- No new product UI is required.
  If an internal policy explorer is added, it must remain read-only, present action-family-to-policy mappings cleanly, support reduced motion, and be Playwright tested.

Validation rules:
- Unit-test authn-level mapping, approval mapping, segregation-of-duties restrictions, step-up-vs-approval precedence, and service-principal rejection.
- Integration-test session challenge rotation, expiry of step-up proof, invalidation of stale command artifacts, and approval resolution coupled to governance simulation outputs.
- Prove that the same request context yields the same required authn level and approval vectors under repeated runs.
- Prove that no service principal or stale session can satisfy human-only gates.
- Prove that changing policy files without snapshot versioning is detectable and does not silently alter historical decisions.

Definition of done:
- The repo has one authoritative policy substrate for required authentication level, step-up, and approval posture.
- Step-up completion safely rotates session challenge state and preserves downstream approval obligations.
- Later command-admission and authority-request tasks can rely on durable, executable policy services instead of controller-level special cases.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
