---
id: pc_0191
slug: phase_04_parallel_wave_02_track_backend_governance_003_implement_governance_access_simulation_projection_and_basis_contract_hash_reuse_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "003"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0191

## Task Summary

implement governance access simulation projection and basis contract hash reuse rules.

## Metadata

- ID: `pc_0191`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `003`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_003_implement_governance_access_simulation_projection_and_basis_contract_hash_reuse_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0190](./pc_0190.md) `phase_04_parallel_wave_02_track_backend_governance_002_implement_governance_policy_snapshot_projector_and_environment_binding_views`
- Next: [pc_0192](./pc_0192.md) `phase_04_parallel_wave_02_track_backend_governance_004_implement_authority_link_inventory_item_projection_and_health_rollups`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0190_to_0197.md](../shared_operating_contract_0190_to_0197.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_003_implement_governance_access_simulation_projection_and_basis_contract_hash_reuse_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0190` if they exist, especially `pc_0086` through `pc_0093`, `pc_0134` through `pc_0141`, and `pc_0190`.

Mission:
Implement the authoritative `GovernanceAccessSimulation` projector and the reusable mutation-basis / hazard-hash continuity rules for `/v1/governance/tenants/{tenant_id}/access-simulations`.
This is not a pretty wrapper around `AUTHORIZE(...)` and not a best-effort advisory JSON blob.
It must bind the exact nested `AuthorizationDecision`, the exact ordered authority-chain explanation, and—when the target is mutation-capable governance scope—the exact frozen mutation hazard and mutation basis contract that later basket, blast-panel, approval, and command paths must reuse without silent recomputation.

Source algorithm grounding you must honor:
- `admin_governance_console_architecture.md`, especially section `4.3 /governance/access`, the simulator rules, authority-chain order, and commit-posture rules.
- `data_model.md`, especially `GovernanceAccessSimulation`, `PrincipalAccessView`, and the explanation-aligned ordered `authority_chain_layers[]`.
- `modules.md`, especially `AUTHORIZE(...)` and `SIMULATE_GOVERNANCE_MUTATION(...)`, including the exact formulas, hashes, and bounded-safe predicates.
- `policy_risk_and_blast_radius_modeling_contract.md`, especially shared hazard-packet reuse and approval ladder law.
- `actor_and_authority_model.md`, especially preview-only posture, access-binding alignment, and mutation-capable governance evaluation rules.
- `northbound_api_and_session_contract.md`, especially `POST /v1/governance/tenants/{tenant_id}/access-simulations`.
- `schemas/governance_access_simulation.schema.json`, `schemas/governance_mutation_hazard_contract.schema.json`, `schemas/governance_mutation_basis_contract.schema.json`, and the schema for nested `AuthorizationDecision` and authority-layer boundary objects.
- `scripts/validate_contracts.py`, especially the governance simulation payload validation, FE-51 continuity checks, and the deterministic formula checks for `policy_risk_score`, `approval_necessity_score`, and `bounded_safe_mutation`.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially TV-65A, TV-65B, TV-65C, and FE-51.

Deliverables to create:
1. `packages/backend-governance/src/projectors/build_governance_access_simulation.ts`
2. `packages/backend-governance/src/services/build_authority_chain_layers.ts`
3. `packages/backend-governance/src/services/build_governance_mutation_hazard.ts`
4. `packages/backend-governance/src/services/build_governance_mutation_basis_contract.ts`
5. `packages/backend-governance/src/services/derive_governance_simulator_posture.ts`
6. `packages/backend-governance/src/tests/governance_access_simulation_projection.spec.ts`
7. `packages/backend-governance/src/tests/governance_access_simulation_hash_continuity.spec.ts`
8. `docs/governance/governance_access_simulation_and_basis_hash_reuse.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/access-simulator-lab.tsx`

You MUST patch any existing policy-simulator route or service so it emits this projector output directly rather than deriving approval posture or blast-radius meaning in the browser.

Required design decisions you must make explicitly:
- define the exact boundary between read-only `AuthorizationDecision`-only simulations and mutation-capable governance previews that also require `SIMULATE_GOVERNANCE_MUTATION(...)`
- define how `authority_chain_layers[]` are built from the same ordered stack used by the selected access cell so the simulator does not invent a second explanation grammar
- define how `authority_layer_boundary` is frozen and how its `access_binding_hash` stays aligned with the nested `authorization_decision`
- define how `count_class_profile_code` and the impacted-count class buckets are derived and serialized
- define the canonical serialization input for `hazard_contract_hash` and `basis_contract_hash`
- define how the simulator reports advisory-only state when confidence or predictability is low without losing the reviewed hazard packet

Execution requirements:
- publish a schema-valid `GovernanceAccessSimulation`
- preserve the exact authority-layer order:
  `SESSION_AUTHN_POSTURE -> TENANT_OPERATIONAL_AUTHORITY -> CLIENT_DELEGATION_COVERAGE -> EXTERNAL_AUTHORITY_LINK_READINESS`, with optional `AUTHORITY_OF_RECORD_OUTCOME` only when materially relevant
- run both `AUTHORIZE(...)` and `SIMULATE_GOVERNANCE_MUTATION(...)` on the same frozen snapshot cut for mutation-capable governance actions; do not show nominal allowability without hazard overlay
- keep `policy_risk_score` derived only from `privilege_gain_score`, `scope_expansion_score`, `masking_relaxation_score`, and `impact_radius_upper_score`
- keep `approval_necessity_score` derived only from `policy_risk_score` and the blast-radius upper bound
- keep `bounded_safe_mutation = 1` lawful only when every frozen condition in `modules.md` / `policy_risk_and_blast_radius_modeling_contract.md` is satisfied
- keep `commit_authority_posture = PREVIEW_ONLY` iff `simulation_confidence_score < 80` or `predictability_score < 75`; else `BOUNDED_SAFE` iff bounded-safe is proven; else `APPROVAL_GATED`
- keep `approval_requirement = NOT_REQUIRED` only when `bounded_safe_mutation = 1`
- preserve one exact reviewed hash chain: `authorization_decision.access_binding_hash` -> `mutation_hazard.access_binding_hash` -> `mutation_basis_contract.access_binding_hash`, and `mutation_basis_contract.hazard_contract_hash` -> `mutation_hazard.hazard_contract_hash`
- keep `simulator_posture` explicit and lawful: `READ_ONLY_DECISION`, `ADVISORY_ONLY`, `APPROVAL_GATED`, or `BOUNDED_SAFE`
- when the preview is read-only and non-mutating, keep `mutation_hazard = null` and `mutation_basis_contract = null` rather than fabricating placeholder hazard data

Gap handling you must close while implementing:
- close any gap where low-confidence previews can still look or behave commit-ready
- close any gap where the simulator emits a mutation basis whose `access_binding_hash` drifts from the nested authorization decision
- close any gap where hazard packets are recomputed or narrowed between simulator render and downstream reuse
- close any gap where authority-chain explanation in the simulator diverges from the access-matrix explanation grammar
- close any gap where approval posture is inferred from ad hoc booleans instead of the canonical risk ladder

Mandatory edge cases:
- mutation-capable previews with `simulation_confidence_score < 80` or `predictability_score < 75` must remain `ADVISORY_ONLY` / `PREVIEW_ONLY`
- `approval_requirement = NOT_REQUIRED` must never coexist with `bounded_safe_mutation = 0`
- mutation-capable previews must preserve both lower and upper blast-radius bounds; do not collapse them to one scalar
- `risk_driver_codes[]` must reflect only non-zero contributors from the governed family set
- `approval_trigger_codes[]` must reflect only the governed approval ladder, not ad hoc booleans
- no preview may widen scope or binding silently between `requested_scope[]`, nested authorization, and mutation basis
- no mutation-capable preview may mutate live grants, live role rows, or live authority links

Playwright and contract-test requirements:
- add direct tests for hazard-hash construction, basis-hash construction, simulator-posture transitions, low-confidence preview-only behavior, bounded-safe gating, and access-binding continuity
- if a northbound simulation route exists, add Playwright `APIRequestContext` tests for nested `authorization_decision`, hazard / basis reuse, advisory-only posture, and typed stale-guard behavior
- if you add the optional lab, add Playwright browser tests that verify the ordered authority-chain ladder, interval blast-radius strip, approval-confidence rail, and semantic locator contract only
- capture request/response payloads and traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use one calm control-plane layout: top context ribbon, left scenario editor, primary simulation canvas, one subordinate explanation sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; secondary surface `#F1F3F0`; ink `#171717` / `#667085`; accents `#0F766E`, `#1D4ED8`, `#B7791F`, `#C2410C`
- render the authority-chain explanation as a vertical ladder with explicit layer labels and typed outcomes, not as badges
- render blast radius as a bounded interval band with lower / upper handles and one compact uncertainty marker; no radar, pie, or gauge charts
- render confidence and predictability as two slim rails with literal threshold labels and one compact simulator-posture badge
- transitions must be subtle, reduced-motion-safe, and never hide the hash continuity ribbon

Backend, performance, and security requirements:
- keep the simulator side-effect-free; it is preview truth, not mutation truth
- keep hazard and basis serialization deterministic and canonical so hash continuity is replay-safe
- index topology and count derivation so simulation reads remain fast under governance scale
- fail closed on missing binding, missing policy snapshot basis, scope widening, or hash mismatch

Validation rules:
- validate every emitted `GovernanceAccessSimulation`, `governance_mutation_hazard_contract`, and `governance_mutation_basis_contract` against schema
- add regression coverage for FE-51 continuity and the TV-65A/B/C scenarios
- keep this task limited to the simulation / projection boundary; do not swallow later mutation command handling

Definition of done:
- the repo has one authoritative governance simulation projector that preserves exact authorization truth, ordered authority layers, reusable hazard packets, and reusable basis contracts
- advisory-only vs approval-gated vs bounded-safe posture is now deterministic and machine-checkable
- downstream basket, blast-panel, approval, and command boundaries can trust the simulator’s reviewed hash chain instead of recomputing risk locally
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
