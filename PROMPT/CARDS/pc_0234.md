---
id: pc_0234
slug: phase_05_parallel_wave_03_track_frontend_shared_006_Playwright_or_other_appropriate_tooling_implement_command_client_receipt_polling_and_problem_envelope_handling
mode: parallel
phase: "05"
wave: "03"
track: "frontend_shared"
task_number: "006"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_shared_006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0234

## Task Summary

006 Playwright or other appropriate tooling implement command client receipt polling and problem envelope handling.

## Metadata

- ID: `pc_0234`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend shared`
- Task Number: `006`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_shared_006_Playwright_or_other_appropriate_tooling_implement_command_client_receipt_polling_and_problem_envelope_handling`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0233](./pc_0233.md) `phase_05_parallel_wave_03_track_frontend_shared_005_Playwright_or_other_appropriate_tooling_implement_frontend_state_containers_for_etags_view_guards_and_shell_tokens`
- Next: [pc_0235](./pc_0235.md) `phase_05_parallel_wave_03_track_frontend_shared_007_Playwright_or_other_appropriate_tooling_implement_sse_client_resume_rebase_and_access_rebind_experience_runtime`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_shared_006_Playwright_or_other_appropriate_tooling_implement_command_client_receipt_polling_and_problem_envelope_handling`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0233` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the shared command client, durable receipt polling, and typed problem-envelope handling used by future operator, portal, collaboration, and governance UI actions.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The northbound contract separates command submission from read-side truth. A POST can be accepted, duplicate-replayed, rejected stale, rejected policy, or expired, and completion must be read from durable `ApiCommandReceipt` plus projections. The frontend must therefore treat command calls as receipt-driven workflows, not optimistic state transitions.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md sections 1, 3, 4, 5, and 6`.
- `authority_truth_and_internal_projection_separation_contract.md governing model and surface rules`.
- `frontend_shell_and_interaction_law.md settlement-aware feedback and writable posture`.
- `low_noise_experience_contract.md settlement-state contract and operator interaction layer feedback truth policy`.
- `schemas/command_envelope.schema.json, api_command_receipt.schema.json, command_truth_boundary_contract.schema.json, action_authority_contract.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/commands/command_client.ts`
2. `packages/frontend-shell-core/src/commands/command_envelope_builder.ts`
3. `packages/frontend-shell-core/src/commands/receipt_polling.ts`
4. `packages/frontend-shell-core/src/commands/problem_envelope.ts`
5. `packages/frontend-shell-core/src/commands/idempotency_key.ts`
6. `packages/shared-ui/src/commands/CommandReceiptInlineStatus.tsx`
7. `packages/playwright-kit/src/fixtures/command_api_fixture.ts`
8. `tests/unit/frontend-shell-core/command_client.spec.ts`
9. `tests/playwright/frontend/command_receipt_polling_and_problem_envelope.spec.ts`
10. `docs/frontend/command_client_receipt_polling_and_problem_handling.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Render command feedback as an inline receipt strip, not a toast storm. The strip sits inside `ACTION_STRIP` or the owning context region and shows: intent label, durable receipt state, stale guard family, latest projection ref, and one next step. Use a slim progress rail only for pending settlement; do not use spinners that replace the dominant question.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- implement stable client-generated `command_id` and idempotency key helpers bound to actor/session, semantic target, and payload hash
- build command envelopes from exact guard snapshots and mutation precondition binding; never embed authority tokens, raw audit signatures, or client-derived legal-state flags
- submit commands through one shared client and poll `GET /v1/commands/{command_id}` until accepted duplicate, rejected, expired, or projection-linked terminal posture
- map typed problem envelopes to inline recovery/stale/actionability states without hiding the mounted shell
- surface durable receipt fields needed for lost POST response recovery, app refresh, or automation polling

Functional, architectural, and edge-case gaps to close:
- close the gap where UI buttons optimistically mutate state after POST success
- close the gap where duplicate submits create duplicate activity or duplicate user feedback
- close the gap where stale-view rejection is treated as generic error copy
- close the gap where command failure feedback appears as disconnected toasts rather than shell-bound settlement posture

Mandatory edge cases:
- lost POST response followed by polling must recover the same command by `command_id`
- duplicate replay must show one receipt and must not trigger a second visible success
- `REJECTED_STALE_VIEW` must request rebase/refresh using the stale guard family from the receipt/problem, not a generic reload
- policy/invalid rejection must omit unsafe actions and keep a deterministic investigation entry point

Playwright, accessibility, and validation requirements:
- unit-test envelope formation, idempotency stability, stale guard inclusion, and problem envelope mapping
- Playwright API-test mocked accepted, duplicate replay, rejected stale, rejected policy, expired, and lost-response recovery paths
- Playwright browser-test inline receipt behavior in reduced-motion mode with keyboard focus preserved
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- all command writes flow through one receipt-driven client
- inline feedback is durable-receipt and typed-problem driven
- duplicates, stale views, failures, and recovery are Playwright-covered
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
