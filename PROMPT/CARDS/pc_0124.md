---
id: pc_0124
slug: phase_03_parallel_wave_01_track_backend_compute_004_implement_parity_result_computation_and_authority_comparison_logic
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "004"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0124

## Task Summary

implement parity result computation and authority comparison logic.

## Metadata

- ID: `pc_0124`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `004`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_004_implement_parity_result_computation_and_authority_comparison_logic`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0123](./pc_0123.md) `phase_03_parallel_wave_01_track_backend_compute_003_implement_risk_report_derivation_and_materiality_threshold_services`
- Next: [pc_0125](./pc_0125.md) `phase_03_parallel_wave_01_track_backend_compute_005_implement_trust_summary_formula_engine_and_threshold_stability_analysis`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0118_to_0125.md](../shared_operating_contract_0118_to_0125.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_004_implement_parity_result_computation_and_authority_comparison_logic`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0123` if they exist, especially `pc_0010`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0074`, `pc_0097`, `pc_0101`, `pc_0121`, `pc_0122`, and `pc_0123`.

Mission:
Implement the `ParityResult` model, repository, lifecycle helper, and deterministic parity engine that compares the frozen internal result against the correct frozen authority or baseline comparison basis.
This task must build the comparison set, enforce exact decimal threshold/floor math, preserve deterministic field ordering, emit per-field deltas and top-level classifications, and fail closed when the comparison set is invalid or insufficient for the requested comparison requirement.

Source algorithm grounding you must honor:
- `core_engine.md`, especially `MERGE_COMPARISON_BASIS(...)`, `EVALUATE_PARITY(...)`, and post-parity gate handoff.
- `modules.md`, especially parity placement in the post-seal graph.
- `compute_parity_and_trust_formulas.md`, especially sections `8.6 Authority comparison scope`, `8.7 Per-field parity formulas`, and `8.8 Aggregate parity formulas`.
- `exact_gate_logic_and_decision_tables.md`, especially the parity decision table and the `comparison_set_state = INVALID` fail-closed rule.
- `authority_calculation_contract.md` and `authority_interaction_protocol.md`, especially `CalculationBasis` and authority-comparison path binding.
- `data_model.md`, especially `ParityResult`.
- `state_machines.md`, especially section `6.10 ParityResult.lifecycle_state and parity_classification`.
- `test_vectors.md`, especially `TV-05`, `TV-05A`, `TV-05B`, and `TV-05C`.
- Existing schemas including `parity_result.schema.json` and `calculation_basis.schema.json`.

Deliverables to create:
1. `packages/backend-compute/src/models/parity_result.ts`.
2. `packages/backend-compute/src/repositories/parity_result_repository.ts`.
3. `packages/backend-compute/src/services/transition_parity_result.ts`.
4. `packages/backend-compute/src/services/resolve_comparison_basis.ts`.
5. `packages/backend-compute/src/services/build_comparison_set.ts`.
6. `packages/backend-compute/src/services/build_field_delta.ts`.
7. `packages/backend-compute/src/services/calculate_breach_ratio.ts`.
8. `packages/backend-compute/src/services/classify_parity_result.ts`.
9. `packages/backend-compute/src/services/evaluate_parity.ts`.
10. `db/migrations/phase03_0124_parity_result.sql`.
11. `docs/compute/parity_result_computation_and_authority_comparison_logic.md`.
12. `tests/unit/backend_compute/parity_result_computation.spec.ts`.
13. `tests/integration/backend_compute/parity_result_authority_basis.spec.ts`.

Required design decisions you must make explicit:
- Define the exact comparison-basis adapter boundary.
  It must consume a frozen `CalculationBasis` or equivalent persisted baseline artifact, not a fresh authority call.
- Define the mapping from provider/profile/scope to `comparison_requirement in {MANDATORY, DESIRABLE, NOT_REQUIRED}`.
- Define the canonical field ordering and dedupe rule.
  The formulas require ordering by `(criticality_rank desc, field_code asc)` after dedupe.
- Define the exact decimal implementation for `delta_signed`, `delta_abs`, `delta_rel`, `breach_abs`, `breach_rel`, and `breach_ratio`.
- Define how `cause_hypotheses[]` are derived.
  They must remain deterministic and reason-coded rather than speculative free text.

Execution steps:
1. Implement the schema-backed `ParityResult` model, repository, and lifecycle helper.
2. Implement `resolve_comparison_basis.ts` so comparison basis selection is frozen before parity evaluation and reusable by later filing/amendment stages.
3. Implement `build_comparison_set.ts` and `build_field_delta.ts` so each field delta carries thresholds, floors, weights, input-state classification, and exact decimal deltas.
4. Implement `calculate_breach_ratio.ts` and `classify_parity_result.ts` so aggregate classification follows the strict precedence ladder and reason ordering from the formulas.
5. Implement `evaluate_parity.ts` so it emits a schema-valid `ParityResult` and persists the correct top-level reason ordering.
6. Add tests for exact threshold equality, critical-field precedence, invalid comparison set, partial coverage, and basis reuse from `CalculationBasis`.

Gap handling you must perform:
- Close the gap where parity depends on a frozen authority/baseline comparison basis that may not yet be fully implemented upstream.
  Use an injected, persisted-basis adapter and fail closed when required basis artifacts are missing.
- Close the gap where duplicate field codes or invalid threshold/floor values could silently degrade the comparison.
  Force `comparison_set_state = INVALID` and preserve the fail-closed posture.
- Close the gap where partial coverage could be mistaken for a match.
  Emit `PARITY_PARTIAL_COVERAGE` and `NOT_COMPARABLE` exactly when the formulas require it.
- Close the gap where reason-code ordering or `dominant_reason_code` could drift by iteration order.

Mandatory edge cases:
- a field whose exact-decimal `breach_ratio` is exactly `1.0` must remain `MATERIAL_DIFFERENCE` across runtimes.
- a critical materially or blockingly breached field must outrank low aggregate pressure from many matching non-critical fields.
- duplicate field codes, non-positive weights, negative thresholds, non-positive floors, or non-numeric inputs must invalidate the comparison set.
- `comparison_set_state = INVALID` must force `parity_classification = NOT_COMPARABLE` and `parity_score = 0`.
- `comparison_requirement in {MANDATORY, DESIRABLE}` with incomplete coverage must not silently pass as comparable.
- top-level reason codes and `dominant_reason_code` must stay deterministic.
- `NOT_EVALUATED -> EVALUATED -> SUPERSEDED` transitions must be legal and auditable.

Backend, performance, and security requirements:
- Keep parity deterministic and replay-safe.
- Reuse exact-decimal utilities from compute rather than creating a second math stack.
- Never perform live authority calls from inside parity evaluation.
- Do not log sensitive authority payloads or raw financial detail beyond bounded reason-coded diagnostics.

Optional internal verification surface (only if you add one):
- If you add a read-only parity inspector, place it behind an internal route such as `/internal/decision/parity/:parityId`.
- The UI must be minimalist premium, not dashboard-like: one narrow left rail for comparison requirement / coverage / classification, one large right pane with a fixed-order field-delta table, subtle inline threshold ladders, and no animated charts beyond restrained hover/focus emphasis.
- Show one compact ladder diagram for the precedence order `NOT_COMPARABLE -> BLOCKING -> MATERIAL -> MINOR -> MATCH`, with the active branch highlighted from persisted artifact fields only.
- Any such surface must be implemented and tested with Playwright using role-based locators, reduced-motion checks, trace capture, and keyboard navigation.

Validation rules:
- Unit-test comparison-basis resolution, exact breach-ratio math, classification precedence, and lifecycle legality.
- Integration-test parity persistence from a frozen `ComputeResult` plus persisted comparison basis.
- Validate persisted artifacts against `parity_result.schema.json`.
- Prove that invalid or incomplete comparison sets fail closed as documented.
- Re-run the contract validators after the implementation bundle is assembled.

Definition of done:
- The repo can emit a deterministic `ParityResult` bound to one frozen comparison basis.
- Threshold-edge and critical-field behavior are replay-safe and byte-stable.
- Later trust and filing work can consume one authoritative parity artifact instead of recomputing comparison logic locally.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
