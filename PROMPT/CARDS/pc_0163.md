---
id: pc_0163
slug: phase_03_parallel_wave_01_track_backend_northbound_007_implement_client_portal_workspace_documents_approvals_onboarding_and_activity_endpoints
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "007"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0163

## Task Summary

implement client portal workspace documents approvals onboarding and activity endpoints.

## Metadata

- ID: `pc_0163`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `007`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_007_implement_client_portal_workspace_documents_approvals_onboarding_and_activity_endpoints`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0162](./pc_0162.md) `phase_03_parallel_wave_01_track_backend_northbound_006_implement_governance_read_endpoints_for_overview_policy_principals_and_roles`
- Next: [pc_0164](./pc_0164.md) `phase_03_parallel_wave_01_track_backend_northbound_008_implement_upload_session_allocate_put_blob_and_status_endpoints`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0158_to_0165.md](../shared_operating_contract_0158_to_0165.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_007_implement_client_portal_workspace_documents_approvals_onboarding_and_activity_endpoints`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0162` if they exist, especially `pc_0134` through `pc_0149`, `pc_0150` through `pc_0156`, and `pc_0160`.

Mission:
Implement the customer-safe portal read surface family:
- `GET /v1/client-portal/workspace`
- `GET /v1/client-portal/documents`
- `GET /v1/client-portal/approvals`
- `GET /v1/client-portal/onboarding`
- `GET /v1/client-portal/activity`
These endpoints must preserve `CLIENT_PORTAL_SHELL` continuity, exact `workspace_version` / `view_guard_ref` / grouped `stability_contract`, customer-safe projection, current-versus-history artifact posture, and route-specific dominant-question semantics without leaking expert-only or staff-only fields.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially section 2.2 and the portal command/rule references that shape stale recovery.
- `customer_client_portal_experience_contract.md`, especially route architecture, secure document-upload flow, approval flow, onboarding flow, help flow, read-model/API translation requirements, semantic selectors, and Playwright validation minimum.
- `frontend_shell_and_interaction_law.md`, especially same object same shell, freshness/recovery posture, artifact preview/handoff, and automation anchors.
- `data_model.md`, especially `ClientPortalWorkspace`, `ClientDocumentRequest`, `ClientUploadSession`, `ClientApprovalPack`, `ClientOnboardingJourney`, `ClientTimelineEvent`, and `PortalHelpRequest`.
- `schemas/client_portal_workspace.schema.json`, `schemas/client_document_request.schema.json`, `schemas/client_upload_session.schema.json`, `schemas/client_approval_pack.schema.json`, `schemas/client_onboarding_journey.schema.json`, `schemas/client_timeline_event.schema.json`, and `schemas/portal_help_request.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 244-253 and 425.

Deliverables to create:
1. `packages/backend-northbound/src/http/get_client_portal_workspace_endpoint.ts`
2. `packages/backend-northbound/src/http/get_client_portal_documents_endpoint.ts`
3. `packages/backend-northbound/src/http/get_client_portal_approvals_endpoint.ts`
4. `packages/backend-northbound/src/http/get_client_portal_onboarding_endpoint.ts`
5. `packages/backend-northbound/src/http/get_client_portal_activity_endpoint.ts`
6. `packages/backend-northbound/src/http/register_client_portal_read_routes.ts`
7. `packages/backend-northbound/src/query/get_client_portal_workspace.ts`
8. `packages/backend-northbound/src/query/get_client_portal_documents_view.ts`
9. `packages/backend-northbound/src/query/get_client_portal_approvals_view.ts`
10. `packages/backend-northbound/src/query/get_client_portal_onboarding_view.ts`
11. `packages/backend-northbound/src/query/get_client_portal_activity_view.ts`
12. `packages/backend-northbound/src/services/enforce_customer_safe_projection_for_portal.ts`
13. `packages/backend-northbound/src/services/build_client_portal_stability_contract.ts`
14. `docs/northbound/client_portal_read_surfaces.md`
15. `tests/unit/backend_northbound/client_portal_workspace_contract.spec.ts`
16. `tests/unit/backend_northbound/client_portal_route_derivative_rules.spec.ts`
17. `tests/integration/backend_northbound/client_portal_read_routes.spec.ts`
18. `tests/playwright_api/northbound/client_portal_read_routes.spec.ts`

Optional diagnostic explorer only if it materially accelerates verification:
19. `apps/client-portal-web/src/routes/debug/portal-route-inspector.tsx`

Required design decisions you must make explicit:
- Decide whether route-specific endpoints return the full `ClientPortalWorkspace` with route-constrained sections or thin derivative envelopes. If derivatives are introduced, they must preserve the same top-level shell spine and stability/recovery basis.
- Define how `ETag = workspace_version` is applied across workspace and route-specific reads, including conditional requests and same-route cache safety.
- Define how contextual request-detail focus, return path, and artifact-focus bucket are expressed on route endpoints without creating a sixth top-level tab or a second shell grammar.
- Define how activity ordering, document counts, approval counts, and onboarding completion counts stay exact projections of serialized content rather than renderer-local heuristics.
- Define how delegated identity context and customer-safe limitation notices remain present across all route derivatives.

Execution steps:
1. Materialize the canonical `ClientPortalWorkspace` read model from durable portal/workflow/authority truth.
2. Implement route-specific queries that either project narrow derivatives from that workspace or reuse the full workspace with route-constrained sections, but preserve the same shell/stability spine.
3. Re-validate all serialized artifacts against their schemas before response.
4. Emit `ETag = workspace_version` and preserve `freshness_state`, `view_guard_ref`, and grouped `stability_contract` on every portal route response.
5. Keep artifact-selection and artifact-affordance posture explicit for documents and approvals so current versus historical meaning never depends on list position.
6. Add deterministic tests for same-shell continuity, route focus preservation, exact count projections, and customer-safe field filtering.

Gap handling you must perform:
- Close the gap where `route` can drift from the one active top-level nav tab.
- Close the gap where document and approval counts are approximate or disagree with the serialized cards.
- Close the gap where `current_upload_ref`, `latest_pack_ref`, activity ordering, or onboarding progress can drift away from the serialized route content.
- Close the gap where portal reliability/completion posture is rebuilt in the browser instead of published by the backend.
- Close the gap where route-specific endpoints would otherwise overfetch expert-only or staff-only data and rely on the renderer to hide it.

Mandatory edge cases:
- `navigation_tabs[]` MUST contain exactly one active entry matching the top-level `route`
- `document_center.open_request_count` MUST equal the serialized open request cards
- every request card’s `current_upload_ref`, if non-null, MUST point to an upload within that same request card
- `approval_center.outstanding_count` MUST equal the serialized approval packs requiring client attention
- `approval_center.latest_pack_ref` MUST point to one serialized pack and `STEP_UP_REQUIRED` packs MUST keep `requires_step_up = true`
- `activity_timeline[]` MUST be newest-first with unique `event_id`s
- onboarding counts MUST remain arithmetically valid and completed journeys must truly be complete
- route responses MUST stay customer-safe and MUST NOT leak staff-only reason codes, gate state, audit lineage, or internal activity

Playwright / contract-test requirements:
- use Playwright APIRequestContext for the endpoint family
- if you add a browser explorer, use semantic/user-facing locators and trace capture on failure
- prove that route-specific reads preserve the same shell continuity spine and customer-safe semantics
- prove that stale/degraded portal posture stays inline and actionable on the same mounted route

Backend, performance, and security requirements:
- perform role filtering and customer-safe projection before serialization
- do not widen portal cache identity across client, access binding, masking posture, or route context
- keep current-versus-history selection explicit in the response, not inferred client-side
- fail closed on customer-safe projection drift, schema mismatch, or stale route basis corruption

Validation rules:
- validate portal workspace and derivative artifacts against their canonical schemas or newly introduced derivative contracts
- unit-test customer-safe filtering, exact counts, route continuity, and artifact selection rules
- integration-test workspace, documents, approvals, onboarding, and activity routes
- API-test the shipped routes with Playwright

Definition of done:
- the repo exposes the portal workspace/documents/approvals/onboarding/activity read surfaces
- every route preserves `CLIENT_PORTAL_SHELL` continuity, grouped stability, and customer-safe posture
- current-versus-history artifacts, counts, focus, and recovery posture are deterministic and backend-authored
- portal reads no longer depend on renderer-local heuristics for reliability or limitation semantics
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
