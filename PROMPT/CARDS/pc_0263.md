---
id: pc_0263
slug: phase_05_parallel_wave_03_track_frontend_portal_011_Playwright_or_other_appropriate_tooling_implement_current_vs_historical_artifact_presentation_and_document_focus_buckets
mode: parallel
phase: "05"
wave: "03"
track: "frontend_portal"
task_number: "011"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_portal_011
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0263

## Task Summary

011 Playwright or other appropriate tooling implement current vs historical artifact presentation and document focus buckets.

## Metadata

- ID: `pc_0263`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend portal`
- Task Number: `011`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_portal_011_Playwright_or_other_appropriate_tooling_implement_current_vs_historical_artifact_presentation_and_document_focus_buckets`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0262](./pc_0262.md) `phase_05_parallel_wave_03_track_frontend_portal_010_Playwright_or_other_appropriate_tooling_implement_portal_language_contract_copy_filters_and_plain_language_components`
- Next: [pc_0264](./pc_0264.md) `phase_05_parallel_wave_03_track_frontend_portal_012_Playwright_or_other_appropriate_tooling_implement_narrow_screen_responsive_continuity_without_shell_metaphor_drift`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_portal_011_Playwright_or_other_appropriate_tooling_implement_current_vs_historical_artifact_presentation_and_document_focus_buckets`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0262` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement portal current-versus-historical artifact presentation and document focus buckets so preview/download/print targets stay bound to current client-safe artifacts.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The algorithm repeatedly separates current required artifacts from historical, superseded, rejected, expired, quarantined, and limited artifacts. Portal document requests and approval packs must publish `artifact_selection`, `artifact_affordance`, externalization governance, and route-level `artifact_focus_*` state so current/history posture survives refresh, reconnect, notification-open, and narrow-screen restack. The renderer must never let row order, latest timestamp, or focused history chip silently become the default handoff target.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md` Documents, Approvals, artifact/print/browser-handoff rules, shell-specific artifact specialization, and read-model/API translation.
- `frontend_shell_and_interaction_law.md` section 7 artifact preview/export/print/browser handoff and client portal specialization.
- `cache_isolation_and_secure_reuse_contract.md` preview/export reuse law and scope rules.
- `focus_restoration_and_return_target_harness_contract.md` return-target rules.
- `schemas/artifact_selection_contract.schema.json`, `artifact_affordance_contract.schema.json`, `client_document_request.schema.json`, `client_approval_pack.schema.json`, `cache_isolation_contract.schema.json`.

Deliverables to create or patch:
1. `apps/client-portal-web/src/components/artifacts/PortalArtifactPanel.tsx`
2. `apps/client-portal-web/src/components/artifacts/CurrentArtifactCard.tsx`
3. `apps/client-portal-web/src/components/artifacts/HistoricalArtifactList.tsx`
4. `apps/client-portal-web/src/components/artifacts/ArtifactFocusBucketTabs.tsx`
5. `packages/frontend-shell-core/src/portal/portal_artifact_selection.ts`
6. `packages/frontend-shell-core/src/portal/portal_artifact_handoff_guard.ts`
7. `tests/unit/frontend-shell-core/portal_artifact_selection.spec.ts`
8. `tests/playwright/frontend/client_portal_current_historical_artifacts.spec.ts`
9. `docs/frontend/client_portal_current_vs_historical_artifacts.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Use a current-first document rail: a prominent `Current copy` card with status/date/target controls, followed by recessed history rows under `Previous files` or `Older approval packs`. Use bucket chips like `Current`, `In review`, `History`, `Limited` only when backed by route focus state.
- Preview header must state posture in plain language: `Current declaration`, `Historical upload`, `Superseded pack`, `Rejected upload`, `Awaiting replacement`, `Expired`, or `Unavailable`. The current card owns default preview/download/print controls; history rows get explicit secondary controls.

Execution requirements:
- parse and enforce `artifact_selection` and `artifact_affordance` for each document request and approval pack
- bind default preview/download/print targets to visible current subject refs; fail closed on target mismatch or stale cache binding
- serialize and restore `artifact_focus_bucket_or_null` and `artifact_focus_subject_ref_or_null` through route context
- integrate same-shell preview first, download-only fallback, print preflight, and external handoff explanation
- preserve acting-for context, limitation notices, timestamp, and current/history posture in downloadable/printable declaration/receipt flows

Functional, architectural, and edge-case gaps to close:
- close the gap where history grouping is visual but invocation still uses a stale/default URL
- close the gap where latest uploaded file is mistaken for current artifact
- close the gap where focused historical chip after reconnect reopens current artifact without explanation
- close the gap where print/download bypasses cache isolation and masking posture

Mandatory edge cases:
- no current artifact but history exists
- current artifact plus in-review replacement
- rejected/quarantined/expired/limited artifact
- historical approval pack vs signed receipt
- preview unavailable due to policy/format/masking
- route rebase while history chip focused

Playwright, accessibility, and validation requirements:
- unit-test current/history target selection, focus bucket restoration, blocked/unavailable posture, and cache/route guard validation
- Playwright-test current preview/download, historical preview, no-current state, rejected/quarantined/expired blocked actions, print preflight, route refresh/reconnect focus bucket restoration, keyboard navigation, and responsive restack
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- portal artifacts are current-primary and history-secondary by contract
- handoff targets cannot drift from visible artifact posture
- Playwright proves artifact focus buckets, cache guards, and blocked history edge cases
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
