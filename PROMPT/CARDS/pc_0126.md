---
id: pc_0126
slug: phase_03_parallel_wave_01_track_backend_compute_006_implement_gate_decision_record_model_and_ordered_gate_evaluation_engine
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "006"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0126

## Task Summary

implement gate decision record model and ordered gate evaluation engine.

## Metadata

- ID: `pc_0126`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `006`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_006_implement_gate_decision_record_model_and_ordered_gate_evaluation_engine`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0125](./pc_0125.md) `phase_03_parallel_wave_01_track_backend_compute_005_implement_trust_summary_formula_engine_and_threshold_stability_analysis`
- Next: [pc_0127](./pc_0127.md) `phase_03_parallel_wave_01_track_backend_compute_007_implement_decision_bundle_assembly_and_terminal_outcome_finalization`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0126_to_0133.md](../shared_operating_contract_0126_to_0133.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_006_implement_gate_decision_record_model_and_ordered_gate_evaluation_engine`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0125` if they exist, especially `pc_0010`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0067`, `pc_0074`, `pc_0076`, `pc_0086`, `pc_0097`, `pc_0102` through `pc_0108`, and `pc_0118` through `pc_0125`.

Mission:
Implement the schema-backed `GateDecisionRecord` model, repository, ordered gate-evaluation engine, semantics-contract builder, explainability builder, and append-only persistence helpers that make the Taxat gate tape authoritative, deterministic, replay-safe, and monotonically ordered.
This task must establish the canonical stage-order profile, preserve the authorized effective scope, freeze override semantics explicitly, emit one deterministic dominant reason, and persist gate batches without letting later stages rewrite or re-rank earlier records.

Source algorithm grounding you must honor:
- `exact_gate_logic_and_decision_tables.md`, especially sections `7.1 Gate result contract`, `7.2 Gate order`, the reason-order / severity / decision precedence rules, and the filing / submission gate dependencies.
- `modules.md`, especially `EVALUATE_GATE_CHAIN(...)`, `PERSIST_GATE_BATCH(...)`, `APPEND_MANIFEST_GATES(...)`, `BUILD_GATE_EXPLAINABILITY(...)`, and manifest gate projection rules.
- `core_engine.md`, especially pre-seal / post-seal gate flow and the requirement that ordered gate refs survive terminal reload.
- `data_model.md`, especially `GateDecisionRecord`.
- `state_machines.md`, plus any state-transition rules already established for downstream artifacts that consume gate refs.
- `gate_decision_explainability_and_reason_code_compression_contract.md`.
- `invariant_enforcement_and_fail_closed_contract.md`.
- `PATCH_RESOLUTION_INDEX.md`, especially fixes around dominant reason drift, effective-scope canonicalization, and override-governed posture.
- `AUDIT_FINDINGS.md` where gate explainability or top-level reason projection is discussed.
- `test_vectors.md`, especially `TV-64H`, `TV-64I`, `TV-73`, and `TV-79L`.
- Existing schema `schemas/gate_decision_record.schema.json`.

Deliverables to create:
1. `packages/backend-compute/src/models/gate_decision_record.ts`.
2. `packages/backend-compute/src/repositories/gate_decision_record_repository.ts`.
3. `packages/backend-compute/src/services/build_gate_semantics_contract.ts`.
4. `packages/backend-compute/src/services/build_gate_decision_explainability.ts`.
5. `packages/backend-compute/src/services/get_canonical_gate_stage_profile.ts`.
6. `packages/backend-compute/src/services/evaluate_gate_chain.ts`.
7. `packages/backend-compute/src/services/persist_gate_batch.ts`.
8. `packages/backend-compute/src/services/append_manifest_gates.ts`.
9. `packages/backend-compute/src/services/project_manifest_gate_outcomes.ts`.
10. `packages/backend-compute/src/services/transition_gate_projection.ts` or equivalent projection helper if needed by existing manifest code.
11. `db/migrations/phase03_0126_gate_decision_record_and_ordered_gate_engine.sql`.
12. `docs/compute/gate_decision_record_and_ordered_gate_evaluation_engine.md`.
13. `tests/unit/backend_compute/gate_decision_record_contract.spec.ts`.
14. `tests/unit/backend_compute/ordered_gate_engine.spec.ts`.
15. `tests/integration/backend_compute/gate_batch_persistence_and_append.spec.ts`.

Required design decisions you must make explicit:
- Define the canonical stage-order table as data, not as implicit enum order.
  It must preserve the sequence described by the algorithm:
  `ACCESS_GATE` is outside this non-access engine,
  then `MANIFEST_GATE`,
  `ARTIFACT_CONTRACT_GATE`,
  `INPUT_BOUNDARY_GATE`,
  `DATA_QUALITY_GATE`,
  `RETENTION_EVIDENCE_GATE`,
  `PARITY_GATE`,
  `TRUST_GATE`,
  and only later conditional stages such as `AMENDMENT_GATE`, `FILING_GATE`, and `SUBMISSION_GATE` when the frozen runtime scope requires them.
- Define how `gate_stage_index` is assigned, persisted, and validated.
  The stage index must remain stable across replay and post-seal append.
- Define the canonical scope serializer for `effective_scope[]`.
  Scope order must be deterministic and identical across child services.
- Define the exact mapping from gate decision enum to `severity`, `decision_rank`, `progression_rank`, `blocking_class`, and `override_dependency_state`.
- Define how `prerequisite_gate_refs[]` and `blocking_dependency_refs[]` are populated so later review and explainability code never needs to reconstruct them from stage order.
- Define the bounded reason-compression and explainability builder shared with later `DecisionBundle` and `TrustSummary` consumers.

Execution steps:
1. Implement the schema-backed model, repository, and persistence contract for `GateDecisionRecord`.
2. Build one canonical gate-stage profile module and make every gate-evaluation caller depend on it instead of local lists or enum order.
3. Implement `build_gate_semantics_contract.ts` so the record freezes decision rank, progression rank, severity profile, reason-order profile, and override dependency posture.
4. Implement `build_gate_decision_explainability.ts` so `reason_codes[]`, `dominant_reason_code`, `plain_explanation`, and `decision_explainability_contract{...}` are emitted once on the backend and stay replay-safe.
5. Implement `evaluate_gate_chain.ts` so it evaluates the selected gates in canonical order, enforces prerequisite posture, and returns a fully ordered batch without persisting out of order.
6. Implement `persist_gate_batch.ts` so ordered writes and paired events happen in one transaction-safe unit and preserve supplied order exactly.
7. Implement `append_manifest_gates.ts` for post-seal append-only behavior and `project_manifest_gate_outcomes.ts` for manifest read-side synchronization without mutating underlying gate records.
8. Add tests for canonical ordering, append-only post-seal behavior, override-governed pass/review posture, dominant-reason stability, and canonical effective scope.

Gap handling you must perform:
- Close the gap where the corpus defines ordered semantics before there is one reusable order-profile implementation.
  Create one shared ordered-gate profile and prohibit ad hoc downstream order tables.
- Close the gap where overridable blocks can leak `NO_VALID_OVERRIDE` into non-blocking records.
  Reserve `override_resolution_state = NO_VALID_OVERRIDE` for `decision = OVERRIDABLE_BLOCK` exactly as the data model requires.
- Close the gap where reason ordering can drift by iteration order or set semantics.
  Persist one deterministic reason ordering and derive `dominant_reason_code` from that order only.
- Close the gap where post-seal gates could silently rewrite or re-rank earlier pre-seal records.
  Make append semantics explicit and immutable.
- Close the gap where later gates might appear to downgrade an earlier `HARD_BLOCK`.
  Preserve earlier progression ceilings even when later records are evaluated for completeness of the tape.

Mandatory edge cases:
- `MANIFEST_GATE` is the only non-access gate allowed to persist empty `prerequisite_gate_refs[]`.
- If a later gate is required by runtime scope but its inputs are not yet available pre-seal, the engine must preserve the need for later append rather than pretending the gate does not exist.
- `dominant_reason_code` must always be present in `reason_codes[]`.
- Conditional stages such as `AMENDMENT_GATE`, `FILING_GATE`, and `SUBMISSION_GATE` must be omitted only when runtime scope and manifest posture prove they are not applicable.
- canonical `effective_scope[]` ordering must be byte-stable across replay.
- the engine must fail closed on duplicate `gate_stage_index`, unknown `gate_code`, illegal backwards stage movement, or missing semantics-contract components.
- append-only writes must never duplicate an already persisted same-stage same-manifest gate without idempotent reload semantics.
- later evaluation may add more records, but it must not mutate previously persisted `decided_at`, reason order, or effective scope for existing gate ids.

Backend, performance, and security requirements:
- Keep gate evaluation pure until the persistence boundary.
- Make ordered evaluation deterministic and side-effect free so replay, compare, and trace tools can reuse it.
- Use transaction boundaries that guarantee ordered insert + paired outbox/event publication if the project already has that substrate.
- Do not log raw sensitive inputs; log gate ids, manifest ids, stage indexes, and reason codes only.
- Do not let browser or API callers choose stage order or dominant reason order.

Optional internal verification surface (only if you add one):
- If you add a read-only gate-tape inspector, place it behind an internal route such as `/internal/decision/gates/:manifestId`.
- The UI must feel like a premium operations ledger, not a dashboard:
  left rail `280px` wide for manifest identity, scope, and stage navigation;
  center pane for the ordered gate tape with one row per stage;
  right rail for dominant reason, prerequisite refs, blocking dependencies, and override posture.
- Use the shared palette and typography contract from the shared operating contract.
- Add one restrained vertical “stage spine” diagram showing the canonical gate order with the active row highlighted and blocked stages marked by semantic color only.
- Animate only disclosure and row focus with `140ms` to `180ms` opacity / translate transitions and full reduced-motion support.
- If this surface exists, implement and test it with Playwright using role-based locators, `getByRole` / `getByText` / `getByLabel`, keyboard step-through of the stage spine, reduced-motion assertions, and trace capture on retry.

Validation rules:
- Unit-test stage-order profile integrity, semantics-contract derivation, dominant reason stability, explainability compression, and append immutability.
- Integration-test pre-seal batch persistence, post-seal append, idempotent reload, and manifest gate projection sync.
- Validate persisted artifacts against `gate_decision_record.schema.json`.
- Re-run the contract validators after assembling the implementation bundle.

Definition of done:
- The repo can emit and persist one authoritative ordered gate tape for a manifest.
- Later terminal, proof, filing, and replay stages can rely on persisted gate records rather than reconstructing order or semantics from in-memory convention.
- Override posture, dominant reason, effective scope, and append-only stage order are deterministic and replay-safe.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
