---
id: pc_0233
slug: phase_05_parallel_wave_03_track_frontend_shared_005_Playwright_or_other_appropriate_tooling_implement_frontend_state_containers_for_etags_view_guards_and_shell_tokens
mode: parallel
phase: "05"
wave: "03"
track: "frontend_shared"
task_number: "005"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_shared_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0233

## Task Summary

005 Playwright or other appropriate tooling implement frontend state containers for etags view guards and shell tokens.

## Metadata

- ID: `pc_0233`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend shared`
- Task Number: `005`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_shared_005_Playwright_or_other_appropriate_tooling_implement_frontend_state_containers_for_etags_view_guards_and_shell_tokens`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0232](./pc_0232.md) `phase_05_parallel_wave_03_track_frontend_shared_004_Playwright_or_other_appropriate_tooling_implement_semantic_selector_and_accessibility_anchor_catalog`
- Next: [pc_0234](./pc_0234.md) `phase_05_parallel_wave_03_track_frontend_shared_006_Playwright_or_other_appropriate_tooling_implement_command_client_receipt_polling_and_problem_envelope_handling`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_shared_005_Playwright_or_other_appropriate_tooling_implement_frontend_state_containers_for_etags_view_guards_and_shell_tokens`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0232` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement frontend state containers for ETags, view guards, route stability, shell tokens, and publication-generation metadata without creating a local legal-state engine.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The algorithm requires route-stability primitives such as `shell_route_key`, `workspace_route_key`, `route_context`, `shell_stability_token`, `view_guard_ref`, policy hashes, frame epoch, and grouped `stability_contract` to govern hydration, reconnect, rebase, stale mutation posture, and cache reuse. The frontend must store and compare these markers, but must not infer legality or workflow truth from them.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `frontend_shell_and_interaction_law.md sections 2.2, 5, and 10`.
- `northbound_api_and_session_contract.md command envelope, stale-view rules, stream/session rules`.
- `stream_resume_and_catch_up_ordering_contract.md required rules`.
- `cache_isolation_and_secure_reuse_contract.md reuse law and scope rules`.
- `low_noise_experience_contract.md stability_contract, stream_recovery_contract, and action strip token requirements`.
- `schemas/route_stability_contract.schema.json, cache_isolation_contract.schema.json, stream_recovery_contract.schema.json, command_envelope.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/state/route_stability_store.ts`
2. `packages/frontend-shell-core/src/state/view_guard_store.ts`
3. `packages/frontend-shell-core/src/state/shell_token_store.ts`
4. `packages/frontend-shell-core/src/state/currentness_evaluator.ts`
5. `packages/frontend-shell-core/src/state/stale_guard_snapshot.ts`
6. `packages/frontend-shell-core/src/state/state_container_errors.ts`
7. `packages/shared-ui/src/state/StabilityDebugPill.tsx for internal use only`
8. `tests/unit/frontend-shell-core/route_stability_store.spec.ts`
9. `tests/playwright/frontend/view_guard_state_containers.spec.ts`
10. `docs/frontend/frontend_state_containers_for_route_stability.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Expose an optional internal “stability pill” that renders only opaque refs and posture labels. It should be a compact monospace chip cluster inside internal atlas/debug routes, never a customer-visible dashboard. Use blue for live-aligned, amber for stale/rebase-required, red only for fail-closed guard mismatch.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- create pure state containers that record route identity, ETag/currentness refs, shell stability token, frame epoch, policy snapshot hash, approval pack hash, request version ref, and grouped stability/cache/stream contracts
- provide selectors that answer: current, stale, rebase required, access rebind required, snapshot only, and illegal mixed-generation basis
- return typed guard snapshots for command formation but never fabricate missing guards
- prevent mixed-generation state by comparing grouped `guard_vector_hash` and component fields together
- include route-scope-specific guard families for manifest, workspace, portal, and governance without cross-contaminating them

Functional, architectural, and edge-case gaps to close:
- close the gap where stale-view guards are assembled ad hoc per button
- close the gap where ETags and shell tokens can be overwritten independently into mixed-generation state
- close the gap where local route state implies legal actionability without durable receipt/backend truth
- close the gap where cache, stream, and command guard stores drift from each other

Mandatory edge cases:
- receiving a newer frame epoch with old shell stability token must become `REBASE_REQUIRED` or typed mixed-generation error, not “current”
- missing guard required for a mutation family must prevent command formation
- access/masking/session drift must produce access-rebind posture and purge cues
- snapshot-only route state must not offer stream resume controls

Playwright, accessibility, and validation requirements:
- unit-test guard vector comparison and mixed-generation rejection
- Playwright-test a mocked route where a primary action disappears or changes posture when stale guards drift
- verify internal stability pills do not expose raw regulated values
- run schema validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- state containers preserve stability/currentness metadata without becoming a domain engine
- command clients can request exact guard snapshots safely
- stale, mixed-generation, rebase, and access-rebind states are test-covered
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

## Implementation Notes

- Implemented route stability, currentness, shell-token, view-guard, stale-guard, and typed state-container error modules under `packages/frontend-shell-core/src/state`.
- Added internal-only stability debug pill snapshots in `packages/shared-ui/src/state/StabilityDebugPill.tsx`; opaque refs are rendered instead of raw ETag, shell-token, or regulated values.
- Added the mocked internal route at `apps/operator-web/public/internal/view-guard-state-containers/index.html` and route descriptor `apps/operator-web/src/routes/internal/view-guard-state-containers.tsx`.
- Added focused unit and Playwright coverage for guard-vector comparison, mixed-generation rejection, missing guard command blocking, access-rebind purge cues, snapshot-only resume suppression, stale action posture, opaque refs, keyboard focus, and reduced motion.
- Added `docs/frontend/frontend_state_containers_for_route_stability.md` with the checked contract inputs and runtime guard rules.
