---
id: pc_0093
slug: phase_03_parallel_wave_01_track_backend_access_009_implement_access_binding_hash_and_dependency_topology_hash_calculators
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "009"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0093

## Task Summary

implement access binding hash and dependency topology hash calculators.

## Metadata

- ID: `pc_0093`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `009`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_009_implement_access_binding_hash_and_dependency_topology_hash_calculators`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0092](./pc_0092.md) `phase_03_parallel_wave_01_track_backend_access_008_implement_step_up_approval_and_authentication_level_policy_services`
- Next: [pc_0094](./pc_0094.md) `phase_03_parallel_wave_01_track_backend_access_010_implement_session_revocation_device_binding_and_csrf_protection_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0086_to_0093.md](../shared_operating_contract_0086_to_0093.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_009_implement_access_binding_hash_and_dependency_topology_hash_calculators`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0092` if they exist, especially `pc_0017`, `pc_0027`, `pc_0050`, `pc_0060`, `pc_0061`, `pc_0063`, `pc_0064`, `pc_0065`, `pc_0067`, `pc_0068`, `pc_0074`, `pc_0075`, `pc_0076`, `pc_0084`, `pc_0086`, `pc_0089`, `pc_0090`, `pc_0091`, and `pc_0092`.

Mission:
Implement the canonical hash calculators for `access_binding_hash` and `dependency_topology_hash`, plus any adjacent canonical serialization and `simulation_basis_hash` helpers required to preserve the corpus formulas exactly.
This task is about determinism, replay trust, and auditability.
If hash inputs drift because of list ordering, null handling, or implicit JSON serialization differences, the whole governance and replay model becomes unreliable.

Source algorithm grounding you must honor:
- `actor_and_authority_model.md`, especially the definition of `access_binding_hash` in `3.9 Policy decision model` and the rule that governance mutation access-binding must also bind current simulation / topology basis.
- `modules.md`, especially the explicit formulas for `dependency_topology_hash`, `simulation_basis_hash`, `hazard_contract_hash`, and `basis_contract_hash` inside `SIMULATE_GOVERNANCE_MUTATION(...)`.
- `data_model.md`, especially the hash-bearing fields across `PrincipalContext`, `AuthorizationDecision`, `GovernanceMutationHazardContract`, `GovernanceMutationBasisContract`, `RunManifest`, and `AuthorityOperation`.
- `admin_governance_console_architecture.md`, especially the stale-guard rules that compare policy snapshot, topology, and basis hashes.
- `contract_integrity_requirements.md` and `implementation_conventions.md`, especially any canonical serialization or deterministic data-handling constraints.
- Existing schemas including `authorization_decision.schema.json`, `governance_mutation_hazard_contract.schema.json`, and `governance_mutation_basis_contract.schema.json`.

Deliverables to create:
1. `packages/backend-access/src/hash/canonical_hash_serializer.ts`.
2. `packages/backend-access/src/hash/access_binding_hash.ts`.
3. `packages/backend-access/src/hash/dependency_topology_hash.ts`.
4. `packages/backend-access/src/hash/simulation_basis_hash.ts`.
5. `packages/backend-access/src/hash/hazard_contract_hash.ts` if not already implied by another package.
6. `packages/backend-access/src/hash/basis_contract_hash.ts` if not already implied by another package.
7. `packages/backend-access/src/hash/hash_vector_fixtures.ts`.
8. `docs/access/hash_calculation_contracts.md`.
9. `tests/unit/backend_access/hash_calculators.spec.ts`.
10. `tests/integration/backend_access/hash_stability_and_replay.spec.ts`.

Required design decisions you must make explicit:
- Reuse the canonical primitive and hashing substrate from `pc_0064` if one exists.
  Do not silently invent a new digest algorithm or serialization format.
  If no prior decision exists, choose one canonical approach, document it, and emit `PROVIDER_DEFAULT_APPLIED` with rationale.
- Define the exact canonical serialization rules for strings, enums, arrays, object key ordering, nulls, absent fields, timestamps, decimals, and nested vectors.
- Define the separation between generic serialization helpers and domain-specific input assembly for each hash type.
- Define how versioning works if the platform ever needs to evolve hash inputs without invalidating historical artifacts.
- Define how hash fixtures are published so other languages or services can verify compatible implementations.

Execution steps:
1. Inventory all places where the corpus expects hash-bearing fields and map each field to its exact input vector.
2. Implement one canonical serializer that produces byte-stable inputs independent of host-language map ordering.
3. Implement `access_binding_hash` assembly using the exact fields from the corpus: decision posture, effective scope, effective partition scope refs, policy snapshot, required authn level, canonical required approvals, masking rules, delegation snapshot refs, authority-link snapshot refs, and for governance mutation decisions any required topology / simulation basis fields.
4. Implement `dependency_topology_hash` using the exact formula from `modules.md`: canonical ordered impacted node ids, canonical ordered edge list, node-weight profile ref, edge-weight profile ref, and referenced object-version refs.
5. Implement `simulation_basis_hash` so governance simulation can bind policy snapshot, topology hash, canonical proposed diff, acting principal ref, requested approver scope, and simulation profile ref exactly as specified.
6. Add deterministic fixtures and golden tests that are reusable by other languages or future services.
7. Document the canonicalization contract clearly enough that later agents do not accidentally hash pretty-printed JSON or unordered maps.

Gap handling you must perform:
- Close the gap where access and simulation hashes are described in prose but not implemented consistently.
- Close the gap where different packages or languages could hash the same logical payload differently.
- Close the gap where governance mutation decisions carry topology or basis fields but the hash calculators omit them.
- Close the gap where null-versus-absent or list-order drift changes hashes accidentally.
- Close the gap where replay and audit tooling lack fixed golden vectors for conformance checks.

Mandatory edge cases:
- same logical scope list with different input ordering must produce the same hash after canonicalization
- null field vs absent field behavior must match the documented serializer contract and stay stable
- governance decision includes topology and simulation basis; `access_binding_hash` must change when either changes
- non-governance decision omits topology and simulation basis; hash must remain stable and not smuggle placeholder strings
- topology hash must change if referenced object-version refs change even when node ids and edge ids stay the same
- simulation basis hash must change when requested approver scope changes
- nested vectors with duplicate entries must normalize or fail according to the canonical input contract, never hash unpredictably
- two services using different JSON key insertion orders must still produce identical digests

Backend, performance, and security requirements:
- Hashing must be deterministic, portable, and fast enough for authorization hot paths.
- Never hash raw secrets, raw tokens, or opaque authority payloads when stable references or snapshot ids are the contractually correct inputs.
- Keep serializer and hash calculators side-effect free.
- Prefer explicit typed input builders over “hash whatever object we have at runtime.”
- No new product UI is required.
  If you add an internal hash-vector inspection page, keep it internal-only, render canonical input vectors and digests clearly, and verify it with Playwright using semantic locators.

Validation rules:
- Unit-test canonical serialization, list ordering, null handling, timestamp normalization, and domain-specific input assembly.
- Integration-test replay scenarios where historical decisions and simulations are rehydrated and must reproduce their stored hashes exactly.
- Publish cross-language-ready golden vectors and verify that they round-trip through the generated bindings where possible.
- Prove that governance stale checks can compare stored and recomputed hashes deterministically.
- Prove that unsupported or ambiguous input states fail closed instead of generating ad hoc hashes.

Definition of done:
- The repo has one canonical hashing substrate for access and governance topology semantics.
- Later replay, approval, stale-guard, and audit flows can trust the hash values as durable evidence.
- Deterministic hash fixtures exist so future implementations can prove compatibility instead of guessing.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
