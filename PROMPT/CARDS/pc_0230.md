---
id: pc_0230
slug: phase_05_parallel_wave_03_track_frontend_shared_002_Playwright_or_other_appropriate_tooling_implement_cross_shell_design_tokens_and_theme_runtime
mode: parallel
phase: "05"
wave: "03"
track: "frontend_shared"
task_number: "002"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_shared_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0230

## Task Summary

002 Playwright or other appropriate tooling implement cross shell design tokens and theme runtime.

## Metadata

- ID: `pc_0230`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend shared`
- Task Number: `002`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_shared_002_Playwright_or_other_appropriate_tooling_implement_cross_shell_design_tokens_and_theme_runtime`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0229](./pc_0229.md) `phase_05_parallel_wave_03_track_frontend_shared_001_Playwright_or_other_appropriate_tooling_create_shared_web_frontend_workspace_and_route_contract_scaffold`
- Next: [pc_0231](./pc_0231.md) `phase_05_parallel_wave_03_track_frontend_shared_003_Playwright_or_other_appropriate_tooling_implement_interaction_layer_foundation_and_surface_registry`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_shared_002_Playwright_or_other_appropriate_tooling_implement_cross_shell_design_tokens_and_theme_runtime`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0229` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the cross-shell design-token system and theme runtime that turns the three shell families into explicit, schema-aligned visual contracts rather than route-local CSS conventions.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The algorithm says shared design tokens are not decorative variables: `InteractionLayerFoundationContract` freezes density, spacing, responsive compaction, selector profile, motion, preview, history, notification, continuity, and feedback-truth posture per shell family. Therefore the theme runtime must derive family-specific CSS variables and component affordance tokens from explicit shell/foundation contracts, not from arbitrary route names or designer preference.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `cross_shell_design_token_and_interaction_layer_foundation_contract.md, especially governing model and required family mappings`.
- `frontend_shell_and_interaction_law.md sections 1, 2, 3, 5, 8, and 10`.
- `low_noise_experience_contract.md default visible shell, operator interaction layer, cognitive budget, and FE-25 cache isolation notes`.
- `customer_client_portal_experience_contract.md shell continuity, simplified visualization, copy rules, accessibility`.
- `admin_governance_console_architecture.md profile boundary, shared interaction rules, frontend systems architecture`.
- `UIUX_DESIGN_SKILL.md core design language, layout strategy, signature module constraints, Playwright-first expectation`.
- `schemas/interaction_layer_foundation_contract.schema.json, operator_interaction_layer.schema.json, portal_interaction_layer.schema.json, governance_interaction_layer.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/tokens/shell_token_contracts.ts`
2. `packages/frontend-shell-core/src/tokens/interaction_foundation_to_theme.ts`
3. `packages/frontend-shell-core/src/tokens/motion_tokens.ts`
4. `packages/frontend-shell-core/src/tokens/responsive_compaction_tokens.ts`
5. `packages/shared-ui/src/foundation/TaxatThemeProvider.tsx`
6. `packages/shared-ui/src/foundation/taxat-theme.css`
7. `packages/shared-ui/src/foundation/shellTheme.css`
8. `apps/operator-web/src/routes/internal/frontend-shell-foundation-atlas.tsx patch or create`
9. `apps/client-portal-web/src/routes/internal/portal-theme-proof.tsx only if internal routes are already allowed`
10. `packages/playwright-kit/src/fixtures/theme_contract_fixture.ts`
11. `tests/playwright/frontend/cross_shell_theme_runtime.spec.ts`
12. `docs/frontend/cross_shell_design_tokens_and_theme_runtime.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Create a “Foundation Atlas” internal page with three horizontally aligned shell cards, not KPI tiles. Each card shows a small contract glyph: Calm = four stacked planes; Portal = one primary task column with a below-primary support shelf; Governance = dense canvas with one auxiliary sidecar. Use operator blue, portal teal, and governance purple only as 2px accent rules, focus rings, and small contract pins. Show token samples as labelled strips: density, spacing, motion, current/history, preview, notification, recovery. Do not use gradients except a subtle stone wash for surface separation; no AI sparkles or neon diagrams.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- create a typed `ShellFamily` and `InteractionLayerFoundationContract` adapter that mirrors the schema enums exactly
- derive CSS custom properties from the foundation contract, including density, spacing, support spacing, compaction, motion duration/easing, focus ring, surface radius, and state accents
- block route-local overrides for shell density/motion unless they are explicitly bound to a shell-family token
- expose a `TaxatThemeProvider` that accepts `shellFamily`, `foundationContract`, and optional `reducedMotion` while validating shell/profile alignment
- include a tiny typography-led Taxat wordmark/mark if branding is needed: one `T` monogram inside a rounded square, no icon library dependency
- ensure portal and governance can consume the same token runtime without inheriting calm-shell layout semantics

Functional, architectural, and edge-case gaps to close:
- close the gap where shared scaffolds can exist without real token enforcement
- close the gap where visual styling can bypass `InteractionLayerFoundationContract`
- close the gap where reduced-motion, support-region spacing, and current/history grammar are not first-class theme tokens
- close the gap where portal/governance accents could be implemented as route-local colors instead of contract-bound family accents

Mandatory edge cases:
- mismatched shell family and selector profile must fail closed in development/test builds
- missing foundation contract must render a typed contract error in internal fixtures, not silently fall back to generic theme
- reduced-motion preference must disable spatial displacement while preserving state labels and focus order
- compact viewport must change compaction tokens only, not shell family or route meaning

Playwright, accessibility, and validation requirements:
- add Playwright tests that mount each shell family and assert computed token variables, role/heading structure, focus ring visibility, and reduced-motion behavior
- assert no route-local CSS class can override shell motion duration beyond the contract ceiling in the atlas fixture
- capture screenshots for the three shell-family cards as visual baselines only after deterministic seeded data is used
- run schema self-test and forensic guard after adding token docs or samples
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- theme runtime maps every allowed shell family to exact contract tokens
- internal atlas proves token and motion behavior without becoming a dashboard
- Playwright verifies shell token parity, reduced motion, responsive compaction, and semantic anchors
```

## Verification

- Implemented contract-bound theme runtime in `packages/frontend-shell-core/src/tokens/`:
  - `shell_token_contracts.ts`
  - `interaction_foundation_to_theme.ts`
  - `motion_tokens.ts`
  - `responsive_compaction_tokens.ts`
- Updated `packages/shared-ui/src/foundation/TaxatThemeProvider.tsx` to validate `shellFamily` plus `foundationContract`, derive CSS custom properties, expose data attributes, and fail closed on missing or mismatched contracts.
- Added shared CSS foundations:
  - `packages/shared-ui/src/foundation/taxat-theme.css`
  - `packages/shared-ui/src/foundation/shellTheme.css`
- Patched the internal frontend shell foundation atlas to render three shell-family theme cards with contract glyphs, token strips, focus proof, motion probes, reduced-motion behavior, compact-mode data, and a typed missing-foundation error fixture.
- Added `packages/playwright-kit/src/fixtures/theme_contract_fixture.ts`.
- Added `tests/playwright/frontend/cross_shell_theme_runtime.spec.ts`.
- Added `docs/frontend/cross_shell_design_tokens_and_theme_runtime.md`.
- Did not create `apps/client-portal-web/src/routes/internal/portal-theme-proof.tsx` because the current portal app has debug routes but no established internal route namespace; portal proof is covered by the shared internal atlas and Playwright fixture.
- Validated interaction foundations for all 3 shell routes and validated synthesized operator, portal, and governance interaction-layer samples against:
  - `Algorithm/schemas/interaction_layer_foundation_contract.schema.json`
  - `Algorithm/schemas/operator_interaction_layer.schema.json`
  - `Algorithm/schemas/portal_interaction_layer.schema.json`
  - `Algorithm/schemas/governance_interaction_layer.schema.json`
- `pnpm exec playwright test --config=playwright.config.ts --project=browser tests/playwright/frontend/cross_shell_theme_runtime.spec.ts tests/playwright/frontend/shared_workspace_scaffold.spec.ts tests/playwright/frontend/shared_workspace_route_contracts.spec.ts` passed: 21 tests.
- `PYTHONDONTWRITEBYTECODE=1 ./.venv/bin/python3 Algorithm/scripts/validate_contracts.py --self-test` passed.
- `PYTHONDONTWRITEBYTECODE=1 python3 Algorithm/tools/forensic_contract_guard.py` passed.
- `pnpm run validate-contracts` passed.
- `python3 ./scripts/tasks/task_graph.py --json validate >/dev/null` passed.
- Markdown link cross-check passed for 40 links across the frontend docs.
- `git diff --check` passed for the touched prompt, frontend, shared package, docs, and test paths.
- `pnpm exec tsc --noEmit -p tsconfig.quality.json` still fails only on pre-existing `packages/domain-kernel` strictness issues and generated-model missing type names; no `pc_0230` files remained in the TypeScript error output after patching.
