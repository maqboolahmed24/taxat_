---
id: pc_0132
slug: phase_03_parallel_wave_01_track_backend_compute_012_implement_twin_reconciliation_state_interpretation_state_and_portfolio_summary_services
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "012"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_012
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0132

## Task Summary

implement twin reconciliation state, interpretation state, and portfolio summary services.

## Metadata

- ID: `pc_0132`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `012`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_012_implement_twin_reconciliation_state_interpretation_state_and_portfolio_summary_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0131](./pc_0131.md) `phase_03_parallel_wave_01_track_backend_compute_011_implement_twin_state_snapshot_twin_view_and_mismatch_summary_builder`
- Next: [pc_0133](./pc_0133.md) `phase_03_parallel_wave_01_track_backend_authority_001_implement_obligation_mirror_filing_case_and_filing_packet_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0126_to_0133.md](../shared_operating_contract_0126_to_0133.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_012_implement_twin_reconciliation_state_interpretation_state_and_portfolio_summary_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0131` if they exist, especially `pc_0010`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0065`, `pc_0067`, `pc_0074`, `pc_0076`, `pc_0086` through `pc_0093`, `pc_0097`, `pc_0108`, `pc_0124`, `pc_0125`, `pc_0128`, `pc_0129`, `pc_0130`, and `pc_0131`.

Mission:
Implement the persisted twin follow-up layer that plans and transitions `TwinReconciliationState`, derives low-noise `TwinInterpretationState`, and aggregates `TwinPortfolioSummary` from persisted twin artifacts without recomputing subject-level comparison logic.
This task must preserve bounded reconciliation budget posture, next-action ownership, escalation workflow linkage, authority-waiting deadlines, interpretation defaults that stay low-noise and authority-first, and a portfolio attention ranking derived from existing persisted twin artifacts rather than new ad hoc comparisons.

Source algorithm grounding you must honor:
- `twin_view_contract.md`, especially reconciliation triggers, waiting-on-authority handling, interpretation-state rules, authority-first summary defaults, and portfolio attention ranking.
- `modules.md`, especially `PLAN_TWIN_RECONCILIATION(...)`, `BUILD_TWIN_PORTFOLIO_SUMMARY(...)`, and the twin view output contract that includes `TwinReconciliationState` and `TwinInterpretationState`.
- `state_machines.md`, especially `TwinReconciliationState.lifecycle_state`.
- `data_model.md`, especially `TwinReconciliationState`, `TwinInterpretationState`, and `TwinPortfolioSummary`.
- `authority_truth_and_internal_projection_separation_contract.md`.
- `PATCH_RESOLUTION_INDEX.md`, especially any fixes around automatic budget persistence, escalation, mismatch aggregation drift, or interpretation focus defaults.
- `test_vectors.md`, especially `TV-45`, `TV-46`, `TV-47`, `TV-49A`, `TV-49C`, `TV-50`, and any workflow / authority-waiting twin vectors.
- Existing schemas:
  `twin_reconciliation_state.schema.json`,
  `twin_interpretation_state.schema.json`,
  and `twin_portfolio_summary.schema.json`.

Deliverables to create:
1. `packages/backend-twin/src/models/twin_reconciliation_state.ts`.
2. `packages/backend-twin/src/models/twin_interpretation_state.ts`.
3. `packages/backend-twin/src/models/twin_portfolio_summary.ts`.
4. `packages/backend-twin/src/repositories/twin_reconciliation_state_repository.ts`.
5. `packages/backend-twin/src/repositories/twin_interpretation_state_repository.ts`.
6. `packages/backend-twin/src/repositories/twin_portfolio_summary_repository.ts`.
7. `packages/backend-twin/src/services/plan_twin_reconciliation.ts`.
8. `packages/backend-twin/src/services/transition_twin_reconciliation_state.ts`.
9. `packages/backend-twin/src/services/build_twin_interpretation_state.ts`.
10. `packages/backend-twin/src/services/build_twin_portfolio_summary.ts`.
11. `packages/backend-twin/src/services/rank_twin_attention.ts`.
12. `db/migrations/phase03_0132_twin_reconciliation_interpretation_and_portfolio.sql`.
13. `docs/twin/twin_reconciliation_state_interpretation_state_and_portfolio_summary_services.md`.
14. `tests/unit/backend_twin/twin_reconciliation_contract.spec.ts`.
15. `tests/unit/backend_twin/twin_interpretation_and_portfolio_ranking.spec.ts`.
16. `tests/integration/backend_twin/twin_reconciliation_and_portfolio_summary.spec.ts`.

Required design decisions you must make explicit:
- Define the reconciliation dedupe key.
  Identical unresolved deltas must dedupe to one active reconciliation state per comparison subject chain.
- Define the automatic budget model:
  attempt counters, deadline semantics, escalation trigger, and no-resend posture after exhaustion must be persisted and replay-safe.
- Define `next_action_owner`, workflow linkage, and authority-waiting semantics.
- Define low-noise interpretation defaults:
  default view space, compare mode, sort mode, noise filter, dominant attention state, authority-first summary, and overlay defaults.
- Define portfolio attention ranking.
  It must aggregate persisted twin posture rather than recomputing mismatch semantics from raw subjects.

Execution steps:
1. Implement schema-backed models and repositories for reconciliation state, interpretation state, and portfolio summary.
2. Implement `plan_twin_reconciliation.ts` so contradictory or out-of-band authority states open reconciliation immediately, identical unresolved deltas dedupe correctly, and bounded budget posture is frozen on the state.
3. Implement `transition_twin_reconciliation_state.ts` so allowed transitions preserve attempt count, deadlines, escalation workflow refs, and no-reset semantics across replay or continuation.
4. Implement `build_twin_interpretation_state.ts` so the default operator view stays low-noise, authority-first, and focused on the dominant mismatch or reconciliation issue rather than rendering audit detail first.
5. Implement `rank_twin_attention.ts` and `build_twin_portfolio_summary.ts` so portfolio rollups aggregate persisted twins and their summaries without recomputing per-subject comparison results.
6. Add tests for immediate reconciliation on contradictory authority state, waiting-on-authority deadline behavior, exhausted automatic budget, low-noise interpretation defaults, and portfolio attention ranking.

Gap handling you must perform:
- Close the gap where reconciliation semantics exist before one persisted reconciliation state service does.
  Build one authoritative state machine with explicit budget, escalation, and owner fields.
- Close the gap where reconnect / replay / continuation could silently reset automatic attempts.
  Persist counters, deadlines, and budget posture explicitly and never rebuild them from UI memory.
- Close the gap where interpretation defaults could drift per surface.
  Persist one low-noise interpretation state rather than letting each shell pick a different focus.
- Close the gap where portfolio summaries could recalculate mismatch logic differently from per-twin builders.
  Aggregate persisted twin artifacts only.

Mandatory edge cases:
- `NOT_REQUIRED` may exist only when no active mismatch requires reconciliation.
- contradictory or out-of-band authority states must open reconciliation immediately.
- waiting-on-authority past deadline must open owned workflow rather than silently looping.
- no transition path may silently reset automatic resend or reconciliation budget after exhaustion.
- `RESOLVED` must carry explicit terminal `resolution_state`; absence of open work is not enough.
- interpretation defaults must stay authority-first and suppress informational noise when higher-severity issues exist.
- portfolio ranking must preserve deterministic ordering and stable top-ref selection on ties.
- aggregation must not recompute or mutate underlying twin mismatch state.

Backend, performance, and security requirements:
- Keep reconciliation planning deterministic and side-effect free until the persistence boundary.
- Reuse workflow / ownership / escalation helpers from earlier cards where available.
- Do not store renderer-only UI toggles that are not part of the persisted interpretation contract.
- Avoid logging full subject payloads; log twin ids, delta refs, budget state, and owner transitions only.

Optional internal verification surface (recommended because this task spans individual and portfolio posture):
- If you add a read-only twin portfolio atlas, place it behind internal routes such as `/internal/twin/portfolio/:scopeRef` and `/internal/twin/view/:twinId/reconciliation`.
- The UI must be minimalist premium and highly structured:
  a left `280px` attention ladder for portfolio-ranked twins,
  a central twin summary canvas with readiness / reconciliation cards,
  and a right `320px` ownership-and-budget rail showing attempts, deadlines, escalation, and workflow refs.
- Use one restrained diagram only: a thin “attention ladder” with ranked rows and a small inline budget-state meter.
- Typography and palette should follow the shared contract, with attention ranks in mono chips and no celebratory color overload.
- Motion:
  `140ms` row focus,
  `180ms` detail-rail reveal,
  no animated charts,
  full reduced-motion support.
- If this surface exists, implement and verify it with Playwright using semantic locators, keyboard ladder traversal, reduced-motion assertions, trace capture, and focus restoration after drill-down.

Validation rules:
- Unit-test reconciliation dedupe, transition legality, budget exhaustion, interpretation defaults, and attention ranking.
- Integration-test twin -> reconciliation -> portfolio summary flow under contradictory, stale, waiting-on-authority, and resolved scenarios.
- Validate persisted artifacts against the relevant twin schemas.
- Re-run the contract validators after assembling the implementation bundle.

Definition of done:
- The repo can persist one authoritative reconciliation state, one low-noise interpretation state, and one deterministic portfolio summary per scope.
- Later shell, operations, and authority-follow-up surfaces can consume persisted twin posture without recomputing budgets, attention ranking, or interpretation defaults.
- Automatic budget and escalation semantics remain durable across replay, restore, and continuation.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
