---
id: pc_0087
slug: phase_03_parallel_wave_01_track_backend_access_003_implement_authority_layer_boundary_contract_model_and_validations
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "003"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0087

## Task Summary

implement authority layer boundary contract model and validations.

## Metadata

- ID: `pc_0087`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `003`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_003_implement_authority_layer_boundary_contract_model_and_validations`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0086](./pc_0086.md) `phase_03_parallel_wave_01_track_backend_access_002_implement_principal_context_and_authorization_decision_models`
- Next: [pc_0088](./pc_0088.md) `phase_03_parallel_wave_01_track_backend_access_004_implement_delegation_grant_authority_link_and_exceptional_authority_grant_repositories`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0086_to_0093.md](../shared_operating_contract_0086_to_0093.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_003_implement_authority_layer_boundary_contract_model_and_validations`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0086` if they exist, especially `pc_0022`, `pc_0023`, `pc_0034`, `pc_0038`, `pc_0039`, `pc_0040`, `pc_0050`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0068`, `pc_0076`, `pc_0083`, `pc_0085`, and `pc_0086`.

Mission:
Implement the typed `AuthorityLayerBoundaryContract` model, its validation rules, and the shared resolver that materializes boundary state for every integration-capable authorization result.
This task exists to prevent a dangerous category error: collapsing tenant permission, client delegation, authority-link validity, exceptional authority, human-gate evidence, and authority-of-record posture into a single boolean or a single role check.

Source algorithm grounding you must honor:
- `data_model.md`, especially `AuthorityLayerBoundaryContract` and its field-level semantics.
- `actor_and_authority_model.md`, especially `3.4 Authority layers`, `3.9 Policy decision model`, `3.10 Delegation rules`, `3.11 Non-delegable and step-up actions`, `3.12 Authority precedence rules`, and `3.14 Invariants`.
- `connector_delegation_contract.md`, especially the rules that live authority mutation or projections depending on delegated client authority must resolve current `ConnectorBinding`, `DelegationGrant`, `AuthorityLink`, `AuthorityOperationProfile`, and any `ExceptionalAuthorityGrant` before execution.
- `authority_truth_and_internal_projection_separation_contract.md`, especially the rule that internal overrides and accepted risk do not become external authority confirmation.
- `invariants_and_gates.md`, especially authority-facing-operation and override-governance invariants.
- Existing schemas including `authority_layer_boundary_contract.schema.json`, `authorization_decision.schema.json`, `principal_context.schema.json`, and `governance_access_simulation.schema.json`.

Deliverables to create:
1. `packages/backend-access/src/models/authority_layer_boundary_contract.ts`.
2. `packages/backend-access/src/services/authority_layer_boundary_resolver.ts`.
3. `packages/backend-access/src/services/authority_chain_layer_builder.ts`.
4. `packages/backend-access/src/services/authority_boundary_validator.ts`.
5. `packages/backend-access/src/services/authority_boundary_reasoning.ts`.
6. `config/access/authority_layer_interpretation_rules.json`.
7. `docs/access/authority_layer_boundary_contract.md`.
8. `tests/unit/backend_access/authority_layer_boundary_validator.spec.ts`.
9. `tests/integration/backend_access/authority_layer_boundary_resolution.spec.ts`.

Required design decisions you must make explicit:
- Define how the resolver receives its inputs: tenant permission state, delegation grant state, delegation freshness state, authority-link state, exceptional-authority state, principal class, human-gate requirement, and precedence posture.
- Define whether the contract is persisted as a nested JSON object, relationally decomposed for indexing, or both.
- Define a stable representation for `authority_chain_layers[]` that can be reused later by governance simulations and authority request envelopes without leaking UI-only semantics into the core model.
- Define how authority-of-record precedence is represented when external truth conflicts with internal workflow truth or masked projections.
- Define which fields are derived vs source-frozen, and document exactly when recomputation is legal.

Execution steps:
1. Import and honor the exact schema semantics, including the reverse guards and enum pairings already encoded in `authority_layer_boundary_contract.schema.json`.
2. Build a resolver that materializes the contract from `PrincipalContext`, `DelegationGrant`, `AuthorityLink`, `ExceptionalAuthorityGrant`, tenant permission state, and the current action family / resource class.
3. Implement validation that rejects illegal cross-layer combinations, such as `INTERNAL_ONLY` while an external authority state is simultaneously asserted, or service principals claiming satisfied human-gate resolution.
4. Implement an authority-chain builder that emits a stable ordered list of layers suitable for simulation, audit, and internal explanation.
5. Provide shared reasoning helpers so downstream problem responses and simulation endpoints can explain why a boundary packet resolved the way it did without re-implementing logic in controllers.
6. Ensure the resulting contract is embedded in `AuthorizationDecision` and `GovernanceAccessSimulation` exactly where the corpus expects it.

Gap handling you must perform:
- Close the gap where layered authority is currently implicit in scattered role checks or connector code paths.
- Close the gap where authority-of-record truth could be confused with internal acceptance, masking, or override posture.
- Close the gap where exceptional authority could accidentally be treated as a generic permission escalation rather than a bounded, incident-linked exception.
- Close the gap where simulation and live execution might compute different boundary packets from the same inputs.
- Close the gap where downstream consumers would otherwise invent their own string descriptions and state mappings.

Mandatory edge cases:
- `binding_scope_class=INTERNAL_ONLY` while client delegation or authority-link state is not `NOT_REQUIRED`
- service principal resolves `human_gate_requirement=STEP_UP_REQUIRED` and claims `human_gate_resolution_state=SATISFIED`
- imported delegation basis resolves `delegation_freshness_state=NOT_APPLICABLE` when freshness is required
- `exceptional_authority_state=BOUNDED_INTERNAL_EXCEPTION` without `human_gate_resolution_state=EVIDENCE_FROZEN`
- authority-link lifecycle is healthy but token-client binding state is mismatch
- authority-of-record indicates uncertainty while internal workflow tries to present a confirmed state
- tenant permission is sufficient but delegation is stale or absent for a client-affecting action
- supporting-agent or reduced-authority boundary tries to reach an action family reserved to main-agent or full-authority posture

Backend, performance, and security requirements:
- The resolver must be deterministic and side-effect free.
  It computes a packet; it does not mutate delegation or authority-link state.
- Validation must be fail-closed and should surface typed reason codes that are stable enough for audit and API problems.
- Keep the boundary packet queryable in control-store or audit records without having to parse connector-specific token blobs.
- Never equate internal override posture with external authority confirmation.
- No new product UI is required.
  If you add an internal inspection route for manual debugging, it must render the layered packet as a clear stacked boundary panel, use semantic headings and lists, support reduced motion, and be covered by Playwright using role-first locators.

Validation rules:
- Validate every resolved contract against `authority_layer_boundary_contract.schema.json`.
- Unit-test every schema reverse guard and every forbidden combination from the actor model.
- Integration-test resolution across self-acting, client-delegated, tenant-internal, service, stale-imported, exceptional-authority, and authority-of-record-conflict cases.
- Prove that `AuthorizationDecision` and `GovernanceAccessSimulation` can both embed the identical packet without ad hoc transformation.
- Prove that the same inputs always yield the same ordered `authority_chain_layers[]` vector.

Definition of done:
- The repo has one authoritative typed model and resolver for layered authority boundaries.
- Authorization, governance simulation, and later authority-request work can all share the same packet instead of recomputing incompatible interpretations.
- The implementation makes it structurally difficult to collapse layered authority into a single weak permission flag.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
