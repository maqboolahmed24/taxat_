---
id: pc_0006
slug: phase_00_seq_006_model_core_engine_end_to_end_execution_swimlane
mode: sequential
phase: "00"
wave: null
track: null
task_number: "006"
block_key: phase_00_seq
original_key: phase_00_seq_006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0006

## Task Summary

model core engine end to end execution swimlane.

## Metadata

- ID: `pc_0006`
- Mode: `sequential`
- Phase: `00`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `006`
- Source Slug: `phase_00_seq_006_model_core_engine_end_to_end_execution_swimlane`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0005](./pc_0005.md) `phase_00_seq_005_extract_invention_boundary_and_system_boundary_requirements`
- Next: [pc_0007](./pc_0007.md) `phase_00_seq_007_catalog_named_module_procedures_and_dependency_edges`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt

Use the shared operating contract first:
[../shared_operating_contract_0006_to_0013.md](../shared_operating_contract_0006_to_0013.md)

```text
Execute roadmap task `phase_00_seq_006_model_core_engine_end_to_end_execution_swimlane`.

Inherit the shared operating contract and consume the outputs from cards `pc_0001` through `pc_0005` if they exist.

Mission:
Transform the authoritative `RUN_ENGINE(...)` narrative into an implementation-grade execution spine.
You are not paraphrasing the engine. You are building the phase map that later service, workflow,
authority, observability, and frontend agents will use as their canonical run-order contract.
The output must make the end-to-end algorithm queryable at the level of phase order, branch predicates,
module calls, state transitions, artifact writes, live experience updates, transaction boundaries,
and terminalization rules.

Algorithm grounding you must honor:
- `core_engine.md -> Summary (technical solution)` for the high-level engine story.
- `core_engine.md -> Outputs (DecisionBundle)` for the authoritative returned object shape.
- `core_engine.md -> Required invariants` for safety properties that span phases.
- `core_engine.md -> Procedure: RUN_ENGINE(...)` for the numbered 18-phase source of truth.
- `core_engine.md -> Execution helper constraints used above` for helper-level behaviors that tighten the pseudocode.
- `core_engine.md -> Boundary specification` for what the run does and does not own.
- `modules.md` for every uppercase procedure called by the core pseudocode.
- `state_machines.md -> 6.2 RunManifest.lifecycle_state`, `6.5 SourceCollectionRun.lifecycle_state`,
  `6.6 Snapshot.lifecycle_state`, `6.18 FilingPacket.lifecycle_state`, `6.19 SubmissionRecord.lifecycle_state`,
  `6.22 AmendmentCase.lifecycle_state`, and `6.24 LowNoiseExperienceFrame.*` for state consequences.
- `invariants_and_gates.md -> Global invariants` and `-> Standard gate chain (canonical families)`.
- `exact_gate_logic_and_decision_tables.md -> 7.2 Gate evaluation order` and the gate sections consumed during the run.
- `observability_and_audit_contract.md -> 14.4 Mandatory correlation keys`, `14.5 Audit event contract`,
  and `14.6 Required audit event families`.
- `replay_and_reproducibility_contract.md`, `manifest_and_config_freeze_contract.md`,
  `late_data_authority_correction_and_replay_propagation_contract.md`,
  and `low_noise_experience_contract.md` for replay, freeze, late-data, and UX continuity rules.

Deliverables to create:
1. `docs/analysis/06_run_engine_end_to_end_execution_swimlane.md`
2. `docs/analysis/06_run_engine_phase_contracts.md`
3. `docs/analysis/06_run_engine_branch_and_terminalization_matrix.md`
4. `diagrams/analysis/06_run_engine_swimlane.mmd`
5. `data/analysis/run_engine_phase_index.json`
6. `data/analysis/run_engine_step_ledger.jsonl`
7. `data/analysis/run_engine_event_timeline.csv`
8. `data/analysis/run_engine_branch_conditions.json`
9. `data/analysis/run_engine_live_experience_map.json`
10. `tools/analysis/build_run_engine_swimlane.py`
11. `tools/analysis/validate_run_engine_swimlane.py`
12. `prototypes/analysis/run_engine_swimlane_atlas/` containing a static browser-viewable atlas
13. `tests/playwright/run_engine_swimlane_atlas.spec.ts`

Each phase record must capture at minimum:
- `phase_id`
- `phase_name`
- `ordered_index`
- `source_heading_or_logical_block`
- `entry_conditions[]`
- `exit_conditions[]`
- `branch_predicates[]`
- `called_modules[]`
- `state_transitions[]`
- `artifact_writes[]`
- `manifest_mutations[]`
- `gate_evaluations[]`
- `audit_events[]`
- `live_experience_updates[]`
- `transaction_boundary`
- `failure_exit_paths[]`
- `notes`

Execution steps:
1. Parse the 18 numbered phases from `core_engine.md -> Procedure: RUN_ENGINE(...)` exactly as written.
   Do not merge, renumber, or rename phases.
2. Expand each phase into ordered internal steps by following uppercase procedure calls and the nearby architectural notes.
3. Build a lane taxonomy that later engineers can read quickly. At minimum include:
   `CALLER_AND_SCOPE`, `AUTHORIZATION`, `MANIFEST_AND_LINEAGE`, `CONFIG_AND_FREEZE`,
   `SOURCE_COLLECTION_AND_CANONICALIZATION`, `PRESEAL_GATES`, `POSTSEAL_DAG`,
   `AUTHORITY_CONTEXT`, `DRIFT_AND_AMENDMENT`, `TRUST_AND_WORKFLOW`,
   `FILING_AND_SUBMISSION`, `LIVE_EXPERIENCE`, `RETENTION_AND_TERMINALIZATION`.
4. Map every `TRANSITION_MANIFEST(...)`, `UPDATE_MANIFEST_OUTPUTS(...)`, `WRITE_ARTIFACT(...)`,
   `RECORD_EVENT(...)`, `SYNC_LIVE_EXPERIENCE(...)`, `SYNC_LIVE_EXPERIENCE_FROM_GATES(...)`,
   `BEGIN_ATOMIC_TRANSACTION()`, `COMMIT_ATOMIC_TRANSACTION()`, and rollback branch.
5. Build a branch matrix covering at minimum:
   - access blocked early return
   - invalid scope or prior context
   - return existing decision bundle
   - new manifest vs reuse sealed manifest vs continuation child vs replay child
   - late-data spawn-child / review-if-late / exclude-late
   - preseal blocked vs sealed success
   - pre-start system fault vs post-start system fault
   - replay reuses frozen post-seal basis
   - early filing-readiness terminalization
   - trust posture terminalization
   - amendment-intent path vs amendment-submit path
   - submit path vs non-submit path
6. Build a live-experience map that records, for each phase, which shell modules are updated and what semantic posture they carry.
   The atlas must preserve the distinction between command-side truth and read-side `ExperienceDelta`.
7. Distinguish the immutable pre-seal context from the started post-seal run.
   The model must show that the sealed manifest exists before `RunStarted`.
8. Capture the ordered phase-to-event timeline so audit and observability engineers can reconstruct legal progression.
9. Implement a static browser-viewable `run_engine_swimlane_atlas` that visualizes the phase spine without changing the semantics.

Visual and interaction specification for `run_engine_swimlane_atlas`:
- This is not a generic dashboard. Design it as a premium “execution observatory” for engineers.
- Use a 12-column desktop grid with `max-width: 1440px`, page padding `32px` desktop / `24px` tablet / `16px` mobile.
- Layout:
  - a sticky left phase rail (`264px`) listing phases 1–18 with branch and terminal posture chips,
  - a central swimlane canvas with horizontally scrollable lane tracks,
  - a sticky right inspector (`352px`) showing source refs, modules, outputs, and failure exits for the selected phase.
- Required visual tokens:
  - background `#0B0D12`
  - primary surfaces `#121721`, `#181E29`
  - border `rgba(255,255,255,0.08)`
  - primary text `#F5F7FA`
  - secondary text `#A7B1BF`
  - accent `#5AA9FF`
  - notice `#E7B04B`
  - success `#52C18C`
  - block/failure `#E96B6B`
- Typography:
  - `Inter` or system sans stack for UI text,
  - `ui-monospace` for event codes, manifest refs, and hashes,
  - title scale 32/40, section heading 20/28, body 14/20, meta 12/16.
- The main diagram must show:
  - phase bands,
  - atomic transaction spans,
  - gate diamonds,
  - artifact capsules,
  - event pins,
  - branch forks,
  - terminal outcomes.
- Motion must be restrained: opacity + 8px translate transitions only, 160–220ms, no looping animation.
- `prefers-reduced-motion` must disable spatial translation and keep only opacity/highlight transitions.
- Include a minimal monochrome Taxat wordmark or monogram only if it stays secondary to the content.

Mandatory Playwright requirements:
- Develop the atlas against deterministic fixture data generated from the analysis outputs.
- Use semantic locators and `data-testid` anchors such as:
  `run-engine-atlas`, `phase-rail`, `phase-row-01`, `lane-canvas`, `lane-CALLER_AND_SCOPE`,
  `selected-phase-detail`, `branch-chip`, `transaction-span`, `event-pin`, `gate-node`,
  `artifact-capsule`, `experience-update-list`.
- Tests must cover:
  - first render
  - phase selection updates inspector state
  - keyboard navigation across the sticky phase rail
  - horizontal lane scrolling preserves selected phase
  - reduced-motion rendering
  - screenshot baseline for at least the full run overview and one branch-heavy phase
  - trace capture on failure

Gap handling you must perform:
- Close the “no authoritative phase-to-step ledger” gap by producing one exact ordered step ledger.
- Close the “no branch matrix for reuse / replay / continuation / late-data / terminalization” gap.
- Close the “audit events are distributed across prose and not indexed per phase” gap.
- Close the “live experience updates are specified but not phase-mapped” gap.
- Close the “transaction and lease boundaries are easy to miss in raw pseudocode” gap.

Mandatory edge cases:
- `raw_requested_scope[]` is preserved for audit, but `runtime_scope[]` drives downstream behavior.
- `masking_context` is read-side only and must not contaminate compute, filing, or transport.
- same-manifest retry against a terminal manifest reloads an existing `DecisionBundle`; it does not allocate a continuation child merely because continuation would otherwise be legal.
- a pre-start system fault while the manifest is already `SEALED` but before `RunStarted` still finalizes as `BLOCKED`.
- pre-seal decisions in `{PASS_WITH_NOTICE, MANUAL_REVIEW}` remain binding after seal.
- replay may reuse frozen post-seal basis and must not be modeled as a fresh authority read.
- `AMENDMENT_GATE` is not evaluated during drift preparation; it happens later in the run.
- authority-owned truth and out-of-band authority corrections must remain distinct from internal progression state.

Validation rules:
- Every numbered phase in `RUN_ENGINE(...)` must appear exactly once and in source order.
- Every uppercase module call in those phases must either resolve to `modules.md` or be explicitly classified as a primitive/helper outside module scope.
- Every `RECORD_EVENT(...)` and `WRITE_ARTIFACT(...)` call in the run spine must be indexed or explicitly marked non-phase-local.
- Every early return and terminalization path must be represented in the branch matrix.
- The Mermaid swimlane, JSON phase index, and browser atlas must describe the same ordered phases.
- The Playwright suite must pass for the atlas, with no arbitrary sleeps.

Definition of done:
- A new engineer can read your outputs and know the exact order of a Taxat run, where the manifest changes state,
  when artifacts are frozen, when gates bind, when authority context is consulted, where late-data branches occur,
  and what the live shell is expected to show at each major point.
- The outputs are detailed enough for later implementation cards to build orchestrators, workers, audit traces,
  and read models without re-deriving the phase structure from raw pseudocode.
```
## Verification

- Prompt authored from direct analysis of the `RUN_ENGINE(...)` phase spine and aligned to the authoritative card slug.
- The swimlane atlas requirement is framed as a contract visualizer and executable validation surface, not as product redesign work.
- Instructions explicitly preserve card status neutrality; claim and completion state must continue to live only in `Checklist.md`.

### Implementation Outputs

- Created `tools/analysis/build_run_engine_swimlane.py`.
- Created `tools/analysis/validate_run_engine_swimlane.py`.
- Created `docs/analysis/06_run_engine_end_to_end_execution_swimlane.md`.
- Created `docs/analysis/06_run_engine_phase_contracts.md`.
- Created `docs/analysis/06_run_engine_branch_and_terminalization_matrix.md`.
- Created `diagrams/analysis/06_run_engine_swimlane.mmd`.
- Created `data/analysis/run_engine_phase_index.json`.
- Created `data/analysis/run_engine_step_ledger.jsonl`.
- Created `data/analysis/run_engine_event_timeline.csv`.
- Created `data/analysis/run_engine_branch_conditions.json`.
- Created `data/analysis/run_engine_live_experience_map.json`.
- Created `prototypes/analysis/run_engine_swimlane_atlas/index.html`.
- Created `prototypes/analysis/run_engine_swimlane_atlas/styles.css`.
- Created `prototypes/analysis/run_engine_swimlane_atlas/app.js`.
- Created `prototypes/analysis/run_engine_swimlane_atlas/atlas_data.json`.
- Created `tests/playwright/run_engine_swimlane_atlas.spec.ts`.
- Created screenshot baselines in `tests/playwright/run_engine_swimlane_atlas.spec.ts-snapshots/`.
- Added `playwright.config.ts`, `package.json`, and `package-lock.json` to support the required browser regression run.

### Swimlane Extraction Findings

- The phase index now captures all `18` numbered `RUN_ENGINE(...)` phases in exact source order with `905` ordered logical statements, `98` indexed phase-local audit events, `50` artifact writes, `16` gate evaluations/helpers, and `12` atomic transaction primitives across the phase ledger.
- The branch matrix closes the explicit reuse/replay/continuation/late-data/terminalization gap with `12` named branch families, covering access early return, manifest strategy split, late-data spawn-review-exclude, pre-seal blocked vs seal success, pre-start vs post-start fault handling, trust terminalization, amendment split, and submit vs non-submit flow.
- The live-experience map now records every phase’s shell updates against the low-noise four-surface profile and preserves the command-truth versus read-side `ExperienceDelta` boundary instead of letting the atlas flatten them into product UI folklore.
- The static browser atlas ships as a deterministic observatory rather than a generic dashboard: sticky phase rail, horizontally scrollable lane canvas, sticky inspector, branch/gate/artifact/event semantics, transaction spans, reduced-motion handling, and screenshot-backed regression coverage.

### Verification Evidence

- `python3 tools/analysis/build_run_engine_swimlane.py` completed successfully with status `PASS`.
- `python3 tools/analysis/validate_run_engine_swimlane.py` completed successfully with status `PASS`.
- `./.venv/bin/python3 Algorithm/scripts/validate_contracts.py --self-test` completed successfully with status `PASS`.
- `./.venv/bin/python3 Algorithm/tools/forensic_contract_guard.py` completed successfully with status `PASS`.
- `npm install --workspaces=false` completed successfully to install the atlas regression dependency set.
- `npm exec --workspaces=false -- playwright test tests/playwright/run_engine_swimlane_atlas.spec.ts --update-snapshots` completed successfully after generating the two required baseline screenshots.
- `npm exec --workspaces=false -- playwright test tests/playwright/run_engine_swimlane_atlas.spec.ts` completed successfully with `7` passing tests and no arbitrary sleeps.
