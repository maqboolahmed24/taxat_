---
id: pc_0238
slug: phase_05_parallel_wave_03_track_frontend_shared_010_Playwright_or_other_appropriate_tooling_implement_cache_isolation_storage_partition_and_session_scoped_persistence
mode: parallel
phase: "05"
wave: "03"
track: "frontend_shared"
task_number: "010"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_shared_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0238

## Task Summary

010 Playwright or other appropriate tooling implement cache isolation storage partition and session scoped persistence.

## Metadata

- ID: `pc_0238`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend shared`
- Task Number: `010`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_shared_010_Playwright_or_other_appropriate_tooling_implement_cache_isolation_storage_partition_and_session_scoped_persistence`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0237](./pc_0237.md) `phase_05_parallel_wave_03_track_frontend_shared_009_Playwright_or_other_appropriate_tooling_implement_auth_step_up_and_session_state_integration`
- Next: [pc_0239](./pc_0239.md) `phase_05_parallel_wave_03_track_frontend_shared_011_Playwright_or_other_appropriate_tooling_implement_focus_keyboard_live_region_and_reduced_motion_utilities`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_shared_010_Playwright_or_other_appropriate_tooling_implement_cache_isolation_storage_partition_and_session_scoped_persistence`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0237` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement cache isolation, storage partitioning, and session-scoped persistence for browser frontend state using `CacheIsolationContract` as the authoritative reuse envelope.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The cache contract eliminates leakage from masking drift, cross-tenant/client reuse, session/access changes, preview reuse, stale hydration, and partial invalidation. The frontend must key all local persistence and shared cache reuse by exact contract fields and must purge broader variants on narrowing rather than relying on broad object ids or local route names.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `cache_isolation_and_secure_reuse_contract.md end to end`.
- `frontend_shell_and_interaction_law.md route stability, artifact handoff, telemetry fencing`.
- `northbound_api_and_session_contract.md session/browser/native rules`.
- `stream_resume_and_catch_up_ordering_contract.md FE-75 composition note`.
- `retention_and_privacy.md expiry/limitation behavior and privacy minimization`.
- `schemas/cache_isolation_contract.schema.json, route_stability_contract.schema.json, stream_recovery_contract.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/cache/cache_isolation_contract.ts`
2. `packages/frontend-shell-core/src/cache/cache_partition_manager.ts`
3. `packages/frontend-shell-core/src/cache/session_scoped_storage.ts`
4. `packages/frontend-shell-core/src/cache/purge_on_scope_narrowing.ts`
5. `packages/frontend-shell-core/src/cache/preview_export_cache_guard.ts`
6. `packages/shared-ui/src/cache/CachePartitionInternalMap.tsx for internal/debug only`
7. `tests/unit/frontend-shell-core/cache_isolation.spec.ts`
8. `tests/playwright/frontend/cache_isolation_storage_partition.spec.ts`
9. `docs/frontend/cache_isolation_storage_partition_and_session_persistence.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Internal-only diagnostics may show a “partition map” with safe opaque refs: tenant/session/access/masking/route/projection/preview as small hash chips connected to a cache partition capsule. Never show actual customer data, file names, free text, masked values, or screenshots. Use red only for proven invalid reuse; amber for purge pending.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- implement a cache key builder that includes scope class, tenant, client, principal/session binding, access binding, masking fingerprint, shell stability, route identity, canonical object, shell family, projection version, visibility partition, customer-safe posture, preview subject, and delivery binding where applicable
- wrap localStorage/sessionStorage/IndexedDB or equivalent behind session-scoped APIs that refuse writes without a valid cache isolation contract
- purge on tenant, principal, session, access, masking, route, projection, preview, or scope-narrowing drift
- prevent preview/export reuse outside the mounted route and selected artifact subject
- provide test-only hooks to inspect partition posture without leaking payload content

Functional, architectural, and edge-case gaps to close:
- close the gap where frontend cache keys use broad ids such as manifest/client/request only
- close the gap where preview/export blobs outlive revocation or route changes
- close the gap where customer-safe and internal projections can reuse the same local data
- close the gap where scope narrowing leaves broader variants cached

Mandatory edge cases:
- cross-tenant/client/principal/session reuse must fail closed
- masking tightening must purge broader cached variants immediately
- all non-secondary scopes must clear preview subject refs
- client portal workspace/customer request list must carry visibility partition and customer-safe posture

Playwright, accessibility, and validation requirements:
- unit-test exact key derivation and every drift/purge trigger
- Playwright-test tenant switch, access narrowing, masking drift, route drift, preview subject drift, and session revocation with mocked safe data
- assert diagnostics expose only opaque refs and no payload text
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- all browser persistence goes through contract-bound cache partition manager
- unsafe cache reuse and preview/export replay fail closed
- drift/purge behavior is tested across session, access, masking, route, and preview changes
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
