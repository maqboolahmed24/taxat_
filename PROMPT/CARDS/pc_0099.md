---
id: pc_0099
slug: phase_03_parallel_wave_01_track_backend_manifest_003_implement_config_version_config_change_request_and_config_freeze_models
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "003"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0099

## Task Summary

implement config version config change request and config freeze models.

## Metadata

- ID: `pc_0099`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `003`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_003_implement_config_version_config_change_request_and_config_freeze_models`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0098](./pc_0098.md) `phase_03_parallel_wave_01_track_backend_manifest_002_implement_manifest_lineage_trace_and_branch_selection_persistence_models`
- Next: [pc_0100](./pc_0100.md) `phase_03_parallel_wave_01_track_backend_manifest_004_implement_schema_bundle_schema_reader_window_and_backfill_execution_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0094_to_0101.md](../shared_operating_contract_0094_to_0101.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_003_implement_config_version_config_change_request_and_config_freeze_models`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0098` if they exist, especially `pc_0027`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0066`, `pc_0074`, `pc_0080`, and `pc_0097`.

Mission:
Implement the durable models, repositories, lifecycle machines, and freeze-hash helpers for `ConfigVersion`, `ConfigChangeRequest`, and `ConfigFreeze`.
This task must make governed config change, approved release state, and manifest-bound frozen config explicit and machine-checkable before any resolution or reuse logic is added in the next card.

Source algorithm grounding you must honor:
- `data_model.md`, especially `ConfigVersion`, `ConfigChangeRequest`, and `ConfigFreeze`.
- `state_machines.md`, especially `6.3 ConfigVersion.lifecycle_state` and `6.4 ConfigChangeRequest.lifecycle_state`.
- `manifest_and_config_freeze_contract.md`, especially `5.4 ConfigFreeze contract`, `5.5 Freeze timing rules`, and `5.9 Hash contract` for `config_freeze_hash` and `config_surface_hash`.
- `config_freeze_inheritance_and_consumption_contract.md`, especially basis mapping, source-lineage rules, cross-layer mirrors, and the completeness barrier.
- `modules.md`, especially `TRANSITION_CONFIG_VERSION(...)`, `TRANSITION_CONFIG_CHANGE_REQUEST(...)`, `RESOLVE_CONFIG(...)`, and `FREEZE_CONFIG(...)` as downstream consumers of these models.
- Existing schemas including `config_version.schema.json`, `config_change_request.schema.json`, and `config_freeze.schema.json`.

Deliverables to create:
1. `packages/backend-manifest/src/models/config_version.ts`.
2. `packages/backend-manifest/src/models/config_change_request.ts`.
3. `packages/backend-manifest/src/models/config_freeze.ts`.
4. `packages/backend-manifest/src/repositories/config_version_repository.ts`.
5. `packages/backend-manifest/src/repositories/config_change_request_repository.ts`.
6. `packages/backend-manifest/src/repositories/config_freeze_repository.ts`.
7. `packages/backend-manifest/src/state/config_version_lifecycle_machine.ts`.
8. `packages/backend-manifest/src/state/config_change_request_lifecycle_machine.ts`.
9. `packages/backend-manifest/src/hash/config_freeze_hash.ts`.
10. `packages/backend-manifest/src/hash/config_surface_hash.ts`.
11. `db/migrations/phase03_0099_config_version_ccr_freeze.sql`.
12. `docs/manifest/config_version_change_request_and_freeze_models.md`.
13. `tests/unit/backend_manifest/config_freeze_hash_and_state.spec.ts`.
14. `tests/integration/backend_manifest/config_models_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the persisted config-type catalog or lookup contract the repo uses to decide `required_config_types_present[]` and freeze completeness.
- Define what lives in `ConfigVersion` versus what lives in `ConfigChangeRequest` versus what lives only in `ConfigFreeze`.
  Do not blur long-lived approved config artifacts with one-off mutation requests or with manifest-bound frozen basis.
- Define the exact canonical ordering for `ConfigFreeze.entries[]` and the exact input vectors for `config_freeze_hash` and `config_surface_hash`.
- Define how `source_config_*` nullability works and how direct resolution remains distinguishable from exact or historical reuse even when hashes happen to match.
- Define how compliance-capable runs are barred from freezing non-approved config versions while historical replay still preserves the corpus carve-outs for deprecated/revoked historical basis.
- Define how state-transition contracts are stored and enforced for both config versions and change requests.

Execution steps:
1. Implement schema-backed model wrappers and repositories for `ConfigVersion`, `ConfigChangeRequest`, and `ConfigFreeze`.
2. Create the migration for tables, indexes, lifecycle fields, approvals, provenance, source-lineage mirrors, and hash columns.
3. Implement the lifecycle machines for config versions and change requests with explicit allowed transitions and typed rejection reasons.
4. Implement `config_freeze_hash.ts` and `config_surface_hash.ts` using the shared canonical hashing substrate, preserving the exact corpus input formulas.
5. Enforce the `ConfigFreeze` completeness barrier: no partial freeze, no later fill-in, no fake completeness.
6. Document how historical exact reuse, recovery exact reuse, direct resolution, and fresh child resolution are represented, even though the actual decision logic lands in `pc_0101`.
7. Add tests for state transitions, hash stability, completeness rules, and source-lineage nullability/equality rules.

Gap handling you must perform:
- Close the gap where governed config changes exist only as prose or loose tickets instead of one durable `ConfigChangeRequest` lane.
- Close the gap where config-release state could be mutated by ad hoc status strings instead of an explicit lifecycle machine.
- Close the gap where `ConfigFreeze` might exist only as incidental data embedded in manifests instead of a first-class control object.
- Close the gap where exact or historical reuse could be mislabeled as fresh resolution because only hashes are compared later.
- Close the gap where freeze completeness might be inferred instead of proven by required config-type coverage.

Mandatory edge cases:
- new compliance-capable run tries to freeze a `CANDIDATE` or `VERIFIED` config version and must fail closed
- historical replay references a `DEPRECATED` or `REVOKED` version that was frozen historically and remains lawful under the replay carve-out
- `ConfigFreeze.config_resolution_basis = DIRECT_REQUEST_RESOLUTION` keeps all `source_config_*` fields null
- exact or historical reuse keeps all `source_config_*` fields non-null and exact-hash-equal
- a fresh resolution yields the same hashes as an earlier freeze but must still remain typed as direct resolution
- required config types are incomplete and the freeze is rejected
- `ConfigChangeRequest` transitions to `IMPLEMENTED`, then rollback occurs and lifecycle/audit lineage stays explicit
- `config_surface_hash` changes because a top-level profile ref changes even when the per-entry freeze hash does not

Backend, performance, and security requirements:
- Hashing and state-transition evaluation must be deterministic and cheap enough for routine manifest allocation paths.
- Persist provenance, approvals, and audit refs explicitly; do not hide them in untyped blobs.
- Repositories must support point lookup by hash/id plus lifecycle-scoped queries for resolution logic.
- Do not expose or persist secrets or provider credentials in config freezes; preserve references only.
- Keep model/repository logic side-effect free except for explicit persistence APIs.

Validation rules:
- Unit-test lifecycle transitions, freeze hash canonicalization, surface hash canonicalization, and source-lineage rules.
- Integration-test migration application, repository persistence, and lawful versus unlawful config-state usage.
- Validate persisted payloads against `config_version.schema.json`, `config_change_request.schema.json`, and `config_freeze.schema.json`.
- Prove that freeze completeness must be established before any compliance-capable artifact may consume the freeze.
- Prove that exact reuse, recovery reuse, historical explicit reuse, and direct resolution remain distinguishable even under identical content hashes.

Definition of done:
- The repo has durable models and repositories for config versions, config change requests, and config freezes.
- Freeze hashes and lifecycle machines become executable truth instead of prose-only expectations.
- The next config-resolution card can build on explicit objects, exact reuse semantics, and completeness proof rather than reconstructing them ad hoc.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
