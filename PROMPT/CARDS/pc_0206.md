---
id: pc_0206
slug: phase_04_parallel_wave_02_track_backend_recovery_008_implement_nightly_batch_identity_batch_run_and_selection_disposition_persistence
mode: parallel
phase: "04"
wave: "02"
track: "backend_recovery"
task_number: "008"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_recovery_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0206

## Task Summary

008 implement nightly batch identity batch run and selection disposition persistence.

## Metadata

- ID: `pc_0206`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend recovery`
- Task Number: `008`
- Source Slug: `phase_04_parallel_wave_02_track_backend_recovery_008_implement_nightly_batch_identity_batch_run_and_selection_disposition_persistence`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0205](./pc_0205.md) `phase_04_parallel_wave_02_track_backend_recovery_007_implement_cross_device_continuity_contract_native_secondary_window_rules_and_shell_fuzz_harnesses`
- Next: [pc_0207](./pc_0207.md) `phase_04_parallel_wave_02_track_backend_recovery_009_implement_nightly_autopilot_scheduler_sharding_reclaim_and_operator_morning_digest_derivation`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0206_to_0213.md](../shared_operating_contract_0206_to_0213.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_recovery_008_implement_nightly_batch_identity_batch_run_and_selection_disposition_persistence`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0205` if they exist, especially `pc_0102` through `pc_0109`, `pc_0110` through `pc_0117`, `pc_0126` through `pc_0127`, `pc_0134` through `pc_0141`, and `pc_0198` through `pc_0205`.

Mission:
Implement the tenant-scoped nightly batch identity envelope, persisted `NightlyBatchRun`, and authoritative per-candidate selection-disposition ledger that freezes the nightly universe before any autonomous execution begins.
This task is not allowed to approximate nightly selection from queue state, scheduler logs, or UI-side heuristics.
The persisted batch and selection rows must become the canonical truth for same-window duplicate suppression, terminal-result reuse, active-attempt deferral, reclaim lineage, and later shard planning.

Source algorithm grounding you must honor:
- `nightly_selection_disposition_and_batch_isolation_contract.md`, especially identity law, selection law, shard-isolation law, and recovery-reclaim law.
- `nightly_autopilot_contract.md`, especially sections covering batch allocation and duplicate suppression, frozen batch envelope, nightly manifest launch context, candidate-universe derivation, selection dispositions, and same-window active-attempt rules.
- `modules.md`, especially `ALLOCATE_NIGHTLY_BATCH_RUN(...)`, `SELECT_NIGHTLY_PORTFOLIO(...)`, and the batch/selection accounting helpers they imply.
- `data_model.md`, especially `NightlyBatchRun` and any linked `RunManifest` nightly fields.
- `schemas/nightly_batch_identity_contract.schema.json` and `schemas/nightly_batch_run.schema.json`.
- `scripts/validate_contracts.py`, especially `nightly_batch_run_recovery_and_accounting_alignment`, `nightly_batch_run_identity_contract_mirror`, `nightly_batch_run_reuse_precedence_before_new_allocation`, `nightly_batch_run_shard_plan_must_cover_execution_entries`, `nightly_batch_run_terminal_batches_require_explicit_outcomes`, and `nightly_batch_run_reader_window_mirror`.
- `tools/forensic_contract_guard.py`, especially the pinned nightly batch identity constants and typed-policy values.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially any vectors around silent candidate omission, same-window duplicates, stale-attempt recovery, or cross-window continuity drift.

Deliverables to create:
1. `packages/backend-recovery/src/models/nightly_batch_identity_contract.ts`
2. `packages/backend-recovery/src/models/nightly_batch_run.ts`
3. `packages/backend-recovery/src/repositories/nightly_batch_run_repository.ts`
4. `packages/backend-recovery/src/services/derive_nightly_selection_universe_hash.ts`
5. `packages/backend-recovery/src/services/allocate_nightly_batch_run.ts`
6. `packages/backend-recovery/src/services/select_nightly_portfolio.ts`
7. `packages/backend-recovery/src/services/persist_nightly_selection_dispositions.ts`
8. `packages/backend-recovery/src/tests/nightly_batch_identity_contract.spec.ts`
9. `packages/backend-recovery/src/tests/nightly_batch_selection_dispositions.spec.ts`
10. `docs/recovery/nightly_batch_identity_batch_run_and_selection_disposition_persistence.md`
11. Optional but recommended: `apps/admin-console-web/src/routes/debug/recovery/nightly-batch-ledger-lab.tsx`

You MUST patch any existing nightly scheduler or worker path that begins execution without first persisting a lawful `NightlyBatchRun` and a complete `selection_entries[]` set.

Required design decisions you must make explicitly:
- define the exact batch-dedupe tuple and canonical `identity_contract_hash` derivation
- define how `selection_universe_hash` and `selection_universe_count` are derived and frozen
- define the exact persisted row structure for `selection_entries[]`, including lineage refs, typed reason codes, workflow refs, checkpoint timing, and `outcome_bucket`
- define the precedence ladder between reusable terminal result, same-window active attempt, continuation child, new manifest allocation, deferral, escalation, and ineligibility
- define how `RECOVERY_RECLAIM_WINDOW` successors link to stale predecessors and prove `recovery_resume_state`
- define how `NightlyBatchRun` lifecycle transitions remain append-only except for the explicitly mutable operational fields allowed by contract

Execution requirements:
- allocate or recover exactly one `NightlyBatchRun` for the tuple `(tenant_id, nightly_window_key, trigger_class, release_verification_manifest_ref, policy_snapshot_hash)` using the canonical `scheduler_dedupe_key` contract
- freeze `identity_contract{...}` with scheduler tuple, execution-basis fields, candidate-universe fields, reclaim lineage fields, and typed policy fields before selection begins
- ensure every candidate in the frozen nightly universe materializes exactly one persisted `selection_entries[]` row
- persist canonical `candidate_identity_hash`, one typed `selection_disposition`, `terminal_result_reuse_state`, `active_attempt_resolution_state`, and continuation lineage refs when required
- make `REUSE_EXISTING_TERMINAL_RESULT` win before any new manifest allocation or continuation-child allocation
- keep same-window active attempts from silently allocating duplicates; they must resolve to `DEFER_ACTIVE_ATTEMPT` or a lawful continuation-child posture
- keep non-execution rows off-shard with `fairness_group_key = null` and `shard_key = null`
- retain `reason_codes[]`, `workflow_item_refs[]`, `next_checkpoint_at`, and `outcome_bucket` on non-execution rows so later digest/recovery logic does not need to reconstruct intent
- preserve `nightly_batch_run_ref` and `nightly_window_key` as manifest-identity inputs for any new nightly manifest created from this batch

Gap handling you must close while implementing:
- close any gap where the batch identity is reduced to one opaque hash string without freezing the structured tuple fields required by schema and prose
- close any gap where candidate selection can silently omit rows, persist partial universes, or rely on log statements rather than durable `selection_entries[]`
- close any gap where terminal-result reuse is checked after new-allocation logic rather than before it
- close any gap where same-window active attempts can produce a duplicate manifest because the deferral/continuation decision lives outside the canonical selection service
- close any gap where recovery batches fail to retain explicit predecessor linkage and `recovery_resume_state`
- close any gap where outcome buckets appear only after shard execution instead of being part of the persisted per-entry posture

Mandatory edge cases:
- duplicate scheduler delivery for the same dedupe tuple must reuse the same active batch or return `BATCH_ALREADY_TERMINAL`
- a batch with unfrozen release admissibility, policy snapshot, or tenant schedule scope must land in `BLOCKED`, not `RUNNING`
- non-recovery triggers must keep predecessor linkage null and `recovery_resume_state = NOT_APPLICABLE`
- same-window active attempts must not silently allocate `EXECUTE_NEW_MANIFEST`
- a candidate with a reusable terminal result must not receive a new manifest or continuation child
- the number of persisted `selection_entries[]` rows must equal `selection_universe_count`
- terminal/quiescing/completed batches must keep explicit per-entry `outcome_bucket` values
- non-execution rows must never be assigned shard ownership keys

Playwright and contract-test requirements:
- add deterministic contract tests for batch identity hashing, selection-universe accounting, selection-disposition precedence, and recovery-lineage freeze behavior
- if you expose or patch an admin or northbound batch-read route, add Playwright `APIRequestContext` coverage for duplicate delivery reuse, blocked-prerequisite posture, and complete selection-row publication
- if you add the optional lab, add Playwright browser tests that verify the batch-identity ribbon, selection-disposition ledger ordering, shard-eligibility markers, keyboard navigation, and reduced-motion-safe section expansion using semantic locators only
- capture payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a three-band layout: batch-identity ribbon, ordered selection ledger, narrow lineage/outcome sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; accent `#0F766E`; info accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- each selection row must privilege disposition, reason codes, terminal-result reuse posture, and active-attempt resolution state before any counts or badges
- use a restrained step/timeline treatment for predecessor-reclaim lineage instead of a generic analytics chart
- animate only opacity/translate/height with `140ms` to `180ms`; respect reduced motion

Backend, performance, and security requirements:
- persist batch allocation and selection rows transactionally so duplicate scheduler delivery cannot create split-brain batch state
- keep all nightly identity and selection services idempotent under repeated scheduler delivery and worker retry
- store hashes, policy refs, and typed enums exactly as required by schema; do not invent synonym states
- prohibit live queue residue, browser state, operator notes, or transport-local worker memory from entering candidate eligibility truth
- keep repository methods append-only for identity and selection provenance; later lifecycle mutation paths may update only the allowed operational fields
- ensure row ordering is stable and deterministic so later digest and simulation work can reuse `selection_entries[]` without re-ranking

Validation rules:
- validate every `NightlyBatchIdentityContract` and `NightlyBatchRun` against schema before persistence
- update fixtures/builders so all nightly validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one canonical backend path for allocating/recovering nightly batches and one canonical backend path for deriving/persisting complete selection rows
- same-window duplicate suppression, terminal-result reuse, active-attempt isolation, and recovery-lineage freeze are durable and test-covered
- optional labs, if added, are read-only reflections of persisted truth rather than alternate implementations
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
