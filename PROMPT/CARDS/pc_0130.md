---
id: pc_0130
slug: phase_03_parallel_wave_01_track_backend_compute_010_implement_enquiry_pack_assembly_and_retention_limited_explainability_support
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "010"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0130

## Task Summary

implement enquiry pack assembly and retention-limited explainability support.

## Metadata

- ID: `pc_0130`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `010`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_010_implement_enquiry_pack_assembly_and_retention_limited_explainability_support`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0129](./pc_0129.md) `phase_03_parallel_wave_01_track_backend_compute_009_implement_proof_bundle_generation_and_defensible_filing_graph_closure`
- Next: [pc_0131](./pc_0131.md) `phase_03_parallel_wave_01_track_backend_compute_011_implement_twin_state_snapshot_twin_view_and_mismatch_summary_builder`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0126_to_0133.md](../shared_operating_contract_0126_to_0133.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_010_implement_enquiry_pack_assembly_and_retention_limited_explainability_support`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0129` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0028`, `pc_0061`, `pc_0065`, `pc_0069`, `pc_0074`, `pc_0076`, `pc_0097`, `pc_0108`, `pc_0118`, `pc_0126`, `pc_0128`, and `pc_0129`.

Mission:
Implement the `EnquiryPack` model, repository, assembly service, and retention-limited explainability helpers that convert a persisted graph and proof bundle into one bounded, defensible, replay-safe explanation artifact for operator, reviewer, and governed externalization use.
This task must preserve critical path identity, supporting evidence refs, transformation steps, config and override lineage, limitation notes, masking posture, omission entries, render contracts, and externalization governance without leaking unavailable material or silently widening visibility beyond retention policy.

Source algorithm grounding you must honor:
- `provenance_graph_semantics.md`, especially the enquiry-pack obligations for target figure/decision, critical paths, transformation steps, supporting evidence, config refs, override refs, authority refs, limitation notes, and audit refs.
- `retention_limited_explainability_and_audit_sufficiency_contract.md`, especially typed omission / masking / limitation posture, retention binding, and audit sufficiency.
- `defensible_filing_graph_contract.md`, especially explanation surfaces, proof bundle continuity, and replay-safe reconstruction.
- `modules.md`, especially graph/proof/enquiry build placement and any externalization or render contract rules.
- `data_model.md`, especially `EnquiryPack`.
- `PATCH_RESOLUTION_INDEX.md`, especially any fixes around `primary_path_ref`, omission handling, or externalization governance.
- `test_vectors.md`, especially `TV-62`, `TV-62A`, and `TV-62B`.
- Existing schema `schemas/enquiry_pack.schema.json`.

Deliverables to create:
1. `packages/backend-provenance/src/models/enquiry_pack.ts`.
2. `packages/backend-provenance/src/repositories/enquiry_pack_repository.ts`.
3. `packages/backend-provenance/src/services/build_masking_posture.ts`.
4. `packages/backend-provenance/src/services/build_omission_entries.ts`.
5. `packages/backend-provenance/src/services/build_render_contract.ts`.
6. `packages/backend-provenance/src/services/build_externalization_governance.ts`.
7. `packages/backend-provenance/src/services/generate_enquiry_pack.ts`.
8. `packages/backend-provenance/src/services/validate_enquiry_pack.ts`.
9. `db/migrations/phase03_0130_enquiry_pack_and_retention_limited_explainability.sql`.
10. `docs/provenance/enquiry_pack_assembly_and_retention_limited_explainability_support.md`.
11. `tests/unit/backend_provenance/enquiry_pack_contract.spec.ts`.
12. `tests/unit/backend_provenance/retention_limited_explainability.spec.ts`.
13. `tests/integration/backend_provenance/enquiry_pack_generation_and_externalization.spec.ts`.
14. Optional internal route and page objects only if a verification surface is added.

Required design decisions you must make explicit:
- Define the exact source set for `EnquiryPack`.
  It must bind one graph, one proof bundle, one target, one primary path, and one bounded supporting set.
- Define how `critical_path_refs[]` are selected and ordered.
  `primary_path_ref` must appear inside `critical_path_refs[]`.
- Define the omission-entry grammar:
  omission cause class, withheld target, policy basis, recovery unavailability state, and reviewer-facing note must remain typed rather than free-form.
- Define the masking posture and externalization governance contracts so internal preview, download, print, or external handoff can reuse one persisted policy packet.
- Define the human-readable versus machine-readable render-ref policy and how both are versioned.

Execution steps:
1. Implement the schema-backed `EnquiryPack` model, repository, and validator.
2. Implement `build_masking_posture.ts` and `build_omission_entries.ts` so retention-limited explainability remains explicit and typed.
3. Implement `build_render_contract.ts` so the pack can point to stable human-readable and machine-readable render targets without embedding renderer-specific layout state in the durable artifact.
4. Implement `build_externalization_governance.ts` so delivery-surface limits, masking state, approval requirements, and preview/download/print eligibility remain persisted.
5. Implement `generate_enquiry_pack.ts` so it binds graph / proof / target / critical paths / evidence / transformation / config / override / authority / audit lineage in one deterministic artifact.
6. Implement `validate_enquiry_pack.ts` so impossible path refs, missing limitation notes under limited posture, or externalization rules inconsistent with masking / omission state fail closed.
7. Add tests for fully available explanation, retention-limited explanation, and failed / unavailable explanation posture.

Gap handling you must perform:
- Close the gap where proof closure exists before a bounded explainability artifact does.
  Build one durable enquiry pack rather than letting each surface compose its own explanation bundle.
- Close the gap where limitation or omission posture could be implied by missing data.
  Persist typed omission entries and limitation notes explicitly.
- Close the gap where internal preview and governed externalization could diverge in what they reveal.
  Persist one externalization governance contract consumed by both.
- Close the gap where renderer-specific layout choices could become durability truth.
  Persist semantic render refs and contracts, not DOM snapshots.
- Close the gap where a pack might appear “available” even when key material is retained away.
  Preserve `explanation_status` and omission / masking posture accurately.

Mandatory edge cases:
- `primary_path_ref` must always be one of `critical_path_refs[]`.
- retention-limited packs must still preserve `retention_binding`, `limitation_notes`, and typed omission entries.
- packs with failed explanation posture must remain auditable and explicit about what is unavailable.
- masking state and externalization governance must remain consistent with omission and limitation posture.
- packs must preserve audit refs and config refs even when human-readable material is limited.
- human-readable and machine-readable render refs must not point outside the pack’s governance posture.
- deterministic ordering must apply to critical paths, supporting evidence, transformation refs, config refs, override refs, and authority refs.

Backend, performance, and security requirements:
- Never embed raw sensitive source payloads unnecessarily; prefer stable refs plus governed render targets.
- Make masking and omission rules policy-driven and deterministic.
- Keep explanation assembly replay-safe and independent of browser rendering.
- Avoid free-text generation that is not anchored to persisted reason / limitation / governance codes.

Optional internal verification surface (recommended if you need a reviewer-facing preview):
- If you add a read-only enquiry-pack preview, place it behind an internal route such as `/internal/provenance/enquiry/:enquiryPackId`.
- The UI must be minimalist premium and distinctly non-generic:
  a `260px` left rail for target, explanation status, and critical-path switching;
  a broad central narrative panel that renders the primary proof ladder with evidence and transformation steps;
  a `340px` right rail for limitation notes, masking posture, omission entries, and externalization controls.
- Use the shared palette plus these emphasis rules:
  `AVAILABLE` accents in `#0F766E`,
  `LIMITED` accents in `#B7791F`,
  `FAILED` accents in `#C23616`,
  neutral narrative links in `#2F6FED`.
- Typography:
  page title `28/32`,
  ladder section headers `16/22`,
  evidence labels `12/16` medium,
  omission / masking chips `11/14` uppercase mono.
- Add one restrained “proof ladder” diagram only: vertical path spine with branching markers for rejected or omitted branches, no force-directed graph.
- Motion:
  branch expand/collapse `160ms`,
  detail drawer slide `180ms`,
  zero decorative easing,
  full reduced-motion support.
- If this surface exists, implement and verify it with Playwright using semantic locators, keyboard path switching, focus retention after drawer close, reduced-motion assertions, and trace capture.

Validation rules:
- Unit-test omission-entry grammar, masking posture, externalization governance, and render-contract stability.
- Integration-test graph + proof -> enquiry-pack assembly under fully available, limited, and failed explanation posture.
- Validate persisted artifacts against `enquiry_pack.schema.json`.
- Re-run the contract validators after assembling the implementation bundle.

Definition of done:
- The repo can emit one durable `EnquiryPack` that later reviewer, audit, and governed externalization surfaces can consume directly.
- Retention-limited explainability remains explicit, typed, and replay-safe rather than inferred from missing fields.
- Later browser or export surfaces do not need to reconstruct critical paths, masking rules, or omission posture on their own.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
