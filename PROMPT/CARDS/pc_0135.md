---
id: pc_0135
slug: phase_03_parallel_wave_01_track_backend_authority_003_implement_authority_operation_binding_request_and_response_envelope_models
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "003"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0135

## Task Summary

implement authority operation binding request and response envelope models.

## Metadata

- ID: `pc_0135`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `003`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_003_implement_authority_operation_binding_request_and_response_envelope_models`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0134](./pc_0134.md) `phase_03_parallel_wave_01_track_backend_authority_002_implement_submission_record_lifecycle_and_authority_truth_projection`
- Next: [pc_0136](./pc_0136.md) `phase_03_parallel_wave_01_track_backend_authority_004_implement_authority_request_identity_idempotency_and_duplicate_meaning_key_logic`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0134_to_0141.md](../shared_operating_contract_0134_to_0141.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_003_implement_authority_operation_binding_request_and_response_envelope_models`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0134` if they exist, especially `pc_0028`, `pc_0050`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0067`, `pc_0068`, `pc_0069`, `pc_0074`, `pc_0076`, `pc_0086` through `pc_0093`, `pc_0097`, `pc_0102`, `pc_0104`, `pc_0108`, `pc_0109`, and `pc_0133` through `pc_0134`.

Mission:
Implement the first canonical sealed-model layer for `AuthorityOperation`, `AuthorityBinding`, `AuthorityRequestEnvelope`, and `AuthorityResponseEnvelope`.
These artifacts must become the only lawful typed carriers of authority execution intent, binding lineage, sealed request identity, and normalized provider response meaning.
Do not allow later transport code, queue workers, or callback pipelines to invent partial request/response structs outside these models.

Source algorithm grounding you must honor:
- `data_model.md`, especially the full field definitions and explanatory paragraphs for `AuthorityOperation`, `AuthorityBinding`, `AuthorityRequestEnvelope`, and `AuthorityResponseEnvelope`.
- `modules.md`, especially `RESOLVE_AUTHORITY_OPERATION(...)`, `RESOLVE_AUTHORITY_BINDING(...)`, `CANONICALIZE_AUTHORITY_REQUEST(...)`, `BUILD_AUTHORITY_REQUEST_ENVELOPE(...)`, and `NORMALIZE_AUTHORITY_RESPONSE(...)`.
- `authority_interaction_protocol.md`, especially sections on post-seal preflight, token/client binding, request hashing, fraud-header lineage, and response normalization posture.
- `authority_truth_and_internal_projection_separation_contract.md`.
- `actor_and_authority_model.md` and `connector_delegation_contract.md` for delegated acting context, authority-link readiness, and client-scope boundaries.
- `PATCH_RESOLUTION_INDEX.md`, especially the entries that corrected invalid `AuthorityResponseEnvelope` body/hash pairings and early authority-domain contradictions.
- `test_vectors.md`, especially vectors around bound vs ambiguous response posture, timeout placeholders, and exact transport lineage.

Deliverables to create:
1. `packages/backend-authority/src/models/authority_operation.ts`
2. `packages/backend-authority/src/models/authority_binding.ts`
3. `packages/backend-authority/src/models/authority_request_envelope.ts`
4. `packages/backend-authority/src/models/authority_response_envelope.ts`
5. `packages/backend-authority/src/repositories/authority_operation_repository.ts`
6. `packages/backend-authority/src/repositories/authority_binding_repository.ts`
7. `packages/backend-authority/src/repositories/authority_request_envelope_repository.ts`
8. `packages/backend-authority/src/repositories/authority_response_envelope_repository.ts`
9. `packages/backend-authority/src/services/resolve_authority_operation.ts`
10. `packages/backend-authority/src/services/resolve_authority_binding.ts`
11. `packages/backend-authority/src/services/canonicalize_authority_request.ts`
12. `packages/backend-authority/src/services/build_authority_layer_boundary_contract.ts`
13. `packages/backend-authority/src/services/build_authority_request_envelope.ts`
14. `packages/backend-authority/src/services/build_authority_response_envelope.ts`
15. `packages/backend-authority/src/services/validate_authority_transport_lineage.ts`
16. `db/migrations/phase03_0135_authority_operation_binding_request_response_models.sql`
17. `docs/authority/authority_operation_binding_request_response_models.md`
18. `tests/unit/backend_authority/authority_operation_binding_models.spec.ts`
19. `tests/unit/backend_authority/authority_request_response_envelope_contract.spec.ts`
20. `tests/integration/backend_authority/authority_transport_model_roundtrip.spec.ts`

Required design decisions you must make explicit:
- Define exactly how `AuthorityOperation` freezes:
  `requested_scope[]`,
  `runtime_scope[]`,
  `tenant_id`,
  `client_id`,
  `attempt_lineage_manifest_id`,
  `provider_environment`,
  `provider_api_version`,
  `authority_scope`,
  target obligation / basis context,
  `authority_binding_ref`,
  `authority_link_ref`,
  `delegation_grant_ref`,
  `binding_lineage_ref`,
  `token_binding_ref`,
  `subject_ref`,
  `acting_party_ref`,
  and `policy_snapshot_hash`.
- Define how `AuthorityBinding` distinguishes:
  internal authorization posture,
  client-acting delegation posture,
  authority-link posture,
  token/client binding posture,
  binding health,
  expiry posture,
  and any required step-up or approval evidence.
- Define how `AuthorityRequestEnvelope` freezes canonically ordered header profiles, partition refs, path/query/body identity, normalized null sentinels, fraud-header lineage refs, and the grouped `request_identity_contract{...}`.
- Define how `AuthorityResponseEnvelope` distinguishes:
  inline HTTP responses,
  transport timeout placeholders,
  callback-delivered responses,
  poll-derived responses,
  recovery-derived responses,
  corroborating responses,
  conflicting responses,
  superseding responses,
  and ambiguous correlation posture.

Execution steps:
1. Implement schema-backed models and repositories for the four authority-domain transport artifacts.
2. Implement `resolve_authority_operation.ts` so it materializes authorized runtime scope and binding lineage from frozen preflight output rather than from ad hoc request adapters.
3. Implement `resolve_authority_binding.ts` so the binding preserves tenant/client/reporting-subject/acting-party/scope/environment/version coherence and fails closed on ambiguity.
4. Implement `canonicalize_authority_request.ts` so canonical path, canonical query, canonical payload bytes, and ordered header profile refs are derived from frozen operation/binding input and canonical packet bytes only.
5. Implement `build_authority_request_envelope.ts` so request identity is never partially constructed; every envelope must already carry the full grouped request identity spine and authority-layer boundary contract.
6. Implement `build_authority_response_envelope.ts` so normalized response meaning is explicit, source-labelled, chronology-preserving, and correlation-safe.
7. Implement `validate_authority_transport_lineage.ts` so illegal pairings between operation/binding/request/response are rejected before persistence.

Gap handling you must perform:
- Close the gap where authority execution intent could otherwise be reconstructed loosely from request adapters, queue payloads, or live connector state.
- Close the gap where delegated acting context, client scope, and token/client binding can drift into one flattened “binding ok” boolean.
- Close the gap where request envelopes could be persisted without the full grouped request identity spine.
- Close the gap where response envelopes could collapse timeout placeholders, callbacks, polls, and recovery reads into one unlabeled current-response blob.
- Close the gap where normalized nulls and ordering could vary by caller language or serialization library.

Mandatory edge cases:
- `runtime_scope[]` must be canonical and must reflect authorized executable scope, not raw caller intent
- mutation and calculation families must remain partition-bound; empty `business_partitions[]` is only lawful for global/account-level read posture
- `AuthorityBinding.token_version_ref` is the preflight-selected version and must not be mutated later to reflect send-time rotation
- delegated acting context must retain `delegation_grant_ref`; non-delegated context must keep `acting_party_ref = subject_ref`
- `AuthorityRequestEnvelope` must use the `<NONE>` null-body sentinel and explicit normalized obligation/basis sentinels
- `AuthorityRequestEnvelope` must freeze `authority_layer_boundary{...}` and `request_identity_contract{...}` before any live transport is attempted
- asynchronous `AuthorityResponseEnvelope` must carry non-null `ingress_receipt_ref` and `authority_ingress_proof_contract{...}`
- `INLINE_HTTP` and `TRANSPORT_TIMEOUT` responses must keep ingress-proof fields null
- timeout/no-body posture must not serialize a non-sentinel body hash, and non-empty body hash must not coexist with “missing body” posture
- response chronology must remain explicit; later response arrival cannot erase the historical existence of earlier conflicting or provisional observations

Backend, performance, and security requirements:
- fail closed on tenant/client mismatch, subject/acting-party mismatch, authority-scope mismatch, provider-version mismatch, missing grouped contracts, or illegal response-source combinations
- persist artifacts with indexes for manifest, request hash, duplicate meaning, binding lineage, provider delivery ref, and active response lookup
- do not store raw refresh tokens, opaque IdP payloads, or unrestricted provider blobs on these models
- preserve deterministic serialization for all grouped contracts and ordered arrays
- ensure repositories support append-only response history and immutable sealed request identity

Validation rules:
- validate each artifact against its authoritative schema
- unit-test ordering, null normalization, delegated vs non-delegated binding, timeout response posture, and grouped contract completeness
- integration-test operation -> binding -> request envelope -> response envelope roundtrips, including callback vs poll vs timeout vs recovery variants
- prove that canonical request material and grouped request identity remain byte-stable across replay
- prove that response envelopes remain source-labelled and do not collapse conflicting observations into an unlabeled “latest” record

Definition of done:
- the repo has canonical schema-backed models for authority operation, binding, sealed request identity, and normalized response meaning
- downstream send / recovery / callback code can no longer invent partial transport artifacts
- request/response lineage is explicit, replay-safe, and aligned to the algorithm’s authority boundary contracts
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
