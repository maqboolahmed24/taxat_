---
id: pc_0244
slug: phase_05_parallel_wave_03_track_frontend_low_noise_004_Playwright_or_other_appropriate_tooling_implement_action_strip_state_rendering_and_mutation_legality_controls
mode: parallel
phase: "05"
wave: "03"
track: "frontend_low_noise"
task_number: "004"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_low_noise_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0244

## Task Summary

004 Playwright or other appropriate tooling implement action strip state rendering and mutation legality controls.

## Metadata

- ID: `pc_0244`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend low noise`
- Task Number: `004`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_low_noise_004_Playwright_or_other_appropriate_tooling_implement_action_strip_state_rendering_and_mutation_legality_controls`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0243](./pc_0243.md) `phase_05_parallel_wave_03_track_frontend_low_noise_003_Playwright_or_other_appropriate_tooling_implement_decision_summary_state_rendering_and_limitation_notices`
- Next: [pc_0245](./pc_0245.md) `phase_05_parallel_wave_03_track_frontend_low_noise_005_Playwright_or_other_appropriate_tooling_implement_detail_drawer_state_support_surface_and_non_modal_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_low_noise_004_Playwright_or_other_appropriate_tooling_implement_action_strip_state_rendering_and_mutation_legality_controls`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0243` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement `ActionStripState` rendering and mutation-legality controls for one safe primary action, bounded secondary actions, explicit `NO_SAFE_ACTION`, and command-envelope precondition handoff.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The action strip is the dominant-action surface. The algorithm requires one primary action or explicit `NO_SAFE_ACTION`, secondary actions bounded to two and non-mutating when a primary mutation exists, stale/freshness requirements on mutation actions, durable receipt/problem feedback, and omission of illegal actions rather than disabled clutter. The UI must never present contradictory writable posture.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md action strip, settlement-state, algorithm translation, no safe action, analysis-only posture`.
- `dominant_question_and_single_action_contract.md single action invariants`.
- `frontend_shell_and_interaction_law.md dominant-action law and no contradictory writable posture`.
- `northbound_api_and_session_contract.md command envelope/stale guards/receipt`.
- `schemas/action_strip_state.schema.json, command_envelope.schema.json, api_command_receipt.schema.json, action_authority_contract.schema.json`.

Deliverables to create or patch:
1. `packages/shared-ui/src/low-noise/ActionStrip.tsx`
2. `packages/shared-ui/src/low-noise/PrimaryActionButton.tsx`
3. `packages/shared-ui/src/low-noise/NoSafeActionPanel.tsx`
4. `packages/frontend-shell-core/src/low-noise/action_strip_presenter.ts`
5. `packages/frontend-shell-core/src/low-noise/action_legality_bridge.ts`
6. `tests/unit/frontend-shell-core/action_strip_presenter.spec.ts`
7. `tests/playwright/frontend/low_noise_action_strip_mutation_legality.spec.ts`
8. `docs/frontend/action_strip_state_and_mutation_legality_controls.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Use a horizontal consequence rail: primary action as a single composed button/card with action label, target object/module, freshness requirement, and consequence line; secondary actions as two quiet text buttons. `NO_SAFE_ACTION` becomes a compact explanation card with one investigation entry point. Use no disabled-button rows; omit illegal actions and explain the reason once.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- render `actionability_state`, `mode_safety_posture`, `primary_action`, `secondary_actions`, available/blocked action codes, ownership/waiting labels, blocking reason, `no_safe_action_reason_code`, investigation entry point, suggested/active detail module, focus anchor, scores, dominance margin, and suppressed count
- when primary action is mutation-capable, ensure visible secondary actions are non-mutating; omit blocked/illegal actions rather than disabled clutter
- build command-envelope input from action target, stale guard snapshot, mutation precondition binding, and live freshness requirements without sending local legal-state flags
- integrate command receipt inline status from `pc_0234` when available
- route investigation entry points through detail drawer without changing shell family

Functional, architectural, and edge-case gaps to close:
- close the gap where action buttons are just labels with implicit routing
- close the gap where stale/degraded views leave unsafe mutation actions visible
- close the gap where blocked actions appear as disabled clutter instead of omitted with one reason
- close the gap where `NO_SAFE_ACTION` lacks deterministic next investigation path

Mandatory edge cases:
- analysis-only/replay-only/masked-beyond-actionability must omit filing/approval/authority mutations
- stale guard drift must remove or rebase the primary mutation before click
- runner-up action cannot visually compete with primary when dominance margin is lawful
- ownership wait states must not look like user-actionable controls

Playwright, accessibility, and validation requirements:
- unit-test action budgets, mutation/non-mutating secondary rules, no-safe-action mapping, and command payload guard binding
- Playwright-test primary action, stale rejection, no-safe-action, blocked/omitted actions, receipt feedback, keyboard focus, and reduced motion
- assert disabled mutation clutter does not appear for illegal actions
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- action strip enforces one safe primary action or explicit no-safe-action
- mutation controls bind to exact stale/freshness guards
- Playwright covers legality, stale, receipt, ownership, and non-live edge cases
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
