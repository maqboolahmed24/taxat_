---
id: pc_0180
slug: phase_04_parallel_wave_02_track_backend_portal_002_implement_client_document_request_and_current_vs_historical_upload_projection_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "002"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0180

## Task Summary

implement client document request and current vs historical upload projection rules.

## Metadata

- ID: `pc_0180`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `002`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_002_implement_client_document_request_and_current_vs_historical_upload_projection_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0179](./pc_0179.md) `phase_04_parallel_wave_02_track_backend_portal_001_implement_client_portal_workspace_route_specific_derivatives_and_view_guards`
- Next: [pc_0181](./pc_0181.md) `phase_04_parallel_wave_02_track_backend_portal_003_implement_client_upload_session_identity_freeze_request_binding_and_rebase_logic`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0174_to_0181.md](../shared_operating_contract_0174_to_0181.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_002_implement_client_document_request_and_current_vs_historical_upload_projection_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0179` if they exist, especially `pc_0179`, `pc_0163`, and `pc_0174` through `pc_0178`.

Mission:
Implement the document-route projection layer that turns request truth plus upload lineage into one customer-safe document-request model and one route-ready request-card model with explicit current-versus-history semantics.
The key outcome is that the portal never guesses which upload is current, which upload satisfies the request now, which artifact is the default preview/download target, or whether history is limited.
Those distinctions must become backend truth.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially the `Documents` route rules, contextual request-detail rules, artifact/handoff rules, and validation minimum.
- `frontend_shell_and_interaction_law.md`, especially artifact handling, automation anchors, and current-primary-history-secondary rules.
- `modules.md`, especially the document/upload projection rules around request version, upload lineage, current-request pointer, preview posture, and current-versus-history separation.
- `data_model.md`, especially `ClientDocumentRequest`, the portal `document_center` request-card semantics, and the `ClientPortalWorkspace` document-route requirements.
- `state_machines.md`, especially the `ClientDocumentRequest` and cross-state invariants for upload-bearing requests.
- `schemas/client_document_request.schema.json`, `schemas/client_portal_workspace.schema.json`, `schemas/client_upload_session.schema.json`, `schemas/artifact_selection_contract.schema.json`, `schemas/artifact_affordance_contract.schema.json`, and `schemas/externalization_governance_contract.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 247, 248, and 302-310.
- `test_vectors.md`, especially the document-request, reconnect, current-vs-history, and stale-request rebase cases.

Deliverables to create:
1. `packages/backend-portal/src/projectors/build_client_document_request.ts`
2. `packages/backend-portal/src/projectors/build_document_request_card.ts`
3. `packages/backend-portal/src/projectors/build_document_center.ts`
4. `packages/backend-portal/src/services/project_request_upload_rows.ts`
5. `packages/backend-portal/src/services/derive_request_history_and_preview_posture.ts`
6. `packages/backend-portal/src/services/derive_request_artifact_selection_contract.ts`
7. `packages/backend-portal/src/services/derive_minimal_request_artifact_affordance_adapter.ts`
8. `packages/backend-portal/src/services/validate_request_local_upload_lineage.ts`
9. `packages/backend-portal/src/tests/client_document_request_projection.spec.ts`
10. `docs/portal/client_document_request_and_upload_history_projection.md`
11. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/document-request-preview-lab.tsx`

You MUST patch the portal workspace builder and any existing documents-route northbound adapters so they consume these projectors instead of inferring current-versus-history posture from list order.

Required design decisions you must make explicitly:
- define the relationship between base `ClientDocumentRequest`, route-local request-card projection, and document-center aggregation so there is exactly one authoritative lineage model
- define the exact separation among `latest_upload_ref`, `current_request_upload_ref_or_null`, request-card `current_upload_ref`, and request-card `current_artifact_upload_ref`
- define when history becomes `NONE`, `LIMITED`, or `MASKED_PRESENT`, and how limited history disclosure is surfaced without pretending no history exists
- define how preview posture and preview reason codes are derived without swallowing the later full artifact-affordance card; use the smallest lawful adapter now and make it replaceable
- define how request-card status is derived from request lifecycle plus current upload transfer/review posture so the card cannot claim a state contradicted by the mounted current upload

Execution requirements:
- publish a valid base `ClientDocumentRequest` for each governed request
- publish a route-ready request card for the portal documents surface that carries one explicit `why_requested_label`, one explicit `due_label`, one explicit `request_version_ref`, one explicit current-request-satisfaction pointer, and one explicit current-artifact pointer
- every visible upload row must mirror `request_version_ref`, `request_binding_state`, `resumability_state`, `attachment_state`, `history_state`, `preview_posture`, and typed `preview_reason_code`
- `latest_upload_ref` is chronology only; `current_request_upload_ref_or_null` is the sole field that may say the request is currently satisfied
- `current_upload_ref` on a request card must point only to an upload inside that same request card
- `current_artifact_upload_ref` must remain distinct from request satisfaction when a stale/rejected/superseded upload should not become the default client artifact
- publish explicit `artifact_selection` and a minimal lawful `artifact_affordance` adapter so preview/download/print targets remain server-authored through refresh, reconnect, and narrow-screen recovery
- if you add the preview lab, make it a document-center preview with one dominant request stack, one bounded status list, and clearly secondary history disclosure; no enterprise table wall

Gap handling you must close while implementing:
- close request-local upload identity drift and duplicate `upload_session_id` drift inside one request card
- close the gap where request-card status can disagree with the transfer posture of the current upload
- close the gap where current versus historical meaning is reconstructed from row order, chip color, or the last focused row
- close the gap where preview/download posture is left implicit until the browser tries to open a file
- close the gap where withdrawn or expired requests retain invalid upload lineage

Mandatory edge cases:
- `latest_upload_ref` MUST resolve to one entry in `upload_refs[]` whenever present
- upload-bearing request states MUST not serialize empty `upload_refs[]` or null `latest_upload_ref`
- `WITHDRAWN` requests MUST clear upload lineage and current-upload pointers
- `EXPIRED` requests MUST keep a non-null `due_at`
- `current_upload_ref`, when non-null, MUST point to one upload in the same request card and must be status-compatible with the request card lifecycle
- `history_state` must be explicit on every visible upload row; the portal may not infer current/history posture from visual grouping alone
- rebase must not silently relabel a stale or rejected upload as the current request-satisfying upload

Playwright and contract-test requirements:
- unit-test base request, request-card, and document-center projectors directly
- patch or add Playwright APIRequestContext assertions against any shipped documents-route responses that now consume these projectors
- if you add the preview lab, add Playwright browser tests that verify explicit current vs history labels, preview-target stability, and focusable browse/drag/camera affordances using semantic locators only
- no fixed sleeps; use explicit state/assertion transitions

Backend, performance, and security requirements:
- keep customer-safe filtering, artifact selection, and history limitation server-side
- do not let stale, rejected, superseded, or masked artifacts become default preview/download targets through local cache reuse
- keep request-local upload membership and current-artifact selection indexed and deterministic
- fail closed on upload lineage drift, mixed request-version inputs, or ambiguous preview posture

Validation rules:
- validate base requests, request-card projections, and document-center aggregates against canonical or newly introduced typed contracts
- add regression coverage for current-upload membership, current-artifact separation, limited-history disclosure, preview posture, withdrawn cleanup, and expired due-window requirements
- ensure the portal workspace builder consumes the explicit request projectors rather than duplicating their logic

Definition of done:
- the repo has one authoritative document-request projector and one route-ready request-card projection
- current-versus-history artifact meaning becomes backend-authored instead of list-order folklore
- document-route request cards remain consistent with upload truth, stale rebases, and limited-history posture
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
