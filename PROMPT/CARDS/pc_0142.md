---
id: pc_0142
slug: phase_03_parallel_wave_01_track_backend_authority_010_implement_out_of_band_authority_correction_and_truth_propagation_services
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "010"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_010_implement_out_of_band_authority_correction_and_truth_propagation_services
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0142

## Task Summary

out of band authority correction and truth propagation services.

## Metadata

- ID: `pc_0142`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `010`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_010_implement_out_of_band_authority_correction_and_truth_propagation_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0141](./pc_0141.md) `phase_03_parallel_wave_01_track_backend_authority_009_implement_authority_calculation_request_result_and_user_confirmation_flow`
- Next: [pc_0143](./pc_0143.md) `phase_03_parallel_wave_01_track_backend_authority_011_implement_filing_notice_step_and_resolution_state_handling`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0142_to_0149.md](../shared_operating_contract_0142_to_0149.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_010_implement_out_of_band_authority_correction_and_truth_propagation_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0141` if they exist, especially `pc_0061`, `pc_0068`, `pc_0074`, `pc_0075`, `pc_0076`, `pc_0097` through `pc_0109`, `pc_0118` through `pc_0127`, and `pc_0134` through `pc_0141`.

Mission:
Implement the authoritative out-of-band / authority-correction propagation layer.
This card must detect post-seal authority truth that contradicts or escapes the original packet lineage, persist one durable `TemporalPropagationEvent`, and drive every dependent invalidation / reopen path from that event rather than from ad hoc workflow, UI, or queue logic.

Source algorithm grounding you must honor:
- `late_data_authority_correction_and_replay_propagation_contract.md`, especially the governing model, propagation rules, and exact replay / recovery reuse requirements.
- `authority_interaction_protocol.md`, especially sections `9.13`, `9.13A`, `9.13B`, and `9.14`.
- `authority_truth_and_internal_projection_separation_contract.md`.
- `amendment_and_drift_semantics.md`, especially sections on baseline precedence, out-of-band baseline, and `10.14 Authority-correction semantics`.
- `data_model.md`, especially `SubmissionRecord`, `TemporalPropagationEvent`, `ObligationMirror`, `FilingCase`, and the truth / replay / proof / baseline fields nearby.
- `modules.md`, especially `RECONCILE_AUTHORITY_STATE(...)`, filing-case reuse rules, trust currency, and any downstream packet / proof reuse constraints.
- `replay_and_reproducibility_contract.md`, `provenance_graph_semantics.md`, and `defensible_filing_graph_contract.md` where stale proof and exact replay behavior are defined.
- `PATCH_RESOLUTION_INDEX.md`, especially the out-of-band audit-correlation and authority-basis / baseline-lineage fixes.
- `test_vectors.md`, especially vectors covering unknown, out-of-band, authority-corrected, stale-trust, and replay-exact behavior.

Deliverables to create:
1. `packages/backend-authority/src/models/temporal_propagation_event.ts`
2. `packages/backend-authority/src/repositories/temporal_propagation_event_repository.ts`
3. `packages/backend-authority/src/services/classify_authority_temporal_event.ts`
4. `packages/backend-authority/src/services/build_temporal_propagation_event.ts`
5. `packages/backend-authority/src/services/apply_out_of_band_truth_to_submission_chain.ts`
6. `packages/backend-authority/src/services/apply_authority_correction_to_submission_chain.ts`
7. `packages/backend-authority/src/services/propagate_temporal_event_to_baselines.ts`
8. `packages/backend-authority/src/services/propagate_temporal_event_to_filing_case_and_packet.ts`
9. `packages/backend-authority/src/services/propagate_temporal_event_to_proof_and_trust.ts`
10. `packages/backend-authority/src/services/propagate_temporal_event_to_workflow_and_client_projection.ts`
11. `packages/backend-authority/src/services/enforce_temporal_replay_basis_integrity.ts`
12. `packages/backend-authority/src/services/project_temporal_event_summary.ts`
13. `db/migrations/phase03_0142_temporal_propagation_and_authority_correction.sql`
14. `docs/authority/out_of_band_authority_correction_and_temporal_propagation.md`
15. `tests/unit/backend_authority/temporal_propagation_event_contract.spec.ts`
16. `tests/unit/backend_authority/authority_correction_classification.spec.ts`
17. `tests/integration/backend_authority/out_of_band_and_authority_correction_flow.spec.ts`
18. `tests/integration/backend_authority/temporal_replay_reuse.spec.ts`

Required design decisions you must make explicit:
- Resolve the schema / prose gap correctly.
  The corpus prose speaks about `SubmissionRecord` becoming authority-corrected, but `schemas/submission_record.schema.json` does not permit `lifecycle_state = AUTHORITY_CORRECTED`.
  Do **not** invent an illegal lifecycle state.
  Represent:
  - authority-corrected legal truth by lawful `SubmissionRecord` state plus `baseline_type = AUTHORITY_CORRECTED` where appropriate; and
  - out-of-band legal truth by `lifecycle_state = OUT_OF_BAND`;
  while `TemporalPropagationEvent.event_class` captures the correction / out-of-band trigger class.
- Define the exact trigger matrix for `TemporalPropagationEvent.event_class in {LATE_DATA_INVALIDATION, AUTHORITY_CORRECTION, OUT_OF_BAND_DISCOVERY, TEMPORAL_UNCERTAINTY_BLOCK}` for this card’s authority-facing paths.
- Define how `affected_scope_refs[]`, `affected_submission_refs[]`, and `active_exact_scope_key` are derived when the correction covers only a subset of obligations / partitions.
- Define the propagation ordering:
  1. authoritative submission / authority basis classification,
  2. temporal event persistence,
  3. baseline rebuild invalidation,
  4. filing-case / packet reuse invalidation,
  5. proof / trust / parity / graph invalidation,
  6. mirror / workflow / client-safe reopen.
- Define how replay safety works.
  `historical_reuse_policy` is fixed as `NO_FRESH_RECLASSIFICATION`; exact replay and exact same-attempt recovery must reuse the persisted temporal event lineage instead of reclassifying from fresh scans.
- Define when a prior `READY_TO_SUBMIT`, `APPROVED_TO_SUBMIT`, or customer-safe “resolved” posture must be reopened and demoted.

Execution steps:
1. Implement the schema-backed `TemporalPropagationEvent` model and repository with canonical serialization, deterministic ids / hashes, and indexes by manifest, exact scope key, event class, affected submission, and emitted timestamp.
2. Implement a classifier that takes authority-grounded evidence from reconciliation / ingress / mirror review and decides whether the event is:
   - a new out-of-band discovery,
   - a correction to earlier authority-grounded settlement,
   - a temporal uncertainty block,
   - or no new temporal event.
3. Implement `build_temporal_propagation_event.ts` so every persisted event carries all mandated effect families:
   `trust_effect`,
   `proof_effect`,
   `baseline_effect`,
   `retroactive_effect`,
   `amendment_effect`,
   `replay_effect`,
   `mirror_reopen_effect`.
4. Implement submission-chain mutation helpers that:
   - preserve historical `SubmissionRecord` lineage,
   - classify lawful `OUT_OF_BAND` posture,
   - map authority-corrected truth to lawful schema fields,
   - and never overwrite earlier confirmed history into a false single-state narrative.
5. Implement propagation services that mark stale or reopen state on:
   - `DriftBaselineEnvelope`,
   - `FilingCase`,
   - `FilingPacket`,
   - `ProofBundle`,
   - `TrustSummary`,
   - `EvidenceGraph`,
   - `ParityResult`,
   - `ObligationMirror`,
   - `WorkflowItem`,
   - customer-safe timeline / status projections where those read models already exist.
6. Implement exact replay enforcement so `ReplayBasisIntegrityContract` and any recovery path bind to persisted temporal-event refs and fail closed or downgrade explicitly if the historical event lineage is missing.
7. Emit typed audit / metric events for correction discovery, out-of-band discovery, propagation fan-out, and replay-block posture without letting telemetry become the source of truth.

Gap handling you must perform:
- Close the gap where corpus prose suggests a `SubmissionRecord` state that the schema forbids.
- Close the gap where out-of-band or authority-corrected truth could otherwise update only `ObligationMirror` and leave trust / proof / packet reuse stale.
- Close the gap where exact replay could silently substitute fresh reclassification for historical temporal-event lineage.
- Close the gap where partial-scope authority corrections could flatten all obligations or all partitions into one blunt client-level state.
- Close the gap where previously “ready” or “approved” packet posture could survive a later authority correction without explicit demotion and timestamped invalidation reason.
- Close the gap where workflow / client-safe projections could stay resolved while post-seal authority truth has reopened the case.

Mandatory edge cases:
- partial-scope correction affecting only one obligation or one partition while sibling scope remains unchanged
- out-of-band authority truth discovered for a meaning that already has a packet-chain submission record
- correction discovered after prior `CONFIRMED` truth and after downstream trust/proof/packet readiness had already been published
- authenticated but only partially correlated authority evidence that is strong enough to block reuse but not strong enough to manufacture a new confirmed state
- duplicate temporal-event detection on retried reconciliation so the same authoritative trigger does not fan out duplicate invalidations
- exact replay of a historical run after the live authority state has changed again
- stale same-attempt recovery after a temporal event was emitted between the original send and the recovery attempt

Backend, performance, and security requirements:
- fail closed on missing authority basis refs, contradictory effect families, illegal submission-state invention, or partial propagation writes
- use transaction boundaries or saga-style durable checkpoints so authoritative submission mutation and temporal-event emission cannot diverge silently
- preserve append-only history for submissions, temporal events, audit events, and downstream stale / reopen markers
- do not persist raw callback payload blobs or secrets on the temporal event itself; store typed refs to governed evidence instead
- make propagation idempotent by persisted event hash and affected-scope / affected-artifact refs
- keep queryability strong enough for investigation and replay without scanning raw logs

Validation rules:
- validate every persisted `TemporalPropagationEvent` against `schemas/temporal_propagation_event.schema.json`
- prove that authority-correction handling does not invent illegal `SubmissionRecord.lifecycle_state` values
- prove that exact replay reuses persisted temporal events instead of live reclassification
- integration-test out-of-band discovery, authority correction, partial-scope correction, stale packet demotion, stale trust demotion, workflow reopen, and customer-safe projection reopen
- prove that unresolved or out-of-band truth never silently republishes a confirmed-ready packet or resolved workflow posture

Definition of done:
- the repo contains a durable, schema-backed temporal propagation layer for out-of-band and authority-correction events
- post-seal authority truth now invalidates and reopens all downstream truth consumers from one canonical event packet
- exact replay and same-attempt recovery now honor persisted temporal-event lineage instead of fresh reinterpretation
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
