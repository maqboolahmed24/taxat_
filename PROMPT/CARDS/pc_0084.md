---
id: pc_0084
slug: phase_02_seq_084_create_release_branching_versioning_and_artifact_naming_conventions
mode: sequential
phase: "02"
wave: null
track: null
task_number: "084"
block_key: phase_02_seq
original_key: phase_02_seq_084
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0084

## Task Summary

create release branching versioning and artifact naming conventions.

## Metadata

- ID: `pc_0084`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `084`
- Source Slug: `phase_02_seq_084_create_release_branching_versioning_and_artifact_naming_conventions`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0083](./pc_0083.md) `phase_02_seq_083_create_baseline_access_control_matrix_and_default_role_seed_data`
- Next: [pc_0085](./pc_0085.md) `phase_03_parallel_wave_01_track_backend_access_001_implement_tenant_user_and_actor_session_persistence_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0078_to_0085.md](../shared_operating_contract_0078_to_0085.md)
```text
Execute roadmap task `phase_02_seq_084_create_release_branching_versioning_and_artifact_naming_conventions`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0083` if they exist, especially `pc_0027`, `pc_0030`, `pc_0055`, `pc_0056`, `pc_0057`, `pc_0058`, `pc_0060`, `pc_0066`, `pc_0080`, `pc_0081`, `pc_0082`, and `pc_0083`.

Mission:
Create the machine-readable release naming and branching law for the repo.
This task must bind human-facing version strings, branch families, build IDs, artifact filenames, registry paths, release channels, and promotion evidence back to the canonical candidate identity and compatibility posture defined by the corpus.
A future build pipeline should be able to derive names and artifact coordinates without improvisation.

Source algorithm grounding you must honor:
- `deployment_and_resilience_contract.md`, especially `2. Promotion pipeline`, `3. Schema and datastore migration rules`, and `6. Rollout, rollback, and fail-forward posture`.
- `release_candidate_identity_and_promotion_evidence_contract.md`, especially the governing candidate identity tuple and admissibility boundary.
- `verification_and_release_gates.md`, especially blocking gates, executed test-run identifiers, and promotion evidence.
- `schemas/build_artifact.schema.json`, `schemas/deployment_release.schema.json`, and `schemas/release_verification_manifest.schema.json`.
- Prior supply-chain, edge, preview, credential-smoke, and migration foundations from `pc_0055`, `pc_0056`, `pc_0057`, `pc_0058`, `pc_0066`, and `pc_0080`.

Deliverables to create:
1. `config/release/branching_policy.json`.
2. `config/release/versioning_policy.json`.
3. `config/release/artifact_naming_policy.json`.
4. `config/release/release_channel_matrix.json`.
5. `scripts/release/derive_release_identity.ts`.
6. `scripts/release/render_artifact_names.ts`.
7. `scripts/release/validate_release_metadata.ts`.
8. `docs/release/branching_versioning_and_artifact_naming.md`.
9. `tests/unit/release/release_identity_derivation.spec.ts`.
10. `tests/integration/release/release_metadata_contract.spec.ts`.

Required design decisions you must make explicit:
- Define the branch families for mainline, release preparation, hotfix, and experimental work, and explain how they interact with promotion and rollback posture.
- Define the human-facing version format and its relationship to immutable build IDs and candidate identity hashes.
- Define artifact naming templates for backend images, worker images, web bundles, desktop builds, SBOMs, provenance bundles, signatures, and verification manifests.
- Define the mapping between branch family, release channel, environment admissibility, and rollout strategy.
- Define how artifact names include schema bundle, config bundle, or candidate identity data without becoming unreadable or violating storage constraints.
- Define how fail-forward-only migrations, pinned baselines, canary aborts, and emergency promotions are reflected in naming and metadata.

Execution steps:
1. Materialize machine-readable policies for branch families, version derivation, release channels, artifact classes, filename templates, registry paths, and forbidden collisions.
2. Implement a release-identity derivation utility that combines VCS ref, build metadata, schema bundle, config bundle, channel posture, and candidate identity into one validated structure.
3. Implement an artifact-name renderer that emits deterministic filenames and paths for every build artifact and evidence artifact.
4. Implement release metadata validation that checks branch/channel compatibility, naming compliance, schema/config bundle presence, and promotion evidence completeness.
5. Add tests that cover standard canary, emergency promote, pinned baseline, fail-forward compensating, rollback-safe, and fail-forward-only scenarios.
6. Document how branch names, versions, build IDs, and artifact names should be used by CI, release tooling, and human reviewers.

Gap handling you must perform:
- Close the gap where candidate identity is defined but no naming law turns it into branch and artifact conventions.
- Close the gap where teams could mix human-friendly semver labels with untraceable build outputs.
- Close the gap where different artifact classes drift into inconsistent registry paths or filenames.
- Close the gap where emergency and fail-forward release posture has no visible naming expression.
- Close the gap where release metadata validation happens ad hoc in CI instead of through one governed contract.

Mandatory edge cases:
- same commit built for two release channels
- hotfix branch from a pinned baseline
- candidate includes a schema change that is fail-forward only
- canary aborted and previous baseline re-promoted
- desktop artifact requiring notarization companion files
- web-only config change with unchanged application code
- build artifact name collision caused by insufficient metadata fields
- release metadata present but incompatible with branch family rules

Backend, performance, and security requirements:
- Artifact names and metadata must never include secrets or unsafe environment material.
- Identity derivation must be deterministic across repeated runs for the same inputs.
- Human-readable versions may change presentation, but immutable artifact identities must not.
- Validation should fail closed when required provenance, signatures, bundles, or manifests are missing.
- Policy files must be diff-friendly and machine-consumable by future CI or release orchestrators.

Validation rules:
- Unit-test version derivation, branch classification, and artifact-name rendering.
- Integration-test release metadata validation against representative candidate identities and rollout postures.
- Prove that artifact names remain stable for identical inputs and diverge when candidate identity inputs change.
- Prove that illegal branch/channel combinations are rejected.
- Prove that promotion-evidence artifacts render names consistent with the naming policy.

Definition of done:
- The repo has one machine-readable law for branches, versions, channels, and artifact names.
- Human-readable release labels now map cleanly to candidate identity and promotion evidence.
- Future release automation can build on this policy instead of re-inventing naming rules.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

