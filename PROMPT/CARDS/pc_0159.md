---
id: pc_0159
slug: phase_03_parallel_wave_01_track_backend_northbound_003_implement_decision_bundle_snapshot_endpoint_with_etag_support
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "003"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0159

## Task Summary

implement decision bundle snapshot endpoint with etag support.

## Metadata

- ID: `pc_0159`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `003`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_003_implement_decision_bundle_snapshot_endpoint_with_etag_support`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0158](./pc_0158.md) `phase_03_parallel_wave_01_track_backend_northbound_002_implement_get_command_receipt_endpoint_with_recovery_anchor_semantics`
- Next: [pc_0160](./pc_0160.md) `phase_03_parallel_wave_01_track_backend_northbound_004_implement_experience_snapshot_endpoint_and_stability_contract_publication`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0158_to_0165.md](../shared_operating_contract_0158_to_0165.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_003_implement_decision_bundle_snapshot_endpoint_with_etag_support`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0158` if they exist, especially `pc_0102` through `pc_0133`, `pc_0134` through `pc_0141`, and `pc_0157` through `pc_0158`.

Mission:
Implement `GET /v1/manifests/{manifest_id}/decision-bundle` as the canonical terminal/review bundle read surface.
This endpoint must return the latest persisted `DecisionBundle`, set `ETag = decision_bundle_hash`, honor conditional requests safely, and avoid inventing bundle meaning from transient execution state or UI heuristics.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially sections 1, 2, 5, and 6.
- `data_model.md`, especially the `DecisionBundle` entry and its truth-boundary semantics.
- `modules.md`, especially the bundle-persist/reload semantics for terminal manifests and same-request retry.
- `state_machines.md` where terminal/review posture is represented through `DecisionBundle` rather than a separate manifest lifecycle.
- `schemas/decision_bundle.schema.json` and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 69-70, and `AUDIT_FINDINGS.md` item 30 about stale or mismatched bundle artifacts.

Deliverables to create:
1. `packages/backend-northbound/src/http/get_decision_bundle_endpoint.ts`
2. `packages/backend-northbound/src/http/register_get_decision_bundle_route.ts`
3. `packages/backend-northbound/src/query/get_latest_decision_bundle_for_manifest.ts`
4. `packages/backend-northbound/src/services/apply_decision_bundle_conditional_request.ts`
5. `packages/backend-northbound/src/services/build_decision_bundle_not_ready_problem.ts`
6. `packages/backend-northbound/src/services/validate_decision_bundle_publication.ts`
7. `docs/northbound/get_decision_bundle.md`
8. `tests/unit/backend_northbound/decision_bundle_etag_rules.spec.ts`
9. `tests/unit/backend_northbound/decision_bundle_publication_rules.spec.ts`
10. `tests/integration/backend_northbound/get_decision_bundle_contract.spec.ts`
11. `tests/integration/backend_northbound/get_decision_bundle_not_ready.spec.ts`
12. `tests/playwright_api/northbound/get_decision_bundle_etag.spec.ts`

Optional diagnostic explorer only if it materially accelerates verification:
13. `apps/admin-console-web/src/routes/debug/decision-bundle/[manifestId].tsx`

Required design decisions you must make explicit:
- Define the endpoint posture when the manifest has no persisted bundle yet: typed not-ready problem versus not-found, and the exact recovery reference you return.
- Define the conditional request policy for `If-None-Match` and how it interacts with authorization, retention limits, and fresh bundle publication.
- Define how same-request retries against terminal manifests reuse the persisted bundle instead of allocating a continuation path.
- Define how blocked, review-required, and fully completed bundles all remain first-class return shapes rather than transport exceptions.
- Define whether the endpoint exposes historical bundle selection; default to current-only unless the algorithm explicitly requires historical lookup.

Execution steps:
1. Load the latest persisted bundle by manifest id from durable storage only.
2. Re-validate the bundle against `decision_bundle.schema.json` before response.
3. Set `ETag` exactly to `decision_bundle_hash` and implement conditional `304 Not Modified` only when the caller presented the current hash and is still authorized to see that bundle.
4. When no bundle exists yet, emit a typed `ProblemEnvelope` that points the caller toward the correct next recovery or wait surface rather than a generic transport miss.
5. Preserve truth-boundary semantics: this is a persisted bundle read, not a synthetic assembly endpoint.
6. Add deterministic contract tests covering accepted retrieval, blocked/review bundles, conditional caching, and no-bundle-yet posture.

Gap handling you must perform:
- Close the gap where stale or example-only bundle shapes drift away from the schema and are served as if they were canonical.
- Close the gap where `decision_reason_codes[]` and top-level `reason_codes[]` can diverge without detection.
- Close the gap where `primary_action_code` drifts outside the legal next-action set or appears in blocked actions at the same time.
- Close the gap where a manifest with no persisted bundle forces clients to infer readiness from unrelated manifest state.

Mandatory edge cases:
- `ETag` MUST equal the current `decision_bundle_hash`
- conditional `304` MUST NOT bypass authorization or retention checks
- `decision_status = BLOCKED` and `decision_status = REVIEW_REQUIRED` remain valid bundle returns when persisted
- same-request retry against a terminal manifest MUST reload the existing bundle, not synthesize a new one
- schema-invalid bundle rows MUST fail closed and surface operational evidence instead of partial reads
- the not-ready path MUST be typed and actionable, not a silent `204`

Playwright / contract-test requirements:
- use Playwright APIRequestContext or equivalent deterministic HTTP contract tooling
- no fixed sleeps around bundle publication checks; drive setup through deterministic fixtures
- prove `If-None-Match` returns `304` only for the exact live hash
- prove not-ready posture returns a typed problem envelope and does not masquerade as an empty success

Backend, performance, and security requirements:
- use indexed manifest lookup and avoid rebuilding bundle content on every read
- never treat UI filters or route state as the source of bundle legality
- keep response caching scoped to the exact manifest and authorization context
- fail closed on schema mismatch or integrity drift

Validation rules:
- validate all success responses against `decision_bundle.schema.json`
- validate no-bundle and other failures against `problem_envelope.schema.json`
- unit-test ETag calculation, conditional handling, and bundle publication integrity
- integration-test completed, blocked, review-required, and not-ready cases
- API-test the shipped endpoint with Playwright

Definition of done:
- the repo exposes `GET /v1/manifests/{manifest_id}/decision-bundle`
- `ETag = decision_bundle_hash` and conditional retrieval works correctly
- callers receive persisted terminal/review bundle truth rather than synthetic transport guesses
- not-ready bundle posture is typed, reproducible, and recovery-aware
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
