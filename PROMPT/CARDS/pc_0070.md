---
id: pc_0070
slug: phase_02_seq_070_create_queue_abstraction_for_worker_dispatch_retries_and_dead_letter_flows
mode: sequential
phase: "02"
wave: null
track: null
task_number: "070"
block_key: phase_02_seq
original_key: phase_02_seq_070
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0070

## Task Summary

create queue abstraction for worker dispatch retries and dead letter flows.

## Metadata

- ID: `pc_0070`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `070`
- Source Slug: `phase_02_seq_070_create_queue_abstraction_for_worker_dispatch_retries_and_dead_letter_flows`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0069](./pc_0069.md) `phase_02_seq_069_create_object_storage_abstraction_with_quarantine_and_retention_metadata_hooks`
- Next: [pc_0071](./pc_0071.md) `phase_02_seq_071_create_streaming_abstraction_for_sse_resume_rebase_and_monotonic_sequence_delivery`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0070_to_0077.md](../shared_operating_contract_0070_to_0077.md)
```text
Execute roadmap task `phase_02_seq_070_create_queue_abstraction_for_worker_dispatch_retries_and_dead_letter_flows`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0069` if they exist, especially `pc_0020`, `pc_0026`, `pc_0052`, `pc_0063`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0067`, `pc_0068`, and `pc_0069`.

Mission:
Build the reusable queue abstraction that later stage workers, authority-transmit workers, reconciliation workers, projection workers, and recovery jobs can all share without confusing transport with durable truth.
This task must encode the corpus' ordering, lease, retry, resend-legality, and dead-letter semantics explicitly.
The result should let later features dispatch work safely through a broker while preserving manifest-bound execution, deterministic retry posture, and queue rebuildability from durable records.

Source algorithm grounding you must honor:
- `deployment_and_resilience_contract.md`, especially the rules that the queue/broker is delivery fabric only, that queue loss is recoverable from outbox/inbox truth, and that restore / rebuild work may not re-send live authority mutations from queue state alone.
- `core_engine.md`, especially `PERSIST_STAGE_WORKSET_AND_OUTBOX(...)`, `PERSIST_STAGE_DAG_AND_OUTBOX(...)`, `AUTHORITY_TRANSMIT`, `WAIT_FOR_TRANSMIT_OR_RECONCILIATION(...)`, ordering by `order_domain_key` / `transmit_order_domain_key`, and the competing-consumer requirements.
- `authority_interaction_protocol.md`, especially `dispatch_ref`, `resend_legality_state`, `retry_class`, `correlation_status`, mandatory send-time revalidation, duplicate-truth basis, and inbound normalization through the transactional inbox.
- `manifest_and_config_freeze_contract.md` and `manifest_start_claim_protocol.md`, especially the rule that workers consume manifest-bound identities and that queue state never substitutes for durable claim truth.
- `error_model_and_remediation_model.md`, especially the deterministic retry backoff, phase offset, retry budget, and retry precondition rules.
- `security_and_runtime_hardening_contract.md` and `observability_and_audit_contract.md`, especially opaque refs in queue payloads, structured correlation, and redaction-safe transport metadata.
- Earlier messaging and northbound scaffolds from `pc_0067` and `pc_0068`.

Deliverables to create:
1. `packages/domain-kernel/src/queue/queue_port.ts`.
2. `packages/domain-kernel/src/queue/worker_dispatch_envelope.ts`.
3. `packages/domain-kernel/src/queue/dispatch_claim.ts`.
4. `packages/domain-kernel/src/queue/retry_scheduler.ts`.
5. `packages/domain-kernel/src/queue/dead_letter_classifier.ts`.
6. `packages/domain-kernel/src/queue/order_domain_policy.ts`.
7. `config/queue/worker_queue_catalog.json`.
8. `config/queue/retry_budget_matrix.json`.
9. `config/queue/dead_letter_resolution_policy.json`.
10. `config/queue/order_domain_scope_matrix.json`.
11. `docs/queue/worker_dispatch_retry_and_dead_letter_runbook.md`.
12. `apps/operator-web/src/routes/internal/queue-fabric-atlas.tsx`.
13. `tests/unit/queue/queue_foundation.spec.ts`.
14. `tests/integration/queue/worker_dispatch_retry_flow.spec.ts`.
15. `tests/playwright/internal/queue_fabric_atlas.spec.ts`.

Required design decisions you must make explicit:
- Define queue families and routing keys for stage work, authority transmit, reconciliation follow-up, projection refresh, and recovery jobs without inventing one giant queue.
- Define the exact ordering scope for each family and preserve the corpus rule that ordering is only lawful within one declared domain key.
- Define lease / claim / visibility-timeout semantics so stale workers, crash recovery, and duplicate delivery remain safe.
- Define how retry budgets, next-attempt scheduling, and dead-letter classification consume the deterministic rules from `error_model_and_remediation_model.md`.
- Define how authority-bound sends carry persisted resend-legality and mandatory send-time revalidation state instead of inferring legality from queue state.
- Define the payload redaction envelope so packets carry opaque refs, hashes, policy refs, and correlation keys rather than broad business blobs or secrets.
- Define how queue rebuild works from durable outbox / inbox / interaction / receipt truth after broker loss.
- Define the relationship between queue telemetry, inbox/outbox truth, and append-only audit evidence.

Execution steps:
1. Create a machine-readable queue catalog that names every queue family, the lawful producer families, the consumer worker class, the ordering key basis, and the dead-letter posture.
2. Implement the queue port and dispatch envelope as a transport boundary that points to durable records instead of copying authoritative truth into broker payloads.
3. Implement claim / lease helpers for worker dispatch, reclaim, visibility timeout extension, and stale-claim fencing.
4. Implement a retry scheduler that consumes the deterministic retry and economic-gate formulas from the corpus instead of random jitter or vendor-default retry knobs.
5. Implement dead-letter classification and resolution policy files that distinguish safe replay, reconcile-then-retry, rebuild-then-retry, operator review, and terminal no-retry cases.
6. Implement queue rebuild helpers and runbook guidance showing how durable outbox / inbox / authority interaction truth repopulates delivery fabric after loss.
7. Build `queue-fabric-atlas` as a read-only inspection surface showing emit → outbox → queue → worker → inbox / effect → audit, with per-family ordering and dead-letter posture.
8. Add unit, integration, and Playwright coverage for duplicate delivery, stale worker reclaim, deterministic retry cadence, dead-letter routing, and broker-rebuild posture.

Visual and interaction specification for `queue-fabric-atlas`:
- Tone: a premium dispatch loom.
  The page should feel like a disciplined transport atelier, not a cloud message-console clone.
- Layout:
  - `64px` top bar with queue-family selector, ordering-scope badge, and dead-letter posture chip.
  - left family rail `252-268px` listing `OUTBOX`, `QUEUE`, `CLAIM`, `WORKER`, `INBOX`, `AUDIT`.
  - center canvas with a braided lane diagram: one strand per queue family, one vertical stitch per lifecycle stage.
  - right inspector `372-404px` with selected family metadata, ordering basis, retry budget class, and dead-letter resolution notes.
- Diagram:
  - hero diagram is a woven six-lane dispatch braid with subtle retry rings orbiting any stalled node.
  - packet nodes are narrow rounded lozenges showing refs and hashes only; expanding a node reveals source record family and resend-legality posture.
  - include a custom monoline spool glyph near the heading.
- Palette:
  - background `#F4F4F1`
  - primary surface `#FFFFFF`
  - secondary surface `#EEF0EB`
  - ink `#0F1418`
  - muted ink `#69727B`
  - hairline `rgba(15,20,24,0.08)`
  - accent indigo `#495B7A`
  - accent evergreen `#5B7059`
  - accent bronze `#8A6540`
  - success `#17614B`
  - warning `#8B5D1B`
  - danger `#A63B31`
- Typography:
  - premium ledger styling; use the adopted sans stack for prose and labels, monospace only for refs, hashes, queue names, and policy codes.
- Dimensional constraints:
  - max width `1500px`
  - lane min height `76px`
  - node pill height `30px`
  - inspector rule rows `36px`
- Motion:
  - `120ms` node highlight and inspector reveal.
  - `180ms` retry-ring pulse only when reduced motion is not preferred.
  - reduced motion removes pulse and path drift entirely, replacing it with outline and opacity changes.
- Interaction:
  - selecting a queue family must update the ordering-key explanation, retry budget class, and dead-letter policy.
  - selecting a node must reveal the authoritative durable record family it points to and the last lawful retry / resend posture.
  - accessible names should read as transport statements such as `authority transmit queue packet references interaction record and uses resend legality clear to send`.
  - Playwright must use role- and text-first locators and must prove that no raw secrets or broad payload blobs render anywhere in the DOM.

Gap handling you must perform:
- Close the gap where broker packets could accidentally become the practical source of truth instead of pointers to durable records.
- Close the gap where retry rules are described textually in the corpus but not serialized into reusable runtime and policy files.
- Close the gap where dead-letter posture often remains an ops convention instead of a product-safe, testable policy contract.
- Close the gap where stale worker reclaim and queue rebuild could accidentally re-send authority mutations without checking persisted resend-legality truth.
- Close the gap where queue telemetry could exist without enough typed lineage to explain backlog, delay, or redrive posture safely.

Mandatory edge cases:
- the same broker packet delivered twice to the same consumer
- worker crash after external side effect but before ack
- stale worker reclaim after visibility timeout expiry
- `AUTHORITY_TRANSMIT` packet delayed long enough that send-time revalidation must fail closed
- queue rebuild from durable outbox / inbox truth after broker loss
- dead-letter escalation for a retryable family whose retry budget becomes economically or legally exhausted
- unrelated manifests sharing one queue family but requiring independent ordering scopes

Backend, performance, and security requirements:
- Queue publication and authoritative state transitions should remain transactionally coordinated through outbox / inbox foundations where the datastore allows.
- Consumers must be idempotent and must persist effect proof before acking or marking work complete.
- Keep queue payloads small, opaque, and secret-safe; never serialize raw declaration text, credential material, uploaded bytes, or authority tokens into queue packets.
- Competing-consumer scaling is allowed, but only ordering within one declared order domain may be enforced.
- Dead-letter records must retain enough refs, hashes, and policy lineage for investigation without requiring raw payload dumps.
- The atlas must visualize lawful topology and failure posture, not live broker credentials or vendor-admin controls.

Validation rules:
- Unit-test queue family resolution, ordering-key derivation, retry-budget scheduling, dead-letter classification, and claim fencing.
- Integration-test emit → outbox → queue stub → worker → inbox / effect flow with duplicate delivery and stale-claim recovery.
- Prove deterministic retry cadence under repeated failure and prove that exhausted or illegal retries go to dead-letter or review posture.
- Use Playwright to verify semantic lane labels, keyboard traversal, reduced-motion behavior, and redaction-safe rendering in `queue-fabric-atlas`.
- Ensure every queue family and policy file traces back to the corpus and earlier eventing decisions.

Definition of done:
- The repo has a reusable queue abstraction instead of ad hoc broker calls scattered through services.
- Transport packets remain secondary to durable truth.
- Ordering, retry, reclaim, and dead-letter posture are explicit, serialized, and testable.
- Later worker implementations can dispatch and recover safely without inventing queue semantics from memory.
- The internal atlas explains queue truth and failure posture without becoming a generic broker console.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
