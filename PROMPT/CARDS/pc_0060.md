---
id: pc_0060
slug: phase_02_seq_060_import_existing_json_schemas_validator_scripts_and_sample_payloads_into_shared_contracts_package
mode: sequential
phase: "02"
wave: null
track: null
task_number: "060"
block_key: phase_02_seq
original_key: phase_02_seq_060
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0060

## Task Summary

import existing json schemas validator scripts and sample payloads into shared contracts package.

## Metadata

- ID: `pc_0060`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `060`
- Source Slug: `phase_02_seq_060_import_existing_json_schemas_validator_scripts_and_sample_payloads_into_shared_contracts_package`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0059](./pc_0059.md) `phase_02_seq_059_bootstrap_monorepo_with_backend_web_native_and_shared_contract_packages`
- Next: [pc_0061](./pc_0061.md) `phase_02_seq_061_generate_language_models_and_type_bindings_from_json_schemas`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt

Use the shared operating contract first:
[../shared_operating_contract_0054_to_0061.md](../shared_operating_contract_0054_to_0061.md)

```text
Execute roadmap task `phase_02_seq_060_import_existing_json_schemas_validator_scripts_and_sample_payloads_into_shared_contracts_package`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0059` if they exist,
especially the monorepo/package bootstrap from `pc_0059`, the stack ADR from `pc_0019`,
and the earlier schema/release/governance outputs that already depend on canonical contracts.

Mission:
Import the authoritative contract corpus into the repository as a reusable shared-contracts package without semantic drift.
This task must preserve the exact meaning of the upstream JSON schemas, sample payloads, and Python validator scripts.
It is not a rewrite.
It is a governed import/mirroring task with source hashes, traceability, and executable validation.
When finished, the repo must have one canonical imported contracts package that downstream code can consume while still proving exact lineage back to the original `Algorithm/` source bundle.

Source algorithm grounding you must honor:
- `README.md`, especially the note that JSON schemas plus sample payloads live in `schemas/`
  and that the authoritative validator entrypoints are `python3 Algorithm/scripts/validate_contracts.py --self-test`
  and `python3 Algorithm/tools/forensic_contract_guard.py`.
- `implementation_conventions.md`, especially schema-discipline, deterministic serialization,
  prose/schema synchronization, and the requirement that schemas and prose stay aligned.
- `scripts/validate_contracts.py`, including:
  schema discovery,
  sample discovery,
  registry construction,
  sample/schema binding,
  repo coherence checks,
  forensic-guard integration,
  and self-test behavior.
- `tools/forensic_contract_guard.py` and its closure/guard semantics.
- every canonical schema under `Algorithm/schemas/` and every bundled `sample_*.json` payload.
- Prior cards `pc_0019`, `pc_0028`, `pc_0029`, `pc_0055`, and `pc_0059`.
- Current official documentation for JSON Schema Draft 2020-12 and any repository tooling used for import/sync.

Deliverables to create:
1. `packages/contracts/schemas/` populated from the authoritative imported schema set
2. `packages/contracts/samples/` populated from the authoritative imported sample set
3. `packages/contracts/python/validate_contracts.py`
4. `packages/contracts/python/forensic_contract_guard.py`
5. `packages/contracts/data/schema_source_map.json`
6. `packages/contracts/data/sample_binding_map.json`
7. `packages/contracts/src/schemaCatalog.ts`
8. `tools/contracts/import_algorithm_contracts.ts`
9. `tools/contracts/sync_contract_hashes.py`
10. `docs/contracts/schema_import_and_sync_runbook.md`
11. `apps/operator-web/src/routes/internal/schema-catalog-atlas.tsx`
12. `tests/unit/contracts/schema_source_map.spec.ts`
13. `tests/integration/contracts/import_algorithm_contracts.spec.ts`
14. `tests/playwright/internal/schema_catalog_atlas.spec.ts`

Required design decisions and import dimensions you must make explicit:
- whether the contracts package uses vendored copies, generated mirrors, or another import strategy
  and why that strategy best preserves source truth and repo autonomy
- how source hashes are captured for every imported schema/sample/validator artifact
- how `$id`, relative `$ref`, file names, and folder structure are preserved or normalized
- how sample payloads are bound to schemas, including cases where inference is by filename convention
- how the Python validators remain executable and authoritative inside the repo
- how TypeScript code discovers the imported schema catalog without hand-maintained duplication
- how future upstream Algorithm changes are detected, imported, and reviewed

Execution steps:
1. Inventory the authoritative upstream artifacts in `Algorithm/schemas/`, `scripts/validate_contracts.py`, and `tools/forensic_contract_guard.py`.
   Preserve counts, file names, and hashes in machine-readable form.
2. Design an import strategy that keeps the shared contracts package usable from inside the repo while preserving upstream traceability.
   The default should be a mirrored import with source hashes and a sync script unless a prior ADR forbids that.
3. Implement `import_algorithm_contracts.ts`.
   It must:
   - copy or mirror schemas and samples deterministically,
   - preserve canonical filenames and refs,
   - emit `schema_source_map.json` with source path, destination path, source hash, schema id, and source logical family,
   - emit `sample_binding_map.json` with source path, destination path, inferred schema name if applicable, and validation posture.
4. Import the Python validators into `packages/contracts/python/`.
   Preserve behavior and invocation shape as much as possible.
   If wrappers are needed for repo paths, keep the validator logic authoritative and keep wrappers thin.
5. Create `schemaCatalog.ts`.
   It must expose a deterministic programmatic index of imported schemas, ids, families, source hashes, and sample bindings for downstream TS consumers.
6. Create `schema_import_and_sync_runbook.md`.
   It must explain the import strategy, sync workflow, validation commands, and how to review upstream drift.
7. Build `schema-catalog-atlas` in the operator web internal route set.
   The page must let engineers inspect schema families, ids, source hashes, sample bindings, and validator lineage without turning into a generic docs browser.
8. Add unit/integration tests for source maps, import determinism, and validator availability.
9. Run the imported validator path and preserve that the authoritative self-test remains executable after import.

Visual and interaction specification for `schema-catalog-atlas`:
- This page must feel like a premium contract cabinet, not an API portal clone.
  Quiet, archivally precise, and immediately inspectable.
- Layout:
  - `64px` top context bar with contract-bundle badge, schema count badge, and import posture chip.
  - left schema-family rail `252-268px` listing logical families inferred from source files and document clusters.
  - central atlas canvas with three vertical planes:
    `Schema Identity`,
    `Source / Hash Lineage`,
    `Sample / Validator Binding`.
  - right inspector drawer `360-392px` showing selected schema id, file path, source hash, refs, and bundled samples.
- Palette:
  background `#F5F6F4`,
  primary surface `#FFFFFF`,
  secondary surface `#EEF1EC`,
  ink `#111418`,
  muted ink `#65707A`,
  hairline `rgba(17,20,24,0.08)`,
  accent slate `#456178`,
  accent moss `#5C715F`,
  accent bronze `#8A6733`,
  success `#16624B`,
  warning `#8A5C18`,
  danger `#A63B32`.
- Typography:
  typography-led, with monospace only for schema ids, hashes, file paths, and validator entrypoints.
- Diagram:
  the three-plane contract cabinet is the diagram.
  Include a thin reference ribbon showing `schema -> samples -> validator`.
  Do not add endpoint playgrounds or schema-form generators.
- Motion:
  120-150ms max for family switch and inspector reveal.
  Reduced-motion mode must use opacity/focus transitions only.
- Interaction:
  family rows, schema rows, and sample-binding rows must be keyboard reachable and expose stable semantic locators.

Gap handling you must perform:
- Close the “the authoritative schemas and validators still live only in the archive, not in the executable repo” gap.
- Close the “there is no machine-readable source-hash map connecting imported contracts back to the original Algorithm corpus” gap.
- Close the “sample payloads are bundled upstream, but there is no normalized binding map for downstream tooling” gap.
- Close the “later TS/Python/native code would otherwise consume contracts through ad hoc copies or forgotten paths” gap.
- Close the “validator scripts could be forked or rewritten informally without an explicit import discipline” gap.

Mandatory edge cases:
- preserve relative `$ref` resolution and schema ids; do not break bundle integrity through path normalization shortcuts
- preserve validator behavior even when repo paths differ from the original archive layout
- samples that validate by filename convention must still be traceable and explicit in the binding map
- duplicate schema ids, hash drift, or missing sample/source files must emit typed failures
- imported artifacts must remain read-only by convention where appropriate; do not invite manual drift
- future upstream changes must be detectable via hash or diff, not by human memory
- import determinism must survive different operating systems and line-ending environments

Backend, performance, and security requirements:
- Keep the import deterministic and idempotent.
- Preserve UTF-8 and stable formatting unless the upstream validator requires exact byte retention.
- Keep source hashes machine-readable and reviewable.
- Treat the imported Python validators as source-of-truth tooling, not as transpilation targets.
- Do not let the schema atlas mutate imported contracts.
- Avoid hidden schema rewriting or lossy normalization.

Validation rules:
- Unit tests must prove every imported schema has a source map entry and every imported sample has a binding-map entry.
- Unit tests must prove hash drift is detectable and relative refs remain resolvable.
- Integration tests must prove the import script can re-run without destructive duplication and that the imported validator self-test remains runnable.
- Playwright must validate semantic locators, keyboard flow, reduced-motion parity, inspector rendering, and schema/sample lineage visibility on the atlas page.

Definition of done:
- A later implementation or codegen agent can consume one authoritative contracts package inside the repo,
  prove exact lineage back to the original archive,
  and run the canonical validators without inventing a parallel schema world.
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm corpus and aligned to the authoritative card slug.
- Instructions explicitly preserve card status neutrality; claim and completion state must continue to live only in `Checklist.md`.
