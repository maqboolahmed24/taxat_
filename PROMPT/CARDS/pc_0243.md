---
id: pc_0243
slug: phase_05_parallel_wave_03_track_frontend_low_noise_003_Playwright_or_other_appropriate_tooling_implement_decision_summary_state_rendering_and_limitation_notices
mode: parallel
phase: "05"
wave: "03"
track: "frontend_low_noise"
task_number: "003"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_low_noise_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0243

## Task Summary

003 Playwright or other appropriate tooling implement decision summary state rendering and limitation notices.

## Metadata

- ID: `pc_0243`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend low noise`
- Task Number: `003`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_low_noise_003_Playwright_or_other_appropriate_tooling_implement_decision_summary_state_rendering_and_limitation_notices`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0242](./pc_0242.md) `phase_05_parallel_wave_03_track_frontend_low_noise_002_Playwright_or_other_appropriate_tooling_implement_context_bar_state_rendering_and_status_badges`
- Next: [pc_0244](./pc_0244.md) `phase_05_parallel_wave_03_track_frontend_low_noise_004_Playwright_or_other_appropriate_tooling_implement_action_strip_state_rendering_and_mutation_legality_controls`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_low_noise_003_Playwright_or_other_appropriate_tooling_implement_decision_summary_state_rendering_and_limitation_notices`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0242` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement `DecisionSummaryState` rendering and limitation notices for the low-noise shell’s dominant question, bounded reasons, uncertainty, and explicit empty/limited states.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The decision summary is the dominant-question surface. The algorithm freezes one primary issue, a bounded explanation, no more than three visible reasons, one warning, explicit uncertainty, and a limitation taxonomy. It also says hidden/erased/limited support must never imply non-occurrence. The component must render source truth plainly without local re-ranking or decorative analytics.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md design principles, decision summary, cognitive budget, empty/limited states, algorithm translation requirements`.
- `dominant_question_and_single_action_contract.md core contract and calm shell mapping`.
- `frontend_shell_and_interaction_law.md one dominant question and state presentation`.
- `schemas/decision_summary_state.schema.json, low_noise_budget_audit.schema.json, low_noise_experience_frame.schema.json`.

Deliverables to create or patch:
1. `packages/shared-ui/src/low-noise/DecisionSummary.tsx`
2. `packages/shared-ui/src/low-noise/LimitationNotice.tsx`
3. `packages/shared-ui/src/low-noise/ReasonList.tsx`
4. `packages/frontend-shell-core/src/low-noise/decision_summary_presenter.ts`
5. `packages/frontend-shell-core/src/low-noise/limitation_taxonomy_copy.ts`
6. `tests/unit/frontend-shell-core/decision_summary_presenter.spec.ts`
7. `tests/playwright/frontend/low_noise_decision_summary_limitation_notices.spec.ts`
8. `docs/frontend/decision_summary_state_and_limitation_notices.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Render a single calm decision card: 28px headline, one plain-language explanation, up to three reason rows with quiet icons/strata, an optional uncertainty line, and one limitation notice. If a tiny diagram is helpful, use a “reason stack” of three thin strata, not a chart. Hard blocks can show a subtle fracture line; review/waiting states use amber edge treatment; limited states use a visible “limited view” sash.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- render `headline`, `attention_state`, `primary_issue_ref`, `visible_reasons`, `additional_reason_count`, `plain_explanation`, `uncertainty_statement`, `limitation_state`, `limitation_reason_codes`, `limitation_statement`, and `blocking_reason` from schema fields
- enforce `visible_reasons.length <= 3` and one promoted warning in default view; show additional count as disclosure, not extra first-view clutter
- map limitation states `NONE`, `NOT_REQUESTED`, `NOT_YET_MATERIALIZED`, `LIMITED`, `NOT_APPLICABLE` to distinct copy and anchors
- preserve `machine_reason_codes` in inspectable metadata/full-text path without exposing raw regulated detail
- never compute a new primary issue in the component; trust server-authored attention policy/dominance contract

Functional, architectural, and edge-case gaps to close:
- close the gap where decision summary could become a gate/status dashboard
- close the gap where limitation states collapse into generic empty copy
- close the gap where hidden or erased data is mistaken for not existing
- close the gap where local UI re-ranks or expands reasons beyond budget

Mandatory edge cases:
- hard block must collapse secondary detail and elevate one primary issue without extra peer surfaces
- `NOT_REQUESTED`, `NOT_YET_MATERIALIZED`, `LIMITED`, and `NOT_APPLICABLE` must render visibly different copy
- additional reasons must remain discoverable through disclosure/full-text ref
- uncertainty must be legible without weakening the primary next-action relationship

Playwright, accessibility, and validation requirements:
- unit-test presenter budgets and limitation copy for every enum
- Playwright-test attention states, hard block, limited/masked view, additional reason disclosure, keyboard traversal, and reduced motion
- assert default summary never renders more than three reasons
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- decision summary renders one dominant question with bounded reasons
- limitation and uncertainty are explicit, accessible, and copy-disciplined
- Playwright proves budgets, edge states, keyboard path, and reduced-motion parity
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
