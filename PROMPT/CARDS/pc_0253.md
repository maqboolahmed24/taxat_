---
id: pc_0253
slug: phase_05_parallel_wave_03_track_frontend_portal_001_Playwright_or_other_appropriate_tooling_create_client_portal_app_scaffold_navigation_and_shell_identity
mode: parallel
phase: "05"
wave: "03"
track: "frontend_portal"
task_number: "001"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_portal_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0253

## Task Summary

001 Playwright or other appropriate tooling create client portal app scaffold navigation and shell identity.

## Metadata

- ID: `pc_0253`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend portal`
- Task Number: `001`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_portal_001_Playwright_or_other_appropriate_tooling_create_client_portal_app_scaffold_navigation_and_shell_identity`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0252](./pc_0252.md) `phase_05_parallel_wave_03_track_frontend_low_noise_012_Playwright_or_other_appropriate_tooling_implement_operator_interaction_layer_quick_actions_and_stale_guard_affordances`
- Next: [pc_0254](./pc_0254.md) `phase_05_parallel_wave_03_track_frontend_portal_002_Playwright_or_other_appropriate_tooling_implement_portal_home_route_status_hero_task_queue_and_recent_activity`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_portal_001_Playwright_or_other_appropriate_tooling_create_client_portal_app_scaffold_navigation_and_shell_identity`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0252` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Create the client portal app scaffold, navigation frame, and shell identity for `CLIENT_PORTAL_SHELL`, grounded in `ClientPortalWorkspace` rather than generic customer-dashboard conventions.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The portal is a task-first client-safe shell with at most five top-level destinations and one dominant next step per route. It is not the operator shell with fields hidden, and it is not a marketing dashboard. The scaffold must enforce portal shell family, identity context, top-level navigation labels/badges, route context, language contract, customer-safe projection, cache partition, and continuity contracts from the start.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md` navigation contract, route architecture, shell continuity, read-model/API translation, minimum selectors, and Playwright validation minimum.
- `frontend_shell_and_interaction_law.md` client portal shell topology, shell-specific artifact specialization, accessibility, and constrained channel return rules.
- `cross_shell_design_token_and_interaction_layer_foundation_contract.md` portal interaction layer and shell-family token boundaries.
- `cache_isolation_and_secure_reuse_contract.md` reuse law and scope rules.
- `schemas/client_portal_workspace.schema.json`, `portal_interaction_layer.schema.json`, `portal_language_contract.schema.json`, `customer_safe_projection_contract.schema.json`, `cache_isolation_contract.schema.json`.

Deliverables to create or patch:
1. `apps/client-portal-web/src/App.tsx` or repository-approved portal app root
2. `apps/client-portal-web/src/routes/PortalShellRoute.tsx`
3. `apps/client-portal-web/src/routes/portal_routes.ts`
4. `apps/client-portal-web/src/components/PortalShell.tsx`
5. `apps/client-portal-web/src/components/PortalHeader.tsx`
6. `apps/client-portal-web/src/components/PortalNavigationTabs.tsx`
7. `packages/frontend-shell-core/src/portal/client_portal_workspace_contract.ts`
8. `tests/playwright/frontend/client_portal_shell_navigation_identity.spec.ts`
9. `docs/frontend/client_portal_shell_scaffold_navigation_identity.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Build a one-column premium service shell: max content width 1040–1120px, centered stone background, white task cards, teal accent as a 2px top rule/focus ring, and a compact Taxat wordmark with a monogram `T` only if branding is needed. Header shows client/business identity, acting-for/delegation context, period label, and one reassurance sentence.
- Navigation uses literal tabs `Home`, `Documents`, `Approvals`, optional `Onboarding`, and `Help`; never show Home/Help badges. On narrow screens, tabs collapse into a labelled segmented control or overflow menu without renaming routes or creating a sixth destination.

Execution requirements:
- create a portal app root that requires `ClientPortalWorkspace.shell_family = CLIENT_PORTAL_SHELL` and `PortalInteractionLayer.selector_profile = PORTAL_SEMANTIC_SELECTORS_V1`
- render navigation from `navigation_tabs[]`, preserving canonical labels, badge rules, active route, contextual route metadata, and omission of inactive onboarding
- bind identity header to `identity_context`; delegated sessions must show acting-for context in header and command confirmation surfaces
- wire route frame to `route_context`, `cross_device_continuity_contract`, `semantic_accessibility_contract`, `cache_isolation_contract`, and `customer_safe_projection`
- add typed contract error and safe blank-state surfaces for missing/invalid workspace payloads; do not fall back to generic dashboard shell

Functional, architectural, and edge-case gaps to close:
- close the gap where portal routes could be implemented as independent pages without shared shell identity
- close the gap where top-level navigation count/badge rules are enforced only by visual convention
- close the gap where customer-safe projection and cache partition are not checked before rendering
- close the gap where delegated identity is visible in header but not in actions/receipts

Mandatory edge cases:
- onboarding complete so tab is omitted without leaving dead route focus
- delegated acting-for session with route restore from notification
- workspace payload has stale/degraded posture before first paint
- route_context points at contextual detail but parent tab must remain active
- cache/access/masking drift during hydration
- narrow-screen nav and keyboard navigation

Playwright, accessibility, and validation requirements:
- Playwright-test shell landmarks, tab labels/badges, onboarding omission, delegated identity, contextual parent active tab, contract-error state, keyboard navigation, reduced motion, and cache/access drift invalidation
- unit-test route/tab derivation, portal shell-family validation, and nav badge discipline
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- client portal shell exists as a contract-bound app frame
- navigation and identity obey portal algorithm rules
- Playwright proves same-shell navigation, badges, focus, reduced motion, and customer-safe gating
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
