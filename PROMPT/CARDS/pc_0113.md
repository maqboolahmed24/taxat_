---
id: pc_0113
slug: phase_03_parallel_wave_01_track_backend_collection_005_implement_freeze_normalization_context_declared_exclusions_and_missing_source_tracking
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "005"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0113

## Task Summary

implement freeze normalization context declared exclusions and missing source tracking.

## Metadata

- ID: `pc_0113`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `005`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_005_implement_freeze_normalization_context_declared_exclusions_and_missing_source_tracking`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0112](./pc_0112.md) `phase_03_parallel_wave_01_track_backend_collection_004_implement_materialize_source_records_and_evidence_item_persistence_pipeline`
- Next: [pc_0114](./pc_0114.md) `phase_03_parallel_wave_01_track_backend_collection_006_implement_extract_candidate_facts_pipeline_with_schema_validated_outputs`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0110_to_0117.md](../shared_operating_contract_0110_to_0117.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_005_implement_freeze_normalization_context_declared_exclusions_and_missing_source_tracking`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0112` if they exist, especially `pc_0010`, `pc_0011`, `pc_0028`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0066`, `pc_0074`, `pc_0080`, `pc_0098`, `pc_0099`, `pc_0101`, `pc_0109`, `pc_0111`, and `pc_0112`.

Mission:
Implement the frozen `NormalizationContext` artifact and the explicit source-domain declaration layer for excluded, confirmed-empty, missing, and stale intake posture.
This task is where omission becomes governed machine state rather than implication.
It must freeze the rule/version basis used for normalization and it must persist explicit declaration artifacts that later `InputFreeze` assembly can reference without guessing whether a domain was excluded, truly empty, missing, or stale.

Source algorithm grounding you must honor:
- `modules.md`, especially `FREEZE_NORMALIZATION_CONTEXT(...)`, `DECLARED_EXCLUSIONS(...)`, `DECLARE_MISSING_SOURCES(...)`, and `DECLARE_STALE_SOURCES(...)`.
- `core_engine.md`, especially the construction of `normalization_context`, `DECLARED_EXCLUSIONS(...)`, `DECLARE_MISSING_SOURCES(...)`, `DECLARE_STALE_SOURCES(...)`, and the rule that required domains cannot disappear into implicit clean absence.
- `input_boundary_and_cutoff_contract.md`, especially explicit per-domain accounting.
- `late_data_policy_contract.md`, especially the requirement that undeclared omission cannot later be reinterpreted as mere late-data absence.
- `data_model.md`, especially `NormalizationContext` and the declaration refs persisted inside `InputFreeze`.
- Existing schemas including `normalization_context.schema.json` and `input_freeze.schema.json`.

Deliverables to create:
1. `packages/backend-collection/src/models/normalization_context.ts`.
2. `packages/backend-collection/src/repositories/normalization_context_repository.ts`.
3. `packages/backend-collection/src/models/source_domain_declaration.ts`.
4. `packages/backend-collection/src/repositories/source_domain_declaration_repository.ts`.
5. `packages/backend-collection/src/services/freeze_normalization_context.ts`.
6. `packages/backend-collection/src/services/build_transformation_version_set.ts`.
7. `packages/backend-collection/src/services/declare_exclusions.ts`.
8. `packages/backend-collection/src/services/declare_confirmed_empty_sources.ts`.
9. `packages/backend-collection/src/services/declare_missing_sources.ts`.
10. `packages/backend-collection/src/services/declare_stale_sources.ts`.
11. `packages/backend-collection/src/services/validate_source_domain_declarations.ts`.
12. `packages/backend-collection/src/schemas/source_domain_declaration.schema.json`.
13. `db/migrations/phase03_0113_normalization_context_and_source_domain_declarations.sql`.
14. `docs/collection/normalization_context_and_source_domain_declarations.md`.
15. `tests/unit/backend_collection/normalization_context_and_source_domain_declarations.spec.ts`.
16. `tests/integration/backend_collection/normalization_context_and_source_domain_declarations_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the canonical shape of a source-domain declaration artifact and whether one generic artifact with `declaration_kind` is preferable to four separate tables/types.
- Define how `NO_DATA_CONFIRMED_AT_CUTOFF` is recognized from successful empty fetch evidence without collapsing into `MISSING_AT_CUTOFF`.
- Define how `transformation_version_set[]` is derived from mapping, evidence, promotion, normalization, extractor, and connector-build lineage.
- Define the exact disjointness and uniqueness rules across exclusion, confirmed-empty, missing, and stale declarations.
- Define how declaration refs later map into `InputFreeze.exclusion_refs[]`, `no_data_confirmed_declarations[]`, `missing_source_declarations[]`, and `stale_source_declarations[]` without lossy translation.

Execution steps:
1. Implement the schema-backed `NormalizationContext` model and repository.
2. Implement `freeze_normalization_context.ts` so the frozen artifact captures rule refs, transformation-version set, deterministic ordering, and stable context hashing.
3. Introduce one schema-backed declaration artifact surface for domain posture and repositories for durable refs.
4. Implement `declare_exclusions.ts`, `declare_confirmed_empty_sources.ts`, `declare_missing_sources.ts`, and `declare_stale_sources.ts` so later input-freeze assembly can consume explicit refs instead of inferring omissions from absence.
5. Implement `validate_source_domain_declarations.ts` so declaration kinds remain disjoint, complete, and partition-aware.
6. Add tests for policy exclusion, successful empty domain, missing domain, stale domain, and ambiguous multi-source coverage.

Gap handling you must perform:
- Close the gap where the corpus names exclusion/missing/stale declarations but supplies no dedicated declaration schemas or artifact family.
- Close the gap where `InputFreeze` requires `no_data_confirmed_declarations[]` even though the module list does not yet name an explicit helper for them.
- Close the gap where `NormalizationContext.transformation_version_set[]` exists in schema but is not concretely derived in prose.
- Close the gap where a source domain covered by multiple planned sources could otherwise receive contradictory declarations.

Mandatory edge cases:
- a domain cannot be both excluded and missing.
- a domain cannot be both confirmed empty and missing.
- a domain cannot be both confirmed empty and stale.
- successful empty responses with request/page audit evidence must become confirmed-empty posture, not missing posture.
- an omitted required domain with no exclusion and no successful empty confirmation must become missing or stale by explicit rule, never silent absence.
- stale posture must remain distinct from missing posture even if both block filing-capable work later.
- `NormalizationContext` must fail closed if any required rule ref or transformation version element is absent.

Backend, performance, and security requirements:
- Declarations must be strongly typed, auditable, and replay-safe.
- Keep declaration generation deterministic across the same source plan, collection boundary, and freshness inputs.
- Never log policy-sensitive reasoning in free-form only; retain machine reason codes and stable refs.
- Reuse migration, hashing, and contract-envelope utilities from earlier cards.

Validation rules:
- Unit-test normalization-context hashing and deterministic version-set ordering.
- Unit-test declaration disjointness, uniqueness, and confirmed-empty versus missing semantics.
- Integration-test persistence and lookup of declaration refs by manifest and source domain.
- Validate `NormalizationContext` and the new declaration schema against the imported contract bundle.
- Prove that no required domain can disappear from later input-freeze assembly without one explicit declaration or collected posture.

Definition of done:
- The repo has a durable `NormalizationContext` artifact and explicit declaration artifacts for source-domain omission posture.
- Later candidate, canonical, late-data, and input-freeze work can reason from explicit declarations instead of implicit absence.
- Rule-version lineage for normalization becomes replay-safe and auditable.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
