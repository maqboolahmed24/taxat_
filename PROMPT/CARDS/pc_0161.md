---
id: pc_0161
slug: phase_03_parallel_wave_01_track_backend_northbound_005_implement_manifest_experience_stream_sse_endpoint_with_resume_and_rebase_rules
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "005"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0161

## Task Summary

implement manifest experience stream sse endpoint with resume and rebase rules.

## Metadata

- ID: `pc_0161`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `005`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_005_implement_manifest_experience_stream_sse_endpoint_with_resume_and_rebase_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0160](./pc_0160.md) `phase_03_parallel_wave_01_track_backend_northbound_004_implement_experience_snapshot_endpoint_and_stability_contract_publication`
- Next: [pc_0162](./pc_0162.md) `phase_03_parallel_wave_01_track_backend_northbound_006_implement_governance_read_endpoints_for_overview_policy_principals_and_roles`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0158_to_0165.md](../shared_operating_contract_0158_to_0165.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_005_implement_manifest_experience_stream_sse_endpoint_with_resume_and_rebase_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0160` if they exist, especially `pc_0070` through `pc_0077`, `pc_0159`, and `pc_0160`.

Mission:
Implement `GET /v1/manifests/{manifest_id}/experience/stream?resume_token=...` as the reconnect-safe manifest SSE surface.
This endpoint must honor strict monotonic sequence application within one epoch, persist and enforce `ExperienceCursor` lifecycle, deliver catch-up before live events, and fail closed with typed rebase or access-rebind posture instead of fabricating missed history or reusing tokens across route/session/scope drift.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially sections 2, 5, 7, and 8.
- `data_model.md`, especially `ExperienceCursor`, `ExperienceStreamEvent`, `LowNoiseExperienceFrame`, and `ProblemEnvelope`.
- `state_machines.md`, especially `ExperienceCursor.cursor_state` / shared cursor-state rules.
- `low_noise_experience_contract.md`, especially manifest stream recovery and reconnect sections.
- `schemas/experience_cursor.schema.json`, `schemas/experience_stream_event.schema.json`, `schemas/stream_recovery_contract.schema.json`, and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 286-289.

Deliverables to create:
1. `packages/backend-northbound/src/models/experience_cursor.ts`
2. `packages/backend-northbound/src/repositories/experience_cursor_repository.ts`
3. `packages/backend-northbound/src/http/get_manifest_experience_stream_endpoint.ts`
4. `packages/backend-northbound/src/http/register_get_manifest_experience_stream_route.ts`
5. `packages/backend-northbound/src/streams/serialize_experience_stream_event.ts`
6. `packages/backend-northbound/src/services/validate_manifest_resume_token.ts`
7. `packages/backend-northbound/src/services/open_or_resume_experience_cursor.ts`
8. `packages/backend-northbound/src/services/load_manifest_catch_up_events.ts`
9. `packages/backend-northbound/src/services/mark_experience_cursor_rebased_or_revoked.ts`
10. `packages/backend-northbound/src/services/build_manifest_stream_problem_envelope.ts`
11. `docs/northbound/get_manifest_experience_stream.md`
12. `tests/unit/backend_northbound/experience_cursor_rules.spec.ts`
13. `tests/unit/backend_northbound/experience_stream_event_contract.spec.ts`
14. `tests/integration/backend_northbound/manifest_stream_resume_and_rebase.spec.ts`
15. `tests/integration/backend_northbound/manifest_stream_access_rebind.spec.ts`
16. `tests/playwright_api/northbound/manifest_stream_sse_contract.spec.ts`
17. `tests/playwright_browser/northbound/manifest_stream_eventsource.spec.ts`

Optional diagnostic explorer only if it materially accelerates verification:
18. `apps/admin-console-web/src/routes/debug/manifest-stream/[manifestId].tsx`

Required design decisions you must make explicit:
- Decide whether the endpoint requires a snapshot-issued `resume_token` for all connects or supports token-less bootstrap; default to snapshot-issued token only unless you can prove no semantic drift.
- Define the heartbeat cadence and framing without introducing non-deterministic sleeps into tests.
- Define how `ExperienceCursor` transitions among `LIVE`, `REBASED`, `CLOSED`, `REVOKED`, and `EXPIRED`, and when successor cursors are allocated.
- Define how you surface `terminal.bundle` relative to `experience.snapshot` / `experience.delta` and how you avoid duplicate semantic application on replay.
- Define whether `Last-Event-ID` is ignored, mirrored for observability only, or mapped to governed cursor state; it must not override the published resume-token contract.

Execution steps:
1. Require a lawful manifest-scoped resume token and bind it to the current manifest, route key, session, access binding, masking posture, schema compatibility, epoch, and sequence frontier.
2. Persist `ExperienceCursor` rows and re-validate them against schema when resumed.
3. Deliver catch-up events strictly before live events and keep sequence application monotonic and gap-free within one epoch.
4. Emit only schema-valid `ExperienceStreamEvent` objects of the allowed types.
5. On epoch advance, compaction, shell/route drift, or out-of-window cursor state, fail closed with `REBASE_REQUIRED` and the freshest lawful snapshot reference.
6. On session/access/masking/schema drift, fail closed with `ACCESS_REBIND_REQUIRED` rather than replaying cached state.
7. Add deterministic stream tests for duplicate delivery, catch-up ordering, rebase, revocation, expiry, and cross-session token rejection.

Gap handling you must perform:
- Close the gap where `last_ack_sequence` could exceed `last_published_sequence`.
- Close the gap where a rebase cursor points back to the same stale snapshot it is replacing.
- Close the gap where invalidation times imply impossible chronology relative to `last_seen_at` or `expires_at`.
- Close the gap where manifest and workspace tokens can be confused or replayed across the wrong stream scope.
- Close the gap where clients would need to infer rebase versus access-rebind from transport failure alone.

Mandatory edge cases:
- event application MUST remain idempotent by `(MANIFEST_EXPERIENCE, manifest_id, frame_epoch, experience_sequence)`
- `experience_sequence` MUST be strictly monotonic within one `(manifest_id, frame_epoch)`
- duplicate delivery is legal and MUST NOT duplicate semantic application
- catch-up MUST complete before live delivery is treated as current
- `REBASE_REQUIRED` MUST be emitted on compaction, epoch advance, or route/shell drift instead of fabricating missing deltas
- `ACCESS_REBIND_REQUIRED` MUST be emitted on session/access/masking/schema drift
- expired or revoked cursors remain terminal records; recovery allocates or returns a successor cursor rather than reopening the old one
- stream events MUST preserve the same shell family/object anchor lineage as the mounted snapshot

Playwright / contract-test requirements:
- test the HTTP/SSE contract with Playwright plus a deterministic EventSource/browser harness
- no fixed sleeps; drive waits from event arrival and explicit server signals
- capture traces or serialized event logs on failure
- prove resume, duplicate delivery, rebase, and access-rebind behavior end to end

Backend, performance, and security requirements:
- keep cursor writes, resume checks, and sequence frontier updates transactionally safe
- do not leak one session’s token into another principal/tenant/scope
- preserve no-store posture and avoid intermediary caching of SSE responses
- fail closed on malformed or schema-invalid stream events or cursor state

Validation rules:
- validate cursors against `experience_cursor.schema.json`
- validate every emitted event against `experience_stream_event.schema.json`
- validate rebase/access-rebind failures against `problem_envelope.schema.json`
- unit-test cursor state rules and event serialization
- integration-test resume, rebase, revocation, expiry, and wrong-scope rejection
- run Playwright API and browser harness tests for the shipped stream route

Definition of done:
- the repo exposes a governed manifest SSE endpoint with persisted cursor semantics
- resume, catch-up, rebase, and access-rebind flows are deterministic and typed
- clients cannot silently replay stale or cross-scope manifest deltas
- stream recovery remains aligned with the snapshot and grouped contract law
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
