---
id: pc_0254
slug: phase_05_parallel_wave_03_track_frontend_portal_002_Playwright_or_other_appropriate_tooling_implement_portal_home_route_status_hero_task_queue_and_recent_activity
mode: parallel
phase: "05"
wave: "03"
track: "frontend_portal"
task_number: "002"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_portal_002
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0254

## Task Summary

002 Playwright or other appropriate tooling implement portal home route status hero task queue and recent activity.

## Metadata

- ID: `pc_0254`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend portal`
- Task Number: `002`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_portal_002_Playwright_or_other_appropriate_tooling_implement_portal_home_route_status_hero_task_queue_and_recent_activity`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0253](./pc_0253.md) `phase_05_parallel_wave_03_track_frontend_portal_001_Playwright_or_other_appropriate_tooling_create_client_portal_app_scaffold_navigation_and_shell_identity`
- Next: [pc_0255](./pc_0255.md) `phase_05_parallel_wave_03_track_frontend_portal_003_Playwright_or_other_appropriate_tooling_implement_portal_documents_route_request_cards_and_literal_copy_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_portal_002_Playwright_or_other_appropriate_tooling_implement_portal_home_route_status_hero_task_queue_and_recent_activity`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0253` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the portal Home route with status hero, task queue, and recent activity, preserving one dominant client-safe next step.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The Home route is not a KPI dashboard; it is the client’s reassurance-and-next-task surface. `PORTAL_HEADER`, `STATUS_HERO`, `TASK_QUEUE`, and `RECENT_ACTIVITY` must render in the canonical order, with the hero CTA bound by `home_primary_task_ref` to exactly one queue item. Activity is bounded and customer-safe; hidden staff activity, internal lifecycle, or authority ambiguity must not change Home copy or badges except through published portal workspace truth.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md` Home route, simplified status visualization, shell continuity, content/copy rules, read-model/API translation, and Playwright validation minimum.
- `frontend_shell_and_interaction_law.md` dominant action, settlement-aware feedback, accessibility, focus, and motion law.
- `dominant_question_and_single_action_contract.md` one dominant question/one action requirements.
- `schemas/client_portal_workspace.schema.json`, `client_timeline_event.schema.json`, `portal_language_contract.schema.json`, `customer_safe_projection_contract.schema.json`, `portal_interaction_layer.schema.json`.

Deliverables to create or patch:
1. `apps/client-portal-web/src/routes/HomeRoute.tsx`
2. `apps/client-portal-web/src/components/home/PortalStatusHero.tsx`
3. `apps/client-portal-web/src/components/home/PortalTaskQueue.tsx`
4. `apps/client-portal-web/src/components/home/RecentActivityList.tsx`
5. `packages/frontend-shell-core/src/portal/home_route_presenter.ts`
6. `tests/unit/frontend-shell-core/portal_home_route_presenter.spec.ts`
7. `tests/playwright/frontend/client_portal_home_status_hero_task_queue.spec.ts`
8. `docs/frontend/client_portal_home_route.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Design Home as a quiet vertical service summary: header identity, large status hero card, task queue cards, recent activity strip. The hero should use a readable 28–36px headline, one primary teal CTA, due label near the CTA, and a short `what happens next` sentence.
- Task queue buckets are literal and ordered: `Do now`, `Coming up`, `Done`. Use small step/queue cards with an icon-like text glyph, not charts. Recent activity is capped at six rows with absolute timestamps available on focus and no internal event names.

Execution requirements:
- render `home_surface_order` exactly as `[PORTAL_HEADER, STATUS_HERO, TASK_QUEUE, RECENT_ACTIVITY]`
- bind the hero CTA to `home_primary_task_ref`; fail closed if hero and task queue advertise different primary actions
- group tasks into `Do now`, `Coming up`, and `Done`, omitting empty buckets and preserving literal bucket order
- render only customer-safe status vocabulary and activity events; authority states must remain pending/unknown/rejected/out-of-band aware
- surface draft resume, limitation, stale, or degraded posture as at most one promoted support region beneath the dominant task

Functional, architectural, and edge-case gaps to close:
- close the gap where Home can become a dashboard of badges, metrics, and unrelated cards
- close the gap where hero and queue compete with different CTAs
- close the gap where recent activity is derived from hidden staff activity or internal chronology
- close the gap where stale/degraded feedback appears as parallel warning stacks

Mandatory edge cases:
- no action needed right now
- ready-to-sign state with matching approvals task
- onboarding-required state with onboarding tab present
- draft resume competes with limitation notice
- new activity arrives during keyboard focus
- masked/withheld content creates explicit limitation rather than silent disappearance

Playwright, accessibility, and validation requirements:
- unit-test status hero/task binding, queue bucketing, activity ordering, and support-region priority
- Playwright-test one dominant CTA, bucket order, recent activity cap, delegated identity copy, stale/degraded inline notice, keyboard flow, responsive stack, and reduced-motion updates
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- Home answers current status and next task without competing actions
- activity and task counts are customer-safe and bounded
- Playwright proves hero/queue alignment, accessibility, and responsive continuity
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
