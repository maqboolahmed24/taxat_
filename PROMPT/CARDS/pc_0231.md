---
id: pc_0231
slug: phase_05_parallel_wave_03_track_frontend_shared_003_Playwright_or_other_appropriate_tooling_implement_interaction_layer_foundation_and_surface_registry
mode: parallel
phase: "05"
wave: "03"
track: "frontend_shared"
task_number: "003"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_shared_003
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0231

## Task Summary

003 Playwright or other appropriate tooling implement interaction layer foundation and surface registry.

## Metadata

- ID: `pc_0231`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend shared`
- Task Number: `003`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_shared_003_Playwright_or_other_appropriate_tooling_implement_interaction_layer_foundation_and_surface_registry`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0230](./pc_0230.md) `phase_05_parallel_wave_03_track_frontend_shared_002_Playwright_or_other_appropriate_tooling_implement_cross_shell_design_tokens_and_theme_runtime`
- Next: [pc_0232](./pc_0232.md) `phase_05_parallel_wave_03_track_frontend_shared_004_Playwright_or_other_appropriate_tooling_implement_semantic_selector_and_accessibility_anchor_catalog`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_shared_003_Playwright_or_other_appropriate_tooling_implement_interaction_layer_foundation_and_surface_registry`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0230` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement the shared interaction-layer foundation and surface registry that all shell families use to decide which surfaces can mount, promote, recover, notify, preview, and preserve continuity.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The source algorithm makes `foundation_contract` a grouped, mandatory payload below operator, portal, and governance interaction layers. The frontend must therefore maintain a registry of lawful shell surfaces and support-surface promotion rules that is keyed by the explicit foundation contract, not by React component hierarchy. The registry must prevent support-region multiplication and interaction-layer drift before later route cards add real data.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `cross_shell_design_token_and_interaction_layer_foundation_contract.md governing model and required family mappings`.
- `frontend_shell_and_interaction_law.md sections 2.2A, 3.2, 3.3, 4, 5, 7, and 8`.
- `low_noise_experience_contract.md Operator interaction layer and cognitive budget`.
- `customer_client_portal_experience_contract.md shell continuity/support budget`.
- `admin_governance_console_architecture.md governance interaction layer and non-modal support rules`.
- `schemas/interaction_layer_foundation_contract.schema.json, operator_interaction_layer.schema.json, portal_interaction_layer.schema.json, governance_interaction_layer.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/interaction/foundation_contract.ts`
2. `packages/frontend-shell-core/src/interaction/operator_interaction_layer.ts`
3. `packages/frontend-shell-core/src/interaction/portal_interaction_layer.ts`
4. `packages/frontend-shell-core/src/interaction/governance_interaction_layer.ts`
5. `packages/frontend-shell-core/src/interaction/surface_registry.ts`
6. `packages/frontend-shell-core/src/interaction/support_surface_budget.ts`
7. `packages/frontend-shell-core/src/interaction/interaction_contract_errors.ts`
8. `packages/shared-ui/src/interaction/SurfaceRegistryDiagram.tsx`
9. `tests/playwright/frontend/interaction_layer_surface_registry.spec.ts`
10. `tests/unit/frontend-shell-core/interaction_layer_foundation.spec.ts`
11. `docs/frontend/interaction_layer_foundation_and_surface_registry.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Build an internal “Surface Registry Diagram” that reads like a control-plane blueprint: left column shell family, middle column allowed primary surfaces, right column one promoted support region. Use thin connector lines, labelled budget chips, and compact monospace contract codes. The diagram should animate only when a support region is promoted or demoted; use 160ms opacity/height and disable movement under reduced motion.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- model `foundation_contract` as required on every interaction layer and expose builders for calm, portal, and governance with exact schema enum values
- create a surface registry that lists allowed surface codes, default reading order, promoted-support eligibility, modal eligibility, live-region role, artifact-preview policy, and return-focus behavior
- enforce `ONE_PROMOTED_SUPPORT_SURFACE_MAX` for default calm/portal/governance render paths and make compare/audit exceptions explicit modes
- make invalid surface combinations throw typed contract errors with stable reason codes usable by tests
- ensure registry decisions are pure and deterministic so Playwright fixtures and native mappings can reuse the same logical contract

Functional, architectural, and edge-case gaps to close:
- close the gap where surface promotion could be implemented locally in drawers or route components
- close the gap where modal-vs-non-modal behavior lacks a single registry
- close the gap where portal and governance consume shared tokens but not shared recovery/preview/notification posture
- close the gap where support surface budget is described in prose but not executable in the frontend

Mandatory edge cases:
- attempting to mount two default promoted support regions must fail in tests
- ordinary governance inspectors must remain non-modal unless an explicit high-risk ceremony marks a modal checkpoint
- portal help/recovery support must stack below primary instead of becoming a competing rail
- calm compare/audit support modes must be mutually exclusive unless a later contract explicitly changes that

Playwright, accessibility, and validation requirements:
- unit-test all legal and illegal surface combinations for each shell family
- Playwright-test the registry diagram with support promotion, demotion, keyboard traversal, and reduced-motion behavior
- assert semantic `data-testid` anchors are domain terms such as `promoted-support-region`, not layout names
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- foundation contract builders and surface registry encode all three shell families
- illegal surface promotion fails closed with typed reason codes
- Playwright proves the registry is navigable, semantic, reduced-motion safe, and visually contract-bound
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
- Implemented the frontend interaction-layer runtime in `packages/frontend-shell-core/src/interaction/`:
  - `foundation_contract.ts`
  - `operator_interaction_layer.ts`
  - `portal_interaction_layer.ts`
  - `governance_interaction_layer.ts`
  - `surface_registry.ts`
  - `support_surface_budget.ts`
  - `interaction_contract_errors.ts`
- Exported the interaction runtime from `packages/frontend-shell-core/src/index.ts`.
- Added the shared serializable diagram contract in `packages/shared-ui/src/interaction/SurfaceRegistryDiagram.tsx` and exported it from `packages/shared-ui/src/index.ts`.
- Patched the internal foundation atlas route/static fixture to include a Surface Registry Diagram with canonical primary surfaces, one promoted support slot, keyboard promotion/demotion, reduced-motion parity, and semantic `data-testid` anchors.
- Added unit coverage in `tests/unit/frontend-shell-core/interaction_layer_foundation.spec.ts` for:
  - schema-valid foundation contracts and interaction layers for all three shell families
  - deterministic default surface plans
  - two default promoted support regions failing closed
  - calm compare/audit conflict failing closed
  - portal support stacking below primary
  - governance modal checkpoints requiring an explicit high-risk reference
  - cross-family foundation drift failing before registry decisions are returned
- Added Playwright coverage in `tests/playwright/frontend/interaction_layer_surface_registry.spec.ts` for:
  - diagram rendering across all shell families
  - support promotion/demotion
  - keyboard traversal and focus restoration
  - reduced-motion behavior without spatial movement
  - semantic test anchors such as `promoted-support-region` and absence of layout-named test IDs
- Added implementation documentation in `docs/frontend/interaction_layer_foundation_and_surface_registry.md`.
- Assumption recorded: the static internal atlas mirrors the pure TypeScript registry because the current public atlas is served directly from browser-readable JS without a build step. The typed source of truth remains `packages/frontend-shell-core/src/interaction/surface_registry.ts`.
- `pnpm exec playwright test --config=playwright.config.ts --project=unit tests/unit/frontend-shell-core/interaction_layer_foundation.spec.ts` passed: 5 tests.
- `pnpm exec playwright test --config=playwright.config.ts --project=browser tests/playwright/frontend/interaction_layer_surface_registry.spec.ts` passed: 4 tests.
- `pnpm exec playwright test --config=playwright.config.ts --project=browser tests/playwright/frontend/interaction_layer_surface_registry.spec.ts tests/playwright/frontend/cross_shell_theme_runtime.spec.ts tests/playwright/frontend/shared_workspace_scaffold.spec.ts tests/playwright/frontend/shared_workspace_route_contracts.spec.ts` passed: 25 tests.
- `PYTHONDONTWRITEBYTECODE=1 ./.venv/bin/python3 Algorithm/scripts/validate_contracts.py --self-test` passed.
- `PYTHONDONTWRITEBYTECODE=1 python3 Algorithm/tools/forensic_contract_guard.py` passed.
- `pnpm run validate-contracts` passed.
- Markdown link cross-check passed for 9 links in `docs/frontend/interaction_layer_foundation_and_surface_registry.md`.
- `git diff --check` passed for the touched prompt, frontend-shell-core, shared-ui, operator atlas, docs, tests, and TypeScript config paths.
- `pnpm exec tsc --noEmit -p tsconfig.quality.json` still fails only on pre-existing `packages/domain-kernel` strict optional issues and generated-model missing type names; the new frontend interaction files no longer appear in the TypeScript error output after literal narrowing.
