---
id: pc_0089
slug: phase_03_parallel_wave_01_track_backend_access_005_implement_authorize_module_with_scope_partition_and_masking_logic
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "005"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0089

## Task Summary

implement authorize module with scope partition and masking logic.

## Metadata

- ID: `pc_0089`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `005`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_005_implement_authorize_module_with_scope_partition_and_masking_logic`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0088](./pc_0088.md) `phase_03_parallel_wave_01_track_backend_access_004_implement_delegation_grant_authority_link_and_exceptional_authority_grant_repositories`
- Next: [pc_0090](./pc_0090.md) `phase_03_parallel_wave_01_track_backend_access_006_implement_validate_effective_scope_binding_and_access_scope_enforcement_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0086_to_0093.md](../shared_operating_contract_0086_to_0093.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_005_implement_authorize_module_with_scope_partition_and_masking_logic`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0088` if they exist, especially `pc_0010`, `pc_0017`, `pc_0022`, `pc_0023`, `pc_0038`, `pc_0040`, `pc_0050`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0067`, `pc_0068`, `pc_0074`, `pc_0075`, `pc_0076`, `pc_0083`, `pc_0085`, `pc_0086`, `pc_0087`, and `pc_0088`.

Mission:
Implement the core `AUTHORIZE(...)` module with exact scope, partition, masking, step-up, approval, denial, and governance-mutation semantics.
This is one of the most sensitive pieces of the system.
It must execute the corpus' tuple-evaluation model precisely and must not silently narrow live mutation requests, blur masking into fact-layer changes, or recompute layered authority ad hoc.

Source algorithm grounding you must honor:
- `modules.md`, especially the full `AUTHORIZE(...)` contract, tuple-level evaluation model, decision ordering, governance-mutation semantics, and output fields.
- `actor_and_authority_model.md`, especially `3.7 Resource classes`, `3.8 Action families`, `3.9 Policy decision model`, `3.10 Delegation rules`, `3.11 Non-delegable and step-up actions`, and `3.14 Invariants`.
- `data_model.md`, especially `AuthorizationDecision`, `PrincipalContext`, `AuthorityLayerBoundaryContract`, and `scope_execution_binding` expectations.
- `invariants_and_gates.md`, especially authority-facing operation invariants and the rule that partial execution for atomic mutation-capable actions is illegal.
- `northbound_api_and_session_contract.md`, especially the rule that decisioning happens before route composition and command acceptance.
- Existing schemas including `authorization_decision.schema.json`, `principal_context.schema.json`, and `authority_layer_boundary_contract.schema.json`.

Deliverables to create:
1. `packages/backend-access/src/services/authorize.ts`.
2. `packages/backend-access/src/services/authorization_tuple_evaluator.ts`.
3. `packages/backend-access/src/services/effective_scope_reducer.ts`.
4. `packages/backend-access/src/services/partition_scope_evaluator.ts`.
5. `packages/backend-access/src/services/masking_projection_policy.ts`.
6. `packages/backend-access/src/services/access_blocked_response.ts`.
7. `packages/backend-access/src/services/authorization_decision_persistence_service.ts`.
8. `config/access/authorization_reason_code_map.json`.
9. `docs/access/authorize_module.md`.
10. `tests/unit/backend_access/authorize_module.spec.ts`.
11. `tests/integration/backend_access/authorize_module_end_to_end.spec.ts`.

Required design decisions you must make explicit:
- Define the canonical tuple shape used during evaluation, including resource class, action family, requested scope token, partition refs, masking eligibility, tenant permission state, delegation requirements, authority-link requirements, exceptional-authority posture, and human-gate posture.
- Define how policy data from the access matrix created in `pc_0083` is consumed at runtime and frozen into snapshot refs.
- Define the exact precedence between `ALLOW`, `ALLOW_MASKED`, `REQUIRE_STEP_UP`, `REQUIRE_APPROVAL`, and `DENY`, preserving the corpus rule that `REQUIRE_STEP_UP` surfaces first when both step-up and approval remain outstanding.
- Define the rule for atomic mutation-capable requests so narrowing never creates a hidden partial-execution outcome.
- Define how masking rules are stored and propagated without contaminating compute- or authority-facing payload generation.

Execution steps:
1. Implement a tuple evaluator that computes at least the tuple-level buckets described by the corpus: direct allow, step-up path, approval path, and blocked.
2. Implement canonical reduction from candidate tuples to `effective_scope[]` and `effective_partition_scope_refs[]`, preserving subset semantics and deterministic ordering.
3. Integrate `PrincipalContext`, the authority-layer boundary resolver, delegation / authority-link repositories, and the baseline access matrix into one coherent decision engine.
4. Implement decision finalization that constructs a schema-valid `AuthorizationDecision`, including reason codes, required approvals, required authn level, masking rules, and governance-mutation hash fields when applicable.
5. Persist or durably mirror the frozen decision for later runtime-scope validation and audit.
6. Implement blocked-response helpers for pre-manifest exits so controllers do not improvise denial or requirement responses differently.
7. Document exact mappings from actor-model resource/action semantics to runtime evaluator branches.

Gap handling you must perform:
- Close the gap where scope reduction could be done informally downstream instead of inside one governed authorizer.
- Close the gap where masking could accidentally affect source computation or authority packets rather than just projections.
- Close the gap where missing delegation, stale authority-link state, or supporting-agent restrictions might be discovered too late in a connector flow.
- Close the gap where governance mutation decisions are emitted without preserving topology/simulation hash basis.
- Close the gap where different routes or handlers return incompatible blocked-response structures.

Mandatory edge cases:
- request asks for a live mutation-capable scope family and the only legal outcome is a narrowed subset; atomic request must fail closed rather than silently narrow
- decision has both approval and step-up obligations outstanding; final posture must surface `REQUIRE_STEP_UP` first while preserving pending approval lineage
- masking is active for a readable/exportable action but the underlying authority packet must stay unmasked
- authority-facing action lacks current `AuthorityLink` despite tenant permission and must deny or require the appropriate upstream state, never allow
- requested partition set exceeds both delegation scope and authority-link scope
- supporting-agent or limited-authority posture requests a full-authority action
- service principal requests a human-only or client-portal-only action family
- requested scope list contains duplicates, mixed ordering, or incompatible family tokens
- governance mutation is evaluated without the required simulation basis and must not surface bounded-safe or approval-gated posture loosely

Backend, performance, and security requirements:
- The authorizer must be a pure policy-and-fact evaluation layer with explicit dependencies and deterministic outputs.
- No network call to external authority should occur during pure authorization evaluation.
  Authorization consumes persisted facts and frozen snapshots; live authority operations come later.
- Keep hot-path lookups indexed and batch-friendly.
- Never branch downstream on raw requested scope once an `AuthorizationDecision` exists.
- No new product UI is required.
  If you add an internal debug route, it must visualize tuple evaluation and final decision as an internal-only evidence surface, with reduced motion, semantic selectors, and Playwright coverage.

Validation rules:
- Unit-test tuple evaluation, precedence ordering, scope reduction, partition reduction, masking posture, service-principal restrictions, supporting-agent restrictions, and blocked-response shaping.
- Integration-test end-to-end evaluation from persisted `ActorSession` / `PrincipalContext` / delegation artifacts into a frozen `AuthorizationDecision`.
- Validate every emitted decision against `authorization_decision.schema.json`.
- Prove that atomic mutation requests cannot degrade into hidden narrowed execution.
- Prove that identical frozen inputs always yield identical final decisions and identical ordered scope vectors.

Definition of done:
- The repo has one authoritative `AUTHORIZE(...)` implementation that matches the corpus semantics.
- Downstream routes can consume frozen decisions instead of rebuilding access logic.
- Scope, partition, masking, step-up, approval, and denial semantics are explicit, deterministic, and fail-closed.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
