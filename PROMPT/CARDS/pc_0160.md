---
id: pc_0160
slug: phase_03_parallel_wave_01_track_backend_northbound_004_implement_experience_snapshot_endpoint_and_stability_contract_publication
mode: parallel
phase: "03"
wave: "01"
track: "backend_northbound"
task_number: "004"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_northbound_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0160

## Task Summary

implement experience snapshot endpoint and stability contract publication.

## Metadata

- ID: `pc_0160`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend northbound`
- Task Number: `004`
- Source Slug: `phase_03_parallel_wave_01_track_backend_northbound_004_implement_experience_snapshot_endpoint_and_stability_contract_publication`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0159](./pc_0159.md) `phase_03_parallel_wave_01_track_backend_northbound_003_implement_decision_bundle_snapshot_endpoint_with_etag_support`
- Next: [pc_0161](./pc_0161.md) `phase_03_parallel_wave_01_track_backend_northbound_005_implement_manifest_experience_stream_sse_endpoint_with_resume_and_rebase_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0158_to_0165.md](../shared_operating_contract_0158_to_0165.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_northbound_004_implement_experience_snapshot_endpoint_and_stability_contract_publication`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0159` if they exist, especially `pc_0070` through `pc_0077`, `pc_0118` through `pc_0127`, `pc_0134` through `pc_0141`, and `pc_0159`.

Mission:
Implement `GET /v1/manifests/{manifest_id}/experience/snapshot` as the canonical low-noise manifest shell read surface.
This endpoint must return the latest materialized `LowNoiseExperienceFrame`, publish coherent `decision_bundle_hash`, `frame_epoch`, `shell_stability_token`, `last_published_sequence`, a fresh `resume_token`, one grouped `stability_contract`, and one grouped `stream_recovery_contract`, without mixing generations or letting the client infer route continuity locally.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially sections 1, 2, 5, 7, and 8.
- `data_model.md`, especially `LowNoiseExperienceFrame`, `ExperienceCursor`, and `ExperienceStreamEvent`.
- `low_noise_experience_contract.md`, especially the shell, recovery, and attention-policy sections.
- `frontend_shell_and_interaction_law.md`, especially shell ownership, same object same shell, freshness/recovery presentation, and automation anchors.
- `cross_device_continuity_and_restoration_contract.md` and `semantic_selector_and_accessibility_regression_pack_contract.md`.
- `schemas/low_noise_experience_frame.schema.json`, `schemas/route_stability_contract.schema.json`, `schemas/stream_recovery_contract.schema.json`, and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially items 286-289 and 405, 411-412.

Deliverables to create:
1. `packages/backend-northbound/src/http/get_experience_snapshot_endpoint.ts`
2. `packages/backend-northbound/src/http/register_get_experience_snapshot_route.ts`
3. `packages/backend-northbound/src/query/get_latest_low_noise_experience_frame.ts`
4. `packages/backend-northbound/src/services/build_manifest_route_stability_contract.ts`
5. `packages/backend-northbound/src/services/build_manifest_stream_recovery_contract.ts`
6. `packages/backend-northbound/src/services/issue_manifest_resume_token.ts`
7. `packages/backend-northbound/src/services/validate_low_noise_experience_publication.ts`
8. `packages/backend-northbound/src/services/build_experience_snapshot_not_ready_problem.ts`
9. `docs/northbound/get_experience_snapshot.md`
10. `tests/unit/backend_northbound/manifest_stability_contract.spec.ts`
11. `tests/unit/backend_northbound/manifest_stream_recovery_contract.spec.ts`
12. `tests/integration/backend_northbound/get_experience_snapshot_contract.spec.ts`
13. `tests/integration/backend_northbound/get_experience_snapshot_recovery_posture.spec.ts`
14. `tests/playwright_api/northbound/get_experience_snapshot_contract.spec.ts`

Optional diagnostic explorer only if it materially accelerates verification:
15. `apps/admin-console-web/src/routes/debug/manifest-experience/[manifestId].tsx`

Required design decisions you must make explicit:
- Define the typed endpoint posture when the manifest has no materialized frame yet: snapshot-not-ready problem, snapshot rebuild, or degraded read-only posture.
- Define the exact inputs to `publication_generation` and `guard_vector_hash` so all marker components move together when route-visible truth or mutation safety changes.
- Define how `resume_token` is minted, rotated, and bound to manifest, route key, session binding, access binding, masking posture, and schema compatibility.
- Define whether snapshot responses are cacheable; default to `no-store` unless you can prove route-safe scoped caching.
- Define how browser reload, native cold start, and rebase recovery reuse this snapshot without changing shell family or mounted object.

Execution steps:
1. Load the latest persisted `LowNoiseExperienceFrame` for the manifest from durable projection storage.
2. Re-validate the frame against `low_noise_experience_frame.schema.json` before response.
3. Build and publish one grouped `stability_contract` from the current decision-bundle hash, shell stability token, frame epoch, publication generation, and sequence frontier.
4. Build and publish one grouped `stream_recovery_contract` that binds route key, subject, session, access, masking, epoch, last published sequence, compaction floor, and rebase semantics.
5. Issue a fresh transport `resume_token` that is downstream of those grouped contracts rather than a free-floating authority.
6. Return the complete frame plus the grouped contracts and fresh token.
7. Add deterministic contract tests for coherent grouped markers, recovery posture, and not-ready publication gaps.

Gap handling you must perform:
- Close the gap where `LowNoiseExperienceFrame` can drift focus, active-detail routing, warning counts, or primary action linkage across summary, drawer, and attention policy.
- Close the gap where ranked drawer entry points drift away from the published attention policy.
- Close the gap where grouped stability and recovery contracts are absent and clients have to reconstruct route continuity from raw scalar fields.
- Close the gap where snapshot readers could mix generations of `decision_bundle_hash`, `shell_stability_token`, `frame_epoch`, and `last_published_sequence`.
- Close the gap where a raw `resume_token` is treated as the authority instead of the grouped contract.

Mandatory edge cases:
- `shell_route_key` MUST remain equal to `manifest_id`
- `resume_token` is transport-only and MUST NOT be the sole recovery basis
- `stability_contract.guard_vector_components` MUST align to the same current `decision_bundle_hash`, `shell_stability_token`, and `frame_epoch`
- `stream_recovery_contract` MUST preserve the same route key, subject ref, session/access/masking binding, publication generation, and sequence frontier visible in the frame
- non-`NONE` recovery posture MUST remain same-shell and MUST NOT remount a second shell family
- schema-invalid or mixed-generation frames MUST fail closed

Playwright / contract-test requirements:
- use Playwright APIRequestContext or equivalent deterministic HTTP contract tooling
- if you add a browser explorer, use semantic/user-facing locators and no fixed sleeps
- prove snapshot responses publish coherent grouped contracts and a fresh resume token
- prove stale or degraded posture remains inline and same-shell in any optional browser explorer

Backend, performance, and security requirements:
- do not synthesize route continuity from local client state
- keep resume-token issuance and grouped-contract publication deterministic and auditable
- fail closed on access-binding drift, masking drift, schema drift, or mixed-generation markers
- preserve exact truth-boundary separation between persisted frame content and observational projections

Validation rules:
- validate success responses against `low_noise_experience_frame.schema.json`
- validate any snapshot-not-ready failures against `problem_envelope.schema.json`
- unit-test grouped contract builders, token binding, and mixed-generation rejection
- integration-test steady, freshening, stale-review, degraded, and not-ready cases
- API-test the shipped route with Playwright

Definition of done:
- the repo exposes `GET /v1/manifests/{manifest_id}/experience/snapshot`
- snapshot responses publish coherent grouped stability and recovery contracts plus a fresh token
- browser/native/reconnect consumers can restore the same manifest shell without inventing continuity locally
- low-noise frame publication is deterministic, typed, and fail-closed
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
