---
id: pc_0102
slug: phase_03_parallel_wave_01_track_backend_manifest_006_implement_load_and_validate_prior_manifest_context_and_reuse_decision_modules
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "006"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0102

## Task Summary

implement load and validate prior manifest context and reuse decision modules.

## Metadata

- ID: `pc_0102`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `006`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_006_implement_load_and_validate_prior_manifest_context_and_reuse_decision_modules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0101](./pc_0101.md) `phase_03_parallel_wave_01_track_backend_manifest_005_implement_resolve_config_freeze_config_and_materialize_cfg_from_freeze_modules`
- Next: [pc_0103](./pc_0103.md) `phase_03_parallel_wave_01_track_backend_manifest_007_implement_begin_manifest_begin_child_manifest_and_manifest_start_claim_protocol`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0102_to_0109.md](../shared_operating_contract_0102_to_0109.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_006_implement_load_and_validate_prior_manifest_context_and_reuse_decision_modules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0101` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0064`, `pc_0066`, `pc_0067`, `pc_0074`, `pc_0076`, `pc_0077`, `pc_0080`, `pc_0084`, `pc_0086` through `pc_0093`, and `pc_0097` through `pc_0101`.

Mission:
Implement the prior-manifest context loader, sealed-reuse validator, continuation-eligibility guard, and branch-decision modules that decide whether a request should allocate a new manifest, reuse a sealed manifest, return an existing terminal bundle, or branch into replay / recovery / continuation child behavior.
This task establishes the exact request-time branch-selection semantics that later cards must reuse.
It must distinguish request-time outcome from persisted manifest continuation basis, fail closed on lineage or access-binding drift, and never allow a replay or recovery shortcut to bypass frozen-basis rules.

Source algorithm grounding you must honor:
- `core_engine.md`, especially the request analysis, prior-manifest lookup, and branch-selection flow in the early run pipeline.
- `modules.md`, especially `LOAD_AND_VALIDATE_PRIOR_MANIFEST_CONTEXT(...)`, `VALIDATE_REUSE_SEALED_CONTEXT(...)`, `DECIDE_MANIFEST_REUSE_STRATEGY(...)`, `CONTINUATION_ALLOWED(...)`, `CONTINUATION_REUSES_FROZEN_CONFIG(...)`, `CONTINUATION_REUSES_FROZEN_INPUT(...)`, and `LOAD_SEALED_RUN_CONTEXT(...)`.
- `manifest_branch_selection_contract.md`, especially the canonical branch action vocabulary, reason codes, request identity inputs, and candidate evaluation semantics.
- `manifest_lineage_explorer_and_reuse_decision_tracer_contract.md`, especially branch selection versus selected manifest continuation basis and exhaustive candidate evaluation requirements.
- `manifest_and_config_freeze_contract.md`, especially lineage mirrors, same-manifest reuse legality, exact replay / exact recovery inheritance rules, nightly-window semantics, and access-binding hash semantics.
- `replay_and_reproducibility_contract.md`, especially exact replay / recovery rules, sealed pre-start context reuse, and fail-closed historical basis loading.
- `state_machines.md`, especially same-request terminal retry behavior and active-lease recovery prohibitions.
- Existing schemas including `run_manifest.schema.json`, `manifest_lineage_trace.schema.json`, `manifest_branch_decision_contract.schema.json`, `config_freeze.schema.json`, `input_freeze.schema.json`, and `decision_bundle.schema.json`.

Deliverables to create:
1. `packages/backend-manifest/src/services/load_and_validate_prior_manifest_context.ts`.
2. `packages/backend-manifest/src/services/validate_reuse_sealed_context.ts`.
3. `packages/backend-manifest/src/services/decide_manifest_reuse_strategy.ts`.
4. `packages/backend-manifest/src/services/continuation_allowed.ts`.
5. `packages/backend-manifest/src/services/compute_request_identity_hash.ts`.
6. `packages/backend-manifest/src/services/prior_manifest_compatibility_validator.ts`.
7. `packages/backend-manifest/src/services/continuation_inheritance_mode_mapper.ts`.
8. `packages/backend-manifest/src/types/manifest_reuse_strategy.ts`.
9. `packages/backend-manifest/src/types/manifest_prior_context_status.ts`.
10. `docs/manifest/prior_manifest_context_and_reuse_decision.md`.
11. `tests/unit/backend_manifest/prior_manifest_context_and_reuse_decision.spec.ts`.
12. `tests/integration/backend_manifest/prior_manifest_context_and_strategy.spec.ts`.

Required design decisions you must make explicit:
- Define the canonical request-identity vector and hashing order.
  It must include the fields the branch-selection contract treats as semantically relevant, and it must remain distinct from later deterministic outcome hashing.
- Define the exact precedence order across `RETURN_EXISTING_BUNDLE`, `REUSE_SEALED_MANIFEST`, `REPLAY_CHILD`, `RECOVERY_CHILD`, `CONTINUATION_CHILD`, `NEW_REQUEST_CHILD`, and `NEW_MANIFEST`.
  Explain why that order preserves idempotency and avoids duplicate replay/recovery children.
- Define how same-request identity, compatible continuation, and incompatible drift differ.
  Tenant, client, period, mode, run kind, scope, nightly window, access binding, and lineage mirror consistency must be checked separately with typed reasons.
- Define how same-manifest sealed reuse is validated without mutating the reused manifest.
- Define how nightly same-window dedupe differs from later-window continuation.
- Define how candidate evaluation objects are produced so `ManifestLineageTrace` can later persist one exhaustive, auditable decision explanation.

Execution steps:
1. Materialize typed request-identity and branch-decision helper structures using generated contract bindings where available.
2. Implement `load_and_validate_prior_manifest_context.ts` so it loads the prior manifest, validates tenant/client/period/mode/lineage/access compatibility, computes request identity exactly once, and returns `ABSENT`, `VALID`, or `INVALID` with typed reason codes.
3. Implement `continuation_allowed.ts` so it enforces recovery legality, active-lease blocking, same-request terminal return semantics, and replay-specific constraints without side effects.
4. Implement `validate_reuse_sealed_context.ts` so it rejects any mutated pre-start sealed manifest, including hidden drift in outputs, submissions, decision bundle hash, deterministic outcome hash, replay attestation ref, or drift refs.
5. Implement `decide_manifest_reuse_strategy.ts` so the canonical action and typed reason code are selected deterministically and candidate evaluation metadata is assembled for later trace persistence.
6. Implement helpers that map request-time branch action to continuation inheritance modes without confusing the selected manifest's own continuation basis.
7. Add tests for absent prior manifest, invalid prior manifest, terminal same-request idempotent return, pre-start sealed reuse, replay rerun dedupe, stale-recovery eligibility, and active-lease rejection.

Gap handling you must perform:
- Close the gap where branch selection exists only as prose and schema vocabulary but not as executable, deterministic code.
- Close the gap where request-time branch outcome could be conflated with the selected manifest's own continuation basis.
- Close the gap where replay and recovery could accidentally recollect input or refresh frozen config through generic continuation code.
- Close the gap where lineage mirror drift or access-binding drift might be normalized away instead of failing closed.
- Close the gap where duplicate replay children could be allocated for the same exact replay rerun.

Mandatory edge cases:
- `manifest_id` absent returns `ABSENT` with no storage side effects.
- prior manifest exists but tenant, client, period, mode, run kind, or lineage mirror mismatches produce typed invalid outcomes.
- exact same-request terminal manifest returns `RETURN_EXISTING_BUNDLE` before any child allocation.
- exact same-request still-sealed manifest returns `REUSE_SEALED_MANIFEST` only if the manifest is truly pre-start and unmutated.
- exact same replay request against an already completed replay child returns the existing replay result instead of allocating a duplicate replay child.
- started-run recovery is allowed only when the claim state is stale / reclaim-required.
- `ACTIVE_LEASED` blocks recovery-child allocation.
- sealed reuse must fail closed on any existing `opened_at`, outputs, submissions, drift refs, `decision_bundle_hash`, `deterministic_outcome_hash`, or `replay_attestation_ref`.
- top-level lineage mirrors diverge from `continuation_set{...}` or `manifest_branch_decision{...}` and prior-manifest validation must reject rather than normalize.
- nightly-window change must produce a distinct reason path from generic request-identity drift.

Backend, performance, and security requirements:
- All branch-decision helpers must be deterministic and side-effect free.
- Load prior manifest once per decision path and cache the validated result in-request rather than repeatedly reloading mutable state.
- Never perform ambient live-config or live-authority reads here; rely only on request envelope plus persisted manifest truth.
- Typed failure reasons must be stable and log-safe; never leak secrets or raw credential material.
- Keep data access index-friendly for manifest-id, request-identity hash, attempt-lineage, and replay-of lookups that later cards will need.

Validation rules:
- Unit-test the request-identity hash and candidate action precedence order.
- Unit-test typed invalid reasons and sealed-reuse disqualifiers.
- Integration-test terminal same-request return, sealed pre-start reuse, replay rerun dedupe, and stale-versus-active recovery eligibility.
- Validate emitted branch-decision shapes against the imported schemas.
- Prove that branch selection is deterministic across identical inputs and that illegal paths fail closed without allocating children.

Definition of done:
- The repo has executable modules that can classify prior-manifest state and decide the correct request-time branch action.
- Later manifest tasks can build child allocation, trace persistence, pre-seal, and replay logic on one auditable reuse-decision foundation instead of controller-local branching.
- Exact replay, exact recovery, and same-request retry semantics are no longer ambiguous prose; they are enforceable code paths with typed reasons.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
