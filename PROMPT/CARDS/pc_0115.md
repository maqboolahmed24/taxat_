---
id: pc_0115
slug: phase_03_parallel_wave_01_track_backend_collection_007_implement_conflict_detection_and_conflict_set_persistence
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "007"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0115

## Task Summary

implement conflict detection and conflict set persistence.

## Metadata

- ID: `pc_0115`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `007`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_007_implement_conflict_detection_and_conflict_set_persistence`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0114](./pc_0114.md) `phase_03_parallel_wave_01_track_backend_collection_006_implement_extract_candidate_facts_pipeline_with_schema_validated_outputs`
- Next: [pc_0116](./pc_0116.md) `phase_03_parallel_wave_01_track_backend_collection_008_implement_canonical_fact_promotion_and_partition_isolation_pipeline`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0110_to_0117.md](../shared_operating_contract_0110_to_0117.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_007_implement_conflict_detection_and_conflict_set_persistence`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0114` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0076`, `pc_0077`, `pc_0080`, `pc_0113`, and `pc_0114`.

Mission:
Implement the conflict-detection pipeline and first-class `ConflictRecord` / `ConflictSet` persistence so unresolved inconsistencies become explicit machine state rather than hidden heuristics.
This task must preserve the conflict frontier used by later promotion and gate logic.
Cross-partition contamination must become explicit conflict posture instead of widened fact scope.

Source algorithm grounding you must honor:
- `modules.md`, especially `DETECT_CONFLICTS(...)`, `DETECT_CROSS_PARTITION_CONFLICTS(...)`, and `MERGE_CONFLICTS(...)`.
- `core_engine.md`, especially partition-conflict merge, cross-partition conflict handling, and the rule that candidate/canonical scope may not widen.
- `canonical_fact_promotion_and_partition_isolation_contract.md`, especially partition-isolation and promotion-blocking semantics.
- `data_model.md`, especially `ConflictRecord` and `ConflictSet`.
- `exact_gate_logic_and_decision_tables.md`, especially `DATA_QUALITY_GATE` consumption of frozen conflict frontier.
- Existing schemas including `conflict_record.schema.json`, `conflict_set.schema.json`, and `sample_conflict_set.json`.

Deliverables to create:
1. `packages/backend-collection/src/models/conflict_record.ts`.
2. `packages/backend-collection/src/models/conflict_set.ts`.
3. `packages/backend-collection/src/repositories/conflict_record_repository.ts`.
4. `packages/backend-collection/src/repositories/conflict_set_repository.ts`.
5. `packages/backend-collection/src/services/detect_conflicts.ts`.
6. `packages/backend-collection/src/services/detect_cross_partition_conflicts.ts`.
7. `packages/backend-collection/src/services/conflict_identity_hash.ts`.
8. `packages/backend-collection/src/services/conflict_frontier_projector.ts`.
9. `packages/backend-collection/src/services/build_conflict_set.ts`.
10. `packages/backend-collection/src/services/rebind_candidate_conflict_membership.ts`.
11. `docs/collection/conflict_detection_and_conflict_set_persistence.md`.
12. `tests/unit/backend_collection/conflict_detection.spec.ts`.
13. `tests/integration/backend_collection/conflict_set_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the canonical conflict identity preimage and dedupe rules.
- Define the mapping from conflict type and contradiction class to severity, blocking class, decisive targets, and resolution frontier.
- Define how cross-partition contamination is detected from candidate/source/evidence lineage and how it is surfaced without mutating fact scope.
- Define how candidate facts are rebound to `conflict_membership_refs[]` and updated readiness after conflict detection.
- Define how a conflict-specific set wrapper works now without undermining the later generic artifact-set-wrapping task.

Execution steps:
1. Implement schema-backed `ConflictRecord` and `ConflictSet` models plus repositories.
2. Implement `detect_conflicts.ts` for same-partition logical inconsistencies such as duplicates, amount/date/category mismatch, missing required fields, low-confidence extraction, out-of-period records, source precedence issues, and authority divergence where applicable.
3. Implement `detect_cross_partition_conflicts.ts` so cross-partition contamination is surfaced as explicit conflict posture.
4. Implement `conflict_frontier_projector.ts` and `build_conflict_set.ts` so `open_conflict_ids[]`, `blocking_conflict_ids[]`, counts, dominant blocking class, and `resolution_frontier` are deterministic and schema-coherent.
5. Implement `rebind_candidate_conflict_membership.ts` so candidate facts are updated with the actual conflict frontier produced here.
6. Add tests for duplicate candidates, decisive contradictions, non-blocking monitoring conflicts, blocking conflicts, cross-partition contamination, and schema-invalid single-fact or reason-less conflicts.

Gap handling you must perform:
- Close the gap where conflict semantics are present in prose but not as a deterministic detector matrix.
- Close the gap where cross-partition contamination could otherwise be silently normalized into wider candidate/canonical scope.
- Close the gap where the roadmap introduces generic artifact-set wrapping later; create a conflict-set implementation that is correct now yet refactor-friendly later.
- Close the gap where candidate facts created in the previous task require real conflict-frontier rebinding before promotion can be trusted.

Mandatory edge cases:
- `ConflictRecord.involved_fact_refs[]` must always have at least two refs.
- `reason_codes[]` must never be empty.
- decisive contradiction or authority divergence must produce at least one decisive target ref.
- `ConflictSet.open_conflict_ids[]`, `blocking_conflict_ids[]`, counts, and `resolution_frontier` must exactly mirror unresolved item posture.
- a conflict set with zero open conflicts must have `resolution_frontier = CLEAR` and null dominant blocking class.
- a conflict set with open conflicts but zero blocking conflicts must have `resolution_frontier = MONITORING_ONLY`.
- a conflict set with any blocking conflict must have `resolution_frontier = BLOCKING_PRESENT` and a non-null dominant blocking class.
- cross-partition inconsistencies must never be resolved by widening fact scope.

Backend, performance, and security requirements:
- Conflict detection must be deterministic and order-stable.
- Keep detectors pure where possible; persist only the resulting first-class records and frontier projection.
- Never let free-form reviewer comments substitute for machine reason codes or conflict types.
- Conflict detection should scale by partition and fact family without quadratic uncontrolled scans where indexing or bucketing can avoid it.

Validation rules:
- Unit-test detector outputs, severity/blocking mappings, and frontier projection rules.
- Unit-test conflict-set coherence invariants against the schema.
- Integration-test candidate rebinding and persisted conflict set round trips.
- Validate `ConflictRecord` and `ConflictSet` against their schemas.
- Prove that cross-partition contamination becomes explicit conflict posture and never mutated fact scope.

Definition of done:
- The repo can persist explicit `ConflictRecord` and `ConflictSet` artifacts with stable frontier semantics.
- Candidate readiness is now grounded in actual conflict posture instead of provisional placeholders.
- Later canonical promotion and gate evaluation can consume a frozen conflict frontier rather than recomputing raw inconsistencies ad hoc.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
