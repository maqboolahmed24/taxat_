---
id: pc_0156
slug: phase_03_parallel_wave_01_track_backend_workflow_012_implement_quantitative_queue_health_and_escalation_analytics_services
mode: parallel
phase: '03'
wave: '01'
track: backend_workflow
task_number: '012'
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_workflow_012_implement_quantitative_queue_health_and_escalation_analytics_services
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0156

## Task Summary

quantitative queue health and escalation analytics services.

## Metadata

- ID: `pc_0156`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend workflow`
- Task Number: `012`
- Source Slug: `phase_03_parallel_wave_01_track_backend_workflow_012_implement_quantitative_queue_health_and_escalation_analytics_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0155](./pc_0155.md) `phase_03_parallel_wave_01_track_backend_workflow_011_implement_cross_device_continuity_route_anchor_and_focus_restore_metadata_persistence`
- Next: [pc_0157](./pc_0157.md) `phase_03_parallel_wave_01_track_backend_northbound_001_implement_post_commands_endpoint_and_durable_api_command_receipt_store`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0150_to_0157.md](../shared_operating_contract_0150_to_0157.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_workflow_012_implement_quantitative_queue_health_and_escalation_analytics_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0155` if they exist, especially `pc_0146`, `pc_0151`, and `pc_0155`.

Mission:
Implement the frozen queue-health and escalation analytics layer that powers inbox posture, intervention recommendations, and replay-safe operational diagnostics.
This card must turn the documented formulas into executable deterministic services so queue-health state is not guessed from backlog size, badge counts, or local operator perception.

Source algorithm grounding you must honor:
- `compute_parity_and_trust_formulas.md`, especially the queue-health and collaboration-priority sections.
- `collaboration_workspace_contract.md`, especially queue-health contract usage inside `WorkInboxSnapshot`.
- `data_model.md`, especially queue-health fields on inbox rows and snapshots.
- `schemas/work_queue_health_contract.schema.json` and `schemas/collaboration_routing_contract.schema.json`.
- `observability_and_audit_contract.md` where analytics evidence or basis hashes are persisted.

Deliverables to create:
1. `packages/backend-workflow/src/analytics/compute_work_queue_health.ts`
2. `packages/backend-workflow/src/analytics/compute_queue_pressure.ts`
3. `packages/backend-workflow/src/analytics/derive_queue_intervention_recommendation.ts`
4. `packages/backend-workflow/src/analytics/build_work_queue_health_contract.ts`
5. `packages/backend-workflow/src/analytics/query_queue_health_inputs.ts`
6. `packages/backend-workflow/src/analytics/query_queue_health_snapshot.ts`
7. `packages/backend-workflow/src/services/publish_queue_health_posture.ts`
8. `docs/workflow/queue_health_and_escalation_analytics.md`
9. `tests/unit/backend_workflow/work_queue_health_formula.spec.ts`
10. `tests/unit/backend_workflow/work_queue_health_contract.spec.ts`
11. `tests/integration/backend_workflow/queue_health_publication_flow.spec.ts`
12. `tests/playwright/admin_console/queue_health_preview.spec.ts`

Optional verification harness if a read-only surface helps validate the numbers:
13. `apps/admin-console-web/src/routes/debug/queue-health-preview.tsx`
14. `apps/shared-ui/src/components/debug/QueueHealthPreview.tsx`

Required design decisions you must make explicit:
- Define the frozen rolling window and input collection rules for `arrival_rate_q`, `service_rate_q`, `staffed_parallelism_q`, `backlog_age_p90_hours_q`, `stale_view_rejection_rate_q`, and `reassignment_churn_q`.
- Define the exact threshold mapping from numeric scores to `queue_health_state` and `intervention_recommendation_state`.
- Define how `basis_hash` and `routing_profile_hash` are computed and mirrored into the contract.
- Define how queue-health analytics feed both top-level `WorkInboxSnapshot.queue_health_contract` and row-level routing / pressure mirrors.
- Define whether analytics are computed on demand, scheduled, or both, and how replay uses the same frozen basis.

Execution steps:
1. Implement the formula engine exactly as written, including Erlang C handling, clamp/round behavior, and `queue_health_score_q` derivation.
2. Implement deterministic input queries and frozen-basis hashing.
3. Implement the persisted `work_queue_health_contract` builder and publication service.
4. Wire the contract into inbox/projector code paths without recomputing inside UI or transport handlers.
5. If useful for verification, add a read-only internal preview surface that renders the score, pressure band, recommendation, and reason codes.
6. Use Playwright to verify that the preview or route reflects the persisted contract rather than ad hoc UI math.

Gap handling you must perform:
- Close the gap where queue-health posture exists only in formulas/prose and not in executable services.
- Close the gap where queue order or intervention recommendation could drift from the basis hash that justified them.
- Close the gap where backlog counts alone masquerade as queue health.
- Close the gap where reassignments, stale-view rejection, and service-rate collapse are invisible to the work-inbox posture.

Mandatory edge cases:
- `staffed_parallelism_q = 0` or `service_rate_q = 0` MUST force zero-signal health and explicit degraded/saturated posture
- `ρ_q >= 1` MUST force `P_wait_q = 1` and `expected_wait_hours_q = +∞` semantics without numeric instability
- high `stale_view_rejection_rate_q` MUST depress health even when raw backlog is modest
- high `reassignment_churn_q` MUST depress health and may drive `intervention_recommendation_state = REBALANCE`
- `queue_health_score_q < queue_health_floor` MUST emit degraded posture and a matching reason code family
- published contract values MUST remain exactly mirrored into the inbox / routing contracts that consume them

Optional preview UI specification if you create it:
- render one compact queue-health band, one small pressure strip, one recommendation card, and one ordered reason table
- no gauges, no donuts, no heatmaps
- keep the surface internal-only and read-only
- selectors:
  - `queue-health-preview`
  - `queue-health-band`
  - `queue-pressure-strip`
  - `queue-intervention-card`
  - `queue-reason-table`

Playwright requirements if a preview exists:
- use Playwright during implementation and verification
- semantic locators only
- no fixed sleeps
- capture traces on retry / failure
- prove that the surface mirrors the persisted contract and not locally recomputed values

Backend, performance, and security requirements:
- fail closed on invalid numeric inputs, basis-hash mismatch, or schema mismatch
- preserve deterministic rounding and ordering across replay and scheduled recomputation
- keep heavy aggregation bounded and index-friendly; do not scan whole history per request once basis artifacts can be persisted
- do not let a preview or UI become the source of truth for queue health

Validation rules:
- validate every published work-queue-health contract against schema
- unit-test the formula math, rounding, threshold mapping, and recommendation state logic
- integration-test publication into inbox consumers and replay-safe rebuilds from frozen inputs
- Playwright-test any preview surface if created

Definition of done:
- the repo contains executable queue-health and escalation analytics services backed by the documented formulas
- inbox consumers can now rely on one persisted queue-health contract instead of local heuristics
- intervention recommendations and pressure posture remain deterministic and replay-safe
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
