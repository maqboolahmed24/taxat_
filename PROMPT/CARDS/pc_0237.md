---
id: pc_0237
slug: phase_05_parallel_wave_03_track_frontend_shared_009_Playwright_or_other_appropriate_tooling_implement_auth_step_up_and_session_state_integration
mode: parallel
phase: "05"
wave: "03"
track: "frontend_shared"
task_number: "009"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_shared_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0237

## Task Summary

009 Playwright or other appropriate tooling implement auth step up and session state integration.

## Metadata

- ID: `pc_0237`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend shared`
- Task Number: `009`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_shared_009_Playwright_or_other_appropriate_tooling_implement_auth_step_up_and_session_state_integration`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0236](./pc_0236.md) `phase_05_parallel_wave_03_track_frontend_shared_008_Playwright_or_other_appropriate_tooling_implement_artifact_preview_export_print_and_browser_handoff_components`
- Next: [pc_0238](./pc_0238.md) `phase_05_parallel_wave_03_track_frontend_shared_010_Playwright_or_other_appropriate_tooling_implement_cache_isolation_storage_partition_and_session_scoped_persistence`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0230_to_0249.md](../shared_operating_contract_0230_to_0249.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_shared_009_Playwright_or_other_appropriate_tooling_implement_auth_step_up_and_session_state_integration`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0236` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement auth step-up and session-state integration so provider-owned identity checkpoints are governed handoffs that return to the same shell, object context, focus anchor, and mutation basis.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The algorithm treats external authority and identity steps as temporary governed handoffs, not independent shells. Commands that require step-up must include fresh proof and exact stale guards. Session/access/masking drift must rebind or fail closed. The frontend must integrate session state, step-up prompts, and return anchors without creating a detached auth experience.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `frontend_shell_and_interaction_law.md sections 1.3, 2.3, 2.4, 8.2`.
- `northbound_api_and_session_contract.md session/browser/native rules and command envelope step-up requirements`.
- `actor_and_authority_model.md actor/session/authority concepts`.
- `authority_interaction_protocol.md boundary, binding, token/client rules`.
- `cache_isolation_and_secure_reuse_contract.md session/access/masking drift`.
- `schemas/actor_session.schema.json, authorization_decision.schema.json, command_envelope.schema.json, cache_isolation_contract.schema.json, focus_restoration_contract.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/auth/session_state.ts`
2. `packages/frontend-shell-core/src/auth/step_up_controller.ts`
3. `packages/frontend-shell-core/src/auth/return_anchor_binding.ts`
4. `packages/shared-ui/src/auth/StepUpInlineCheckpoint.tsx`
5. `packages/shared-ui/src/auth/SessionStateRibbon.tsx`
6. `packages/playwright-kit/src/fixtures/auth_step_up_fixture.ts`
7. `tests/unit/frontend-shell-core/auth_step_up_controller.spec.ts`
8. `tests/playwright/frontend/auth_step_up_session_state.spec.ts`
9. `docs/frontend/auth_step_up_and_session_state_integration.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, or artifact current/history posture.
- Render step-up as an inline checkpoint card inside the owning action/support surface: identity requirement, why now, exact return target, and one primary “Continue verification” control. Use a compact session ribbon for internal/operator surfaces: principal class, session freshness, access binding state. Do not create a full-screen auth wizard or decorative lock splash.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.

Execution requirements:
- create session state model that distinguishes authenticated, expiring, step-up-required, handoff-in-progress, returned, cancelled, timed-out, access-rebind-required, and revoked
- bind every step-up handoff to route identity, canonical object ref, focus anchor, stale guard snapshot, and command intent where applicable
- on return, verify session binding/access/masking/route stability before restoring action state; otherwise show inline access rebind or stale review
- prevent step-up proof from being reused across mismatched command targets or stale guard families
- keep provider-owned pages outside product shell but persist same-shell return context only as opaque/safe refs

Functional, architectural, and edge-case gaps to close:
- close the gap where auth becomes a separate product shell
- close the gap where step-up return loses focus, route, or action basis
- close the gap where stale session proof can be attached to a different command intent
- close the gap where session revocation leaves cached privileged surfaces mounted

Mandatory edge cases:
- cancelled or timed-out step-up must restore shell with action omitted or blocked, not auto-retry
- role/access narrowing after return must purge broader caches and show access-rebind posture
- provider error must map to typed inline problem copy
- refresh during handoff must resume or fail closed from the same return anchor binding

Playwright, accessibility, and validation requirements:
- unit-test return anchor binding, proof scope matching, and session drift handling
- Playwright-test step-up required, cancel, timeout, successful return, revoked session, and reduced-motion transitions
- assert focus returns to invoking action or lawful ancestor after handoff completes
- run validator self-test and forensic guard
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, and polling loops that can cause layout churn or duplicate announcements.

Definition of done:
- step-up is a governed handoff with exact return/focus/mutation basis
- session/access drift fails closed and purges unsafe state
- Playwright covers success, cancellation, timeout, revocation, and return focus
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229`, and tranche-specific frontend gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
