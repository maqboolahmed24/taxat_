---
id: pc_0105
slug: phase_03_parallel_wave_01_track_backend_manifest_009_implement_build_preseal_gate_evaluation_and_seal_manifest_services
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "009"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0105

## Task Summary

implement build preseal gate evaluation and seal manifest services.

## Metadata

- ID: `pc_0105`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `009`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_009_implement_build_preseal_gate_evaluation_and_seal_manifest_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0104](./pc_0104.md) `phase_03_parallel_wave_01_track_backend_manifest_008_implement_update_manifest_preseal_context_and_scope_execution_binding_services`
- Next: [pc_0106](./pc_0106.md) `phase_03_parallel_wave_01_track_backend_manifest_010_implement_release_candidate_identity_contract_and_release_candidate_hashing`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0102_to_0109.md](../shared_operating_contract_0102_to_0109.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_009_implement_build_preseal_gate_evaluation_and_seal_manifest_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0104` if they exist, especially `pc_0010`, `pc_0016`, `pc_0017`, `pc_0027`, `pc_0028`, `pc_0067`, `pc_0076`, `pc_0080`, `pc_0097`, `pc_0098`, `pc_0099`, `pc_0100`, `pc_0101`, `pc_0103`, and `pc_0104`.

Mission:
Implement the canonical pre-seal gate chain, the durable `preseal_gate_evaluation{...}` contract, and the seal transition that makes a manifest officially pre-start sealed.
This task must guarantee that seal readiness is based on one explicit gate tape, that blocked pre-start outcomes are durably recorded instead of silently abandoned, and that later gates can append but never rewrite or downgrade the canonical pre-seal prefix.

Source algorithm grounding you must honor:
- `core_engine.md`, especially the pre-seal phase, blocked-prestart behavior, reuse of persisted pre-seal tape, and seal transition.
- `modules.md`, especially `BUILD_PRESEAL_GATE_EVALUATION(...)`, `PERSIST_GATE_BATCH(...)`, `SEAL_MANIFEST(...)`, `UPDATE_MANIFEST_GATES(...)`, and related gate append behavior.
- `manifest_and_config_freeze_contract.md`, especially the seal contract, immutable-versus-append-only fields, canonical pre-seal gate ordering, and execution-basis prerequisites.
- `invariants_and_gates.md`, especially gate semantics and invariant classes relevant to pre-start sealing.
- `state_machines.md`, especially `ALLOCATED -> FROZEN -> SEALED`, blocked transitions, and fail-closed path distinctions.
- `replay_and_reproducibility_contract.md`, especially reuse of persisted pre-seal tapes for exact replay and same-manifest sealed reuse.
- Existing schemas including `preseal_gate_evaluation_contract.schema.json`, `gate_decision_record.schema.json`, `run_manifest.schema.json`, and related sample payloads.

Deliverables to create:
1. `packages/backend-manifest/src/services/build_preseal_gate_evaluation.ts`.
2. `packages/backend-manifest/src/services/persist_gate_batch.ts`.
3. `packages/backend-manifest/src/services/seal_manifest.ts`.
4. `packages/backend-manifest/src/services/update_manifest_gates.ts`.
5. `packages/backend-manifest/src/services/append_manifest_gates.ts`.
6. `packages/backend-manifest/src/services/preseal_gate_chain_validator.ts`.
7. `packages/backend-manifest/src/services/seal_readiness_validator.ts`.
8. `packages/backend-manifest/src/types/preseal_gate_result.ts`.
9. `docs/manifest/preseal_gate_chain_and_seal.md`.
10. `tests/unit/backend_manifest/preseal_gate_evaluation.spec.ts`.
11. `tests/integration/backend_manifest/seal_manifest_and_gate_append.spec.ts`.

Required design decisions you must make explicit:
- Define the durable storage pattern for the canonical pre-seal gate prefix versus later appended gates.
- Define how `completion_state`, `blocking_gate_codes[]`, `missing_prerequisite_refs[]`, `durability_boundary`, and `reuse_policy` are populated and validated.
- Define the exact seal readiness predicate, including what must already exist on the manifest before seal is legal.
- Define how same-manifest reuse and exact replay consume persisted pre-seal gate tape rather than recomputing it.
- Define how appended later gates preserve historical prefix truth and cannot downgrade a prior `HARD_BLOCK`.
- Define how pre-start blocked outcomes are recorded as durable manifest truth.

Execution steps:
1. Implement `preseal_gate_chain_validator.ts` so the canonical pre-seal order is fixed to the corpus sequence and any mismatch is a typed invariant failure.
2. Implement `build_preseal_gate_evaluation.ts` so the required gate chain yields one durable `preseal_gate_evaluation{...}` with explicit completion state and blocking reasons.
3. Implement `persist_gate_batch.ts` and `update_manifest_gates.ts` so the gate tape persists deterministically and can be reloaded without ordering drift.
4. Implement `seal_readiness_validator.ts` so seal only succeeds when required pre-seal gate tape, execution basis hash, config/input freeze refs, lineage mirrors, and access binding are present and consistent.
5. Implement `seal_manifest.ts` so the transition into `SEALED` is atomic and preserves immutable-versus-append-only field boundaries.
6. Implement `append_manifest_gates.ts` for later stages while enforcing that the pre-seal prefix stays historically frozen.
7. Add tests for ready-to-seal, blocked-prestart, pending prerequisites, persisted pre-seal tape reuse, and illegal attempts to rewrite the prefix.

Gap handling you must perform:
- Close the gap where pre-start gate readiness exists only in prose and schemas but not in one reusable service.
- Close the gap where manifests could skip durable blocked-prestart recording.
- Close the gap where later stages could rewrite or reorder the pre-seal gate prefix.
- Close the gap where seal might succeed without a fully frozen execution basis.
- Close the gap where exact replay or same-manifest reuse might recompute missing historical pre-seal context instead of failing closed.

Mandatory edge cases:
- pre-seal gate order must be exactly `MANIFEST_GATE`, `ARTIFACT_CONTRACT_GATE`, `INPUT_BOUNDARY_GATE`, `DATA_QUALITY_GATE`.
- `PENDING_PREREQUISITES` cannot transition to `SEALED`.
- `COMPLETE_BLOCKED_PRESTART` must durably record blocked-prestart state and must not transition to `SEALED`.
- seal must fail if `execution_basis_hash`, `ConfigFreeze`, `InputFreeze`, required lineage mirrors, or access-binding data are absent or inconsistent.
- same-manifest pre-start reuse must reload persisted gate tape rather than recomputing it.
- later appended gates may extend history but must not downgrade an earlier `HARD_BLOCK` or rewrite the canonical prefix.
- any mismatch between persisted gate tape and pre-seal evaluation hash / invariant class must fail closed.
- seal attempts against already-sealed or post-start manifests must reject deterministically.

Backend, performance, and security requirements:
- Gate evaluation and sealing must be deterministic and side-effect bounded.
- Keep gate batching append-only and audit-friendly.
- Never leak sensitive rule internals or secrets in gate error strings.
- Use atomic writes for seal transition and gate tape persistence where required by the contract.
- Avoid N+1 reload patterns when validating seal readiness; reuse the manifest aggregate and frozen context already loaded in the request pipeline.

Validation rules:
- Unit-test canonical gate ordering, completion-state mapping, readiness validation, and append-only gate behavior.
- Integration-test blocked-prestart persistence, successful seal transition, persisted pre-seal tape reuse, and attempted prefix rewrite rejection.
- Validate stored pre-seal gate evaluation and gate records against the imported schemas.
- Prove that a later gate append cannot mutate or weaken the historical pre-seal chain.
- Prove that illegal seal attempts fail without partial lifecycle mutation.

Definition of done:
- The repo has a canonical pre-seal gate service and a trustworthy seal transition.
- Blocked-prestart outcomes become durable, auditable manifest truth instead of hidden control flow.
- Later replay, reuse, and execution cards can rely on one persisted pre-seal tape and one immutable sealed boundary.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
