---
id: pc_0205
slug: phase_04_parallel_wave_02_track_backend_recovery_007_implement_cross_device_continuity_contract_and_shell_continuity_fuzz_harness_generation
mode: parallel
phase: "04"
wave: "02"
track: "backend_recovery"
task_number: "007"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_recovery_007
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0205

## Task Summary

007 implement cross device continuity contract and shell continuity fuzz harness generation.

## Metadata

- ID: `pc_0205`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend recovery`
- Task Number: `007`
- Source Slug: `phase_04_parallel_wave_02_track_backend_recovery_007_implement_cross_device_continuity_contract_and_shell_continuity_fuzz_harness_generation`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0204](./pc_0204.md) `phase_04_parallel_wave_02_track_backend_recovery_006_implement_native_cache_hydration_automation_pack_generation_and_compatibility_checks`
- Next: [pc_0206](./pc_0206.md) `phase_04_parallel_wave_02_track_backend_recovery_008_implement_nightly_batch_identity_batch_run_and_selection_disposition_persistence`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0198_to_0205.md](../shared_operating_contract_0198_to_0205.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_recovery_007_implement_cross_device_continuity_contract_and_shell_continuity_fuzz_harness_generation`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0204` if they exist, especially `pc_0166` through `pc_0181`, `pc_0189`, `pc_0203`, and `pc_0204`.

Mission:
Implement the shared `CrossDeviceContinuityContract` builder, emit that contract across every route-visible shell and native scene that survives refresh/reconnect/resize/restoration, and generate the deterministic `ShellContinuityFuzzHarness` that proves same-object same-shell continuity under perturbation.
This task is not allowed to leave continuity as a UI convention.
It must make route identity, object anchor, parent return context, dominant action posture, compatibility basis, and invalidation reasons explicit machine truth.

Source algorithm grounding you must honor:
- `cross_device_continuity_and_restoration_contract.md`, end to end.
- `shell_continuity_fuzzing_and_recovery_contract.md`, end to end.
- `frontend_shell_and_interaction_law.md`, especially cross-device continuity and shell stability clauses.
- `focus_restoration_and_return_target_harness_contract.md`, where return anchors and focus restoration interact with continuity; you do not need to implement the full focus harness here, but you MUST preserve the anchors it will later consume.
- `customer_client_portal_experience_contract.md`, `dominant_question_and_single_action_contract.md`, and `low_noise_experience_contract.md` for shell semantics.
- `schemas/cross_device_continuity_contract.schema.json`, `schemas/sample_cross_device_continuity_contract.json`, `schemas/shell_continuity_fuzz_harness.schema.json`, and `schemas/sample_shell_continuity_fuzz_harness.json`.
- `scripts/validate_contracts.py`, especially `cross_device_continuity_contract_notification_action_state_clear`, `cross_device_continuity_contract_visibility_only_basis_fields`, `cross_device_continuity_contract_secondary_window_native_rules`, `shell_continuity_fuzz_harness_valid`, `shell_continuity_fuzz_harness_missing_native_coverage`, `shell_continuity_fuzz_harness_route_drift_without_truth_change`, `shell_continuity_fuzz_harness_browser_native_perturbation_mix`, `shell_continuity_fuzz_harness_inline_recovery_reason_missing`, `shell_continuity_fuzz_harness_shrink_not_subset`, and `shell_continuity_fuzz_harness_secondary_return_anchor_lost`.
- `PATCH_RESOLUTION_INDEX.md`, especially any fixes around route identity drift, lost anchors, or shell-family swaps under recovery.
- `test_vectors.md`, especially the continuity and perturbation vectors across manifest, workspace, portal, governance, primary-scene, and secondary-window surfaces.

Deliverables to create:
1. `packages/backend-recovery/src/services/build_cross_device_continuity_contract.ts`
2. `packages/backend-recovery/src/services/classify_continuity_basis_class.ts`
3. `packages/backend-recovery/src/services/build_shell_continuity_fuzz_harness.ts`
4. `packages/backend-recovery/src/services/enumerate_shell_continuity_cases.ts`
5. `packages/backend-recovery/src/services/derive_continuity_invalidation_reasons.ts`
6. `packages/backend-recovery/src/tests/cross_device_continuity_contract.spec.ts`
7. `packages/backend-recovery/src/tests/shell_continuity_fuzz_harness.spec.ts`
8. `docs/recovery/cross_device_continuity_contract_and_shell_continuity_fuzz_harness.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/recovery/continuity-atlas.tsx`

You MUST patch every governed shell or native scene publisher that survives refresh/reconnect/resize/restoration so it emits the shared continuity packet instead of local restoration heuristics.

Required design decisions you must make explicitly:
- define the exact mapping from surface type to `continuity_scope` and `compatibility_basis_class`
- define the exact `allowed_embodiments[]` per scope and preserve the canonical ordering rules from the validator
- define how parent context, focus anchor, and return focus anchor are serialized for contextual routes and native secondary windows
- define the exact supported invalidation reason vocabulary and which reasons are lawful per scope
- define how the fuzz harness enumerates perturbations and shrink sequences deterministically
- define how `expected_outcome = PRESERVED` differs from `expected_outcome = INLINE_RECOVERY` while preserving the same route, object, dominant question, and meaning

Execution requirements:
- publish schema-valid `cross_device_continuity_contract` packets on every relevant route-visible shell and native scene
- preserve `allowed_embodiments[]` ordering so browser embodiments always lead when required
- preserve scope-specific rules such as:
  `WORK_ITEM_NOTIFICATION` clears dominant-action state and keeps `narrow_layout_policy = NOT_APPLICABLE`
  support-only secondary native windows require `secondary_window_policy = SUPPORT_ONLY_PARENT_BOUND`
  manifest and governance routes using `compatibility_basis_class = ROUTE_GUARD_ONLY` retain non-empty route guard hashes and clear non-applicable visibility/access fields
- generate a schema-valid `ShellContinuityFuzzHarness` with deterministic seeded enumeration
- cover browser and native continuity, at least one preserved case, at least one inline-recovery case, at least one `REBASE` case, at least one `RECONNECT` case, at least one resize/responsive-collapse case, at least one stream catch-up or frame-epoch-advance case, at least one native restore case, and at least one secondary-window parent-return-anchor case
- preserve shrink sequences as strict subsets of the injected perturbations

Gap handling you must close while implementing:
- close any gap where resize or restoration semantics are inferred by renderer layout code instead of a shared contract
- close any gap where contextual routes or support windows lose parent return anchors on reopen or close
- close any gap where browser and native perturbation coverage are split across ad hoc test suites instead of one canonical harness artifact
- close any gap where inline recovery can silently remount a different shell family or route identity without a real truth change
- close any gap where continuity packets omit invalidation reasons and leave clients to guess when to purge or rebase

Mandatory edge cases:
- when `truth_change_detected = false`, preserved or inline-recovery cases must keep route identity, object anchor, shell family, dominant question, dominant meaning, active context, focus anchor, and return focus anchor stable whenever the object remains lawful
- `INLINE_RECOVERY` must preserve same route or scene and same object while surfacing one typed recovery posture; no silent remounts or generic dashboard fallbacks
- native secondary windows must retain a non-null parent return anchor through restore and close
- shrink sequences must remain subsets of the injected perturbation set
- browser-only cases must not masquerade as native coverage and vice versa
- support-only secondary windows must not publish action posture that implies autonomous primary-scene truth

Playwright and contract-test requirements:
- add Playwright browser tests or `APIRequestContext` route checks for continuity packet emission on browser-governed surfaces
- if you add the optional atlas, add Playwright browser tests for continuity-scope filters, perturbation matrices, preserved-vs-inline recovery rendering, keyboard navigation, reduced-motion-safe expansion, and semantic locator coverage
- native continuity harness exports must stay deterministic and machine-readable for the native automation layer
- capture traces and serialized harness payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a premium continuity atlas: left scope/filter rail, center perturbation matrix, right anchor/meaning sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717` / `#667085`; continuity accent `#0F766E`; explainability accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- render continuity cases as ordered rows with perturbation chips, preserved invariant list, and typed inline-recovery reasons
- show parent/return anchors in monospace tokens; do not bury them inside expandable raw JSON
- if you show a diagram, show one continuity lattice or one perturbation rail; no charts or KPI panels
- animate only opacity / translate / height in `140ms`–`180ms`; reduced motion must preserve the same causal reading order

Backend, performance, and security requirements:
- keep the contract builder and harness generator deterministic and side-effect free
- do not log sensitive content; log route ids, object refs, anchor refs, perturbations, and typed invalidation reasons only
- make scope-specific basis rules explicit so browser and native clients cannot widen visibility or action posture during restoration
- fail closed when required anchors or invalidation reasons are missing

Validation rules:
- validate every emitted continuity packet and fuzz harness against schema
- add regression coverage for the named validator cases above
- keep this task limited to continuity packets and shell fuzz harness generation; do not absorb the full focus-return harness work beyond preserving the anchors it depends on

Definition of done:
- every governed browser/native shell that survives restoration now emits one authoritative continuity contract
- the repo can generate one deterministic shell continuity fuzz harness that proves same-object same-shell behavior under perturbation
- browser and native continuity semantics are now machine-checkable instead of heuristic
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
