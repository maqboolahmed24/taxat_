---
id: pc_0176
slug: phase_04_parallel_wave_02_track_backend_low_noise_008_implement_cross_shell_design_token_binding_and_interaction_semantics_projection
mode: parallel
phase: "04"
wave: "02"
track: "backend_low_noise"
task_number: "008"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_low_noise_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0176

## Task Summary

implement cross shell design token binding and interaction semantics projection.

## Metadata

- ID: `pc_0176`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend low noise`
- Task Number: `008`
- Source Slug: `phase_04_parallel_wave_02_track_backend_low_noise_008_implement_cross_shell_design_token_binding_and_interaction_semantics_projection`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0175](./pc_0175.md) `phase_04_parallel_wave_02_track_backend_low_noise_007_implement_operator_interaction_layer_and_interaction_layer_foundation_projectors`
- Next: [pc_0177](./pc_0177.md) `phase_04_parallel_wave_02_track_backend_low_noise_009_implement_focus_restoration_return_target_and_recovery_posture_projection_rules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0174_to_0181.md](../shared_operating_contract_0174_to_0181.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_low_noise_008_implement_cross_shell_design_token_binding_and_interaction_semantics_projection`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0175` if they exist, especially `pc_0175`, `pc_0169` through `pc_0174`, `pc_0163`, and the future-facing shell-law groundwork from `pc_0134` through `pc_0149`.

Mission:
Create the reusable cross-shell semantic registry that binds shell family to design-token semantics and interaction grammar without turning visual style into route-local code.
This task must turn the source contract’s shell-family mapping into one machine-authored projection layer that future portal and governance builders can consume safely.
It must especially unblock `CLIENT_PORTAL_SHELL` by publishing its shared interaction semantics now, while leaving full portal workspace composition to the dedicated portal cards.

Source algorithm grounding you must honor:
- `cross_shell_design_token_and_interaction_layer_foundation_contract.md`, entire document.
- `frontend_shell_and_interaction_law.md`, especially shell-family ownership, interaction-layer boundary, automation anchors, focus/motion law, and preview/history rules.
- `customer_client_portal_experience_contract.md`, especially the interaction-layer and route-order rules.
- `admin_governance_console_architecture.md`, especially any governance shell-family semantics you need for a future-facing seed registry.
- `data_model.md`, especially `InteractionLayerFoundationContract`, `PortalInteractionLayer`, and the shell-family read-model descriptions.
- `schemas/interaction_layer_foundation_contract.schema.json` and `schemas/portal_interaction_layer.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially drift items involving shell-family route behavior, selector profile, and portal continuity.

Deliverables to create:
1. `packages/backend-low-noise/src/semantics/shell_family_token_registry.ts`
2. `packages/backend-low-noise/src/services/project_foundation_contract_for_shell.ts`
3. `packages/backend-low-noise/src/services/project_portal_interaction_layer.ts`
4. `packages/backend-low-noise/src/services/project_governance_interaction_semantics_seed.ts`
5. `packages/backend-low-noise/src/services/export_shell_semantic_binding_manifest.ts`
6. `packages/backend-low-noise/src/services/validate_cross_shell_token_binding.ts`
7. `packages/backend-low-noise/src/tests/cross_shell_design_token_binding_projection.spec.ts`
8. `docs/design/cross_shell_design_token_binding_and_interaction_semantics.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/design/cross-shell-token-atlas.tsx`

Required design decisions you must make explicitly:
- define the canonical registry shape: whether shell-family semantics are authored as one immutable registry, typed factories, or a registry plus factory wrapper
- define how `PortalInteractionLayer` is emitted here without swallowing full `ClientPortalWorkspace` projection from later portal cards
- define the minimal governance semantic seed you will publish now so future governance work inherits the same shell-family grammar without copying constants prematurely
- define how the exported semantic binding manifest is consumed by browser and native renderers: typed enum payload only, or enum payload plus stable semantic-token aliases; never raw CSS-variable folklore
- define which fields are truly cross-shell and which remain shell-specific so future teams cannot accidentally give portal the calm-shell preview or recovery grammar

Execution requirements:
- centralize shell-family mappings in one place and make every projector consume that canonical registry
- publish `CLIENT_PORTAL_SHELL` foundation values exactly as the source contract freezes them: comfortable task density, primary stack spacing, inline support spacing, stacked-below-primary responsive compaction, portal selector profile, same-shell contextual return, inline review/recovery notice, current-primary-history-secondary, primary-context-with-stacked-support preview posture, and context-bound inline feedback
- emit a valid `PortalInteractionLayer` from that mapping, but keep full portal workspace composition out of scope for this card
- publish a governance semantic seed or descriptor only as the minimum reusable mapping needed for later cards; do not implement the governance snapshot itself here
- if you add the atlas, show shell-family semantic differences with one quiet comparison matrix and one embodiment ladder only

Gap handling you must close while implementing:
- the source corpus freezes shell-family semantics, but without a shared registry teams could still fork portal/mobile/governance behavior in route-local code
- portal interaction semantics are required by the portal workspace schema before the full portal builders land; publish the reusable projector now so later cards consume it instead of inventing it
- governance work is later in the roadmap; give it a typed semantic seed now so future cards do not backfill cross-shell defaults from memory

Mandatory edge cases:
- portal `secondary_window_policy` MUST remain `NOT_APPLICABLE`
- portal `selector_profile` MUST remain `PORTAL_SEMANTIC_SELECTORS_V1`
- portal `continuity_policy` MUST remain `SAME_SHELL_CONTEXTUAL_RETURN`
- portal `recovery_surface_policy` MUST remain `INLINE_REVIEW_OR_RECOVERY_NOTICE`
- governance seed values must not collapse into calm-shell or portal defaults simply because the route is not implemented yet
- exported bindings must be semantically named and stable; no visual-token-only keys and no route-local override tables

Playwright and contract-test requirements:
- unit-test the registry and projectors directly
- if you add the token atlas, test it with Playwright using semantic locators and reduced-motion coverage
- if portal route payloads start consuming the new projector in this card, add Playwright APIRequestContext assertions against those payloads

Backend, performance, and security requirements:
- treat shell-family mappings as immutable release-level semantics, not mutable runtime theme state
- do not store sensitive or audience-specific data in the exported semantic binding manifest; it must remain shell-family semantics only
- avoid circular dependencies between backend-low-noise, backend-portal, and future governance packages; the registry must be import-safe and boundary-clean
- fail closed on shell-family mismatch or invalid foundation/interaction-layer combinations

Validation rules:
- validate any emitted `InteractionLayerFoundationContract` or `PortalInteractionLayer` against schema
- add regression coverage proving that calm, portal, and governance family values do not drift into one another
- ensure the exported manifest remains deterministic across runs

Definition of done:
- the repo has one canonical cross-shell semantic registry
- portal interaction-layer semantics are reusable before full portal composition lands
- future portal and governance cards can consume shared shell-family semantics instead of copying token rules into route code
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
