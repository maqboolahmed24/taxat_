---
id: pc_0252
slug: phase_05_parallel_wave_03_track_frontend_low_noise_012_Playwright_or_other_appropriate_tooling_implement_operator_interaction_layer_quick_actions_and_stale_guard_affordances
mode: parallel
phase: "05"
wave: "03"
track: "frontend_low_noise"
task_number: "012"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_low_noise_012
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0252

## Task Summary

012 Playwright or other appropriate tooling implement operator interaction layer quick actions and stale guard affordances.

## Metadata

- ID: `pc_0252`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend low noise`
- Task Number: `012`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_low_noise_012_Playwright_or_other_appropriate_tooling_implement_operator_interaction_layer_quick_actions_and_stale_guard_affordances`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0251](./pc_0251.md) `phase_05_parallel_wave_03_track_frontend_low_noise_011_Playwright_or_other_appropriate_tooling_implement_same_object_route_continuity_and_back_return_behavior`
- Next: [pc_0253](./pc_0253.md) `phase_05_parallel_wave_03_track_frontend_portal_001_Playwright_or_other_appropriate_tooling_create_client_portal_app_scaffold_navigation_and_shell_identity`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_low_noise_012_Playwright_or_other_appropriate_tooling_implement_operator_interaction_layer_quick_actions_and_stale_guard_affordances`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0251` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement operator interaction-layer quick actions and stale-guard affordances inside the low-noise shell, preserving one dominant action and backend-authored action legality.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The low-noise action strip may show one dominant safe next move and subordinate non-competing controls, but it cannot decide legality locally. Operator quick actions must be projections of action authority, command truth, stale-view guards, recovery posture, and `OperatorInteractionLayer`; when the view is stale or degraded the UI must demote unsafe mutations to explicit review/rebase affordances rather than disabled clutter or optimistic commands.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `low_noise_experience_contract.md` ACTION_STRIP, operator interaction layer, settlement-state contract, cognitive budget, no-safe-action, and edge-case sections.
- `dominant_question_and_single_action_contract.md`, end to end.
- `frontend_shell_and_interaction_law.md` dominant-action hierarchy, stale/frozen/blocked/degraded presentation, and automation anchors.
- `northbound_api_and_session_contract.md` command envelope, receipt, problem response, concurrency, stale-view rules, and session rules.
- `schemas/action_strip_state.schema.json`, `operator_interaction_layer.schema.json`, `command_envelope.schema.json`, `api_command_receipt.schema.json`, `problem_envelope.schema.json`, `authorization_decision.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/actions/operator_action_legality.ts`
2. `packages/frontend-shell-core/src/actions/stale_guard_affordances.ts`
3. `packages/shared-ui/src/low-noise/OperatorActionStrip.tsx`
4. `packages/shared-ui/src/low-noise/StaleGuardNotice.tsx`
5. `packages/shared-ui/src/actions/DominantActionButton.tsx`
6. `apps/operator-web/src/routes/calm/action_strip_adapter.ts`
7. `tests/unit/frontend-shell-core/operator_action_legality.spec.ts`
8. `tests/playwright/frontend/operator_quick_actions_stale_guard.spec.ts`
9. `docs/frontend/operator_interaction_layer_quick_actions_and_stale_guards.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Design the action strip as a single calm capsule: one dominant button, up to two subordinate text/chip controls, and an inline stale/recovery message when needed. Use blue only for the lawful primary action. Stale/rebase states should use a quiet amber left rule plus explicit text such as `Review latest before continuing`; do not show a wall of disabled buttons.
- Provide a tiny action-basis disclosure in the detail drawer, not beside the primary CTA: basis hash, guard family, visible reason codes, command receipt state, and suggested investigation module.

Execution requirements:
- consume server-authored action strip state, action authority, stale guard, command receipt, and problem envelope data; never recompute legal action from visible labels
- build a command-launch adapter that attaches `command_id`, idempotency key, stale guard, route key, object anchor, and focus anchor
- render `RECEIPT_PENDING`, `STALE_REVIEW_REQUIRED`, `DEGRADED_READ_ONLY`, `RECOVERY_REQUIRED`, `NO_SAFE_ACTION`, blocked, and step-up-required postures distinctly
- omit illegal actions rather than disabling them; show blocked inventory only in an explicit explanation surface when useful
- ensure stale rejection routes focus to the inline problem notice and offers one safe review/rebase path

Functional, architectural, and edge-case gaps to close:
- close the gap where low-noise quick actions can drift from command/stale-view legality
- close the gap where receipt-pending local intent is confused with durable settlement
- close the gap where stale and degraded states show disabled clutter instead of one safe recovery action
- close the gap where action menus remain live after access/masking/session drift

Mandatory edge cases:
- primary action requires step-up or external authority handoff
- stale guard rejected after user clicks publish/submit
- receipt accepted but settlement pending or fails with typed problem
- no safe action with suggested detail module
- route rebased while action menu is open
- same idempotency key retried after network failure

Playwright, accessibility, and validation requirements:
- unit-test action priority, illegal-action omission, command envelope creation, stale guard binding, idempotency retry, and problem mapping
- Playwright-test dominant action click, stale rejection, receipt-pending posture, no-safe-action detail promotion, step-up posture, keyboard action menu, and reduced-motion status update
- assert there is never more than one mutation-capable primary action visible
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- operator quick actions are backend-legality-bound and stale-guarded
- unsafe/stale/degraded states fail closed with one clear recovery path
- Playwright proves command, receipt, focus, and no-safe-action behavior
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
