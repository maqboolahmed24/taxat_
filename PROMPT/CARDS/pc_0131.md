---
id: pc_0131
slug: phase_03_parallel_wave_01_track_backend_compute_011_implement_twin_state_snapshot_twin_view_and_mismatch_summary_builder
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "011"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_011
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0131

## Task Summary

implement twin state snapshot, twin view, and mismatch summary builder.

## Metadata

- ID: `pc_0131`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `011`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_011_implement_twin_state_snapshot_twin_view_and_mismatch_summary_builder`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0130](./pc_0130.md) `phase_03_parallel_wave_01_track_backend_compute_010_implement_enquiry_pack_assembly_and_retention_limited_explainability_support`
- Next: [pc_0132](./pc_0132.md) `phase_03_parallel_wave_01_track_backend_compute_012_implement_twin_reconciliation_state_interpretation_state_and_portfolio_summary_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0126_to_0133.md](../shared_operating_contract_0126_to_0133.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_011_implement_twin_state_snapshot_twin_view_and_mismatch_summary_builder`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0130` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0069`, `pc_0074`, `pc_0075`, `pc_0076`, `pc_0086` through `pc_0093`, `pc_0118`, `pc_0124`, `pc_0125`, `pc_0128`, `pc_0129`, and `pc_0130`.

Mission:
Implement the twin assembly foundation that produces authoritative `TwinStateSnapshot` artifacts for internal and authority lanes, computes deterministic `TwinDeltaArc` records and `TwinMismatchSummary`, derives `TwinReadinessState`, and persists the root `TwinView` and `TwinTimeline`.
This task must keep the internal lane and authority lane separate, preserve the frozen comparison-key profile, generate exactly one terminal delta class per comparison subject, summarize mismatches deterministically, and cap readiness correctly when baseline, freshness, or contradiction posture prevents safe action.

Source algorithm grounding you must honor:
- `twin_view_contract.md`, especially lane separation, comparison-key profile, delta precedence, mismatch ranking, readiness posture, stale / contradictory / missing baseline handling, and low-noise interpretation defaults.
- `modules.md`, especially `BUILD_TWIN_VIEW(...)`, `ASSEMBLE_TWIN_STATE_SNAPSHOT(...)`, `COMPUTE_TWIN_DELTA_SET(...)`, `SUMMARIZE_TWIN_MISMATCHES(...)`, and `DERIVE_TWIN_READINESS(...)`.
- `state_machines.md`, especially `TwinView.lifecycle_state` and `TwinStateSnapshot.assembly_state`.
- `data_model.md`, especially `TwinStateSnapshot`, `TwinView`, `TwinTimeline`, `TwinDeltaArc`, `TwinMismatchSummary`, and `TwinReadinessState`.
- `authority_truth_and_internal_projection_separation_contract.md`.
- `PATCH_RESOLUTION_INDEX.md`, especially fixes around mismatch counts, `TwinDeltaArc.priority_rank`, timeline windows, and authority contradiction posture.
- `test_vectors.md`, especially `TV-45`, `TV-46`, `TV-47`, `TV-48`, `TV-49A`, `TV-49C`, and `TV-50`.
- Existing schemas:
  `twin_state_snapshot.schema.json`,
  `twin_view.schema.json`,
  `twin_timeline.schema.json`,
  `twin_delta_arc.schema.json`,
  `twin_mismatch_summary.schema.json`,
  and `twin_readiness_state.schema.json`.

Deliverables to create:
1. `packages/backend-twin/src/models/twin_state_snapshot.ts`.
2. `packages/backend-twin/src/models/twin_view.ts`.
3. `packages/backend-twin/src/models/twin_timeline.ts`.
4. `packages/backend-twin/src/models/twin_delta_arc.ts`.
5. `packages/backend-twin/src/models/twin_mismatch_summary.ts`.
6. `packages/backend-twin/src/models/twin_readiness_state.ts`.
7. `packages/backend-twin/src/repositories/twin_state_snapshot_repository.ts`.
8. `packages/backend-twin/src/repositories/twin_view_repository.ts`.
9. `packages/backend-twin/src/repositories/twin_delta_arc_repository.ts`.
10. `packages/backend-twin/src/repositories/twin_mismatch_summary_repository.ts`.
11. `packages/backend-twin/src/repositories/twin_readiness_state_repository.ts`.
12. `packages/backend-twin/src/services/assemble_twin_state_snapshot.ts`.
13. `packages/backend-twin/src/services/compute_twin_delta_set.ts`.
14. `packages/backend-twin/src/services/summarize_twin_mismatches.ts`.
15. `packages/backend-twin/src/services/derive_twin_readiness.ts`.
16. `packages/backend-twin/src/services/build_twin_timeline.ts`.
17. `packages/backend-twin/src/services/build_twin_view.ts`.
18. `db/migrations/phase03_0131_twin_view_snapshot_and_mismatch_foundation.sql`.
19. `docs/twin/twin_state_snapshot_twin_view_and_mismatch_summary_builder.md`.
20. `tests/unit/backend_twin/twin_state_snapshot_contract.spec.ts`.
21. `tests/unit/backend_twin/twin_delta_and_mismatch_ranking.spec.ts`.
22. `tests/integration/backend_twin/twin_view_build_and_readiness.spec.ts`.

Required design decisions you must make explicit:
- Define the comparable-subject identity contract.
  `comparison_key` must depend only on the frozen subject identity-and-scope tuple, not on mutable value content.
- Define the comparison-key profile, delta precedence profile, and ranking formula as data, not renderer-local rules.
- Define the authority-lane admission rule:
  the authority snapshot may assemble only from authority-originated or reconciliation-proven artifacts.
- Define how `subject_key_collision_refs[]`, contradiction posture, and `non_comparable_subject_count` are produced.
- Define the mismatch-summary ranking source:
  `top_mismatch_refs[]` must come from persisted ranking, not from UI sorting.
- Define the readiness-ceiling mapping from trust/gate posture, delta posture, stale truth, baseline availability, and reconciliation requirement.

Execution steps:
1. Implement schema-backed models and repositories for twin snapshots, view, timeline, deltas, mismatch summary, and readiness state.
2. Implement `assemble_twin_state_snapshot.ts` so internal and authority lanes are built separately, share the same comparison-key profile, and explicitly preserve partial / limited / stale / contradictory posture.
3. Implement `compute_twin_delta_set.ts` so the comparison universe is the ordered union of normalized comparison keys and each key yields exactly one terminal delta class with persisted comparability state, reason, and `priority_rank`.
4. Implement `summarize_twin_mismatches.ts` so counts and `top_mismatch_refs[]` are derived from persisted ranked deltas rather than recomputed independently.
5. Implement `derive_twin_readiness.ts` so filing readiness alone cannot imply twin readiness; stale authority, contradictory lane posture, missing baseline, or unresolved reconciliation must cap safe action.
6. Implement `build_twin_timeline.ts` and `build_twin_view.ts` so the root twin artifact binds both snapshots, the delta set, timeline, mismatch summary, and readiness state deterministically.
7. Add tests for partial authority acknowledgement, stale authority snapshot, contradictory authority components, missing baseline, and deterministic mismatch ranking.

Gap handling you must perform:
- Close the gap where twin semantics are distributed across modules, contracts, and state machines before a reusable assembly service exists.
  Build one twin assembly path with frozen profiles and deterministic rankings.
- Close the gap where authority truth could be inferred from internal readiness or mirrored values.
  Keep lane admission and readiness caps authority-first and explicit.
- Close the gap where one comparison subject could produce multiple overlapping delta classes.
  Enforce exactly one terminal delta class per normalized comparison key.
- Close the gap where mismatch counts or top refs could drift between builders and surfaces.
  Persist one ranked delta set and summarize from it only.
- Close the gap where stale / contradictory / missing-baseline posture could still render safe mutation action.
  Cap readiness explicitly.

Mandatory edge cases:
- an authority snapshot may not reach `ASSEMBLED` from internal inference alone.
- subject-key collisions in the same lane must force `assembly_state = CONTRADICTORY`.
- contradictory or otherwise non-ready lane subjects must remain countable in `non_comparable_subject_count`.
- ordering must be driven by `priority_rank`, then `last_compared_at`, then `comparison_key`.
- one normalized comparison key must yield exactly one terminal delta class.
- `TwinView` must become `STALE` when either lane becomes stale, a new authority observation lands, late data changes the internal lane, or reconciliation materially changes delta meaning.
- readiness must be capped when authority truth is stale / contradictory / missing or reconciliation is still required.
- cross-source bridges must persist as `TwinDeltaArc` records rather than renderer-local comparison lines.

Backend, performance, and security requirements:
- Keep comparison and readiness logic deterministic and free of UI-only concepts.
- Avoid reloading whole evidence graphs when a smaller persisted twin input set suffices.
- Never log full mirrored subject payloads unnecessarily; log comparison keys, delta classes, and mismatch counts.
- Keep authority and internal payload handling partition-safe and scope-safe.

Optional internal verification surface (recommended because twin semantics are hard to validate without a read-only atlas):
- If you add a read-only twin explorer, place it behind an internal route such as `/internal/twin/view/:twinId`.
- The UI must be minimalist premium and clearly differentiated from generic dashboards:
  left lane rail for internal snapshot subjects,
  center “delta spine” for ordered mismatches and matched subjects,
  right lane rail for authority snapshot subjects,
  with a floating lower detail panel for readiness, freshness, and mismatch explanation.
- Layout:
  two `300px` mirrored lane columns,
  one `minmax(420px, 1fr)` center column,
  `24px` gutters,
  `18px` radius cards,
  sticky section headers.
- Use the shared palette, with contradiction markers only in `#C23616`, stale markers in `#B7791F`, and matched low-noise indicators in neutral ink / muted text rather than celebratory green.
- Add one restrained mirrored-lane diagram only: thin connecting strokes from internal subject rows to the center delta spine and then to authority rows.
- Motion must be extremely restrained: `160ms` focus shifts, `180ms` detail-panel reveal, full reduced-motion support.
- If this surface exists, implement and verify it with Playwright using semantic locators, keyboard traversal across the mirrored lanes, reduced-motion assertions, trace capture, and focus-retention checks.

Validation rules:
- Unit-test lane assembly rules, comparison-key derivation, single-terminal-delta enforcement, mismatch ranking, and readiness ceilings.
- Integration-test twin build from persisted internal and authority inputs, including stale / contradictory / missing baseline scenarios.
- Validate persisted artifacts against the relevant twin schemas.
- Re-run the contract validators after assembling the implementation bundle.

Definition of done:
- The repo can persist one authoritative `TwinView` with lane snapshots, deterministic deltas, mismatch summary, readiness state, and timeline.
- Later reconciliation, interpretation, portfolio, shell, and operator surfaces can consume persisted twin truth instead of recomputing mismatch logic on the fly.
- Authority truth remains separate from internal readiness throughout the twin stack.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
