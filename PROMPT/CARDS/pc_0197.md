---
id: pc_0197
slug: phase_04_parallel_wave_02_track_backend_governance_009_implement_manifest_lineage_explorer_and_reuse_decision_tracer_queries
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "009"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_009
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0197

## Task Summary

implement manifest lineage explorer and reuse decision tracer queries.

## Metadata

- ID: `pc_0197`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `009`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_009_implement_manifest_lineage_explorer_and_reuse_decision_tracer_queries`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0196](./pc_0196.md) `phase_04_parallel_wave_02_track_backend_governance_008_implement_policy_risk_and_blast_radius_modeling_projection_and_preview_confidence_rules`
- Next: [pc_0198](./pc_0198.md) `phase_04_parallel_wave_02_track_backend_governance_010_implement_gate_decision_explainability_projection_and_reason_code_compression`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0190_to_0197.md](../shared_operating_contract_0190_to_0197.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_009_implement_manifest_lineage_explorer_and_reuse_decision_tracer_queries`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0196` if they exist, especially `pc_0102` through `pc_0110`, `pc_0158` through `pc_0168`, and `pc_0194`.

Mission:
Implement the read-side explorer and query services for persisted `ManifestLineageTrace` so governance or operator tooling can explain manifest reuse, bundle return, same-manifest sealed reuse, replay children, recovery children, continuation children, and request-identity-change children from durable lineage traces instead of inference.
This is not a manifest-adjacency diff helper and not a best-effort visualization layer.
It must render from persisted `ManifestLineageTrace` plus explicit selected-manifest refs, preserve selected and rejected branch candidates, preserve typed disqualifier reasons, preserve mirror-consistency state, and preserve nightly predecessor context.

Source algorithm grounding you must honor:
- `manifest_lineage_explorer_and_reuse_decision_tracer_contract.md`, all sections.
- `manifest_branch_selection_contract.md`, especially the paired truth model and branch-selection law.
- `data_model.md`, especially `ManifestLineageTrace` and `RunManifest.manifest_lineage_trace_refs[]`.
- `modules.md`, especially `EMIT_MANIFEST_LINEAGE_TRACE(...)`.
- `northbound_api_and_session_contract.md` where manifest / audit reads and immutable refs shape the explorer query boundary.
- `audit_and_provenance.md`, especially audit/span linkage and forensic package coupling.
- `schemas/manifest_lineage_trace.schema.json`.
- `scripts/validate_contracts.py` and `tools/forensic_contract_guard.py`, especially the manifest-lineage tests and guards.
- `test_vectors.md`, especially TV-72A through TV-72G.

Deliverables to create:
1. `packages/backend-governance/src/projectors/build_manifest_lineage_explorer.ts`
2. `packages/backend-governance/src/queries/query_manifest_lineage_trace.ts`
3. `packages/backend-governance/src/services/build_manifest_reuse_decision_summary.ts`
4. `packages/backend-governance/src/services/assert_manifest_lineage_candidate_coverage.ts`
5. `packages/backend-governance/src/tests/manifest_lineage_explorer_projection.spec.ts`
6. `packages/backend-governance/src/tests/manifest_lineage_trace_query_scenarios.spec.ts`
7. `docs/governance/manifest_lineage_explorer_and_reuse_decision_tracer.md`
8. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/manifest-lineage-explorer-lab.tsx`

You MUST patch any existing lineage or reuse diagnostic surface so it consumes persisted traces instead of inferring branch meaning from adjacent manifests, timestamps, or original manifest continuation basis alone.

Required design decisions you must make explicitly:
- define the explorer query boundary: by `lineage_trace_id`, by selected manifest, and by request-identity / idempotency context when appropriate
- define how the explorer chooses which trace is primary when a manifest has multiple append-only `manifest_lineage_trace_refs[]`
- define how `mirror_consistency_state` and `mirror_sources[]` are surfaced and when the explorer fails closed rather than guessing a single truth source
- define how nightly predecessor context is rendered so same-window reuse and later-window continuation stay distinct
- define how audit/span refs are exposed for drill-in without forcing the explorer to reconstruct branch meaning from those refs
- define how the explorer keeps `selected_branch_action` distinct from the selected manifest’s own persisted `continuation_basis`

Execution requirements:
- build the explorer from persisted `ManifestLineageTrace` plus explicit selected-manifest refs only
- keep `candidate_evaluations[]` exhaustive over the canonical vocabulary:
  `NEW_MANIFEST`, `RETURN_EXISTING_BUNDLE`, `REUSE_SEALED_MANIFEST`, `REPLAY_CHILD`, `RECOVERY_CHILD`, `CONTINUATION_CHILD`, `NEW_REQUEST_CHILD`
- keep exactly one `SELECTED` candidate per trace and typed `disqualifier_reason_codes[]` for every `REJECTED` candidate
- keep `selected_branch_action` explicit even when the selected manifest’s persisted `continuation_basis` remains `NEW_MANIFEST`
- preserve prior-manifest id/hash/lifecycle where applicable, bundle hash for bundle return, and exact inheritance modes for replay / recovery children
- preserve nightly predecessor batch / manifest context and explicit nightly context reason when `run_kind = NIGHTLY`
- preserve audit / span refs as linked evidence, not as reconstructed lineage truth
- preserve mirror parity checks across top-level lineage mirrors, `continuation_set{...}`, `manifest_branch_decision{...}`, and `frozen_execution_binding{...}` where applicable; fail closed on divergence

Gap handling you must close while implementing:
- close any gap where lineage explorer meaning is inferred from adjacent manifests or timestamps instead of persisted traces
- close any gap where rejected candidates disappear or lose typed disqualifier reasons
- close any gap where bundle return or same-manifest sealed reuse rewrites manifest-local continuation truth instead of emitting request-time trace truth
- close any gap where nightly predecessor context can disappear on continuation paths
- close any gap where audit or span linkage exists without a durable `manifest_lineage_trace_ref`

Mandatory edge cases:
- `selected_branch_action = NEW_MANIFEST` must preserve root-manifest semantics and clear child / prior refs appropriately
- `RETURN_EXISTING_BUNDLE` must keep explicit returned decision bundle hash and terminal prior lifecycle
- `REUSE_SEALED_MANIFEST` must stay distinct from bundle return and child allocation
- `REPLAY_CHILD` must preserve exact replay inheritance modes and reject ordinary continuation candidates with typed reasons
- `RECOVERY_CHILD` must preserve exact recovery inheritance modes and prior lifecycle `IN_PROGRESS`
- nightly continuation traces must preserve predecessor batch / manifest context and the typed nightly context reason
- no trace may widen `effective_scope[]` outside `requested_scope[]`

Playwright and contract-test requirements:
- add direct tests for all canonical scenarios: new manifest, bundle return, sealed reuse, replay child, recovery child, continuation / new-request child, and nightly continuation
- add direct tests for exhaustive candidate coverage, one-selected-candidate rule, rejected-candidate reason requirement, mirror parity handling, and nightly predecessor context requirement
- if a northbound lineage explorer route is added or patched, add Playwright `APIRequestContext` tests for selected / rejected candidate rendering, nightly context, and immutable refs
- if you add the optional lab, add Playwright browser tests that verify selected-path rendering, rejected-candidate reasons, nightly predecessor markers, and semantic locators only
- capture serialized trace payloads and traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- make the lab a quiet forensic surface: top lineage summary ribbon, dominant branch lattice, one selected-path detail panel, one rejected-candidate ledger, and one subdued evidence sidecar
- max width `1440px`; palette `#F7F5F1 / #FFFFFF / #F1F3F0 / #171717 / #667085 / #0F766E / #1D4ED8 / #B7791F / #C2410C`
- render the selected path as a vertical lineage ribbon with explicit branch-action chips and manifest ids
- render rejected candidates as a compact ledger with disqualifier reason codes grouped by mismatch family
- render nightly predecessor context as a small predecessor rail rather than generic metadata text
- never turn this into a graph-for-graph’s-sake visualizer; the explorer is for branch meaning, not decorative topology

Backend, performance, and security requirements:
- keep the explorer read-only and trace-backed
- index by `lineage_trace_id`, selected manifest id, and append-only trace refs so queries do not scan whole manifest history
- do not let the explorer or query layer repair inconsistent lineage mirrors silently; surface typed failure or investigation state instead
- fail closed on missing selected trace, missing selected candidate, inconsistent mirror state, or missing nightly predecessor context where required

Validation rules:
- validate every emitted or queried `ManifestLineageTrace` against schema
- keep all manifest-lineage validator scenarios green, including the named scenario fixtures and rejected-candidate / nightly-context guard checks
- keep this task limited to lineage explorer queries and read-side rendering semantics

Definition of done:
- the repo has one authoritative manifest-lineage explorer query layer built on persisted `ManifestLineageTrace` truth
- reuse, replay, recovery, continuation, and nightly branch meaning are explainable without adjacency heuristics or manifest-local truth rewriting
- governance or operator clients can inspect branch decisions as a premium forensic surface rather than a brittle debug screen
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
