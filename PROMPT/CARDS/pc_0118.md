---
id: pc_0118
slug: phase_03_parallel_wave_01_track_backend_collection_010_implement_snapshot_assembly_and_snapshot_lifecycle_transitions
mode: parallel
phase: "03"
wave: "01"
track: "backend_collection"
task_number: "010"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_collection_010
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0118

## Task Summary

implement snapshot assembly and snapshot lifecycle transitions.

## Metadata

- ID: `pc_0118`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend collection`
- Task Number: `010`
- Source Slug: `phase_03_parallel_wave_01_track_backend_collection_010_implement_snapshot_assembly_and_snapshot_lifecycle_transitions`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0117](./pc_0117.md) `phase_03_parallel_wave_01_track_backend_collection_009_implement_late_data_indicators_bindings_and_input_freeze_persistence`
- Next: [pc_0119](./pc_0119.md) `phase_03_parallel_wave_01_track_backend_collection_011_implement_artifact_set_wrapping_hashing_and_contract_reference_recording`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0118_to_0125.md](../shared_operating_contract_0118_to_0125.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_collection_010_implement_snapshot_assembly_and_snapshot_lifecycle_transitions`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0117` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0066`, `pc_0076`, `pc_0080`, `pc_0097`, `pc_0100`, `pc_0101`, and `pc_0110` through `pc_0117`.

Mission:
Implement the governed `Snapshot` artifact, its repository, and the named-transition helper that moves snapshot state through `BUILT`, `VALID`, `WARNED`, `INVALID`, `SUPERSEDED`, `RETENTION_LIMITED`, and `ERASED`.
This task must build a snapshot only from authoritative intake set artifacts, bind exact set refs and hashes, compute machine-usable quality/completeness posture, and persist lifecycle changes through `state_transition_contract{...}` rather than controller-local status mutation.

Source algorithm grounding you must honor:
- `core_engine.md`, especially `BUILD_SNAPSHOT(...)`, `VALIDATE(...)`, `MEASURE_COMPLETENESS(...)`, `SnapshotBuilt`, and `SnapshotValidated`.
- `modules.md`, especially `BUILD_SNAPSHOT(...)`, `VALIDATE / MEASURE_COMPLETENESS(...)`, and the invariant that the first persisted snapshot enters `BUILT` through a named transition.
- `data_model.md`, especially `Snapshot`, `SourceRecordSet`, `EvidenceItemSet`, `CandidateFactSet`, `ConflictSet`, and `CanonicalFactSet`.
- `state_machines.md`, especially section `6.6 Snapshot.lifecycle_state`.
- `input_boundary_and_cutoff_contract.md`, `audit_and_provenance.md`, `observability_and_audit_contract.md`, and `deployment_and_resilience_contract.md`.
- `test_vectors.md`, especially `TV-44G`, `TV-76D`, and `TV-76E`.
- Existing schemas including `snapshot.schema.json`, `state_transition_contract.schema.json`, `schema_bundle.schema.json`, and the set schemas consumed by the snapshot.

Deliverables to create:
1. `packages/backend-collection/src/models/snapshot.ts`.
2. `packages/backend-collection/src/repositories/snapshot_repository.ts`.
3. `packages/backend-collection/src/services/build_snapshot.ts`.
4. `packages/backend-collection/src/services/transition_snapshot.ts`.
5. `packages/backend-collection/src/services/validate_snapshot_quality.ts`.
6. `packages/backend-collection/src/services/measure_snapshot_completeness.ts`.
7. `packages/backend-collection/src/services/classify_snapshot_validation_posture.ts`.
8. `packages/backend-collection/src/types/snapshot_assembly_input.ts`.
9. `db/migrations/phase03_0118_snapshot.sql`.
10. `docs/collection/snapshot_assembly_and_lifecycle.md`.
11. `tests/unit/backend_collection/build_snapshot.spec.ts`.
12. `tests/unit/backend_collection/transition_snapshot.spec.ts`.
13. `tests/integration/backend_collection/snapshot_lifecycle.spec.ts`.

Required design decisions you must make explicit:
- Define the atomic persistence boundary for snapshot assembly.
  The schema requires `quality` and `completeness` on the persisted artifact, but the engine pseudocode computes them after `BUILD_SNAPSHOT(...)`.
  Decide whether you use an in-memory draft followed by one authoritative persist, or a single transaction that persists `BUILT` with already-populated quality/completeness before the follow-up state transition.
- Define the canonical ref/hash binding from each authoritative set artifact to the snapshot.
  The snapshot must retain the exact set ref and the exact set hash for all five intake families.
- Define the mapping from validation/completeness posture to `VALID`, `WARNED`, and `INVALID`.
  This must be rule-based and replay-safe, not based on ad hoc controller heuristics.
- Define how supersession, retention limitation, and erasure transitions preserve lineage.
  Historical snapshots must remain auditable rather than being updated in place.
- Define canonical ordering for `audit_refs[]` and `provenance_refs[]`.

Execution steps:
1. Implement the schema-backed `Snapshot` model and repository with indexes on `manifest_id`, `lifecycle_state`, and the five authoritative set refs.
2. Implement `build_snapshot.ts` so it accepts only authoritative set artifacts plus manifest execution context, binds all required refs/hashes, and creates a schema-valid snapshot draft.
3. Implement `validate_snapshot_quality.ts` and `measure_snapshot_completeness.ts` as deterministic services that emit machine-usable scores, reason codes, and invalid/missing domain refs.
4. Implement `classify_snapshot_validation_posture.ts` so the snapshot transitions from `BUILT` to `VALID`, `WARNED`, or `INVALID` via named legal transitions.
5. Implement `transition_snapshot.ts` using the shared state-transition rules instead of raw status-field mutation.
6. Add tests for initial build, warn-class posture, invalid-class posture, supersession, retention limitation, and erasure.

Gap handling you must perform:
- Close the algorithm gap where `core_engine.md` mutates `snapshot.quality` / `snapshot.completeness` after build, but `snapshot.schema.json` requires both fields on the authoritative artifact.
  Never persist a partial snapshot that violates the schema.
- Close the roadmap-ordering gap where generic artifact-set wrapping lands in the next card.
  Snapshot assembly must consume schema-valid set refs/hashes through a dependency interface or fixture adapters in tests; do not invent a raw-list-only snapshot path.
- Close the gap where build progress could be mis-modeled as a persisted lifecycle state.
  The persisted lifecycle starts at `BUILT`; draft assembly progress belongs outside the authoritative artifact.
- Close the gap where snapshot supersession or retention limitation could otherwise be inferred from downstream compute or trust artifacts.
  Persist explicit transitions and lineage on the snapshot itself.

Mandatory edge cases:
- the same frozen set inputs must always produce the same set-ref binding and the same artifact contract envelope.
- a structurally invalid snapshot may exist for audit as `INVALID`, but it must not be accepted as a compliance compute basis.
- `WARNED` snapshots may only exist when your explicit classification policy says the warning posture is compatible with downstream gate review.
- `VALID`/`WARNED` snapshots superseded by a newer same-scope snapshot must move through the legal supersession transition; no in-place overwrite.
- `RETENTION_LIMITED` must not erase historical lineage; it must preserve limitation refs for later replay/explainability.
- `ERASED` must only occur from `RETENTION_LIMITED` through the named `erasure_complete` transition.
- duplicate or unstable `audit_refs[]` / `provenance_refs[]` ordering must fail tests.
- snapshot quality/completeness reason arrays must stay canonically ordered and deduped.

Backend, performance, and security requirements:
- Keep snapshot assembly deterministic, append-only in audit lineage, and replay-safe.
- Avoid duplicating large artifact payloads unnecessarily; bind by authoritative refs and hashes.
- Never log raw source payloads or sensitive evidence content in snapshot transitions.
- Ensure concurrent writers cannot both publish competing “current” snapshots for the same manifest/scope without explicit supersession policy.

Optional internal verification surface (only if you add one):
- If you add a read-only operator snapshot inspector, place it behind an internal route such as `/internal/intake/snapshots/:snapshotId`.
- The page must be minimalist premium: 12-column grid, max-width 1360–1440px, quiet ivory surface `#F6F3EE`, near-black text `#111318`, mono only for hashes/refs, one lifecycle rail across the top, one two-column body showing authoritative set bindings on the left and validation/completeness posture on the right.
- Use one restrained diagram only: a horizontal lineage ribbon that shows `BUILT -> VALID|WARNED|INVALID -> SUPERSEDED|RETENTION_LIMITED -> ERASED`.
- Any such surface must be implemented and verified with Playwright using role-based locators, reduced-motion checks, and trace capture.

Validation rules:
- Unit-test snapshot draft assembly, quality/completeness scoring, and all legal/illegal snapshot transitions.
- Integration-test authoritative persistence from set refs/hashes through final lifecycle classification.
- Validate persisted snapshots against `snapshot.schema.json`.
- Prove that no authoritative snapshot can exist without populated `quality` and `completeness`.
- Re-run the contract validators after the implementation bundle is assembled.

Definition of done:
- The repo can build and persist a schema-valid `Snapshot` from authoritative intake sets.
- Snapshot lifecycle changes are typed, auditable, and replay-safe.
- Later artifact-contract, compute, parity, trust, replay, and amendment work can consume one explicit snapshot basis instead of reconstructing meaning from loose set artifacts.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
