---
id: pc_0085
slug: phase_03_parallel_wave_01_track_backend_access_001_implement_tenant_user_and_actor_session_persistence_models
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "001"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0085

## Task Summary

implement tenant user and actor session persistence models.

## Metadata

- ID: `pc_0085`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `001`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_001_implement_tenant_user_and_actor_session_persistence_models`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0084](./pc_0084.md) `phase_02_seq_084_create_release_branching_versioning_and_artifact_naming_conventions`
- Next: [pc_0086](./pc_0086.md) `phase_03_parallel_wave_01_track_backend_access_002_implement_principal_context_and_authorization_decision_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0078_to_0085.md](../shared_operating_contract_0078_to_0085.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_001_implement_tenant_user_and_actor_session_persistence_models`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0084` if they exist, especially `pc_0021`, `pc_0038`, `pc_0039`, `pc_0040`, `pc_0042`, `pc_0043`, `pc_0049`, `pc_0050`, `pc_0054`, `pc_0063`, `pc_0064`, `pc_0066`, `pc_0067`, `pc_0068`, `pc_0073`, `pc_0074`, `pc_0075`, `pc_0076`, `pc_0083`, and `pc_0084`.

Mission:
Start Phase 03 backend-access work by implementing the durable persistence layer for `Tenant`, `User`, and `ActorSession`.
This is the foundation on which principal-context construction, authorization decisions, session revocation, cross-device continuity, step-up enforcement, and access simulation will depend.
The result must preserve the corpus' session law explicitly instead of hiding it in framework cookies or transient auth middleware.

Source algorithm grounding you must honor:
- `data_model.md`, especially the canonical fields for `Tenant`, `User`, and `ActorSession`.
- `actor_and_authority_model.md`, especially `3.6 Principal context schema`, `3.9 Policy decision model`, and the rules that session and delegated authority lineage remain explicit.
- `northbound_api_and_session_contract.md`, especially `8. Session, browser, and native-client rules` and the invariants around session revocation, device binding, effective scope resolution, and safe interactive sessions.
- `security_and_runtime_hardening_contract.md`, especially identity/session trust, federated identity, MFA/step-up, anti-CSRF posture, short-lived session records, and secret/token handling.
- Existing schemas including `actor_session.schema.json`, `principal_context.schema.json`, and `authorization_decision.schema.json`.
- The session, notification, telemetry, cache, and release foundations from earlier cards, because session persistence will later drive revocation, stream invalidation, and audit evidence.

Deliverables to create:
1. `schemas/tenant.schema.json`.
2. `schemas/user.schema.json`.
3. `packages/backend-access/src/models/tenant.ts`.
4. `packages/backend-access/src/models/user.ts`.
5. `packages/backend-access/src/models/actor_session.ts`.
6. `packages/backend-access/src/repositories/tenant_repository.ts`.
7. `packages/backend-access/src/repositories/user_repository.ts`.
8. `packages/backend-access/src/repositories/actor_session_repository.ts`.
9. `packages/backend-access/src/session/session_lifecycle_service.ts`.
10. `db/migrations/phase03_0001_tenant_user_actor_session.sql`.
11. `config/access/session_lifecycle_policy.json`.
12. `docs/access/tenant_user_and_actor_session_models.md`.
13. `tests/unit/backend_access/actor_session_model.spec.ts`.
14. `tests/integration/backend_access/tenant_user_actor_session_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the database representation for `Tenant`, `User`, and `ActorSession` and justify primary keys, unique constraints, foreign keys, soft-delete posture, and tenant scoping.
- Define whether `ActorSession` revocation, expiry, device binding, anti-CSRF references, and step-up completion live directly in one row or in supporting child tables; keep the external contract stable either way.
- Define the lifecycle states for sessions: issued, active, stepped-up, expired, revoked, device-invalidated, and any internal transitional posture needed for consistency.
- Define indexes for revocation checks, tenant+user lookups, expiry sweeps, last-seen updates, and session-binding hash lookups.
- Define how user roles and attributes are represented at this phase without pre-empting the later principal-context and authorization-decision tasks.
- Define the boundary between durable session truth and transport/session-token material; raw authority credentials and opaque refresh secrets must not become first-class persisted session payloads.

Execution steps:
1. Create governed schemas for `Tenant` and `User` to close the corpus gap where the data model defines them but the schema bundle does not.
2. Implement typed domain models and persistence mappings for `Tenant`, `User`, and `ActorSession` consistent with the canonical field sets and imported schemas.
3. Create the initial migration with tables, indexes, constraints, timestamps, and tenant-scoped integrity rules.
4. Implement repositories and lifecycle services for creating, reading, revoking, expiring, rotating, and updating `ActorSession` state.
5. Encode a session lifecycle policy file that captures expiry, revocation classes, step-up rotation rules, device-binding invalidation, last-seen update cadence, and anti-CSRF binding expectations.
6. Add integration tests covering browser-style sessions, native-bound sessions, step-up completion, revocation, expiry, and tenant isolation.
7. Record audit-friendly source lineage and reason codes for lifecycle transitions even if the full audit write path is implemented in later tasks.

Gap handling you must perform:
- Close the gap where `Tenant` and `User` exist in the data model but have no dedicated schemas.
- Close the gap where frameworks often treat session posture as implicit middleware state rather than a durable governed object.
- Close the gap where anti-CSRF posture, device binding, and step-up completion could otherwise be lost across process restarts or cross-device continuity flows.
- Close the gap where revocation needs to invalidate streams, uploads, and future commands but there is no durable session record to query.
- Close the gap where user and tenant persistence could drift from the actor model before later authorization tasks arrive.

Mandatory edge cases:
- browser session created without anti-CSRF binding must fail validation
- native session with invalidated device binding must surface as revoked session
- step-up completes after session issue and must rotate effective challenge posture
- expired session looked up after revocation timestamp also exists
- same user holds concurrent sessions on two devices with one revoked
- tenant disabled or missing while session still exists
- `last_seen_at` updates must remain monotonic under race conditions
- repository call tries to store raw provider refresh token or raw authority credential material

Backend, performance, and security requirements:
- Sessions must be short-lived, revocable, and queryable by session ID and binding hash.
- Sensitive data must be minimized; persist references, hashes, posture states, and reason codes rather than raw credential material.
- Cross-tenant leakage must be impossible by construction through constraints, scoped queries, and repository APIs.
- Expiry and revocation checks must be fast enough for hot request paths.
- Session lifecycle services must be idempotent and race-safe; revoking an already revoked session should be a no-op with clear reason lineage.
- Keep the implementation ready for future `PrincipalContext` and `AuthorizationDecision` construction without overreaching into those tasks.

Validation rules:
- Validate the new `tenant.schema.json` and `user.schema.json` alongside the existing imported schema bundle.
- Unit-test model serialization, lifecycle transitions, and repository guardrails against raw credential persistence.
- Integration-test migration application, row constraints, tenant isolation, concurrent session behavior, revocation, expiry, and step-up completion.
- Prove that browser and native sessions can be distinguished safely by persisted posture fields.
- Prove that session lookups and revocation checks remain deterministic and monotonic.

Definition of done:
- The repo has durable, schema-backed persistence for `Tenant`, `User`, and `ActorSession`.
- Session posture is explicit and queryable rather than hidden in transient auth middleware.
- Later authorization and session-boundary tasks can build on a trustworthy persistence foundation.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.

