---
id: pc_0096
slug: phase_03_parallel_wave_01_track_backend_access_012_implement_principal_access_view_read_model_and_query_service
mode: parallel
phase: "03"
wave: "01"
track: "backend_access"
task_number: "012"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_access_012
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0096

## Task Summary

implement principal access view read model and query service.

## Metadata

- ID: `pc_0096`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend access`
- Task Number: `012`
- Source Slug: `phase_03_parallel_wave_01_track_backend_access_012_implement_principal_access_view_read_model_and_query_service`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0095](./pc_0095.md) `phase_03_parallel_wave_01_track_backend_access_011_implement_role_template_matrix_projector_and_policy_snapshot_hashing`
- Next: [pc_0097](./pc_0097.md) `phase_03_parallel_wave_01_track_backend_manifest_001_implement_run_manifest_repository_and_lifecycle_state_machine`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0094_to_0101.md](../shared_operating_contract_0094_to_0101.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_access_012_implement_principal_access_view_read_model_and_query_service`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0095` if they exist, especially `pc_0086`, `pc_0087`, `pc_0088`, `pc_0089`, `pc_0090`, `pc_0091`, `pc_0092`, `pc_0093`, `pc_0094`, and `pc_0095`.

Mission:
Implement the `PrincipalAccessView` read model and query service that drive the governance access-principals workspace.
The result must preserve the corpus' explicit authority-layer stack, exact decision vocabulary, and same-shell selection semantics so operators can inspect access truth without the UI inventing its own permission logic.

Source algorithm grounding you must honor:
- `data_model.md`, especially `PrincipalAccessView`, the required `action_matrix[]`, `delegation_summaries[]`, `selected_action_detail`, and `access_workspace{...}` groups.
- `admin_governance_console_architecture.md`, especially `/governance/access`, the required authority-layer order, fixed route reading path, simulator linkage, and minimum route read-model contract.
- `actor_and_authority_model.md`, especially `3.4 Authority layers`, `3.9 Policy decision model`, and `3.15 Frontend and governance-console rendering contract`.
- `modules.md`, especially `AUTHORIZE(...)`, `ENFORCE_ACCESS_SCOPE_AND_MASKING(...)`, and any simulation-related outputs that must be reflected in `latest_simulation_ref` or required approvals.
- `frontend_shell_and_interaction_law.md`, `dominant_question_and_single_action_contract.md`, and `semantic_selector_and_accessibility_contract.md` for the preview surface.
- Existing schemas including `principal_access_view.schema.json`, `authorization_decision.schema.json`, and `governance_access_simulation.schema.json`.

Deliverables to create:
1. `packages/backend-access/src/read_models/principal_access_view.ts`.
2. `packages/backend-access/src/queries/get_principal_access_view.ts`.
3. `packages/backend-access/src/services/principal_access_view_query_service.ts`.
4. `packages/backend-access/src/projectors/principal_access_view_projector.ts`.
5. `packages/backend-access/src/services/action_matrix_assembler.ts`.
6. `packages/backend-access/src/services/delegation_summary_builder.ts`.
7. `packages/backend-access/src/services/authority_chain_stack_builder.ts`.
8. `packages/backend-access/src/repositories/principal_access_view_repository.ts`.
9. `apps/operator-web/src/routes/internal/principal-access-view-preview.tsx`.
10. `docs/access/principal_access_view_query_contract.md`.
11. `tests/unit/backend_access/principal_access_view_projector.spec.ts`.
12. `tests/integration/backend_access/principal_access_view_query_service.spec.ts`.
13. `tests/playwright/internal/principal_access_view_preview.spec.ts`.

Required design decisions you must make explicit:
- Define the query boundary for `PrincipalAccessView`: required input refs, optional filters, selection anchors, workspace mode, and whether the service returns a single fully mounted principal view or supports list/detail pagination.
- Define the canonical sorting and stable identity rules for `action_matrix[]`, `delegation_summaries[]`, `active_filters`, `surface_order`, and `selected_cell_ref`.
- Define how `authority_chain_layers[]` are assembled in the exact required sequence: `SESSION_AUTHN_POSTURE -> TENANT_OPERATIONAL_AUTHORITY -> CLIENT_DELEGATION_COVERAGE -> EXTERNAL_AUTHORITY_LINK_READINESS`, plus optional `AUTHORITY_OF_RECORD_OUTCOME`.
- Define how the read model preserves `focus_anchor_ref`, `selected_action_detail`, and `recovery_posture` when filters, refreshes, or stale-view rebases occur.
- Define the caching and invalidation strategy so the view can be refreshed efficiently without hiding current policy or delegation drift.
- Define how `latest_simulation_ref` is surfaced without letting simulator output overwrite committed access truth.

Execution steps:
1. Map the exact data dependencies for one principal access view: principal context, role set, authorization decisions, delegation grants, authority links, exceptional authority, policy snapshot, and latest applicable simulation refs.
2. Implement `action_matrix_assembler.ts` so cells preserve decision, reason codes, effective scope, masking rules, required approvals, required authn level, policy-path ref, and ordered authority-chain layers.
3. Implement `delegation_summary_builder.ts` and `authority_chain_stack_builder.ts` so delegation and authority readiness are explicit read-model facts rather than UI-local inferences.
4. Implement `principal_access_view_projector.ts` and `principal_access_view_query_service.ts` to assemble one route-ready, schema-valid `PrincipalAccessView`.
5. Persist or cache the projected view only if it stays invalidation-safe against policy hash, delegation/link snapshots, and principal changes.
6. Build `principal-access-view-preview` as an internal read-only inspection surface wired directly to the query service.
7. Add tests for filter stability, selected-cell persistence, authority-layer ordering, and distinction between masked, step-up, approval, and deny outcomes.

Visual and interaction specification for `principal-access-view-preview`:
- Tone: a premium access observatory.
  It should feel like an instrument for lawful reasoning, not a noisy admin dashboard.
- Layout:
  - `64px` top context bar with principal chip, authn-level chip, last-step-up chip, and `policy_snapshot_hash` chip.
  - left directory/filter rail `260-276px` with principal filters, role chips, delegated-client filters, and recent-change owner filters.
  - central workspace canvas with an action matrix on top and a lower explanation shelf for current selected action detail.
  - right inspector `384-412px` with authority-layer stack, effective scope, masking rules, required approvals, and related simulation ref.
- Signature visual:
  - use a thin vertical “authority stack” diagram in the inspector with five ordered rails.
  - each rail highlights one layer outcome; this replaces any generic traffic-light summary.
- Palette:
  - background `#F4F4F1`
  - primary surface `#FFFFFF`
  - secondary surface `#ECEBE5`
  - ink `#10171D`
  - muted ink `#66707A`
  - hairline `rgba(16,23,29,0.08)`
  - accent slate `#4A6075`
  - accent moss `#61715D`
  - accent amber `#8B6934`
  - success `#156049`
  - warning `#875A1C`
  - danger `#A23934`
- Typography:
  - quiet, editorial hierarchy.
  - monospace is reserved for principal refs, cell refs, scope tokens, and policy-path refs.
  - keep the matrix legible without giant outcome pills.
- Dimensional constraints:
  - max width `1560px`
  - directory rail min width `260px`
  - matrix row height `46px`
  - explanation shelf min height `168px`
  - inspector authority-stack rows `40px`
- Motion:
  - `140ms` matrix-cell focus transition.
  - `180ms` inspector layer emphasis.
  - reduced-motion mode removes animated shelf transitions and keeps only static highlight changes.
- Interaction:
  - the semantic reading order must remain `PRINCIPAL_DIRECTORY -> WORKSPACE_CANVAS -> ACCESS_INSPECTOR -> AUTHORITY_CHAIN_PANEL -> POLICY_SIMULATOR_LINK`.
  - keyboard navigation across the matrix is mandatory and must preserve `focus_anchor_ref`.
  - selecting a cell updates `selected_action_detail` and the inspector without changing the mounted principal.
  - filters must preserve selection when lawful; when not, show explicit typed recovery posture rather than clearing state silently.
  - accessible names must read as full statements such as `client contributor requires step up for submit on employment-income`.
  - Playwright must verify keyboard travel, focus persistence, filter determinism, and reduced-motion behavior.

Component hierarchy hint:
~~~ts
<InternalShell route="/internal/principal-access-view-preview">
  <PrincipalDirectoryRail />
  <AccessMatrixCanvas />
  <SelectedActionShelf />
  <AuthorityStackInspector />
</InternalShell>
~~~

Gap handling you must perform:
- Close the gap where the access-principals route could rebuild authority logic in the UI instead of consuming one backend read contract.
- Close the gap where `ALLOW_MASKED`, `REQUIRE_STEP_UP`, `REQUIRE_APPROVAL`, and `DENY` might collapse into a generic allow/deny boolean.
- Close the gap where the ordered authority-layer explanation could drift from the actual decision basis.
- Close the gap where selection and filter state are lost on refresh or rebase, making the governance workspace feel generic and stateless.
- Close the gap where simulator context can overwrite committed access truth instead of sitting alongside it.

Mandatory edge cases:
- service principal with `BASIC` authn level must never appear to satisfy human-only gates
- principal has tenant permission but missing client delegation; the layer stack must show the exact blocking layer
- masked access remains visible and distinct from allow
- selected cell is filtered out and the route must show deterministic recovery posture
- latest simulation ref is absent and the route must not fabricate a simulator state
- stale policy snapshot changes between reads and the view must surface typed stale posture instead of silently reinterpreting selection
- authority-of-record contradiction adds the fifth layer and does not reorder the first four
- keyboard navigation and selection persistence survive responsive compaction and reconnect

Backend, performance, and security requirements:
- The query service must be deterministic and cheap enough for routine governance reads.
- Never expose raw authority credentials, raw CSRF material, or internal secret refs in the read model.
- Read-model assembly must be side-effect free; no policy mutation, no access reevaluation that is not explicitly tracked, and no UI-local widening of scope.
- The preview route must render from the query service, not from duplicate front-end decision logic.
- Cached read models must be invalidated by principal, policy hash, delegation snapshot, authority-link snapshot, or relevant simulator publication changes.

Validation rules:
- Unit-test matrix assembly, authority-chain ordering, filter grammar, selection persistence, and masking/approval/step-up separation.
- Integration-test query service assembly from persisted access facts, stale-view changes, and simulator linkage.
- Use Playwright to verify the preview route with role-first locators, keyboard traversal, focus restoration, filter behavior, and reduced-motion-safe rendering.
- Prove that the same underlying access facts yield identical projected views under repeated runs.
- Prove that `PrincipalAccessView` never widens scope or hides the blocking authority layer.

Definition of done:
- The repo has one authoritative `PrincipalAccessView` query service and read model.
- Governance access inspection can rely on deterministic backend truth instead of client-local permission recomputation.
- Later governance UI work gains a stable same-shell access contract with explicit selection, explanation, and stale-view basis.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
