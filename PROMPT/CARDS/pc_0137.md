---
id: pc_0137
slug: phase_03_parallel_wave_01_track_backend_authority_005_implement_hmrc_oauth_token_client_binding_and_send_time_revalidation_service
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "005"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0137

## Task Summary

implement hmrc oauth token client binding and send time revalidation service.

## Metadata

- ID: `pc_0137`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `005`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_005_implement_hmrc_oauth_token_client_binding_and_send_time_revalidation_service`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0136](./pc_0136.md) `phase_03_parallel_wave_01_track_backend_authority_004_implement_authority_request_identity_idempotency_and_duplicate_meaning_key_logic`
- Next: [pc_0138](./pc_0138.md) `phase_03_parallel_wave_01_track_backend_authority_006_implement_fraud_prevention_header_capture_validation_and_profile_binding_service`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0134_to_0141.md](../shared_operating_contract_0134_to_0141.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_005_implement_hmrc_oauth_token_client_binding_and_send_time_revalidation_service`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0136` if they exist, especially `pc_0031`, `pc_0038`, `pc_0039`, `pc_0048`, `pc_0051`, `pc_0058`, `pc_0061`, `pc_0066`, `pc_0067`, `pc_0068`, `pc_0074`, `pc_0075`, `pc_0086` through `pc_0093`, `pc_0097`, `pc_0102` through `pc_0109`, and `pc_0135` through `pc_0136`.

Mission:
Implement the HMRC OAuth token-client binding layer and the mandatory send-time revalidation service that runs immediately before any live authority bytes leave the process.
This card must make delayed send, queue recovery, replay-protected continuation, token refresh, and duplicate-bucket drift auditable and fail-closed.
It must never silently rebind a sealed request to a different subject, client, authority link, or executable authorization posture after request identity has been frozen.

Source algorithm grounding you must honor:
- `authority_interaction_protocol.md`, especially `9.6 Token and client binding rule`, the send-time revalidation rule, and the grouped `binding_drift_sentinel_contract{...}` requirements.
- `modules.md`, especially `AUTHORITY_PREFLIGHT(...)`, `RESOLVE_AUTHORITY_BINDING(...)`, `RECORD_AUTHORITY_INTERACTION(...)`, and the interaction lifecycle rules around send revalidation, duplicate truth, and exclusive send claim.
- `data_model.md`, especially `AuthorityBinding`, `AuthorityInteractionRecord`, and the grouped sentinel / send-revalidation fields.
- `state_machines.md`, especially `AuthorityInteractionRecord` lifecycle expectations and the requirement to re-check duplicate bucket plus latest authority-grounded state before send.
- `security_and_runtime_hardening_contract.md`, `northbound_api_and_session_contract.md`, and `connector_delegation_contract.md`.
- official HMRC OAuth operational constraints only as provider-specific sharpening, not as a replacement for Taxat semantics:
  redirect URI matching, short-lived access tokens, refresh-token rotation behavior, and subject-specific token correctness.

Deliverables to create:
1. `packages/backend-authority/src/services/resolve_hmrc_oauth_token_client_binding.ts`
2. `packages/backend-authority/src/services/refresh_hmrc_access_token_with_lineage_guard.ts`
3. `packages/backend-authority/src/services/build_binding_drift_sentinel_contract.ts`
4. `packages/backend-authority/src/services/revalidate_authority_binding_before_send.ts`
5. `packages/backend-authority/src/services/revalidate_authority_binding_for_reconciliation_read.ts`
6. `packages/backend-authority/src/services/acquire_exclusive_authority_send_claim.ts`
7. `packages/backend-authority/src/services/release_or_close_authority_send_claim.ts`
8. `packages/backend-authority/src/services/classify_send_revalidation_outcome.ts`
9. `packages/backend-authority/src/services/persist_send_revalidation_projection.ts`
10. `packages/backend-authority/src/services/assert_binding_lineage_continuity.ts`
11. `db/migrations/phase03_0137_hmrc_oauth_binding_and_send_time_revalidation.sql`
12. `docs/authority/hmrc_oauth_binding_and_send_time_revalidation.md`
13. `tests/unit/backend_authority/send_time_revalidation_rules.spec.ts`
14. `tests/unit/backend_authority/token_rotation_within_lineage.spec.ts`
15. `tests/integration/backend_authority/queue_delay_and_send_revalidation_flow.spec.ts`

Required design decisions you must make explicit:
- Define the exact identity tuple that must remain unchanged between seal-time and send-time:
  `authority_link_ref`,
  `binding_lineage_ref`,
  subject ref,
  acting-party ref,
  authority scope,
  provider environment,
  provider API version,
  `access_binding_hash`,
  `policy_snapshot_hash`,
  required step-up posture,
  required approval posture.
- Define when token refresh is lawful:
  only as `token_version_ref` advancement inside the same `binding_lineage_ref` without any drift in subject/client/scope/environment/version.
- Define the exact fields that the grouped `binding_drift_sentinel_contract{...}` freezes and the exact mapping from that grouped contract to the projection fields:
  `send_revalidation_state`,
  `send_revalidated_at`,
  `send_authorized_token_version_ref`,
  `send_revalidation_reason_codes[]`.
- Define the concurrency contract for the exclusive send claim:
  compare-and-swap scope, state preconditions, claim owner semantics, and how blocked non-send outcomes are persisted.

Execution steps:
1. Implement token-client binding resolution that proves the selected OAuth token belongs to the same client/reporting-subject/legal-scope context as the sealed authority binding.
2. Implement refresh handling that may rotate only the token version inside the same lineage and never rebind a request to a different client or subject.
3. Implement `build_binding_drift_sentinel_contract.ts` so the grouped safety boundary captures checked binding identity, sealed token version, checked token version, duplicate truth consultation inputs, exclusive send claim posture, pass/block reasons, and latest stronger-truth refs.
4. Implement `revalidate_authority_binding_before_send.ts` so every live mutation send re-checks the binding identity tuple, duplicate bucket, stronger truth, and exclusive send claim immediately before bytes leave the process.
5. Reuse the same grouped sentinel logic for `RECONCILIATION_POLL` and `RECOVERY_READ`, with explicit checked action classes and fail-closed outcomes.
6. Persist the grouped sentinel and its send projection onto `AuthorityInteractionRecord` before any transition to `TRANSMIT_IN_FLIGHT` or blocked non-send terminal posture.
7. Ensure queue recovery / delayed worker execution reuses the sealed persisted binding lineage and never resolves a fresh live binding for an already-sealed request.

Gap handling you must perform:
- Close the gap where queued or delayed sends could silently resolve a newer but different token/client/subject context.
- Close the gap where within-lineage token rotation is allowed, but there is no typed proof distinguishing lawful rotation from illegal rebinding.
- Close the gap where stronger authority truth appearing during queue delay could be ignored and a stale mutation still sent.
- Close the gap where exclusive send claim behavior could remain an implicit broker convention instead of a persisted auditable contract.
- Close the gap where reconciliation reads could bypass the same binding-identity checks required for sends.

Mandatory edge cases:
- lawful token refresh inside the same lineage must serialize as a clear-to-proceed sentinel with explicit `TOKEN_ROTATED_WITHIN_LINEAGE` reason and a non-null checked/send-authorized token version
- any drift in subject, acting party, client, authority scope, provider environment, or API version must block the send
- any drift in `access_binding_hash` or `policy_snapshot_hash` must block the send
- stale or missing step-up evidence must block the send
- stale or missing required approval evidence must block the send
- stronger external truth for the same duplicate meaning appearing before bytes leave the process must block the send and route toward reconciliation or non-send closure
- if another worker already owns the exclusive send claim, the current worker must not send
- blocked non-sends must keep checked/send token version refs null and preserve explicit reason codes
- replay/resume paths must reuse persisted binding lineage and must not re-run a fresh live authority binding selection for a sealed request

Backend, performance, and security requirements:
- fail closed on any ambiguity
- never log raw access tokens, refresh tokens, or full OAuth payloads
- isolate token refresh and send claim transitions in transactionally safe boundaries
- support idempotent resume after worker crash or broker redelivery
- index the hot-path lookup by binding lineage, subject/client tuple, request hash, duplicate meaning, and interaction lifecycle state
- keep provider-specific OAuth details behind narrow adapters so the domain layer preserves Taxat vocabulary

Validation rules:
- unit-test lawful token rotation, illegal rebinding, stale stronger truth, stale step-up, stale approval, claim contention, and reconciliation-read reuse of the same sentinel logic
- integration-test queue delay with token rotation, queue delay with subject drift, crash/recovery with persisted claim/sentinel state, and duplicate-bucket change before send
- prove that a sealed request never silently swaps to a different subject/client context
- prove that blocked non-sends are durable, replay-safe, and auditable

Definition of done:
- every live authority send and follow-up read now passes through a durable, fail-closed send-time revalidation service
- HMRC token-client binding is explicit, lineage-bound, and safe under delay/recovery
- the grouped binding drift sentinel becomes the authoritative record of pre-send legality
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
