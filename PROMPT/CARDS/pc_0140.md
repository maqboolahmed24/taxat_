---
id: pc_0140
slug: phase_03_parallel_wave_01_track_backend_authority_008_implement_authority_interaction_record_and_reconciliation_budget_engine
mode: parallel
phase: "03"
wave: "01"
track: "backend_authority"
task_number: "008"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_authority_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0140

## Task Summary

implement authority interaction record and reconciliation budget engine.

## Metadata

- ID: `pc_0140`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend authority`
- Task Number: `008`
- Source Slug: `phase_03_parallel_wave_01_track_backend_authority_008_implement_authority_interaction_record_and_reconciliation_budget_engine`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0139](./pc_0139.md) `phase_03_parallel_wave_01_track_backend_authority_007_implement_authority_ingress_receipt_callback_poll_and_recovery_normalization_pipeline`
- Next: [pc_0141](./pc_0141.md) `phase_03_parallel_wave_01_track_backend_authority_009_implement_authority_calculation_request_result_and_user_confirmation_flow`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0134_to_0141.md](../shared_operating_contract_0134_to_0141.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_authority_008_implement_authority_interaction_record_and_reconciliation_budget_engine`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0139` if they exist, especially `pc_0048`, `pc_0049`, `pc_0052`, `pc_0061`, `pc_0066`, `pc_0067`, `pc_0068`, `pc_0070`, `pc_0071`, `pc_0074`, `pc_0075`, `pc_0076`, `pc_0086`, `pc_0097`, `pc_0108`, `pc_0109`, `pc_0134`, `pc_0135`, `pc_0136`, `pc_0137`, `pc_0138`, and `pc_0139`.

Mission:
Implement the durable `AuthorityInteractionRecord` lifecycle plus the reconciliation-budget, resend-legality, escalation, and follow-up engine.
This card must turn the runtime exchange ledger into the single authoritative source of truth for request identity reuse, active admissible response meaning, send-revalidation outcome, open follow-up window, blocked resend posture, and escalation ownership.

Source algorithm grounding you must honor:
- `data_model.md`, especially `AuthorityInteractionRecord` and the grouped `reconciliation_control_contract{...}` / `binding_drift_sentinel_contract{...}` / `authority_truth_contract{...}` fields.
- `state_machines.md`, especially the `AuthorityInteractionRecord` lifecycle.
- `modules.md`, especially `RECORD_AUTHORITY_INTERACTION(...)`, `PERSIST_AUTHORITY_RECONCILIATION_CONTROL(...)`, `RECONCILE_AUTHORITY_STATE(...)`, and `EMIT_AUTHORITY_RECONCILIATION_ANALYTICS(...)`.
- `authority_interaction_protocol.md`, especially the runtime-ledger boundary and budget/exhaustion/escalation rules.
- `authority_truth_and_internal_projection_separation_contract.md`.
- `PATCH_RESOLUTION_INDEX.md`, especially the entries that corrected premature `resolution_basis`, chronology regressions, and premature reconciliation deadlines.
- `test_vectors.md`, especially `TV-70F` through `TV-70V`.

Deliverables to create:
1. `packages/backend-authority/src/models/authority_interaction_record.ts`
2. `packages/backend-authority/src/repositories/authority_interaction_record_repository.ts`
3. `packages/backend-authority/src/services/validate_authority_interaction_transition.ts`
4. `packages/backend-authority/src/services/record_authority_interaction.ts`
5. `packages/backend-authority/src/services/build_reconciliation_control_contract.ts`
6. `packages/backend-authority/src/services/persist_authority_reconciliation_control.ts`
7. `packages/backend-authority/src/services/schedule_next_reconciliation.ts`
8. `packages/backend-authority/src/services/classify_resend_legality_state.ts`
9. `packages/backend-authority/src/services/reconcile_authority_state.ts`
10. `packages/backend-authority/src/services/classify_authority_resolution_basis.ts`
11. `packages/backend-authority/src/services/derive_reconciliation_outcome_class.ts`
12. `packages/backend-authority/src/services/emit_authority_reconciliation_analytics_seed.ts` or a similarly narrow analytics-seed helper if later analytics tasks need it
13. `db/migrations/phase03_0140_authority_interaction_record_and_reconciliation_budget.sql`
14. `docs/authority/authority_interaction_record_and_reconciliation_budget_engine.md`
15. `tests/unit/backend_authority/authority_interaction_contract.spec.ts`
16. `tests/unit/backend_authority/reconciliation_budget_and_resend_legality.spec.ts`
17. `tests/integration/backend_authority/authority_interaction_recovery_and_escalation_flow.spec.ts`

Required design decisions you must make explicit:
- Define the exact lifecycle transition matrix for:
  `REQUEST_REGISTERED`,
  `DISPATCH_READY`,
  `TRANSMIT_IN_FLIGHT`,
  `RESPONSE_CAPTURED`,
  `RECONCILING`,
  `RESOLVED`,
  `ABANDONED`.
- Define the rules for when `active_response_id`, `response_history_ids[]`, `meaning_resolution_state`, and `resolution_basis` may change.
- Define the full `reconciliation_control_contract{...}` structure, including:
  budget state,
  attempt count,
  cadence,
  next follow-up time,
  escalation state,
  escalation owner,
  workflow ref,
  evidence refs,
  replay-resume policy,
  resend legality,
  resend reason codes,
  analytics outcome class.
- Define the legal distinction between:
  `IDEMPOTENT_RECOVERY_ONLY`,
  `FOLLOW_UP_READ_ONLY`,
  blocked resend postures,
  escalated posture,
  and closed posture.

Execution steps:
1. Implement the schema-backed interaction model and repository with immutable request-identity reuse and append-only response history.
2. Implement lifecycle transition guards so `resolution_basis` appears only in `RESOLVED`, `abandonment_reason_code` appears only in `ABANDONED`, and `reconciliation_deadline_at` is legal only when unresolved follow-up is actually open.
3. Implement `record_authority_interaction.ts` so every live authority exchange is registered before send and can carry request identity, authority truth contract, send revalidation projection, and response history.
4. Implement `build_reconciliation_control_contract.ts` and `persist_authority_reconciliation_control.ts` so all automatic follow-up budget and escalation semantics are frozen into one grouped contract.
5. Implement `schedule_next_reconciliation.ts` and `classify_resend_legality_state.ts` so budget exhaustion, contradiction, stronger truth, and escalation explicitly block automatic resend rather than relying on queue-worker heuristics.
6. Implement `reconcile_authority_state.ts` so it consumes durable response history and durable control contracts, updates unresolved settlement/mirror posture lawfully, and reuses the exact persisted budget rather than reopening a fresh retry ladder.
7. Optionally emit a narrow analytics seed artifact if later analytics work needs persisted counts/outcome-class lineage, but do not build a separate product surface here.

Gap handling you must perform:
- Close the gap where runtime exchange truth could be reconstructed from request/response tables without one authoritative interaction object.
- Close the gap where queue rebuild or continuation might reopen a fresh automatic resend budget for an existing unresolved meaning.
- Close the gap where response history could be treated as “latest only” instead of append-only admissible lineage.
- Close the gap where resend legality could be inferred from stale queue state instead of from a persisted grouped control contract.
- Close the gap where escalation owner/workflow/due time could be transient and disappear on crash or replay.

Mandatory edge cases:
- `send_revalidation_state` must remain `NOT_PERFORMED` before the mandatory pre-send check, become `CLEAR_TO_SEND` only after a successful check, and `BLOCKED` only for a persisted fail-closed non-send
- `send_authorized_token_version_ref` must remain null unless `send_revalidation_state = CLEAR_TO_SEND`
- `response_history_ids[]` must preserve all observations; `active_response_id` is the currently admissible meaning only
- `meaning_resolution_state` must preserve `NO_RESPONSE`, `PROVISIONAL_TIMEOUT`, `RECONCILIATION_REQUIRED`, and `RECONCILIATION_RESOLVED` explicitly
- `next_reconciliation_at` must be populated only while the budget state is active
- `resolution_basis` must be non-null only in `RESOLVED`
- `abandonment_reason_code` must be non-null only in `ABANDONED`
- crash/recovery/continuation must reuse the persisted budget packet instead of reopening attempt count or cadence
- contradictory evidence must block resend even if nominal automatic attempts remain
- escalation must preserve owner, workflow item, evidence refs, due time, and blocked resend posture durably

Backend, performance, and security requirements:
- fail closed on chronology regressions, premature resolution basis, premature abandonment reason, or illegal resend state transitions
- preserve append-only response history and durable resolution basis without overwriting earlier observations
- index interactions by request hash, duplicate meaning, manifest, lifecycle state, reconciliation budget state, and next follow-up time
- avoid any logic that depends on broker-specific retry counts as authoritative budget truth
- emit structured audit evidence for state transitions and grouped control-contract changes

Validation rules:
- unit-test lifecycle legality, grouped control-contract creation, resend legality changes, contradiction handling, exhaustion, escalation, and replay-resume posture
- integration-test unresolved interaction recovery, contradiction-driven block, budget exhaustion, escalation, and final resolution
- prove that the same persisted budget packet is reused across recovery/resume
- prove that blocked resend posture cannot silently reopen
- prove that response history remains append-only and active meaning selection is explicit

Definition of done:
- the repo has one authoritative runtime interaction ledger for authority exchanges
- reconciliation budget, resend legality, and escalation posture are durable and replay-safe
- later authority-state and analytics work can rely on persisted grouped control contracts instead of queue-worker memory
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
