---
id: pc_0121
slug: phase_03_parallel_wave_01_track_backend_compute_001_implement_compute_result_formula_engine_with_exact_decimal_handling
mode: parallel
phase: "03"
wave: "01"
track: "backend_compute"
task_number: "001"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_compute_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0121

## Task Summary

implement compute result formula engine with exact decimal handling.

## Metadata

- ID: `pc_0121`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend compute`
- Task Number: `001`
- Source Slug: `phase_03_parallel_wave_01_track_backend_compute_001_implement_compute_result_formula_engine_with_exact_decimal_handling`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0120](./pc_0120.md) `phase_03_parallel_wave_01_track_backend_collection_012_implement_artifact_contract_gate_and_validate_artifact_set_services`
- Next: [pc_0122](./pc_0122.md) `phase_03_parallel_wave_01_track_backend_compute_002_implement_forecast_set_generation_with_execution_mode_boundary_enforcement`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0118_to_0125.md](../shared_operating_contract_0118_to_0125.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_compute_001_implement_compute_result_formula_engine_with_exact_decimal_handling`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0120` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0017`, `pc_0028`, `pc_0060`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0074`, `pc_0077`, `pc_0097`, `pc_0101`, `pc_0117`, `pc_0118`, `pc_0119`, and `pc_0120`.

Mission:
Implement the `ComputeResult` model, repository, lifecycle helper, and deterministic formula engine that turns frozen canonical facts into reportable totals under exact decimal arithmetic.
This task must preserve reporting-scope truth, quarterly-basis truth, exact partition scope, adjustment-layer boundaries, deterministic contribution ordering, and canonical decimal-string outputs.
It must never use binary floating-point and must never let analysis-only flexibility contaminate compliance artifacts.

Source algorithm grounding you must honor:
- `core_engine.md`, especially post-seal `COMPUTE_OUTCOME`.
- `modules.md`, especially `COMPUTE_OUTCOME(...) / FORECAST(...)` and the `ComputeResult` lifecycle placement.
- `compute_parity_and_trust_formulas.md`, especially sections `8.4 Record-layer and adjustment-layer compute formulas`.
- `data_model.md`, especially `ComputeResult`.
- `state_machines.md`, especially section `6.9 ComputeResult.lifecycle_state`.
- `replay_and_reproducibility_contract.md`, especially canonical decimal-string and execution-basis requirements.
- `test_vectors.md`, especially `TV-01`, `TV-02`, `TV-03`, `TV-10`, and `TV-44F`.
- Existing schemas including `compute_result.schema.json`, `schema_bundle.schema.json#/$defs/moneyProfile`, and `sample_compute_result.json`.

Deliverables to create:
1. `packages/backend-compute/src/models/compute_result.ts`.
2. `packages/backend-compute/src/repositories/compute_result_repository.ts`.
3. `packages/backend-compute/src/services/compute_outcome.ts`.
4. `packages/backend-compute/src/services/transition_compute_result.ts`.
5. `packages/backend-compute/src/services/exact_decimal.ts`.
6. `packages/backend-compute/src/services/canonical_fact_contribution_order.ts`.
7. `packages/backend-compute/src/services/reporting_scope_resolver.ts`.
8. `packages/backend-compute/src/services/adjustment_inclusion_policy.ts`.
9. `packages/backend-compute/src/services/rule_evaluation_adapter.ts`.
10. `db/migrations/phase03_0121_compute_result.sql`.
11. `docs/compute/compute_result_formula_engine_with_exact_decimal_handling.md`.
12. `tests/unit/backend_compute/compute_outcome.spec.ts`.
13. `tests/integration/backend_compute/compute_result_persistence.spec.ts`.

Required design decisions you must make explicit:
- Define the exact in-repo exact-decimal strategy.
  Reuse an existing exact-decimal primitive if present; otherwise create one canonical adapter that preserves scale, rounding mode, canonical decimal-string serialization, and deterministic comparison semantics.
- Define the canonical contribution ordering within each `(business_partition, category, time window)` slice.
- Define how `reporting_scope(runtime_scope[])` resolves to the single schema-valid `reporting_scope` token without leaking amendment action posture into compute scope.
- Define how `eligible_compute_facts(mode)` is enforced for compliance versus analysis runs.
- Define the rule-evaluation adapter boundary.
  The algorithm names `RuleEvaluation(...)`, but the full rule library may not be materialized yet; you must make the adapter deterministic and injectable without inventing fake regulatory math.

Execution steps:
1. Implement the schema-backed `ComputeResult` model, repository, and lifecycle transitions.
2. Implement `exact_decimal.ts` and canonical contribution ordering utilities.
3. Implement `reporting_scope_resolver.ts` and `adjustment_inclusion_policy.ts` so compliance and analysis use the correct reporting and adjustment scope boundaries.
4. Implement `compute_outcome.ts` so it derives record totals, adjustment totals, quarterly/year-end totals, and the final ordered totals map through the rule-evaluation adapter.
5. Implement `rule_evaluation_adapter.ts` as a deterministic seam that consumes frozen totals/profile facts and emits an ordered outcome vector.
6. Add tests for periodic quarterly basis, cumulative quarterly basis, year-end adjustments, analysis-only provisional-fact allowance, and exact-decimal serialization stability.

Gap handling you must perform:
- Close the gap where the corpus specifies `RuleEvaluation(...)` but not the full implementation boundary.
  Create a deterministic adapter interface and typed outputs rather than burying rule logic in controllers or shipping stub math as if it were final regulatory truth.
- Close the gap where `ComputeResult.totals` and `assumptions` are open objects.
  Emit canonical ordered maps and explicit documented value-shape rules so replay and hashing stay stable.
- Close the gap where compliance and analysis can both see the same fact pool.
  Compliance must reject non-canonical compute inputs; analysis may include provisional facts only under explicit frozen policy.
- Close the gap where floating-point or locale formatting could perturb totals, thresholds, or replay.
  Persist canonical decimal strings only.

Mandatory edge cases:
- the same frozen input facts must produce identical totals, decimal strings, and diagnostic reason ordering across runtimes.
- missing required `(business_partition, category)` pairs must resolve to explicit zero rather than disappearing from the totals map.
- `quarterly_basis = PERIODIC` and `quarterly_basis = CUMULATIVE` must produce distinct, testable totals for the same underlying data.
- year-end compute must include adjustment totals only through the executable reporting scope and exact partition scope.
- compliance runs must reject provisional-only facts rather than silently computing with them.
- analysis runs that include provisional facts must remain `analysis_only = true`.
- `NOT_RUN -> RUNNING -> COMPUTED|BLOCKED` transitions must be legal and replay-safe.
- canonical ordering must not depend on database insertion order or worker shard order.
- no NaN, infinity, exponent form, or locale-formatted money strings may reach persistence.

Backend, performance, and security requirements:
- Keep compute deterministic, side-effect free except for explicit artifact persistence, and replay-safe.
- Preserve full exact-decimal intermediate precision until the declared aggregation boundary, then round once.
- Do not log raw fact payloads or sensitive evidence in compute diagnostics.
- Keep the rule adapter pure and injectable so later rule-library expansion does not force a rewrite of persistence or lifecycle code.

Validation rules:
- Unit-test exact-decimal arithmetic, contribution ordering, quarterly-basis differences, reporting-scope resolution, and lifecycle legality.
- Integration-test `ComputeResult` persistence from `InputFreeze`/`Snapshot`/`CanonicalFactSet` inputs through schema validation.
- Validate persisted artifacts against `compute_result.schema.json`.
- Verify canonical decimal-string serialization against `sample_compute_result.json` conventions and `TV-44F`.
- Re-run the contract validators after the implementation bundle is assembled.

Definition of done:
- The repo can synthesize and persist a schema-valid `ComputeResult` from frozen intake artifacts under exact decimal arithmetic.
- Compliance and analysis compute boundaries are explicit and cannot contaminate each other.
- Later forecast, risk, parity, trust, filing, and replay work can consume one deterministic compute basis.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
