---
id: pc_0071
slug: phase_02_seq_071_create_streaming_abstraction_for_sse_resume_rebase_and_monotonic_sequence_delivery
mode: sequential
phase: "02"
wave: null
track: null
task_number: "071"
block_key: phase_02_seq
original_key: phase_02_seq_071
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0071

## Task Summary

create streaming abstraction for sse resume rebase and monotonic sequence delivery.

## Metadata

- ID: `pc_0071`
- Mode: `sequential`
- Phase: `02`
- Wave: `n/a`
- Track: `n/a`
- Task Number: `071`
- Source Slug: `phase_02_seq_071_create_streaming_abstraction_for_sse_resume_rebase_and_monotonic_sequence_delivery`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0070](./pc_0070.md) `phase_02_seq_070_create_queue_abstraction_for_worker_dispatch_retries_and_dead_letter_flows`
- Next: [pc_0072](./pc_0072.md) `phase_02_seq_072_create_upload_blob_transfer_service_scaffold_with_checksum_and_resume_support`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0070_to_0077.md](../shared_operating_contract_0070_to_0077.md)
```text
Execute roadmap task `phase_02_seq_071_create_streaming_abstraction_for_sse_resume_rebase_and_monotonic_sequence_delivery`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0070` if they exist, especially `pc_0023`, `pc_0024`, `pc_0026`, `pc_0053`, `pc_0063`, `pc_0064`, `pc_0065`, `pc_0067`, `pc_0068`, and `pc_0070`.

Mission:
Build the shared streaming substrate that later northbound endpoints and frontend shells will use for experience and workspace continuity.
This task must preserve the corpus' resumable-SSE semantics, strict monotonic sequence rules, frame-epoch rebase behavior, session/access/masking drift handling, and catch-up-before-live delivery.
The result should let later APIs and shells stream lawful deltas without inventing their own cursor, resume, or rebase logic.

Source algorithm grounding you must honor:
- `northbound_api_and_session_contract.md`, especially `GET /experience/stream`, the `stream_recovery_contract{...}` boundary, typed event families, duplicate-delivery semantics, catch-up-before-live rules, and the fail-closed `REBASE_REQUIRED` / `ACCESS_REBIND_REQUIRED` posture.
- `core_engine.md`, especially `INIT_EXPERIENCE_STREAM(...)`, `frame_epoch`, `last_published_sequence`, deterministic resume token material, and the rule that epoch changes only when clients must rebase.
- `frontend_shell_and_interaction_law.md`, `cross_device_continuity_and_restoration_contract.md`, and `focus_restoration_and_return_target_harness_contract.md`, especially same-shell continuity, focus restoration, stale-view boundaries, and reconnect / resize / rebase survival rules.
- `schemas/stream_recovery_contract.schema.json`, `schemas/experience_stream_event.schema.json`, `schemas/workspace_stream_event.schema.json`, and `schemas/sample_stream_recovery_contract.json`.
- `cache_isolation_and_secure_reuse_contract.md` and `native_cache_hydration_purge_and_rebase_contract.md`, especially route/access/masking drift boundaries.
- Earlier command, object, queue, and cache work from `pc_0068`, `pc_0069`, and `pc_0070`.

Deliverables to create:
1. `packages/domain-kernel/src/streaming/stream_scope.ts`.
2. `packages/domain-kernel/src/streaming/stream_recovery.ts`.
3. `packages/domain-kernel/src/streaming/sse_frame_encoder.ts`.
4. `packages/domain-kernel/src/streaming/sequence_window.ts`.
5. `packages/domain-kernel/src/streaming/cursor_store.ts`.
6. `packages/domain-kernel/src/streaming/rebase_decider.ts`.
7. `config/streaming/stream_scope_catalog.json`.
8. `config/streaming/event_type_catalog.json`.
9. `config/streaming/rebase_trigger_matrix.json`.
10. `config/streaming/cursor_state_policy.json`.
11. `config/streaming/catch_up_delivery_policy.json`.
12. `docs/streaming/sse_resume_rebase_and_sequence_runbook.md`.
13. `apps/operator-web/src/routes/internal/stream-recovery-atlas.tsx`.
14. `tests/unit/streaming/stream_recovery_foundation.spec.ts`.
15. `tests/integration/streaming/sse_resume_rebase_flow.spec.ts`.
16. `tests/playwright/internal/stream_recovery_atlas.spec.ts`.

Required design decisions you must make explicit:
- Define stream scope classes and how experience streams differ from workspace streams without forking the recovery model.
- Define the exact cursor tuple: scope, route key, subject ref, epoch, last sequence, session/access/masking hashes, and publication generation.
- Define how SSE event framing maps to authoritative event objects and how transport-only `resume_token` differs from authoritative `stream_recovery_contract`.
- Define when the server can lawfully resume, when it must catch up, and when it must fail closed into `REBASE_REQUIRED` or `ACCESS_REBIND_REQUIRED`.
- Define heartbeat, compaction-floor, and duplicate-delivery handling without allowing sequence gaps or mixed-generation payload sets.
- Define how cursor state is stored, revoked, expired, rebased, or closed.
- Define how stream correlation context aligns with telemetry and later audit without overloading the stream channel with privileged data.
- Define how browser and native shells consume the abstraction without inventing shell-local resume rules.

Execution steps:
1. Create a machine-readable stream scope catalog and event-type catalog aligned to experience and workspace stream contracts.
2. Implement authoritative `stream_recovery_contract` helpers and cursor stores that preserve route, object, epoch, session, access, and masking truth.
3. Implement SSE frame encoding that emits typed events, comments / heartbeats where needed, and deterministic sequence metadata without broad payload duplication.
4. Implement catch-up and rebase logic that enforces strict monotonic gap-free delivery inside one epoch and fails closed when epoch or context truth drifts.
5. Implement duplicate-delivery idempotency helpers keyed exactly to the corpus' scope / epoch / sequence semantics.
6. Implement cursor-state rules for `REBASED`, `REVOKED`, `EXPIRED`, and `CLOSED`.
7. Write a runbook explaining how later northbound stream endpoints should compose this abstraction instead of hand-rolling SSE logic.
8. Build `stream-recovery-atlas` as a read-only continuity surface showing snapshot, catch-up, live flow, epoch advance, rebase, and access-rebind posture.
9. Add unit, integration, and Playwright tests for reconnect, duplicate delivery, compaction, epoch advance, access drift, and focus continuity hints.

Visual and interaction specification for `stream-recovery-atlas`:
- Tone: a premium epoch observatory.
  The page should feel like inspecting a calm continuity instrument, not a websocket debug panel.
- Layout:
  - `64px` top bar with stream-scope selector, route-key chip, and rebase-status badge.
  - left event rail `248-264px` listing `SNAPSHOT`, `CATCH-UP`, `LIVE`, `HEARTBEAT`, `REBASE`, `REVOKE`.
  - center ribbon canvas with one horizontal sequence ribbon per selected stream scope.
  - right inspector `376-404px` with epoch, sequence window, compaction floor, cursor state, and drift triggers.
- Diagram:
  - hero diagram is a thin ribbon timeline with numbered sequence markers and a visible epoch seam.
  - the seam appears as a subtle stitched fold; crossing it forces the UI into a rebase explanation panel.
  - use a custom monoline wave-ribbon glyph near the heading.
- Palette:
  - background `#F4F5F2`
  - primary surface `#FFFFFF`
  - secondary surface `#EEF0EC`
  - ink `#101418`
  - muted ink `#68717A`
  - hairline `rgba(16,20,24,0.08)`
  - accent blue `#44627A`
  - accent pine `#5C725E`
  - accent plum `#65596F`
  - success `#17614B`
  - warning `#8C5D1B`
  - danger `#A53A31`
- Typography:
  - quiet, precision-first, and typography-led; monospace only for sequence numbers, hashes, cursor ids, and event codes.
- Dimensional constraints:
  - max width `1480px`
  - ribbon row min height `68px`
  - sequence marker size `20px`
  - inspector section gap `16px`
- Motion:
  - `120ms` ribbon highlight and inspector reveal.
  - optional `160ms` sliding seam animation for epoch advance only when reduced motion is not preferred.
  - reduced motion removes seam travel and uses static contrast changes.
- Interaction:
  - selecting a sequence marker must reveal event type, snapshot / delta / terminal refs, and the governing recovery contract fields.
  - accessible names should read like `workspace stream sequence 21 in epoch 3 requires exact route session scope masking match`.
  - include a keyboard-reachable toggle to overlay duplicate-delivery and compaction-floor posture.
  - Playwright must use semantic locators and verify that no mixed-epoch dataset renders after a simulated rebase.

Gap handling you must perform:
- Close the gap where SSE transport details could be mistaken for authoritative stream recovery truth.
- Close the gap where sequence monotonicity and duplicate idempotency are specified in prose but not reusable in runtime helpers.
- Close the gap where reconnect logic could silently continue across access, masking, session, or route drift.
- Close the gap where stream catch-up and compaction posture could devolve into opaque client-side heuristics.
- Close the gap where later endpoints might each invent their own heartbeat, cursor, or rebase semantics.

Mandatory edge cases:
- duplicate delivery of the same scope / epoch / sequence tuple
- reconnect after the compaction floor moved past the requested sequence
- epoch advance while the connection remains open
- access binding drift without route drift
- masking drift that forces `ACCESS_REBIND_REQUIRED`
- browser reload during catch-up followed by live continuity
- cursor state transition from active to rebased, revoked, expired, or closed

Backend, performance, and security requirements:
- Keep stream state lightweight and correlation-rich; do not embed broad privileged payloads into transport frames when refs or typed deltas suffice.
- No manual sleeps or polling heuristics in tests; rely on Playwright's actionability-safe flow and deterministic stream test harnesses.
- Heartbeats must support connection liveness without acting as pseudo business events.
- Cursor writes must be idempotent and durable enough to survive reconnect / rebase flows.
- The abstraction must preserve low-noise shell continuity: no mixed-generation state sets and no hidden fallback to stale cached data.
- The atlas must explain stream law, not expose raw session secrets or internal tokens.

Validation rules:
- Unit-test sequence advancement, duplicate idempotency, rebase decider logic, and cursor-state transitions.
- Integration-test snapshot → catch-up → live progression, reconnect, compaction-floor failure, epoch advance, and access-rebind behavior.
- Use Playwright to verify semantic event rails, keyboard navigation, reduced-motion posture, and rebase explanation behavior in `stream-recovery-atlas`.
- Ensure the implementation matches the imported stream schemas and sample payloads exactly.
- Prove that a raw transport token alone is insufficient to resume when the governing recovery contract no longer matches.

Definition of done:
- The repo has one reusable streaming abstraction for lawful SSE delivery rather than endpoint-local stream logic.
- Resume, catch-up, duplicate handling, and rebase posture are explicit, serialized, and testable.
- Stream continuity aligns with shell continuity, cache isolation, and access-binding rules.
- Later stream endpoints can compose this layer without re-inventing cursor or SSE behavior.
- The internal atlas makes epoch and recovery behavior legible without turning into a generic network debugger.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
