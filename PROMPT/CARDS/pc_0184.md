---
id: pc_0184
slug: phase_04_parallel_wave_02_track_backend_portal_006_implement_client_onboarding_journey_projection_and_one_step_at_a_time_progression_rules
mode: parallel
phase: "04"
wave: "02"
track: "backend_portal"
task_number: "006"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_portal_006
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0184

## Task Summary

implement client onboarding journey projection and one step at a time progression rules.

## Metadata

- ID: `pc_0184`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend portal`
- Task Number: `006`
- Source Slug: `phase_04_parallel_wave_02_track_backend_portal_006_implement_client_onboarding_journey_projection_and_one_step_at_a_time_progression_rules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0183](./pc_0183.md) `phase_04_parallel_wave_02_track_backend_portal_005_implement_client_approval_pack_projection_and_stale_view_signoff_guards`
- Next: [pc_0185](./pc_0185.md) `phase_04_parallel_wave_02_track_backend_portal_007_implement_client_timeline_event_projection_and_customer_safe_activity_compression`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0182_to_0189.md](../shared_operating_contract_0182_to_0189.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_portal_006_implement_client_onboarding_journey_projection_and_one_step_at_a_time_progression_rules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0183` if they exist, especially `pc_0179`, `pc_0181`, and `pc_0183`.

Mission:
Implement the authoritative `ClientOnboardingJourney` projector and the step-progression services that keep onboarding guided, resumable, and deterministic.
This task must prevent the portal from degrading into a free-form wizard or reopening multiple writable steps at once.
The read side must always publish one primary onboarding step, one route-stable resume posture, one explicit terminal posture when onboarding is complete/expired/abandoned, and one clear transition back into the normal portal once the dedicated onboarding route is no longer valid.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially `Onboarding`, `Onboarding flow`, `Route stability across devices`, `Responsive fallback rules`, and the Playwright validation minimum for one-step-at-a-time progression and save-and-return continuity.
- `data_model.md`, especially `ClientOnboardingJourney`.
- `state_machines.md`, especially `ClientOnboardingJourney.lifecycle_state` and the related completion / expiry / abandonment rules.
- `modules.md`, especially `BUILD_CLIENT_ONBOARDING_STATE(...)` and `BUILD_CLIENT_PORTAL_WORKSPACE(...)`.
- `frontend_shell_and_interaction_law.md`, especially same-shell continuity, dominant-action law, and stale / recovery presentation.
- `schemas/client_onboarding_journey.schema.json`, `schemas/client_portal_workspace.schema.json`, and `schemas/problem_envelope.schema.json`.
- `PATCH_RESOLUTION_INDEX.md`, especially the onboarding hardening items covering resume-step validity, reconfirmation mapping, document draft continuity, and chronology drift.
- `test_vectors.md`, especially onboarding resume, reconfirmation, and terminal-state regression cases.

Deliverables to create:
1. `packages/backend-portal/src/projectors/build_client_onboarding_journey.ts`
2. `packages/backend-portal/src/services/derive_onboarding_resume_state.ts`
3. `packages/backend-portal/src/services/derive_onboarding_step_workspace_state.ts`
4. `packages/backend-portal/src/services/validate_onboarding_progression_and_chronology.ts`
5. `packages/backend-portal/src/services/build_onboarding_completion_summary.ts`
6. `packages/backend-portal/src/tests/client_onboarding_journey_projection.spec.ts`
7. `packages/backend-portal/src/tests/client_onboarding_route_progression.spec.ts`
8. `docs/portal/client_onboarding_journey_projection_and_progression.md`
9. Optional but recommended: `apps/client-portal-web/src/routes/debug/portal/onboarding-journey-lab.tsx`

You MUST patch any existing portal workspace builder or onboarding route read adapter so it consumes the new onboarding projector rather than rebuilding step order, resume semantics, or terminal posture in UI code.

Required design decisions you must make explicitly:
- define how `required_steps[]`, `completed_steps[]`, `current_step_code`, `resume_state`, `resume_step_code`, and `reconfirmation_step_codes[]` interact when requirements change after a saved draft exists
- define how `authority_link_requirement` and `authority_link_state` influence step order without reopening multiple writable steps
- define the exact route-removal and route-fallback behavior once onboarding becomes `COMPLETED`, `EXPIRED`, or `ABANDONED`
- define how document-upload draft continuity plugs into the onboarding projector without duplicating upload-session truth
- define the single source of truth for terminal exit reasons and completion summary publication

Execution requirements:
- publish a schema-valid `ClientOnboardingJourney` for every active or terminal onboarding artifact
- keep route order and semantics exact: `WELCOME_PANEL -> ONBOARDING_STEPPER -> STEP_WORKSPACE -> SUPPORT_PANEL`
- ensure only one required onboarding step is primary at a time
- make `STEP_WORKSPACE` render one mode only: active-step work, reconfirmation review, stale-review recovery, completion summary, or terminal exit/support handling
- preserve save-and-return continuity for the current step, entered answers, and any in-progress document-upload sessions
- keep terminal onboarding states explicit with timestamps, completion summary or abandonment reason, and `what happens next` guidance
- once onboarding is terminal, remove the dedicated onboarding tab and transition back into the ordinary portal shell rather than preserving a dead wizard shell
- if you add the lab, make it a calm step rail with one active card and bounded completed summaries; no endless wizard stack

Gap handling you must close while implementing:
- close any gap where `resume_step_code` can target a step outside frozen `required_steps[]`
- close any gap where `resume_state = LIVE` can reopen an already completed step
- close any gap where reconfirmation posture does not identify which steps actually need reconfirmation
- close any gap where document-upload draft continuity can persist outside `DOCUMENTS_PENDING`
- close any gap where completion, expiry, or abandonment lacks durable terminal timestamps and clear transition posture

Mandatory edge cases:
- `resume_step_code` must remain inside `required_steps[]` whenever non-null
- `resume_state = LIVE` must not target an already completed step
- `resume_state = RECONFIRMATION_REQUIRED` must use a step listed in `reconfirmation_step_codes[]`
- draft upload sessions may persist only while `lifecycle_state = DOCUMENTS_PENDING`
- when draft upload sessions exist, `current_step_code` and `resume_step_code` must both remain `DOCUMENT_COLLECTION`
- `completed_at` must not predate `invited_at` or `state_changed_at`
- `expired_at` must not predate `invited_at`, `state_changed_at`, or `expires_at`
- `abandoned_at` must not predate `invited_at` or `state_changed_at`
- later requirement changes must not silently mutate a previously completed journey into a half-open wizard; reopen explicitly or mint a new journey as the source algorithm requires

Playwright and contract-test requirements:
- add direct tests for resume-state derivation, reconfirmation mapping, document-draft continuity, and terminal-state chronology
- if the onboarding route is already shipped, add Playwright APIRequestContext or route-level tests that verify stale workspace rejection, current-step publication, and terminal route removal semantics
- if you add the lab, add Playwright browser tests for one-primary-step rendering, save-and-return, rebased reconfirmation, and terminal completion / expiry / abandonment posture using semantic locators only
- capture traces or serialized onboarding payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- max width `1120px`, background `#F6F4EE`, primary surface `#FFFFFF`, support surface `#F3F4F6`
- one active step card centered in the reading path, one concise vertical step rail, one support panel collapsed below on narrow layouts
- the active step should feel premium and contained, with generous spacing and clear labels for why the step exists and what happens next
- completed steps collapse into compact summary rows; do not keep full completed forms open
- support remains visibly subordinate unless the journey is expired or abandoned
- motion is limited to 160ms opacity / height transitions with reduced motion honored

Backend, performance, and security requirements:
- keep step order, reconfirmation, and completion legality server-authored and deterministic
- do not let browser-local progress or cached form position invent onboarding truth
- keep terminal states append-only and auditable; do not reopen terminal journeys in place unless the governing algorithm allows it explicitly
- role filtering and client scope binding must happen before serialization

Validation rules:
- validate every emitted `ClientOnboardingJourney` against schema
- add regression coverage for save-and-return, requirement rebase, terminal chronology, document-draft continuity, and route-removal semantics
- keep the onboarding projector reusable by the broader `ClientPortalWorkspace` builder

Definition of done:
- the repo has one authoritative onboarding projector that preserves one-primary-step semantics
- save-and-return, reconfirmation, stale-review, and terminal exit posture become governed backend truth instead of local UI memory
- onboarding now transitions into and out of the client portal shell without route drift or wizard-state ambiguity
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
