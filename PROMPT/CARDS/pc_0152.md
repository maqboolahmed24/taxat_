---
id: pc_0152
slug: phase_03_parallel_wave_01_track_backend_workflow_008_implement_customer_safe_projection_and_visibility_partition_contract_enforcement
mode: parallel
phase: '03'
wave: '01'
track: backend_workflow
task_number: 008
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_workflow_008_implement_customer_safe_projection_and_visibility_partition_contract_enforcement
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0152

## Task Summary

customer safe projection and visibility partition contract enforcement.

## Metadata

- ID: `pc_0152`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend workflow`
- Task Number: `008`
- Source Slug: `phase_03_parallel_wave_01_track_backend_workflow_008_implement_customer_safe_projection_and_visibility_partition_contract_enforcement`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0151](./pc_0151.md) `phase_03_parallel_wave_01_track_backend_workflow_007_implement_work_inbox_snapshot_and_work_inbox_delta_projectors`
- Next: [pc_0153](./pc_0153.md) `phase_03_parallel_wave_01_track_backend_workflow_009_implement_remediation_task_accepted_risk_approval_and_compensation_record_models`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0150_to_0157.md](../shared_operating_contract_0150_to_0157.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_workflow_008_implement_customer_safe_projection_and_visibility_partition_contract_enforcement`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0151` if they exist, especially `pc_0040`, `pc_0041`, `pc_0044`, `pc_0084`, `pc_0148`, `pc_0149`, `pc_0150`, and `pc_0151`.

Mission:
Implement one canonical enforcement layer for `customer_safe_projection` and `visibility_partition` across workflow-owned read models and notification-open surfaces.
This card must make customer-safe publication a fail-closed, schema-backed backend boundary rather than a route naming convention or a front-end filtering afterthought.

Source algorithm grounding you must honor:
- `customer_client_portal_experience_contract.md`, especially `customer_safe_projection` rules, banned field families, portal-safe action/status posture, and route continuity.
- `collaboration_workspace_contract.md`, especially customer-visible workspace, request-list, activity, attachment, and notification-open rules.
- `frontend_shell_and_interaction_law.md`, especially explicit visibility-partition and customer-safe-projection requirements for route-visible models.
- `cache_isolation_and_secure_reuse_contract.md`.
- `cross_device_continuity_and_restoration_contract.md` where visibility cache partitioning affects hydration / restoration.
- `authority_truth_and_internal_projection_separation_contract.md` where customer-facing projection must not masquerade as legal confirmation.
- `schemas/customer_safe_projection_contract.schema.json` plus every workflow or portal schema that embeds `visibility_partition` and/or `customer_safe_projection`.
- validator drift cases for workspace, request-list, activity, attachment, and notification customer-safe boundary alignment.

Deliverables to create:
1. `packages/backend-workflow/src/contracts/enforce_visibility_partition.ts`
2. `packages/backend-workflow/src/contracts/enforce_customer_safe_projection.ts`
3. `packages/backend-workflow/src/contracts/enforce_customer_safe_projection_and_visibility_partition.ts`
4. `packages/backend-workflow/src/contracts/strip_internal_only_projection_fields.ts`
5. `packages/backend-workflow/src/contracts/validate_customer_safe_action_contract.ts`
6. `packages/backend-workflow/src/contracts/validate_portal_same_shell_navigation_contract.ts`
7. `packages/backend-workflow/src/services/build_customer_safe_projection_contract.ts`
8. `packages/backend-workflow/src/services/build_visibility_partition_contract.ts`
9. `packages/backend-workflow/src/services/assert_customer_safe_projection_alignment.ts`
10. `docs/workflow/customer_safe_projection_and_visibility_partition_enforcement.md`
11. `tests/unit/backend_workflow/customer_safe_projection_contract.spec.ts`
12. `tests/unit/backend_workflow/visibility_partition_contract.spec.ts`
13. `tests/integration/backend_workflow/customer_safe_boundary_enforcement_flow.spec.ts`
14. `tests/playwright/admin_console/customer_safe_boundary_inspector.spec.ts`

Optional diagnostic harness if it materially speeds up verification:
15. `apps/admin-console-web/src/routes/debug/customer-safe-boundary-inspector.tsx`
16. `apps/shared-ui/src/components/debug/CustomerSafeBoundaryInspector.tsx`

Required design decisions you must make explicit:
- Define exactly which artifact families require non-null `customer_safe_projection` and which MUST keep it `null`.
- Define the canonical relationship between:
  - `visibility_partition.access_binding_hash`
  - `visibility_partition.masking_posture_fingerprint`
  - `visibility_partition.cache_partition_key`
  - `customer_safe_projection.access_binding_hash`
  - `customer_safe_projection.masking_posture_fingerprint`
  - `customer_safe_projection.visibility_cache_partition_key`
- Define field-stripping / remapping rules for staff-only families including assignment, escalation, raw gate state, staff reason codes, audit lineage, internal activity, internal attachments, internal participants, internal counts, and staff route context.
- Define how customer-safe `authoritative_action{...}` and recovery routing stay in the portal same-shell family only.
- Define fail-closed behavior for mismatched binding hashes, masking fingerprints, visibility classes, or forbidden internal field families.

Execution steps:
1. Implement one reusable contract-enforcement module that every workflow projector can call before publishing customer-visible output.
2. Implement explicit field-family stripping and validation for `WorkspaceSnapshot`, `CustomerRequestListSnapshot`, `CollaborationActivitySlice`, attachment slices, and `WorkItemNotification` payloads.
3. Build validators that compare the visible artifact, the `visibility_partition`, and the `customer_safe_projection` contract as one coupled boundary.
4. Wire the enforcement layer into existing projector / notification code paths rather than duplicating route-specific redaction helpers.
5. If useful for fast verification, add a read-only diagnostic inspector that shows stripped field families, preserved field families, and the active binding hashes.
6. Use Playwright to verify that the inspector or preview surface presents exactly the allowed families and never reveals blocked internal families.

Gap handling you must perform:
- Close the gap where customer-visible projection could be inferred from shell family or route prefix rather than an explicit backend-authored boundary contract.
- Close the gap where different projectors could strip different subsets of internal fields and silently diverge.
- Close the gap where cache partition keys, access-binding hashes, and masking fingerprints could drift between `visibility_partition` and `customer_safe_projection`.
- Close the gap where customer-visible routes could still point at staff-only module codes, fallback routes, or hidden activity semantics.

Mandatory edge cases:
- a customer-visible `WorkspaceSnapshot` with non-null `internal_head_sequence_or_null` MUST fail closed
- a customer-visible `CollaborationActivitySlice` with `thread_visibility_class = INTERNAL_ONLY` MUST fail closed
- a `CustomerRequestListSnapshot` with mismatched `customer_safe_projection.access_binding_hash` or `visibility_cache_partition_key` MUST fail closed
- customer-visible notifications MUST keep `notification_navigation_policy = PORTAL_SAME_SHELL_AND_VISIBILITY_ONLY`
- internal-only artifacts MUST NOT publish a fake customer-safe contract merely to satisfy a common interface
- current-vs-history artifact posture MUST remain lawful after stripping internal-only history and placeholders

Optional diagnostic harness UI specification if you create it:
- use a two-column comparison layout: candidate internal payload on the left, customer-safe published payload on the right
- show binding hashes, masking fingerprint, and cache partition key in a monospace summary rail
- render stripped field families as grouped chips, not as a noisy diff wall
- keep the surface read-only and internal-only
- selectors:
  - `customer-safe-boundary-inspector`
  - `candidate-internal-payload`
  - `published-customer-safe-payload`
  - `stripped-field-family-chip`
  - `visibility-binding-summary`

Playwright requirements if a diagnostic surface is created:
- use Playwright during implementation and verification
- semantic locators only
- no fixed sleeps
- capture traces on retry / failure
- prove that forbidden internal field families never render in the published customer-safe side

Backend, performance, and security requirements:
- fail closed on any binding mismatch or forbidden-field leakage
- preserve one canonical implementation; route-specific copies are forbidden
- make the enforcement layer pure, deterministic, and replay-safe
- do not allow fallback routes, focus anchors, or notification-open targets to widen customer visibility beyond the published contract

Validation rules:
- validate the `customer_safe_projection` contract and all embedding artifacts against their schemas
- unit-test binding alignment, field-family stripping, route-family restrictions, and action-contract restrictions
- integration-test end-to-end customer-safe publication from internal candidate objects through final route-visible payloads
- if a harness exists, Playwright-test that it never exposes forbidden families and that binding hashes match the published payload

Definition of done:
- the repo contains one canonical enforcement layer for customer-safe projection and visibility partitioning
- all workflow-owned customer-visible artifacts now share the same fail-closed boundary semantics
- customer-visible projections can no longer drift or leak internal-only posture by route or renderer convention
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
