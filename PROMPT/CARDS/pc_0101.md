---
id: pc_0101
slug: phase_03_parallel_wave_01_track_backend_manifest_005_implement_resolve_config_freeze_config_and_materialize_cfg_from_freeze_modules
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "005"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_005
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0101

## Task Summary

implement resolve config freeze config and materialize cfg from freeze modules.

## Metadata

- ID: `pc_0101`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `005`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_005_implement_resolve_config_freeze_config_and_materialize_cfg_from_freeze_modules`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0100](./pc_0100.md) `phase_03_parallel_wave_01_track_backend_manifest_004_implement_schema_bundle_schema_reader_window_and_backfill_execution_models`
- Next: [pc_0102](./pc_0102.md) `phase_03_parallel_wave_01_track_backend_manifest_006_implement_load_and_validate_prior_manifest_context_and_reuse_decision_modules`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0094_to_0101.md](../shared_operating_contract_0094_to_0101.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_005_implement_resolve_config_freeze_config_and_materialize_cfg_from_freeze_modules`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0100` if they exist, especially `pc_0063`, `pc_0064`, `pc_0074`, `pc_0097`, `pc_0099`, and `pc_0100`.

Mission:
Implement the config-resolution and frozen-config materialization modules that turn governed config models into an executable manifest-bound basis.
This task must make fresh resolution, exact reuse, historical explicit reuse, and worker-side consumption explicit and deterministic.
It must preserve the rule that downstream execution consumes frozen config only and never falls back to live config after seal.

Source algorithm grounding you must honor:
- `modules.md`, especially `RESOLVE_CONFIG(...)`, `FREEZE_CONFIG(...)`, `LOAD_CONFIG_FREEZE(...)`, `CONTINUATION_REUSES_FROZEN_CONFIG(...)`, `MATERIALIZE_CFG_FROM_FREEZE(...)`, and `RESOLVE_CONFIG_FOR_REQUEST(...)`.
- `manifest_and_config_freeze_contract.md`, especially `5.4 ConfigFreeze contract`, `5.5 Freeze timing rules`, `5.9 Hash contract`, and the freeze-before-decision / freeze-before-compute rules.
- `config_freeze_inheritance_and_consumption_contract.md`, especially basis mapping, source-lineage rules, cross-layer mirrors, and the completeness barrier.
- `state_machines.md`, especially `ConfigVersion.lifecycle_state` rules that constrain which versions are legal for new compliance-capable runs.
- `replay_and_reproducibility_contract.md`, especially immutable execution basis and exact replay semantics.
- Existing schemas including `config_freeze.schema.json`, `schema_bundle.schema.json`, and any manifest-bound schema/window contracts that the resolved config must surface.

Deliverables to create:
1. `packages/backend-manifest/src/services/resolve_config.ts`.
2. `packages/backend-manifest/src/services/freeze_config.ts`.
3. `packages/backend-manifest/src/services/load_config_freeze.ts`.
4. `packages/backend-manifest/src/services/continuation_reuses_frozen_config.ts`.
5. `packages/backend-manifest/src/services/materialize_cfg_from_freeze.ts`.
6. `packages/backend-manifest/src/services/resolve_config_for_request.ts`.
7. `packages/backend-manifest/src/services/config_completeness_validator.ts`.
8. `packages/backend-manifest/src/services/config_basis_mapper.ts`.
9. `config/manifest/required_config_types_catalog.json`.
10. `docs/manifest/resolve_and_materialize_frozen_config.md`.
11. `tests/unit/backend_manifest/resolve_config_modules.spec.ts`.
12. `tests/integration/backend_manifest/config_freeze_reuse_and_materialization.spec.ts`.

Required design decisions you must make explicit:
- Define the executable runtime `cfg` shape that `materialize_cfg_from_freeze.ts` returns, and how it references frozen config ids/hashes instead of re-reading live config.
- Define the catalog of required config types and top-level profile refs needed to satisfy `COMPLETE_REQUIRED_CONFIG_SET`.
- Define the exact mapping from `continuation_set.config_inheritance_mode` to `ConfigFreeze.config_resolution_basis` and how that mapping is recorded for root, replay, recovery, historical-explicit, and fresh-child cases.
- Define how feature-flag snapshot posture participates in fresh resolution and in frozen materialization.
- Define how schema-bundle identity and schema-reader window posture are attached to the resolved config basis.
- Define how direct fresh resolution remains distinguishable from exact reuse even when hashes happen to match an earlier freeze.

Execution steps:
1. Implement `resolve_config.ts` to select the legally usable config versions for a requested run context, obeying compliance-only versus replay/historical carve-outs.
2. Implement `freeze_config.ts` to build a first-class `ConfigFreeze` with ordered entries, required profile refs, `config_freeze_hash`, `config_surface_hash`, and explicit `config_resolution_basis`.
3. Implement `load_config_freeze.ts` for replay or continuation-safe reload of an existing freeze.
4. Implement `continuation_reuses_frozen_config.ts` so the reuse decision returns both boolean reuse posture and typed inheritance mode exactly as the corpus requires.
5. Implement `materialize_cfg_from_freeze.ts` so workers receive an executable config view derived entirely from `ConfigFreeze` and its attached schema/feature-flag context.
6. Implement `resolve_config_for_request.ts` to orchestrate fresh resolution versus inherited reuse without silently widening later-card scope into full prior-manifest branch decisioning.
7. Add tests proving direct resolution, replay exact reuse, recovery exact reuse, historical explicit reuse, and fresh child resolution remain typed and deterministic.

Gap handling you must perform:
- Close the gap where `ConfigFreeze` exists as a model but no executable modules consume it as the only lawful runtime basis.
- Close the gap where exact reuse could be guessed from hash coincidence instead of declared through typed inheritance mode and basis mapping.
- Close the gap where workers or replay loaders might silently fall back to live config after seal.
- Close the gap where partial config freezes or missing profile refs could slip into runtime.
- Close the gap where feature flags or schema-bundle identity are omitted from the frozen config basis even though they materially change behavior.

Mandatory edge cases:
- root manifest uses direct fresh resolution and all `source_config_*` fields remain null
- replay child uses `REPLAY_EXACT` and must preserve exact source freeze/hash/surface identity
- recovery child uses `RECOVERY_EXACT` and must preserve exact source freeze/hash/surface identity
- historical explicit child uses `HISTORICAL_EXPLICIT` and cannot masquerade as fresh resolution
- fresh child resolution happens to produce the same content hashes as a previous freeze but still remains typed as direct resolution
- new compliance-capable request encounters only deprecated/revoked config versions and must fail closed
- `materialize_cfg_from_freeze.ts` receives a freeze missing required config types or profile refs and must reject it
- worker attempts to consume a freeze whose `config_consumption_mode` is not `FROZEN_CONFIG_ONLY`
- `schema_bundle_hash`, `feature_flag_snapshot_hash`, or `config_surface_hash` drift between `ConfigFreeze` and the worker-facing frozen packet

Backend, performance, and security requirements:
- Config resolution and materialization must be deterministic and side-effect free except for explicit persistence.
- No live config or live environment lookup is allowed after the freeze is built or loaded.
- The returned runtime `cfg` must carry enough lineage for replay, auditing, and stale-guard comparison without exposing secrets.
- Materialization should be efficient enough for manifest allocation and replay entry paths.
- Keep the services ready for later manifest-branch and start-claim cards without prematurely absorbing their logic here.

Validation rules:
- Unit-test basis mapping, completeness validation, hash equality versus typed basis distinction, and worker-side materialization.
- Integration-test fresh resolution, replay reuse, recovery reuse, historical explicit reuse, and fail-closed behavior on incomplete or unlawful config states.
- Validate persisted or emitted `ConfigFreeze` payloads against the schema bundle.
- Prove that downstream runtime code can reconstruct executable config solely from the frozen artifact.
- Prove that exact replay and recovery preserve the exact frozen config identity while fresh child resolution stays explicitly distinct.

Definition of done:
- The repo has executable modules for resolving, freezing, loading, and materializing governed config.
- Workers and later manifest orchestration can consume manifest-bound frozen config without live fallback.
- Replay and continuation semantics gain typed config-basis truth before broader branch-decision logic is introduced.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
