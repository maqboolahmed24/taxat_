---
id: pc_0189
slug: phase_04_parallel_wave_02_track_backend_governance_001_implement_tenant_governance_snapshot_projector_and_dominant_attention_summary
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "001"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0189

## Task Summary

implement tenant governance snapshot projector and dominant attention summary.

## Metadata

- ID: `pc_0189`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `001`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_001_implement_tenant_governance_snapshot_projector_and_dominant_attention_summary`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0188](./pc_0188.md) `phase_04_parallel_wave_02_track_backend_portal_010_implement_artifact_affordance_contract_for_preview_export_print_and_browser_handoff`
- Next: [pc_0190](./pc_0190.md) `phase_04_parallel_wave_02_track_backend_governance_002_implement_governance_policy_snapshot_projector_and_environment_binding_views`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0182_to_0189.md](../shared_operating_contract_0182_to_0189.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_001_implement_tenant_governance_snapshot_projector_and_dominant_attention_summary`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0188` if they exist, especially the access / authority groundwork from `pc_0086` through `pc_0093`, the northbound read work from `pc_0160` and `pc_0166`, and the shell / interaction foundations from `pc_0174` through `pc_0178`.

Mission:
Implement the first governance overview read model: `TenantGovernanceSnapshot`.
This projector must produce one dominant question, one promoted worklist, one stable selected canvas object, one bounded risk ledger, and one explicit support-sidecar posture for `/governance` without degenerating into a generic KPI dashboard.
The dominant attention summary is governed math, not a design flourish.
It must follow the family-score and hysteresis rules from the algorithm, keep every top-level count bound to a concrete worklist, and preserve same-object same-shell continuity across responsive collapse, filter changes, reconnect, and browser refresh.

Source algorithm grounding you must honor:
- `admin_governance_console_architecture.md`, especially `/governance` overview layout, `OVERVIEW_ATTENTION_SUMMARY`, `GovernanceRiskLedger`, filter rules, selected-object rules, and the family-score / hysteresis rules.
- `data_model.md`, especially `TenantGovernanceSnapshot` and `GovernancePolicySnapshot` so you do not accidentally swallow the later policy-route task.
- `modules.md`, especially `BUILD_TENANT_GOVERNANCE_SNAPSHOT(...)`.
- `dominant_question_and_single_action_contract.md`, especially the governance-density shell mapping.
- `frontend_shell_and_interaction_law.md`, especially dominant-action hierarchy, promoted support surfaces, stale/recovery presentation, and automation anchors.
- `low_noise_experience_contract.md` only for the shared restraint / hierarchy mindset; governance is not the calm shell, but it still must obey one-dominant-question law.
- `schemas/tenant_governance_snapshot.schema.json`, `schemas/governance_interaction_layer.schema.json`, `schemas/shell_dominance_contract.schema.json`, `schemas/shell_state_taxonomy_contract.schema.json`, `schemas/cross_device_continuity_contract.schema.json`, and `schemas/semantic_accessibility_contract.schema.json`.
- `scripts/validate_contracts.py`, especially `validate_tenant_governance_snapshot` and the attention / ledger / filter / support alignment checks.
- `PATCH_RESOLUTION_INDEX.md`, especially the governance overview and stale-guard hardening entries.

Deliverables to create:
1. `packages/backend-governance/src/projectors/build_tenant_governance_snapshot.ts`
2. `packages/backend-governance/src/services/derive_governance_family_scores.ts`
3. `packages/backend-governance/src/services/derive_governance_attention_summary.ts`
4. `packages/backend-governance/src/services/build_governance_risk_ledger.ts`
5. `packages/backend-governance/src/services/build_governance_overview_filters.ts`
6. `packages/backend-governance/src/tests/tenant_governance_snapshot_projection.spec.ts`
7. `packages/backend-governance/src/tests/tenant_governance_attention_hysteresis.spec.ts`
8. `docs/governance/tenant_governance_snapshot_and_dominant_attention.md`
9. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/overview-lab.tsx`

You MUST patch any existing governance overview read endpoint so it consumes this projector rather than composing attention summaries, counts, or worklist targets in controllers or frontend code.

Required design decisions you must make explicitly:
- define the durable source for `previous_primary_family` and how hysteresis is evaluated against the prior still-live snapshot
- define how each governance family produces `critical_open_count`, `open_count`, `oldest_open_age_hours`, `requires_operator_action`, and `noise_penalty`
- define the exact mapping from promoted family to `primary_queue_code`, `primary_worklist_ref`, and `attention_summary.attention_family`
- define when the snapshot becomes `CALM` with `NO_SAFE_ACTION`
- define how `support_region_state` and `selected_canvas_object_ref` stay aligned when audit or diff sidecars are open

Execution requirements:
- publish a schema-valid `TenantGovernanceSnapshot`
- emit one dominant question, one dominant action posture, and one promoted worklist or explicit no-safe-action state
- compute family score exactly as the algorithm requires:
  `family_score(f) = family_base(f) + 25*critical_open_count(f) + 6*min(open_count(f), 9) + 4*oldest_age_bucket(f) + 10*requires_operator_action(f) + 8*1[f = previous_primary_family] - 8*noise_penalty(f)`
- keep `family_base(PENDING_APPROVALS)=500`, `family_base(CONFIGURATION_DRIFT)=420`, `family_base(AUTHORITY_LINK_RISK)=380`, `family_base(RETENTION_EXCEPTION)=340`, `family_base(AUDIT_HOTSPOT)=300`
- keep `oldest_age_bucket(f) = min(floor(oldest_open_age_hours(f) / 8), 6)`
- preserve hysteresis: when the leading family does not exceed the prior still-live family by at least `20`, keep the prior family dominant
- serialize all five governance families in the ledger with the promoted family first and the remainder in canonical order
- keep every count worklist-backed; no decorative KPI counts
- keep `active_filters` route-stable and sorted where the validator expects deterministic ordering
- keep `selected_canvas_object_ref` and `focus_anchor_ref` explicit and stable through responsive collapse and reconnect
- if you add the lab, follow the governance overview architecture exactly: `GOVERNANCE_CONTEXT_BAR`, `INVENTORY_RAIL`, `WORKSPACE_CANVAS`, `AUDIT_SIDECAR`

Gap handling you must close while implementing:
- close any gap where dominant-attention selection is hard-coded instead of formula-driven
- close any gap where counts can be shown without a concrete worklist ref
- close any gap where `support_region_state.selected_object_ref` can drift away from `selected_canvas_object_ref`
- close any gap where active filter chips are not mirrored into the interaction layer or where risk-family filters exclude the promoted queue
- close any gap where the governance overview becomes a badge wall rather than one dominant worklist plus bounded supporting stacks

Mandatory edge cases:
- `primary_worklist_ref` must match the queue-specific top-level worklist field for the promoted queue
- non-empty `active_filters.risk_families[]` must include the promoted `primary_queue_code`
- `risk_ledger_entries[].queue_code` must be unique
- `risk_ledger_entries[]` must list the promoted queue first and the rest in canonical order
- each `risk_ledger_entries[].worklist_ref` must match the top-level queue worklist ref
- each `risk_ledger_entries[].open_count` must mirror the corresponding top-level count projection
- `attention_summary.attention_family` must align with `primary_queue_code`
- non-calm `attention_summary.primary_worklist_ref` must match the top-level `primary_worklist_ref`
- the first risk-ledger row must match `attention_summary` for `attention_family`, `affected_scope_label`, and `next_legal_action_label`
- `authority_link_risk_refs[]` and `retention_exception_refs[]` must clear when their top-level counts are zero and must never exceed those counts
- `pending_change_refs[]` must be a subset of `recent_change_refs[]`
- `dominance_contract.safe_action_state` and `dominant_action_ref_or_null` must mirror whether `attention_summary` exposes one actionable primary worklist

Playwright and contract-test requirements:
- add direct tests for family-score computation, hysteresis retention, ledger ordering, count / worklist alignment, filter sorting, and support-object alignment
- if a governance overview route already exists, add Playwright APIRequestContext tests for snapshot JSON, cache isolation, continuity contracts, and semantic accessibility contract publication
- if you add the lab, add Playwright browser tests that verify promoted queue ordering, filter chip persistence, sidecar subordination, and same-object continuity through wide/narrow layouts using semantic locators only
- capture traces or serialized snapshot payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- do not build a KPI dashboard or tile wall
- max width `1440px`, background `#F7F5F1`, primary surface `#FFFFFF`, muted sidecar `#F1F3F0`
- keep one dominant `ATTENTION_SUMMARY` block at the top of the canvas with `why now`, `affected scope`, and `next legal action`
- show the supporting families as bounded list/ledger stacks, not as competing hero cards
- the filter rail should be narrow, explicit, and stable; chip state must mirror server-authored active filters
- the audit sidecar stays support-only and aligned to the selected object
- if a diagram helps, use one family-score ladder or one compact hysteresis explainer table; no donut or pie charts
- motion stays subtle and reduced-motion-safe

Backend, performance, and security requirements:
- keep the snapshot strictly read-side; it must never be mistaken for mutation truth or approval authority
- derive counts and family scores from durable governance state, not from browser-local cache or presentation state
- index the promoted families and worklists so overview reads remain fast under tenant scale
- keep cache isolation, continuity, accessibility, and interaction-layer publication aligned to the governance shell contracts

Validation rules:
- validate every emitted `TenantGovernanceSnapshot` and all nested shell contracts against schema
- add regression coverage for dominant-family thrash suppression, ledger alignment, filter / selection drift, and support sidecar subordination
- keep this task limited to the overview route; do not swallow the later policy snapshot or access simulation routes

Definition of done:
- the repo has one authoritative governance overview projector with formula-driven dominant attention and hysteresis
- every top-level governance count is worklist-backed, filter-stable, and same-shell continuous
- the governance overview becomes a premium, high-trust control-plane surface instead of a generic dashboard
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
