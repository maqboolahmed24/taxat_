---
id: pc_0251
slug: phase_05_parallel_wave_03_track_frontend_low_noise_011_Playwright_or_other_appropriate_tooling_implement_same_object_route_continuity_and_back_return_behavior
mode: parallel
phase: "05"
wave: "03"
track: "frontend_low_noise"
task_number: "011"
block_key: phase_05_parallel_wave_03
original_key: phase_05_parallel_wave_03_track_frontend_low_noise_011
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0251

## Task Summary

011 Playwright or other appropriate tooling implement same object route continuity and back return behavior.

## Metadata

- ID: `pc_0251`
- Mode: `parallel`
- Phase: `05`
- Wave: `03`
- Track: `frontend low noise`
- Task Number: `011`
- Source Slug: `phase_05_parallel_wave_03_track_frontend_low_noise_011_Playwright_or_other_appropriate_tooling_implement_same_object_route_continuity_and_back_return_behavior`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0250](./pc_0250.md) `phase_05_parallel_wave_03_track_frontend_low_noise_010_Playwright_or_other_appropriate_tooling_implement_low_noise_budget_audit_overlay_and_developer_diagnostics`
- Next: [pc_0252](./pc_0252.md) `phase_05_parallel_wave_03_track_frontend_low_noise_012_Playwright_or_other_appropriate_tooling_implement_operator_interaction_layer_quick_actions_and_stale_guard_affordances`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.
- The shared operating contract for this tranche: [../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md).

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, changed paths, and verification evidence in this card.
- Keep task status out of the card body; status lives only in [../Checklist.md](../Checklist.md).

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0250_to_0269.md](../shared_operating_contract_0250_to_0269.md)
```text
Execute roadmap task `phase_05_parallel_wave_03_track_frontend_low_noise_011_Playwright_or_other_appropriate_tooling_implement_same_object_route_continuity_and_back_return_behavior`.

Inherit the shared operating contract and consume outputs from cards `pc_0229` through `pc_0250` if they exist. If a named dependency is absent, create the smallest compatible seam and record an explicit assumption in this card instead of inventing a divergent architecture.

Mission:
Implement same-object route continuity and back/return behavior for the low-noise operator shell so deep links, module switches, artifact handoffs, stale rebases, and browser back navigation keep the same object and lawful focus anchor.
This task is not allowed to create a generic dashboard, generic SPA utility, route-local heuristic, customer-unsafe projection, or visually impressive but contract-drifting UI.

Core source-algorithm analysis:
The source algorithm treats route continuity as a domain contract, not router convenience. For the same manifest or work object, route changes must preserve shell family, object anchor, active module, focus anchor, and parent return target; browser back cannot remount a different layout metaphor or drop the user into a generic list. This task must bind the UI router to `cross_device_continuity_contract` and `focus_restoration_contract` instead of reconstructing return behavior from URL crumbs or history stack shape.
Treat this interpretation as a constraint: if the implementation discovers repository facts that conflict with it, resolve the conflict by returning to the source algorithm and schemas, then record the resolution as an assumption or gap closure.

Source algorithm grounding you must honor:
- `frontend_shell_and_interaction_law.md` sections 1 and 2, especially same object/same shell, stable route keys, deep-link restoration, back/return behavior, and queue/inbox continuity.
- `low_noise_experience_contract.md` shell continuity, constrained layouts, artifact handoff, edge-tolerant object permanence, and responsive/narrow-screen continuity.
- `cross_device_continuity_and_restoration_contract.md` required rules and surface mapping.
- `focus_restoration_and_return_target_harness_contract.md` required rules and failure modes.
- `schemas/cross_device_continuity_contract.schema.json`, `focus_restoration_contract.schema.json`, `focus_restore_return_target_harness.schema.json`, `route_stability_contract.schema.json`.

Deliverables to create or patch:
1. `packages/frontend-shell-core/src/routing/same_object_route_continuity.ts`
2. `packages/frontend-shell-core/src/routing/return_target_resolver.ts`
3. `packages/frontend-shell-core/src/routing/browser_back_contract.ts`
4. `packages/shared-ui/src/continuity/ReturnPathRibbon.tsx`
5. `packages/shared-ui/src/continuity/FocusReturnAnchor.tsx`
6. `apps/operator-web/src/routes/calm/continuity_adapter.ts`
7. `tests/unit/frontend-shell-core/same_object_route_continuity.spec.ts`
8. `tests/playwright/frontend/low_noise_route_continuity_back_return.spec.ts`
9. `docs/frontend/same_object_route_continuity_and_back_return.md`

If the repository already has equivalent ADR-approved files, use those paths and record the mapping. Do not duplicate package responsibilities just to match this prompt literally.

Required design and UX specification:
- Shared visual baseline to enforce in addition to task-specific UI: near-stone background `#F7F5F1`, white primary cards, mist secondary planes, charcoal ink, muted slate metadata, one shell accent at a time, `Inter`/system typography with mono refs, 18–20px radii, quiet borders, soft shadow ceiling, 8px spacing grid, 140–180ms causal transitions only, and reduced-motion parity. Use diagrams only when they explain a contract, lineage, focus order, stream sequence, upload state, lane separation, or artifact current/history posture.
- The UI must feel bespoke, calm, and premium. It must not resemble a template dashboard, generic AI product, metric wall, or decorative “sci-fi” control panel.
- All visible state must be text-plus-shape-plus-placement; color alone is never sufficient.
- Use Playwright during development to exercise the component/page in a real browser before considering the implementation complete.
- Add a restrained “Return Path Ribbon” above or inside the context bar: compact chips for current object, active module, parent source, and return anchor. It must look like routing metadata, not breadcrumbs-as-marketing. Use a 2px operator-blue continuity rule and mono object refs only where needed.
- On module switches, back/return, and deep-link restoration, animate only the changed support module with 140–180ms opacity/translate; preserve the context bar and decision summary. Reduced motion removes displacement but keeps the same labels and focus movement.

Execution requirements:
- read continuity and focus restoration payloads from the mounted frame; do not infer return targets from pathname segments alone
- create a route-key guard that checks shell family, object anchor, parent context, focus anchor, visibility/cache partition, stability guard, and masking posture before restoring local view state
- bind browser `Back`, explicit return controls, module deep links, artifact preview return, and stale rebase return through the same resolver
- preserve active detail module, scroll/focus anchor, and draft/file-picker safety where lawful; invalidate with explicit copy when object or access binding changed
- keep same-object navigation inside the SPA shell; no full document reload or alternate layout frame

Functional, architectural, and edge-case gaps to close:
- close the gap where browser back can reopen a generic list instead of the exact parent focus anchor
- close the gap where module query params become local state not protected by route stability
- close the gap where artifact or browser handoff return loses the invoking object and focus
- close the gap where narrow-screen restack creates a mobile-only return path

Mandatory edge cases:
- deep link names a valid object and missing focus anchor; restore nearest lawful ancestor with explicit disposition
- object was rebased, merged, revoked, or hidden after notification open
- back from preview after cache/session/masking drift
- module switch while a composer/file picker/draft is active
- browser back crosses from work inbox into work item and back while row focus remains stable
- reduced-motion and 200 percent zoom navigation

Playwright, accessibility, and validation requirements:
- unit-test return-target resolution dispositions: exact anchor, same-route remap, governed parent return, latest lawful object, and explicit invalidation
- Playwright-test deep link restore, browser back, explicit return chip, module query switch, preview handoff return, stale rebase return, revoked object fallback, narrow-screen return, and focus restoration
- assert no full-page reload by tracking shell root identity and stable landmarks
- Use Playwright locator-first tests, semantic roles, stable domain `data-testid` anchors, auto-waiting/actionability, and trace capture on failure.
- Include keyboard-only and reduced-motion coverage for every browser-visible behavior touched by this task.
- Include responsive coverage for desktop, tablet, and narrow/mobile where the task renders a page or route.
- Assert that no test stores regulated fixture content in screenshots, traces, console logs, or snapshots; use redacted deterministic data.
- Run `python3 Algorithm/scripts/validate_contracts.py --self-test`.
- Run `python3 Algorithm/tools/forensic_contract_guard.py`.

Security, privacy, and performance requirements:
- Do not log or persist regulated free text, uploaded bytes, raw authority tokens, hidden internal notes, masked values, screenshots of regulated content, keystroke streams, or DOM snapshots of regulated surfaces.
- Keep client code read/command separated: local UI state may describe focus, route, visibility, staged upload/draft posture, and pending receipt posture, but must not invent legal truth.
- Prefer deterministic pure adapters/builders around contract payloads so tests can exercise edge cases without live services.
- Fail closed on contract mismatch, stale guard drift, access/masking/session drift, visibility partition drift, cache isolation drift, route/context drift, or missing required semantic anchors.
- Avoid broad subscriptions, unnecessary remounts, duplicated announcements, fixed sleeps, and layout churn that can reorder focus or recast a secondary support region as primary.

Definition of done:
- same-object navigation preserves shell family, object, module, focus, and lawful parent return
- invalid return targets fail closed with clear recovery copy
- Playwright proves browser back/return behavior across desktop and narrow layouts
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, previously written `pc_0229` through `pc_0249`, and the task-specific frontend/portal/collaboration gap patterns.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
