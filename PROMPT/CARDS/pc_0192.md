---
id: pc_0192
slug: phase_04_parallel_wave_02_track_backend_governance_004_implement_authority_link_inventory_item_projection_and_health_rollups
mode: parallel
phase: "04"
wave: "02"
track: "backend_governance"
task_number: "004"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_governance_004
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0192

## Task Summary

implement authority link inventory item projection and health rollups.

## Metadata

- ID: `pc_0192`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend governance`
- Task Number: `004`
- Source Slug: `phase_04_parallel_wave_02_track_backend_governance_004_implement_authority_link_inventory_item_projection_and_health_rollups`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0191](./pc_0191.md) `phase_04_parallel_wave_02_track_backend_governance_003_implement_governance_access_simulation_projection_and_basis_contract_hash_reuse_rules`
- Next: [pc_0193](./pc_0193.md) `phase_04_parallel_wave_02_track_backend_governance_005_implement_retention_governance_frame_projection_legal_hold_register_and_erasure_queues`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0190_to_0197.md](../shared_operating_contract_0190_to_0197.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_governance_004_implement_authority_link_inventory_item_projection_and_health_rollups`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0191` if they exist, especially `pc_0086` through `pc_0093`, `pc_0134` through `pc_0141`, and `pc_0191`.

Mission:
Implement the authoritative `AuthorityLinkInventoryItem` projector and the supporting health-rollup services for `/v1/governance/tenants/{tenant_id}/authority-links`.
This is not a raw connector credentials page and not a generic “status + last checked” list.
It must keep authority-link lifecycle, delegation coverage, token/client binding, provider environment, validation history, affected operations, and preflight posture distinct and machine-checkable while preserving the guided handshake stepper that makes link / relink lawful and reviewable.

Source algorithm grounding you must honor:
- `admin_governance_console_architecture.md`, especially section `4.4 /governance/authority-links`, the primary layout, fixed detail-module order, guided handshake rules, and unlink blast-radius rules.
- `data_model.md`, especially `AuthorityLinkInventoryItem`, `authority_link_workspace`, `guided_handshake_stepper`, `binding_health_timeline`, `affected_operation_list`, and `preflight_checklist`.
- `actor_and_authority_model.md`, `authority_interaction_protocol.md`, and `authority_truth_and_internal_projection_separation_contract.md`, especially authority-edge layering, delegation coverage, and authority-of-record separation.
- `connector_delegation_contract.md` where it influences delegation freshness and token/client binding posture.
- `northbound_api_and_session_contract.md`, especially `GET /v1/governance/tenants/{tenant_id}/authority-links`.
- `schemas/authority_link_inventory_item.schema.json`, `schemas/governance_interaction_layer.schema.json`, and `schemas/externalization_governance_contract.schema.json`.
- `scripts/validate_contracts.py`, especially `authority_link_inventory_item_surface_issue_and_handshake_alignment` and `authority_link_inventory_item_preflight_and_operation_alignment`.
- `PATCH_RESOLUTION_INDEX.md`, especially authority-link empty-field hardening and the forensic guard assertions.
- `test_vectors.md`, especially the externalization contract expectations and authority-layer boundary preservation.

Deliverables to create:
1. `packages/backend-governance/src/projectors/build_authority_link_inventory_item.ts`
2. `packages/backend-governance/src/services/derive_authority_link_health_rollup.ts`
3. `packages/backend-governance/src/services/build_guided_handshake_stepper.ts`
4. `packages/backend-governance/src/services/build_binding_health_timeline.ts`
5. `packages/backend-governance/src/services/build_affected_operation_list.ts`
6. `packages/backend-governance/src/services/build_preflight_checklist.ts`
7. `packages/backend-governance/src/tests/authority_link_inventory_projection.spec.ts`
8. `packages/backend-governance/src/tests/authority_link_preflight_alignment.spec.ts`
9. `docs/governance/authority_link_inventory_and_health_rollups.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/governance/authority-link-lab.tsx`

You MUST patch any existing authority-link inventory route so it consumes this projector instead of flattening link health into one generic status field or exposing raw handshake internals directly.

Required design decisions you must make explicitly:
- define how `binding_health`, `delegation_state`, and `token_client_binding_state` are derived independently and what durable records each one trusts
- define how `prominent_issue_ref_or_null` is selected when more than one issue family is active
- define how expiry risk bands are computed and how `expires_at` interacts with `next_validation_due_at_or_null`
- define how handshake attempts are summarized into `handshake_history.latest_attempt_state` and `latest_failure_ref_or_null`
- define how the preflight checklist maps to blocked operations so `primary_blocked_operation_ref_or_null` is not guesswork
- define how the read model binds `externalization_governance_contract{...}` to the selected authority link, active filtered slice, and browser handoff posture without leaking secrets

Execution requirements:
- publish a schema-valid `AuthorityLinkInventoryItem`
- preserve the fixed workspace reading order exactly: `INVENTORY_RAIL -> WORKSPACE_CANVAS -> AUDIT_SIDECAR`
- preserve the fixed detail-module order exactly:
  `AuthorityLinkIdentityCard -> BindingHealthTimeline -> HandshakeHistory -> AffectedOperationList -> PreflightChecklist`
- preserve the fixed handshake step order exactly:
  `SELECT_AUTHORITY -> CONFIRM_CLIENT_SCOPE -> RUN_PREFLIGHT_CHECKS -> AUTHORISE_EXTERNAL_HANDOFF -> VALIDATE_BINDING`
- keep `credential_capture_mode = GUIDED_HANDSHAKE_ONLY`
- keep token/client mismatch, missing delegation, environment drift, and lifecycle failure as first-class distinct states in both rollup and detail views
- keep `affected_operation_list.section_order = PREFLIGHT -> SUBMISSION -> RECONCILIATION -> AMENDMENT`
- keep preflight blockers explicit and aligned to `preflight_blocking_check_refs[]` and the primary blocked operation
- preserve one selected link, one focus anchor, one current handshake step, and one promoted support surface through reconnect, stale-view rebase, and responsive compaction
- keep export / handoff eligibility bound to the active slice through `externalization_governance_contract{...}`

Gap handling you must close while implementing:
- close any gap where lifecycle, delegation, and token-client mismatch collapse into one generic health pill
- close any gap where affected operations are not directly traceable to preflight blockers
- close any gap where the route can degrade into a raw credential form or detached audit log
- close any gap where browser handoff or external validation is not bound to the same selected link and filtered slice
- close any gap where selected link or handshake-step continuity is lost during compaction or reconnect

Mandatory edge cases:
- `primary_blocked_operation_ref_or_null` must remain null when there is no blocked operation and must point into the affected-operation lists when there is one
- `prominent_issue_ref_or_null` must stay null only when there is no material issue to promote
- `preflight_blocking_check_refs[]` must be a subset of the checklist’s `checks[]`
- `binding_health_timeline.promoted_issue_ref_or_null` must align with the rollup’s promoted issue when one exists
- `latest_failure_ref_or_null` must stay null unless the latest handshake attempt actually failed
- no authority-link item may expose secrets, raw tokens, or browser-unsafe credential payloads in the read model

Playwright and contract-test requirements:
- add direct tests for distinct lifecycle / delegation / token-binding states, workspace alignment, handshake-step order, preflight-check / operation mapping, and externalization binding
- if an authority-links read route exists, add Playwright `APIRequestContext` tests for JSON shape, active-filter continuity, selected-link continuity, and externalization-governance publication
- if you add the optional lab, add Playwright browser tests that verify guided-step progression visuals, issue promotion, timeline continuity, and affected-operation grouping using semantic locators only
- capture traces or serialized read-model payloads on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a quiet inventory/detail/audit layout with a narrow list rail, one dominant detail canvas, and one subdued sidecar
- max width `1440px`; palette `#F7F5F1 / #FFFFFF / #F1F3F0 / #171717 / #667085 / #0F766E / #1D4ED8 / #B7791F / #C2410C`
- show the guided handshake as a precise five-step rail with one active step and compact completed-state markers
- render binding health as a timeline strip with typed events and one promoted issue callout, not as a traffic-light summary
- render affected operations as four bounded groups in the canonical order; use concise counts plus one blocked operation highlight
- any diagram should be a slim handshake ladder or validation timeline only; no doughnuts, no generic dashboard metrics

Backend, performance, and security requirements:
- keep the projector strictly read-side and secret-safe
- derive rollups from durable authority, delegation, and validation records rather than browser cache or latest event text
- keep selected-link and filter continuity explicit so same-shell refresh does not remount a different authority link
- index link-health and affected-operation derivation so list reads remain cheap under tenant scale
- fail closed on ambiguous client scope, ambiguous environment, or missing authority binding truth

Validation rules:
- validate every emitted `AuthorityLinkInventoryItem` and nested externalization contract against schema
- add regression coverage for the named validator drifts and for distinction between lifecycle, delegation, and token/client binding states
- keep this task limited to authority-link read / projection semantics; do not swallow later mutation command handlers

Definition of done:
- the repo has one authoritative authority-link inventory projector with explicit guided handshake semantics, health rollups, and operation impact truth
- authority-link health can no longer be flattened into generic connector status
- browser or native governance surfaces can consume the same read model and stay same-object, same-link, and same-slice under reconnect and compaction
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
