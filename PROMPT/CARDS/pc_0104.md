---
id: pc_0104
slug: phase_03_parallel_wave_01_track_backend_manifest_008_implement_update_manifest_preseal_context_and_scope_execution_binding_services
mode: parallel
phase: "03"
wave: "01"
track: "backend_manifest"
task_number: "008"
block_key: phase_03_parallel_wave_01
original_key: phase_03_parallel_wave_01_track_backend_manifest_008
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0104

## Task Summary

implement update manifest preseal context and scope execution binding services.

## Metadata

- ID: `pc_0104`
- Mode: `parallel`
- Phase: `03`
- Wave: `01`
- Track: `backend manifest`
- Task Number: `008`
- Source Slug: `phase_03_parallel_wave_01_track_backend_manifest_008_implement_update_manifest_preseal_context_and_scope_execution_binding_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0103](./pc_0103.md) `phase_03_parallel_wave_01_track_backend_manifest_007_implement_begin_manifest_begin_child_manifest_and_manifest_start_claim_protocol`
- Next: [pc_0105](./pc_0105.md) `phase_03_parallel_wave_01_track_backend_manifest_009_implement_build_preseal_gate_evaluation_and_seal_manifest_services`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.

## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0102_to_0109.md](../shared_operating_contract_0102_to_0109.md)
```text
Execute roadmap task `phase_03_parallel_wave_01_track_backend_manifest_008_implement_update_manifest_preseal_context_and_scope_execution_binding_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0103` if they exist, especially `pc_0010`, `pc_0011`, `pc_0016`, `pc_0023`, `pc_0028`, `pc_0061`, `pc_0064`, `pc_0065`, `pc_0074`, `pc_0086` through `pc_0093`, and `pc_0097` through `pc_0103`.

Mission:
Implement the pre-seal context update path that freezes scope semantics, access semantics, config/input inheritance mirrors, and worker-facing frozen execution binding before seal.
This task must ensure the manifest's pre-start mutable envelope stays coherent while it is still mutable, and then becomes a stable authoritative packet for workers and later replay/recovery logic.
It must reject partial mirror updates and any attempt to rewrite a sealed manifest's frozen envelope.

Source algorithm grounding you must honor:
- `modules.md`, especially `VALIDATE_RUNTIME_SCOPE_BINDING(...)`, `MATERIALIZE_SCOPE_EXECUTION_BINDING(...)`, `ENFORCE_ACCESS_SCOPE_AND_MASKING(...)`, `UPDATE_MANIFEST_PRESEAL_CONTEXT(...)`, and `VALIDATE_MANIFEST_LINEAGE_PROJECTION(...)`.
- `manifest_and_config_freeze_contract.md`, especially scope execution binding, access-binding hash semantics, pre-seal mutation boundaries, atomic frozen envelope publication, and exact mapping from inheritance mode to config resolution basis.
- `config_freeze_inheritance_and_consumption_contract.md`, especially direct resolution versus exact reuse semantics and worker consumption of frozen config.
- `actor_and_authority_model.md` and `authority_truth_and_internal_projection_separation_contract.md`, especially how executable scope, masking, approvals, and authn level bind into one access decision.
- `manifest_branch_selection_contract.md` and `manifest_lineage_explorer_and_reuse_decision_tracer_contract.md`, especially mirrored lineage and branch metadata.
- `data_model.md`, especially `RunManifest.scope_execution_binding`, `RunManifest.frozen_execution_binding`, `RunManifest.continuation_set`, `RunManifest.access_decision`, `ConfigFreeze`, `InputFreeze`, and `hash_set`.
- Existing schemas including `run_manifest.schema.json`, `config_freeze.schema.json`, `input_freeze.schema.json`, `scope_execution_binding.schema.json` if present, and the branch / lineage related schemas.

Deliverables to create:
1. `packages/backend-manifest/src/services/update_manifest_preseal_context.ts`.
2. `packages/backend-manifest/src/services/materialize_scope_execution_binding.ts`.
3. `packages/backend-manifest/src/services/validate_runtime_scope_binding.ts`.
4. `packages/backend-manifest/src/services/enforce_access_scope_and_masking.ts`.
5. `packages/backend-manifest/src/services/build_frozen_execution_binding.ts`.
6. `packages/backend-manifest/src/services/validate_manifest_lineage_projection.ts`.
7. `packages/backend-manifest/src/services/sync_manifest_preseal_mirrors.ts`.
8. `packages/backend-manifest/src/types/preseal_context_patch.ts`.
9. `docs/manifest/preseal_context_and_scope_binding.md`.
10. `tests/unit/backend_manifest/preseal_context_and_scope_binding.spec.ts`.
11. `tests/integration/backend_manifest/frozen_execution_binding_atomicity.spec.ts`.

Required design decisions you must make explicit:
- Define the authoritative structure of `scope_execution_binding{...}` and how raw requested scope, executable scope, partition scope refs, masking rules, approval requirements, and authn level are represented without ambiguity.
- Define the exact shape and publication timing of `frozen_execution_binding{...}`.
  Explain which fields are mirrored for worker convenience and why those mirrors remain subordinate to canonical sources.
- Define the atomic update boundary across `ConfigFreeze`, `InputFreeze`, `hash_set.execution_basis_hash`, `continuation_set`, lineage mirrors, `access_decision`, and `frozen_execution_binding`.
- Define how `access_binding_hash` is consumed and preserved rather than re-derived inconsistently by downstream code.
- Define how continuation patches are allowed pre-seal without creating partial updates or mirror drift.
- Define how the code fails closed if an attempted patch conflicts with the manifest's existing freeze basis or sealed state.

Execution steps:
1. Implement `materialize_scope_execution_binding.ts` so raw requested scope and authoritative access decision yield one canonical executable-scope packet with deterministic ordering.
2. Implement `validate_runtime_scope_binding.ts` and `enforce_access_scope_and_masking.ts` so the runtime scope cannot exceed authorized executable scope and masking/approval semantics stay bound to the same decision packet.
3. Implement `build_frozen_execution_binding.ts` so worker-facing frozen execution state is assembled from canonical sources, not reconstructed ad hoc.
4. Implement `validate_manifest_lineage_projection.ts` and `sync_manifest_preseal_mirrors.ts` so top-level lineage mirrors, continuation metadata, and frozen execution mirrors update byte-identically or fail closed.
5. Implement `update_manifest_preseal_context.ts` so pre-seal mutable fields can be patched atomically while the manifest is pre-start and unsealed, but never once sealed.
6. Add tests for raw-versus-executable scope narrowing, masking-only outcomes, approval requirements, continuation patching, mirror drift rejection, and sealed-manifest rewrite rejection.

Gap handling you must perform:
- Close the gap where `scope_execution_binding{...}` exists in schemas and prose but not as one authoritative executable service.
- Close the gap where workers might derive execution semantics from raw request scope instead of the frozen binding.
- Close the gap where continuation and lineage mirrors could be updated independently and drift.
- Close the gap where exact config/input inheritance mode mapping could be lost or silently normalized during pre-seal updates.
- Close the gap where a sealed manifest could still be rewritten through a generic repository patch path.

Mandatory edge cases:
- requested scope broader than authorized executable scope must produce the exact authorized posture required by the access decision, not a silent broadened runtime scope.
- `ALLOW_MASKED` may alter projection or masking context but must not invent executable scope beyond the authorized binding.
- `REQUIRE_STEP_UP`, `REQUIRE_APPROVAL`, and `DENY` remain pre-start boundary outcomes; pre-seal update must not treat them as runnable states.
- `execution_basis_hash` must be persisted no later than `FROZEN` and carried atomically into the frozen execution binding.
- `ConfigFreeze.config_resolution_basis` and `continuation_set.config_inheritance_mode` must remain exactly aligned.
- `continuation_set.input_inheritance_mode` must align with input freeze posture and exact replay/recovery semantics.
- top-level lineage mirrors diverge from nested continuation or frozen execution mirrors and the update must fail closed rather than normalize.
- once `lifecycle_state` is `SEALED`, any attempt to rewrite config/input/scope/access/preseal lineage fields must be rejected.

Backend, performance, and security requirements:
- Keep scope and access computations deterministic and pure where possible.
- Never log raw authority credentials, raw policy payloads, or sensitive masking material.
- Use one atomic persistence boundary for the full pre-seal context patch rather than multiple best-effort updates.
- Do not re-read live config or authority state during a patch if the data should already be frozen by prior steps.
- Keep ordering canonical and index-friendly for policy hash, access-binding hash, and execution-basis lookups.

Validation rules:
- Unit-test scope binding materialization, masking/approval handling, inheritance mode mapping, and mirror-parity checks.
- Integration-test atomic patch application and fail-closed rejection of partial or conflicting updates.
- Validate persisted manifest fragments against the imported schemas.
- Prove that a sealed manifest cannot be rewritten through the pre-seal update path.
- Prove that `frozen_execution_binding{...}` and top-level mirrors remain subordinate to the authoritative underlying control objects.

Definition of done:
- The repo has one authoritative path for turning request scope, access truth, and frozen config/input state into a coherent pre-seal manifest envelope.
- Workers and later replay/recovery flows can consume a frozen execution binding instead of reconstructing semantics from scattered fields.
- Mirror drift and sealed-envelope rewrites are no longer latent risks; they are enforced fail-closed conditions.
```

## Verification

- Prompt authored from direct analysis of the attached Taxat algorithm corpus and aligned to the specific logical contracts most relevant to this task.
- Instructions explicitly preserve card status neutrality: they tell the implementation agent how to execute the task, what to verify, and how to record typed gaps without prematurely claiming completion inside the prompt itself.
