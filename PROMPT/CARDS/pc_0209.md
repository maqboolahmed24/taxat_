---
id: pc_0209
slug: phase_04_parallel_wave_02_track_backend_retention_security_observability_001_implement_retention_tag_artifact_retention_and_expiry_limitation_propagation_services
mode: parallel
phase: "04"
wave: "02"
track: "backend_retention_security_observability"
task_number: "001"
block_key: phase_04_parallel_wave_02
original_key: phase_04_parallel_wave_02_track_backend_retention_security_observability_001
checklist_path: ../Checklist.md
agent_path: ../AGENT.md
---
# pc_0209

## Task Summary

001 implement retention tag artifact retention and expiry limitation propagation services.

## Metadata

- ID: `pc_0209`
- Mode: `parallel`
- Phase: `04`
- Wave: `02`
- Track: `backend retention security observability`
- Task Number: `001`
- Source Slug: `phase_04_parallel_wave_02_track_backend_retention_security_observability_001_implement_retention_tag_artifact_retention_and_expiry_limitation_propagation_services`
- Checklist: [../Checklist.md](../Checklist.md)
- Protocol: [../AGENT.md](../AGENT.md)

## Neighbour Context

- Previous: [pc_0208](./pc_0208.md) `phase_04_parallel_wave_02_track_backend_recovery_010_implement_nightly_portfolio_what_if_simulation_and_release_admissibility_counterfactuals`
- Next: [pc_0210](./pc_0210.md) `phase_04_parallel_wave_02_track_backend_retention_security_observability_002_implement_erasure_proof_legal_hold_and_privacy_minimization_workflows`

## Agent Instructions

1. Read [../AGENT.md](../AGENT.md).
2. Claim this task in [../Checklist.md](../Checklist.md) by changing `[ ]` to `[-]`.
3. Re-read the checklist to confirm the claim still holds.
4. Review adjacent cards if dependency context is needed.
5. Execute the task and capture working notes below.
6. Mark the checklist item `[X]` only after verification.

## Inputs

- Repository source files and current project context.
- The canonical task slug and neighbouring cards for dependency context.

## Expected Outputs

- Implement the work implied by this task.
- Record assumptions, constraints, and verification evidence in this card.



## Working Notes

### Autonomous Coding Prompt
Use the shared operating contract first:
[../shared_operating_contract_0206_to_0213.md](../shared_operating_contract_0206_to_0213.md)
```text
Execute roadmap task `phase_04_parallel_wave_02_track_backend_retention_security_observability_001_implement_retention_tag_artifact_retention_and_expiry_limitation_propagation_services`.

Inherit the shared operating contract and consume outputs from cards `pc_0001` through `pc_0208` if they exist, especially `pc_0046` through `pc_0053`, `pc_0069`, `pc_0152` through `pc_0156`, `pc_0166`, and `pc_0199` through `pc_0200`.

Mission:
Implement the canonical retention-tag and artifact-retention services that derive expiry, legal-hold, erasure-eligibility, limitation behavior, and privacy survivability from one machine-readable source of truth and propagate those outcomes into every evidence-bearing artifact and related failure/remediation object.
This task is not allowed to restate retention semantics ad hoc inside individual modules.
`RetentionTag` and `ArtifactRetention` must become the typed privacy/expiry backbone for authoritative artifacts, projections, and retained lineage.

Source algorithm grounding you must honor:
- `retention_and_privacy.md`, especially retention classes, expiry derivation, limitation semantics, survivability ratios, and proof-preserving retention posture.
- `modules.md`, especially `APPLY_RETENTION_POLICY(...)` and the failure/remediation helpers that must stay bound to the same retention object.
- `data_model.md`, especially `RetentionTag` and `ArtifactRetention`.
- `schemas/retention_tag.schema.json` and `schemas/artifact_retention.schema.json`.
- `scripts/validate_contracts.py`, especially `retention_tag_chronology_and_blocking_basis_posture` and `artifact_retention_state_reverse_and_expiry_chronology`.
- `PATCH_RESOLUTION_INDEX.md` and `test_vectors.md`, especially expiry/hold/limitation drift cases.

Deliverables to create:
1. `packages/backend-retention/src/models/retention_tag.ts`
2. `packages/backend-retention/src/models/artifact_retention.ts`
3. `packages/backend-retention/src/services/derive_retention_tag.ts`
4. `packages/backend-retention/src/services/apply_retention_policy.ts`
5. `packages/backend-retention/src/services/propagate_limitation_and_expiry.ts`
6. `packages/backend-retention/src/services/bind_retention_to_error_and_remediation.ts`
7. `packages/backend-retention/src/tests/retention_tag.spec.ts`
8. `packages/backend-retention/src/tests/artifact_retention.spec.ts`
9. `docs/retention/retention_tag_artifact_retention_and_expiry_limitation_propagation_services.md`
10. Optional but recommended: `apps/admin-console-web/src/routes/debug/retention/retention-lifecycle-lab.tsx`

If `packages/backend-retention` does not exist, create it deliberately and record `ASSUMPTION_BACKEND_RETENTION_PACKAGE_CREATED` in the notes or docs you add.

Required design decisions you must make explicitly:
- define the canonical anchor-event mapping from artifact/object classes to `RetentionTag.anchor_event`
- define how `minimum_expiry_at`, `policy_expiry_at`, and `effective_expiry_at` are derived and how chronology conflicts fail closed
- define how `legal_hold_state`, `legal_hold_ref`, `erasure_eligibility`, `pseudonymisation_mode`, `limitation_behavior`, and basis refs stay synchronized between `RetentionTag` and `ArtifactRetention`
- define how quantitative survivability/projection-fidelity semantics are derived and surfaced without ambiguity
- define how retained failure/remediation/investigation objects inherit the same retention object rather than restating a weaker policy snapshot

Execution requirements:
- emit one canonical `RetentionTag` for every authoritative artifact or evidence-bearing object that falls under retention/privacy law
- derive and persist `ArtifactRetention` as the live lifecycle control object bound to that tag
- preserve explicit fields for expiry, legal hold, erasure eligibility, erasure reasons, pseudonymisation mode, limitation behavior, limitation reasons, retention-basis ref, proof-preservation-basis ref, and authority ambiguity ref
- propagate retention refs and class into error, remediation, compensation, accepted-risk, and investigation artifacts that retain or discuss the same regulated data
- enforce the information-ratio and survivability posture described by the algorithm; silent ambiguity is a structural defect and must fail closed
- keep pending hold/erasure states, checkpoint refs, and workflow refs lawful only in the states where the schema and prose permit them
- preserve completed-erasure proof linkage when lifecycle reaches a terminal erasure-compatible state

Gap handling you must close while implementing:
- close any gap where modules restate a weaker retention rule locally instead of binding to canonical `RetentionTag` / `ArtifactRetention`
- close any gap where expiry propagation ignores proof-preservation basis or legal-hold posture
- close any gap where limitation behavior is implicit in view code rather than machine-readable on the retention object
- close any gap where retained error/remediation/investigation artifacts lose the exact `artifact_retention_ref` and `retention_class` that justify their survivorship
- close any gap where survivability and projection fidelity can become non-deterministic or silently ambiguous

Mandatory edge cases:
- chronology drift between anchor event, minimum expiry, policy expiry, and effective expiry must fail closed
- legal hold active must block erasure eligibility even when minimum/policy expiry would otherwise allow erasure
- proof-preservation basis must survive even when the main artifact is otherwise eligible for erasure or pseudonymisation
- limitation-only states must remain distinct from pseudonymised and erased states
- silent ambiguity in limitation semantics must be treated as invalid state
- retained failure/remediation artifacts must not survive without an explicit retained basis and matching retention refs

Playwright and contract-test requirements:
- add deterministic tests for retention-tag derivation, expiry chronology, legal-hold blocking, limitation propagation, survivability/projection-fidelity semantics, and binding to retained error/remediation objects
- if you expose or patch a retention-read route, add Playwright `APIRequestContext` coverage for chronology failures, hold blocking, and limitation-state publication
- if you add the optional lab, add Playwright browser tests that verify the retention clock lattice, limitation matrix, expiry lineage ledger, keyboard navigation, and reduced-motion-safe expansion using semantic locators only
- capture payload traces on failure

UI / route-consumption requirements if a browser lab or real route patch is touched:
- use a three-zone layout: retention-basis ribbon, lifecycle/limitation matrix canvas, narrow expiry/proof sidecar
- max width `1440px`; background `#F7F5F1`; primary surface `#FFFFFF`; support surface `#F1F3F0`; text `#171717`; secondary text `#667085`; accent `#0F766E`; info accent `#1D4ED8`; caution `#B7791F`; danger `#C2410C`
- visualize chronology and limitation semantics as ordered ledgers or matrices, not as generic KPI charts
- surface survivability and projection fidelity as exact typed values with explanatory labels, not decorative gauges
- animate only opacity/translate/height with `140ms` to `180ms`; respect reduced motion

Backend, performance, and security requirements:
- keep retention derivation deterministic, schema-first, and side-effect aware
- persist retention objects before downstream modules publish retained error/remediation/investigation objects that depend on them
- prohibit retention drift from view-layer masking or exporter-local rules
- keep privacy-sensitive proof and basis refs in governed storage only; do not leak them through logs or opaque ad hoc debugging blobs

Validation rules:
- validate every `RetentionTag` and `ArtifactRetention` against schema before persistence or response emission
- update fixtures/builders so retention validator expectations continue to pass
- rerun `python3 Algorithm/scripts/validate_contracts.py --self-test` and `python3 Algorithm/tools/forensic_contract_guard.py`

Definition of done:
- the codebase has one canonical backend path for deriving retention tags, one canonical path for maintaining artifact-retention lifecycle, and explicit lineage binding into retained failure/remediation objects
- expiry, limitation, and privacy survivability semantics are durable and test-covered
- optional labs, if added, remain read-only projections of persisted retention truth
```

## Verification

- Prompt authored from direct analysis of the Taxat algorithm archive, relevant schemas, validator invariants, and tranche-specific gap patterns.
- Instructions preserve card-status neutrality and do not claim the task is already implemented.
