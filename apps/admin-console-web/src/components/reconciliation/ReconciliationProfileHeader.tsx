export const reconciliationProfileHeader = {
  test_id: "reconciliation-profile-header",
  responsibility:
    "Expose the selected provider environment, operation family, profile, window, and durable source policy.",
} as const;
