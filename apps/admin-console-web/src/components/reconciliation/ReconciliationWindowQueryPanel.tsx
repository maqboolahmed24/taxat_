export const reconciliationWindowQueryPanel = {
  filter_test_id: "reconciliation-window-filter",
  test_id: "reconciliation-query-panel",
  responsibility:
    "Apply read-only profile, environment, operation family, and window filters against persisted snapshots.",
} as const;
