export const provisioningWorkbenchBootstrap = {
  workspace: "apps/provisioning-workbench",
  note: "This bootstrap utility app preserves a home for provisioning/readiness tools without redefining the four edge-product apps chosen in phase 00.",
} as const;
