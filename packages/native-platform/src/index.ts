export const nativePlatformBoundary = {
  packageId: "native-platform",
  note: "Swift-native shared bindings and scene/runtime bridges remain separate from browser UI code and start here.",
} as const;
