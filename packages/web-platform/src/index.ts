export const webPlatformBoundary = {
  packageId: "web-platform",
  note: "Shared browser route/runtime semantics, selector grammar, and internal atlas composition live here before app-specific shells layer on top.",
} as const;
